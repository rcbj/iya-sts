'use strict';
//
// File: admin.ts
//
// ---------------------------------------------------------------------------
// The admin console. It began as five pages over the state admin_stats.js
// holds, and one more — /admin/groups — over the embedded LDAP directory next
// door; the list below is those first pages, and `SECTIONS` further down is
// the whole of it now.
//
//   GET  /admin           what the console is, and what it can do to this service
//   GET  /admin/metrics   every call, every artifact, and both kinds of session
//   GET  /admin/users     everyone this service has authenticated; with ?user= it is
//                         one of them, their sessions, and what was issued on each
//   GET  /admin/groups    every group in the embedded LDAP directory; with ?group= it
//                         is one of them, every attribute it has, and everybody in it
//   GET  /admin/tokens    what was issued — every JWT, every SAML assertion and every
//                         Kerberos ticket, filtered and paged — and the buttons that
//                         invalidate the ones that can be
//   POST /admin/tokens    revoke / restore, one token or a whole class of them
//   GET  /admin/audit     what happened here, in order — every authentication,
//                         session, directory operation, console interaction,
//                         management API call and protocol endpoint call, as
//                         rows rather than as counters, filtered and paged
//   GET  /admin/claims    the custom claims every new token will carry
//   POST /admin/claims    add, remove, clear, or replace a whole set
//   GET  /admin/saml-attributes   the same two halves for the two SAML sets
//   POST /admin/saml-attributes   the same seven actions, on those two sets
//
// Every GET also answers `?format=json`, and every POST answers JSON when it
// was sent JSON. That is not decoration: this repository's own
// `tests/vendored/admin_api.js` and its siblings drive the console over HTTP
// with no browser, and a console reachable only by clicking is a console no
// test can assert against.
//
// **This module renders; it decides nothing.** All the state, all the caps and
// all the rules about what a claim may be called live in admin_stats.js, so a
// test can exercise them without going near an HTML page, and so this file
// stays the one place the markup is. It reads the browser sign-on session
// store, which `../authn/authn.ts` owns, so the metrics page can report real
// sign-on sessions beside the ones derived from what was issued.
//
// **It must come AFTER oauth2.js in `common/protocol_stack.ts`** (rule 5) —
// in the require order and, since #50's R1, in the order of that file's
// `register()` calls as well — and that is a dependency rather than a
// preference: it requires
// `../oauth-oidc/oauth2.ts` for the drift report (see that require below).
// The dependency is one way — oauth2.js knows nothing about this module — so
// it is not a cycle.
//
// ---------------------------------------------------------------------------
// THIS CONSOLE IS PROTECTED NOW, AND THE OLD PARAGRAPH IS KEPT BELOW BECAUSE
// MOST OF IT IS STILL TRUE.
//
// The console gate is UNCONDITIONAL. Every page and every form under /admin
// needs a session of the console's own — got through the OIDC code flow
// against this service's authorization server (`common/oidc_rp.ts`, since
// 2026-09-06) — and one of two roles — Admin Read and Admin Write — held as
// two ordinary groups in the embedded directory of the realm the person
// signed in through (per realm since 2026-09-14, #32; admin-ui/CLAUDE.md 8d).
// The gate is one `app.use('/admin', ...)` further down this file and the
// roles are `./admin_rbac.js`; both have headers of their own.
//
// **IT IS A TURNSTILE AND NOT A LOCK, and that distinction is the same one
// SCIM's authentication carries.** In development mode this service checks no
// password — the username typed at the sign-in screen simply becomes the
// identity — so what the gate proves there is that somebody TYPED a name that
// holds a role (product mode verifies the password; `common/mode.js`). What it
// buys is what a mock is for: a client, or a person, can now be driven through
// 302 to a sign-in screen, 401 with no session, 403 with the wrong role and a
// role model that can be granted and revoked, none of which was reachable here
// before.
//
// **`/admin-api` WAS NOT GATED UNTIL 2026-09-09** and it is now: it takes an
// OAuth 2.0 access token carrying `admin:read` / `admin:write`, and
// `adminApi.authRequired=false` restores the open API
// (`mgmt-api/CLAUDE.md`). With that switch off, the sentence below is true of
// this PORT even though it is not true of this PAGE — which is why it stays
// rather than being deleted:
//
//   anyone who can reach this port can revoke every token this service has
//   issued and add a claim to every token it issues next. That is fine for a
//   mock on a laptop or on a compose network and is not fine on a public
//   address, which is the same thing that was already true of /oauth2/token —
//   it will mint a token for any username asked of it. Do not put this service
//   on a public address.
//
// Every page says which state the gate is in (see gateBanner()),
// because "protected" and "protected, but nobody holds a role so anybody who
// signs in is an administrator" are very different things to be reading a
// console under.
// ---------------------------------------------------------------------------

// ---------------------------------------------------------------------------
// TYPESCRIPT, AS A CLASS (#50, 2026-09-16) — `common/realm_chooser.ts`'s
// shape, and `oauth-oidc/oauth2.ts`'s for a file this size. `AdminConsole`
// takes every module it reads through its constructor as
// `AdminConsoleDeps`, whose names are the ones this file used at its top
// level before, so each method takes what it reads with one destructuring
// line and its body reads as it did. Every function of the old file is a
// method of the same name — with ONE exception each for two names the old
// file declared TWICE (`credentialCell()`, `samlOverrideFieldRow()`): the
// later declaration is the one JavaScript's hoisting gave every caller, so
// it keeps the name. The earlier `samlOverrideFieldRow()` is kept as an
// `unreached…()` method nothing calls; the earlier `credentialCell()` was
// the delegation table's own and is `delegationCredentialCell()` since #70,
// which fixed that table's two calls to reach it.
//
// **THE ROUTES ARE REGISTERED BY `registerRoutes()`**, which holds every
// route and middleware of the old file in the old order — the console gate
// (`app.use('/admin', …)`) and the realm chooser ahead of every page, and
// `/admin/api-explorer`'s CSP relaxation where it was. Until #50's R1 the
// transitional code called it at load, where the LAST of them used to be
// registered rather than the first, because the old file declared tables
// between its routes that some registrations read at load
// (`PROTOCOL_SETTINGS_PAGES`); nothing between the first route and the last
// registers a route or requires a module. **Since R1 (2026-09-16) requiring
// this module registers nothing**: the module exports `registerRoutes(app)`
// and `common/protocol_stack.ts` calls it at 18, straight after the require,
// which is where requiring this module used to register them — so rule 1's
// order, within this file and against every other module, is unchanged.
//
// The stores, the slot variables every `set…()` fills (rule 3e) and the
// tables are still declared at module scope, where they were, so what a slot
// sets is what the pages read.
//
// **SINCE R2 (#50, 2026-09-16) THE COMPOSITION ROOT BUILDS THE INSTANCE**
// and installs it; this module builds none of its own. It exports
// `registerRoutes(app)` for the root to call, and the old names, as FACADES
// that forward to that instance, for every module that requires this one by
// them. What loading this module used to do with its own instance — the
// sidebar's flattening, the setting-homes check, the module constants drawn
// with `note()` and `warn()`, and the read layer's settings block — is a
// list of `WIRE_STEPS`, each beside the declaration it fills, run by
// `AdminConsole.wire()` when the instance is installed. A process without the
// root builds a default instance at load, as loading this module always did.
// ---------------------------------------------------------------------------

import app = require('../common/app');
import helpers = require('../common/helpers');
const { log, xmlEscape, baseUrlOf, parseBody, b64uDecode, userFor,
        // ONE named realm's signing key, for /admin/realms — which lists every
        // realm's `kid` and is therefore the one page here that needs a key
        // belonging to a realm other than the one it is being read in.
        stsKeysFor,
        // The count as a word, for the refusal sentence at the bottom of
        // applicationsAction() — which is built from its own table now rather
        // than typed, because a typed one was short by an action.
        numberWord,
        // The parts of an upload, for the RFC 9728 import's file field: the
        // one place this console needs a FILENAME, which parseBody() does not
        // keep.
        multipartParts } = helpers;
// The error-code registry. A leaf: it records which failure condition a
// response reports, on the response object and never in its bytes.
import errorCodes = require('../common/error_codes');
// RFC 9728 protected resource metadata, for /admin/applications/new's import:
// the realm's authorization servers a document is compared with, the redraw of
// a loaded document after a refused create, and the document the third tab
// leaves. A library that registers nothing.
import resourceMetadata = require('../oauth-oidc/protected_resource_metadata');

// ---------------------------------------------------------------------------
// THE ACTION LAYER (2026-09-12), AND WHY THESE NAMES ARE STILL IN SCOPE HERE.
//
// Thirty-one actions, the tables they dispatch on and the pure helpers they
// share moved to `admin-core/admin_actions.ts` — see that file's header for
// what may be in it and what may not. `mgmt-api/admin_api.ts` requires it too,
// and no longer requires this module for them, which is the whole point: the
// surface a machine drives is no longer downstream of the surface a person
// reads.
//
// **THEY ARE ALIASED BACK RATHER THAN REWRITTEN AT EVERY CALL SITE**, and
// that is a deliberate choice about what a reviewer has to check. This file
// calls these names several hundred times — from the routes, from the views
// that draw the buttons an action dispatches on, and from each other. Turning
// each into `adminActions.x` would have made the move a diff nobody could
// read, in which a behaviour change and a rename look identical. Here the
// move is a deletion and one block, and every remaining line of this file is
// untouched.
// ---------------------------------------------------------------------------
import adminActions = require('../admin-core/admin_actions');

// ---------------------------------------------------------------------------
// AND THE READ HALF (2026-09-12), on the same terms.
//
// Thirty-seven functions that answer a question and reach no markup at all —
// `scimJson()`, `realmsJson()`, `cryptoView()`, the six XACML views and the
// rest — moved to `admin-core/admin_views.ts`. They needed no surgery: they
// were already pure, so they travelled exactly as the actions did.
//
// **WHAT DID NOT GO IS THE POINT OF THE LINE.** Every view that builds HTML
// stayed, because `{ json, inner }` computed in one pass is the strongest
// form of rule 7 there is and splitting it is bespoke work on interleaved
// code. And so did this console's own STRUCTURE — `consoleJson()`,
// `configJson()`, `settingsGroupsFor()`, `protocolSettingsJsonFor()` and
// `configSettingsJson()` — which is pure and still belongs here: a caller
// asking which pages this console has is asking the console about itself.
// ---------------------------------------------------------------------------
import adminViews = require('../admin-core/admin_views');
// Which endpoints each Protocols page lists. It reads every route-registering
// module out of `require.cache` and never loads one, so requiring it here
// moves nothing; see its header and `respond()`.
import protocolEndpoints = require('../admin-core/protocol_endpoints');
const pagingOf = adminViews.pagingOf;
const pagingJson = adminViews.pagingJson;
const pagedRows = adminViews.pagedRows;
const tokensView = adminViews.tokensView;
const sessionProtocolsIn = adminViews.sessionProtocolsIn;
const sessionsView = adminViews.sessionsView;
const DEFAULT_PER_PAGE = adminViews.DEFAULT_PER_PAGE;
const DELEGATION_PER_PAGE = adminViews.DELEGATION_PER_PAGE;
const MAX_ROWS = adminViews.MAX_ROWS;
const auditView = adminViews.auditView;
const errorCodesView = adminViews.errorCodesView;
const usedAssertionsView = adminViews.usedAssertionsView;
const delegationView = adminViews.delegationView;
const clusterSummary = adminViews.clusterSummary;
const permissionGroupsView = adminViews.permissionGroupsView;
const queryOne = adminViews.queryOne;
const chooserMatches = adminViews.chooserMatches;
const claimsRequestPreview = adminViews.claimsRequestPreview;
const claimsRequestJson = adminViews.claimsRequestJson;
const userinfoClaimsJson = adminViews.userinfoClaimsJson;
const signalsJson = adminViews.signalsJson;
const signalsState = adminViews.signalsState;
const ssfJson = adminViews.ssfJson;
const ssfDeadLettersJson = adminViews.ssfDeadLettersJson;
const caepJson = adminViews.caepJson;
const caepSessionsState = adminViews.caepSessionsState;
const caepApplicationsState = adminViews.caepApplicationsState;
const caepSessionsJson = adminViews.caepSessionsJson;
const riscJson = adminViews.riscJson;
const riscAccountsState = adminViews.riscAccountsState;
const riscApplicationsState = adminViews.riscApplicationsState;
const riscAccountsJson = adminViews.riscAccountsJson;
const spiffeListeners = adminViews.spiffeListeners;
const spiffeJson = adminViews.spiffeJson;
const spiffeEntriesJson = adminViews.spiffeEntriesJson;
const spiffeAgentsJson = adminViews.spiffeAgentsJson;
const spiffeSelectorText = adminViews.spiffeSelectorText;
const newUserContainer = adminViews.newUserContainer;
const CREDENTIAL_CHOICES = adminViews.CREDENTIAL_CHOICES;
const knownUserKeys = adminViews.knownUserKeys;
const saml2ServiceProviders = adminViews.saml2ServiceProviders;
const saml2Facts = adminViews.saml2Facts;
const valuesFor = adminViews.valuesFor;
const saml11RelyingParties = adminViews.saml11RelyingParties;
const saml11Facts = adminViews.saml11Facts;
const asDriftRows = adminViews.asDriftRows;
const asTruthRequest = adminViews.asTruthRequest;
const pageParamsOf = adminViews.pageParamsOf;
const NOT_A_VIEW = adminViews.NOT_A_VIEW;
const applicationPermissionsState = adminViews.applicationPermissionsState;
const federationRow = adminViews.federationRow;
const peopleRows = adminViews.peopleRows;
const mergeFactors = adminViews.mergeFactors;
const queryWith = adminViews.queryWith;
const DEFAULT_BLOCKS_PER_PAGE = adminViews.DEFAULT_BLOCKS_PER_PAGE;
const sessionRowsFor = adminViews.sessionRowsFor;
const tokensBySession = adminViews.tokensBySession;
const logoutInventoryFor = adminViews.logoutInventoryFor;
const logoutFamilies = adminViews.logoutFamilies;

const claimSetsJson = adminViews.claimSetsJson;
const claimsJson = adminViews.claimsJson;
const claimsPreviewUser = adminViews.claimsPreviewUser;
const claimsRequestParameter = adminViews.claimsRequestParameter;
const consentView = adminViews.consentView;
const consoleRpSession = adminViews.consoleRpSession;
const cryptoView = adminViews.cryptoView;
const directoryPageJson = adminViews.directoryPageJson;
const gateStateFor = adminViews.gateStateFor;
const keysExport = adminViews.keysExport;
const keysView = adminViews.keysView;
const metricsJson = adminViews.metricsJson;
const permissionsView = adminViews.permissionsView;
const realmJson = adminViews.realmJson;
const realmRootUrl = adminViews.realmRootUrl;
const realmSettingRows = adminViews.realmSettingRows;
const realmsJson = adminViews.realmsJson;
const rolesPreview = adminViews.rolesPreview;
const rolesRegister = adminViews.rolesRegister;
const rolesView = adminViews.rolesView;
const samlAssertionSeconds = adminViews.samlAssertionSeconds;
const samlAssertionsJson = adminViews.samlAssertionsJson;
const samlAttributesJson = adminViews.samlAttributesJson;
const scimJson = adminViews.scimJson;
const scimMappingRow = adminViews.scimMappingRow;
const scimMonitorJson = adminViews.scimMonitorJson;
const signOnSessionRows = adminViews.signOnSessionRows;
const tokenLifetimesJson = adminViews.tokenLifetimesJson;
const tokenSetView = adminViews.tokenSetView;
const vcJson = adminViews.vcJson;
const vcPreviewUser = adminViews.vcPreviewUser;
const vpConfigJson = adminViews.vpConfigJson;
const xacmlDecideView = adminViews.xacmlDecideView;
const xacmlEditorView = adminViews.xacmlEditorView;
const xacmlMonitorView = adminViews.xacmlMonitorView;
const xacmlPepsView = adminViews.xacmlPepsView;
const xacmlPoliciesView = adminViews.xacmlPoliciesView;
const xacmlView = adminViews.xacmlView;

const APPLICATION_ACTIONS = adminActions.APPLICATION_ACTIONS;
const CONSENT_ACTIONS = adminActions.CONSENT_ACTIONS;
const FIELD_PREFIX = adminActions.FIELD_PREFIX;
const FORM_FURNITURE = adminActions.FORM_FURNITURE;
const MFA_ACTIONS = adminActions.MFA_ACTIONS;
const PERMISSION_ACTIONS = adminActions.PERMISSION_ACTIONS;
const ROLE_ACTIONS = adminActions.ROLE_ACTIONS;
const ROLE_MEMBER_KINDS = adminActions.ROLE_MEMBER_KINDS;
const SAML11_RP_KIND = adminActions.SAML11_RP_KIND;
const SAML2_SP_KIND = adminActions.SAML2_SP_KIND;
const SAML_ASSERTION_KEYS = adminActions.SAML_ASSERTION_KEYS;
const SAML_ASSERTION_SETTINGS = adminActions.SAML_ASSERTION_SETTINGS;
const SIGNALS_CONSOLE_ACTIONS = adminActions.SIGNALS_CONSOLE_ACTIONS;
const SPIFFE_AGENT_ACTIONS = adminActions.SPIFFE_AGENT_ACTIONS;
const SPIFFE_ENTRY_ACTIONS = adminActions.SPIFFE_ENTRY_ACTIONS;
const SPIFFE_FIELD_ATTRIBUTES = adminActions.SPIFFE_FIELD_ATTRIBUTES;
const TOKEN_LIFETIME_KEYS = adminActions.TOKEN_LIFETIME_KEYS;
const USERS_ACTIONS = adminActions.USERS_ACTIONS;
const USER_FIELD_PREFIX = adminActions.USER_FIELD_PREFIX;
const applicationFieldsFrom = adminActions.applicationFieldsFrom;
const applicationsAction = adminActions.applicationsAction;
const asAction = adminActions.asAction;
const caepAction = adminActions.caepAction;
const claimsAction = adminActions.claimsAction;
const configAction = adminActions.configAction;
const configKnows = adminActions.configKnows;
const configSettingFor = adminActions.configSettingFor;
const consentAction = adminActions.consentAction;
const federationAction = adminActions.federationAction;
const fieldToAttribute = adminActions.fieldToAttribute;
const groupsAction = adminActions.groupsAction;
const jtiFrom = adminActions.jtiFrom;
const logoutAction = adminActions.logoutAction;
const mfaAction = adminActions.mfaAction;
const noXacml = adminActions.noXacml;
const permissionsAction = adminActions.permissionsAction;
const rbacAction = adminActions.rbacAction;
const realmsAction = adminActions.realmsAction;
const riscAction = adminActions.riscAction;
const roleMemberKindOf = adminActions.roleMemberKindOf;
const rolesAction = adminActions.rolesAction;
const saml11Action = adminActions.saml11Action;
const saml2Action = adminActions.saml2Action;
const samlAssertionRowFor = adminActions.samlAssertionRowFor;
const samlAssertionsAction = adminActions.samlAssertionsAction;
const sessionsAction = adminActions.sessionsAction;
const signalsAction = adminActions.signalsAction;
const spiffeAgentsAction = adminActions.spiffeAgentsAction;
const spiffeCommaList = adminActions.spiffeCommaList;
const spiffeEntriesAction = adminActions.spiffeEntriesAction;
const spiffeUnknownAction = adminActions.spiffeUnknownAction;
const spiffeAction = adminActions.spiffeAction;
const SPIFFE_ACTIONS = adminActions.SPIFFE_ACTIONS;
const ssfAction = adminActions.ssfAction;
const sweepText = adminActions.sweepText;
const tokenAction = adminActions.tokenAction;
const tokenLifetimesAction = adminActions.tokenLifetimesAction;
const truthy = adminActions.truthy;
const userFieldsFrom = adminActions.userFieldsFrom;
const usersAction = adminActions.usersAction;
const vcAction = adminActions.vcAction;
const vpConfigAction = adminActions.vpConfigAction;
const xacmlAction = adminActions.xacmlAction;
import config = require('../common/config');
// The credential lifecycle, for the activation link the users page issues. A
// LEAF (rule 3): it registers no route and requires nothing here.
import credentials = require('../common/credentials');
// RFC 6238 ITSELF, for /admin/totp: the settings it draws and the algorithm
// table `?format=json` reports. A LIBRARY (rule 3) that registers no route, and
// `credentials.js` above already requires it — so this is a second reader of
// one module rather than a new edge in the require order.
import totp = require('../common/totp');
// THE THIRD SECOND FACTOR (2026-09-10), for `/admin/backup-codes` and for the
// block on a person's own row under `/admin/users`. A LIBRARY (rule 3): it
// registers nothing and requires only `config`, `crypto` and `helpers`, so it
// can neither move a route nor join a cycle.
import backupCodes = require('../common/backup_codes');
// THE OTHER SECOND FACTOR, for /admin/webauthn — the ceremony's options and
// this service's policy about what a key may be. A LIBRARY (rule 3) on the same
// terms: it registers no route and requires only `config`, `helpers` and
// `authn/webauthn.js`, so it can neither move a route nor close a cycle, and
// `common/credentials.ts` above already requires it. **It is NOT
// `authn/authn.ts`**, which is 8 and owns the session: a require from here to
// that module would be the console reaching into the sign-in service, and the
// rule `/admin/crypto-metadata` is built on says an algorithm table is read
// from the module that PERFORMS the algorithm.
import webauthnPolicy = require('../authn/webauthn_policy');
// CSRF and rate limiting for this console. A LEAF (rule 3): it registers no
// route and requires only config, crypto and helpers, so it can neither move a
// route nor close a cycle.
import websecurity = require('../common/websecurity');
// The access-control gate. A LEAF (rule 3): registers nothing, requires only
// config and helpers. Its decider is filled by xacml/xacml_access_pep.ts at
// 23c, so before that line every check here is allowed — which is what a
// process without the XACML family does.
import accessGate = require('../common/access_gate');
// The mode. A LEAF (rule 3): registers nothing, requires only `config`.
import mode = require('../common/mode');
// THE VERSION, M.N.O, drawn at the foot of every page in this console. A LEAF
// (rule 3) that requires nothing from this repository, so it can neither move a
// route nor join a cycle — which matters here, because this module is at 18 and
// four other modules spread across the require order read the same file.
//
// **READ ONCE, AT REQUIRE TIME, AND NOT PER PAGE.** The version cannot change
// while the process runs: it is stamped into the artifact at build time or
// computed once at startup, and reading a file on every page render to learn
// something that is fixed for the life of the process is work in the one place
// this console does the most of it.
import version = require('../common/version');
const APP_VERSION = version.load();
const APP_BUILD_INFO = version.buildInfo(APP_VERSION);
// What this service writes down, and whether it is working. A PLAIN REQUIRE in
// the ordinary direction: that module is a library — it registers no route, so
// this line moves nothing in the router — and it requires only `config.js` and
// `realms.js`, neither of which reaches back here. Rule 3e's test therefore
// asks for no slot, both ways round. `/admin/persistence` renders its
// `status()` and `GET /admin/ldap/service` publishes the same object.
import persistence = require('../persistence/persistence');
// THE CLUSTER (2026-09-14, #46), for /admin/cluster's status block. Libraries:
// they register no route, and every one of them is already loaded by
// persistence.js, app.js or service_state.js, so these are cache hits.
import cluster = require('../cluster/cluster');
import clusterSecrets = require('../cluster/cluster_secrets');
import clusterBarrier = require('../cluster/cluster_barrier');
// WHERE THE TWO PRIMORDIAL SECRETS COME FROM, and whether the keystore is
// reading one at all — for the runtime line at the foot of every page
// (`runtimeFooter()`). Both are LEAVES (rule 3): `secrets.js` requires only
// `config` and `error_codes`, `keystore.js` is loaded long before this module
// by the startup it serves, so neither require can move a route or close a
// cycle.
import secrets = require('../common/secrets');
import keystore = require('../common/keystore');
// TRUST REALMS: the registry behind /admin/realms, and the switcher this shell
// draws on every page. It requires config.js and nothing else here, registers
// no route, and is already loaded by helpers.js — so its position is not a
// position at all.
import realms = require('../common/realms');
// A CREATE CLAIMS ITS NAME ACROSS NODES (2026-09-14, #46 follow-up) — the
// console's own user and group forms, as `/admin-api` already did. A LIBRARY
// (rule 3): it registers no route, and reaches the directory module through
// the require cache, so this require moves nothing in the route order.
import createClaims = require('../ldap/directory_create_claims');
import stats = require('../common/admin_stats');
// The browser sign-on sessions, from the authentication service that creates
// them — shared between the OAuth 2.0 / OIDC flow, WS-Federation and SAML 2.0.
//
// **THIS FILE STILL READS THEM AND NEVER ENDS ONE, AND THE REASON CHANGED ON
// 2026-08-24.** It used to be that ending a session from a third place would be
// a third way to get the cleanup wrong: /oauth2/logout and wsignout1.0 each had
// a fan-out written into it, and a console button would have been a third copy
// that quietly notified nobody.
//
// That is no longer the argument, because the fan-outs are now FUNCTIONS rather
// than copies — `wsfed.cleanupTargetsFor()`, `saml2_sso.logoutTargetsFor()` and
// `frontchannel_logout.js`, each owned by the module whose protocol it belongs
// to — and `authn.js`'s `dropSession()` is the single place a session actually
// stops existing. So /admin/logout DOES end sessions, and it ends them through
// exactly those functions.
//
// What is still true is the line this file holds to: this map is READ here and
// written nowhere here. The console's page calls `logout.ts`, which calls
// `authn.js`. A `sessions.delete()` in this file would be the fourth way, and
// the one that skipped the RFC 9700 refresh revocation and the audit row.
//
// THREE MORE THINGS CAME FROM THAT MODULE, and until 2026-09-06 they were the
// whole of how this console was protected: `consoleSession()` read the
// cookie, `beginAuthentication()` stashed the page somebody asked for and
// handed back the URL of the sign-in screen, and `LOGIN_PATH` was where they
// were sent. The console is a relying party now (the paragraph below), so
// `beginAuthentication()` has no caller here and `LOGIN_PATH` only names the
// sign-in screen in a refusal. Nothing about signing in is implemented here —
// see the guard below — because a console with a login screen of its own
// would be a second authentication service, and this service has exactly one
// on purpose.
//
// **`consoleSession()` AND NOT `sessionOf()`, AND IT IS THE ONE CALLER OF IT.**
// The protocol reader answers out of the ambient realm's partition, which is
// exactly right for `/oauth2/authorize` and exactly wrong here: the realm
// SWITCHER at the top of every page in this console is a link to the same page
// in another realm, and with the per-realm reader every click on it landed on
// the sign-in screen — then overwrote the browser's only session cookie, so
// clicking back landed there again.
//
// **IT IS THE DEFAULT REALM'S SESSION, ALWAYS**, which is what ends the
// switcher loop rather than merely surviving it — `authn.js`'s header above
// `consoleSession()` argues it at length. This paragraph used to go on to say
// that the two console roles are groups in the DEFAULT realm's `ou=groups`
// and nowhere else; that stopped being true on 2026-09-14 (#32), when a realm
// got a roster of its own, asked for the realm the person SIGNED IN THROUGH
// and confined to that realm. The session store did not move; the roster it
// is asked against did — `gateStateFor()` in `admin-core/admin_views.ts`, and
// admin-ui/CLAUDE.md 8d.
//
// THE RELYING PARTY (2026-09-06). This console authenticates through the
// AUTHORIZATION CODE FLOW
// against this service's own authorization server now, rather than by
// redirecting to the sign-in screen and reading the session that screen minted.
// `common/oidc_rp.ts` runs the flow and argues the whole of it; what this file
// keeps is the GATE, which is a question about roles rather than about
// authentication.
//
// `consoleSession` is still imported and still has exactly one caller — see
// consoleSignOn() below, where the sign-on session is what the console REPORTS
// about rather than what it lets somebody in on.
import oidcRp = require('../common/oidc_rp');
// `endSessionById` is here for the Sign out button and for nothing else — see
// SIGNOUT_PATH below, where the console ends the SIGN-ON session behind its own
// as well as the console session itself, and argues why it must.
import authn = require('../authn/authn');
const { sessions, consoleSession, beginAuthentication, endSessionById,
        clearSessionCookie, LOGIN_PATH } = authn;
// WHO MAY USE THIS CONSOLE. A library (rule 3): it registers no route, so
// requiring it here moves nothing in the router, and it requires only
// config.js, helpers.js and audit.js — none of which requires it back. The two
// roles it decides from are two ORDINARY GROUPS in the embedded directory,
// which it reaches through a slot ldap_server.js fills; see its header for why
// that is a directory group rather than a store of this console's own.
import rbac = require('./admin_rbac');
// WHAT A REALM ADMINISTRATOR MAY NOT REACH (2026-09-14, #32): the service-wide
// pages, actions and settings, in one table the management API reads too. A
// library with no route.
import adminScope = require('./admin_scope');
// WHICH REALM TO SIGN IN THROUGH (2026-09-14, #32), asked before a bare /admin
// sends anybody to sign in. A library with no route, shared with the portal.
import loginRealmChooser = require('../common/realm_chooser');
// The credential claim set: which LDAP attributes an issued Verifiable
// Credential carries, and the invented values behind them. A library like
// admin_stats.js — it registers no route — so requiring it here neither adds to
// the express router nor makes a cycle, and /admin/vc below is the page that
// sets it. The DIRECTORY half of it (populating entries, reading values back)
// is ldap_server.js's, wired into that module through a slot for the
// route-order reason rule 6 gives.
import vcClaims = require('../oid4vc/vc_claims');
// The other end of that: which of those claims the mock OID4VP Verifier — the
// "bar door" at /oid4vp/verifier — ASKS a wallet for, and in which credential
// format. A library like vc_claims.js and admin_stats.js, registering no route,
// so requiring it here neither moves a route nor makes a cycle; vc_verifier.js
// reads the same module from the other side of the require order.
import vpConfig = require('../oid4vc/vc_verifier_config');
// The THIRD reader of that same catalogue: which LDAP attributes a token or an
// assertion carries, per claim set. A library like the two above, registering
// no route, so requiring it here neither moves a route nor makes a cycle — it
// requires admin_stats.js and vc_claims.js, and neither requires it back. It is
// what turns /admin/claims from a page of typed constants into one that can put
// what the directory says into an access token. See its header for why the
// selection is per-set and why nothing is selected on a fresh start.
import claimAttributes = require('../common/claim_attributes');
// The FOURTH library over that catalogue's territory: which LDAP attribute each
// SCIM member is, in both directions. Read here for the mapping tables on
// /admin/scim, and by scim.js for the conversions themselves. It registers no
// route and requires only helpers.js and vc_claims.js, so requiring it here
// neither moves a route nor makes a cycle — which is exactly why the
// conversions live in a library rather than in scim.js, where a require from
// this file would have dragged every /scim and /ldap route ahead of the
// console's own.
import scimMap = require('../scim/scim_map');
// The groups claim: which directory groups reach a token, whether it is on, and
// what it would say about one person. A LIBRARY like the line above — it
// registers no route, so requiring it here cannot reorder the router
// /admin/sts-metadata is built by walking, and it requires helpers.js,
// config.js and admin_stats.js, none of which requires this file. It is
// required for the same reason claim_attributes.js is: the page and the
// management API both report this feature, and neither should be reading its
// four settings itself.
import groupClaims = require('../common/group_claims');
// The audit log: what happened here, in order, as rows rather than as counters.
// A library like the three above — it registers no route — so requiring it here
// neither moves a route nor makes a cycle. It holds the events and this file
// renders them at /admin/audit, which is the same split admin_stats.js has and
// for the same reason: a test can walk the log over JSON without going near an
// HTML page.
import auditLog = require('../common/audit');
// The application registry, whose store is the ou=applications container in the
// embedded directory. A library that registers no route, so requiring it from
// the console cannot move anything in the route order — unlike ldap_server.js,
// which is why the user and group readers below are hooks rather than requires.
// Nothing is cached on either side: every read here is a directory read, which
// is what lets this page show an ldapmodify that happened a second ago.
import applications = require('../common/applications');
// A LIBRARY (rule 3): it registers no route, and it is required here for the
// metadata refresh action. It requires helpers, config and applications and
// nothing that requires this file, so it closes no cycle.
import spMetadata = require('../saml/sp_metadata');
// The SAML 2.0 Web Browser SSO profile, for the slug rule, the per-application
// entityID and the endpoint URLs — so that /admin/saml2 names the same
// addresses the metadata document publishes rather than rebuilding them here.
// A page that computed its own would be the console and the endpoints
// disagreeing about a URL, which is the failure a service provider meets as a
// 404 that looks like this service being down.
//
// **A PLAIN REQUIRE IN THE ORDINARY DIRECTION, AND NOT A SIXTH SLOT.** Rule
// 3e's test is whether a require would close a cycle or move a route, and this
// one does neither: `common/protocol_stack.ts` requires `saml/saml2_sso.ts` at
// position 10a and this file at 18, so that module's routes are already in the
// router by the time this line runs, and it requires nothing from here. (Since
// #50's R1 requiring it registers nothing in any case — the stack's
// `register()` call at 10a does — so the route half of the test cannot fail.)
import saml2 = require('../saml/saml2_sso');
// The SAML 1.1 browser profiles, for the same reason and on the same terms:
// this page must name the endpoints and the providerID that module names,
// because a console that derived a URL of its own would be a console telling
// somebody to configure a path nothing serves. It is required at position 10b
// in `common/protocol_stack.ts` and this file at 18, so this is a plain
// require in the ordinary direction — not an inverted slot, and rule 3e's test
// is why.
import saml11 = require('../saml/saml11_sso');
// The authorization server profiles — what each discovery document publishes.
// A library that registers no route, so requiring it here moves nothing.
import authorizationServers = require('../oauth-oidc/authorization_servers');
// The federation REGISTER, and only the register. It is a library that
// registers no route, so requiring it here moves nothing and closes no cycle —
// rule 3e's test again, and it passes both ways round: that module requires
// only config.js, helpers.js and audit.js.
//
// **`federation/federation_sp.ts` is deliberately NOT required here**, and it
// is the same line drawn around `spiffe_server.js` twenty lines down: that
// module registers /federation and its four endpoints, and
// `common/protocol_stack.ts` requires it at position 10c — BEFORE this file —
// so a require from here would be harmless today and would silently become
// the reason a route moved the day somebody reorders the two. (That was the
// argument while a require was a registration. Since #50's R1 the stack
// registers those routes at 10c with its own `register()` call, so no require
// could move them; a require from here would still load the module, and run
// its load-time work, from inside the console's — which is reason enough.)
// What this page needs from it is the shape of the URLs to configure at the
// partner, and those
// come from `federation.PATHS` — one copy of the strings, in the library both
// sides may reach, so this page
// and that router cannot come to name different paths. See that constant's
// header, where the failure it prevents is spelt out.
import federation = require('../federation/federation');
// ---------------------------------------------------------------------------
// THE FEDERATION PICTURE, in the two halves everything else in this console
// draws a diagram in: a MODEL that knows the register and no geometry, and a
// RENDERER that knows the geometry and nothing about federation. It is the same
// split as `common/delegation.js` / `./delegation_map.js` next door and it is
// the same reason — the code that decides what a box IS must not be able to
// reach the code that decides where a box GOES.
//
// Both are libraries and neither registers a route, so neither can be the
// reason one is missing and their position here does not matter.
// ---------------------------------------------------------------------------
import federationGraph = require('../federation/federation_graph');
import federationDiagram = require('./federation_diagram');
// The two SPIFFE LIBRARIES, and only those two. `spiffe_ca.js` holds the trust
// domain's authorities and `spiffe_registry.js` the registration entries and
// agents; both register nothing, so requiring them here moves no route and
// cannot close a cycle. `spiffe_id.js` comes with them for the server ID.
//
// **`spiffe_server.js` is deliberately NOT required here.** That module
// registers the bundle endpoint and /spiffe, and `common/protocol_stack.ts`
// requires this file FIRST — so a require from here would pull those routes
// into the express router ahead of the console's own, and
// GET /admin/sts-metadata is built by walking that router. (Since #50's R1
// requiring it registers nothing — the stack's `register()` call at 23 does —
// but that module requires THIS file for the shell, so a require from here
// would still close a cycle, and the slot below stays.) What this page
// needs from it is two facts about sockets, and they arrive through a reader
// slot instead: the same inversion setDirectoryReader(), setGroupReader() and
// setScimReader() already use, and justified by rule 3e's test in exactly the
// same way.
import spiffeCa = require('../spiffe/spiffe_ca');
import spiffeRegistry = require('../spiffe/spiffe_registry');
// WHO MAY CALL THE SPIRE SERVER API. A library like the two above it — it
// registers nothing and starts nothing — so neither thing that forces a slot
// applies, and it is required directly rather than read through
// `setSpiffeReader()`. What DOES need the slot is which of the four sockets
// bound, which is a fact about a socket and only `spiffe_server.js` knows it.
import spiffeAuth = require('../spiffe/spiffe_auth');
import spiffeIdLib = require('../spiffe/spiffe_id');
// ---------------------------------------------------------------------------
// THIS CONSOLE AS A SHARED SIGNALS RECEIVER (2026-09-10), and it is a PLAIN
// REQUIRE rather than a ninth slot for the reason rule 3e states: a slot is
// what you pay for a require that would close a cycle or move a route, and
// this one does neither. `ssf/ssf_receivers.ts` registers nothing (rule 3) and
// requires only libraries — `helpers`, `config`, `realms`, `audit`,
// `ssf_subjects`, `ssf_events`, `ssf_streams`, `ssf_http` — none of which
// requires this file.
//
// It is emphatically NOT `ssf/ssf.ts`, which is at 23b and registers every
// /ssf route and the well-known document: a require of THAT from here would
// drag all of it ahead of the management API's own routes, which is exactly
// what the eighth slot exists to prevent. Since #50's R1 its own routes are
// registered by `common/protocol_stack.ts` at 23b wherever it is loaded, but
// it requires this file (a cycle) and `ldap/ldap_server.js`, which is still
// JavaScript and still registers the directory's routes when required.
//
// What crosses it is this console's own inbox and the stream behind it. See
// GET /admin/signals below.
// ---------------------------------------------------------------------------
import signals = require('../ssf/ssf_receivers');
// For the DRIFT report: the document this service would publish, to compare a
// profile's overrides against. oauth2.js is required before admin.ts in
// `common/protocol_stack.ts` (rule 5), so this is a plain require in the
// ordinary direction.
import oauth2 = require('../oauth-oidc/oauth2');
// WHO ACTED ON WHOSE BEHALF. A library like the four above — it registers no
// route — so requiring it here neither moves a route nor closes a cycle. It
// holds the acts and this file renders them at /admin/delegation, the same
// split audit.js has.
import delegation = require('../common/delegation');
// WHO MAY ACT ON WHOSE BEHALF — THE CONFIGURED HALF OF THE SAME QUESTION, and a
// SEPARATE REGISTER from the one above rather than a filter over it. That
// module holds delegation ACTS, which are evidence; this one holds delegated
// PERMISSIONS, which are intent: a resource application exposes an API and a
// client application is granted permissions on it, in Microsoft Entra ID's
// shape, before anybody has asked for anything. `/admin/delegation` draws both
// and labels which is which on every heading, because the interesting reading
// is the DIFFERENCE — a grant nobody has used, and a delegation nobody granted.
//
// A library like the ones around it: it registers no route, requires only
// helpers.js and applications.js, and holds the MODEL while this file holds the
// HTML. Its header argues the model and says why the ordering rule (define
// before grant) is enforced in applications.js rather than in either of them.
import appPermissions = require('../common/app_permissions');
// WHAT A PERSON AGREED AN APPLICATION MAY ASK FOR ON THEIR BEHALF — the THIRD
// register in this family and the first one whose rows are about a person
// rather than about two applications. `delegation.js` is what happened,
// `app_permissions.js` is what an operator allowed, and this is what somebody
// SAID YES TO. The three are drawn on two pages and never merged, for the
// reason /admin/delegation gives about the first two: an act, an intent and a
// consent look alike in a table and answer three different questions.
//
// A library like the ones around it: it registers no route, requires
// helpers.js, config.js, applications.js and admin_stats.js, and holds the
// MODEL while this file holds the HTML. Its header argues both halves — the
// per-person record and the per-application override — and why the second is
// an override rather than a record.
import consent = require('../common/consent');
// THE ROLE REGISTER, a plain require in the ordinary direction and it can stay
// one: `common/roles.js` is a LEAF that requires `helpers` and `config` and
// nothing else here, so no slot is needed and rule 3e is explicit that a slot
// is what you reach for when a require would close a cycle or move a route.
import roles = require('../common/roles');
// THE ISSUANCE VOCABULARY, for the preview's menu. A leaf too, and it is the
// TABLE this page needs rather than the decision — what decides is reached
// through setRolePreviewer() below, because that lives in `xacml/` at 23c.
import issuanceGate = require('../common/issuance_gate');
// THE PICTURE OF THAT REGISTER, at /admin/delegation/map. A library like
// `./admin_rbac.js` — it registers no route, requires only helpers.js and
// @dagrejs/dagre, and knows nothing about this console: it is HANDED a resolver
// that says what each box is, because what a party IS belongs here (the
// registry and the directory reader are here) and where a box GOES belongs
// there. Its own header weighs the dependency and argues why the layout is not
// hand-rolled.
import delegationMap = require('./delegation_map');
// ONE PERSON, END TO END: the same picture drawn of everything this service has
// done in one identity's name — the delegation acts naming them AND the
// ordinary OAuth 2.0, OIDC, SAML, Kerberos and SPIFFE issuance that no
// delegation register has ever held. A library like the two above: it registers
// no route, requires only helpers.js, admin_stats.js and delegation.js, and
// holds the MODEL while this file holds the HTML. Its header argues why the
// union of the two registers is there rather than here.
import userGraph = require('../common/user_graph');
// ONE CREDENTIAL'S ANCESTRY, at /admin/tokens/credential. A library like the
// one above (rule 3): it registers nothing, requires nothing in this service
// but the two registers and `user_graph.js`, and is HANDED nothing — it is
// asked for an identifier and answers with a graph in the shape this file
// already draws.
import credentialGraph = require('../common/credential_graph');
// The CONFIGURED half of that page: which Kerberos principals may delegate to
// which, out of the two attributes that decide it. It is read from the module
// that OWNS the principal database, for the reason every store rule here is
// where it is — what those two attributes mean is a statement about that store,
// and this file renders and decides nothing.
//
// A plain require in the ordinary direction, and both tests that would force a
// slot pass: `krb5_principals.js` registers no route (the KDC's own `/KdcProxy`
// and `/krb5/principals` are in `krb5_kdc.js`), and `common/protocol_stack.ts`
// requires the Kerberos modules BEFORE this one, so nothing here can be the
// reason a route moved. It is the same argument the two SPIFFE libraries above
// are required under.
import krb5Principals = require('../kerberos/krb5_principals');

// The input validator. A LEAF (rule 3): it registers no route of its own and
// requires only `config`, `bunyan` and zod, so it closes no cycle here and
// moves nothing in the route order.
import validation = require('../common/validation');
import InstanceSlot = require('../common/instance_slot');

// REQUIRED FOR THE ORDER THEY WERE ALWAYS REQUIRED IN, AND READ NOWHERE HERE
// (#50). TypeScript drops an `import … = require()` whose name nothing reads,
// and that would take the require out of this module's load sequence; each
// `void` is the read that keeps it where it is.
void scimMap;
void spMetadata;
void saml2;
void spiffeIdLib;
void consent;
void roles;
const vt = validation.types;
const vz = validation.z;
// THE PROTOCOL-INDEPENDENT LOGOUT IS NOT REQUIRED HERE, AND THAT IS RULE 3e's
// TEST ANSWERING YES FOR THE SIXTH TIME. It is reached through a SLOT below —
// setLogoutReader(), which `../logout/logout.ts` fills at its own require time,
// exactly as ldap_server.js, spiffe_server.js and scim.js fill the five above
// it.
//
// A plain require would close a cycle AND move routes, which is both halves of
// the test at once — and the second half survives #50's R1, because the
// routes it would move are `ldap_server.js`'s, which is still JavaScript and
// registers when required. That module requires `ldap_server.js` (for the
// bound connections that are the LDAP session), and `ldap_server.js` requires
// THIS file to fill those five slots — so `admin.ts -> logout.ts ->
// ldap_server.js -> admin.ts` hands ldap_server.js a half-initialised console
// whose `setDirectoryReader` is undefined, and the symptom arrives as
// something that is not a function. It would also drag every `/ldap` route
// into the router ahead of the console's own, which is rule 6 read backwards.
//
// See the slot itself, further down, beside the other five.





// How many subjects the metrics page names in one "Who" cell before it says how
// many more there are. A separate cap from MAX_ROWS because it bounds a cell
// rather than a list: the ceiling that matters here is the width of one row,
// and the full list is on /admin/users and in `?format=json` either way.
const MAX_WHO = 12;

// ---------------------------------------------------------------------------
// The page shell.
//
// One for every page, with the nav in it, so a page cannot be added without
// a way back — which held for the SECTIONS and did not hold for the pages under
// them until `up` existed, because the nav's answer on a drill-down was the
// section's own tab, drawn as text. See navBar(). The CSS is inline because
// app.js sets `default-src 'none'` with `style-src 'unsafe-inline'`: a
// stylesheet as its own resource would need its own exception and would buy
// nothing.
//
// There is NO SCRIPT anywhere in this file, and that constrains the design
// rather than merely describing it — `script-src 'none'` is what makes the
// whole family of reflected-content problems moot for this service, so these
// pages do not get an exception. Every control on them is therefore a plain
// form POST, and every list is sorted server-side. (The one console page with
// a script is `/admin/api-explorer`, drawn by `./api_explorer.js` since
// 2026-09-09; admin-ui/CLAUDE.md argues it.)
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// THE NAVIGATION, WHICH IS NOW A LIST DOWN THE LEFT RATHER THAN A ROW ACROSS
// THE TOP, AND IS GROUPED.
//
// Seventeen tabs on one line wrapped to three rows on a laptop and to five on
// anything narrower, and a reader looking for `Verifier request` had to read
// all seventeen labels to find out it was not `Credential claims`. What the row
// could not express is that these pages are not seventeen peers: some are about
// PROTOCOLS this service speaks, some are about what is in its DIRECTORY, one
// is history, and the rest are about the service itself.
//
// So SECTIONS is the structure and `NAV` below is derived from it. Two rules
// about that and both are load-bearing:
//
//   * **`NAV` is DERIVED and never written by hand.** `upTo()` and `trailBar()`
//     look a path up in it to label a breadcrumb, so a page in SECTIONS that
//     was not in NAV would leave a drill-down whose trail names a path instead
//     of a section. Deriving it makes that impossible rather than merely
//     unlikely.
//   * **The section a page is in is NOT a crumb.** A section has no page of its
//     own, so a crumb for it could not be a link, and a trail with a dead crumb
//     in the middle of it teaches a reader not to trust the ones beside it —
//     the rule the LAST crumb already follows for the same reason. The section
//     is visible where it is useful, which is the sidebar: the heading above
//     the page you are on is the answer to "what else is near this".
//
// WHERE THE PAGES THE ASK DID NOT NAME WENT. Four sections were asked for by
// name — Protocols (SCIM, SPIFFE, Verifier request, Credential claims, Custom
// claims), Directory (Users, Groups, Applications), Monitoring (Audit log) and
// Server configuration (Service metadata) — and seven pages were not mentioned.
// They are placed by the same question rather than left dangling: SPIFFE's
// registration entries and agents are SPIFFE, and authorization servers are
// OAuth, so all three are protocol pages; the console index, the metrics and
// the issued-token list describe the SERVICE'S OWN state rather than any one
// protocol, so they were an Overview at the top; and the configuration page and
// the admin roles below it are what this service is set up with, so they sit
// with the service metadata.
//
// TWO OF THOSE THREE MOVED TO MONITORING ON 2026-08-24, and what the move
// corrected is the question the section was answering. "Describes the service's
// own state rather than a protocol" is true of Metrics and Tokens, and it is
// equally true of the Audit log — which was in Monitoring by itself — so the
// old split was not between two kinds of page, it was between two kinds of
// ANSWER: a number, a list, and a history of what this service has done. Those
// are three ways of asking one question, and a reader who wants to know what
// happened should find all three under one heading rather than learn that the
// counters are filed away from the events they count.
//
// So Monitoring holds them in widening detail — Metrics (how much), Tokens
// (what came out), Audit log (what happened, in order) — and its `what` was
// rewritten with them, because "history rather than state" was the sentence
// that made the old split sound principled and is now false of two of its three
// pages. A section description that survives the pages moving under it is a
// description that was never about them.
//
// OVERVIEW IS A SECTION OF ONE NOW, and that is deliberate rather than a
// leftover. `/admin` is where a reader LANDS: it is the only page here whose
// job is to point at the others, so it is the one page that cannot sit under a
// heading naming a kind of content. Folding it into Monitoring would put the
// front door inside one of the rooms. The rule this trades against is the one
// stated for GROUPS below — a group of one is a heading buying nothing, which
// is why SCIM is ungrouped — and it does not reach here: a group's heading
// competes with the pages beside it inside a section, while a section's heading
// is the top-level structure, and dropping this one would leave `/admin` as a
// page under no heading at all in a sidebar where every other page has one.
//
// PROTOCOLS HAS A THIRD LEVEL NOW, AND ONLY PROTOCOLS DOES. Eight pages under
// one heading is the same list the row across the top was — a reader looking
// for `Registration entries` had to know that entries are a SPIFFE idea before
// the label could tell them anything — and the eight are not eight peers
// either: three configure what this service ISSUES over OAuth2/OIDC, three are
// the workload-identity side, one is the verifier's request and one is
// provisioning. So an item in a section may be a page (`path` + `label`) or a
// GROUP (`title` + `items`), and `isNavGroup()` is the one place that decides
// which. Three rules about a group, each the section rule one level down:
//
//   * A GROUP IS NOT A CRUMB AND HAS NO PAGE, for exactly the reason a section
//     does not. `trailBar()` is therefore untouched by this and must stay that
//     way — the trail is still `Admin console › Registration entries › <id>`.
//   * `NAV` IS STILL DERIVED, now through `sectionPages()`, which flattens a
//     group's pages into the section that holds it. Everything downstream —
//     `upTo()`, the trail, `consoleJson().pages` — reads NAV and so cannot tell
//     a grouped page from an ungrouped one, which is the point: grouping is a
//     fact about the SIDEBAR, not about the pages.
//   * NESTING STOPS HERE. A group holds pages, never another group. Nothing
//     enforces it beyond `sectionPages()` not recursing, which is deliberate —
//     a fourth level would be a sidebar that needs its own breadcrumb.
//
// ONE PLACEMENT IN HERE IS A JUDGEMENT RATHER THAN AN OBVIOUS FACT, and it is
// worth knowing which. `Credential claims` (`/admin/vc`) is the OID4VCI
// ISSUER's claim configuration, so it could be argued into OAuth2 / OIDC — the
// issuer is an OAuth2 authorization server wearing another hat, and the page
// sits beside `Custom claims` in the code. It is under Verifiable Credentials
// because the reader looking for it is thinking about a CREDENTIAL rather than
// about a token, and because the two halves of the credential lifecycle —
// what the issuer puts in and what the verifier asks for — answer each other
// and are worth reading together.
//
// `SAML` IS A GROUP WITH ONE PAGE IN IT AND THAT IS AN EXCEPTION WORTH
// ARGUING, because the rule right beside it says the opposite: `SCIM` is left
// ungrouped for the smaller reason that a group of one is a heading buying
// nothing, and it still is. What makes SAML different is that the heading names
// a PROTOCOL FAMILY this service speaks in two versions and two profiles, and
// the page under it — `Custom SAML attributes` (`/admin/saml-attributes`), the
// two assertion claim sets, moved off `/admin/claims` on 2026-08-24 — is one
// aspect of that family rather than the whole of it. A reader looking for what
// a WS-Federation assertion will carry looks for the word SAML; ungrouped, that
// page would sit among the protocol pages reading as something OAuth-adjacent,
// which is exactly the confusion the grouping was introduced to end. SCIM's one
// page IS the whole of SCIM here, so its heading would say the label twice.
// The test for a second group of one is that question and not this precedent:
// does the heading name more than the page under it does?
// ONE ROW BELOW IS A PAGE THIS FILE DOES NOT DRAW. `Service metadata`
// (`/admin/sts-metadata`) is built by `../sts_metadata.js`, which derives its
// whole content from the live express router and therefore has to be the LAST
// module `common/protocol_stack.ts` loads; it calls `respond()` for this
// shell. Nothing about the nav knows that, and that is the point — a page
// here is a `path` and a
// `label` whoever builds it. It was `/sts-metadata`, outside the console
// altogether, until 2026-08-24.
//
// EVERY PAGE ROW CARRIES A `blurb`, AND THAT IS WHAT `/admin` IS BUILT FROM.
// The Overview page's *What this console is* list used to be a hand-written
// `<ul>` in the index route, and it drifted exactly the way this repository
// warns everything else about: it described seven pages while the sidebar
// offered twenty-five, so the front door of this console was quietly the least
// complete description of it. The list is DERIVED from this table now
// (`consoleGuide()` below), so a page added here appears there with no second
// edit — and a page added here with no `blurb` appears there SAYING SO, in the
// same spirit as `/admin/sts-metadata` reporting an undescribed route rather
// than omitting it. A blurb is prose about ONE page and belongs beside that
// page's `path` and `label` for the same reason its label does; a second table
// keyed by path would be the drift back again with an extra lookup.
const SECTIONS = [
  { title: 'Overview',
    what: 'Where a reader lands, and what it points at.',
    items: [
      // NO `blurb`, and that is not an omission. `consoleGuide()` drops the
      // page it is being drawn ON before it looks for one, and this is the
      // only page it is ever drawn on — a row here describing this page would
      // be text nothing can render, which is the kind of thing that is edited
      // for years after it stopped being read.
      { path: '/admin', label: 'Console' }
    ] },
  { title: 'Protocols',
    what: 'One page per family, each configuring or reporting what that ' +
          'protocol does here.',
    items: [
      { title: 'OAuth2 / OIDC',
        what: 'Which authorization server a flow runs against, how long what ' +
              'it issues lasts, and what this service puts into it.',
        items: [
          // FIRST IN ITS GROUP, and the position is the argument for the page
          // existing: the other four are about one aspect each — which server,
          // how long, which claims — and this one is the family's own
          // configuration, which is what a reader who has not decided what
          // they are looking at yet wants first.
          { path: '/admin/oauth2', label: 'OAuth 2.0 / OIDC settings',
            blurb: 'The appconfig rows behind the authorization ' +
                   'server: the issuer identifier, RFC 9700 mode and OAuth ' +
                   '2.1 mode, the ' +
                   'registered redirect URIs and how a loopback one is ' +
                   'matched, Front-Channel Logout, the refresh token\'s idle ' +
                   'timeout and whether a sign-out revokes it, the client ' +
                   'assertion\'s clock skew — and the deliberate defect ' +
                   '<code>oauth2.breakIdTokenNonce</code>, which makes this ' +
                   'service return an ID Token whose <code>nonce</code> is ' +
                   'wrong so that a client can find out whether it checks. ' +
                   'The five a CLIENT may answer for itself — the three ' +
                   'lifetimes, the refresh idle timeout and whether a ' +
                   'sign-out revokes refresh tokens — moved to ' +
                   '<a href="/admin/token-lifetimes">Token lifetimes</a> ' +
                   'on 2026-08-27, which is where their defaults live now.' },
          { path: '/admin/authorization-servers',
            label: 'Authorization servers',
            blurb: 'Several authorization servers in ONE process, told apart ' +
                   'by the path component the two discovery shapes already ' +
                   'carry — RFC 8414 inserts it after the well-known segment ' +
                   'and OpenID Connect Discovery appends the segment to it. ' +
                   'Each has its own issuer, its own keys and its own idea ' +
                   'of what it will accept, so a client can be pointed at a ' +
                   'second one without a second process. A path nobody has ' +
                   'configured publishes the document this service always ' +
                   'published, which is what keeps a client that has never ' +
                   'heard of this page unaffected by it.' },
          { path: '/admin/token-lifetimes', label: 'Token lifetimes',
            blurb: 'How long an access token, an ID Token and a refresh ' +
                   'token issued here are good for, and how far out a clock ' +
                   'may be before this service stops believing one of its ' +
                   'own. Four configuration settings with a page of their ' +
                   'own, written through the same function <a ' +
                   'href="/admin/oauth2">the OAuth 2.0 / OIDC settings</a> ' +
                   'write through — a change made on either page is one ' +
                   'change.' },
          { path: '/admin/claims', label: 'Custom claims',
            blurb: 'What to add to every OAuth 2.0 access token and every ' +
                   'OIDC ID Token this service issues FROM NOW ON. Two sets, ' +
                   'because those two go to different readers and the ' +
                   'interesting configuration is usually the one where they ' +
                   'DIFFER. Additive only: a custom claim is never allowed ' +
                   'to displace one the protocol defines, because an ' +
                   '<code>exp</code> settable from a web form would produce ' +
                   'tokens that fail to verify with nothing pointing back at ' +
                   'the page. Nothing already issued changes — a token is a ' +
                   'signed document and this page cannot reach inside one.' },
          { path: '/admin/userinfo-claims', label: 'UserInfo claims',
            blurb: 'The same two halves for the <strong>UserInfo ' +
                   'response</strong>, and the one claims page here with no ' +
                   '"nothing already issued changes" warning on it — which ' +
                   'is the point of it being a page of its own. That ' +
                   'response is built on EVERY call, so a claim added here ' +
                   'reaches a client that signed in an hour ago and has done ' +
                   'nothing since. It is also the one claim set a CLIENT can ' +
                   'add to: OpenID Connect Core section 5.5\'s ' +
                   '<code>claims</code> request parameter names individual ' +
                   'claims, and they are answered off that person\'s entry ' +
                   'under <code>ou=users</code>.' }
        ] },
      { title: 'SAML',
        what: 'BOTH identity providers — SAML 2.0\'s Web Browser SSO profile ' +
              'and SAML 1.1\'s two browser profiles, each with its relying ' +
              'parties and the metadata each one is configured from — and ' +
              'what this service puts into an assertion, 2.0 and 1.1 alike, ' +
              'which is also what WS-Trust and WS-Federation carry. The two ' +
              'profiles are separate pages because they are separate ' +
              'implementations: SAML 1.1 has no request message and no ' +
              'Single Logout, so half of what the 2.0 page reports has no ' +
              'spelling over there.',
        items: [
          { path: '/admin/saml2', label: 'SAML 2.0 identity provider',
            blurb: 'Every service provider this identity provider has been ' +
                   'asked about, what each one was sent and over which ' +
                   'binding, and what this service calls ITSELF to each of ' +
                   'them. What goes INTO the next assertion moved to ' +
                   '<a href="/admin/saml-assertions">SAML assertions</a> ' +
                   'on 2026-08-27, because those five are per application ' +
                   'now and this page configures the identity provider. ' +
                   'Metadata is published PER service provider — the ' +
                   'identity provider names itself differently to each one, ' +
                   'the way Okta and Ping do — and it is minted for anything ' +
                   'asked for, so nothing has to be registered here first: ' +
                   'asking for a service provider\'s metadata is what ' +
                   'creates it.' },
          { path: '/admin/saml11', label: 'SAML 1.1 identity provider',
            blurb: 'The same, for the older profiles, and it is a SEPARATE ' +
                   'implementation rather than a version flag. SAML 1.1 has ' +
                   'no request message, so a relying party cannot identify ' +
                   'itself in the protocol at all — it is named by ' +
                   'Shibboleth\'s <code>providerId</code>, by the path ' +
                   'segment of a scoped endpoint, or GUESSED from the origin ' +
                   'of the <code>TARGET</code> — and there is no Single ' +
                   'Logout to configure and no request signature to record. ' +
                   'What it has that 2.0 does not is a SOAP responder that ' +
                   'is also an attribute authority.' },
          { path: '/admin/saml-assertions', label: 'SAML assertions',
            blurb: 'THE DEFAULTS EVERY SAML APPLICATION INHERITS: how long ' +
                   'an assertion is valid, whether it and its response are ' +
                   'signed, the NameID format and the artifact lifetime — ' +
                   'five per profile — plus the clock skew written into ' +
                   'both ends of the validity window. Ten of the eleven can ' +
                   'be overridden PER APPLICATION on ' +
                   '<a href="/admin/applications">an application entry</a>, ' +
                   'and this page names the attribute that does it beside ' +
                   'each row; the skew cannot, because it is a fact about ' +
                   'the clocks in this estate rather than about one relying ' +
                   'party. All eleven reach WS-Trust and WS-Federation too — ' +
                   'their assertions come out of the same two builders.' },
          { path: '/admin/saml-attributes', label: 'Custom SAML attributes',
            blurb: 'The same two halves for the assertions: every SAML 2.0 ' +
                   'and SAML 1.1 attribute added to what the two identity ' +
                   'providers, WS-Trust and WS-Federation issue from now on. ' +
                   'One store behind this page and ' +
                   '<a href="/admin/claims">Custom claims</a>; two ' +
                   'vocabularies, because 2.0 has <code>Name</code> and an ' +
                   'optional <code>NameFormat</code> where 1.1 has ' +
                   '<code>AttributeName</code> and a REQUIRED ' +
                   '<code>AttributeNamespace</code>, and one list could not ' +
                   'have served both.' }
        ] },
      { title: 'Verifiable Credentials',
        what: 'Both halves: what the OID4VCI issuer puts in a credential, ' +
              'and what the OID4VP verifier asks a wallet to present.',
        items: [
          // THE ORDER PAIRS EACH PROTOCOL WITH THE PAGE THAT SAYS WHAT GOES
          // THROUGH IT — issuance, then what an issued credential carries;
          // presentation, then what a wallet is asked for. Grouping the two
          // settings pages together instead would put the OID4VP settings
          // three rows from the request they configure.
          { path: '/admin/oid4vci', label: 'OpenID4VCI',
            blurb: 'The issuer\'s own settings: which wallet the offer pages ' +
                   'send somebody to, which authorization server the ' +
                   'credential endpoint trusts a token from, how many ' +
                   'credentials one request may ask for, how long a deferred ' +
                   'issuance pretends to take, whether a credential request ' +
                   'must be encrypted, and whether the SD-JWT VC and ldp_vc ' +
                   'issuers name themselves by DID or by URL — the one ' +
                   'choice here that changes what a verifier has to resolve.' },
          { path: '/admin/vc', label: 'Credential claims',
            blurb: 'Which claims a Verifiable Credential issued from now on ' +
                   'carries, chosen from a catalogue of LDAP attribute TYPES ' +
                   'rather than of claim names: the value of a claim is the ' +
                   'value on that person\'s directory entry. Saving a ' +
                   'selection also populates the directory, so an LDAP ' +
                   'client and a wallet describe one person. It applies to ' +
                   'all five OID4VCI configurations at once.' },
          { path: '/admin/vc-status', label: 'Credential status',
            blurb: 'This realm\'s status lists — the Token Status List every ' +
                   'JOSE credential names and the two Bitstring Status ' +
                   'Lists the W3C ones name — where they are served, and ' +
                   'every issued credential\'s index and status, with ' +
                   'Suspend, Reinstate and Revoke. A revoked or suspended ' +
                   'credential is refused by every verifier that reads the ' +
                   'list, this one included, and signs nobody in.' },
          { path: '/admin/oid4vp', label: 'OpenID4VP',
            blurb: 'The verifier\'s own settings: the client identifier it ' +
                   'presents as, where it sends a holder to present, how ' +
                   'fresh a Key Binding JWT has to be, and the claims its ' +
                   'request asks for by default. The DCQL query itself is on ' +
                   '<a href="/admin/vc-verifier-config">Verifier request</a> ' +
                   'next door — these four are the settings around it.' },
          { path: '/admin/vc-verifier-config', label: 'Verifier request',
            blurb: 'The other end of that lifecycle: what the mock Verifier ' +
                   'at <code>/oid4vp/verifier</code> asks a wallet for, and ' +
                   'in which credential format. It reaches the wire as the ' +
                   '<code>dcql_query</code> of the next OID4VP ' +
                   'Authorization Request, and it is what the presentation ' +
                   'is then checked against — a claim asked for and not ' +
                   'presented fails by name.' }
        ] },
      // A GROUP OF FIVE, and the heading names more than any one page does —
      // which is the test the SAML group set and the one SCIM fails, so SCIM
      // is ungrouped one row below and this is not. XACML here is a decision
      // point, a repository, an editor, the enforcement points elsewhere that
      // pull it, and a way to try the thing — five ways of saying what
      // authorization is CONFIGURED to do.
      //
      // IT WAS A GROUP OF SIX UNTIL 2026-09-06 and the page that left is the
      // one worth recording: `/admin/xacml/monitor` is about TRAFFIC rather
      // than configuration, so it belongs in Monitoring with every other page
      // that answers "what has this service done", and it is there now. It is
      // still drawn by `xacml/xacml_admin.ts` and still lives under
      // `/admin/xacml/` — a console page is a `path` and a `label` in this
      // table whoever builds the body, which is the same arrangement the eight
      // `/admin/ldap/*` pages have with the Directory section. The rule read
      // off it: WHERE A PAGE IS FILED IS DECIDED BY THE QUESTION IT ANSWERS,
      // not by the module that draws it or the path space it sits in.
      { title: 'XACML',
        what: 'Authorization: what this service DECIDES, the policies it ' +
              'decides with, and how to write one.',
        items: [
          { path: '/admin/xacml', label: 'XACML settings',
            blurb: 'What the Policy Decision Point is, whether it is on, and ' +
                   'the one setting that belongs to the Policy ENFORCEMENT ' +
                   'point rather than to the PDP — a deny-biased PEP refuses ' +
                   'an Indeterminate and a permit-biased one allows it, and ' +
                   'they agree on everything else.' },
          { path: '/admin/xacml/policies', label: 'Policies',
            blurb: 'The repository, which IS ou=policies in the embedded ' +
                   'directory rather than a copy of it. Enable a policy, ' +
                   'disable it without deleting it, choose which one is the ' +
                   'ROOT the PDP starts from, and create one from an RBAC or ' +
                   'ABAC template.' },
          { path: '/admin/xacml/editor', label: 'Policy editor',
            blurb: 'The guided editor. Every element offers exactly what ' +
                   'XACML allows at that point — computed on the server by ' +
                   'the same code that validates the result, so it cannot ' +
                   'offer something that will then be refused. There is no ' +
                   'draft state: the policy you are editing is the policy ' +
                   'the PDP is using.' },
          { path: '/admin/xacml/peps', label: 'Remote PEPs',
            blurb: 'The Policy Enforcement Points in OTHER processes that ' +
                   'pull this repository and enforce it there. They hold ' +
                   'their own copy of the engine, so the question this page ' +
                   'answers is not whether they are up but whether ' +
                   'everybody is deciding with the SAME policy &mdash; ' +
                   'which is the question a distributed authorization ' +
                   'deployment actually has and the one nothing else here ' +
                   'can answer. Nothing on it reaches into another process.' },
          { path: '/admin/xacml/decide', label: 'Try a decision',
            blurb: 'Ask the PDP about somebody and see the answer, which ' +
                   'policies applied, what the PIP found on their directory ' +
                   'entry, and — separately — what the embedded PEP would do ' +
                   'with it. The last two are different answers, and when a ' +
                   'policy "is not working" it is nearly always because only ' +
                   'one of them was being looked at.' }
        ] },
      { path: '/admin/scim', label: 'SCIM',
        blurb: 'The provisioning endpoint at <code>/scim/v2</code>: what it ' +
               'requires of a caller, which of RFC 7644 section 2\'s six ' +
               'schemes it will take, and what it has written. It is the ' +
               'only protocol family here whose purpose is to WRITE, and ' +
               'what it writes is the embedded directory — there is no ' +
               'second store and no cache, so a <code>POST ' +
               '/scim/v2/Users</code> and an <code>ldapadd</code> create the ' +
               'same entry and somebody provisioned over SCIM appears on ' +
               '<a href="/admin/users">Users</a>.' },
      // UNGROUPED, beside SCIM, and for the smaller of that page's two
      // reasons: a group of one would be a heading saying the label twice.
      // What makes it worth its own page rather than a section of another is
      // that it is the one family here that runs the OTHER WAY ROUND — this
      // service delivering an event to a receiver that agreed in advance —
      // so there is no family above it whose page it would be a corner of.
      { path: '/admin/ssf', label: 'Shared Signals',
        blurb: 'The <strong>Shared Signals Framework</strong> (OpenID SSF ' +
               '1.0): the streams this transmitter has agreed, who each one ' +
               'is about, what is queued for it and what a receiver refused. ' +
               'It is the one protocol family here that TALKS BACK &mdash; ' +
               'every other answers a request, and this one delivers a ' +
               'Security Event Token nobody asked for, at the moment ' +
               'something happens. <strong>SSF is the pipe and not the ' +
               'vocabulary</strong>: it defines two events of its own, both ' +
               'about the pipe, and the vocabularies are ' +
               '<a href="/admin/caep">CAEP</a> (what happened to a SESSION) ' +
               'and <a href="/admin/risc">RISC</a> (what happened to an ' +
               'ACCOUNT). TWO things here generate an event on their own ' +
               'now, and they watch different registers: CAEP, where a ' +
               'session starting, being presented or ending sends a ' +
               'Security Event Token with nobody having asked, and RISC, ' +
               'where a change to the DIRECTORY does.' },

      // BESIDE Shared Signals rather than a section of it, and the argument
      // is the same one that made CAEP rows in `ssf_events.js` rather than a
      // family of its own: SSF is the PIPE and CAEP is a VOCABULARY over it.
      // They are two specifications answering two questions, and a reader
      // looking for "what has this service said about that session" is not
      // looking for "what streams exist", which is the whole of that page.
      //
      // Not a GROUP of two, because a heading saying "Shared Signals" over
      // pages called "Shared Signals" and "CAEP" would say the label twice —
      // which is the test this console already applied to the SSF page.
      { path: '/admin/caep', label: 'CAEP',
        blurb: 'The <strong>Continuous Access Evaluation Profile</strong> ' +
               '(OpenID CAEP 1.0, final 2 September 2025): the enterprise ' +
               'SESSION vocabulary spoken over ' +
               '<a href="/admin/ssf">Shared Signals</a>. Eight event types ' +
               '&mdash; session revoked, established and presented, token ' +
               'claims change, credential change, assurance level change, ' +
               'device compliance change, risk level change &mdash; each ' +
               'with the members the specification gives it and the four it ' +
               'gives them all. <strong>It is the one page here that ' +
               'configures this service to act without being asked</strong>: ' +
               'with <code>caep.autoEmit</code> on, a sign-in, a single ' +
               'sign-on and a sign-out each send an event to whoever agreed ' +
               'to be told. Five of the eight describe things nothing here ' +
               'does &mdash; no device reports compliance to this service ' +
               'and no risk engine talks to it &mdash; so this page carries ' +
               'the form that emits one by hand.' },

      // AND RISC BESIDE IT, for the reason CAEP is beside Shared Signals
      // rather than inside it: they are two specifications answering two
      // questions. CAEP is about a SESSION and RISC is about an ACCOUNT, and
      // a reader looking for "what has been said about that person's account"
      // is not looking for "what has been said about that session of theirs"
      // — which is the whole of the other page.
      { path: '/admin/risc', label: 'RISC',
        blurb: 'The <strong>Risk Incident Sharing and Coordination</strong> ' +
               'profile (OpenID RISC Profile Specification 1.0, published 29 ' +
               'August 2025 and final on 2 September 2025): the ACCOUNT ' +
               'vocabulary spoken over <a href="/admin/ssf">Shared ' +
               'Signals</a>, and the second of the two. Fourteen event types ' +
               '&mdash; account disabled, enabled and purged, credential ' +
               'change required, credential compromise, identifier changed ' +
               'and recycled, the four opt-out events, recovery activated ' +
               'and recovery information changed, and one that RISC itself ' +
               'deprecates in favour of a CAEP event. <strong>Eleven of them ' +
               'carry no payload members at all</strong>, so the subject is ' +
               'the entire message, which is what makes ' +
               '<code>risc.subjectFormat</code> the most consequential ' +
               'setting on the page. With <code>risc.autoEmit</code> on, a ' +
               'person deleted from the directory, an account marked ' +
               'inactive and a changed mail address each send an event ' +
               '&mdash; a DIFFERENT observer from CAEP\'s, watching the ' +
               'directory rather than the sessions.' },

      // UNGROUPED, beside SCIM, and the placement needed the same argument the
      // delegation page needed. Federation spans FIVE protocol families, so
      // under any one of the groups above it would mean choosing which four
      // fifths of the answer to hide — which is exactly what put
      // /admin/delegation in Monitoring. It does not go there, though: that
      // page is an OBSERVATION and this one is CONFIGURATION, and it is the
      // only page in this console that configures something a protocol
      // endpoint will REFUSE on. A section of its own was considered and
      // fails this console's own test for one — the heading would name
      // nothing the page under it does not.
      { path: '/admin/federation', label: 'Federation',
        blurb: 'Relationships with a FOREIGN identity service, in either ' +
               'direction, in five protocols — consuming somebody else\'s ' +
               'assertions as a service provider, or asserting to a foreign ' +
               'service provider with a per-partner attribute release ' +
               'policy. It is the one page in this console that configures a ' +
               'REFUSAL: everywhere else this service accepts what it is ' +
               'given, and it cannot do that at an assertion consumer ' +
               'service, because what arrives there is an unauthenticated ' +
               'request claiming to be a person and the session it would ' +
               'produce is the same one every other family and this console ' +
               'read. So a relationship is created DISABLED and an assertion ' +
               'is refused unless it verifies against the certificate ' +
               'configured on it.' },

      // FIVE PAGES ADDED ON 2026-08-27, AND EVERY ONE OF THEM EXISTS BECAUSE
      // ITS FAMILY HAD SETTINGS AND NO PAGE. Kerberos has nineteen appconfig
      // rows, SCIM has eighteen and SPIFFE twenty-eight — and until this date
      // only the last two were reachable from a page about the protocol they
      // configure. The other five families' settings were on /admin/config
      // among a hundred and fifty-four rows, which is the same as being
      // nowhere for anybody who looked under the protocol first.
      //
      // They are UNGROUPED, beside SCIM and Federation, for the reason a group
      // of one is refused everywhere else here: the heading would say the
      // label twice. They come after those two rather than before, in the
      // order the families arrived in this service, because there is no
      // ranking between them that a reader would predict — alphabetical would
      // put TLS before WS-Trust and teach nobody anything.
      // ---------------------------------------------------------------
      // THE TWO SECOND FACTORS, UNDER PROTOCOLS (2026-09-10).
      //
      // A group of two would be a heading that said the label twice, so they
      // are two flat items — the arrangement /admin/federation and
      // /admin/scim already have. They sit together and before Kerberos
      // because they are the two mechanisms a PERSON authenticates with here
      // beside a password, and somebody looking for one is usually about to
      // look at the other.
      //
      // **WHO HOLDS ONE IS NOT HERE.** That is <a
      // href="/admin/users">Users</a>, which is filed under Directory — where a
      // page is FILED is decided by the question it answers, and these two
      // answer *what does the mechanism do*.
      // ---------------------------------------------------------------
      // GNAP (RFC 9635 + RFC 9767), 2026-09-12. A FLAT item rather than a
      // group: its one Protocols page carries the settings, the endpoints, the
      // authorization server capabilities, the grants and the resource sets,
      // and the traffic page is filed under Monitoring by the question it
      // answers. Both are drawn by gnap/gnap_admin.ts.
      { path: '/admin/gnap', label: 'GNAP',
        blurb: 'The Grant Negotiation and Authorization Protocol (RFC 9635) ' +
               'and its resource server connections (RFC 9767): one ' +
               'authorization server per trust realm and per named ' +
               'authorization server. The endpoints, what each authorization ' +
               'server offers, the five token formats and their verification ' +
               'material, the grants it is holding, the resource sets ' +
               'registered with it, and the <code>gnap.*</code> settings. ' +
               '<strong>A key proof is verified in every mode</strong> ' +
               '&mdash; what is mode-gated is whether a proved key nobody ' +
               'registered is admitted.' },
      { path: '/admin/totp', label: 'TOTP MFA',
        blurb: 'An authenticator app as a second factor &mdash; RFC 6238 ' +
               'over RFC 4226 &mdash; and the eight parameters the ' +
               'specification leaves open: the digest, the digits, the ' +
               'length of a step, how much clock skew is forgiven, the ' +
               'secret length, the label a phone shows, and how long an ' +
               'unconfirmed enrolment lives. <strong>Codes are checked FOR ' +
               'REAL in every mode</strong>, which almost nothing else on ' +
               'this service is &mdash; a permissive one-time password ' +
               'verifier is not permissive, it is broken. Changing the ' +
               'digest, the digits or the period affects NEW enrolments ' +
               'only; the skew window applies to everybody. Who actually ' +
               'holds one, and the Clear that is the only way back for a ' +
               'lost phone, are on that person\'s row under <a ' +
               'href="/admin/users">Users</a>.' },
      { path: '/admin/backup-codes', label: 'Recovery codes',
        blurb: 'The way back in when the second factor is not to hand ' +
               '&mdash; a set of single-use codes a person generates for ' +
               'themselves, standing in for an authenticator app or a ' +
               'security key in the <code>mfa</code> role. <strong>The ' +
               'only mechanism on this console that no specification ' +
               'defines</strong>: there is no RFC for a recovery code, so ' +
               'every decision behind it is this service\'s own and ' +
               '<code>common/backup_codes.ts</code> argues each one. Since ' +
               '2026-09-11 it is <strong>hashed with scrypt, the same way a ' +
               'password is</strong> &mdash; it was encrypted until then, so ' +
               'that a person could read their remaining codes back, and ' +
               'hashing is what made that impossible. A person GENERATES ' +
               'their own set from the user portal, is shown it once, and it ' +
               'is stored only when they confirm they have saved it; ' +
               'generating again REPLACES. There is no control on THIS ' +
               'console that issues a set &mdash; a set belongs to the ' +
               'person and is generated from their own account page. Who ' +
               'holds one, how many they have left, and the Clear that ' +
               'removes a set an operator has reason to distrust, are on ' +
               'that person\'s row under <a href="/admin/users">Users</a>.' },
      { path: '/admin/webauthn', label: 'WebAuthn',
        blurb: 'Security keys &mdash; W3C WebAuthn Level 3 over FIDO CTAP2 ' +
               '&mdash; as a second factor OR as the only credential on an ' +
               'account, and the thirteen settings behind the ceremony. ' +
               '<strong>There were none of these until 2026-09-10</strong>: ' +
               'the RP name, the algorithms offered, the user verification ' +
               'requirement, the attestation conveyance and the timeout were ' +
               'literals in a string, and this service said there was ' +
               'nothing an operator could usefully turn &mdash; which was ' +
               'true of the cryptography and false of the ceremony. One of ' +
               'them is ENFORCED and the rest are requests: user ' +
               'verification is checked against a flag inside the bytes the ' +
               'authenticator signed, and nothing signed says what the ' +
               'browser was asked about attestation, the resident key or the ' +
               'attachment. Who holds a key is on that person\'s row under ' +
               '<a href="/admin/users">Users</a>.' },
      // A GROUP OF TWO SINCE 2026-09-13, asked for by rcbj. The two pages were
      // flat siblings — `Kerberos` and `Kerberos principals` — on the argument
      // that a `Kerberos` heading over a `Kerberos` page would say the label
      // twice. That test is about the LABELS, and renaming the settings page
      // `Kerberos settings` (the `XACML settings` and `OAuth 2.0 / OIDC
      // settings` shape) answers it; the heading now names more than either
      // page does, which is the test a group has to pass. The stored keys are
      // under Protocols and not Directory because the question they answer is
      // what the KDC holds a key for, which is configuration of that protocol
      // — even though both halves of the store are directory entries.
      { title: 'Kerberos',
        what: 'The KDC: its settings, and the long-term keys it holds for ' +
              'people and services.',
        items: [
          { path: '/admin/kerberos', label: 'Kerberos settings',
            blurb: 'The KDC\'s own settings: the realm, the two raw ports, ' +
                   'the clock skew and the deliberate clock OFFSET, the one ' +
                   'password every user account shares, the names that stay ' +
                   'unknown so a client can be shown a real KDC error, the ' +
                   'long-term keys behind krbtgt and the inter-realm trust, ' +
                   'and whether a ticket presented at ' +
                   '<code>/authn/spnego</code> may start a browser session ' +
                   'at all. Most of them are restart-only: the principal ' +
                   'database is built from them when the process starts.' },
          { path: '/admin/kerberos/principals', label: 'Principals',
            blurb: 'Who this KDC holds a stored long-term key for: the ' +
                   'directory people whose keys were derived from their own ' +
                   'password (product mode) with the kvno and enctypes and ' +
                   'whether the keys still match the password, and the ' +
                   'SERVICE principals an operator created here with a ' +
                   'random key. A create or a rotate hands over an MIT ' +
                   'keytab ONCE; no page ever shows a key. Delete and clear ' +
                   'controls need Admin Write.' } ] },
      { path: '/admin/ldap', label: 'LDAP / LDAPS',
        blurb: 'The embedded directory\'s two raw sockets and the store ' +
               'behind them: the ports, the base DN every realm\'s subtree ' +
               'hangs under, whether a name seen for the first time gets an ' +
               'entry, and the two ceilings that keep a mock from being ' +
               'filled up. What is IN the directory is on ' +
               '<a href="/admin/users">Users</a> and ' +
               '<a href="/admin/groups">Groups</a>; this page is the ' +
               'directory itself.' },
      { path: '/admin/wstrust', label: 'WS-Trust',
        blurb: 'The security token service at <code>/sts</code>, ' +
               'WS-Trust 1.0 to 1.4, and the one setting it has of its own — ' +
               'who its tokens say issued them. What an assertion CONTAINS ' +
               'is the SAML pages next door, because the assertion is built ' +
               'by the same two functions the SAML profiles use.' },
      { path: '/admin/wsfed', label: 'WS-Federation',
        blurb: 'The passive requestor profile at <code>/wsfed</code>, its ' +
               'entity ID, and the mock relying party that checks a sign-in ' +
               'response line by line. The assertion it carries is a SAML ' +
               '1.1 one, so its issuer and its attributes are configured on ' +
               'the SAML pages — this page says which setting is which and ' +
               'links to them rather than offering a third form onto the ' +
               'same value.' },
      // PKI, UNGROUPED, BESIDE TLS AND SCIM. A group of its own would be a
      // heading naming nothing the one page under it does not — this
      // console's own test for one, applied the way Federation and SCIM are
      // filed. It goes NEXT TO TLS deliberately: they are the two pages here
      // about X.509, and a reader who has just read that a verified client
      // certificate starts a session is one keystroke from the page that
      // issues certificates.
      { path: '/admin/pki', label: 'PKI',
        blurb: 'A <strong>certificate authority this service maintains for ' +
               'this trust realm</strong> — Root CA, Intermediate CA, ' +
               'Issuing CA — and the signing key pairs it issues from the ' +
               'bottom of it to applications. It exists for <strong>RFC 7521 ' +
               'and RFC 7523</strong>: an application authenticates at the ' +
               'token endpoint, or presents an authorization grant, with a ' +
               'signed assertion instead of a shared secret, and a signing ' +
               'key nobody vouched for is a key an operator has to move by ' +
               'hand. All three tiers are built in one act or none is — a ' +
               'half-built hierarchy is exactly the state in which somebody ' +
               'issues a certificate that verifies here and nowhere else. ' +
               'ONE ROOT CA FOR THE SERVICE, an Intermediate CA per realm ' +
               'and per the process, and an Issuing CA under each for ' +
               'every use case — so one anchor covers every key this ' +
               'service holds. It was a Root PER REALM until 2026-09-11, ' +
               'on the argument that a CA shared across realms would be ' +
               'one authority vouching for several identity services. ' +
               'Every authority on it signs a CRL and answers OCSP, and a ' +
               'certificate replaced or rotated goes on its issuer\'s list ' +
               'as superseded without anybody asking — but this service ' +
               'CONSULTS no list of its own or anybody else\'s, so a ' +
               'certificate revoked here still authenticates here. Taking a ' +
               'key pair off an application is a third thing again: it stops ' +
               'this service ACCEPTING what that key signs and puts nothing ' +
               'on any list. The encoder is the parent ' +
               'project\'s own PKI code, vendored byte-identical, so a ' +
               'certificate issued here and one issued on its PKI / X.509 ' +
               'page are built by one encoder.' },
      // CERT ISSUANCE (2026-09-13 as *Certificate enrollment*; RENAMED AND
      // WIDENED 2026-09-22 at rcbj's ask): every protocol by which something
      // asks the certificate authority above for a certificate. It was the
      // three enrollment protocols, with SPIFFE a group of its own beneath
      // them; SPIFFE is IN it now, because an X509-SVID is a certificate this
      // realm's SPIFFE Issuing CA issues (`common/pki.js`) and a reader who
      // has just read PKI is looking for every way this service hands one
      // out, not for three of the four. Each page is still drawn by its own
      // family module (acme/, est/, scep/, spiffe/); the three enrollment
      // pages have a Monitoring twin, grouped there under the same heading.
      { title: 'Cert issuance',
        what: 'ACME, EST, SCEP and SPIFFE all issue from this realm\'s ' +
              'certificate authority. The three enrollment protocols issue ' +
              'to the person or application that authenticated — or, for a ' +
              'holder of Admin Write, to any entry in the realm — and keep ' +
              'every certificate on the entry it names; SPIFFE issues an ' +
              'X509-SVID to a WORKLOAD, against a registration entry rather ' +
              'than a directory identity.',
        items: [
          // ===== ACME section row (acme/acme_admin.ts) =====
          { path: '/admin/acme', label: 'ACME',
            blurb: 'Automatic Certificate Management Environment (RFC 8555): ' +
                   'the directory, accounts bound by External Account ' +
                   'Binding, orders, pre-validated authorizations and ' +
                   'revocation, issuing from this realm\'s ACME Issuing CA.' },
          // ===== EST section row (est/est_admin.ts) =====
          { path: '/admin/est', label: 'EST',
            blurb: 'Enrollment over Secure Transport (RFC 7030): cacerts, ' +
                   'simpleenroll, simplereenroll, serverkeygen and csrattrs, ' +
                   'authenticated by a password, a client secret or a ' +
                   'certificate this realm issued.' },
          // ===== SCEP section row (scep/scep_admin.ts) =====
          { path: '/admin/scep', label: 'SCEP',
            blurb: 'Simple Certificate Enrolment Protocol (RFC 8894): ' +
                   'GetCACaps, GetCACert and PKIOperation over CMS, the RA ' +
                   'certificate, and single-use challenge passwords issued ' +
                   'for one entry and one profile.' },
          // ===== SPIFFE's three pages (spiffe/spiffe_server.ts) =====
          { path: '/admin/spiffe', label: 'SPIFFE',
            blurb: 'The trust domain, the signing authority behind every ' +
                   'X509-SVID and JWT-SVID, the four sockets the Workload ' +
                   'API and the SPIRE Server API answer on, and how a caller ' +
                   'at the Workload API is identified — the ' +
                   '<code>transport:</code>, <code>endpoint:</code> and ' +
                   '<code>peer:</code> selectors, and whether an ASSERTED ' +
                   'one is believed. A caller on the Unix socket is ' +
                   'ATTESTED (unix, docker, k8s) and a caller over TCP is ' +
                   'not; an agent is attested by its node attestor.' },
          { path: '/admin/spiffe/entries', label: 'Registration entries',
            blurb: 'Which workload gets which SPIFFE ID, and what an SVID ' +
                   'issued against that entry carries. The store is the ' +
                   'embedded directory under ' +
                   '<code>ou=entries,ou=spiffe</code>, so a form here, an ' +
                   '<code>ldapmodify</code> and the SPIRE Server API\'s ' +
                   '<code>BatchUpdateEntry</code> are three doors onto one ' +
                   'entry, and nothing caches it — a change takes effect on ' +
                   'the next SVID.' },
          { path: '/admin/spiffe/agents', label: 'Agents',
            blurb: 'Every agent that has attested, with what it was given ' +
                   'and when. These entries are a RECORD rather than ' +
                   'configuration — this service wrote all of it when the ' +
                   'agent attested — which is why nothing on an agent is ' +
                   'editable and the ban is the only control.' },
        ] },
      { path: '/admin/tls', label: 'TLS / mutual TLS',
        blurb: 'The certificate the main port and LDAPS 636 present, ' +
               'regenerated on every start, and what this service makes of a ' +
               'CLIENT certificate presented to it. The names and addresses ' +
               'that certificate carries are settings here; whether the MAIN ' +
               'port is HTTPS at all is <code>global.https</code> on <a ' +
               'href="/admin/config">Configuration</a>, because it is a fact ' +
               'about the process rather than about this certificate. The ' +
               'two listeners of this service\'s own — 8443, and 9443 which ' +
               'required a client certificate — were deleted on ' +
               '2026-09-16.' },
      // Beside the certificate it configures, and UNGROUPED: a `TLS` heading
      // over `TLS / mutual TLS` would say the label twice.
      { path: '/admin/tls/trust', label: 'Client-certificate truststore',
        blurb: 'Every anchor the main port verifies a client ' +
               'certificate against (LDAPS 636 asks for none) — which, since ' +
               'a verified certificate is an identity here, is the list of ' +
               'whose certificates this service believes. Each row says ' +
               'whether it came from <code>tls.trustAnchorsFile</code> or was ' +
               'added while the process was running; add PEM certificates or ' +
               'remove one row at a time, in either mode. An anchor added ' +
               'here is kept in the directory (<code>ou=trustAnchors</code>), ' +
               'so it survives a restart and reaches every process, and there ' +
               'is deliberately no button that empties it.' }
    ] },
  { title: 'Directory',
    what: 'The embedded LDAP directory, and the identities this service has ' +
          'seen. The two are different questions and these pages keep them ' +
          'apart.',
    items: [
      { path: '/admin/users', label: 'Users',
        blurb: 'Every userid this service has been given as part of an ' +
               'interaction that SUCCEEDED, across all sixteen protocol ' +
               'families, with what each one holds. Click a name for the ' +
               'sessions they are signed in on, the tokens issued on each of ' +
               'those sessions, and the assertions, tickets and credentials ' +
               'issued to them. It also lists the subjects that were never ' +
               'here at all — an exchanged token names one, so does a ' +
               'WS-Trust <code>OnBehalfOf</code> and a Kerberos S4U request ' +
               '— and says so on the row. <strong>A person can be created ' +
               'here ahead of their first sign-in</strong>: the Create box ' +
               'on this page opens <em>New user</em>, where every attribute ' +
               'a person in this directory may carry is a field, only the ' +
               'username is required, a box left empty records NO VALUE, and ' +
               'somebody is given a way IN — a password you type, one ' +
               'generated and shown once, or a single-use activation link ' +
               'shown once at which they choose a password or a security key ' +
               'for themselves. The invented person this console used to ' +
               'create without asking is a button there rather than the ' +
               'default. Nobody is ever removed.' },
      // -------------------------------------------------------------------
      // `/admin/users/new` HAS NO ROW HERE, AND THAT IS A PLACEMENT RATHER
      // THAN AN OMISSION (2026-09-06).
      //
      // It had one for a day, between Users and Groups, and what that row
      // said in the sidebar was that creating a person is a PLACE in this
      // console rather than something you do to the list you are looking at.
      // It is the second: the page is reached from the Create box on
      // `/admin/users` — the box carries the typed name to it — and from the
      // *Create another* link on its own success page. Nothing else links to
      // it and nothing needs to.
      //
      // **THE RULE IT FOLLOWS IS THE ONE `sectionPages()` ALREADY IMPLIED**:
      // a row here is a DESTINATION, and a page that only ever makes sense
      // as the next step from another page is a DRILL-DOWN. Seven pages were
      // already in that position before this one — the two pictures, the
      // delegation chain, the SPIFFE and XACML drill-downs — and none of them
      // has a row either. Neighbouring sections make the same distinction
      // without needing it said: Groups and Roles both create from a form on
      // the list page and neither has a *New group* tab.
      //
      // Three things follow and each is done rather than assumed:
      //
      //   * **It is drawn with `active` = `/admin/users` and an `up`**, so
      //     the Users tab is marked (as a LINK, which is what `up` means to
      //     `navItem()`) and the trail reads
      //     `Admin console › Users › New user`. All seven `respond()` calls
      //     in that handler pass it, including the two refusals and the
      //     success page — a page that lost its trail on the one response
      //     that says "here is a password, once" would be the worst one to
      //     lose it on.
      //   * **`/admin` stops listing it.** `consoleGuide()` is derived from
      //     this table, so a page with no row here is absent from the
      //     Overview list as well as from the sidebar. That is why the Users
      //     blurb above now carries what this row used to say: the front door
      //     still describes the create flow, on the page it belongs to.
      //   * **`GET /admin-api/users/new` STAYS.** Rule 7 requires an
      //     operation for every console PAGE and says nothing against one for
      //     a drill-down; the catalogue it publishes is what a script builds
      //     a create from, and `tests/vendored/bulk_load.js` reads it.
      //     Deleting a working operation to tidy a table would be a
      //     regression dressed as consistency.
      // -------------------------------------------------------------------
      { path: '/admin/groups', label: 'Groups',
        blurb: 'Every group in the embedded LDAP directory, with how many of ' +
               'its members name an entry that is actually there. Click one ' +
               'for every attribute it holds and everybody in it, each ' +
               'member linked back to their row on Users. It reports the ' +
               'DIRECTORY rather than what this service has issued, and the ' +
               'one thing to know about it is that <strong>a group here ' +
               'grants nothing</strong>: no endpoint reads one and nothing ' +
               'decides anything on one. A token can CARRY one — see ' +
               '<code>groups.claim</code> — which is a different sentence. ' +
               'The two named on <a href="/admin/rbac">Admin roles</a> are ' +
               'the exception and grant exactly one thing: this console.' },
      { path: '/admin/roles', label: 'Roles',
        blurb: 'Who holds a role, and what requires one. A role is a name a ' +
               'person, a GROUP or an APPLICATION can be mapped into, and ' +
               'holding one is what this service decides an ISSUANCE on: an ' +
               'application entry names the roles it requires, and nothing ' +
               'is issued for it &mdash; no token, assertion, ticket or ' +
               'session &mdash; to somebody who holds none of them. The ' +
               'decision is made by the XACML PDP against a policy you can ' +
               'read and edit, never by an <code>if</code> in an issuance ' +
               'site. <strong>It is not <a href="/admin/rbac">Admin ' +
               'roles</a></strong>, which is two directory groups that grant ' +
               'this console and nothing else. An application that names no ' +
               'required role requires <code>EVERYBODY</code>, which ' +
               'everybody holds, so an unedited service refuses nobody.' },
      { path: '/admin/applications', label: 'Applications',
        blurb: 'Every relying party this service has been asked about — a ' +
               '<code>client_id</code> at the token endpoint, a ' +
               '<code>wtrealm</code> on a sign-in response, an entityID on ' +
               'an AuthnRequest — with what each one was given and when. An ' +
               'entry usually appears because an identifier was ACCEPTED, ' +
               'and one can also be created here ahead of the first ' +
               'connection, which is what RFC 9700 mode needs if it is to ' +
               'judge a client against its own redirect URIs rather than ' +
               'against a setting. A hand-made entry records that it was ' +
               'made by hand, so it cannot be mistaken for one that turned ' +
               'up once and never came back. <strong>There are TWO ways to ' +
               'add one and both are at the foot of this page</strong>: the ' +
               'short row, which takes an identifier and a name and nothing ' +
               'else, and the <em>New application</em> button beside it, ' +
               'where the PROTOCOL FAMILIES the entry is declared for are ' +
               'ticked from a closed list and its per-protocol identifiers ' +
               'and redirect URIs are typed. The declaration is a RECORD OF ' +
               'INTENT and nothing reads it — an application declared for ' +
               'SAML 2.0 alone is still issued an access token, because ' +
               'refusing would remove a test case rather than add one — but ' +
               'the redirect URIs and the secret beside it are what RFC 9700 ' +
               'mode judges the next request against.' },
      // -------------------------------------------------------------------
      // POLICIES (2026-09-12), asked for by rcbj as *Directory → Policies*,
      // with the password policy as the first kind of policy it configures.
      //
      // **IT IS IN DIRECTORY BECAUSE ITS STORE IS**: the profile is an entry
      // under `ou=passwordPolicies` in this realm's directory, like every
      // other page in this section draws a container. It is a DESTINATION
      // rather than a drill-down — nothing else in the console leads to it
      // as a next step — which is the test the two `/new` pages failed.
      //
      // **THE LABEL IS SHARED WITH `/admin/xacml/policies` AND
      // `/admin/ldap/policies`**, and the blurb says the difference on the
      // Overview page rather than leaving it to a reader: those draw
      // `ou=policies`, which holds documents a PDP evaluates, and this draws
      // profiles `credentials.setPassword()` checks. Different sections,
      // different containers; the label is the one rcbj asked for.
      // -------------------------------------------------------------------
      { path: '/admin/policies', label: 'Policies',
        blurb: 'The rules this realm holds a CREDENTIAL to, starting with ' +
               'the <strong>password policy</strong>: a minimum length, how ' +
               'many previous passwords may not be reused, how many symbols, ' +
               'and whether an uppercase letter and a number are required — ' +
               'the <em>default profile</em>, stored as ' +
               '<code>cn=default,ou=passwordPolicies</code> in this realm\'s ' +
               'directory. <strong>Enforced in product mode</strong>, at ' +
               'every door that sets a password: the console, ' +
               '<code>/admin-api</code>, the user portal, an activation link ' +
               'and an LDAP modify. A generated password is drawn to satisfy ' +
               'it in both modes. It is not the XACML policy repository, ' +
               'which is <a href="/admin/xacml/policies">Protocols → XACML → ' +
               'Policies</a>.' },
      // -------------------------------------------------------------------
      // `/admin/applications/new` HAS NO ROW HERE EITHER, and it is the
      // second page to leave this table on 2026-09-06. The rule and the three
      // consequences are written out beside the Users row above and are not
      // repeated; `admin-ui/CLAUDE.md` carries the argument.
      //
      // **IT IS A WEAKER CASE THAN `/admin/users/new`'s ONLY IN THAT IT IS
      // EVEN CLEARER.** That page is the ONLY way to create a person, so a
      // reader could at least argue the row was a destination. This one is
      // not even that: `/admin/applications` has carried a short *Add an
      // application* row since it grew its six actions, and both it and this
      // page POST to `/admin/applications` with `action=create` and reach one
      // `createApplication()`. So the sidebar was offering a tab for the
      // LONGER of two forms on one page — which is a fact about that page's
      // layout, not a place in this console.
      //
      // Its `respond()` count is ONE rather than seven, and that difference
      // is the same fact read again: this page posts to the list rather than
      // to itself, so a refusal and a success are the list page's 303 and
      // never a redraw here.
      // -------------------------------------------------------------------
      // -------------------------------------------------------------------
      // THE SECOND GROUP IN THIS CONSOLE, AND THE FIRST OUTSIDE PROTOCOLS.
      //
      // These five pages were `/ldap`, `/admin/ldap/directory`,
      // `/admin/ldap/applications`, `/admin/ldap/federations` and
      // `/admin/ldap/spiffe` until 2026-09-01 — five HTML pages outside the
      // console, in their own shell, with their own CSS, no sidebar, no
      // breadcrumb, no realm switcher and no gate. Every one of them shows what
      // is in this service's directory, which is what the four pages above them
      // show; the only thing that made them a separate surface was where they
      // happened to have been written. They are drawn by `ldap/ldap_server.js`
      // still — a page here is a `path` and a `label` whoever builds it,
      // exactly as `/admin/sts-metadata` is — and they answer in this shell
      // now.
      //
      // THEY ARE A GROUP AND NOT EIGHT MORE ITEMS, and the test is the one
      // stated for SAML above: does the heading name more than the page under
      // it does? It does. The four pages above are each ONE KIND OF THING
      // this service has seen, drawn the way the console draws things; these
      // five are the STORE UNDERNEATH all four, entry by entry and attribute
      // by attribute, which is a different question and one a reader either
      // wants or does not. Ungrouped they would have doubled the length of
      // this section with rows that read as alternatives to Users and
      // Applications rather than as the layer beneath them.
      //
      // THREE MORE JOINED THEM ON 2026-09-05 — `ou=roles`, `ou=policies` and
      // `ou=peps`. Each of those containers' owning modules published a
      // SCHEMA whose comment said it was drawn on a page under
      // `/admin/ldap/*`, and for three of them no such page had ever been
      // written: the export was dead in `common/roles.js` since that
      // afternoon, in `xacml/xacml_store.ts` since XACML phase two and in
      // `xacml/xacml_pep_registry.ts` since phase five. They are drawn by
      // `ldap/ldap_server.js` like the other five, for the reason stated
      // there.
      //
      // WHAT MOVING THEM COST is that they are GATED now: they are `/admin`
      // pages, so the console gate applies and a reader needs a session
      // and a role. That is a real change and it is the right one — a dump of
      // every attribute of every entry includes `oauthClientSecret` and
      // `fedClientSecret` in the clear, and it was the one surface in this
      // service printing those to anybody who could reach the port while the
      // console next door asked for a role to show far less. `/admin-api`
      // mirrors all eight, behind its access token since 2026-09-09, and that
      // is what a test drives.
      { title: 'As the directory holds it',
        what: 'The store underneath the five pages above: every entry, every ' +
              'attribute, and the vocabulary each container uses.',
        items: [
          { path: '/admin/ldap/directory', label: 'Every entry',
            blurb: 'The whole store, DN by DN, with where each entry came ' +
                   'from — seeded, added over LDAP, or created because ' +
                   'somebody authenticated — and every attribute it holds ' +
                   'with every value. It is not LDAP: it is this service ' +
                   'showing its own store, which is how a reader tells an ' +
                   'empty directory from a search filter that matched ' +
                   'nothing. A value too long for its column is shortened ' +
                   'and the whole of it is one hover away.' },
          { path: '/admin/ldap/applications', label: 'Application entries',
            blurb: 'The same applications <a href="/admin/applications">' +
                   'Applications</a> lists, as the DIRECTORY holds them: one ' +
                   'entry per identifier under <code>ou=applications</code>, ' +
                   'every attribute on it, and the published SCHEMA — the ' +
                   'object classes and every attribute name with what sets ' +
                   'it. That schema is why this page exists rather than ' +
                   'being a column on the other one: this directory is ' +
                   'schemaless, so an entry carrying thirty invented ' +
                   'attribute names needs somewhere to say what they mean or ' +
                   'a client reading one back is guessing.' },
          { path: '/admin/ldap/federations', label: 'Federation entries',
            blurb: 'The application registry\'s twin, for ' +
                   '<code>ou=federations</code> — and the one container in ' +
                   'this directory where an <code>ldapmodify</code> is a ' +
                   'SECURITY change. Everywhere else an edit changes what ' +
                   'this service hands out; ' +
                   '<code>fedSigningCertificate</code> decides whose ' +
                   'assertions it will BELIEVE and <code>fedEnabled</code> ' +
                   'turns a partner on. It publishes the schema with a ' +
                   'column the applications page has no need of: which ' +
                   'DIRECTION each attribute is for.' },
          { path: '/admin/ldap/roles', label: 'Role entries',
            blurb: 'The membership half of the role register, as the ' +
                   'directory holds it: one entry per role under ' +
                   '<code>ou=roles</code>, and a person, a GROUP and an ' +
                   'APPLICATION are all first-class members of one. The ' +
                   'other half is not in that container — which roles an ' +
                   'application DEMANDS is <code>appRequiredRole</code> on ' +
                   'the application\'s own entry — so nothing here refuses ' +
                   'anybody by itself. It also lists the six BUILT-IN roles, ' +
                   'which are computed and in no container at all: an empty ' +
                   '<code>ou=roles</code> is the ordinary state of a service ' +
                   'deciding every issuance against <code>EVERYBODY</code> ' +
                   'and refusing nobody.' },
          { path: '/admin/ldap/policies', label: 'Policy entries',
            blurb: 'The XACML policy repository as the directory holds it: ' +
                   'one entry per policy or policy set under ' +
                   '<code>ou=policies</code>, holding the document itself, ' +
                   'with exactly one of them the root. <strong>A write here ' +
                   'skips the typechecker</strong>, which is not true of any ' +
                   'other door into this repository \u2014 every write ' +
                   'through <a href="/admin/xacml">XACML</a> is statically ' +
                   'validated so that a policy which does not typecheck is ' +
                   'refused rather than answering Indeterminate on every ' +
                   'request, and an <code>ldapmodify</code> reaches the ' +
                   'entry directly.' },
          { path: '/admin/ldap/peps', label: 'PEP entries',
            blurb: 'The remote Policy Enforcement Points that have ' +
                   'registered with this PDP, under <code>ou=peps</code>. ' +
                   'Almost every attribute is a RECORD this service wrote ' +
                   'rather than configuration \u2014 an identity here was ' +
                   'taken from the CLIENT CERTIFICATE the PEP presented and ' +
                   'never from the body it sent. An empty container is not a ' +
                   'feature that is off: a PEP pulls the repository and ' +
                   'converges whether or not it ever registers, and ' +
                   'registering is what buys it the change nudge.' },
          { path: '/admin/ldap/spiffe', label: 'SPIFFE entries',
            blurb: 'The two SPIFFE containers as the directory holds them. ' +
                   '<code>ou=entries</code> is CONFIGURATION — which SPIFFE ' +
                   'ID a workload gets, under which parent, matching which ' +
                   'selectors — and <code>ou=agents</code> is a RECORD of ' +
                   'what has attested, which is why nothing about an agent ' +
                   'is editable anywhere. The entries ARE the registry: ' +
                   'nothing caches them, so an <code>ldapmodify</code> of ' +
                   '<code>spiffeX509SvidTtl</code> changes the lifetime of ' +
                   'the next SVID the Workload API hands out.' },
          { path: '/admin/ldap/service', label: 'The directory service',
            blurb: 'The two raw sockets and the store behind them, as they ' +
                   'actually are right now rather than as they are ' +
                   'configured: whether TCP 389 and LDAPS 636 really bound ' +
                   '(this page is HTTP and answers either way, so it is the ' +
                   'only way to tell a running listener from one whose port ' +
                   'was taken), how many entries are held, whether any of it ' +
                   'survives a restart, the bind policy, and the four ' +
                   'structural rules this directory does still enforce. What ' +
                   'the sockets are SET to is ' +
                   '<a href="/admin/ldap">LDAP / LDAPS</a> under Protocols.' }
        ] }
    ] },
  { title: 'Monitoring',
    what: 'What this service has done: how much of it, what came out, and ' +
          'what happened in order.',
    items: [
      { path: '/admin/metrics', label: 'Metrics',
        blurb: 'Every endpoint call by route and status class, every token ' +
               'and artifact this service has issued with how many are still ' +
               'valid, and sessions counted BOTH ways: the browser sign-on ' +
               'sessions this service really holds, and the sessions implied ' +
               'by what it has issued. Every figure is computed when the ' +
               'page is drawn rather than kept up to date as things happen, ' +
               'because "valid" and "expired" are functions of the clock.' },
      // BEFORE Tokens and not after it, and the order is the argument: a
      // session is the thing that is live NOW and a token is what came out of
      // one, so a reader working out what is going on reads them in that
      // direction. It sits under Metrics because that page COUNTS sessions
      // two ways and names none of them — this is the list behind the first
      // of those two figures.
      { path: '/admin/sessions', label: 'Sessions',
        blurb: 'Every session this service is holding RIGHT NOW, across the ' +
               'three protocols that have one: the browser sign-on session ' +
               'every browser family here shares, the Kerberos ' +
               'ticket-granting ticket (a TGT IS the Kerberos session), and ' +
               'the LDAP connection (RFC 4511 makes the Bind a state of the ' +
               'CONNECTION, so in LDAP the connection is the session). Who ' +
               'is signed in, through which protocol, what it carries, when ' +
               'it expires &mdash; and <strong>how that expiry is worked ' +
               'out, which is different in all three</strong>. Each row ' +
               'links to the credentials issued on it and carries a Revoke ' +
               'button that goes through the same termination ' +
               '<a href="/logout">the protocol-independent sign-out</a> ' +
               'uses. What this service has HANDED OUT is ' +
               '<a href="/admin/tokens">Tokens</a>: those outlive every ' +
               'session here.' },
      { path: '/admin/tokens', label: 'Tokens',
        blurb: 'Everything issued and still remembered, in ONE table: every ' +
               'JWT, every SAML assertion (WS-Trust\'s, WS-Federation\'s and ' +
               'both browser profiles\' alike) and every Kerberos ticket, ' +
               'newest first — one table rather than three because a ' +
               'sign-in that produced an ID Token and an assertion is one ' +
               'event. And the buttons that invalidate the ones that can be: ' +
               'one access token, one ID Token, one refresh token, ' +
               'everything for one subject, or everything of one kind. ' +
               'Revocation here is the SAME revocation RFC 7009\'s ' +
               '<code>/oauth2/revoke</code> performs, so introspection, ' +
               'UserInfo and the refresh grant all honour it.' },
      // IMMEDIATELY AFTER TOKENS, AND THE PAIR IS THE ARGUMENT: that page is
      // what this service HANDED OUT and this one is what it was HANDED and
      // spent. Filed under Monitoring rather than beside the RFC 7523 and RFC
      // 7522 settings under Protocols, because it answers "has this assertion
      // been used, by whom, and until when is that remembered" — a question
      // about what happened, which is this section's heading.
      { path: '/admin/used-assertions', label: 'Used assertions',
        blurb: 'Every RFC 7523 JWT and RFC 7522 SAML assertion this realm ' +
               'has ACCEPTED — as client authentication or as an ' +
               'authorization grant — and that has not yet expired. <strong>' +
               'Each is accepted once, ever</strong>: one history for both ' +
               'uses and both profiles, persisted in whatever store is open ' +
               'so a restart forgets nothing, and on postgres claimed ' +
               'atomically so no two processes accept one assertion. An ' +
               'assertion is spent only when the token request it came with ' +
               'issued tokens; a request that failed for another reason ' +
               'releases it. A row is kept until the assertion would have ' +
               'expired and not a moment longer. The <code>jti</code> of ' +
               'every RFC 9101 request object something was issued on is ' +
               'kept here too, as a request object. No control: forgetting ' +
               'a row would make a still-valid assertion usable again.' },
      // Beside the tokens it points at rather than under Protocols, and that
      // was the decision: delegation is the one feature here that is
      // deliberately NOT a protocol family — six of its eight mechanisms come
      // from three different families and the whole value is reading them
      // against each other in one table. Under Protocols it would have had to
      // be filed under one of the three.
      { path: '/admin/delegation', label: 'Delegation',
        blurb: 'Who acted on whose behalf, through what, to reach what: ' +
               'eight mechanisms from three protocol families — Kerberos\'s ' +
               'S4U2Self, two flavours of S4U2Proxy and a forwarded TGT, ' +
               'WS-Trust\'s <code>OnBehalfOf</code> and <code>ActAs</code>, ' +
               'and RFC 8693\'s impersonation and delegation — against ONE ' +
               'model, because the question a reader arrives with is ' +
               'protocol-independent. Beside them, who MAY delegate to whom, ' +
               'which only Kerberos polices; that asymmetry is the most ' +
               'useful thing on the page. ' +
               '<a href="/admin/delegation/map">The picture</a> draws the ' +
               'same acts as a graph, laid out on the SERVER because this ' +
               'console runs no script.' },
      // THE SHARED SIGNALS PAGES ARE A GROUP (2026-09-14), and the fourth is
      // what made them one. CAEP sessions, RISC accounts and Signals received
      // were three loose rows in this section, each argued into Monitoring on
      // its own; a dead-letter page made four answers to one family's "what
      // happened", and four rows a reader has to recognise as one family is
      // what a group is for. The group is filed where the pages were, beside
      // Delegation. Where each page is filed is still decided by the question
      // it answers: Protocols → Shared Signals is what the streams are
      // CONFIGURED to do and holds every control on them; everything here is
      // what happened. The paths did not move, so nothing but this table
      // changed for the three that were already here.
      { title: 'Shared Signals',
        what: 'What this service has said about sessions and accounts, what ' +
              'its own console has been told, and what it could not deliver ' +
              '— in the realm being read.',
        items: [
          // Beside Delegation and for the reason Delegation is beside the
          // tokens it points at: this is an OBSERVATION and not a
          // configuration. The CAEP settings are at /admin/caep under
          // Protocols, and putting the table there with them would have buried
          // the one thing on it no other page in this console can show — a
          // session this service NO LONGER HOLDS, and what was said about it on
          // its way out.
          { path: '/admin/caep-sessions', label: 'CAEP sessions',
            blurb: 'One row per session this service has held, ' +
                   '<strong>including the ones it no longer holds</strong>, ' +
                   'with the CAEP state it is in &mdash; established, ' +
                   'presented, revoked &mdash; its assurance level, its ' +
                   'device compliance, its risk level, and a count of every ' +
                   'CAEP event type sent about it. The register outliving ' +
                   'the session is the point: the session store forgets one ' +
                   'the moment it is signed out, so a row saying ' +
                   '<code>revoked</code> is the only remaining evidence ' +
                   'that it existed and was revoked. Beside it, which ' +
                   'streams would take a CAEP event at all &mdash; because ' +
                   'a count of zero almost always means nobody asked for ' +
                   'that type, and SSF gives a receiver no other notice of ' +
                   'that.' },

          // And the ACCOUNT register beside the session one. Two pages rather
          // than two tables on one, because a session and an account are not
          // the same kind of thing: a session begins, is used and ends and
          // there are many per person, and an account IS the person and
          // outlives every session on it. One page would have had a first
          // column that was sometimes one and sometimes the other.
          { path: '/admin/risc-accounts', label: 'RISC accounts',
            blurb: 'One row per account this service has been told anything ' +
                   'about, <strong>including accounts that no longer ' +
                   'exist</strong>, with the three states RISC tracks: the ' +
                   'lifecycle (active, disabled, purged), the opt-out ' +
                   'state, and whether a credential has been reported ' +
                   'compromised. They move independently &mdash; an account ' +
                   'can be opted out and perfectly healthy &mdash; which is ' +
                   'why they are three columns rather than one word. The ' +
                   'register outliving the account is starker than the CAEP ' +
                   'one outliving a session: a purged account is gone from ' +
                   'the directory entirely, so the row is the only ' +
                   'remaining evidence that anybody was told. Beside the ' +
                   'counts, the events this transmitter built and ' +
                   'deliberately did NOT send, because the account had ' +
                   'opted out.' },

          // THE RECEIVER'S PAGE, after the transmitter's two registers. The
          // Shared Signals settings, the streams and every other receiver's
          // stream are at Protocols → Shared Signals, which is where somebody
          // goes to change what arrives here rather than to read it.
          //
          // **IT IS THE ONE PAGE IN THIS CONSOLE ABOUT SOMETHING THIS CONSOLE
          // WAS SENT.** Every other page here reads a store this process
          // holds; this one reads a queue that was delivered to it over HTTP,
          // signed, addressed to it by name, which it verified. That is the
          // whole difference between a console showing its own notes and an
          // application that is a receiver.
          { path: '/admin/signals', label: 'Signals received',
            blurb: 'Every Security Event Token this console has been ' +
                   'DELIVERED, in the realm being read. This console is a ' +
                   'registered Shared Signals receiver with a stream of its ' +
                   'own (<code>sts-admin-console</code>), seeded at ' +
                   'startup, asking for every CAEP and every RISC event ' +
                   'type — so what is on this page arrived over RFC 8935 ' +
                   'push at <code>/admin/signals/receive</code>, carrying ' +
                   'the stream\'s own bearer token, and was verified ' +
                   'against this service\'s signing key before it was ' +
                   'recorded. Each row opens out into the SET as it ' +
                   'arrived. <strong>An empty page has five causes and only ' +
                   'one of them is &ldquo;nothing has ' +
                   'happened&rdquo;</strong>, so the ones that apply are ' +
                   'named at the top rather than left to be guessed: the ' +
                   'transmitter off, the receivers off, the stream deleted, ' +
                   '<code>ssf.pushDelivery</code> off, or a vocabulary ' +
                   'turned off under it. What a PERSON sees about ' +
                   'themselves is the same delivery to a different ' +
                   'receiver, at <a href="/portal/signals">the user ' +
                   'portal</a>.' },

          // LAST IN THE GROUP, because it is what the other three cannot show:
          // every one of them is full and correct while a SET is failing to
          // reach its receiver. Its own page rather than a table on
          // /admin/ssf, which already draws each stream's letters in that
          // stream's card: the questions somebody arrives with during an
          // incident — how many, since when, why, which streams, still
          // happening? — are counts over every stream at once. Read-only; the
          // Revive and Drop controls stay on the stream's card, linked from
          // each row. `ssf/ssf_dead_letter_report.ts` computes it.
          { path: '/admin/ssf/dead-letters', label: 'Dead letters',
            blurb: 'Every Security Event Token this realm\'s transmitter ' +
                   'could not deliver and is still holding, counted: how ' +
                   'many, <strong>when</strong> (a timeline over ' +
                   '<code>ssf.deadLetterRetentionS</code>), <strong>why' +
                   '</strong> &mdash; a push that failed, the push backlog ' +
                   'full, a stream declared dead, a SET sent to a dead ' +
                   'stream &mdash; by error code, by the receiver\'s HTTP ' +
                   'status and by event type, and which streams are dead, ' +
                   'half-open or failing. Then the letters themselves, ' +
                   'searched and paged, without their tokens. Two things on ' +
                   'it are <strong>per process</strong> and say so: the push ' +
                   'cap, which every realm shares, and the recent sweeps. ' +
                   'Nothing here resends or drops anything; those controls ' +
                   'are on each stream at ' +
                   '<a href="/admin/ssf">Protocols &rarr; Shared Signals</a>.' }
        ] },

      // Beside Delegation and not inside it, and the argument is the one both
      // of that page's pictures rest on: every row there is about two
      // APPLICATIONS, and every row here has a PERSON in it. Merging them would
      // put "webapp1 may reach the API" and "alice let webapp1 read her
      // profile" under one heading, which are the two halves of the question a
      // reader arrives with and are answered by two different registers.
      { path: '/admin/consent', label: 'Consent',
        blurb: 'What a person agreed an application may ask for on their ' +
               'behalf. The authorization endpoint asks the first time a ' +
               'given username signs in to a given <code>client_id</code> ' +
               'for a given scope, and issues nothing until they answer — ' +
               '<code>oauth2.consentRequired</code>, which is ON by default ' +
               'and is the one policy here that is. Answers are ordinary ' +
               'attributes on ordinary entries: <code>oauthConsent</code> on ' +
               'the person, one value per (person, application, scope). ' +
               'Beside them the OVERRIDE — <code>oauthGlobalConsent</code> ' +
               'on an application\'s entry, which skips the prompt for ' +
               'everybody and writes nothing about anybody, so taking one ' +
               'away asks everybody again.' },
      // AFTER CONSENT AND BEFORE SIGN-OUT, and the pair either side is the
      // argument. Consent is what a person AGREED an application may ask for;
      // this is what the policy DECIDED when something asked. Both are records
      // of a permission question already answered, which is what this
      // section's heading says and what kept it out of the XACML group under
      // Protocols: every other page in that group is CONFIGURATION — the
      // settings, the repository, the editor, the registered enforcement
      // points, the what-if — and this one is the only one about TRAFFIC.
      // A reader asking "why was that request refused" is asking a monitoring
      // question, and the durable half of the answer is the Audit log two rows
      // down. It is drawn by `xacml/xacml_admin.ts` and still lives under
      // `/admin/xacml/`; a console page is a `path` and a `label` in this
      // table whoever builds the body.
      { path: '/admin/xacml/monitor', label: 'XACML decisions',
        blurb: 'What authorization is actually DOING rather than how it is ' +
               'configured: how many decisions are being made, by which ' +
               'enforcement point, and how many of them are refusals. Every ' +
               'PEP is on it &mdash; the three EMBEDDED in this process and ' +
               'every REMOTE one that has registered &mdash; with its own ' +
               'figures. Two things it keeps apart on purpose: <strong>a ' +
               'decision is not an enforcement</strong>, because a ' +
               'deny-biased PEP refuses a NotApplicable that a permit-biased ' +
               'one allows and an undischargeable obligation turns a Permit ' +
               'into a refusal; and <strong>a remote PEP\'s numbers are ' +
               'reported by it</strong> rather than seen here, so the totals ' +
               'are given as here, remote and the sum. The counters are in ' +
               'memory and start with the process; the durable record of a ' +
               'refusal is the <a href="/admin/audit">audit log</a>, and the ' +
               'policies these decisions are made with are under ' +
               '<a href="/admin/xacml">Protocols</a>.' },
      // GNAP's traffic, filed here beside XACML's for the same reason: what the
      // applications using it have DONE is a monitoring question, and the
      // configuration is on Protocols -> GNAP.
      { path: '/admin/gnap/monitor', label: 'GNAP grants',
        blurb: 'Every application that uses GNAP &mdash; client instances ' +
               'and resource servers, declared or seen &mdash; with what ' +
               'each has done since the process started: grants requested, ' +
               'approved and denied, tokens issued by format, rotations, ' +
               'revocations, failed key proofs, introspections and ' +
               'registrations, and the GNAP error codes it was answered ' +
               'with. No reset: the durable record is the Audit log.' },
      // CERT ISSUANCE TRAFFIC (2026-09-13; GROUPED 2026-09-22 at rcbj's ask,
      // under the heading its Protocols counterpart carries). Three flat rows
      // sat here beside the other traffic pages, which read as three
      // unrelated protocols rather than as one question — *what has been
      // issued, and to whom* — asked over three wire formats. **SPIFFE is
      // NOT in this group**, unlike the Protocols one: it has no monitoring
      // page, and a heading listing three of four families is the drift a
      // reader cannot see. Adding one puts it here.
      { title: 'Cert issuance',
        what: 'What each enrollment protocol has done in this realm — the ' +
              'requests, the certificates issued, and the refusals with the ' +
              'code each was answered with. The durable record is the Audit ' +
              'log; these counters are this process\'s own.',
        items: [
          // ===== ACME monitoring row =====
          { path: '/admin/acme/monitor', label: 'ACME enrollments',
            blurb: 'What the ACME server has done in this realm: requests ' +
                   'by operation, certificates issued and revoked, refusals ' +
                   'by error code, the profiles asked for, the accounts and ' +
                   'EAB keys that asked, and the most recent requests.' },
          // ===== EST monitoring row =====
          { path: '/admin/est/monitor', label: 'EST enrollments',
            blurb: 'What the EST server has done in this realm: requests by ' +
                   'operation, certificates issued (and server-generated ' +
                   'keys), refusals by error code, the profiles asked for, ' +
                   'who authenticated and how, and the most recent ' +
                   'requests.' },
          // ===== SCEP monitoring row =====
          { path: '/admin/scep/monitor', label: 'SCEP enrollments',
            blurb: 'What the SCEP server has done in this realm: GetCACaps, ' +
                   'GetCACert and PKIOperation counts, certificates issued, ' +
                   'challenges created and redeemed, refusals by error code ' +
                   'and failInfo, and the most recent requests.' }
        ] },
      // THE AUTHORIZATION SERVER'S OWN TRAFFIC (2026-09-13), filed here and
      // not under Protocols beside `/admin/oauth2` for the XACML monitor's
      // reason: that page is what the server is CONFIGURED to do and this is
      // what it has DONE. Drawn by `oauth-oidc/oauth2_monitor_admin.ts`; a
      // console page is a `path` and a `label` in this table whoever builds
      // the body. It is in SECTIONS, one per mechanism, so the next OAuth
      // mechanism counted is a section of it rather than a row here.
      { path: '/admin/oauth2/monitor', label: 'OAuth 2.0 / OIDC activity',
        blurb: 'What the authorization server has done in this realm, one ' +
               'section per mechanism. RFC 9126 pushed authorization ' +
               'requests first: pushes and refusals, request_uris read, ' +
               'spent, expired and refused at the authorization endpoint, ' +
               'per client with the OAuth errors returned — and every ' +
               'pushed request the store still holds, with a Withdraw ' +
               'button on each. No reset.' },
      // AFTER THE XACML MONITOR AND BEFORE SIGN-OUT, and the pair either
      // side is the argument again: the page above it counts authorization
      // DECISIONS and this one counts PROVISIONING CALLS, and both are the
      // same kind of thing — traffic, in memory, since the process started,
      // with the audit log two rows down as the durable half. It is drawn by
      // `admin.ts` itself and lives under `/admin/scim/`; a console page is a
      // `path` and a `label` in this table whoever builds the body and
      // whatever path space it sits in, which is the rule
      // `/admin/xacml/monitor` established and `/admin/sts-metadata` has had
      // since 2026-08-24.
      //
      // IT IS NOT UNDER PROTOCOLS WITH `/admin/scim`, for that page's own
      // reason: where a page is filed is decided by the QUESTION IT ANSWERS.
      // `/admin/scim` answers "what is this surface" — the schemes, the
      // endpoints, the attribute mapping, the eighteen settings. This one
      // answers "how much traffic is there, from whom, and how much of it is
      // failing", which is what somebody asks when a provisioning client is
      // misbehaving rather than when it is being set up.
      { path: '/admin/scim/monitor', label: 'SCIM metrics',
        blurb: 'What the provisioning surface has actually been asked to do: ' +
               'how many calls, how many worked, how many failed, and ' +
               '<strong>who is calling</strong> &mdash; one row per ' +
               'authenticated principal, with what it used and what it ' +
               'touched. Broken down by API call type with the latency and ' +
               'the bytes each returned, by resource type, by authentication ' +
               'scheme (including the ones at zero, because a scheme that is ' +
               'OFF is the most useful row for somebody asking why a client ' +
               'cannot get in), and by what went back &mdash; status class, ' +
               'status and <code>scimType</code>. Two things it is careful ' +
               'about: <strong>a client is an authenticated principal, not a ' +
               'connection</strong>, because SCIM is stateless HTTP and has ' +
               'nothing to be connected; and <strong>a caller the gate ' +
               'refused is not a client</strong> and appears in no row, even ' +
               'when the credential carried a name. The counters are in ' +
               'memory, per trust realm, and start with the process; the ' +
               'durable record is the <a href="/admin/audit">audit log</a>, ' +
               'and what the surface IS lives on ' +
               '<a href="/admin/scim">Protocols &rarr; SCIM</a>.' },
      // Beside the tokens page rather than under Protocols, and for the same
      // reason delegation is: signing somebody out is deliberately NOT a
      // protocol family. It reaches nine stores across six families and the
      // whole value is doing all of them at once, so filing it under one would
      // be filing it under the wrong one. It is an ACTION page in a section
      // whose heading says "what this service has done" — which /admin/tokens
      // already is, since revoking is a control and that page has four of them.
      { path: '/admin/logout', label: 'Sign-out',
        blurb: 'Name an identity to see everything this service is still ' +
               'holding for them — every browser sign-on session, every ' +
               'token it can still revoke, every outstanding authorization ' +
               'code and credential offer, every directory connection bound ' +
               'as them, and the Kerberos sign-out instant — and to end any ' +
               'of it. It is <a href="/logout">/logout</a> done to somebody ' +
               'else: the same nine stores through the same functions, ' +
               'except that the front-channel notifications cannot be ' +
               'delivered from here. The back-channel Logout Tokens are, and ' +
               'the page lists where each got to.' },
      // AFTER the traffic pages and BEFORE the audit log, and the order is
      // the same widening-detail argument the rest of this section follows:
      // Metrics says how much, the pages between say what came out, this says
      // what was SEALED on the way to the store, and the audit log says what
      // happened in order. It is the last page here whose subject is state
      // rather than history.
      //
      // **IT IS NOT A SECOND `/admin/crypto-metadata`**, which is the filing
      // question somebody will ask: that page is what this service DOES when
      // it signs or encrypts, is identical on a service that started a second
      // ago, and is filed under Protocols with the rest of what this service
      // IS. The numbers on this one go up while a reader watches.
      // AFTER Encryption and before the audit log, on this section's
      // widening-detail order: Encryption is what this service does to what
      // it writes down, and this is what the thing it writes down is DOING.
      //
      // **IT IS NOT A SECOND HALF OF `/admin/persistence`**, which is the
      // filing question somebody will ask: that page is under Settings and
      // answers what this service is CONFIGURED to write down and where,
      // reads the same on a service that started a second ago, and owns the
      // eighteen `persistence.*` settings. The numbers on this one move while
      // a reader watches.
      { path: '/admin/database', label: 'Database',
        blurb: 'Everything PostgreSQL will tell this service about itself ' +
               '&mdash; commits and rollbacks, the buffer cache hit ratio, ' +
               'every backend and every lock, the background writer, the ' +
               'checkpointer and the write-ahead log &mdash; beside the ' +
               'state of the schema this service owns in it: per-table and ' +
               'per-index statistics, sizes, columns, constraints, and which ' +
               'indexes nothing has ever scanned. <strong>The shape of the ' +
               'page is decided by the server it is pointed at</strong>: ' +
               'every statement behind it asks for all of a view\'s columns, ' +
               'because PostgreSQL moves them between major versions and a ' +
               'page naming its own would be wrong on every server but one. ' +
               'A probe the service\'s least-privilege role may not read ' +
               'costs a ROW here and not the page. Empty, with a sentence ' +
               'saying which of three reasons it is, unless ' +
               '<code>persistence.mode</code> is <code>postgres</code>.' },
      { path: '/admin/encryption', label: 'Encryption',
        blurb: 'What this service encrypts AT REST &mdash; its own signing ' +
               'keys, the certificate authority, the assertion key pairs it ' +
               'issues to applications, authenticator secrets, recovery ' +
               'codes and, in product mode on postgres, everything it mints ' +
               '&mdash; with the key that protects it, the algorithm, and ' +
               'how many encryptions and decryptions have happened in this ' +
               'process. It lists what is DELIBERATELY not sealed beside ' +
               'what is, because the question a reader brings is almost ' +
               'always &ldquo;is <em>this</em> encrypted&rdquo; and a table ' +
               'of only the yeses answers it by silence. No sealed value and ' +
               'no opened one appears on it, and it has no control: rotating ' +
               'the key-encryption key is a deployment act, and a ' +
               'decrypt-this button would be the one door onto material no ' +
               'door is supposed to have.' },
      // AFTER Encryption and before the audit log, and the order is this
      // section's widening-detail argument once more: Encryption is what this
      // service seals and with which key, and this is where that key CAME
      // FROM and what the thing holding it is doing. It is the last page here
      // whose subject is a system somebody else is running.
      //
      // **IT IS NOT A SECOND HALF OF `/admin/config`'s Key material group**,
      // which is the filing question somebody will ask: those rows say what
      // this service is CONFIGURED to read and where, and read identically on
      // a service that started a second ago. Everything on this page can be
      // broken while every one of them is right.
      { path: '/admin/secrets', label: 'Secret store',
        blurb: 'Where this service\'s two primordial secrets come from ' +
               '&mdash; the key-encryption key everything it seals is sealed ' +
               'under, and the database password &mdash; whether this ' +
               'process actually read them, and what the store at the other ' +
               'end is doing. For a mounted file that is its path, mode, ' +
               'owner and mtime, and which members are in it if it holds ' +
               'JSON. For HashiCorp Vault or OpenBao it is the whole state ' +
               'the store will publish: initialised, sealed or unsealed, the ' +
               'seal type, the version and build, the cluster and its ' +
               'leader, the certificate this service authenticates with and ' +
               'when it expires, the policies the token it got carries, ' +
               '<strong>what that identity may actually do asked of the ' +
               'store rather than quoted from a policy file</strong>, and ' +
               'every version of the secret the store has kept. For AWS, GCP ' +
               'and Azure it is the metadata each of them publishes &mdash; ' +
               'rotation, the KMS key, version states and staging labels ' +
               '&mdash; through a DESCRIBE rather than a get. <strong>No ' +
               'secret value appears on it, no probe fetches one, and it has ' +
               'no control</strong>: no reveal, no rotate, no test-read. A ' +
               'probe refused with 403 is usually the read-only policy ' +
               'working and is drawn as a row saying so.' },
      // THE CACHES (#74, 2026-09-17), after the secret store and before the
      // audit log: one more page whose subject is the process itself, and
      // the last of them that is state rather than history. Drawn by
      // `admin-ui/caches_admin.ts` out of `common/cache_registry.js`.
      { path: '/admin/caches', label: 'Caches',
        blurb: 'Every cache this service holds in memory &mdash; CRLs and ' +
               'OCSP answers, signed metadata, fetched request objects, ' +
               'parsed policies, the directory\'s indexes, decrypted and ' +
               'derived keys &mdash; with its size against its bound, how ' +
               'many entries are still valid and how many have expired but ' +
               'not yet been evicted, and the hit ratio since the process ' +
               'started. Open one to see its entries, each with how long it ' +
               'is still valid. <strong>Keys only, never values</strong>, ' +
               'and no control: a cache is emptied by the settings that ' +
               'bound it, not by a button. The figures are the answering ' +
               'process\'s own.' },
      // THE SCHEDULER (2026-09-22, #49), after the caches and before the
      // audit log: the last page whose subject is the process itself, and the
      // one that says whether the background work is being DONE. Drawn by
      // `admin-ui/scheduler_admin.ts` out of `cluster/scheduler.ts`.
      { path: '/admin/scheduler', label: 'Scheduler',
        blurb: 'Every periodic job this service runs &mdash; the ' +
               'session-expiry sweep, the CRL directory refresh, and every ' +
               'job registered after them &mdash; with its schedule, its ' +
               'last run and how it ended, and the time to its next run as ' +
               'a duration and an absolute time in UTC by the database\'s ' +
               'clock. <strong>A cluster job runs once for the whole ' +
               'service</strong>, on the scheduler\'s leader, which the ' +
               'page names; a per-process job has a row per process. ' +
               'Admin Write may run a job now, and on a cluster may ask ' +
               'the leader to hand the scheduler to another node.' },
      // RISK (#62, 2026-09-22), after the scheduler and before the audit log:
      // the external datasets a risk score reads and the refused passwords it
      // counts. Drawn by `admin-ui/risk_admin.ts` out of `risk/`.
      { path: '/admin/risk', label: 'Risk',
        blurb: 'The external datasets a risk score reads &mdash; ' +
               'geolocation, the network an address belongs to, Tor exits, ' +
               'IP reputation and an operator\'s own allow and deny lists ' +
               '&mdash; each with its active version, whether it is fresh, ' +
               'and every version loaded or refused. Look up what they say ' +
               'about an address, import a list, activate, roll back. ' +
               'Below, every refused password in the realm, attributed to a ' +
               'person and a network and never to a typed name.' },
      { path: '/admin/audit', label: 'Audit log',
        blurb: 'What this service was ASKED to do, in the order it was ' +
               'asked, newest first. Every other page here is state; this ' +
               'one is history — Metrics can say the directory holds eleven ' +
               'entries, and only this page can say that a twelfth was ' +
               'created at 14:02 and deleted at 14:03 by somebody bound as ' +
               '<code>uid=carol</code> over LDAPS. It is a ring with a ' +
               'settable cap, so the oldest rows are dropped rather than ' +
               'kept forever.' },
      // THE ERROR CODES (2026-09-12), filed under Monitoring and beside the
      // audit log, by the question it answers rather than by what it lists.
      // A reader arrives here holding a code off an audit row or a log line
      // and wanting to know what failed — and the one column only a running
      // service can draw, how often this realm's held rows carry each code, is
      // an account of what this service has DONE. A catalogue with no such
      // column would belong in the documentation, which is where the rest of
      // this table is published (`docs/error-codes.md`).
      { path: '/admin/error-codes', label: 'Error codes',
        blurb: 'Every way this service can fail or refuse, by subsystem, ' +
               'with what the client is told instead and how many rows in ' +
               'the audit log carry each code right now. A code is an ' +
               'operator\'s name for a condition: it is on the audit row and ' +
               'in the log line, and <strong>it is never sent to a ' +
               'client</strong>, so a protocol\'s own errors are exactly ' +
               'what they always were.' }
    ] },
  { title: 'Server configuration',
    what: 'What this service is set up with, and who may change it.',
    items: [
      // FIRST in this section, above Configuration, and the order is an
      // argument rather than a preference: every page in this console shows one
      // realm, and Configuration in particular WRITES to the realm it is being
      // read in. Somebody who does not yet know that realms exist should meet
      // the page that says so before the page that acts on it.
      { path: '/admin/realms', label: 'Trust realms',
        blurb: 'Several logical copies of this service in one process, told ' +
               'apart by a segment at the front of the path. A realm has its ' +
               'own configuration, its own signing key, its own sessions, ' +
               'codes, tokens, artifacts, statistics and audit log — so a ' +
               'token minted in one does not verify against another\'s ' +
               'JWKS, which is the point of a realm rather than a side ' +
               'effect. What a realm separates is what this service ISSUES: ' +
               'the embedded directory is SHARED, and so are Kerberos, the ' +
               'certificate the main port and LDAPS 636 present, and ' +
               'SPIFFE\'s four sockets, because a socket has no path to put ' +
               'a realm segment in.' },
      { path: '/admin/config', label: 'Configuration',
        blurb: 'Every setting this service has, grouped by the protocol it ' +
               'belongs to, with where each value came from: a runtime ' +
               'override set here, an environment variable, the appconfig ' +
               'file this process was started with, or the defaults that one ' +
               'is unioned on top of. Higher beats lower, and a setting with ' +
               'a value NOWHERE stops the service from starting rather than ' +
               'defaulting quietly. Like every writing page here, it writes ' +
               'the realm it is read IN.' },
      { path: '/admin/persistence', label: 'Persistence',
        blurb: 'Whether anything here survives a restart, and where it is ' +
               'written. THREE things can be — the embedded directory (which ' +
               'is also the applications registry, the federation register ' +
               'and the SPIFFE registry), the trust realm registry, and the ' +
               'runtime setting changes made on pages like Configuration. ' +
               'NOTHING THIS SERVICE MINTS ever is: sessions, tokens, codes, ' +
               'artifacts, tickets, the statistics and the audit log go with ' +
               'the process, because the signing key is regenerated on every ' +
               'start and a token that outlived it would verify against ' +
               'nothing. Three modes: memory, which writes nothing and is ' +
               'the default; ldif, a file per realm and no database; and ' +
               'postgres. It is PERSISTENCE and not COORDINATION — one ' +
               'process per store.' },
      { path: '/admin/cluster', label: 'Cluster',
        blurb: 'Whether several containers against one postgres store ' +
               'behave as one service: which node is a member, which holds ' +
               'which lease, and what active-active mode still refuses to ' +
               'start without. Every clustered write is fenced by the ' +
               'node\'s membership, and a node that loses it exits.' },
      { path: '/admin/rbac', label: 'Admin roles',
        blurb: 'Who holds the two roles that grant this console — Admin ' +
               'Read and Admin Write — granted and revoked here. They are ' +
               'ORDINARY GROUPS in the shared directory rather than a store ' +
               'of the console\'s own, so this page, ' +
               '<code>/admin-api</code>, an <code>ldapmodify</code> on 389 ' +
               'or 636 and a SCIM PATCH are four doors onto one membership ' +
               '— which is the point, since a role no test can grant is a ' +
               'role no test can exercise. While NEITHER group has a member, ' +
               'anybody who signs in holds both.' },
      // THE EMBEDDED PROTOCOL DEBUGGER (2026-09-13), beside Admin roles and
      // for the reason it is in this section at all: the question it answers
      // is whether this service is set up to serve the debugger and to whom,
      // and "to whom" is the two roles on the page above. `debugger/
      // debugger_admin.js` draws it.
      { path: '/admin/debugger', label: 'Protocol debugger',
        blurb: 'The identity protocol debugger this service can serve on a ' +
               'listener of its own: whether it is embedded, the port and ' +
               'origin it answers on, the api process it forwards /api to ' +
               'and what that process may dial, and its settings. It is ' +
               'signed in to through this service\'s own authorization ' +
               'server as <code>sts-debugger-ui</code>, and its api needs an ' +
               'access token carrying ' +
               '<code>urn:sts:debugger-api:debugger</code> — issued to ' +
               'console administrators and nobody else. There is no setting ' +
               'that opens it.' },
      { path: '/admin/sts-metadata', label: 'Service metadata',
        blurb: 'Every endpoint this process registered, read off the LIVE ' +
               'express router, with the specification each one claims and ' +
               'how much of that specification is really implemented. ' +
               'Because it is read off the router it cannot go stale, and it ' +
               'reports both kinds of drift: a route registered and ' +
               'undescribed, and a description whose path is not registered ' +
               '— which is what a rename produces. A protocol that registers ' +
               'no route at all is its one blind spot, so the KDC\'s raw ' +
               '88 and the directory\'s 389 and 636 are described by hand.' },
      // THE SECOND PAGE IN THIS CONSOLE THIS FILE DOES NOT DRAW, and it sits
      // beside the first for the reason they are both here: each is a REPORT
      // about the whole service rather than a control over one part of it, and
      // a reader who wants "what does this thing do" wants them together.
      // `admin-ui/crypto_metadata.ts` builds it, calls `respond()` for this
      // shell, and fills setCryptoReporter() below so that `/admin-api/crypto`
      // can mirror it. Nothing about the nav knows any of that — a page here
      // is a `path` and a `label` whoever builds it.
      // THE THIRD PAGE IN THIS CONSOLE THIS FILE DOES NOT DRAW, and the
      // newest. `admin-ui/api_explorer.ts` builds it at 19a, after the
      // management API whose route table its document is built from. It is
      // filed HERE, beside Service metadata, because the question it answers
      // is the same one at a different scale: that page is every endpoint this
      // process registered, and this is every operation of the one API that
      // can change them, with a form that calls it.
      { path: '/admin/api-explorer', label: 'API explorer',
        blurb: 'Every operation of the management API at ' +
               '<code>/admin-api</code>, read from the same OpenAPI document ' +
               'that API publishes — which is generated from the table that ' +
               'registers the routes, so an operation cannot be undocumented ' +
               'nor documented and absent — with a form that calls it and ' +
               'the equivalent <code>curl</code> line beside each one. ' +
               '<strong>It was <code>/admin-api/docs</code> until ' +
               '2026-09-09</strong>, when that API began requiring an OAuth ' +
               '2.0 access token: a browser navigating to a URL carries ' +
               'none, so the one page here written to be opened in a browser ' +
               'had become the one page a browser could not open. Calls from ' +
               'this page use a token minted for YOU, carrying exactly the ' +
               'scopes your console roles grant — <code>admin:read</code> ' +
               'for Admin Read, <code>admin:write</code> for Admin Write — ' +
               'so an operation you may not perform is refused here by the ' +
               'same policy that would refuse it anywhere else. It is the ' +
               'only page in this console with a script on it.' },
      { path: '/admin/keys', label: 'Key pairs',
        blurb: 'Every key pair this process generated at start, what each ' +
               'one is used for, and <strong>a way to take it away</strong>. ' +
               'It is the deliberate opposite of Cryptography beside it: ' +
               'that page publishes key types, identifiers and fingerprints ' +
               'and no key material at all, and this one hands over the ' +
               'private half in PEM, DER, JWK or a password-protected ' +
               'PKCS#12 — the debugger\'s own keystore code, vendored. ' +
               'Defensible because of what these keys are: made at start, ' +
               'held in memory, dead with the process, protecting nothing. ' +
               'It needs ADMIN WRITE, which is stronger than any other read ' +
               'here, because on this page reading is taking.' },
      { path: '/admin/crypto-metadata', label: 'Cryptography',
        blurb: 'What this service does when it SIGNS, VERIFIES, ENCRYPTS or ' +
               'DECRYPTS something — for every identity service it ' +
               'advertises, with the algorithms each really uses and the ' +
               'envelope each is wrapped in (JOSE, XMLDSIG and XML ' +
               'Encryption, WS-Security, COSE, X.509, Kerberos). Every ' +
               'algorithm table is READ FROM THE MODULE THAT PERFORMS THE ' +
               'ALGORITHM, the way Service metadata reads its endpoint list ' +
               'off the live router, so none of it can claim something this ' +
               'service does not do — and it reports drift against that ' +
               'page\'s own family list in both directions. It carries a ' +
               'POST-QUANTUM section whose headline is not the flattering ' +
               'one: the signatures are partly post-quantum and the key ' +
               'establishment is entirely classical. It publishes no private ' +
               'key and no secret.' }
    ] }
];

// What `AdminConsole` needs from the rest of the service: the modules this file
// used to reach for itself, passed in so that the composition root can build
// one and a test can build one with stubs.
interface AdminConsoleDeps {
  log: typeof log;
  xmlEscape: typeof xmlEscape;
  baseUrlOf: typeof baseUrlOf;
  parseBody: typeof parseBody;
  userFor: typeof userFor;
  multipartParts: typeof multipartParts;
  errorCodes: typeof errorCodes;
  resourceMetadata: typeof resourceMetadata;
  adminActions: typeof adminActions;
  adminViews: typeof adminViews;
  protocolEndpoints: typeof protocolEndpoints;
  pagingOf: typeof pagingOf;
  pagingJson: typeof pagingJson;
  pagedRows: typeof pagedRows;
  tokensView: typeof tokensView;
  sessionsView: typeof sessionsView;
  DEFAULT_PER_PAGE: typeof DEFAULT_PER_PAGE;
  DELEGATION_PER_PAGE: typeof DELEGATION_PER_PAGE;
  MAX_ROWS: typeof MAX_ROWS;
  auditView: typeof auditView;
  errorCodesView: typeof errorCodesView;
  usedAssertionsView: typeof usedAssertionsView;
  delegationView: typeof delegationView;
  clusterSummary: typeof clusterSummary;
  permissionGroupsView: typeof permissionGroupsView;
  queryOne: typeof queryOne;
  chooserMatches: typeof chooserMatches;
  claimsRequestPreview: typeof claimsRequestPreview;
  userinfoClaimsJson: typeof userinfoClaimsJson;
  signalsJson: typeof signalsJson;
  ssfJson: typeof ssfJson;
  ssfDeadLettersJson: typeof ssfDeadLettersJson;
  caepJson: typeof caepJson;
  caepSessionsState: typeof caepSessionsState;
  caepApplicationsState: typeof caepApplicationsState;
  riscJson: typeof riscJson;
  riscAccountsState: typeof riscAccountsState;
  riscApplicationsState: typeof riscApplicationsState;
  spiffeJson: typeof spiffeJson;
  spiffeEntriesJson: typeof spiffeEntriesJson;
  spiffeAgentsJson: typeof spiffeAgentsJson;
  spiffeSelectorText: typeof spiffeSelectorText;
  newUserContainer: typeof newUserContainer;
  CREDENTIAL_CHOICES: typeof CREDENTIAL_CHOICES;
  knownUserKeys: typeof knownUserKeys;
  saml2Facts: typeof saml2Facts;
  valuesFor: typeof valuesFor;
  saml11Facts: typeof saml11Facts;
  asDriftRows: typeof asDriftRows;
  pageParamsOf: typeof pageParamsOf;
  applicationPermissionsState: typeof applicationPermissionsState;
  queryWith: typeof queryWith;
  DEFAULT_BLOCKS_PER_PAGE: typeof DEFAULT_BLOCKS_PER_PAGE;
  logoutFamilies: typeof logoutFamilies;
  claimsJson: typeof claimsJson;
  claimsPreviewUser: typeof claimsPreviewUser;
  claimsRequestParameter: typeof claimsRequestParameter;
  consentView: typeof consentView;
  consoleRpSession: typeof consoleRpSession;
  gateStateFor: typeof gateStateFor;
  metricsJson: typeof metricsJson;
  permissionsView: typeof permissionsView;
  realmJson: typeof realmJson;
  realmsJson: typeof realmsJson;
  rolesPreview: typeof rolesPreview;
  rolesRegister: typeof rolesRegister;
  samlAssertionSeconds: typeof samlAssertionSeconds;
  samlAssertionsJson: typeof samlAssertionsJson;
  samlAttributesJson: typeof samlAttributesJson;
  scimJson: typeof scimJson;
  scimMonitorJson: typeof scimMonitorJson;
  signOnSessionRows: typeof signOnSessionRows;
  tokenLifetimesJson: typeof tokenLifetimesJson;
  tokenSetView: typeof tokenSetView;
  vcJson: typeof vcJson;
  vcPreviewUser: typeof vcPreviewUser;
  vpConfigJson: typeof vpConfigJson;
  ROLE_ACTIONS: typeof ROLE_ACTIONS;
  ROLE_MEMBER_KINDS: typeof ROLE_MEMBER_KINDS;
  SAML11_RP_KIND: typeof SAML11_RP_KIND;
  SAML2_SP_KIND: typeof SAML2_SP_KIND;
  SAML_ASSERTION_SETTINGS: typeof SAML_ASSERTION_SETTINGS;
  applicationsAction: typeof applicationsAction;
  asAction: typeof asAction;
  caepAction: typeof caepAction;
  claimsAction: typeof claimsAction;
  configAction: typeof configAction;
  configSettingFor: typeof configSettingFor;
  consentAction: typeof consentAction;
  federationAction: typeof federationAction;
  groupsAction: typeof groupsAction;
  logoutAction: typeof logoutAction;
  permissionsAction: typeof permissionsAction;
  rbacAction: typeof rbacAction;
  realmsAction: typeof realmsAction;
  riscAction: typeof riscAction;
  rolesAction: typeof rolesAction;
  saml11Action: typeof saml11Action;
  saml2Action: typeof saml2Action;
  samlAssertionRowFor: typeof samlAssertionRowFor;
  samlAssertionsAction: typeof samlAssertionsAction;
  sessionsAction: typeof sessionsAction;
  signalsAction: typeof signalsAction;
  spiffeAgentsAction: typeof spiffeAgentsAction;
  spiffeEntriesAction: typeof spiffeEntriesAction;
  spiffeAction: typeof spiffeAction;
  ssfAction: typeof ssfAction;
  tokenAction: typeof tokenAction;
  tokenLifetimesAction: typeof tokenLifetimesAction;
  truthy: typeof truthy;
  userFieldsFrom: typeof userFieldsFrom;
  usersAction: typeof usersAction;
  vcAction: typeof vcAction;
  vpConfigAction: typeof vpConfigAction;
  config: typeof config;
  credentials: typeof credentials;
  totp: typeof totp;
  backupCodes: typeof backupCodes;
  webauthnPolicy: typeof webauthnPolicy;
  websecurity: typeof websecurity;
  accessGate: typeof accessGate;
  mode: typeof mode;
  persistence: typeof persistence;
  cluster: typeof cluster;
  clusterSecrets: typeof clusterSecrets;
  clusterBarrier: typeof clusterBarrier;
  secrets: typeof secrets;
  keystore: typeof keystore;
  realms: typeof realms;
  createClaims: typeof createClaims;
  stats: typeof stats;
  oidcRp: typeof oidcRp;
  sessions: typeof sessions;
  consoleSession: typeof consoleSession;
  endSessionById: typeof endSessionById;
  clearSessionCookie: typeof clearSessionCookie;
  LOGIN_PATH: typeof LOGIN_PATH;
  rbac: typeof rbac;
  adminScope: typeof adminScope;
  loginRealmChooser: typeof loginRealmChooser;
  vcClaims: typeof vcClaims;
  vpConfig: typeof vpConfig;
  claimAttributes: typeof claimAttributes;
  groupClaims: typeof groupClaims;
  auditLog: typeof auditLog;
  applications: typeof applications;
  saml11: typeof saml11;
  authorizationServers: typeof authorizationServers;
  federation: typeof federation;
  federationGraph: typeof federationGraph;
  federationDiagram: typeof federationDiagram;
  spiffeCa: typeof spiffeCa;
  spiffeRegistry: typeof spiffeRegistry;
  spiffeAuth: typeof spiffeAuth;
  signals: typeof signals;
  oauth2: typeof oauth2;
  delegation: typeof delegation;
  appPermissions: typeof appPermissions;
  issuanceGate: typeof issuanceGate;
  delegationMap: typeof delegationMap;
  userGraph: typeof userGraph;
  credentialGraph: typeof credentialGraph;
  krb5Principals: typeof krb5Principals;
  validation: typeof validation;
  // Required when first called, as the JavaScript did, for the reason
  // given where each is called.
  loadHelpers(): typeof import('../common/helpers');
}

type RouteApp = typeof app;

class AdminConsole {
  constructor(private readonly deps: AdminConsoleDeps) {
    deps.log.debug("Entering AdminConsole.constructor().");
    deps.log.debug("Leaving AdminConsole.constructor().");
  }

  // What the composition root passes: the real modules, as the load-time
  // instance was built from before R2 (#50).
  static defaultDeps(): AdminConsoleDeps {
    log.debug("Entering AdminConsole.defaultDeps().");
    log.debug("Leaving AdminConsole.defaultDeps().");
    return {
      log: log,
      xmlEscape: xmlEscape,
      baseUrlOf: baseUrlOf,
      parseBody: parseBody,
      userFor: userFor,
      multipartParts: multipartParts,
      errorCodes: errorCodes,
      resourceMetadata: resourceMetadata,
      adminActions: adminActions,
      adminViews: adminViews,
      protocolEndpoints: protocolEndpoints,
      pagingOf: pagingOf,
      pagingJson: pagingJson,
      pagedRows: pagedRows,
      tokensView: tokensView,
      sessionsView: sessionsView,
      DEFAULT_PER_PAGE: DEFAULT_PER_PAGE,
      DELEGATION_PER_PAGE: DELEGATION_PER_PAGE,
      MAX_ROWS: MAX_ROWS,
      auditView: auditView,
      errorCodesView: errorCodesView,
      usedAssertionsView: usedAssertionsView,
      delegationView: delegationView,
      clusterSummary: clusterSummary,
      permissionGroupsView: permissionGroupsView,
      queryOne: queryOne,
      chooserMatches: chooserMatches,
      claimsRequestPreview: claimsRequestPreview,
      userinfoClaimsJson: userinfoClaimsJson,
      signalsJson: signalsJson,
      ssfJson: ssfJson,
      ssfDeadLettersJson: ssfDeadLettersJson,
      caepJson: caepJson,
      caepSessionsState: caepSessionsState,
      caepApplicationsState: caepApplicationsState,
      riscJson: riscJson,
      riscAccountsState: riscAccountsState,
      riscApplicationsState: riscApplicationsState,
      spiffeJson: spiffeJson,
      spiffeEntriesJson: spiffeEntriesJson,
      spiffeAgentsJson: spiffeAgentsJson,
      spiffeSelectorText: spiffeSelectorText,
      newUserContainer: newUserContainer,
      CREDENTIAL_CHOICES: CREDENTIAL_CHOICES,
      knownUserKeys: knownUserKeys,
      saml2Facts: saml2Facts,
      valuesFor: valuesFor,
      saml11Facts: saml11Facts,
      asDriftRows: asDriftRows,
      pageParamsOf: pageParamsOf,
      applicationPermissionsState: applicationPermissionsState,
      queryWith: queryWith,
      DEFAULT_BLOCKS_PER_PAGE: DEFAULT_BLOCKS_PER_PAGE,
      logoutFamilies: logoutFamilies,
      claimsJson: claimsJson,
      claimsPreviewUser: claimsPreviewUser,
      claimsRequestParameter: claimsRequestParameter,
      consentView: consentView,
      consoleRpSession: consoleRpSession,
      gateStateFor: gateStateFor,
      metricsJson: metricsJson,
      permissionsView: permissionsView,
      realmJson: realmJson,
      realmsJson: realmsJson,
      rolesPreview: rolesPreview,
      rolesRegister: rolesRegister,
      samlAssertionSeconds: samlAssertionSeconds,
      samlAssertionsJson: samlAssertionsJson,
      samlAttributesJson: samlAttributesJson,
      scimJson: scimJson,
      scimMonitorJson: scimMonitorJson,
      signOnSessionRows: signOnSessionRows,
      tokenLifetimesJson: tokenLifetimesJson,
      tokenSetView: tokenSetView,
      vcJson: vcJson,
      vcPreviewUser: vcPreviewUser,
      vpConfigJson: vpConfigJson,
      ROLE_ACTIONS: ROLE_ACTIONS,
      ROLE_MEMBER_KINDS: ROLE_MEMBER_KINDS,
      SAML11_RP_KIND: SAML11_RP_KIND,
      SAML2_SP_KIND: SAML2_SP_KIND,
      SAML_ASSERTION_SETTINGS: SAML_ASSERTION_SETTINGS,
      applicationsAction: applicationsAction,
      asAction: asAction,
      caepAction: caepAction,
      claimsAction: claimsAction,
      configAction: configAction,
      configSettingFor: configSettingFor,
      consentAction: consentAction,
      federationAction: federationAction,
      groupsAction: groupsAction,
      logoutAction: logoutAction,
      permissionsAction: permissionsAction,
      rbacAction: rbacAction,
      realmsAction: realmsAction,
      riscAction: riscAction,
      rolesAction: rolesAction,
      saml11Action: saml11Action,
      saml2Action: saml2Action,
      samlAssertionRowFor: samlAssertionRowFor,
      samlAssertionsAction: samlAssertionsAction,
      sessionsAction: sessionsAction,
      signalsAction: signalsAction,
      spiffeAgentsAction: spiffeAgentsAction,
      spiffeEntriesAction: spiffeEntriesAction,
      spiffeAction: spiffeAction,
      ssfAction: ssfAction,
      tokenAction: tokenAction,
      tokenLifetimesAction: tokenLifetimesAction,
      truthy: truthy,
      userFieldsFrom: userFieldsFrom,
      usersAction: usersAction,
      vcAction: vcAction,
      vpConfigAction: vpConfigAction,
      config: config,
      credentials: credentials,
      totp: totp,
      backupCodes: backupCodes,
      webauthnPolicy: webauthnPolicy,
      websecurity: websecurity,
      accessGate: accessGate,
      mode: mode,
      persistence: persistence,
      cluster: cluster,
      clusterSecrets: clusterSecrets,
      clusterBarrier: clusterBarrier,
      secrets: secrets,
      keystore: keystore,
      realms: realms,
      createClaims: createClaims,
      stats: stats,
      oidcRp: oidcRp,
      sessions: sessions,
      consoleSession: consoleSession,
      endSessionById: endSessionById,
      clearSessionCookie: clearSessionCookie,
      LOGIN_PATH: LOGIN_PATH,
      rbac: rbac,
      adminScope: adminScope,
      loginRealmChooser: loginRealmChooser,
      vcClaims: vcClaims,
      vpConfig: vpConfig,
      claimAttributes: claimAttributes,
      groupClaims: groupClaims,
      auditLog: auditLog,
      applications: applications,
      saml11: saml11,
      authorizationServers: authorizationServers,
      federation: federation,
      federationGraph: federationGraph,
      federationDiagram: federationDiagram,
      spiffeCa: spiffeCa,
      spiffeRegistry: spiffeRegistry,
      spiffeAuth: spiffeAuth,
      signals: signals,
      oauth2: oauth2,
      delegation: delegation,
      appPermissions: appPermissions,
      issuanceGate: issuanceGate,
      delegationMap: delegationMap,
      userGraph: userGraph,
      credentialGraph: credentialGraph,
      krb5Principals: krb5Principals,
      validation: validation,
      loadHelpers: function () {
        return require('../common/helpers');
      }
    };
  }

  // The work loading this module did with its instance (#50, R2): the
  // `WIRE_STEPS` below the class, in the order they are written. Run once by
  // the slot, for whichever instance is installed.
  static wire(instance: AdminConsole): void {
    log.debug("Entering AdminConsole.wire(). " + WIRE_STEPS.length +
              " step(s).");
    WIRE_STEPS.forEach(function (step) {
      step(instance);
    });
    log.debug("Leaving AdminConsole.wire().");
  }

  // Is this row of a section's `items` a GROUP of pages rather than a page? One
  // predicate, used by both the flattening and the sidebar, so the two cannot
  // disagree about what they are looking at. A group has `items`; a page has a
  // `path` and never has `items`.
  isNavGroup(item) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.isNavGroup().");
    log.debug("Leaving AdminConsole.isNavGroup().");
    return item != null && Array.isArray(item.items);
  }

  // Every PAGE in one section, in sidebar order, with a group's pages spliced
  // in where the group sits. One level only — see the note above SECTIONS.
  sectionPages(section) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.sectionPages(). section=" + section.title);
    const pages = [];
    section.items.forEach(function (item) {
      if (self.isNavGroup(item)) {
        item.items.forEach(function (page) {
          pages.push(page);
        });
        return;
      }
      pages.push(item);
    });
    log.debug("Leaving AdminConsole.sectionPages(). " + pages.length +
              " page(s).");
    return pages;
  }

  // Every group `config.js` declares, in its order. Read off the table rather
  // than off `config.groups()` because this runs at start-up (at require time
  // until #50's R2; when the instance is installed now) and that call
  // describes every setting — the group NAME is all that is being checked, and
  // asking for a hundred and fifty-four descriptions to get twenty-two strings
  // would also drag a realm lookup into module load.
  declaredSettingGroups() {
    const { log, config } = this.deps;
    log.debug("Entering AdminConsole.declaredSettingGroups().");
    const seen = [];
    config.SETTINGS.forEach(function (setting) {
      if (seen.indexOf(setting.group) < 0) {
        seen.push(setting.group);
      }
    });
    log.debug("Leaving AdminConsole.declaredSettingGroups().");
    return seen;
  }

  // The three ways this table can be wrong, checked once at start-up (when the
  // instance is installed, since #50's R2). Each would otherwise be found by a
  // person who could not find a setting, which is the slowest way to find any
  // of them.
  checkSettingHomes() {
    const { log, errorCodes } = this.deps;
    log.debug("Entering AdminConsole.checkSettingHomes().");
    const problems = [];
    const declared = this.declaredSettingGroups();
    const homed = SETTING_HOMES.map(function (row) { return row.group; });

    declared.forEach(function (group) {
      const rows =
          homed.filter(function (name) { return name === group; }).length;
      if (rows === 0) {
        problems.push('The setting group "' + group + '" has no page to live ' +
          'on. Add a row to SETTING_HOMES in admin-ui/admin.ts naming the ' +
          'console page that should draw it, or it is editable nowhere.');
      }
      if (rows > 1) {
        problems.push('The setting group "' + group + '" has ' + rows + ' ' +
          'rows in SETTING_HOMES. One row may name several pages; two rows ' +
          'cannot say which of them a reader is meant to believe.');
      }
    });

    SETTING_HOMES.forEach(function (row) {
      if (declared.indexOf(row.group) < 0) {
        problems.push('SETTING_HOMES names the setting group "' + row.group +
          '", which config.js does not declare. It was probably renamed ' +
          'there.');
      }
      row.pages.forEach(function (path) {
        const known = NAV.some(function (item) { return item.path === path; });
        if (!known) {
          problems.push('SETTING_HOMES sends the "' + row.group +
            '" settings to ' +
            path + ', which is not a page in this console\'s SECTIONS. ' +
            'Nothing would draw them.');
        }
      });
    });

    problems.forEach(function (problem) {
      log.error(errorCodes.tag('STS-ADMIN-0015') + 'admin console: ' + problem);
    });
    log.debug("Leaving AdminConsole.checkSettingHomes(). " + problems.length +
              " problem(s).");
    return problems;
  }

  esc(v) {
    const { log, xmlEscape } = this.deps;
    log.debug("Entering AdminConsole.esc().");
    log.debug("Leaving AdminConsole.esc().");
    return xmlEscape(v == null ? '' : String(v));
  }

  // A list of names, each in its own <code>. Written as a function because the
  // obvious one-liner — join with the markup and escape the result — escapes
  // the markup too, and the page then shows the tags it was supposed to render.
  // It did.
  codeList(names) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.codeList().");
    log.debug("Leaving AdminConsole.codeList().");
    return names.map(function (name) { return '<code>' + self.esc(name) +
                                       '</code>'; })
                .join(', ');
  }

  // The list AS THE READER LEFT IT, picked out of a query by that table.
  //
  // This is what makes the trail a way back to where somebody was rather than
  // to the top of an unfiltered list. A drill-down link carries it, every
  // control on the drill-down carries it onward (pageParamsOf() takes the whole
  // query through), and the trail's section crumb spends it. Nothing on the
  // drill-down reads these keys for anything else: their names belong to the
  // list's filter form, and a page showing one application has no `q`.
  listViewOf(section, query) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.listViewOf(). section=" + section);
    const out = {};
    (LIST_PARAMS[section] || []).forEach(function (key) {
      const raw = (query || {})[key];
      // Express hands back an array when a parameter is repeated, and String()
      // on one is "a,b" — a filter nothing matches, reached by a link somebody
      // clicked twice. The same first-wins rule pageParamsOf() uses.
      const value = Array.isArray(raw) ? raw[0] : raw;
      if (value !== undefined && value !== null && String(value) !== '') {
        out[key] = String(value);
      }
    });
    log.debug("Leaving AdminConsole.listViewOf(). " + Object.keys(out).length +
              " " +
        "parameter(s) carried.");
    return out;
  }

  // The same thing out of a form's `back` field, which is a query string a
  // browser sent us rather than one we are looking at.
  //
  // It is REBUILT and never echoed: the names come from LIST_PARAMS, the values
  // are re-encoded by queryWith(), and anything else in the field is dropped.
  // That is the guarantee backTo() gives the tokens page and it is needed here
  // for the same reason — a redirect target taken out of a request body is an
  // open redirect, and one carrying a newline is a header injection. The worst
  // a hand-written `back` can now reach is another page of the same list.
  listViewFromBack(section, raw) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.listViewFromBack(). section=" + section);
    let params = null;
    try {
      params = new URLSearchParams(String(raw || '').replace(/^\?/, ''));
    } catch (e) {
      // Unparseable; the bare list is the right answer and is what a form
      // carrying no `back` at all gets anyway.
      log.debug("Leaving AdminConsole.listViewFromBack(). Unparseable: " +
                e.message);
      return {};
    }
    const query = {};
    params.forEach(function (value, key) {
      // First wins, for the reason listViewOf() takes the first of a repeated
      // parameter: a field sent twice is one value, not "a,b".
      if (!Object.prototype.hasOwnProperty.call(query, key)) {
        query[key] = value;
      }
    });
    log.debug("Leaving AdminConsole.listViewFromBack().");
    return this.listViewOf(section, query);
  }

  // The section a DRILL-DOWN hangs under: where "back" goes, what that section
  // is called, and what the reader has drilled INTO.
  //
  // The label comes out of NAV rather than the call site, so a renamed tab
  // cannot leave a way back that names the old one. A path with no NAV row
  // falls back to the path itself, which is visible rather than silent — a
  // drill-down under a section that is not in the nav is worth noticing.
  //
  // `listView` is listViewOf()'s answer, and it is what the href carries: the
  // reader goes back to page 3 of the filter they were looking at rather than
  // to the top of everything. `leaf` is the thing drilled into, for the last
  // crumb.
  upTo(path, leaf, listView?) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.upTo(). path=" + path);
    const item = NAV.filter(function (row) { return row.path === path; })[0];
    const view = listView || {};
    log.debug("Leaving AdminConsole.upTo(). " +
              (item ? "labelled " + item.label : "no nav row") +
              ", " + Object.keys(view).length + " list parameter(s).");
    return { href: path + queryWith(view, {}), label: item ? item.label : path,
             leaf: leaf, filtered: Object.keys(view).length > 0 };
  }

  // `up` is set on a drill-down — a page reached from a link on one of the
  // sections below rather than one of the sections itself — and what it changes
  // is the ACTIVE TAB.
  //
  // That tab was drawn as plain text on every page whose `active` matched, and
  // `active` is the section's path on the list page and on every page
  // underneath it alike. So on a drill-down the one control that pointed at the
  // list was the one control this shell had turned off, and the only way back
  // from /admin/applications?application=x was the browser's own Back button or
  // a link at the foot of a long page. On a drill-down the tab is a LINK: still
  // bold, because the reader is inside that section, and underlined so that
  // "the section you are in" cannot be read as "not clickable".
  //
  // It is one `<nav>` holding one `<ul>` per section with a heading above it.
  // The heading is plain text and NOT a link, for the reason the section is not
  // a crumb: there is no page behind it. The section containing the page being
  // drawn is marked, so a reader who arrived on a deep link can see where they
  // are without reading every label.
  //
  // A GROUP inside a section is drawn as an `<li>` holding a heading and a
  // `<ul>` of its own, rather than as a second `<ul>` beside the first: a group
  // belongs INSIDE the section's list — it is three of that list's items said
  // together — and a sibling list would tell a screen reader the section ended
  // where the group began. Its heading is plain text for the same reason the
  // section's is, and the marker on the group holding the current page is a
  // rule down the left like the section's, one level in.
  navBar(active, up, req) {
    const { log, gateStateFor } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.navBar(). active=" + active);
    // A REALM ADMINISTRATOR'S SIDEBAR (2026-09-14, #32) leaves out the pages
    // that belong to the whole service, which the gate would refuse them, and
    // the realm switcher, since their console is one realm. The service
    // administrator's is unchanged.
    const state = req ? gateStateFor(req) : null;
    const realmOnly = !!(state && state.authority === 'realm');
    const html = '<nav aria-label="Admin console sections">' +
      // FIRST, inside this card rather than above it. It does not select a page
      // — it selects which service the pages are about — but it is the first
      // question a reader has about the column, so it is the first thing in it.
      (realmOnly ? '' : this.realmChooser(req)) +
      this.visibleSections(state).map(function (section) {
        const inThisSection = self.sectionPages(section)
          .filter(function (item) {
          return item.path === active;
        }).length > 0;
        const links = section.items.map(function (item) {
          return self.navItem(item, active, up);
        }).join('');
        return '<div class="navsec' + (inThisSection ? ' open' : '') + '">' +
          '<p class="navhead" title="' + self.esc(section.what) + '">' +
          self.esc(section.title) + '</p><ul>' + links + '</ul></div>';
      }).join('') + '</nav>';
    log.debug("Leaving AdminConsole.navBar(). " + SECTIONS.length +
              " section(s).");
    return html;
  }

  // SECTIONS as one state may see it: service pages dropped for a realm
  // administrator, a group with nothing left dropped with them, and a section
  // with nothing left dropped too. The table itself is never edited.
  visibleSections(state): any[] {
    const { log, adminScope } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.visibleSections().");
    if (!state || state.authority !== 'realm') {
      log.debug("Leaving AdminConsole.visibleSections(). Everything.");
      return SECTIONS;
    }
    const shown = function (item) {
      log.debug("Entering shown().");
      log.debug("Leaving shown().");
      return adminScope.pageVisible(state, item.path);
    };
    const out = SECTIONS.map(function (section) {
      const items = section.items.map(function (item) {
        if (!self.isNavGroup(item)) {
          return shown(item) ? item : null;
        }
        const pages = item.items.filter(shown);
        return pages.length ? Object.assign({}, item, { items: pages }) : null;
      }).filter(Boolean);
      return Object.assign({}, section, { items: items });
    }).filter(function (section) {
      return section.items.length > 0;
    });
    log.debug("Leaving AdminConsole.visibleSections(). " + out.length +
              " section(s).");
    return out;
  }

  // One row of a section's list: a page, or a group holding pages.
  navItem(item, active, up) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.navItem(). " +
              (this.isNavGroup(item) ? 'group ' + item.title
                                                         : 'page ' +
                                                             item.path));
    if (!this.isNavGroup(item)) {
      log.debug("Leaving AdminConsole.navItem(). A page.");
      return this.navLink(item, active, up);
    }
    const open = item.items.filter(function (page) {
      return page.path === active;
    }).length > 0;
    const html = '<li class="navgrp' + (open ? ' open' : '') + '">' +
      '<p class="navsub" title="' + this.esc(item.what) + '">' +
      this.esc(item.title) +
      '</p><ul>' + item.items.map(function (page) {
        return self.navLink(page, active, up);
      }).join('') + '</ul></li>';
    log.debug("Leaving AdminConsole.navItem(). A group of " +
              item.items.length + " page(s).");
    return html;
  }

  navLink(item, active, up) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.navLink(). path=" + item.path);
    if (item.path === active) {
      if (up) {
        log.debug("Leaving AdminConsole.navLink(). Active, and a drill-down.");
        return '<li><a class="here" href="' + this.esc(up.href) + '"' +
               ' title="Back to ' + this.esc(up.label) + '"' +
               // A drill-down's active item IS a link, so it is already in the
               // tab order and must stay there — only the autofocus is added.
               ' autofocus>' + this.esc(item.label) + '</a></li>';
      }
      log.debug("Leaving AdminConsole.navLink(). Active.");
      return '<li><span class="here"' + ACTIVE_NAV_FOCUS +
             ' aria-current="page">' + this.esc(item.label) + '</span></li>';
    }
    log.debug("Leaving AdminConsole.navLink().");
    return '<li><a href="' + this.esc(item.path) + '">' + this.esc(item.label) +
           '</a></li>';
  }

  shortCrumb(text) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.shortCrumb().");
    const value = String(text == null ? '' : text);
    log.debug("Leaving AdminConsole.shortCrumb().");
    return value.length > MAX_CRUMB ? value.slice(0, MAX_CRUMB - 1) + '…' :
           value;
  }

  // THE BREADCRUMB TRAIL, AND IT IS ON EVERY PAGE RATHER THAN ONLY ON A
  // DRILL-DOWN.
  //
  // One line under the nav saying where the reader is and offering every level
  // above them: `Admin console › Applications › rfc9700-debugger`. The nav
  // answers "what else is there"; the trail answers "where am I and how do I
  // get back", which are different questions — the tab for the section you are
  // standing in is exactly the tab that tells you nothing about the page you
  // are standing on.
  //
  // Three things about it are deliberate.
  //
  // The section crumb on a drill-down is `up.href`, which carries THE FILTER
  // AND THE PAGE the reader clicked in from (see listViewOf()), so the trail
  // goes back to where they were rather than to the top of an unfiltered list.
  // That is the whole difference between a breadcrumb and a link to the
  // section.
  //
  // The last crumb is NOT a link. It is the page being drawn, and a crumb that
  // reloads the page you are on is a control that does nothing — which teaches
  // a reader not to trust the ones beside it.
  //
  // The root crumb is `Admin console` on every page including `/admin` itself,
  // where it is the only crumb and is not a link. A trail that appeared on some
  // pages and not others would be a trail nobody looks for.
  trailBar(active, up, title) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.trailBar(). active=" + active);
    const crumbs: any[] = [{ label: 'Admin console',
                      href: active === '/admin' ? null : '/admin' }];
    if (active !== '/admin') {
      const item =
        NAV.filter(function (row) { return row.path === active; })[0];
      // THE TITLE WHEN THE PAGE IS NOT IN THE NAV, which is every page this
      // shell draws that is not a console page: the sign-out confirmation, the
      // callback's refusal, the gate's own. Those pass `active` as '' or null,
      // so the leaf was an EMPTY crumb — "Admin console ›" with nothing after
      // it — on exactly the pages whose title is the only thing saying what
      // happened.
      const label = item ? item.label : (active || title);
      if (up) {
        crumbs.push({ label: label, href: up.href,
                      // Said in the tooltip rather than in the crumb, because
                      // "as you left it" is reassurance for somebody who
                      // wonders and noise for everybody else — and a crumb
                      // whose text changes with the filter is a crumb that
                      // moves under the pointer.
                      title: up.filtered
                        ? 'Back to ' + label + ' — the filter and page you ' +
                                               'came from'
                        : 'Back to ' + label });
        crumbs.push({ label: this.shortCrumb(up.leaf || title),
                      title: String(up.leaf || title) });
      } else {
        crumbs.push({ label: label });
      }
    }
    const html = '<p class="crumb">' + crumbs.map(function (crumb, index) {
      const sep = index ? '<span class="sep">&rsaquo;</span>' : '';
      if (!crumb.href) {
        return sep + '<span class="leaf"' +
          (crumb.title ? ' title="' + self.esc(crumb.title) + '"' : '') + '>' +
          self.esc(crumb.label) + '</span>';
      }
      return sep + '<a href="' + self.esc(crumb.href) + '"' +
        (crumb.title ? ' title="' + self.esc(crumb.title) + '"' : '') + '>' +
        self.esc(crumb.label) + '</a>';
    }).join('') + '</p>';
    log.debug("Leaving AdminConsole.trailBar(). " + crumbs.length +
              " crumb(s).");
    return html;
  }

  // SAID WHEN — AND ONLY WHEN — THE SESSION BELONGS TO ANOTHER REALM.
  //
  // **NOT DRAWN SINCE 2026-09-06.** `consoleRpSession()` (admin-core/
  // admin_views.js) answers `foreign: false` always — a relying-party session
  // belongs to the surface that minted it — so `info.foreignSession` is never
  // true and this returns ''. It is kept because the banner still reads the
  // member; the text below describes the arrangement it was written for.
  //
  // The console follows its reader across realms (the function that did it,
  // `sessionAnywhere()` in authn.js, is `consoleSession()` now), which is what
  // makes the switcher a switcher rather than a sign-in screen. Doing it
  // silently would be the wrong kind of quiet: a realm is a whole logical copy
  // of this service, so a reader looking at the default realm's tokens on a
  // session minted in `acme` is one keystroke from believing the two realms
  // share the sessions as well — and the next thing they conclude is that
  // `/oauth2/authorize` would have accepted it, which it would not.
  //
  // So the banner names the realm the session is held by, and says the one
  // thing that is easy to get wrong about it. It is empty in every other case,
  // including every service that has defined no realm.
  foreignSessionNote(info) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.foreignSessionNote().");
    if (!info.foreignSession || !info.sessionRealm) {
      log.debug("Leaving AdminConsole.foreignSessionNote().");
      return '';
    }
    log.debug("Leaving AdminConsole.foreignSessionNote().");
    return ' Your session belongs to the trust realm <strong>' +
      this.esc(info.sessionRealm.name) + '</strong> (<code>' +
      this.esc(info.sessionRealm.id) +
      '</code>) and this console follows it into every realm, because the ' +
      'two console roles are groups in the one shared directory. <strong>The ' +
      'protocol endpoints do not:</strong> in this realm ' +
      '<code>/oauth2/authorize</code>, <code>/wsfed</code> and the two SAML ' +
      'profiles see no session at all and would ask you to sign in. <a ' +
      'href="/admin/realms">What a realm separates</a>.';
  }

  gateBanner(gate) {
    const { log, rbac } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.gateBanner().");
    const info = gate || {};
    if (!info.enforced) {
      log.debug("Leaving AdminConsole.gateBanner(). The gate is off.");
      return OPEN_BANNER;
    }
    // ---------------------------------------------------------------------
    // NOBODY IS SIGNED IN, WHICH IS A FOURTH STATE THIS BANNER DID NOT HAVE
    // (2026-09-10) — AND IT WAS REACHED ON THE ONE PAGE THAT IS ABOUT IT.
    //
    // Three pages are drawn in this state: the sign-out confirmation, the
    // callback's refusal, and the 401 a form POSTed without a session gets.
    // With no fourth branch every one of them fell through to the last one
    // below and read **"Signed in as `(nobody)`, holding no console role. This
    // is a READ-ONLY view"** — three sentences of which the first is false, the
    // second is meaningless and the third describes a console this reader
    // cannot open at all. Directly above a page whose whole text is that they
    // have just signed out.
    //
    // It is a branch rather than an empty string for the reason the banner is
    // on every page in the first place: this is the line that says what the
    // gate makes of the request, and a page that says nothing about it leaves a
    // reader who has met the other three states to guess which one this is.
    // `sideColumn()` below takes the NAVIGATION away in this state and this
    // keeps the SENTENCE, which is the difference between a control that cannot
    // work and a fact about why.
    // ---------------------------------------------------------------------
    if (!info.session) {
      log.debug("Leaving AdminConsole.gateBanner(). Nobody is signed in.");
      return '<div class="warn"><strong>Nobody is signed in on this ' +
        'browser.</strong> This console needs a session of its own — it is ' +
        'an ordinary OpenID Connect client of this service, ' +
        '<code>sts-admin-console</code> in the registry — so every page of ' +
        'it will run the sign-in flow again. <a href="/admin">Open the ' +
        'console</a> to start one.</div>';
    }
    const who = '<code>' + this.esc(info.username || '(nobody)') + '</code>';
    const elsewhere = this.foreignSessionNote(info);
    if (info.open && info.bootstrap && info.bootstrap.seeded) {
      log.debug("Leaving AdminConsole.gateBanner(). Open until the bootstrap " +
                "administrator signs in.");
      return this.warn('<strong>Signed in as ' + who + ', and holding both ' +
        'roles because <code>' + this.esc(info.bootstrap.username) +
        '</code> has not ' +
        'signed in to this console yet.</strong> Until this service\'s ' +
        'bootstrap administrator first signs in here, anyone who signs in ' +
        'has the whole console (<code>admin.openWhenEmpty</code>). From that ' +
        'moment only members of <code>' + this.esc(info.readGroup) +
        '</code> and ' +
        '<code>' + this.esc(info.writeGroup) + '</code> may use it — so ' +
        'grant yourself a role on <a href="/admin/rbac">Admin roles</a> ' +
        'first if you will need one.' + elsewhere);
    }
    if (info.open) {
      log.debug("Leaving AdminConsole.gateBanner(). The roster is empty.");
      return this.warn('<strong>Signed in as ' + who + ', and holding both ' +
        'roles because NOBODY HOLDS EITHER.</strong> This console always ' +
        'asks you to sign in — but neither <code>' + this.esc(info.readGroup) +
        '</code> nor ' +
        '<code>' + this.esc(info.writeGroup) + '</code> has a single member, ' +
        'and while that is true anyone who signs in has the whole console. ' +
        'This service has no password to bootstrap an administrator with, ' +
        'which is why the empty roster opens rather than closes ' +
        '(<code>admin.openWhenEmpty</code>). <strong>Grant somebody a role ' +
        'on <a href="/admin/rbac">Admin roles</a></strong> and the roster is ' +
        'enforced from that moment — including against you, so grant ' +
        'yourself one first.' + elsewhere);
    }
    if (info.closed) {
      // Rendered for completeness rather than because a reader will meet it: a
      // request in this state is refused before a page is drawn. It is
      // reachable on the refusal page itself, which IS drawn.
      log.debug("Leaving AdminConsole.gateBanner(). The roster is empty and " +
                "closed.");
      return '<div class="err"><strong>Nobody can use this console.</strong> ' +
        'No role has a member, and ' +
        (info.bootstrap && info.bootstrap.seeded && info.bootstrap.claimedAt
          ? 'the bootstrap administrator has already signed in, which closed ' +
            'the open window. '
          : '<code>admin.openWhenEmpty</code> is off. ') +
        '<code>POST /admin-api/rbac/grant</code> is the way back in — it ' +
        'takes an access token carrying <code>admin:write</code> rather than ' +
        'this console\'s session, which is why it still works when this page ' +
        'cannot.</div>';
    }
    const held = (info.roles || []).map(function (id) {
      const role = rbac.roleFor(id);
      return '<strong>' + self.esc(role ? role.label : id) + '</strong>';
    }).join(' and ');
    log.debug("Leaving AdminConsole.gateBanner(). Enforced; " +
              (info.roles || []).length +
        " " +
        "role(s).");
    return '<div class="ok">Signed in as ' + who + ', holding ' +
      (held || '<strong>no console role</strong>') + '. ' +
      (info.write
        ? 'Every control on these pages is yours.'
        : 'This is a READ-ONLY view: the forms are drawn so that you can see ' +
          'what they do, and posting one is ' +
          'refused. ' + this.esc(info.writeGroup) + ' ' +
          'is the role that changes that.') +
      ' <a href="/admin/rbac">Who holds what</a>. Ending this session: <a ' +
      'href="/logout">/logout</a> signs you out of everything this service ' +
      'holds — every protocol at once — and <a ' +
      'href="/oauth2/logout">/oauth2/logout</a> is OIDC\'s own RP-initiated ' +
      'one. <a href="/admin/logout">/admin/logout</a> is the operator\'s ' +
      'view of the same lists, for somebody else.' + elsewhere + '</div>';
  }

  portalHref(req, gate) {
    const { log, realms } = this.deps;
    log.debug("Entering AdminConsole.portalHref().");
    const home = realms.get((gate && gate.identityRealm) ||
                            realms.DEFAULT_ID) ||
                 realms.get(realms.DEFAULT_ID);
    log.debug("Leaving AdminConsole.portalHref(). realm=" + (home && home.id));
    return this.realmRoot(req) + realms.prefixOf(home) + PORTAL_PATH;
  }

  // The base URL with the CURRENT realm's prefix taken back off, so that what
  // is built from it starts at the root. baseUrlOf() adds the ambient prefix by
  // design — this and the switch route below are the two callers in this
  // service that do not want it, and they say so here rather than working
  // around it somewhere else.
  realmRoot(req) {
    const { log, baseUrlOf, realms } = this.deps;
    log.debug("Entering AdminConsole.realmRoot().");
    const withRealm = baseUrlOf(req);
    log.debug("Leaving AdminConsole.realmRoot().");
    return withRealm.slice(0, withRealm.length - realms.currentPrefix().length);
  }

  // Where the reader is, INSIDE the realm. req.url has already had the prefix
  // stripped by the time any route sees it, which is the whole trick, and is
  // also what makes this the path to re-enter in the other realm — so switching
  // lands on the same page with the same filter rather than back at /admin.
  realmRelativePath(req) {
    const { log, realms } = this.deps;
    log.debug("Entering AdminConsole.realmRelativePath().");
    const here = String(req.originalUrl || '/admin');
    log.debug("Leaving AdminConsole.realmRelativePath().");
    return here.slice(realms.currentPrefix().length) || '/admin';
  }

  // WHERE "REFRESH" POINTS: the page the reader is on, with the message
  // parameters taken back off.
  //
  // Root-relative and WITHOUT the realm prefix, which is the one thing here
  // that is easy to get wrong. app.js rewrites every `href="/…` on the way out
  // to carry the realm being read, so handing it `req.originalUrl` — which
  // already carries one — would produce /realm/acme/realm/acme/admin/tokens and
  // a 404 a long way from this function. realmRelativePath() is that same URL
  // with the prefix off, which is exactly what the rewrite expects to be given.
  //
  // `notice` and `error` come off because respondToAction() puts them there on
  // the redirect after a form POST: they describe something that has ALREADY
  // happened, and a Refresh that carried them would re-announce "12 tokens
  // revoked" over a page where nothing had been revoked this time. Everything
  // else survives — the filter, the page number, the search — because that is
  // what the reader is looking at, which is why this is a subtraction and not a
  // bare path.
  refreshHref(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.refreshHref().");
    const here = this.realmRelativePath(req);
    const cut = here.indexOf('?');
    if (cut < 0) {
      log.debug("Leaving AdminConsole.refreshHref(). No query string.");
      return here;
    }
    const params = new URLSearchParams(here.slice(cut + 1));
    params.delete('notice');
    params.delete('error');
    const query = params.toString();
    log.debug("Leaving AdminConsole.refreshHref(). " +
              (query ? "Kept " + query : "Kept " +
        "nothing") + ".");
    return query ? here.slice(0, cut) + '?' + query : here.slice(0, cut);
  }

  // THE REFRESH CONTROL, at the top of every page in this console.
  //
  // A LINK RATHER THAN A BUTTON, and this service's own CSP is the reason
  // rather than taste: `script-src 'none'` (see CLAUDE.md) means there is no
  // `location.reload()` to be had anywhere in here, and a <form method="get">
  // to the same path would silently drop the query string it was submitted with
  // unless every parameter were re-emitted as a hidden field. An <a> to the
  // current URL fetches the page again either way, because respond() sends
  // every page `Cache-Control: no-store` — so this is a real refresh and not a
  // possibly-cached one.
  //
  // It is worth having at all because every page in this console describes LIVE
  // state held in memory — counts, sessions, tokens expiring while they are
  // being read, a directory something else is writing — so "is this still
  // true?" is a question a reader has on all of them, and until now the only
  // answer was the browser's own reload with a stale `?notice=` still on the
  // URL.
  //
  // It is drawn in the QUIET form of a.btn on purpose. It is on every page
  // beside the heading, and a solid dark button in that position would read as
  // the most important thing on the page on all of them.
  //
  // **AND IT IS DRAWN ONLY WHERE THERE IS A PAGE TO RELOAD (2026-09-10)**,
  // which is `signOutControl()`'s test below and `sideColumn()`'s, met by a
  // third control on the same page. The sign-out confirmation is the answer to
  // a POST: its URL is `/admin/signout`, so Refresh there is a GET of a route
  // that has no GET — the gate sees no session and sends the reader into a
  // fresh sign-in flow. A control labelled *load this page again* that instead
  // starts a sign-in is worse than one that is missing, and the sentence above
  // is not true of that page either: what it describes is a session that has
  // ENDED, which is the one thing on this console that will not have changed
  // since it was drawn.
  refreshLink(req, gate) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.refreshLink().");
    if (gate && gate.enforced && !gate.session) {
      log.debug("Leaving AdminConsole.refreshLink().");
      return '';
    }
    log.debug("Leaving AdminConsole.refreshLink().");
    return '<a class="btn secondary" href="' + this.esc(this.refreshHref(req)) +
      '" title="Load this page again. Everything in this console is live ' +
      'state held in memory.">Refresh</a>';
  }

  // ---------------------------------------------------------------------------
  // THE SIGN OUT BUTTON, at the top of every page in this console (2026-09-06).
  //
  // **A FORM AND NOT A LINK, WHICH IS THE OPPOSITE OF refreshLink() ABOVE**,
  // and the two are worth reading together because the reasons are opposite
  // too. A refresh is a GET of the page you are on: safe, repeatable, and a
  // browser or a scanner following it costs nothing. A sign-out CHANGES STATE,
  // and a GET that ends somebody's session is one a link prefetcher, a mail
  // scanner or a `<link rel=prefetch>` can fire without anybody clicking
  // anything — so it is a POST, it carries this session's CSRF token
  // (`withCsrf()` puts it there, and the gate checks it), and it cannot be
  // triggered by navigation.
  //
  // **IT IS DRAWN ONLY WHEN SOMEBODY IS SIGNED IN.** With the gate off there
  // may be no session at all, and a Sign out button that signs nobody out is a
  // control whose only possible outcome is a refusal — the same test that keeps
  // `/admin/applications/new`'s conditional fields off a form that cannot use
  // them, and `newUserPage()`'s whole form off a process with no directory.
  // ---------------------------------------------------------------------------
  signOutControl(gate) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.signOutControl().");
    if (!gate || !gate.session) {
      log.debug("Leaving AdminConsole.signOutControl(). Nobody is signed in.");
      return '';
    }
    log.debug("Leaving AdminConsole.signOutControl(). Drawn for " +
              gate.username + ".");
    return '<form class="signout" method="post" action="' +
           this.esc(SIGNOUT_PATH) +
      '"><button ' +
      'class="secondary" title="' +
      this.esc('End this console session and the sign-on session behind it. ' +
               'You are signed in as ' + gate.username + '. Signing out of ' +
               'the console alone would not sign you out: the next page ' +
               'would run the sign-in flow, meet the sign-on session that is ' +
               'still live and let you straight back in.') +
               '">Sign out</button></form>';
  }

  // ---------------------------------------------------------------------------
  // THE ACCOUNT MENU, at the top of every page in this console (2026-09-10).
  //
  // It holds two things — a link to this person's OWN account in the user
  // portal, and the Sign out button that used to sit bare in that corner. They
  // belong together because they are the two controls on this console that are
  // about the READER rather than about the service: everything else on every
  // page here changes what some protocol endpoint does for somebody else.
  //
  // **IT IS A `<details>`, WHICH IS TO SAY IT IS NOT A SCRIPT.** `script-src
  // 'none'` covers every page of this console but `/admin/api-explorer` (see
  // CLAUDE.md), and the test for an exception is that the page CANNOT work
  // without one — which a menu plainly can: `<details>`/`<summary>` opens and
  // closes with no JavaScript at all, and it is the same answer the console's
  // collapsible prose got. A new scripted page needs the argument made from
  // scratch and "it is a menu, menus have scripts" is not one.
  //
  // **WHAT THAT COSTS IS SAID OUT LOUD RATHER THAN LEFT TO BE DISCOVERED**: an
  // open `<details>` does not close when you click somewhere else on the page,
  // because closing it would take a listener on the document. It closes when
  // the summary is clicked again, and it is closed on every page load because
  // nothing remembers it. That is the same trade the two pictures made when
  // they lost pan and zoom, and the collapse-all switch the console does not
  // have.
  //
  // **IT IS DRAWN ONLY WHEN SOMEBODY IS SIGNED IN**, which is
  // `signOutControl()`'s test read once more rather than a new one: with the
  // gate off there may be no session at all, and a menu whose first row is
  // "your account" and whose second signs nobody out would be two controls that
  // cannot do anything. The summary is the USERNAME, so the answer to "who am I
  // signed in as here" is on every page of this console without opening
  // anything — which the bare button never gave.
  // ---------------------------------------------------------------------------
  userMenu(req, gate) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.userMenu().");
    if (!gate || !gate.session) {
      log.debug("Leaving AdminConsole.userMenu(). Nobody is signed in.");
      return '';
    }
    const html = '<details class="usermenu">' +
      '<summary title="' +
      this.esc('Your own account. This console is read as ' + gate.username +
               ', and the two controls in here are the ones about YOU rather ' +
               'than about the service.') + '">' + this.esc(gate.username) +
               '</summary>' +
      '<div class="usermenupanel">' +
      '<p class="usermenuwho">Signed in as <strong>' + this.esc(gate.username) +
      '</strong></p>' +
      '<a href="' + this.esc(this.portalHref(req, gate)) + '" title="' +
      this.esc('Your own account in the user portal — your password, your ' +
               'authenticator app, your security keys and the applications ' +
               'you can be signed in to. It is a different application from ' +
               'this console and it signs you in with the session you ' +
               'already hold, so nothing is typed again. The link is to the ' +
               'portal of the realm you signed in through, because that is ' +
               'where your account is, whichever realm you are reading.') +
               '">My account</a>' +
      this.signOutControl(gate) +
      '</div></details>';
    log.debug("Leaving AdminConsole.userMenu(). Drawn for " + gate.username +
              ".");
    return html;
  }

  realmChooser(req) {
    const { log, realms } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.realmChooser().");
    if (!realms.active()) {
      log.debug("Leaving AdminConsole.realmChooser(). No realms are defined.");
      return '';
    }
    const currentId = realms.currentId();
    const options = realms.list().map(function (realm) {
      return '<option value="' + self.esc(realm.id) + '"' +
        (realm.id === currentId ? ' selected' : '') + '>' +
        self.esc(realm.name) + '</option>';
    }).join('');
    log.debug("Leaving AdminConsole.realmChooser(). " + realms.count() +
              " realm(s).");
    return '<form class="realmpick" method="get" action="' +
      this.esc(this.realmRoot(req) + REALM_SWITCH_PATH) + '">' +
      '<label for="realmpick">Trust realm</label>' +
      '<input type="hidden" name="to" value="' +
        this.esc(this.realmRelativePath(req)) + '">' +
      '<div class="realmpickrow">' +
        '<select id="realmpick" name="realm">' + options + '</select>' +
        '<button class="secondary">Go</button>' +
      '</div></form>';
  }

  // ---------------------------------------------------------------------------
  // EVERY SECTION HEADING AND EVERY FIELD GETS A TOOLTIP, DERIVED (2026-09-05).
  //
  // **IT IS A PASS OVER THE RENDERED BODY AND NOT 391 EDITED CALL SITES**, and
  // that is the same decision the folds made: the test is on the RENDERED text,
  // not on the caller's judgement. A page written tomorrow gets its tooltips
  // with nothing added to it, and none of them can drift from the prose they
  // are taken from, because they ARE that prose read at render time. A
  // hand-written hint beside each control would have been 391 new strings and a
  // second copy of every explanation on this console — the exact drift the
  // derived-summary rule exists to prevent.
  //
  // TWO RULES, and they differ because the two things differ:
  //
  //   * **A HEADING** takes the opening sentence of the FIRST note that follows
  //     it, before the next heading. That note is what the section is about, so
  //     its first sentence is what the heading means. The note stays where it
  //     is — sections keep their folds — so this adds a hover and removes
  //     nothing.
  //   * **A FIELD** takes the nearest note ABOVE it, because a hand-built form
  //     is explained by the paragraph introducing it rather than per control.
  //     Every field in one form therefore shares a tooltip, which is honest:
  //     that paragraph is genuinely what all of them are for, and a per-field
  //     sentence does not exist to be derived. Where a real per-field
  //     description DOES exist the caller has already set `title` itself —
  //     `configRow()` and the two other settings rows do — and this pass never
  //     touches an element that has one.
  //
  // **IT ONLY EVER ADDS.** An element that already carries a `title` is left
  // exactly as it was, so every hand-placed tooltip in this file still wins and
  // this can never overwrite a better one.
  //
  // The regexes are deliberately narrow — `<h2>`/`<h3>` with no attributes at
  // all, and `<label>` with none — because this console generates its own
  // markup and those are the two shapes it generates. Anything with an
  // attribute already is either hand-tooltipped or doing something this pass
  // should not guess about.
  withDerivedTips(inner) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.withDerivedTips().");
    const body = String(inner || '');
    let added = 0;

    // The plain text of the first note or fold summary in a fragment.
    const firstProseIn = function (fragment) {
      log.debug("Entering firstProseIn().");
      const summary = /<summary[^>]*>([\s\S]*?)<\/summary>/.exec(fragment);
      if (summary) {
        log.debug("Leaving firstProseIn().");
        return self.plainTextOf(summary[1]);
      }
      const note = /<p class="note"[^>]*>([\s\S]*?)<\/p>/.exec(fragment);
      log.debug("Leaving firstProseIn().");
      return note ? self.plainTextOf(note[1]) : '';
    };

    // HEADINGS. The window is from the heading to the next heading, so a
    // section with no prose of its own borrows nothing from the one below it.
    //
    // The tag may already carry attributes — `<h3 id="member">` is how every
    // anchored subsection on this console is written — so the match keeps them
    // and inserts the title beside them. A tag that already has a `title` is
    // skipped whole, which is the "it only ever adds" rule.
    const out = body.replace(/<(h2|h3)([^>]*)>/g,
                             function (whole, tag, attrs, at) {
      if (/\stitle=/.test(attrs)) {
        return whole;
      }
      const rest = body.slice(at);
      const next = /<h[123][\s>]/.exec(rest.slice(whole.length));
      const window = next ? rest.slice(0, whole.length + next.index) : rest;
      const prose = firstProseIn(window);
      if (!prose) {
        return whole;
      }
      added += 1;
      return '<' + tag + attrs + self.tip(prose, Infinity) + '>';
    });

    // FIELDS. `lastIndexOf` rather than a forward scan: the note that explains
    // a form is above it, and the nearest one above is the one that explains
    // this form rather than the previous section's.
    //
    // `<label for="...">` is the settings rows' shape and they set their own
    // title, so those are skipped by the same rule as the headings — but a
    // hand-built `<label for="x">` with no title is not, which is why the match
    // cannot simply be the bare tag.
    const withFields = out.replace(/<label([^>]*)>/g,
                                   function (whole, attrs, at) {
      if (/\stitle=/.test(attrs)) {
        return whole;
      }
      const before = out.slice(0, at);
      const start = Math.max(before.lastIndexOf('<p class="note"'),
                             before.lastIndexOf('<summary'));
      if (start < 0) {
        return whole;
      }
      const prose = firstProseIn(before.slice(start));
      if (!prose) {
        return whole;
      }
      added += 1;
      return '<label' + attrs + self.tip(prose, Infinity) + '>';
    });

    log.debug("Leaving AdminConsole.withDerivedTips(). " + added +
              " tooltip(s) derived.");
    return withFields;
  }

  // ---------------------------------------------------------------------------
  // THE SIDEBAR COLUMN, AND WHO GETS ONE (2026-09-10).
  //
  // **IT IS DRAWN ONLY FOR A READER WHO CAN REACH SOMETHING IN IT**, which is
  // the same test `signOutControl()` above applies to its own button, read one
  // step further. Every row of that nav is a console page behind the gate: with
  // the gate ON and no session, every one of them answers a redirect to the
  // sign-in screen, so the column is forty links that all go to one place.
  //
  // **THE PAGE THAT FOUND IT IS THE SIGN-OUT PAGE.** `POST /admin/signout`
  // draws its confirmation through `page()` with the gate state read again —
  // which is right, and is what takes the Sign out button out of the corner —
  // and left the whole navigation column standing beside a page whose subject
  // is that the session it navigates with has ended. The realm switcher went
  // with it, and that one is worse than the links: it is a FORM, so using it
  // posted to a console that no longer had a session for it.
  //
  // TWO STATES GET THE COLUMN AND WHAT DECIDES IT IS THE GATE, NOT THE SESSION.
  // A service with the gate off has no session either and every page in that
  // nav is reachable, so `enforced` is what the test reads first and `session`
  // only decides it when the gate is on. A caller that passes no gate state at
  // all (the query refusal further down this file) is treated as the ordinary
  // case, because that page is drawn AFTER the gate has let the request
  // through.
  //
  // WHAT REPLACES IT IS NOTHING. `.main` is `flex:1 1 32rem`, so the card takes
  // the width the column was holding. A column emptied of its nav and keeping
  // its two brand lines would be an inch of white space with nothing in it —
  // and what those lines say is which REALM the pages are about, which is a
  // sentence about pages that are not there.
  // ---------------------------------------------------------------------------
  sideColumn(active, up, req, gate) {
    const { log, realms, config } = this.deps;
    log.debug("Entering AdminConsole.sideColumn().");
    if (gate && gate.enforced && !gate.session) {
      log.debug("Leaving AdminConsole.sideColumn(). Nobody is signed in, so " +
                "there is nothing in it that could be reached.");
      return '';
    }
    const html =
        '<aside class="side">' +
        '<p class="brand">Mock STS admin</p>' +
        // WHAT THIS SERVICE IS AND WHICH REALM YOU ARE IN, rather than the
        // WS-Trust issuer identifier that used to be here.
        //
        // That identifier is `wstrust.issuer`, and it was never the name of
        // this service — it is what ONE of the sixteen protocol families puts
        // in an <Issuer> element. In the corner of a console whose other
        // fifteen families never mention it, it read as the service's identity
        // and is not; and it is the one line on the page that is drawn before
        // the reader knows what the page is about, so it should say something
        // true of the whole of it.
        //
        // THE REALM IS THE THING WORTH SAYING THERE. Every page in this console
        // shows exactly one realm, `/admin/config` WRITES the one it is read
        // in, and a realm is a whole logical copy of this service — so "which
        // one am I looking at" is the question a reader most needs answered
        // before they act. The switcher below says it too, but only when a
        // realm has been DEFINED: it is absent in the ordinary case, which is
        // precisely the case where the corner was saying nothing useful at all.
        //
        // THE ISSUER IDENTIFIER IS NOT IN THIS SHELL AT ALL ANY MORE, and that
        // is the second half of one decision rather than a later reversal of
        // it. It came out of this corner on 2026-08-24 into the line under the
        // heading, and off the shell entirely on 2026-08-25, for the reason
        // above read once more: a name that ONE of sixteen protocol families
        // uses, repeated at the top of every page in the console, reads as the
        // service's identity on all of them and means something on the handful
        // about WS-Trust.
        //
        // NOTHING IS LOST, and here is where each half of it went. It is on
        // `/admin/sts-metadata`, the one page whose subject is what this
        // service IS; it is on `/admin/config` under WS-Trust, which is where
        // it is SET; and it is in this line's tooltip, because somebody who had
        // learnt to read it off the shell should find it where they look rather
        // than have to hunt.
        '<p class="brandsub" title="' +
          this.esc('This console is showing the trust realm "' +
                   realms.current().name +
                   '" (id: ' + realms.currentId() + '). Its WS-Trust issuer ' +
                   'identifier ' +
                   'is ' + config.value('wstrust.issuer') + ', which is what ' +
                   'that protocol puts in an <Issuer> element and is not the ' +
                   'name of this service.') +
          '">Mock STS &middot; ' + this.esc(realms.current().name) + '</p>' +
        this.navBar(active, up, req) +
        '</aside>';
    log.debug("Leaving AdminConsole.sideColumn(). " + html.length + " bytes.");
    return html;
  }

  // ---------------------------------------------------------------------------
  // WHAT THIS PROCESS IS RUNNING AS, under the version at the foot of every
  // page (2026-09-13).
  //
  // Four facts somebody asks in front of this console as often as "which
  // build": whether requests are DISPATCHED to request workers or answered by
  // one process, whether the realm is in PRODUCT or DEVELOPMENT mode, which
  // PERSISTENCE store is open, and where the two primordial SECRETS — the
  // key-encryption key and the database password — are read from. Each answer
  // was a page away (`/admin/persistence`, `/admin/secrets`, `global.mode` on
  // `/admin/config`), and the pages that change what an endpoint does are
  // exactly where a reader needs to know which of those worlds they are
  // changing it in.
  //
  // **EVERY FACT IS READ FROM THE MODULE THAT OWNS IT, PER RENDER.** The mode
  // is runtime-settable and per realm, the store's `status()` is what
  // `/admin/persistence` draws, and `secrets.describe()` is what
  // `/admin/secrets` draws — so the footer cannot say something those pages do
  // not. It says WHERE a secret comes from and never WHAT it is, which is
  // `describe()`'s own rule.
  //
  // **IT MAY NOT BREAK A PAGE.** Each fact is computed inside its own guard and
  // a fact that throws is drawn as `unknown`: a footer that took the console
  // down would cost every page for one line of information.
  // ---------------------------------------------------------------------------
  runtimeFacts(): Record<string, any> {
    const { log, config, mode, persistence, secrets, keystore } = this.deps;
    log.debug("Entering AdminConsole.runtimeFacts().");
    const facts: Record<string, any> = {};
    try {
      const count = parseInt(config.value('workers.requestCount'), 10) || 0;
      const rawDispatch = config.value('workers.dispatch');
      const dispatch = (Array.isArray(rawDispatch) ? rawDispatch
                                                   : String(rawDispatch || '')
                                                       .split(','))
        .map(function (one) { return String(one).trim(); })
        .filter(Boolean);
      // AND THE HOSTED-SURFACE POOL (2026-09-13), which is where this page is
      // drawn from whenever there is one — so "this page from worker N" names
      // which pool N is in, or it would read as a protocol worker.
      const surfaceCount =
        parseInt(config.value('workers.surfaceCount'), 10) || 0;
      const pool = process.env.STS_REQUEST_WORKER_POOL === 'surfaces'
        ? 'hosted-surface ' : '';
      facts.process = (count > 0 || surfaceCount > 0) && dispatch.length
        ? 'dispatch (' + count + ' request worker' + (count === 1 ? '' : 's') +
          (surfaceCount
            ? ' + ' + surfaceCount + ' for the console and portal' : '') +
          (process.env.STS_REQUEST_WORKER
            ? '; this page from ' + pool + 'worker ' + process.pid : '') + ')'
        : 'single process';
    } catch (e) {
      log.debug("Caught in AdminConsole.runtimeFacts(): " +
                ((e && e.message) || e));
      // Drawn as unknown rather than guessed; see the header.
      facts.process = 'unknown';
    }
    try {
      facts.mode = mode.current();
    } catch (e) {
      log.debug("Caught in AdminConsole.runtimeFacts(): " +
                ((e && e.message) || e));
      // Drawn as unknown rather than guessed; see the header.
      facts.mode = 'unknown';
    }
    let store = null;
    // THE STORE THAT IS OPEN, which is not always the one configured:
    // `status().mode` is `memory` until `persistence.start()` has opened the
    // configured store, and a store that cannot open stops the service — so a
    // mismatch is only ever a process that has not opened it, and it is SAID
    // rather than hidden behind the configured name.
    let effective = 'memory';
    try {
      store = persistence.status();
      effective = String(store.mode || store.configuredMode || 'memory');
      const db = store.database;
      facts.database = effective === 'postgres'
        ? (db ? 'postgres (' + db.host + ':' + db.port + '/' + db.database + ')'
              : 'postgres')
        : effective === 'ldif'
          ? 'ldif (' + (store.dataDir || 'no data directory') + ')'
          : effective === 'memory' ? 'memory (nothing written down)'
                                   : effective;
      if (store.mode && store.configuredMode &&
          store.configuredMode !== store.mode) {
        facts.database += ' — persistence.mode is ' + store.configuredMode +
                          ' and no such store is open in this process';
      }
    } catch (e) {
      log.debug("Caught in AdminConsole.runtimeFacts(): " +
                ((e && e.message) || e));
      // Drawn as unknown rather than guessed; see the header.
      facts.database = 'unknown';
    }
    try {
      const kek = secrets.describe();
      const kekFrom = kek.configured
        ? kek.label + (kek.where && kek.where.from ? ' (' + kek.where.from + ')'
                                                   : '')
        : 'not configured';
      facts.keyEncryptionKey = keystore.persists()
        ? kekFrom
        : 'not read (signing keys are generated at start)';
    } catch (e) {
      log.debug("Caught in AdminConsole.runtimeFacts(): " +
                ((e && e.message) || e));
      // Drawn as unknown rather than guessed; see the header.
      facts.keyEncryptionKey = 'unknown';
    }
    try {
      const password = secrets.describeDatabasePassword();
      facts.databasePassword = effective !== 'postgres'
        ? 'not used (no database)'
        : password.configured
          ? password.label + (password.where && password.where.from
            ? ' (' + password.where.from + ')' : '')
          : 'the connection string';
    } catch (e) {
      log.debug("Caught in AdminConsole.runtimeFacts(): " +
                ((e && e.message) || e));
      // Drawn as unknown rather than guessed; see the header.
      facts.databasePassword = 'unknown';
    }
    log.debug("Leaving AdminConsole.runtimeFacts().");
    return facts;
  }

  // **NOT DRAWN FOR A READER WITH NO SESSION**, on the rule `refreshLink()` and
  // `sideColumn()` follow: the shell draws the sign-out page, the callback's
  // refusal and a 401 for somebody the gate has not let in, and this line names
  // a database host and the paths secrets are read from. Those belong to people
  // who may read `/admin/persistence` and `/admin/secrets`, not to anybody who
  // can reach the sign-out page.
  runtimeFooter(gate) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.runtimeFooter().");
    if (gate && gate.enforced && !gate.session) {
      log.debug("Leaving AdminConsole.runtimeFooter(). Nobody is signed in.");
      return '';
    }
    const facts = this.runtimeFacts();
    // A REALM ADMINISTRATOR (2026-09-14, #32) may not read `/admin/persistence`
    // or `/admin/secrets`, so they are not handed the database host, the
    // secret-store paths or the process arrangement those pages draw — nor the
    // two links, which would only answer 403. The realm's mode is theirs.
    if (gate && gate.authority === 'realm') {
      log.debug("Leaving AdminConsole.runtimeFooter(). A realm authority.");
      return '<div class="runtime">mode <code>' + this.esc(facts.mode) +
        '</code></div>';
    }
    const html = '<div class="runtime">' +
      'running as <code>' + this.esc(facts.process) + '</code>' +
      ' &middot; mode <code>' + this.esc(facts.mode) + '</code>' +
      ' &middot; database <code>' + this.esc(facts.database) + '</code>' +
      ' &middot; secret store: key-encryption key <code>' +
      this.esc(facts.keyEncryptionKey) + '</code>, database password <code>' +
      this.esc(facts.databasePassword) + '</code>' +
      ' &middot; <a href="/admin/persistence">persistence</a>, ' +
      '<a href="/admin/secrets">secrets</a></div>';
    log.debug("Leaving AdminConsole.runtimeFooter().");
    return html;
  }

  page(title, active, inner, up, gate, req) {
    const { log, applications, persistence } = this.deps;
    log.debug("Entering AdminConsole.page(). title=" + title + ", up=" +
              (up ? up.href : "none"));
    const html = '<!DOCTYPE html>\n<html lang="en"><head><meta ' +
      'charset="utf-8"><meta name="viewport" content="width=device-width, ' +
      'initial-scale=1"><title>' + this.esc(title) + ' — mock STS ' +
      'admin</title><style>body{font-family:system-ui,-apple-system,"Segoe ' +
      'UI",Arial,sans-serif;background:#f4f4f7;margin:0;padding:2rem ' +
      '1rem;color:#222;line-height:1.45}' +
      // THE TWO COLUMNS. Flex rather than grid because the behaviour wanted at
      // a narrow width is "the sidebar stops being a column and becomes a block
      // above the page", which is what `flex-wrap` does for nothing — and this
      // console runs no script, so a layout that needed one would not be an
      // option anyway. The sidebar's `flex` is `0 0 auto` with a fixed basis so
      // that a long label wraps inside it instead of widening it and squeezing
      // the tables, which is the failure mode of letting it size to content.
      // THE CAP WIDENED FROM 92rem TO 104rem ON 2026-09-01, and the reason is
      // one column on one page rather than a general preference for wide
      // layouts. `/admin/ldap/directory` draws EVERY ATTRIBUTE OF EVERY ENTRY,
      // and an attribute value here is routinely an opaque forty-character
      // identifier — a DN, a certificate thumbprint, a client secret — so that
      // column is the widest thing in this console by a distance. At 92rem it
      // spilled past the card on a laptop. The extra twelve are what let the
      // shortening below (clipped()) keep a value readable instead of cutting
      // it to a stub, and every other page is unaffected: `.main` is `flex:1 1
      // 32rem`, so a page whose content is narrower stays the width its content
      // wants.
      '.shell{display:flex;flex-wrap:wrap;align-items:flex-start;gap:18px;' +
      'max-width:104rem;margin:0 auto}' +
      // THE SIDEBAR SCROLLS ITSELF RATHER THAN WITH THE PAGE. `position:sticky`
      // alone was not enough and the reason is easy to miss: a sticky box that
      // is TALLER than the viewport is pinned by its TOP, so the bottom of the
      // nav sits below the fold and the only way to reach it is to scroll the
      // whole document to its end — which got harder as the console grew, since
      // the page a reader is on decides how far that is. Capping the column at
      // the viewport and letting the nav overflow inside itself makes the two
      // scrollbars independent: the tab list is reachable from anywhere on any
      // page, at any length, and the page under it does not move.
      //
      // The column is a flex COLUMN so that the two lines above the nav keep
      // their natural height and the nav takes what is left; `min-height:0` on
      // the nav is what actually permits it to shrink below its content, which
      // is the flexbox rule that makes an overflow child scroll instead of
      // pushing its parent. The cap is `100vh` less the 1rem the sticky top
      // holds plus 1rem of air below it, which is the geometry once the
      // column is STUCK. Before the first scroll it is still in flow, an inch
      // or so down the document, so the card can reach the bottom edge of the
      // window — CSS cannot subtract an offset it does not know, and the only
      // way to close that inch would be a script this console does not have.
      // It costs nothing: the list is scrollable in that state as well.
      '.side{flex:0 0 13.5rem;position:sticky;top:1rem;max-height:calc(100vh ' +
      '- 2rem);display:flex;flex-direction:column}' +
      // `min-width:0` on a flex child is what stops a wide table inside the
      // card from pushing the whole layout past the viewport: without it the
      // card's minimum width is its content's, and one long DN widens the page
      // rather than scrolling inside its own cell.
      '.main{flex:1 1 ' +
      '32rem;min-width:0}.brand{font-size:.82em;font-weight:700;' +
      'color:#12107c;margin:0 0 ' +
      '2px;letter-spacing:.02em}.brandsub{font-size:.72em;color:#666;' +
      'margin:0 ' +
      '0 14px}.card{background:#fff;border:1px solid ' +
      '#d5d5dd;border-radius:10px;padding:24px 28px;box-shadow:0 6px 24px ' +
      'rgba(0,0,0,.08)}h1{font-size:1.35em;margin:0 0 4px;color:#12107c}' +
      // THE HEAD ROW — the title, and the refresh control pushed to the far end
      // of it. `align-items:baseline` so a control that much smaller sits on
      // the heading's baseline rather than being centred against a taller box,
      // and `flex-wrap` so that at a narrow width it drops underneath the title
      // instead of squeezing it to one word a line. The bottom margin is what
      // `p.sub` used to contribute before the issuer line came off this shell;
      // without it every page's breadcrumb trail sits up against its heading.
      '.pagehead{display:flex;flex-wrap:wrap;gap:10px;align-items:baseline;' +
      'justify-content:space-between;margin:0 0 14px}' +
      '.pagehead h1{margin:0}' +
      // The controls at the right-hand end of that row. `align-items:baseline`
      // again, because a <button> and an <a class=btn> are different boxes and
      // centring them against each other leaves the text half a pixel apart.
      '.pagehead .pagetools{display:flex;gap:8px;align-items:baseline}' +
      '.pagehead form.signout{display:inline;margin:0}' +
      // THE ACCOUNT MENU (2026-09-10). `position:relative` on the <details> and
      // `position:absolute` on the panel, so that opening it OVERLAYS the page
      // rather than pushing the card's first heading down — a menu that reflows
      // the page under it moves whatever the reader was about to click.
      //
      // The summary is drawn as a quiet button so it reads as a control beside
      // Refresh rather than as a word floating in the corner, and the caret is
      // a `::after` because the default triangle sits on the wrong side and
      // cannot be moved. `list-style:none` plus the -webkit- rule is what
      // removes it: the two together, because neither does it in both engines.
      // The same pair is on `details.fold` further down for the same reason.
      '.usermenu{position:relative}' +
      '.usermenu>summary{cursor:pointer;list-style:none;display:inline-flex;' +
      'gap:.4em;align-items:center;border:1px solid ' +
      '#d5d5dd;border-radius:6px;padding:4px ' +
      '10px;background:#fff;color:#333;font-size:.85em}.usermenu>' +
      'summary::-webkit-details-marker{display:none}' +
      '.usermenu>summary::after{content:"\\25be";color:#888;font-size:.9em}' +
      '.usermenu>summary:hover{background:#f4f6fb}' +
      '.usermenu[open]>summary{background:#eef2fb;border-color:#c3d0ea;' +
      'color:#12107c}' +
      // `z-index` because the panel overlaps the card's own content, and
      // `text-align:left` because `.pagetools` sits at the right-hand end of a
      // space-between row and the panel would otherwise inherit that.
      '.usermenupanel{position:absolute;right:0;top:calc(100% + ' +
      '6px);z-index:30;min-width:15rem;background:#fff;border:1px solid ' +
      '#d5d5dd;border-radius:8px;box-shadow:0 8px 28px ' +
      'rgba(0,0,0,.14);padding:8px;text-align:left}.usermenupanel ' +
      '.usermenuwho{margin:0 0 6px;padding:0 ' +
      '8px;color:#666;font-size:.75em}.usermenupanel ' +
      'a{display:block;padding:6px 8px;border-radius:5px;color:#12107c;' +
      'text-decoration:none;font-size:.85em}.usermenupanel ' +
      'a:hover{background:#eef2fb;text-decoration:none}' +
      // The sign-out form is `display:inline` in the rule above, which is right
      // in the head row and wrong in a stacked panel — so it is a block here,
      // separated from the link by the same hairline the nav uses, and its
      // button fills the width so the whole row is the target.
      '.usermenupanel form.signout{display:block;margin:6px 0 ' +
      '0;border-top:1px solid #eee;padding-top:6px}.usermenupanel ' +
      'form.signout button{width:100%}h2{font-size:1.05em;margin:1.8em 0 ' +
      '.5em;color:#12107c;border-bottom:1px solid ' +
      '#eee;padding-bottom:.2em}h3{font-size:.92em;margin:1.2em 0 ' +
      '.4em}p.sub{color:#666;font-size:.85em;margin:0 0 14px}' +
      // THE SIDEBAR. A list per section with a heading over it, in the same
      // card material as the page so the two read as one surface rather than as
      // a frame around a document. `flex:0 1 auto` with `min-height:0` and
      // `overflow-y:auto` is the whole of the independent scrolling described
      // on `.side` above: the card is its natural height while it fits and
      // scrolls inside itself when it does not, so a short nav does not become
      // a stretched empty box.
      'nav{background:#fff;border:1px solid ' +
      '#d5d5dd;border-radius:10px;padding:14px 14px ' +
      '8px;font-size:.85em;box-shadow:0 6px 24px rgba(0,0,0,.06);flex:0 1 ' +
      'auto;min-height:0;overflow-y:auto}' +
      // THE REALM CHOOSER, first inside the nav card and separated from the
      // sections by a hairline rather than by a card of its own. Its label is
      // `.navhead`'s metrics on purpose: it sits above a list of choices in the
      // same column, so anything else would read as a different KIND of thing
      // and put the reader back to wondering which pane it belonged to.
      '.realmpick{margin:0 0 12px;padding:0 0 10px;' +
      'border-bottom:1px solid #e6e6ee}' +
      '.realmpick label{display:block;margin:0 0 4px;font-size:.7em;' +
      'text-transform:uppercase;letter-spacing:.06em;color:#8a8a99;' +
      'font-weight:700}' +
      // The select takes the room and the button takes what it needs, so a long
      // realm name is readable in the column rather than truncated to fit a
      // button beside it.
      '.realmpickrow{display:flex;gap:6px;align-items:stretch}.realmpick ' +
      'select{flex:1 1 auto;min-width:0;font-size:1em;padding:3px ' +
      '4px;border:1px solid ' +
      '#c9c9d4;border-radius:5px;background:#fff;color:#222}.realmpick ' +
      'button{flex:0 0 auto;padding:3px 9px;font-size:.92em}.navsec{margin:0 ' +
      '0 12px}.navsec:last-child{margin-bottom:4px}' +
      // The section heading. Not a link and it must not look like one — there
      // is no page behind a section, which is the same reason it is not a
      // crumb.
      '.navhead{margin:0 0 4px;font-size:.7em;text-transform:uppercase;' +
      'letter-spacing:.06em;color:#8a8a99;font-weight:700}' +
      // The section the current page is in. A rule down its left rather than a
      // background, so that the mark reads as "you are in here" and not as a
      // second selected item beside the selected one.
      '.navsec.open{border-left:3px solid ' +
      '#12107c;margin-left:-14px;padding-left:11px}.navsec.open ' +
      '.navhead{color:#12107c}' +
      // A GROUP INSIDE A SECTION. It is an <li>, so `nav li` has already given
      // it the item metrics; what these rules do is take the item LOOK back off
      // it (it is a heading with a list under it, not a thing to click) and
      // indent the list it holds behind a hairline, which is what says "these
      // three are one thing" without a second colour or a box.
      '.navgrp{margin:6px 0 4px}' +
      // The group heading. Smaller than the section's and sentence-cased rather
      // than upper, so that two headings above one link cannot be read as two
      // sections — the section is the shout, the group is the aside.
      '.navsub{margin:0 0 3px;padding:0 6px;font-size:.78em;color:#6a6a80;' +
      'font-weight:700;letter-spacing:.01em}.navgrp.open ' +
      '.navsub{color:#12107c}.navgrp>ul{margin:0;padding-left:8px;' +
      'border-left:1px solid ' +
      '#e2e2ea}.navgrp.open>ul{border-left-color:#c3c0e0}nav ' +
      'ul{list-style:none;margin:0;padding:0}nav ' +
      'li{margin:0;font-size:1em}nav a,nav .here{display:block;padding:3px ' +
      '6px;border-radius:5px;text-decoration:none;line-height:1.3}nav ' +
      'a{color:#12107c}nav a:hover{background:#f0f0f7}' +
      // THE PAGE YOU ARE ON, AND IT USED TO BE A WHISPER (2026-09-05).
      //
      // It was `background:#eceaf6` — a lavender four shades off the card's own
      // white — which on a list of thirty-odd links read as "very slightly
      // different" rather than as "here". It now takes the same solid brand
      // fill the pager's current page has had all along (`.pagenav .here`), so
      // the two "you are here" markers in this console finally look alike.
      //
      // `scroll-margin` is the other half of the autofocus above: without it
      // the browser reveals the item flush against the top edge of the
      // scrolling card, where it reads as the first item in the list rather
      // than as one somewhere in the middle. Three lines of room above it keeps
      // its neighbours visible, which is what tells a reader where they are.
      'nav .here{font-weight:700;color:#fff;background:#12107c;' +
      'scroll-margin:4.5rem 0}' +
      // The focus ring is suppressed ONLY for the active item, and only because
      // the autofocus above puts focus there on every single page load — a ring
      // drawn around it would be a permanent artefact rather than a signal that
      // the keyboard is there. Every other link in this nav keeps its ring.
      'nav .here:focus{outline:none}' +
      // The active item on a DRILL-DOWN. It keeps the weight and the fill
      // `.here` gives it, because the reader is still inside that section, and
      // takes back the link colour and an underline, because it is a link again
      // and a bold black label reads as text nobody can click — which is
      // exactly what it was.
      'nav a.here{color:#fff;text-decoration:underline;background:#12107c}' +
      '.crumb{font-size:.82em;margin:0 0 14px;color:#666}' +
      '.crumb a{text-decoration:none;font-weight:600}' +
      '.crumb a:hover{text-decoration:underline}' +
      '.crumb .sep{margin:0 .45em;color:#aaa}' +
      // The last crumb is the page being drawn. It is not a link and must not
      // look like one, or the one crumb that does nothing is the one a reader
      // clicks.
      '.crumb ' +
      '.leaf{color:#222;font-weight:600}.warn{background:#fff8e1;border:1px ' +
      'solid #ffe082;padding:9px ' +
      '12px;border-radius:5px;font-size:.82em;margin:0 0 ' +
      '16px}.ok{background:#e8f5e9;border:1px solid #a5d6a7;padding:8px ' +
      '11px;border-radius:5px;font-size:.85em;margin:0 0 ' +
      '14px}.err{background:#fdecea;border:1px solid ' +
      '#f5c6c2;color:#b00020;padding:8px ' +
      '11px;border-radius:5px;font-size:.85em;margin:0 0 14px}' +
      // A VALUE THAT EXISTS ONCE. It is drawn big, monospaced and wrapping —
      // `word-break:break-all` rather than a scroll box — because the two
      // things that land in it are a base64url password and an activation URL,
      // and both are copied by selecting them. A block a reader has to scroll
      // sideways to select the end of is how half a credential gets pasted into
      // a message. `user-select:all` makes one click take the whole value; it
      // is a convenience, and selecting by hand still works where a browser
      // ignores it.
      '.secret{background:#f4f6ff;border:2px solid ' +
      '#12107c;border-radius:6px;padding:12px 14px;margin:0 0 12px;' +
      'font-family:ui-monospace,Menlo,Consolas,monospace;font-size:1em;' +
      'word-break:break-all;user-select:all}.tiles{display:flex;' +
      'flex-wrap:wrap;' +
      'gap:10px;margin:.6em 0 1em}.tile{border:1px solid ' +
      '#e2e2ea;border-radius:8px;padding:10px ' +
      '14px;min-width:9rem;background:#fbfbfd}.tile ' +
      '.n{font-size:1.5em;font-weight:700;color:#12107c;' +
      'line-height:1.1}.tile ' +
      '.l{font-size:.74em;color:#666;text-transform:uppercase;' +
      'letter-spacing:.03em}' +
      // THE FIRST CHART IN THIS CONSOLE (/admin/ssf/dead-letters, 2026-09-14).
      // An inline SVG laid out on the server, so it needs no script; this only
      // lets it shrink with the card, and lays its legend out as one wrapping
      // row of swatch-and-label pairs whose text stays in ink.
      '.chart svg{display:block;width:100%;height:auto;max-width:780px}' +
      '.legend{display:flex;flex-wrap:wrap;gap:4px 18px;font-size:.8em;' +
      'color:#333;margin:.3em 0 .9em}.legend span{display:inline-flex;' +
      'align-items:center;gap:6px}code.ec{white-space:nowrap}' +
      'table{border-collapse:collapse;width:100%;' +
      'margin:.4rem 0 .9rem;font-size:.8em}th,td{border:1px solid ' +
      '#e2e2ea;padding:.3rem .5rem;text-align:left;vertical-align:top}' +
      'th{background:#f0f0f5;font-weight:600}tr:nth-child(even) ' +
      'td{background:#fafafc}td.num,th.num{text-align:right;' +
      'font-variant-numeric:tabular-nums}' +
      // A cell holding a list of opaque identifiers. `overflow-wrap:anywhere`
      // is the one that also shrinks the cell's MINIMUM width, which is the
      // property that matters: break-all alone wraps the text and still lets
      // one unbreakable did:jwk widen the whole table past the card it sits in.
      'td.who{overflow-wrap:anywhere;line-height:1.9}' +
      'td.who code{white-space:normal}' +
      // THE "EVERY ATTRIBUTE" CELL on the four directory pages. One `<div>` per
      // attribute rather than `<br>`-separated text, so a value that DOES wrap
      // (a short multi-valued attribute, say) stays visually under its own
      // name instead of running into the next attribute's line. The cell does
      // not need `overflow-wrap` the way `td.who` does, because everything long
      // in it has been through clipped() — which is the point of that helper.
      'td.attrs>div{margin:.15em 0}' +
      '.vals{display:inline-block;vertical-align:top}' +
      // The DN column on the directory dump. Given a share rather than left to
      // the browser's auto layout, which sizes a column to its widest cell and
      // therefore handed the longest DN in the store a third of the table while
      // the attributes — the column with everything in it — got squeezed.
      'th.dn,td.dn{width:26%}th.from,td.from{width:8%}' +
      // A cell of short counted facts, one per line. Without the nowrap the
      // browser breaks "0 session(s)" across two lines in a narrow column and
      // the number ends up on a line of its own, reading as a value with no
      // label.
      'td.counts{white-space:nowrap}' +

      // A TABLE WITH A COLUMN PER EVENT TYPE, IN A BOX THAT SCROLLS SIDEWAYS.
      // The RISC accounts table is five fixed columns, then one per RISC event
      // type — fourteen of them — then three more; twenty-two columns do not
      // fit the card on a laptop and no amount of shortening inside a cell
      // makes them, because a count is already one character and a heading
      // cannot be narrower than its shortest word. So the TABLE keeps its
      // natural width and the BOX round it scrolls. That is the one arrangement
      // here that takes nothing away: dropping columns loses the number
      // somebody came for, and letting it spill past the card leaves those
      // columns unreachable rather than merely off to one side.
      //
      // `width:auto` with `min-width:100%` is the pair that does the work, and
      // the first half is easy to leave out: `table` above is `width:100%`, and
      // a table told to be exactly its container's width inside a scroll box
      // never overflows it — it squeezes, and the box never scrolls, so the
      // wrapper looks like it did nothing. Auto lets it take what its content
      // needs; the minimum keeps a table that DOES fit looking like every other
      // table on the page instead of shrinking to its text.
      '.wide{overflow-x:auto;max-width:100%}' +
      '.wide>table{width:auto;min-width:100%}' +

      // -------------------------------------------------------------------
      // A VALUE TOO LONG FOR ITS CELL, AND THE POPUP THAT GIVES IT BACK.
      //
      // See clipped() below for what this is for and why the truncation is
      // done in the markup rather than by CSS. What these rules add is the
      // half that has to be a HOVER: the full value, in a box the reader can
      // put the pointer INTO and select out of.
      //
      // IT IS NOT A `title` ATTRIBUTE, and that is the whole design. A native
      // tooltip cannot be selected, so a shortened client secret or DN would
      // be readable and not copyable — and copying it is the entire reason
      // somebody hovers a value on these pages. A `title` is still set beside
      // it, because a native tooltip is what a keyboard user and a screen
      // reader get; the rule this console holds to is that nothing is ever
      // said ONLY in a tooltip, and here the full value is in the document
      // either way.
      //
      // THERE IS NO SCRIPT (app.js sets `script-src 'none'`), so the popup is
      // `:hover` and `:focus-within` and nothing else. `tabindex="0"` on the
      // wrapper is what makes the second one reachable from a keyboard.
      //
      // The box hangs off the BOTTOM EDGE with no gap, so the pointer can
      // travel from the truncated text into the box without crossing dead
      // space and dismissing it — a gap here is the classic way a hover popup
      // becomes unusable. `z-index` puts it over the rows below; `max-width`
      // is in `ch` because what it holds is monospace and the useful measure
      // is characters rather than inches.
      '.trunc{position:relative;border-bottom:1px dotted #9a9ab0;cursor:help}' +
      '.trunc>.full{display:none;position:absolute;z-index:30;left:0;' +
      'top:100%;' +
      'min-width:16rem;max-width:88ch;padding:7px 9px;background:#fffdf3;' +
      'border:1px solid #d9d2a8;border-radius:6px;' +
      'box-shadow:0 6px 18px rgba(0,0,0,.18);font-size:1.05em;' +
      'overflow-wrap:anywhere;white-space:normal;cursor:text}' +
      // ONE CLICK SELECTS THE WHOLE VALUE. `user-select:all` is on the `code`
      // and not on the box, so the hint under it stays out of the selection —
      // a copy that carried "58 characters — select to copy" into somebody's
      // configuration file would be a helper that breaks the thing it helps.
      '.trunc>.full code{user-select:all;-webkit-user-select:all;' +
      'background:transparent;padding:0}.trunc:hover>.full,.trunc:focus>' +
      '.full,' +
      '.trunc:focus-within>.full{display:block}' +
      // The one-line reminder under the value. `user-select:none` so that a
      // reader who selects the box to copy it does not carry this sentence
      // into their clipboard with the value — which would be a tooltip that
      // breaks the thing it exists to help with.
      '.trunc>.full .hint{display:block;margin-top:5px;font-size:.72em;' +
      'color:#7a7460;font-family:system-ui,-apple-system,sans-serif;' +
      'user-select:none;-webkit-user-select:none}.state-valid{color:#0b6b4f;' +
      'font-weight:600}.state-expired{color:#8a6d00}' +
      '.state-revoked{color:#b00020;font-weight:600}.state-none{color:#666}' +
      'form.inline{display:inline;margin:0}button{padding:5px ' +
      '10px;border-radius:5px;border:1px solid #12107c;background:#12107c;' +
      'color:#fff;font-size:.8em;cursor:pointer}' +
      'button.secondary{background:#fff;color:#12107c}' +
      'button.danger{background:#b00020;border-color:#b00020}' +
      // `number` joined this list with /admin/token-lifetimes, whose four
      // inputs are the console's first. Without it they are the one control in
      // the card drawn in the browser's default chrome, beside a form that
      // matches everything else — which reads as a half-finished page rather
      // than as a missing selector.
      'input[type=text],input[type=number],textarea,' +
      'select{box-sizing:border-box;padding:6px 8px;border:1px solid #bbb;' +
      'border-radius:5px;font-size:.85em;font-family:inherit}' +
      'textarea{width:100%;min-height:7rem;font-family:ui-monospace,' +
      'SFMono-Regular,Menlo,monospace}.formrow{display:flex;flex-wrap:wrap;' +
      'gap:8px;align-items:center;margin:.5em 0}.formrow ' +
      'label{font-size:.78em;font-weight:600;color:#555}' +

      // ---------------------------------------------------------------------
      // THE SEARCH PANE UNDER A CHOOSER (chooserPane()). `max-height` with
      // `overflow-y:auto` is the whole point of it rather than trim: the
      // control must be the same size showing one match or twenty, because it
      // sits above the table the reader came for and the `<select>` it replaced
      // was one line whatever the register held. Twenty rows at this size is
      // about 26em, so the pane scrolls from roughly the eleventh — which is
      // what makes the count and the `next 20` link under it the page's own
      // statement about what is off-screen rather than a thing the reader has
      // to notice.
      //
      // `.on` is the entry the page is ALREADY showing, marked because the
      // chooser is drawn under a selected application as well as over a bare
      // list, and a search result identical to the page behind it reads as a
      // link that does nothing.
      // ---------------------------------------------------------------------
      // A FORM THAT IS A FRAGMENT TARGET — the two chooser searches and the
      // delegation table's filter. See chooserPane() for why they submit to
      // themselves by fragment; this is the one rule they need in return.
      // `scroll-margin-top` stops the browser from putting the form flush
      // against the top of the window, and the size of it is measured rather
      // than picked: each of these forms has ONE line above it that says what
      // it is for — a folded sentence over the choosers, the `What happened`
      // heading over the filter — and 3rem is what brings that line back onto
      // the screen with the form. Landing one line lower would hide it, which
      // is the wrong thing to hide from somebody who has just arrived at the
      // control. It is the only effect this class has and the only reason any
      // of those forms has a class at all.
      // ---------------------------------------------------------------------
      // THE PKI PANE (`/admin/pki`). Ten rules, and they are here rather than
      // in `admin-ui/pki_admin.ts` because this is the console's ONE
      // stylesheet: a page with a `<style>` of its own would be the second
      // place a reader has to look for why something is laid out as it is, and
      // `script-src 'none'` is already the reason there is no third.
      //
      // **`.pki-cols` IS THE WHOLE LAYOUT DECISION.** That pane is three blocks
      // — the certificate fields, the key pair and the subject DN — which are
      // one act and read as one act only side by side; stacked, the button at
      // the top of the second is nine screens above the extensions it applies
      // to. `auto-fit` with a 22rem floor drops to two columns and then to one
      // on a narrow window without a media query, which is the same rule the
      // page it is modelled on uses.
      //
      // **`.pki-extlist` IS A COLUMN FLOW AND NOT A GRID**, and that is not
      // taste: a grid lays its items out in ROW order, so twenty-two cards of
      // different heights leave a ragged gap under every short one. A column
      // flow packs them and keeps document order down each column.
      //
      // **THE TEXTAREA RULE IS AN OVERRIDE AND HAS TO BE.** This console's
      // default is `min-height:7rem`, which is right for a policy document and
      // wrong for twenty-odd one-item-per-line boxes: at 7rem each the pane is
      // about four screens of empty box. They are sized by their `rows`
      // attribute here instead, which is what the markup already says.
      // ---------------------------------------------------------------------
      '.pki-cols{display:grid;grid-template-columns:repeat(auto-fit,' +
      'minmax(22rem,1fr));gap:14px;margin:.6em 0 ' +
      '1em;align-items:start}.pki-col{border:1px solid ' +
      '#e2e2ea;border-radius:8px;padding:10px 12px;background:#fbfbfd;' +
      'min-width:0}.pki-group{font-size:.82em;font-weight:700;color:#12107c;' +
      'text-transform:uppercase;letter-spacing:.03em;margin:.9em 0 .4em}' +
      '.pki-col>.pki-group:first-child{margin-top:0}.pki-row{display:flex;' +
      'flex-wrap:wrap;gap:8px;align-items:flex-end;margin:.45em 0}.pki-row>' +
      'label{font-size:.78em;font-weight:600;color:#555;display:flex;' +
      'flex-direction:column;gap:3px}.pki-field{margin:.45em 0}.pki-field>' +
      'label{font-size:.78em;font-weight:600;color:#555;display:block}' +
      '.pki-flag{font-weight:400;font-size:.78em;color:#333;' +
      'display:inline-flex;align-items:center;gap:4px;flex-direction:row}' +
      '.pki-flags{display:flex;flex-wrap:wrap;gap:2px 10px;margin:.3em ' +
      '0}.pki-extlist{columns:24rem 3;column-gap:14px;margin:.6em 0 1em}' +
      '.pki-ext{break-inside:avoid;-webkit-column-break-inside:avoid;' +
      'display:inline-block;width:100%;border:1px solid ' +
      '#e2e2ea;border-radius:8px;padding:8px 10px;margin:0 0 10px;' +
      'background:#fbfbfd}.pki-exthead{display:flex;flex-wrap:wrap;gap:8px;' +
      'align-items:baseline;border-bottom:1px solid ' +
      '#eeeef4;padding-bottom:4px;margin-bottom:5px}.pki-ext ' +
      'textarea,.pki-col ' +
      'textarea{min-height:0}form.finder{scroll-margin-top:3rem}' +
      '.chooser{max-height:13.5em;overflow-y:auto;border:1px solid ' +
      '#e2e2ea;border-radius:6px;background:#fbfbfd;margin:.1em 0 ' +
      '.3em}.chooser ul.hits{list-style:none;margin:0;padding:0}.chooser ' +
      'li{border-bottom:1px solid #eeeef4;padding:.3rem ' +
      '.55rem;font-size:.82em;overflow-wrap:anywhere}.chooser ' +
      'li:last-child{border-bottom:none}.chooser ' +
      'li.on{background:#eef0ff}.chooser ' +
      '.hitwhat{display:block;color:#666;font-size:.9em}.chooser ' +
      'p.none{margin:.55rem;font-size:.82em;color:#666}' +
      'code{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;' +
      'font-size:.85em;background:#f4f4f8;padding:.1rem .25rem;' +
      'border-radius:3px;word-break:break-all}a{color:#12107c}' +
      '.note{font-size:.78em;color:#666;margin:.3em 0 1em}' +

      // ---------------------------------------------------------------------
      // THE FOLDS. See note() for what is folded and why; this is only what one
      // LOOKS like, and two of the rules here are doing more than decoration.
      //
      // `list-style:none` and the ::-webkit-details-marker rule remove the
      // browser's own triangle so that ::before can draw one that points the
      // same way in every engine — Safari's marker and Firefox's differ, and a
      // page of two kinds of triangle reads as two kinds of control. The
      // triangle is drawn in the link colour because that is what this console
      // has already taught a reader to click.
      //
      // The summary is `display:list-item` NOWHERE and flex here on purpose: a
      // summary of two lines with the marker floating beside the first is the
      // one layout that made the fold look broken at narrow widths.
      // ---------------------------------------------------------------------
      'details.fold{margin:.3em 0 1em}details.fold>summary{cursor:pointer;' +
      'list-style:none;display:flex;gap:.45em;align-items:baseline;' +
      'font-weight:600;color:#3a3a4a}details.fold>' +
      'summary::-webkit-details-marker{display:none}details.fold>' +
      'summary::before{content:"\\25b8";color:#12107c;flex:none;' +
      'font-size:.9em}' +
      'details.fold[open]>summary::before{content:"\\25be"}details.fold>' +
      'summary:hover{color:#12107c}' +
      // A SECTION drawn as a fold, for a block whose BODY is a reference
      // table rather than a paragraph. The summary is styled as the <h2> it
      // replaces — same size, same colour, same rule under it — so a reader
      // meets the heading exactly where it was and the table under it costs
      // one click instead of five screens of scrolling. It is not the plain
      // fold's look because a section heading that reads like a paragraph's
      // heading makes a page of eight cards look like a footnote.
      'details.fold.section{margin:1.8em 0 1em}details.fold.section>' +
      'summary{font-size:1.05em;color:#12107c;font-weight:600;' +
      'border-bottom:1px solid #eee;padding-bottom:.2em}details.fold.section>' +
      'summary::before{color:#12107c}details.fold.section>' +
      '.foldbody{margin:.5em 0 0}' +
      // A fold inside a table cell takes the cell's own weight and slant. The
      // summary is emphasised elsewhere because it is a heading over a
      // paragraph; in a column it is the cell's text, and a bold row beside a
      // plain one reads as the bold one meaning more.
      'td details.fold>summary{font-weight:inherit;font-style:inherit;' +
      'color:inherit}td details.fold{margin:0}td details.fold ' +
      '.foldbody{margin:.3em 0 0 ' +
      '1.05em}details.fold>summary:focus-visible{outline:2px solid ' +
      '#12107c;outline-offset:2px;border-radius:3px}' +
      // The body sits under the summary, indented to the width of the marker so
      // that the prose lines up with the words above it rather than with the
      // triangle.
      'details.fold .foldbody{margin:.45em 0 0 1.05em}' +
      'details.fold .foldbody>p:first-child{margin-top:0}' +
      'details.fold .foldbody>p:last-child{margin-bottom:0}' +
      // A folded warning keeps the box, so a page with a caveat on it still
      // looks like one when the caveat is closed.
      'details.warn.fold{margin:.6em 0}' +
      'details.warn.fold>summary{color:#6b4e00}' +
      'details.warn.fold>summary::before{color:#8a6d00}' +
      // A folded list item. The marker of the <li> would sit beside the fold's
      // own triangle, which is two bullets for one item.
      'li.foldli{list-style:none;margin-left:-1.2em}' +
      'li.foldli details.fold{margin:.35em 0}' +

      // ---------------------------------------------------------------------
      // THE TOOLTIP AFFORDANCE, DRAWN BY AN ATTRIBUTE SELECTOR AND NOT BY A
      // MARKER IN THE MARKUP. A label that carries a `title` gets the dotted
      // underline and the help cursor because it carries one — so a caller that
      // adds a tooltip cannot forget to add the sign that there is one, which
      // is the whole failure mode of a tooltip nobody can see. `code[title]` is
      // left alone deliberately: shortened() has put the full value in a title
      // on hundreds of table cells since long before this, and underlining
      // every one of them would draw a page of dotted lines.
      // ---------------------------------------------------------------------
      // A fold that lands inside a form row takes a line of its own. Without
      // this it is a flex item beside the control it captions, which shrinks
      // the summary to a column two words wide.
      '.formrow details.fold{flex-basis:100%;margin:.2em 0 ' +
      '0}label[title]{cursor:help;text-decoration:underline dotted #b4b4c4;' +
      'text-underline-offset:3px}th[title],.tipword[title]{cursor:help;' +
      'text-decoration:underline dotted #b4b4c4;text-underline-offset:3px}' +
      // THE OVERVIEW PAGE'S DERIVED LIST OF EVERY PAGE HERE (consoleGuide()).
      // Its shape is the sidebar's — sections, one group level under Protocols,
      // pages under that — so it is given the sidebar's rhythm rather than the
      // body text's: the section heading close to its own list, the section's
      // `what` tight under the heading rather than at `.note`'s full paragraph
      // spacing, and a group's title and description on their own lines above
      // its three pages. Without the last of those a group reads as a fourth
      // page whose blurb happens to be long.
      '.guide h3{margin:1.6em 0 .1em;color:#12107c}' +
      '.guide p.note{margin:.1em 0 .5em}' +
      '.guide li{margin:.5em 0}' +
      // The page's link and its folded description read as one row: the fold
      // sits tight under the link rather than at a paragraph's distance.
      '.guide details.fold{margin:.1em 0 .1em .1em}.guide ' +
      'details.fold>summary{font-weight:400;color:#666}.guide ' +
      '.grp{display:block;font-weight:700;color:#12107c}.guide ' +
      '.grpwhat{display:block;font-size:.92em;color:#666;margin:.1em 0 .2em}' +
      // A page in the nav that nobody described. Marked rather than dropped —
      // see consoleGuide() — so it has to LOOK like a report and not like prose
      // somebody wrote on purpose.
      '.guide .undescribed{color:#a33}.meta{margin-top:22px;padding-top:12px;' +
      'border-top:1px solid #eee;font-size:.76em;color:#666}.meta ' +
      'div{margin:3px 0}ul{margin:.3em 0;padding-left:1.2em}li{margin:.25em ' +
      '0;font-size:.85em}' +
      // The head copy of this control is a fragment TARGET — see pageNavPair()
      // for why every paging link carries `#list-<param>`. The scroll margin is
      // the same 3rem the search forms take and for the same reason: the line
      // immediately above a pager is the heading or the filter that says which
      // list it moves, and landing flush would put that line just off-screen.
      '.pagenav{display:flex;flex-wrap:wrap;gap:6px;align-items:center;' +
      'margin:.5em 0;font-size:.8em;scroll-margin-top:3rem}.pagenav ' +
      'a,.pagenav .here,.pagenav .off{display:inline-block;padding:3px ' +
      '8px;border-radius:5px;border:1px solid #d5d5dd;background:#fff;' +
      'text-decoration:none;min-width:1.6em;text-align:center}.pagenav ' +
      '.here{background:#12107c;border-color:#12107c;color:#fff;' +
      'font-weight:700}.pagenav .off{color:#aaa;background:#f7f7fa}.pagenav ' +
      '.where{border:0;background:none;color:#666;padding-left:.4em}' +
      // ---------------------------------------------------------------------
      // THE SERVICE METADATA PAGE'S OWN CLASSES, AND WHY THEY ARE IN THIS FILE
      // RATHER THAN IN THE ONE THAT USES THEM.
      //
      // `/admin/sts-metadata` is built by `../sts_metadata.js` — it derives its
      // whole content from the live express router, which is why it is the last
      // module `common/protocol_stack.ts` loads — but it is drawn by page()
      // like every other console page, and page() emits the ONLY <style> this
      // console has. A <style> of its own would have to sit inside <body>,
      // which browsers accept and no validator does, and there would then be
      // two stylesheets to keep in step. So its classes live here, the way
      // .tile's and .state-valid's do: a shared stylesheet with a few
      // page-specific rules in it.
      // ---------------------------------------------------------------------
      // A paragraph of explanation above a table. Wider and quieter than body
      // text; `.sub` is the page subtitle and cannot double as this, because
      // that one is metrics-sized and appears once.
      '.lead{color:#555;font-size:.85em;margin:0 0 12px;max-width:62rem}' +
      '.m{font-weight:600;color:#0b6b4f;white-space:nowrap}' +
      '.none{color:#999}' +
      // Why a path is not a link, beside the path. Italic and grey because it
      // is an aside about the row rather than part of it.
      '.why{color:#999;font-size:.85em;font-style:italic}' +
      '.eff{color:#b26a00;cursor:help}' +
      '.bad{color:#b00020;font-weight:600}' +
      'td.p{width:22%}td.n{width:16%}td.s{width:14%}' +
      'td.p a{text-decoration:none}td.p a:hover ' +
      'code{text-decoration:underline}' +
      // A LINK THAT LOOKS LIKE A BUTTON, which is not decoration here: the
      // download control has to be an <a download> — these pages run no
      // script, so nothing else can hand a browser a file — and a
      // control that saves a document should not read as a sentence.
      'a.btn{display:inline-block;padding:5px 10px;border-radius:5px;' +
      'border:1px solid #12107c;background:#12107c;color:#fff;font-size:.8em;' +
      'text-decoration:none}' +
      'a.btn:hover{background:#0d0b5e;color:#fff}' +
      // The quiet form, matching button.secondary below, for a link that is on
      // every page and is nobody's next action. Both rules out-specify the two
      // above them, so the order they are written in here does not matter.
      'a.btn.secondary{background:#fff;color:#12107c}' +
      'a.btn.secondary:hover{background:#eef0fb;color:#12107c}' +
      // The protocol cards at the top of that page. Flex rather than grid for
      // the reason the shell is: what is wanted at a narrow width is "the row
      // becomes fewer columns", which flex-wrap does for nothing. `flex:1 1
      // 15rem` lets the last row stretch rather than leaving a ragged gap.
      '.protos{display:flex;flex-wrap:wrap;gap:9px;margin:.6em 0 1.4em}' +
      '.proto{flex:1 1 15rem;border:1px solid #e2e2ea;border-radius:8px;' +
      'padding:9px 12px;background:#fbfbfd}' +
      '.proto a,.proto .n{font-weight:700;font-size:.85em;line-height:1.3}' +
      '.proto .n{color:#222}' +
      '.proto .d{font-size:.75em;color:#666;margin-top:3px;line-height:1.4}' +
      // The count and the specification links. Smallest thing on the card,
      // because it is the one part a reader scans rather than reads.
      '.proto .c{font-size:.72em;color:#8a8a99;margin-top:5px}' +
      // ---------------------------------------------------------------------
      // /admin/delegation/map's OWN CLASSES, here for the reason the service
      // metadata page's are: page() emits the console's ONLY <style>, and a
      // second one inside <body> is markup no validator accepts.
      // ---------------------------------------------------------------------
      // THE FRAME ROUND THE DIAGRAM, AND `overflow:auto` IS THE WHOLE OF IT. An
      // SVG is generated at its natural size and a busy one is wider than the
      // card; without this the picture either widens the page past the viewport
      // (the failure `min-width:0` on `.main` exists to prevent, arriving by
      // another door) or is squeezed to fit and becomes unreadable. It scrolls
      // inside its own box instead, and the filter above it is how a reader
      // makes it small enough not to need to.
      '.diagram{overflow:auto;max-width:100%;border:1px solid #e2e2ea;' +
      'border-radius:8px;background:#fff;padding:12px;margin:.6em 0 .4em}' +
      // `max-width:none` states that the picture is not to be scaled to the
      // box, because a diagram scaled down is one whose labels have stopped
      // being legible while still looking as though they should be.
      '.diagram svg{display:block;max-width:none}' +
      // The key. A table like every other, with the shape column sized to the
      // swatches rather than to its heading.
      'table.key td.art{width:5.5rem;text-align:center;vertical-align:middle;' +
      'background:#fff}' +
      'table.key td.art svg{vertical-align:middle}' +
      // The narrow case. One breakpoint and no more: below it the sidebar stops
      // being sticky and sits above the page as an ordinary block, which is
      // what flex-wrap has already done to it by then — the rule only undoes
      // the stickiness, which on a full-width block would pin the whole nav to
      // the top of the viewport and take the screen with it. The cap and the
      // nav's own scrollbar come off with the stickiness, and for the same
      // reason: down here the sidebar is a full-width block ABOVE the page, so
      // a scrollbox inside it would be a second scrolling region in the
      // ordinary flow of the document with nothing pinned beside it to make
      // sense of.
      '@media (max-width:56rem){.side{position:static;flex:1 1 100%;' +
      'max-height:none;display:block}nav{overflow-y:visible}' +
      '.navsec{display:inline-block;vertical-align:top;min-width:11rem;' +
      'margin-right:1.2em}.navsec.open{margin-left:0;padding-left:0;' +
      'border-left:0;border-top:3px solid #12107c;padding-top:6px}}' +
      // ---------------------------------------------------------------------
      // /admin/applications/new's CONDITIONAL FIELDS, and the reason this is
      // CSS and not a script.
      //
      // That page offers a field for every protocol family this service has,
      // and most of them are irrelevant to any one application: somebody
      // registering an OAuth client was reading past a SAML entityID, a
      // Kerberos SPN and ten SAML assertion settings to reach the box they came
      // for. So a field is shown only when the family it belongs to is TICKED.
      //
      // `script-src 'none'` holds. This is `:has()` — the checkbox is already
      // in the same form as the fields, and already has an id — so the browser
      // does the whole thing with a selector and this console still ships no
      // script. It is the same answer the collapsible prose blocks got with
      // `<details>`: the test for a script is that the page CANNOT work without
      // one, and a form that shows every field works, it is just longer.
      //
      // THE FALLBACK IS TO SHOW EVERYTHING, which is why the rules are inside
      // `@supports selector(:has(*))`. A browser without `:has()` gets the page
      // exactly as it was before this existed — every field, all the time — and
      // the page says so rather than leaving somebody to wonder why nothing
      // hides. Showing too much is the safe direction: no control a person
      // needs is ever missing, and nothing they type is ever dropped, because
      // the SERVER reads whatever was posted and does not care what was
      // visible.
      //
      // `display:revert` and not `block`: these rules are applied to <tr> as
      // well as to <div>, and `block` on a table row makes it a block box that
      // no longer lines up with the header above it.
      '@supports selector(:has(*)){' +
      'form.newapp .pf{display:none}' +
      // One rule per family, generated from the same PROTOCOLS table the
      // checkboxes are drawn from — so a family added there gets its rule for
      // nothing, and cannot get a checkbox without one.
      applications.PROTOCOLS.map(function (family) {
        return 'form.newapp:has(#proto-' + family.id + ':checked) .pf-' +
               family.id +
               '{display:revert}';
      }).join('') +
      // The prompt that replaces the fields until something is ticked, and its
      // own disappearance. Without it the page below the checkbox table is
      // empty and reads as broken rather than as waiting.
      'form.newapp:has(input[name="protocol"]:checked) ' +
      '.pf-hint{display:none}}.pf-hint{color:#555;font-size:.85em;' +
      'background:#fbfbfd;border:1px ' +
      'dashed #d5d5dd;border-radius:8px;padding:10px 12px;margin:.6em ' +
      '0}' +
      // The off-screen default button of /admin/applications/new — see the
      // form there. Off-screen rather than `display:none`, which some
      // browsers skip when choosing the button Enter presses.
      '.default-submit{position:absolute;left:-10000px;width:1px;height:1px;' +
      'overflow:hidden}' +
      // ---------------------------------------------------------------------
      // /admin/applications/new's RFC 9728 IMPORT (2026-09-13): a checkbox that
      // shows the three ways to give a document, and a pane of three TABS over
      // what was read. Neither needs a script, and neither needs `:has()`: both
      // are the general sibling combinator, which every browser has, so there
      // is no fallback to argue. The checkbox and the three radio buttons come
      // FIRST in their containers precisely so that `~` can reach what follows.
      //
      // THE RADIO BUTTONS POST NOTHING. Each carries `form=` naming a form id
      // no page has, so its form owner is null: the three still make one group,
      // because a radio group is the name within one owner, and none of them is
      // a field of the create form around it. A hidden panel's inputs ARE
      // posted — `display:none` does not take a control out of a form — which
      // is what lets the third tab be edited and then submitted from the first.
      '.prm-source{display:none;margin:.6em 0}' +
      '.prm-use:checked~.prm-source{display:block}' +
      '.prm-tabs{margin:1em 0;border:1px solid #d5d5dd;border-radius:8px;' +
      'background:#fff}' +
      '.prm-tabs>input[type=radio]{position:absolute;opacity:0;width:1px;' +
      'height:1px;margin:0}' +
      '.prm-tablist{display:flex;flex-wrap:wrap;gap:4px;padding:6px 6px 0;' +
      'border-bottom:1px solid #d5d5dd;background:#f6f6fa;' +
      'border-radius:8px 8px 0 0}' +
      '.prm-tablist label{padding:6px 14px;border:1px solid transparent;' +
      'border-bottom:0;border-radius:6px 6px 0 ' +
      '0;cursor:pointer;color:#12107c}.prm-panel{display:none;padding:12px;' +
      'overflow-x:auto}' +
      '.prm-panel pre{white-space:pre-wrap;word-break:break-all;margin:0}' +
      ['json', 'table', 'fields'].map(function (tab) {
        return '#prm-tab-' + tab + ':checked~.prm-panel-' + tab +
               '{display:block}' +
               '#prm-tab-' + tab + ':checked~.prm-tablist label[for="prm-tab-' +
               tab +
               '"]{background:#fff;border-color:#d5d5dd;font-weight:600;' +
               'margin-bottom:-1px}' +
               '#prm-tab-' + tab + ':focus-visible~.prm-tablist ' +
               'label[for="prm-tab-' + tab + '"]{outline:2px solid #12107c}';
      }).join('') +
      '</style></head><body><div class="shell">' +
      this.sideColumn(active, up, req, gate) +
      '<div class="main"><div class="card">' +
      // THE HEAD ROW: the page's title, and the controls that are on every page
      // of this console. See refreshLink() for why it is a link, and userMenu()
      // for why the second one is a <details> and not a script. Two controls
      // rather than one, so they are grouped: `.pagehead` is a space-between
      // row and a bare second child would push the first away from the heading
      // rather than sitting beside it.
      '<div class="pagehead"><h1>' + this.esc(title) + '</h1>' +
        '<div class="pagetools">' + this.refreshLink(req, gate) +
        this.userMenu(req, gate) +
        '</div></div>' +
      this.trailBar(active, up, title) + this.gateBanner(gate) +
      this.withDerivedTips(inner) +
      '<div class="meta">' +
      // The one sentence drawn at the foot of EVERY page in this console, which
      // is why it is the sentence most worth keeping true. It said "everything
      // here is held in memory" for the whole life of this service and that
      // stopped being true on 2026-08-27 for exactly three things. It is less
      // true again since 2026-09-06: in product mode on postgres what this
      // service MINTS persists too, and product mode keeps its signing keys
      // (persistence/CLAUDE.md, common/CLAUDE.md) — which the sentence below
      // does not yet say.
      '<div>' + (persistence.status().enabled
        ? 'The embedded directory, the trust realms and the settings changed ' +
          'here are written down (<a ' +
          'href="/admin/persistence">persistence.mode=' +
          this.esc(persistence.status().mode) + '</a>) and come back on the ' +
          'next start. Everything this service MINTS is still held in memory ' +
          'and dies with the process — the sessions, the tokens, the ' +
          'artifacts and the tickets — like the signing key it regenerates ' +
          'on every start, because a token that outlived the key it was ' +
          'signed under would verify against nothing.'
        : 'Everything on these pages is held in memory and dies with the ' +
          'process, like the signing key this service regenerates on every ' +
          'start. A statistics file that outlived the key that signed the ' +
          'tokens it described would be worse than none — though the ' +
          'directory, the trust realms and the settings changed here CAN be ' +
          'kept, which is <a href="/admin/persistence">Persistence</a> and ' +
          'is off by default.') + '</div><div>Every page here also answers ' +
      '<code>?format=json</code>, and every form also accepts a JSON body, ' +
      'so a test can drive this console without a browser.</div>' +
      // WHICH BUILD OF THIS SERVICE YOU ARE LOOKING AT, on every page of the
      // console for the reason the sentence above it is here: this is the
      // surface that CHANGES what every protocol endpoint does, so "was that
      // fixed in the build I am on" is a question asked in front of it
      // constantly, and an answer that is one page away is an answer somebody
      // guesses instead.
      //
      // The number is what gets quoted; the tooltip carries the provenance —
      // the build instant, the commit where there is one, and whether this is a
      // stamped artifact or a checkout being run. That last distinction is the
      // one worth a tooltip rather than a footnote: two instances reporting
      // different build numbers mean nothing if neither was ever built.
      '<div title="' + this.esc(APP_BUILD_INFO) + '">mock-sts version <code>' +
      this.esc(APP_VERSION.version) + '</code>' +
      (APP_VERSION.stamped ? '' : ' (not a stamped build — this process is a ' +
       'checkout, and the build number is when it started)') + '</div>' +
      // AND WHAT THIS PROCESS IS RUNNING AS — dispatch or single process, the
      // mode, the store and the secret stores. See runtimeFooter().
      this.runtimeFooter(gate) +
      // FOUR closing divs now, not two: the .meta block, the .card it is
      // inside, the .main column that holds the card and the .shell that holds
      // the two columns. Getting this wrong leaves a document that renders and
      // does not parse, which is the kind of thing only a parser notices — and
      // with a flex layout it is worse than that, because one missing tag nests
      // the next page's sidebar inside the last one's card and the failure
      // looks like a CSS bug.
      '</div></div></div></div></body></html>\n';
    log.debug("Leaving AdminConsole.page(). " + html.length + " bytes.");
    return html;
  }

  // THE SIGN-ON SESSION BEHIND IT, for reporting only. The gate does not read
  // this and must not: what lets somebody into this console is the console's
  // own session, and a page that fell back to the provider's would be a relying
  // party reading the provider's cookie — which is the arrangement this
  // replaced.
  consoleSignOn(req) {
    const { log, consoleSession } = this.deps;
    log.debug("Entering AdminConsole.consoleSignOn().");
    log.debug("Leaving AdminConsole.consoleSignOn().");
    return consoleSession(req);
  }

  // ---------------------------------------------------------------------------
  // THE CSRF TOKEN GOES INTO EVERY POST FORM THIS SHELL DRAWS (2026-09-06).
  // OWASP A01/A08.
  //
  // **AT THE SHELL AND NOT AT EACH FORM, DELIBERATELY.** This console builds
  // something like a hundred and forty forms as inline strings across sixty
  // pages, and a scheme that required each author to remember a hidden field is
  // a scheme that is one page away from being incomplete for ever — silently,
  // since a missing token looks exactly like a page that works. Adding it HERE
  // means a page written tomorrow is protected by having been drawn at all.
  //
  // It is a string rewrite, which is the part worth being uncomfortable about,
  // and it is narrow on purpose: it matches the opening tag of a form whose
  // method is post and inserts one input directly after it. It cannot match
  // anything else, because `<form` with `method="post"` is not a sequence that
  // occurs in prose here — and if it ever did, the worst outcome is a stray
  // hidden input in a paragraph rather than a missing control.
  //
  // **A PAGE DRAWN FOR SOMEBODY WITH NO SESSION GETS NO TOKEN AND NEEDS NONE**:
  // `checkCsrf()` passes a request with no session, because there is nothing to
  // forge on behalf of an anonymous caller. See common/websecurity.ts.
  // ---------------------------------------------------------------------------
  withCsrf(req, html) {
    const { log, consoleRpSession, websecurity } = this.deps;
    log.debug("Entering AdminConsole.withCsrf().");
    const session = consoleRpSession(req);
    const field = session ? websecurity.field(session.session.id) : '';
    if (!field) {
      log.debug("Leaving AdminConsole.withCsrf().");
      return html;
    }
    log.debug("Leaving AdminConsole.withCsrf().");
    return String(html).replace(/(<form\b[^>]*\bmethod\s*=\s*["']post["'][^>]*>)/gi,
                                function (whole) { return whole + field; });
  }

  // ---------------------------------------------------------------------------
  // THE ENDPOINTS OF THIS REALM, ON EVERY PROTOCOLS PAGE (2026-09-13).
  //
  // Drawn HERE, in the one function every page goes out through, rather than by
  // forty pages each remembering to: `admin-core/protocol_endpoints.ts` says
  // which routes a page lists and this adds them to what the page answers. The
  // JSON gets `protocolEndpoints` and the HTML gets a section shaped like
  // GNAP's, which was the model — `mgmt-api/admin_api.ts` adds the same member
  // to the operation mirroring the page, so the two surfaces answer alike.
  //
  // **ONLY WHERE THE REQUEST IS FOR THAT PAGE.** `active` names the tab, and a
  // page drawn under another page's tab (the keytab under Principals, a PEP's
  // listener certificate under Remote PEPs) has a path of its own; and a
  // drill-down (`up`) is about one thing on the page, so it keeps the member in
  // its JSON and goes without the section, which would push the thing it is
  // about below forty rows of the family's addresses.
  //
  // **PLACED BEFORE THE PAGE'S FIRST HEADING**, which is where GNAP's is: after
  // the lead notes and warnings that say what the page is, before the first
  // section of what it holds. A page with no heading gets it at the foot.
  // ---------------------------------------------------------------------------
  endpointsSection(rows) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.endpointsSection(). rows=" + rows.length);
    const body = rows.length ? rows.map(function (row) {
      const how = row.methods.length ? row.methods.join(', ') :
                  (row.transport || '');
      return '<tr><th>' + self.esc(row.name) + '</th><td><code>' +
             self.esc(row.url) +
        '</code>' +
        (how ? ' <span class="sub">' + self.esc(how) + '</span>' : '') +
        (row.registered === false ?
         ' <span class="sub">— not registered in this process</span>' : '') +
        (row.listening === false ?
         ' <span class="sub">— not listening</span>' : '') + '</td></tr>';
    }).join('') : '<tr><td class="sub">None in this realm right now — ' +
                  'nothing of this kind is configured or listening ' +
                  'here.</td></tr>';
    log.debug("Leaving AdminConsole.endpointsSection().");
    return '<h2>Endpoints</h2><table class="kv">' + body + '</table>';
  }

  withProtocolEndpoints(req, json, active, html, up) {
    const { log, protocolEndpoints } = this.deps;
    log.debug("Entering AdminConsole.withProtocolEndpoints().");
    const rows = req.path === active ?
                 protocolEndpoints.forPage(req, active) : null;
    if (!rows || !json || typeof json !== 'object' || Array.isArray(json)) {
      log.debug("Leaving AdminConsole.withProtocolEndpoints(). Not a listed " +
                "page.");
      return { json: json, html: html };
    }
    const out = { json: Object.assign({}, json, { protocolEndpoints: rows }),
                  html: html };
    // A drill-down's `up` is `upTo()`'s object. The four XACML sub-pages pass
    // their section's path as a STRING on the page itself, which is not a
    // drill-down and must not lose the section.
    if (!up || typeof up !== 'object') {
      const section = this.endpointsSection(rows);
      const body = String(html || '');
      const at = body.indexOf('<h2');
      out.html = at < 0 ? body + section :
                 body.slice(0, at) + section + body.slice(at);
    }
    log.debug("Leaving AdminConsole.withProtocolEndpoints(). " + rows.length +
              " row(s).");
    return out;
  }

  // The two directions the table and `SECTIONS` can disagree in: a Protocols
  // page with neither a row nor an exemption, and a row or an exemption naming
  // a page that is not under Protocols (what a rename or a move leaves behind).
  // `tests/protocol_endpoints.js` fails on either; nothing else can see a page
  // appear.
  protocolEndpointDrift() {
    const { log, protocolEndpoints } = this.deps;
    log.debug("Entering AdminConsole.protocolEndpointDrift().");
    const underProtocols = NAV.filter(function (row) {
      return row.section === 'Protocols';
    }).map(function (row) { return row.path; });
    const listed = protocolEndpoints.pages()
                                    .concat(Object.keys(
                                        protocolEndpoints.exempt()));
    const drift = {
      unlisted: underProtocols.filter(function (path) {
        return listed.indexOf(path) < 0;
      }),
      stray: listed.filter(function (path) {
        return underProtocols.indexOf(path) < 0;
      })
    };
    log.debug("Leaving AdminConsole.protocolEndpointDrift(). " +
              drift.unlisted.length +
              " unlisted, " + drift.stray.length + " stray.");
    return drift;
  }

  // Both response shapes for a page, chosen by ?format=json. `no-store` on all
  // of them: they describe live state, and a cached metrics page is a wrong
  // one. `up`, when given, is what upTo() returned for the section this page
  // hangs under. Only a drill-down passes it; a section's own list page does
  // not, and the JSON answer ignores it either way — a way back up is a
  // property of a page a person is reading, and a caller of ?format=json has
  // the URL it asked for.
  respond(req, res, json, title, active, html, up?) {
    const { log, gateStateFor } = this.deps;
    log.debug("Entering AdminConsole.respond(). title=" + title);
    res.set('Cache-Control', 'no-store');
    const listed = this.withProtocolEndpoints(req, json, active, html, up);
    json = listed.json;
    html = listed.html;
    if (String(req.query.format || '') === 'json') {
      res.status(200)
         .type('application/json')
         .send(JSON.stringify(json, null, 2));
      log.debug("Leaving AdminConsole.respond(). Answered JSON.");
      return;
    }
    res.status(200).type('text/html').send(
      this.withCsrf(req,
                    this.page(title, active, html, up, gateStateFor(req),
                              req)));
    log.debug("Leaving AdminConsole.respond(). Answered HTML.");
  }

  // A POST answers the way it was asked: JSON in, JSON out; a form, and the
  // browser is sent back to the page it came from with a message. The redirect
  // is a 303 so that the reload after it is a GET — a 302 here leaves the
  // browser able to repeat the POST on refresh, which for "revoke everything"
  // is a surprise.
  respondToAction(req, res, target, result) {
    const { log, errorCodes } = this.deps;
    log.debug("Entering AdminConsole.respondToAction(). ok=" + result.ok);
    // A REFUSED ACTION IS A FAILURE, whether it is answered as a 400 or as a
    // 303 carrying `error=`. The code is the action's own where it named one,
    // then a more specific one this file already marked, then the generic
    // refusal.
    if (!result.ok) {
      errorCodes.mark(res, result.errorCode || errorCodes.codeOf(result) ||
                            errorCodes.codeOf(res) || 'STS-ADMIN-0012');
    }
    const type = String(req.headers['content-type'] || '');
    if (/json/i.test(type)) {
      // `errorCode` is taken OFF the reply: a code is recorded, never returned.
      // A result without one serialises byte for byte as it always did.
      let reply = result;
      if (result && Object.prototype.hasOwnProperty.call(result, 'errorCode')) {
        reply = Object.assign({}, result);
        delete reply.errorCode;
      }
      res.status(result.ok ? 200 : 400)
         .type('application/json')
         .set('Cache-Control', 'no-store')
         .send(JSON.stringify(reply, null, 2));
      log.debug("Leaving AdminConsole.respondToAction(). Answered JSON.");
      return;
    }
    const key = result.ok ? 'notice' : 'error';
    // `why` IS READ AS WELL AS `errors`, AND THAT IS NOT A CONVENIENCE.
    //
    // This console's own handlers refuse with `errors: [...]`; the XACML
    // family's three — `policyAction()`, `editorAction()` and `pepAction()` in
    // `xacml/xacml_admin.ts` — refuse with a single `why`, which is the shape
    // `xacml_store.js` and `xacml_editor.js` hand up to them. `/admin-api` was
    // already given that translation (`xacmlAction()` above puts `why` into
    // `errors`), and the CONSOLE was not — so every refusal on the three
    // /admin/xacml pages redirected back with `error=` and nothing in it. The
    // person got the page they had just posted from, unchanged, with no
    // explanation, which reads exactly like a control that does nothing.
    //
    // It matters most where the editor's whole design rests on it: an edit that
    // would leave a policy invalid is refused and the stored document is
    // untouched, and the sentence saying so — which names the type error the
    // author has to fix — was the part being dropped. Fixed HERE rather than at
    // the three handlers, so that a fourth handler written in that shape cannot
    // reintroduce it, and so the console and /admin-api cannot disagree about
    // what a refusal said. Caught by tests/vendored/sts_xacml_editor.js.
    const message = result.ok ? result.message
      : ((result.errors || []).join(' ') || String(result.why || ''));
    // `&` when the target already carries a query string, `?` when it does not.
    // The tokens page sends the reader back to the page and filter the button
    // was on, so this is no longer always a bare path — and a second `?` does
    // not start a second query string, it becomes part of the previous
    // parameter's value, which loses the message and corrupts the parameter it
    // landed on in one go. THE QUERY GOES BEFORE THE FRAGMENT, and a target
    // that has none is unaffected — which is every target here bar one.
    // `/admin/delegation`'s configured half posts back to `#allowed`, because
    // that section is four screens down the longest page in this console and a
    // reader who has just granted a permission should land on it. Appended the
    // naive way, the `?notice=` became part of the FRAGMENT: the browser
    // scrolled nowhere and the message the action came back with was never
    // shown, which reads exactly like the form having done nothing.
    const hash = target.indexOf('#');
    const stem = hash < 0 ? target : target.slice(0, hash);
    const anchor = hash < 0 ? '' : target.slice(hash);
    const stemJoiner = stem.indexOf('?') < 0 ? '?' : '&';
    res.set('Cache-Control', 'no-store')
       .redirect(303, stem + stemJoiner + key + '=' +
                      encodeURIComponent(String(message).slice(0, 500)) +
                      anchor);
    log.debug("Leaving AdminConsole.respondToAction(). Redirected to " +
              target + ".");
  }

  // ---------------------------------------------------------------------------
  // THE GATE. WHO GETS INTO THIS CONSOLE AT ALL.
  //
  // It is ONE `app.use('/admin', ...)` registered here, above every route in
  // this file, and that placement is the whole of how it works: express applies
  // middleware only to routes added AFTER it (rule 1), so a route added below
  // this line is guarded and a route added above it would not be. There are
  // none above it, and a new console page must go below — which it will, since
  // every route in this file is below.
  //
  // FOUR THINGS ABOUT IT ARE DELIBERATE.
  //
  // **It authenticates NOTHING itself.** `authn.js` owns the session and the
  // sign-in screen; this asks `consoleRpSession()` who is here and, when nobody
  // is, sends the browser into the OIDC code flow (`sendToConsoleSignIn()`,
  // through `common/oidc_rp.ts`) with the page they wanted as its return
  // address. (Until 2026-09-06 it read the sign-on session directly and sent
  // the browser to `beginAuthentication()`'s URL.) A login screen of this
  // console's own would be a second authentication service, and the one
  // consequence of sharing the first is the good one: sign in at `/authn/login`
  // with a security key and this console knows it, because the flow meets the
  // same sign-on session WS-Federation and the authorization endpoint read.
  //
  // **A BROWSER IS REDIRECTED AND A PROGRAM IS REFUSED, and telling them apart
  // is not a nicety.** Every page here answers `?format=json` and every form
  // accepts a JSON body, precisely so a test can drive this console without a
  // browser — and a 302 to an HTML login screen is not an answer such a caller
  // can read. It would arrive as a 200 full of markup where JSON was expected,
  // which is the shape of failure that costs an afternoon. So: `?format=json`,
  // a JSON content-type or an `Accept` that asks for JSON gets 401 or 403 with
  // a body saying which, and everything else gets the screen.
  //
  // **IT GUARDS `/admin` AND NOT `/admin-api`.** Express matches a `use` path
  // on segment boundaries, so `/admin-api` does not match `/admin` — that is
  // not an accident being relied on, it is the arrangement: the management API
  // has a credential of its own — an OAuth 2.0 access token since 2026-09-09,
  // which `adminApi.authRequired=false` turns off — and is therefore the way
  // back in for somebody who has locked themselves out of the console, which is
  // a state `admin.openWhenEmpty: false` makes reachable. `mgmt-api/CLAUDE.md`
  // argues both halves. It is also why this gate does not break this
  // repository's own `tests/vendored/admin_api.js`.
  //
  // **A REFUSAL IS A PAGE AND NOT A BARE STATUS.** 403 with an empty body is
  // the single least useful thing this could answer: the reader is signed in,
  // the console plainly exists, and nothing tells them that a directory group
  // is what they are missing. The refusal names the role, names the group, and
  // says which four doors can grant it.
  // ---------------------------------------------------------------------------


  // Does this caller want JSON rather than a page? See the header for why the
  // question matters more than it looks.
  //
  // `?format=json` is first because it is the one this console documents on
  // every page. The content-type is what a form-less POST carries. `Accept` is
  // last and is checked for JSON BEFORE html deliberately: a browser sends
  // `text/html,...,*/*` and would match a naive "does it mention json" test on
  // the wildcard alone, so the html half is what decides when both are present.
  wantsJson(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.wantsJson().");
    if (String((req.query || {}).format || '') === 'json') {
      log.debug("Leaving AdminConsole.wantsJson().");
      return true;
    }
    if (/json/i.test(String(req.headers['content-type'] || ''))) {
      log.debug("Leaving AdminConsole.wantsJson().");
      return true;
    }
    const accept = String(req.headers.accept || '');
    log.debug("Leaving AdminConsole.wantsJson().");
    return /json/i.test(accept) && !/text\/html/i.test(accept);
  }

  // A refusal, in whichever of the two shapes the caller can read. The HTML one
  // goes through page() like everything else, so it carries the nav and the
  // banner: a reader who is refused one page can still see the ones they are
  // allowed, which is the difference between a permission and a wall.
  // error-code: none — the helper's definition, not a call to it.
  refuse(req, res, status, code, title, message, detail) {
    const { log, gateStateFor } = this.deps;
    log.debug("Entering AdminConsole.refuse(). status=" + status + ", code=" +
              code);
    res.set('Cache-Control', 'no-store');
    if (this.wantsJson(req)) {
      res.status(status).type('application/json').send(JSON.stringify({
        error: code,
        error_description: message,
        // Named rather than left to be inferred from the status, because the
        // two refusals are genuinely different instructions — one says "sign
        // in", the other says "you are signed in and it is not enough" — and a
        // client that retried the second the way it retries the first would
        // loop.
        needed: detail.needed || '',
        group: detail.group || '',
        signIn: detail.signIn || '',
        roles: detail.roles || []
      }, null, 2));
      log.debug("Leaving AdminConsole.refuse(). Answered JSON.");
      return;
    }
    const inner = '<div class="err"><strong>' + this.esc(title) + '</strong> ' +
                  this.esc(message) + '</div>' +
      (detail.html || '');
    res.status(status).type('text/html')
       .send(this.page(title, '', inner, null, gateStateFor(req), req));
    log.debug("Leaving AdminConsole.refuse(). Answered HTML.");
  }

  // The default realm's CONSOLE as an ABSOLUTE URL. See the note at its one
  // caller for why absolute: a root-relative href in an HTML body is rewritten
  // by app.js to carry the realm being read, and this link must not be.
  //
  // **IT WAS `LOGIN_PATH` UNTIL 2026-09-06 AND THAT LINK COULD NOT WORK.**
  // `/authn/login` draws a form for a PENDING AUTHENTICATION RECORD and answers
  // `There is no sign-in waiting under that id` to a request naming none — so
  // the one link this console offers somebody whose session expired mid-form
  // went to an OAuth error page. The gate below is what mints a record, through
  // `sendToConsoleSignIn()`, so the way to the sign-in screen is the CONSOLE:
  // an unauthenticated GET of `/admin` in the default realm is answered with a
  // 302 to a screen that has a record behind it and comes back here afterwards.
  //
  // **The record is minted when the link is PRESSED**, which is the property
  // that decides this rather than calling `beginAuthentication()` here: a
  // pending record has a lifetime, and this page is drawn precisely for
  // somebody who has been sitting on a form for longer than one.
  // `portal/portal.ts` fixed the same mistake the same way on the same day and
  // its comment carries the rest.
  defaultRealmSignInUrl(req) {
    const { log, realms, baseUrlOf } = this.deps;
    log.debug("Entering AdminConsole.defaultRealmSignInUrl().");
    log.debug("Leaving AdminConsole.defaultRealmSignInUrl().");
    return realms.run(realms.DEFAULT_REALM, function () {
      return baseUrlOf(req) + '/admin';
    });
  }

  // ---------------------------------------------------------------------------
  // WHERE THE CONSOLE SENDS SOMEBODY TO SIGN IN.
  //
  // THE FIRST FOUR PARAGRAPHS ARE THE ARRANGEMENT BEFORE 2026-09-06, kept for
  // the two `app.js` behaviours they record; the paragraphs after the rule say
  // what happens now.
  //
  // The gate accepted the DEFAULT realm's session and nothing else (see
  // `consoleSession()`), so the sign-in it sent an anonymous reader to had to
  // mint one there. Two things stand in the way of just handing back
  // LOGIN_PATH, and both are `app.js` doing exactly what it was built to do:
  //
  //   * `res.location()` is wrapped in a realm to prepend that realm's prefix,
  //     so `/authn/login` becomes `/realm/acme/authn/login` on the way out;
  //   * `beginAuthentication()` stashes the pending transaction in a per-realm
  //     store, so a record created while `acme` is ambient cannot be redeemed
  //     at the default realm's screen — the reader would arrive at a sign-in
  //     that had never heard of the page they asked for.
  //
  // Running BOTH inside `realms.run(DEFAULT_REALM, …)` settles both at once:
  // the record lands in the default realm's store, and `realms.href()` — which
  // reads the prefix at call time — prepends nothing. The URL that comes out is
  // `/authn/login?authn=…`, which is what it says.
  //
  // `returnTo` is deliberately NOT run in the default realm. It is
  // `req.originalUrl`, which `app.js` leaves alone precisely so that it still
  // carries the realm — `/realm/acme/admin/users` — so signing in at the
  // default realm's screen returns the reader to the realm's page they asked
  // for. That is the whole shape of this feature in one line: sign in once, in
  // one realm, read every realm.
  // ---------------------------------------------------------------------------
  // **IT IS AN AUTHORIZATION CODE FLOW SINCE 2026-09-06 AND WAS A REDIRECT TO
  // THE SIGN-IN SCREEN BEFORE IT.** The console's own session still lives in
  // the default realm (`oidc_rp.js`'s `sessionRealm`), but since 2026-09-11 the
  // FLOW runs in the ambient realm (`flowRealm`), so a person signed in to
  // `/realm/acme/portal` is not asked again at `/realm/acme/admin`;
  // `oidc_rp.js`'s SURFACES header argues the split. What changed on 2026-09-06
  // is that this console no longer reaches into the authentication service for
  // a session. It sends the browser to `/oauth2/authorize` as
  // `sts-admin-console`, and the sign-in screen is reached — if it is reached
  // at all — by the AUTHORIZATION ENDPOINT deciding it needs one. Single
  // sign-on falls out of that for free: a person who already has a sign-on
  // session from any other family here is sent straight back with a code.
  //
  // **WHAT THE MOVE COST, said here rather than discovered later.** This
  // function used to hand `beginAuthentication()` four `details` that the
  // sign-in screen then showed somebody about to type a name into this console:
  // what they were signing in to, the page they had asked for, that membership
  // of one of two groups is what decides access, and that it was the DEFAULT
  // realm's `ou=groups` that decided it (the realm signed in through, since
  // 2026-09-14). An authorization request carries no such field — the screen is
  // drawn by the authorization endpoint about the CLIENT, so what a person now
  // sees is `Admin console` and the scopes it asked for, which is what every
  // other application in this registry gets and is the point of the move.
  //
  // The two sentences that were load-bearing are not lost: the 403 for somebody
  // holding no role names both groups and the realm, which is the page they
  // actually reach, and it is the page where that sentence is ACTIONABLE. The
  // two that were context are gone on purpose.
  sendToConsoleSignIn(req, res) {
    const { log, oidcRp, mode, errorCodes, realms } = this.deps;
    log.debug("Entering AdminConsole.sendToConsoleSignIn().");
    const started = oidcRp.beginSignIn(req, res, 'admin', {
      returnTo: String(req.originalUrl || '/admin'),
      fallback: '/admin'
    });
    if (!started.ok) {
      // THREE REASONS REACH HERE, AND THE SENTENCE A READER NEEDS DIFFERS. The
      // client entry is gone, or has no secret — or, since 2026-09-12, product
      // mode refused to start a flow at an address the entry does not carry,
      // because a callback is no longer LEARNT from a request's Host header
      // there. `oidc_rp.js` says which in `why`. What used to follow every one
      // of them — "/admin-api is not gated" — is true only in development,
      // where that API takes no token at all; saying it in product would be
      // pointing somebody locked out at a door that is also locked.
      const unregistered = started.reason === 'unregistered-address';
      const wayBack = unregistered
        ? ' Set global.publicBaseUrl to the address this console is reached ' +
          'at, or add that address\'s /admin/callback to the ' +
          'sts-admin-console application\'s oauthRedirectUri.'
        : (mode.gatesManagementApi()
            ? ' Until it is fixed, /admin-api can do everything this console ' +
              'can, with an access token carrying admin:write.'
            : ' Until it is fixed, /admin-api is not gated and can do ' +
              'everything this console can.');
      errorCodes.mark(res, unregistered ? 'STS-ADMIN-0002' : 'STS-ADMIN-0001');
      this.refuse(req, res, 503, 'temporarily_unavailable',
                  'This console cannot sign anybody in.',
                  started.why + wayBack,
                  { needed: unregistered ? 'address' : 'client', signIn: '',
                    signInRealm: realms.DEFAULT_ID });
    }
    log.debug("Leaving AdminConsole.sendToConsoleSignIn(). " +
              (started.ok ? "Sent to the authorization endpoint." :
               "Refused."));
  }

  // The message a redirect brought back, if any. Escaped where it is rendered;
  // capped where it is read, so a hand-written URL cannot make the page
  // arbitrarily long.
  messagesOf(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.messagesOf().");
    const notice = String(req.query.notice || '').slice(0, 500);
    const error = String(req.query.error || '').slice(0, 500);
    log.debug("Leaving AdminConsole.messagesOf().");
    return (notice ? '<div class="ok">' + this.esc(notice) + '</div>' : '') +
           (error ? '<div class="err">' + this.esc(error) + '</div>' : '');
  }

  tile(n, label) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.tile().");
    log.debug("Leaving AdminConsole.tile().");
    return '<div class="tile"><div class="n">' + this.esc(n) +
           '</div><div class="l">' + this.esc(label) + '</div></div>';
  }

  // ---------------------------------------------------------------------------
  // PROSE LONGER THAN A LINE IS COLLAPSED, AND THIS IS THE ONE PLACE THAT
  // DECIDES IT.
  //
  // Every page in this console explains itself at length, and that prose is the
  // point of a mock — the reasoning is the half a person cannot read off a
  // protocol trace. What it cost was the other half: on most pages here the
  // controls somebody actually came for sat several screens down a wall of
  // paragraphs, and the page read as documentation with a form hidden in it. So
  // a note of more than about a line is drawn as a `<details>` whose
  // `<summary>` is its own opening sentence: the point stays on the page, and
  // the argument for it is one click away rather than gone.
  //
  // FOUR THINGS ABOUT IT ARE DECISIONS RATHER THAN MECHANICS.
  //
  // * IT IS NATIVE `<details>`, AND NOTHING ELSE COULD BE. This console is
  //   served under `script-src 'none'` — see ../CLAUDE.md, where that rule and
  //   the pages that relax it are argued — so the debugger's collapse-all
  //   switch, which is a checkbox and a listener, has no equivalent here.
  //   `<details>` needs no script at all, so the whole of this change leaves
  //   that policy untouched and adds no exception to it. What it costs
  //   is the *expand everything* control, which is why the shape below keeps
  //   every summary a full sentence: a reader skimming for one paragraph has to
  //   be able to find it without opening all of them.
  // * THE SUMMARY IS DERIVED, NOT WRITTEN BESIDE THE PROSE. A hand-written
  //   label over a paragraph is a second copy of that paragraph's point, and
  //   the two drift the way every other pair of hand-kept lists in this
  //   repository has — the Overview page's list of console pages is the
  //   standing example, and it described seven pages of twenty-five before
  //   anybody noticed. So the label is the note's own first sentence, which in
  //   this codebase's house style is already the point said shortly. A caller
  //   may still pass one, for the few places where the opening sentence is a
  //   fact and the paragraph is about something else.
  // * THE TEST IS ON THE RENDERED TEXT, NOT ON THE CALLER'S JUDGEMENT. A caller
  //   deciding "this one is short enough" is a decision made once, at writing
  //   time, against a paragraph that then grows. `plainTextOf()` measures what
  //   a reader will actually see — tags stripped, entities resolved — so a note
  //   that grows past a line starts collapsing itself with no edit anywhere.
  // * A SHORT NOTE IS LEFT ALONE. Collapsing a line of text behind a control
  //   that is itself a line of text saves nothing and costs a click, and a page
  //   of nothing but disclosure triangles is the same wall of text with the
  //   words taken out.
  //
  // EVERY FUNCTION IN THIS BLOCK IS DELIBERATELY WITHOUT ENTERING/LEAVING LOGS,
  // which is the exception whenText() and its neighbours already take and for a
  // stronger version of the same reason: note() alone is called about three
  // hundred times on `/admin/config`, so the pair of lines the style rule asks
  // for would be six hundred of them for one page draw, and the request that
  // drew it would be somewhere in the middle. The two places something is worth
  // saying — a list item left open, a page in the nav nobody described — log at
  // the point the decision is made instead.
  // ---------------------------------------------------------------------------


  // The visible text of a fragment of markup — tags removed, whitespace
  // collapsed, AND ENTITIES LEFT EXACTLY AS THEY WERE.
  //
  // Leaving them is the whole of the fix for a bug this had twice: `&apos;` and
  // `&rarr;` appeared in summaries as themselves. Decoding an entity here and
  // escaping the result on the way into a <summary> turns `&apos;` into
  // `&amp;apos;`, and the escaping cannot be dropped without deciding whether
  // the text was escaped in the first place.
  //
  // So the rule is the other one, and it holds for every caller in this file:
  // WHAT COMES IN IS A VALID HTML FRAGMENT — prose written as markup, or a
  // value already through esc() — so what comes out is valid HTML text and goes
  // into a <summary> UNESCAPED. A caller that hands this an unescaped `<` loses
  // it to the tag stripper either way, which is the same thing escaping would
  // have done to it.
  plainTextOf(html) {
    return String(html == null ? '' : html)
      .replace(/<[^>]*>/g, '')
      .replace(/\s+/g, ' ')
      .trim();
  }

  // How long that text READS, which is not its length: `&mdash;` is one dash
  // and seven characters. Only the two measurements use this — what goes on the
  // page is always plainTextOf()'s own output.
  visibleLength(text) {
    return text.replace(/&(?:#\d+|#x[0-9a-fA-F]+|[a-zA-Z][a-zA-Z0-9]*);/g,
                        '-').length;
  }

  // Where the opening sentence ends, as an offset INTO THE MARKUP — or -1 if it
  // cannot be cut there.
  //
  // The cut has to be at element depth zero and nowhere else. A sentence that
  // ends inside a `<strong>` is the ordinary case in this file's prose
  // ("<strong> This is not a second door. It posts to…</strong>"), and cutting
  // there would hand the summary an unclosed tag and the body an unopened one —
  // which no browser reports and every browser renders differently. So depth is
  // tracked and a boundary inside anything is refused; the caller then falls
  // back to a truncation, which repeats the opening in the body and is merely
  // untidy rather than broken.
  //
  // A boundary is a full stop, question or exclamation mark followed by
  // whitespace. The whitespace is what keeps `oauth2.rfc9700` and `RFC 7644`
  // out of it: an abbreviation's stop is followed by a letter, not a space. The
  // thirty-character minimum is what keeps a leading "e.g. " — or any other
  // short opener — from becoming the whole summary.
  sentenceEnd(html) {
    let depth = 0;
    for (let i = 0; i < html.length; i++) {
      const c = html[i];
      if (c === '<') {
        const close = html.indexOf('>', i);
        if (close < 0) {
          return -1;
        }
        const tag = html.slice(i, close + 1);
        if (/^<\//.test(tag)) {
          depth -= 1;
        } else if (!/\/>$/.test(tag) &&
                   !/^<(br|hr|img|input|meta|link)\b/i.test(tag)) {
          depth += 1;
        }
        i = close;
        continue;
      }
      // A COLON IS NOT A SENTENCE END HERE, though it looks like one. It almost
      // always INTRODUCES the rest — "The same two halves for the assertions:
      // ..." — so cutting there produced summaries that were a fragment with
      // nothing after them, which is worse than no summary at all. A colon
      // still ends a headline (see foldOf), because a title ending in one is a
      // title.
      if (depth === 0 && i >= 30 && /[.!?]/.test(c) &&
          /^\s/.test(html.slice(i + 1, i + 2))) {
        return i + 1;
      }
    }
    return -1;
  }

  // The opening sentence, cut short at a word boundary if it is longer than a
  // summary line. The ellipsis is not decoration: it is what tells a reader
  // that the words in the summary are the words the body opens with, so that
  // the repetition below reads as *read more* rather than as the same sentence
  // printed twice.
  teaserOf(text, max?) {
    const limit = max || SUMMARY_CHARS;
    if (this.visibleLength(text) <= limit) {
      return text;
    }
    let cut = text.slice(0, limit);
    const space = cut.lastIndexOf(' ');
    if (space > 40) {
      cut = cut.slice(0, space);
    }
    // ---------------------------------------------------------------------
    // A CUT CAN LAND INSIDE AN ENTITY — `&mda` — which a browser renders
    // literally, so the half is dropped rather than shown.
    //
    // **AND IT CAN LAND IMMEDIATELY AFTER A WHOLE ONE, WHICH IS THE CASE THIS
    // MISSED UNTIL 2026-09-11.** The two replacements ran in the order
    // entity-then-punctuation, and `;` is in the punctuation class — so a cut
    // ending `&mdash;` survived the first replacement intact (it is complete),
    // lost its semicolon to the second, and reached the page as the literal
    // text `&mdash`. Nothing on this console had ever been cut in that exact
    // place before `/admin/spiffe`'s authority note, which is why a bug in a
    // function every folded note goes through went years without being seen.
    //
    // The `;?` is the whole fix: a trailing entity is taken WHOLE where there
    // is one, and the punctuation strip then only ever sees ordinary text.
    // ---------------------------------------------------------------------
    return cut.replace(/&[a-zA-Z0-9#]*;?$/, '').replace(/[\s,.;:—-]+$/, '') +
           '&hellip;';
  }

  // The entities a title attribute cannot show, resolved. Only tip() needs it —
  // see the comment in there. `&amp;` is resolved LAST so that `&amp;lt;` comes
  // out as the four characters somebody wrote rather than as a `<`.
  unescapeText(text) {
    return String(text)
      .replace(/&mdash;|&ndash;/g, '\u2014')
      .replace(/&middot;/g, '\u00b7')
      .replace(/&rarr;/g, '\u2192')
      .replace(/&hellip;/g, '\u2026')
      .replace(/&nbsp;/g, ' ')
      .replace(/&lt;/g, '<')
      .replace(/&gt;/g, '>')
      .replace(/&quot;|&ldquo;|&rdquo;/g, '"')
      .replace(/&#39;|&apos;|&rsquo;|&lsquo;/g, "'")
      .replace(/&amp;/g, '&');
  }

  // A TOOLTIP, AS AN ATTRIBUTE READY TO GO INTO A TAG — including its leading
  // space, so a caller can drop it into markup without deciding whether one is
  // needed.
  //
  // This is the other half of the folds and it answers the other half of the
  // problem: prose long enough to be an argument folds, and prose short enough
  // to be a caption belongs ON the control it captions rather than under it.
  // Every label in this console that grows one gets the dotted underline and
  // the help cursor from a `label[title]` rule in page(), so the affordance
  // arrives with the tooltip and cannot be forgotten separately.
  //
  // NOTHING IS EVER SAID ONLY IN A TOOLTIP, and that is the rule to hold on to
  // rather than the mechanism: a title attribute is unreachable from a
  // keyboard, invisible on a touch screen and unread by most screen readers.
  // What goes in one is a shorter saying of something the page still carries —
  // which is why shortened() above, this console's first tooltip, puts the FULL
  // value in the title and the truncation on the page rather than the other way
  // round. `max` OVERRIDES THE 190-CHARACTER TEASER, AND A CALLER THAT PASSES
  // ONE IS SAYING THIS TOOLTIP IS THE ONLY COPY (2026-09-05).
  //
  // The default truncates, which was right while everything a tooltip carried
  // was also in a fold under the control — a teaser is a preview of something
  // the reader can go and read. **A field whose fold has been removed has no
  // such thing**, so truncating there would not be hiding the rest of the
  // sentence, it would be DELETING it: the median setting description is 384
  // characters and the default teaser is 190, so more than half of every one
  // would have left the product rather than the screen.
  //
  // So a field that is tooltip-only passes `Infinity` and the title carries the
  // whole text. Browsers wrap a long title perfectly well; the reason the
  // default is short is that a teaser competes with the fold under it, and
  // where there is no fold there is nothing to compete with.
  tip(text, max?) {
    const plain = this.plainTextOf(text);
    if (!plain) {
      return '';
    }
    // Escaped, unlike a summary: a title attribute is TEXT rather than markup —
    // a browser shows `&mdash;` in one literally — and this is also the one
    // helper here that is handed raw strings out of config.js as often as it is
    // handed markup. esc() on an already-escaped fragment would double it, so
    // the entities are resolved for this one path.
    return ' title="' +
           this.esc(this.unescapeText(this.teaserOf(plain, max || TIP_CHARS))) +
           '"';
  }

  // The summary and the body of one collapsed block. Where the opening sentence
  // could be cut out of the markup cleanly it becomes the summary and the body
  // is what is left, so nothing is said twice; where it could not, the summary
  // is a truncation and the body is the whole note.
  foldOf(html, label) {
    if (label) {
      // A LABEL IS THE ONE THING THAT IS ESCAPED. Everything else here is text
      // taken out of markup the caller already built; a label is a plain string
      // a caller passed — a setting's own name, most often — and has been
      // through nothing.
      return { summary: this.esc(label), body: html };
    }
    // THE COMMONEST SHAPE IN THIS FILE IS A BOLDED HEADLINE AND THEN THE
    // ARGUMENT FOR IT — `<strong>This is not a second door.</strong> The form
    // below posts to…` — and that headline is a better summary than any
    // sentence-splitting could find, because somebody wrote it to be one. It is
    // taken only when it ends in sentence punctuation: a bolded PHRASE opening
    // a sentence that runs on ("<strong>THE KEY</strong>, exactly as…") is part
    // of the first sentence rather than a title for the paragraph. A headline
    // under about two dozen characters is a PREFIX rather than a title —
    // `<strong>Restart to apply:</strong>` is the one that showed it, and
    // taking it left a fold whose summary was two words and whose body held the
    // reason somebody opened it for.
    const headline = /^\s*<(strong|b|em)>([\s\S]*?)<\/\1>/.exec(html);
    if (headline) {
      const title = this.plainTextOf(headline[2]);
      if (title.length >= 24 && title.length <= SUMMARY_CHARS &&
          /[.:!?]$/.test(title)) {
        return { summary: title,
                 body: html.slice(headline[0].length).replace(/^\s+/, '') };
      }
    }
    const cut = this.sentenceEnd(html);
    const head = cut > 0 ? this.plainTextOf(html.slice(0, cut)) : '';
    if (head && head.length <= SUMMARY_CHARS) {
      return { summary: head, body: html.slice(cut).replace(/^\s+/, '') };
    }
    return { summary: this.teaserOf(this.plainTextOf(html)), body: html };
  }

  // A paragraph of explanation. Short ones are the paragraph they always were;
  // long ones fold. `label` overrides the derived summary and forces the fold,
  // because a caller that bothered to name a block wanted the block.
  note(html, label?) {
    // Coerced once, here: a caller may hand this a number of rows or a
    // fragment built by a .map(), and everything below slices and measures.
    html = String(html == null ? '' : html);
    const text = this.plainTextOf(html);
    if (!label && this.visibleLength(text) <= ONE_LINE_CHARS) {
      return '<p class="note">' + html + '</p>';
    }
    const fold = this.foldOf(html, label);
    return '<details class="note fold"><summary>' + fold.summary +
           '</summary><div class="foldbody">' + fold.body + '</div></details>';
  }

  // The same, for the amber box. A warning folds like anything else and for the
  // same reason — most of them here are a headline and three sentences of why —
  // but the box KEEPS ITS COLOUR CLOSED, so a page with a caveat on it still
  // looks like a page with a caveat on it. Folding a warning into something
  // that looks like body text would be the one case where this change hid a
  // fact rather than tidying it.
  warn(html, label?) {
    // Coerced once, here: a caller may hand this a number of rows or a
    // fragment built by a .map(), and everything below slices and measures.
    html = String(html == null ? '' : html);
    const text = this.plainTextOf(html);
    if (!label && this.visibleLength(text) <= ONE_LINE_CHARS) {
      return '<div class="warn">' + html + '</div>';
    }
    const fold = this.foldOf(html, label);
    return '<details class="warn fold"><summary>' + fold.summary +
           '</summary><div class="foldbody">' + fold.body + '</div></details>';
  }

  // A TABLE TOO WIDE FOR THE CARD, IN A BOX THAT SCROLLS SIDEWAYS. The caller
  // hands over the whole `<table>…</table>` and gets it back inside the
  // scroller `.wide` describes.
  //
  // `tabindex="0"` is not decoration: a scroll container that only a pointer
  // can move leaves the columns past its right-hand edge unreachable from a
  // keyboard, and this console has no script to give them back. Making the box
  // focusable is what lets the arrow keys move it, and it is the whole reason
  // this is a helper rather than a `<div class="wide">` written at each call
  // site — the attribute is the part somebody copying the markup would drop.
  //
  // `aria-label` names WHICH table, because a page with two of these otherwise
  // announces two identical regions and the label is the only thing telling a
  // reader arriving in one of them which it is.
  wideTable(label, html) {
    return '<div class="wide" tabindex="0" role="region" aria-label="' +
      this.esc(label) + '">' + html + '</div>';
  }

  // One item of a prose list — the *what it deliberately does not do* lists,
  // and nothing else. Every one of those bullets opens with a bolded headline
  // and then argues it for a paragraph, which is exactly the shape a fold
  // suits: the list stays a list of claims, and the argument for each is under
  // it.
  bullet(html, label?) {
    const { log } = this.deps;
    // Coerced once, here: a caller may hand this a number of rows or a
    // fragment built by a .map(), and everything below slices and measures.
    html = String(html == null ? '' : html);
    const text = this.plainTextOf(html);
    if (!label && this.visibleLength(text) <= ONE_LINE_CHARS) {
      return '<li>' + html + '</li>';
    }

    // AN ITEM THAT OPENS WITH A LINK IS NEVER FOLDED, and this is the rule that
    // stops this helper quietly breaking two lists. The Overview page's list of
    // every console page and the sidebar's are the same list, and each row of
    // it IS a link — folding one puts the only control in the row behind a
    // summary made of text, so the reader has to open the thing to find out it
    // was the link they were looking for. A link inside a <summary> is also a
    // control inside a control, which browsers resolve differently from each
    // other.
    if (/^\s*<a\b/i.test(html)) {
      log.debug("bullet(). Left open: the item opens with a link.");
      return '<li>' + html + '</li>';
    }

    // AN ITEM THAT OPENS WITH A CODE PATH KEEPS IT IN THE SUMMARY. The
    // machine-readable lists here are a URL and then a paragraph about it, and
    // the URL is what somebody came for — but <code> is not interactive, so it
    // can sit in the summary beside the teaser rather than forcing the whole
    // row open. This is why fold.summary is escaped everywhere else and this
    // one place composes markup: the prefix is the caller's own element, and
    // only the text after it is escaped.
    const lead = /^\s*(<code\b[^>]*>[\s\S]*?<\/code>)/i.exec(html);
    if (!label && lead) {
      const rest = html.slice(lead[0].length).replace(/^[\s,—-]+/, '');
      return '<li class="foldli"><details class="fold"><summary>' + lead[1] +
             ' ' +
             this.teaserOf(this.plainTextOf(rest)) + '</summary><div ' +
               'class="foldbody">' +
             rest + '</div></details></li>';
    }

    const fold = this.foldOf(html, label);
    return '<li class="foldli"><details class="fold"><summary>' + fold.summary +
           '</summary><div class="foldbody">' + fold.body +
           '</div></details></li>';
  }

  // The three formatters below are deliberately without entering/leaving logs:
  // they are called once per table cell and would drown everything else in the
  // log.
  whenText(ms) {
    if (!ms) return '—';
    return new Date(ms).toISOString().replace('T', ' ').replace(/\.\d+Z$/, 'Z');
  }

  durationText(ms) {
    const s = Math.floor((ms || 0) / 1000);
    const days = Math.floor(s / 86400);
    const hours = Math.floor((s % 86400) / 3600);
    const minutes = Math.floor((s % 3600) / 60);
    const parts = [];
    if (days) parts.push(days + 'd');
    if (days || hours) parts.push(hours + 'h');
    parts.push(minutes + 'm');
    parts.push((s % 60) + 's');
    return parts.join(' ');
  }

  stateClass(state) {
    if (state === 'valid') return 'state-valid';
    if (state === 'expired' ||
        state === 'not yet valid') return 'state-expired';
    if (state === 'revoked') return 'state-revoked';
    return 'state-none';
  }

  // A long opaque value, shortened for the table but recoverable: the full
  // string is the title attribute, so it can be hovered and read. Truncating
  // with no way back would make the jti column decorative, and the jti is the
  // thing every button on the tokens page acts on.
  shortened(value, keep) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.shortened().");
    const text = String(value || '');
    if (text.length <= (keep || 18)) {
      log.debug("Leaving AdminConsole.shortened().");
      return '<code title="' + this.esc(text) + '">' + this.esc(text || '—') +
             '</code>';
    }
    log.debug("Leaving AdminConsole.shortened().");
    return '<code title="' + this.esc(text) + '">' +
           this.esc(text.slice(0, keep || 18)) +
           '&hellip;</code>';
  }

  clipped(value, keep) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.clipped().");
    const text = String(value == null ? '' : value);
    const limit = keep || CLIP_CHARS;
    if (!text) {
      log.debug("Leaving AdminConsole.clipped().");
      return '<code>&mdash;</code>';
    }
    if (text.length <= limit) {
      log.debug("Leaving AdminConsole.clipped().");
      return '<code>' + this.esc(text) + '</code>';
    }
    log.debug("Leaving AdminConsole.clipped().");
    return '<span class="trunc" tabindex="0" title="' + this.esc(text) + '">' +
      '<code>' + this.esc(text.slice(0, limit)) + '&hellip;</code>' +
      '<span class="full"><code>' + this.esc(text) + '</code>' +
      '<span class="hint">' + text.length + ' characters &mdash; click the ' +
      'value to select it all, then copy</span></span></span>';
  }

  // One attribute's values, clipped, one per line. Written once because four
  // directory pages draw exactly this cell and a fifth written by hand is the
  // one that goes back to printing the raw value.
  clippedValues(values, keep?) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.clippedValues().");
    const list = Array.isArray(values) ? values : [values];
    if (!list.length) {
      log.debug("Leaving AdminConsole.clippedValues().");
      return '<code>&mdash;</code>';
    }
    log.debug("Leaving AdminConsole.clippedValues().");
    // ONE VALUE PER LINE, INSIDE AN INLINE BLOCK, and the wrapper is the whole
    // point of it. Joined with a bare `<br>` the second value of a multi-valued
    // attribute starts at the cell's left margin — under the attribute NAME
    // rather than under the first value — so `member` with three DNs on it read
    // as one attribute followed by two nameless ones. `display:inline-block`
    // makes the values a column of their own that begins where the first one
    // does. (They were joined with " | " on one line before 2026-09-01, which
    // has the opposite failure: five values of forty characters is a line
    // nothing can align.)
    return '<span class="vals">' + list.map(function (one) {
      return self.clipped(one, keep);
    }).join('<br>') + '</span>';
  }

  // ---------------------------------------------------------------------------
  // One table, three families.
  //
  // The tokens page lists JWTs, SAML assertions and Kerberos tickets together
  // and newest first, because that is the order they happened in: a
  // WS-Federation sign-in that produced an ID Token and a SAML 1.1 assertion is
  // one event, and a page that showed the two halves of it in two places would
  // be a page somebody has to correlate by timestamp by hand.
  //
  // What that costs is that most columns mean something slightly different
  // depending on which family the row belongs to, and the way a table like that
  // goes wrong is a column that quietly means two things. So the mapping is
  // written down twice over: once here as ONE FUNCTION PER COLUMN answering for
  // all three families — which is what makes a header like "Client, audience or
  // service" checkable against the three answers underneath it — and once on
  // the page itself as a legend, for the reader, who cannot see this comment.
  //
  // Which families exist and what is in them is decided in admin_stats.js.
  // Nothing here chooses what the list contains; these functions only say how a
  // row is drawn.
  // ---------------------------------------------------------------------------

  // A JWT has two names for one person — the `username` that was typed at the
  // login screen and the `sub` derived from it — and seeing both is how you
  // tell those two apart. A SAML NameID and a Kerberos client principal are ONE
  // name each, so they fill the Subject column and leave this one empty rather
  // than being printed twice to avoid an empty cell.
  userCell(record) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.userCell().");
    if (record.family === 'token') {
      log.debug("Leaving AdminConsole.userCell().");
      return this.esc(record.username || '—');
    }
    log.debug("Leaving AdminConsole.userCell().");
    return '<span class="state-none" title="' +
      this.esc('A SAML assertion names a Subject and a Kerberos ticket names ' +
               'a client principal. Neither carries a second, human-readable ' +
               'name beside it the way a JWT carries username beside sub, so ' +
               'the one name it has is in the Subject column.') +
      '">—</span>';
  }

  subjectCell(record) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.subjectCell().");
    if (record.family === 'token') {
      log.debug("Leaving AdminConsole.subjectCell().");
      return this.shortened(record.sub, 30);
    }
    log.debug("Leaving AdminConsole.subjectCell().");
    return this.shortened(record.subject, 30);
  }

  // Who it was issued FOR: the party meant to accept it.
  partyCell(record) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.partyCell().");
    if (record.family === 'assertion') {
      if (record.audience) {
        log.debug("Leaving AdminConsole.partyCell().");
        return this.shortened(record.audience, 30);
      }
      log.debug("Leaving AdminConsole.partyCell().");
      return '<span class="state-none" title="' +
        this.esc('This assertion carries no AudienceRestriction — WS-Trust ' +
                 'was asked to Issue with no AppliesTo. Any relying party ' +
                 'may accept it, which is the thing an audience restriction ' +
                 'exists to prevent, so it is named here rather than shown ' +
                 'as a dash.') +
        '">unrestricted</span>';
    }
    if (record.family === 'ticket') {
      log.debug("Leaving AdminConsole.partyCell().");
      // The realm recorded with a ticket is the realm that ANSWERED, which
      // under a cross-realm referral is not the service's own realm. So it is
      // stated as the issuer in the tooltip rather than appended to the service
      // name as though it were part of the principal — which is what it would
      // look like, since a Kerberos principal is written service/host@REALM.
      return '<code title="' + this.esc(String(record.service || '') +
        (record.realm ? ' — issued by the ' + record.realm + ' KDC' : '')) +
        '">' +
        this.esc(record.service || '—') + '</code>';
    }
    log.debug("Leaving AdminConsole.partyCell().");
    return this.esc(record.client_id || '—');
  }

  // The one extra fact each family has that no other column has room for. It is
  // headed Detail rather than Scope, because the three are not answers to the
  // same question — a scope says what an access token authorises, an enc-type
  // says which cipher sealed a ticket — and a header naming one of them would
  // make the other two rows look like answers to it.
  detailCell(record) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.detailCell().");
    if (record.family === 'assertion') {
      if (record.signed === false) {
        log.debug("Leaving AdminConsole.detailCell().");
        return '<span class="state-revoked" title="' +
          this.esc('Signing threw and the assertion went out unsigned rather ' +
                   'than not at all, so that a relying party can reject it ' +
                   'for the right reason. The log line says what failed.') +
          '">unsigned</span>';
      }
      log.debug("Leaving AdminConsole.detailCell().");
      return '<span title="' +
        this.esc('An enveloped XML signature over the assertion, its ' +
                 'reference naming the ID (SAML 2.0) or the AssertionID ' +
                 '(SAML 1.1).') + '">signed</span>';
    }
    if (record.family === 'ticket') {
      log.debug("Leaving AdminConsole.detailCell().");
      return '<code title="' + this.esc('The enc-type the ticket and its ' +
                                        'session key were sealed with.') +
        '">' + this.esc(record.etype || '—') + '</code>';
    }
    log.debug("Leaving AdminConsole.detailCell().");
    return this.esc(record.scope || '—');
  }

  // How the holder gets to use it, which is the question the DPoP column was
  // already asking and which the other two families have their own answers to.
  presentedCell(record) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.presentedCell().");
    if (record.family === 'assertion') {
      log.debug("Leaving AdminConsole.presentedCell().");
      return '<span title="' +
        this.esc('Both builders write a bearer SubjectConfirmation: whoever ' +
                 'holds the assertion may present it. There is no ' +
                 'holder-of-key confirmation here, so there is nothing for ' +
                 'this column to distinguish between.') + '">bearer</span>';
    }
    if (record.family === 'ticket') {
      if (record.kind === 'Kerberos TGT') {
        log.debug("Leaving AdminConsole.presentedCell().");
        return '<span title="' +
          this.esc('A TGT goes back to the KDC in a TGS-REQ to get a service ' +
                   'ticket. It is never presented to a service, which is why ' +
                   'it is the Kerberos session rather than one use of ' +
                   'one.') + '">TGS-REQ</span>';
      }
      log.debug("Leaving AdminConsole.presentedCell().");
      return '<span title="' +
        this.esc('A service ticket is presented to the service it names, in ' +
                 'an AP-REQ — over raw Kerberos, or wrapped in SPNEGO over ' +
                 'HTTP.') + '">AP-REQ</span>';
    }
    if (record.jkt) {
      log.debug("Leaving AdminConsole.presentedCell().");
      return '<span title="' +
        this.esc('Bound to a key: cnf.jkt is in the token and a DPoP proof ' +
                 'over that key has to accompany it.') + '">DPoP</span>';
    }
    log.debug("Leaving AdminConsole.presentedCell().");
    return 'Bearer';
  }

  // The handle the row can be quoted by — and for one family there is none,
  // which is worth saying rather than leaving as a bare dash beside two columns
  // full of them. THE IDENTIFIER, AND THE WAY TO WHERE THE CREDENTIAL CAME
  // FROM.
  //
  // The cell is a link since 2026-08-26 and the id is the only thing it could
  // be keyed on — /admin/tokens/credential walks the delegation register by
  // exactly this string, which is what both registers hold about the same
  // object. A row with NO identifier therefore cannot have a link, and that is
  // the same sentence the cell already had to make: a Kerberos ticket has
  // nothing to quote, so it has nothing to look up, and the tooltip says why
  // rather than offering a link that could only ever answer "nothing is known".
  //
  // `listView` is the tokens page as the reader left it, carried into the query
  // so the drill-down's trail comes back to the same filter and the same page.
  identifierCell(record, listView) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.identifierCell().");
    if (record.family === 'ticket') {
      log.debug("Leaving AdminConsole.identifierCell().");
      return '<span class="state-none" title="' +
        this.esc('A Kerberos ticket carries no identifier anybody can quote: ' +
                 'no jti, no ID. It is named by its client, its service and ' +
                 'when it was issued — the columns to the left — and the KDC ' +
                 'keeps no handle on it either, because the KDC is stateless ' +
                 'and the ticket is the state. With no identifier there is ' +
                 'nothing to look a lineage up BY either, which is why this ' +
                 'row has no link where the others do.') + '">—</span>';
    }
    if (!record.identifier) {
      log.debug("Leaving AdminConsole.identifierCell().");
      return '<span class="state-none" title="' +
        this.esc('This one carries no identifier — the signed UserInfo ' +
                 'response has no jti, and the WS-Trust JWT is signed ' +
                 'directly rather than through signJwt(). Nothing can be ' +
                 'looked up by a name that does not exist.') + '">—</span>';
    }
    log.debug("Leaving AdminConsole.identifierCell().");
    return '<a href="' +
      this.esc('/admin/tokens/credential' +
               queryWith(listView || {}, { id: record.identifier })) +
      '" title="' +
      this.esc('Where this credential came from: who it was issued to, in ' +
               'whose name, to reach what — and, if it came out of a token ' +
               'exchange, the credential handed in to get it and every ' +
               'generation behind that, back to the issuance the whole line ' +
               'rests on.') + '">' +
      this.shortened(record.identifier, 12) + '</a>';
  }

  // The button, or why there is not one. Two different reasons, and they are
  // not interchangeable: a signed UserInfo response has no jti to act on, and a
  // SAML assertion has an identifier and still cannot be revoked because
  // nothing out there would ask this service about it.
  //
  // **IT DRAWS A BUTTON FOR AN ASSERTION AND A TICKET SINCE 2026-09-05, AND THE
  // PARAGRAPH ABOVE USED TO SAY THAT WAS IMPOSSIBLE.** What it said was true
  // and is still true — nothing consults this service when one of those is
  // presented, so no mark here stops one working — and it was an answer to a
  // question this column was not asking. **What this service KNOWS and what a
  // relying party will HONOUR are two different claims**, and the old cell let
  // the second erase the first: an identity provider that has signed somebody
  // out has a position on every credential it issued them, and being unable to
  // enforce it is not a reason to be unable to state it.
  //
  // So `revocable` says whether there is a button and `revocationReach` says
  // what pressing it changes, and **the second is what the tooltip is built
  // from** — a `record-only` button that read like a `protocol` one would be
  // exactly the lie the old cell was avoiding.
  actionCell(record, backRow) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.actionCell().");
    if (!record.revocable) {
      log.debug("Leaving AdminConsole.actionCell().");
      return '<span class="state-none" title="' +
        this.esc('This one carries no jti to act on — a signed UserInfo ' +
                 'response has none, and the WS-Trust JWT is signed directly ' +
                 'rather than through signJwt(). There is nothing to name in ' +
                 'a revocation.') + '">—</span>';
    }
    const recordOnly = record.revocationReach === 'record-only';
    const target = recordOnly
      ? '<input type="hidden" name="artifact" value="' +
        this.esc(record.key || '') +
        '">'
      : '<input type="hidden" name="target" value="' + this.esc(record.jti) +
        '">';
    const verb = record.revoked ? 'restore' : 'revoke';
    const action = recordOnly ? verb + '-artifact' : verb;
    const title = recordOnly
      ? (record.revoked
          ? 'Take this service\'s disavowal back. NON-SPEC, like every ' +
            'restore here.'
          : 'Mark it revoked IN THIS SERVICE\'S OWN RECORD, which is the ' +
            'whole of what this can do and is worth doing. THE HOLDER WILL ' +
            'NOT BE TOLD BY THIS BUTTON: a relying party validates a SAML ' +
            'assertion\'s signature and its Conditions and asks nobody, a ' +
            'Kerberos service decrypts a ticket with a key it already has, ' +
            'and an X509-SVID chains to a bundle — so this credential goes ' +
            'on working out there until it expires. What the mark buys is ' +
            'that a global logout can say what it disowned, that CAEP can ' +
            'transmit it to a receiver that subscribed, and that SAML Single ' +
            'Logout can carry it for an assertion that came from a browser ' +
            'profile. A WS-Trust assertion has neither channel, and the mark ' +
            'is all there is.')
      : (record.revoked
          ? 'Un-revoke it. NON-SPEC — no real authorization server can undo ' +
            'a revocation.'
          : 'Revoke it. Introspection immediately reports it inactive, ' +
            'UserInfo refuses it with invalid_token, and the refresh grant ' +
            'fails with invalid_grant.');
    log.debug("Leaving AdminConsole.actionCell().");
    return '<form method="post" action="/admin/tokens" class="inline">' +
      '<input type="hidden" name="action" value="' + this.esc(action) + '">' +
      target +
      '<input type="hidden" name="back" value="' + this.esc(backRow) + '">' +
      '<button class="' + (record.revoked ? 'secondary' : 'danger') +
      '" title="' + this.esc(title) + '">' +
      (record.revoked ? 'Restore' : 'Revoke') +
      (recordOnly ? ' (record only)' : '') + '</button></form>';
  }

  issuedRow(record, backRow, listView) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.issuedRow().");
    log.debug("Leaving AdminConsole.issuedRow().");
    return '<tr><td>' + this.esc(record.kind) + '</td>' +
      '<td class="' + this.stateClass(record.state) + '">' +
      this.esc(record.state) +
      '</td><td>' + this.userCell(record) + '</td><td>' +
      this.subjectCell(record) +
      '</td><td>' + this.partyCell(record) + '</td><td>' +
      this.detailCell(record) +
      '</td><td>' + this.presentedCell(record) + '</td><td>' +
      this.esc(this.whenText(record.issuedAt)) + '</td><td>' +
      this.esc(record.expiresAtMs ? this.whenText(record.expiresAtMs) : '—') +
      '</td><td>' +
      this.identifierCell(record, listView) + '</td><td>' +
      this.actionCell(record, backRow) + '</td></tr>';
  }

  // ---------------------------------------------------------------------------
  // A SET — WHAT CAME BACK IN ONE REPLY — WHICH IS WHAT THE TOKENS TABLE LISTS
  // SINCE 2026-09-05.
  //
  // **The row used to be one credential and is now one issuance.** OAuth 2.0
  // and OIDC are the only families this service speaks that hand back several
  // credentials at once: a code redemption returns an access token, an ID Token
  // and a refresh token, and `response_type=id_token token` returns two in one
  // fragment. Three rows for one reply left the reader to reassemble by
  // comparing timestamps the one thing the protocol had handed over whole — and
  // worse, to GUESS, because two people redeeming two codes at the same client
  // in the same millisecond produce six rows a timestamp cannot separate.
  //
  // Every other family issues one credential per act, so a SAML assertion, a
  // Kerberos ticket and an SVID are each a set of one and are drawn exactly as
  // they were. `stats.issuedSets()` decides which is which — from a set id the
  // ISSUER stated, never from a heuristic over these rows — and this file
  // renders what it is handed, which is the same division tokensView() has
  // always been on.
  //
  // THE SET'S OWN CELLS COME FROM ITS FIRST MEMBER and that is right for every
  // column but two, both of which say so on the page: the scope of a set is the
  // ACCESS TOKEN's, which the refresh token beside it deliberately does not
  // share (see tokenSet() — the refresh token keeps the whole authorized
  // scope), and the audience of a set is not one value at all, which is why the
  // party column reads `client_id`. Both disagreements are visible one click
  // away on the set page, where each member is its own row again.
  // ---------------------------------------------------------------------------

  // WHAT THE SET CONTAINS, which is the column that used to be Kind. A set of
  // one prints exactly what that column printed, so three families out of four
  // are untouched; a group prints its kinds in the order they were issued,
  // which for a code redemption is access token, refresh token, ID Token.
  contentsCell(set) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.contentsCell().");
    if (!set.grouped) {
      log.debug("Leaving AdminConsole.contentsCell().");
      return this.esc(set.kinds[0] || '—');
    }
    log.debug("Leaving AdminConsole.contentsCell().");
    return '<strong title="' +
      this.esc(set.size + ' credentials came back in one reply' +
               (set.grant ? ', from the ' + set.grant + ' grant' : '') +
               '. Every one of them is on the set page, with its own ' +
               'identifier, its own expiry and its own button.') + '">' +
      this.esc(set.kinds.join(' + ')) + '</strong>' +
      ' <span class="state-none">(' + set.size + ')</span>';
  }

  // THE STATE OF A SET, WHICH IS OFTEN NOT ONE STATE. An access token expires
  // in fifteen minutes and the refresh token beside it in a day, so within an
  // hour most sets on this page are neither valid nor expired — and reporting
  // either would be this column choosing which member matters. `mixed` says so
  // and the tooltip counts them, which is the same refusal-to-average the
  // sessions page's expiry column makes.
  setStateCell(set) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.setStateCell().");
    if (set.state !== 'mixed') {
      log.debug("Leaving AdminConsole.setStateCell().");
      return '<td class="' + this.stateClass(set.state) + '">' +
             this.esc(set.state) +
             '</td>';
    }
    const parts = Object.keys(set.states).map(function (state) {
      return set.states[state] + ' ' + state;
    });
    log.debug("Leaving AdminConsole.setStateCell().");
    return '<td class="state-none" title="' +
      this.esc('The members of this set are not all in the same state: ' +
               parts.join(', ') + '. That is the ordinary case rather than a ' +
               'fault — an access token and the refresh token issued with it ' +
               'have very different lifetimes — so this column reports the ' +
               'disagreement instead of picking one. The set page has each ' +
               'member and its own state. Filtering by a state finds a set ' +
               'when ANY member holds it.') +
      '">mixed</td>';
  }

  // WHEN THE SET COMES APART, and when it is finished. Two instants because the
  // members have two, and the earlier one first because it is the one somebody
  // debugging a refused call has arrived to find: the access token died at
  // 12:19 and the refresh token that could mint another is good until tomorrow.
  setExpiryCell(set) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.setExpiryCell().");
    if (!set.expiresAtMs) {
      log.debug("Leaving AdminConsole.setExpiryCell().");
      return '—';
    }
    if (!set.lastExpiresAtMs || set.lastExpiresAtMs === set.expiresAtMs) {
      log.debug("Leaving AdminConsole.setExpiryCell().");
      return this.esc(this.whenText(set.expiresAtMs));
    }
    log.debug("Leaving AdminConsole.setExpiryCell().");
    return '<span title="' +
      this.esc('The members of this set expire at different times. The first ' +
        'goes at ' +
               this.whenText(set.expiresAtMs) + ' and the last at ' +
               this.whenText(set.lastExpiresAtMs) +
               '; the set page says which is which.') +
      '">' + this.esc(this.whenText(set.expiresAtMs)) + ' &rarr; ' +
      this.esc(this.whenText(set.lastExpiresAtMs)) + '</span>';
  }

  // THE HANDLE, AND THE WAY IN. A group is named by the set id and opens the
  // set page; a set of one keeps the identifier cell it has always had and
  // opens the credential's lineage, because for one credential the set page
  // would add a click and nothing else. Both are links to somewhere, which is
  // why the two cases are here rather than in identifierCell(): that function
  // answers "what is this credential called", and this one answers "where does
  // this row go".
  setIdentifierCell(set, listView) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.setIdentifierCell().");
    if (!set.grouped) {
      log.debug("Leaving AdminConsole.setIdentifierCell().");
      return this.identifierCell(set.members[0], listView);
    }
    log.debug("Leaving AdminConsole.setIdentifierCell().");
    return '<a href="' +
      this.esc('/admin/tokens/set' +
               queryWith(listView || {}, { id: set.setKey })) +
      '" title="' +
      this.esc('The ' + set.size + ' credentials this one reply carried, ' +
               'each with its own identifier, expiry and button — and a link ' +
               'on to where each of them came from. The set id is this ' +
               'service\'s own handle on the reply: it is in no token, no ' +
               'client ever sees it, and it is not a claim.') + '">set ' +
      // NOT esc()'d: shortened() returns MARKUP — a <code> carrying the whole
      // value in its title, so a truncated identifier can still be read — which
      // is exactly what identifierCell() beside it does with the same call.
      // Escaping it prints the tag instead of the value.
      this.shortened(set.setId, 10) + '</a>';
  }

  // THE BUTTON FOR A WHOLE SET. One act rather than three clicks, and it acts
  // on the members that CAN be acted on — which for a set of one outside OAuth
  // is none of them, so those rows keep the dash and the sentence actionCell()
  // already gives them.
  //
  // Restore rather than Revoke once every revocable member is revoked, which is
  // the same two-state rule the per-token button follows. A PARTLY revoked set
  // offers Revoke, because the useful act there is finishing the job — the way
  // back for the one member somebody wants un-revoked is its own button on the
  // set page.
  setActionCell(set, backRow) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.setActionCell().");
    if (!set.grouped) {
      log.debug("Leaving AdminConsole.setActionCell().");
      return this.actionCell(set.members[0], backRow);
    }
    if (!set.revocableCount) {
      log.debug("Leaving AdminConsole.setActionCell().");
      return '<span class="state-none" title="' +
        this.esc('Nothing in this set can be revoked: only access tokens, ID ' +
                 'Tokens and refresh tokens can be, and the others are ' +
                 'replies rather than credentials or carry no jti to act on.') +
                 '">—</span>';
    }
    const allRevoked = set.revokedCount >= set.revocableCount;
    log.debug("Leaving AdminConsole.setActionCell().");
    return '<form method="post" action="/admin/tokens" class="inline">' +
      '<input type="hidden" name="action" value="' +
      (allRevoked ? 'restore-set' : 'revoke-set') + '">' +
      '<input type="hidden" name="set" value="' + this.esc(set.setKey) + '">' +
      '<input type="hidden" name="back" value="' + this.esc(backRow) + '">' +
      '<button class="' + (allRevoked ? 'secondary' : 'danger') + '" title="' +
      this.esc(allRevoked
        ? 'Un-revoke every revocable credential in this set. NON-SPEC — no ' +
               'real authorization server can undo a revocation.'
        : 'Revoke the ' + set.revocableCount + ' revocable credential(s) in ' +
               'this set in one act. Each one is revoked exactly as its own ' +
               'button would revoke it, into the same set of revoked jtis ' +
               'RFC 7009\'s /oauth2/revoke writes to.') + '">' +
      (allRevoked ? 'Restore set' : 'Revoke set') + '</button></form>';
  }

  issuedSetRow(set, backRow, listView) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.issuedSetRow().");
    const first = set.members[0];
    log.debug("Leaving AdminConsole.issuedSetRow().");
    return '<tr><td>' + this.contentsCell(set) + '</td>' +
      this.setStateCell(set) +
      '<td>' + this.userCell(first) + '</td>' +
      '<td>' + this.subjectCell(first) + '</td>' +
      '<td>' + this.partyCell(first) + '</td>' +
      '<td>' + this.detailCell(first) + '</td>' +
      '<td>' + this.presentedCell(first) + '</td>' +
      '<td>' + this.esc(this.whenText(set.issuedAt)) + '</td>' +
      '<td>' + this.setExpiryCell(set) + '</td>' +
      '<td>' + this.setIdentifierCell(set, listView) + '</td>' +
      '<td>' + this.setActionCell(set, backRow) + '</td></tr>';
  }

  // ---------------------------------------------------------------------------
  // A ONE-BOX SEARCH OVER ONE SECTION OF A PAGE THAT CARRIES SEVERAL.
  //
  // The list views have a filter form each and they can: a page with one table
  // on it can put the filter, the page size and the search in one row above it
  // and every control there is unambiguous. /admin/delegation is seven tables,
  // and a second `q` would have been a box the reader has to guess the scope
  // of. So each searchable section gets its own parameter and its own box,
  // drawn immediately under its own heading, and the box says in its label
  // WHICH table it narrows.
  //
  // It is deliberately NOT chooserPane(). That control is a SEARCH FOR ONE
  // THING — it draws a scrolling pane of candidates and every hit is a link
  // away from this page — and this one narrows a table the reader is going to
  // stay and read. Sharing an implementation would have meant one function with
  // a mode flag deciding whether its results were the answer or the rows, which
  // is two controls wearing one name.
  //
  // THE FRAGMENT IS THE SAME TRICK AND FOR THE SAME REASON chooserPane()'s
  // header argues at length: this is a GET that reloads the page, a reload
  // lands at the top of the document, and this console runs no script (app.js
  // sets `script-src 'none'`) so nothing can restore a scroll offset
  // afterwards. Submitting to `#find-<param>` puts the box back under the
  // reader's eyes. Submitting a GET form replaces the action URL's QUERY and
  // leaves its FRAGMENT alone, which is why the anchor cannot be a hidden
  // input.
  //
  // TWO NAMES COME OUT OF THE CARRIED SET AND EACH FOR ITS OWN REASON. The
  // search term, because the text input re-emits it and a hidden input beside
  // it would submit the old one; and the section's PAGE NUMBER, because a new
  // search starts at its first page — carrying page 4 into a two-page result
  // would be clamped by pagingOf() and read as the box ignoring what was typed.
  //
  // `spec`: { path, query, param, pageParam, label, placeholder, what }
  sectionSearchForm(spec) {
    const { log, queryOne, pageParamsOf, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.sectionSearchForm(). param=" + spec.param);
    const query = spec.query || {};
    const wanted = queryOne(query, spec.param).trim();
    const anchor = 'find-' + spec.param;
    const carried = pageParamsOf(query);
    delete carried[spec.param];
    delete carried[spec.pageParam];
    const hidden = Object.keys(carried).map(function (name) {
      return '<input type="hidden" name="' + self.esc(name) + '" value="' +
             self.esc(carried[name]) + '">';
    }).join('');
    log.debug("Leaving AdminConsole.sectionSearchForm(). wanted=" +
              (wanted || '(nothing)'));
    return '<form method="get" id="' + this.esc(anchor) +
           '" class="finder" action="' +
      this.esc(spec.path) + '#' + this.esc(anchor) + '">' +
      '<div class="formrow">' + hidden +
        '<label for="' + this.esc(spec.param) + '">' + this.esc(spec.label) +
        '</label><input type="text" id="' + this.esc(spec.param) + '" name="' +
      this.esc(spec.param) +
          '" size="32" value="' + this.esc(wanted) + '" placeholder="' +
          this.esc(spec.placeholder) + '">' +
        '<button class="secondary">Search</button>' +
        (wanted
          ? ' <a href="' + this.esc(spec.path + queryWith(carried, {})) + '#' +
            this.esc(anchor) +
            '">clear</a>'
          : '') +
      '</div></form>' +
      // OUTSIDE the form rather than in it. A note() longer than a line is a
      // `<details>`, and a disclosure widget inside a form is legal but reads
      // as part of the control — this sentence is about the TABLE under the
      // box.
      (spec.what ? this.note(spec.what) : '');
  }

  // The per-page select, written once because five surfaces offer it and a
  // sixth written by hand is the one that forgets to add a hand-typed size to
  // the list.
  //
  // MAX_ROWS is offered as the largest choice so the old behaviour — everything
  // on one page, up to the cap — is still one click away for anyone who wants
  // to search the table with the browser's own find.
  //
  // A hand-typed `?per=7` is ADDED to the list rather than ignored, or the
  // select would show a size that is not the one being used and would silently
  // change it on the next Filter — which is a control that lies about the page
  // it is on.
  perPageOptions(perPage) {
    const { log, DEFAULT_PER_PAGE, MAX_ROWS } = this.deps;
    log.debug("Entering AdminConsole.perPageOptions().");
    const choices = [25, DEFAULT_PER_PAGE, 100, MAX_ROWS];
    if (choices.indexOf(perPage) < 0) {
      choices.push(perPage);
      choices.sort(function (a, b) { return a - b; });
    }
    log.debug("Leaving AdminConsole.perPageOptions().");
    return choices.map(function (n) {
      return '<option value="' + n + '"' + (n === perPage ? ' selected' : '') +
             '>' +
             n + ' rows</option>';
    }).join('');
  }

  // The same control as a form of its own, for the two DRILL-DOWNS — which have
  // no filter form to hang it on, and which need it more than the lists do
  // because they carry several tables at once.
  //
  // Submitting it drops every page parameter, which is deliberate rather than
  // an oversight of the hidden inputs: a GET form posts its own fields and
  // nothing else, and page 4 of fifty-row pages is not page 4 of anything after
  // the size changes. Going back to the top of each list is the only answer
  // that is true of all of them.
  //
  // `carry` is the list's FILTER, and it has to be spelt out as hidden inputs
  // for the reason above: a GET form posts its own fields and nothing else, so
  // without them this control quietly empties the breadcrumb's way back to the
  // list the reader came from. Its PAGE is deliberately not carried — `per` is
  // the thing this form changes, and page 4 of fifty-row pages is not page 4 of
  // anything afterwards, which is the same sentence as the paragraph above
  // about the tables below.
  perPageForm(path, key, value, perPage, extraNote?, carry?) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.perPageForm(). path=" + path);
    const carried = Object.keys(carry || {}).map(function (name) {
      return '<input type="hidden" name="' + self.esc(name) + '" value="' +
             self.esc(carry[name]) + '">';
    }).join('');
    const html = '<form method="get" action="' + this.esc(path) + '"><div ' +
      'class="formrow"><input type="hidden" ' +
      'name="' + this.esc(key) + '" value="' + this.esc(value) + '">' +
      carried +
      '<label for="per">Rows per table</label>' +
      '<select id="per" name="per">' + this.perPageOptions(perPage) +
      '</select><button class="secondary">Apply</button>' +
      this.note('Every table below is paged separately and they share this ' +
      'size. Changing it starts each of them at its first page.' +
      (extraNote ? ' ' + extraNote : '')) +
      '</div></form>';
    log.debug("Leaving AdminConsole.perPageForm().");
    return html;
  }

  // The FILTER half of a carried list view — everything except the two
  // parameters that describe how the list was being paged. Written once because
  // both of the controls that need it need the same half: a form that changes
  // the page size, and anything else that lands the reader at the top of a list
  // rather than where they were in it.
  filterOnly(view) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.filterOnly().");
    const out = {};
    Object.keys(view || {}).forEach(function (key) {
      if (key !== 'page' && key !== 'per') {
        out[key] = view[key];
      }
    });
    log.debug("Leaving AdminConsole.filterOnly().");
    return out;
  }

  // The paging control. Drawn above and below the table both, because the
  // reason to want the next page is usually that you have just read to the
  // bottom of this one.
  //
  // The numbered links are a WINDOW around the current page rather than one per
  // page: 5,000 tokens at 50 a page is 100 links, which is a worse navigation
  // aid than none. First and last are always offered so the ends stay one click
  // away.
  //
  // IT RETURNS THE TWO COPIES RATHER THAN ONE STRING, AND THAT IS THE WHOLE
  // REASON THIS FUNCTION WAS RENAMED ON 2026-08-27.
  //
  // Every link here is a page load, and a page load lands at the top of the
  // document — so `next ›` threw the reader back past the sidebar, the folded
  // prose and the filter row to read the rows they had asked for, on pages that
  // are several thousand pixels long. The fix is the one chooserPane() and the
  // delegation filter use: the control submits to a FRAGMENT naming itself, so
  // the browser puts it back under the reader's eyes. This console runs no
  // script (app.js sets `script-src 'none'`), so nothing can restore a scroll
  // offset after the navigation and there is no other fix available.
  //
  // The fragment has to name ONE element, and this control is drawn TWICE with
  // the table between the copies. An id must be unique in a document, and the
  // two copies are not interchangeable anyway: whichever copy was clicked, the
  // reader wants the TOP of the page they have just asked for, which is the
  // head copy. So the head copy carries the id and the foot copy does not, and
  // the call site says which it is drawing — `nav.head` above the table,
  // `nav.foot` below it. Building both from one call is what stops the two from
  // drifting into controls that page different lists.
  //
  // The id is the list's OWN paging parameter, which is already unique per list
  // on a page for the reason pagingOf() gives — a drill-down draws five of
  // these and each must send its reader back to its own table, not to the first
  // one.
  pageNavPair(path, params, pg) {
    const { log, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.pageNavPair(). pages=" + pg.pages + ", " +
      "param=" +
              pg.param);
    if (pg.pages <= 1) {
      log.debug("Leaving AdminConsole.pageNavPair(). One page; no control " +
                "drawn.");
      return { head: '', foot: '' };
    }
    const anchor = 'list-' + pg.param;
    function link(page, label, title?) {
      log.debug("Entering link().");
      const move = {};
      // The list's OWN parameter, off pg, so that a drill-down's five controls
      // move five different lists. Everything else in `params` rides along
      // untouched, which is what keeps the other four where the reader left
      // them.
      move[pg.param] = page;
      log.debug("Leaving link().");
      // The fragment is the head copy of THIS control — see the header. It
      // rides on the href rather than on the page's own URL, so a link somebody
      // copies out of here still opens where they were looking.
      return '<a href="' + self.esc(path + queryWith(params, move)) + '#' +
             self.esc(anchor) +
             '"' + (title ? ' title="' + self.esc(title) + '"' :
                    '') + '>' + label + '</a>';
    }
    const out = [];
    if (pg.page > 1) {
      out.push(link(1, '&laquo; first', 'The newest rows'));
      out.push(link(pg.page - 1, '&lsaquo; prev'));
    } else {
      out.push('<span class="off">&laquo; first</span><span ' +
               'class="off">&lsaquo; prev</span>');
    }
    const from = Math.max(1, Math.min(pg.page - 3, pg.pages - 6));
    const to = Math.min(pg.pages, Math.max(pg.page + 3, 7));
    for (let n = from; n <= to; n++) {
      out.push(n === pg.page ? '<span class="here">' + n + '</span>' :
               link(n, String(n)));
    }
    if (pg.page < pg.pages) {
      out.push(link(pg.page + 1, 'next &rsaquo;'));
      out.push(link(pg.pages, 'last &raquo;', 'The oldest rows still held'));
    } else {
      out.push('<span class="off">next &rsaquo;</span><span class="off">last ' +
               '&raquo;</span>');
    }
    out.push('<span class="where">page ' + pg.page + ' of ' + pg.pages + ' — ' +
             pg.noun + ' ' +
             pg.firstRow + '&ndash;' + pg.lastRow + ' of ' + pg.total +
             '</span>');
    log.debug("Leaving AdminConsole.pageNavPair(). Drew " + out.length +
              " element(s).");
    const inner = out.join('') + '</div>';
    log.debug("Leaving AdminConsole.pageNavPair().");
    return {
      head: '<div class="pagenav" id="' + this.esc(anchor) + '">' + inner,
      // The same control, without the id. A list drawn with ONE copy uses
      // `head` whichever end of the table it is at, because an anchor nothing
      // points to is the bug this exists to prevent.
      foot: '<div class="pagenav">' + inner
    };
  }

  // ---------------------------------------------------------------------------
  // GET /admin — the index.
  // ---------------------------------------------------------------------------

  // ---------------------------------------------------------------------------
  // THE JSON VIEWS, AND WHY EVERY ONE OF THEM IS NOW A FUNCTION.
  //
  // Each page here answers `?format=json`, and until admin_api.js existed each
  // of those objects was built inline in the route handler that also built the
  // markup. That was fine while there was one caller. There are two now — this
  // console and the management API at /admin-api — and two hand-built copies of
  // the same object is precisely the drift the console's own text keeps warning
  // about elsewhere: two views that each look correct alone and never see each
  // other.
  //
  // So the JSON is a function per page, and where the markup needs the same
  // intermediate work (the filtered, paged token list; a user drill-down that
  // is one of three answers) the WHOLE VIEW is the function and the route is
  // what chooses between HTML and JSON. admin_api.js calls these and nothing
  // else — it holds no second opinion about what a metrics reply contains.
  //
  // One cost used to be stated here: the API called usersView() and
  // groupsView(), which build the HTML as well, and threw the markup away. It
  // no longer does — `/admin-api/users` and `/admin-api/groups` call
  // `usersJson()` and `groupsJson()` in `admin-core/admin_views.ts`, which
  // choose between the same answers without drawing a page.
  // ---------------------------------------------------------------------------
  consoleJson() {
    const { log, stats, config, sessions } = this.deps;
    log.debug("Entering AdminConsole.consoleJson().");
    const snap = stats.snapshot();
    const json = {
      issuer: config.value('wstrust.issuer'),
      startedAt: new Date(snap.startedAt).toISOString(),
      uptimeMs: snap.uptimeMs,
      calls: snap.calls.total, tokensHeld: snap.tokens.held,
      tokensRevoked: snap.tokens.revoked,
      artifactsHeld: snap.artifacts.held, signOnSessions: sessions.size,
      usersKnown: snap.users.known,
      usersAuthenticatedHere: snap.users.authenticatedHere,
      pages: NAV.map(function (n) { return n.path; })
    };
    log.debug("Leaving AdminConsole.consoleJson().");
    return json;
  }

  // ---------------------------------------------------------------------------
  // THE OVERVIEW PAGE'S OWN LIST OF WHAT IS HERE, DERIVED FROM `SECTIONS`.
  //
  // It was a hand-written `<ul>` in the route below until 2026-08-25, and it
  // had drifted to describing SEVEN of twenty-five pages — so the one page
  // whose whole job is to point at the others was the least complete
  // description of this console in the repository. Nothing could have shown
  // that: a list of links is correct-looking whatever it omits, which is
  // exactly the shape of problem `/admin/sts-metadata` exists to make
  // impossible for endpoints.
  //
  // So it is built from the same table the sidebar is built from, and three
  // rules follow from that rather than from taste:
  //
  //   * **A page with no `blurb` is DRAWN, marked as undescribed.** The
  //     tempting alternative — skip it — is the bug over again with a mechanism
  //     behind it, since a page added to `SECTIONS` and not described here
  //     would silently vanish from the front door exactly as nineteen of them
  //     had.
  //   * **The page being drawn ON is dropped**, which is `/admin` and only ever
  //     `/admin`. `trailBar()`'s last-crumb rule is the same one: a link that
  //     reloads the page you are standing on teaches a reader not to trust the
  //     ones beside it. A section left empty by that drop is dropped with it,
  //     rather than leaving a heading over nothing.
  //   * **The GROUPS are kept**, unlike `sectionPages()`, which flattens them.
  //     The sidebar's grouping is a fact about the sidebar — but this list is
  //     the sidebar EXPLAINED, and a reader looking for `Registration entries`
  //     needs the same "these three are SPIFFE" that made the group worth
  //     having.
  //
  // The blurbs are prose in `SECTIONS` beside each page's `path` and `label`.
  // This function knows nothing about any particular page, which is what stops
  // it becoming a second place a page has to be described.
  guideItem(page) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.guideItem().");
    if (!page.blurb) {
      // NOT an omission and not a throw. A console that will not start is worse
      // than one line that says what is missing — the same call `admin_rbac.js`
      // makes about a slot nobody filled — and this is the only report anything
      // makes about a page nobody described.
      log.debug("guideItem(). No blurb for " + page.path + ".");
      log.debug("Leaving AdminConsole.guideItem().");
      return this.bullet('<a href="' + this.esc(page.path) + '">' +
                         this.esc(page.label) +
             '</a> — <span class="undescribed">this page has no description ' +
             'in <code>SECTIONS</code>. It was added to the nav and not ' +
             'described here; the list you are reading is derived from that ' +
             'table, so this row is the report rather than a gap.</span>');
    }
    log.debug("Leaving AdminConsole.guideItem().");
    // THE LINK STAYS ON THE ROW AND THE BLURB FOLDS UNDER IT. Every blurb in
    // SECTIONS runs to between three and six lines — they are the description
    // of a page rather than a caption for it — so this list, which is the one
    // page in this console whose whole job is pointing at the others, was
    // twenty-six paragraphs deep and the twenty-six links were scattered
    // through it. It is an index again: the labels are a column somebody can
    // run an eye down, and the paragraph is under whichever one they stopped
    // at.
    //
    // The link is deliberately NOT inside the summary. bullet() refuses to fold
    // an item that opens with one for the reason given there — a link inside a
    // <summary> is a control inside a control — and this row would have been
    // the first place that bit.
    return '<li><a href="' + this.esc(page.path) + '">' + this.esc(page.label) +
           '</a>' +
           this.note(page.blurb) + '</li>';
  }

  consoleGuide(activePath, state) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.consoleGuide(). activePath=" + activePath);
    const out = [];
    this.visibleSections(state).forEach(function (section) {
      const items = [];
      section.items.forEach(function (item) {
        if (self.isNavGroup(item)) {
          const pages = item.items.filter(function (page) {
            return page.path !== activePath;
          });
          if (!pages.length) {
            return;
          }
          // The group's own description folds like a page's, for the same
          // reason: SAML's runs to five lines and sat between the heading and
          // the three pages under it.
          items.push('<li><span class="grp">' + self.esc(item.title) +
                     '</span>' +
                     self.note(self.esc(item.what)) +
                     '<ul>' + pages.map(self.guideItem.bind(self)).join('') +
                     '</ul></li>');
          return;
        }
        if (item.path === activePath) {
          return;
        }
        items.push(self.guideItem(item));
      });
      if (!items.length) {
        log.debug("consoleGuide(). Section " + section.title +
                  " is empty here.");
        return;
      }
      out.push('<h3>' + self.esc(section.title) + '</h3>' +
               self.note(self.esc(section.what)) +
               '<ul>' + items.join('') + '</ul>');
    });
    log.debug("Leaving AdminConsole.consoleGuide(). " + out.length +
              " section(s) drawn.");
    return '<div class="guide">' + out.join('') + '</div>';
  }

  // ---------------------------------------------------------------------------
  // GET /admin/metrics
  // ---------------------------------------------------------------------------


  callTable(snap) {
    const { log, MAX_ROWS } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.callTable().");
    const rows = snap.calls.rows.slice(0, MAX_ROWS);
    const body = rows.map(function (row) {
      return '<tr><td><code>' + self.esc(row.method) + '</code></td>' +
        '<td><code>' + self.esc(row.path) + '</code>' +
        (row.matched ? '' : ' <span class="state-expired">no route</span>') +
        '</td><td ' +
        'class="num">' + row.count + '</td>' +
        '<td class="num">' + (row.statuses['2xx'] || 0) + '</td>' +
        '<td class="num">' + (row.statuses['3xx'] || 0) + '</td>' +
        '<td class="num">' + (row.statuses['4xx'] || 0) + '</td>' +
        '<td class="num">' + (row.statuses['5xx'] || 0) + '</td>' +
        '<td class="num">' + Math.round(row.totalMs / Math.max(row.count, 1)) +
        '</td><td ' +
        'class="num">' + row.maxMs + '</td>' +
        '<td>' + self.esc(self.whenText(row.lastAt)) + '</td></tr>';
    }).join('');
    const hidden = snap.calls.rows.length - rows.length;
    log.debug("Leaving AdminConsole.callTable(). " + rows.length + " row(s).");
    return '<table><tr><th>Method</th><th>Route</th><th ' +
      'class="num">Calls</th><th class="num">2xx</th><th ' +
      'class="num">3xx</th><th class="num">4xx</th><th ' +
      'class="num">5xx</th><th class="num">Avg ms</th><th class="num">Max ' +
      'ms</th><th>Last</th></tr>' +
      (body ||
       '<tr><td colspan="10">No call has been recorded yet.</td></tr>') +
      '</table>' +
      (hidden > 0 ? this.note(hidden + ' further route(s) are not shown; the ' +
                                       'table draws the ' +
                    MAX_ROWS + ' busiest.') : '') +
      (snap.calls.pathsCollapsed > 0
        ? this.note(snap.calls.pathsCollapsed + ' request(s) to paths that ' +
          'matched no route were counted in the single ' +
          '<code>' + this.esc('(other unmatched paths)') + '</code> ' +
          'row: the table is capped so that a scanner inventing URLs cannot ' +
          'grow it without limit.')
        : '');
  }

  tokenKindTable(snap) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.tokenKindTable(). " +
              snap.tokens.byKind.length + " " +
        "kind(s).");
    const body = snap.tokens.byKind.map(function (row) {
      return '<tr><td>' + self.esc(row.kind) + '</td>' +
        '<td class="num">' + row.issued + '</td>' +
        '<td class="num state-valid">' + row.valid + '</td>' +
        '<td class="num state-expired">' + row.expired + '</td>' +
        '<td class="num state-revoked">' + row.revoked + '</td>' +
        '<td class="num">' + row.notYetValid + '</td>' +
        '<td class="num">' + row.noExpiry + '</td>' +
        '<td class="num">' + row.bound + '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.tokenKindTable().");
    return '<table><tr><th>Token</th><th class="num">Issued</th><th ' +
      'class="num">Valid</th><th class="num">Expired</th><th ' +
      'class="num">Revoked</th><th class="num">Not yet valid</th><th ' +
      'class="num">No expiry</th><th class="num">DPoP-bound</th></tr>' +
      (body || '<tr><td colspan="8">No token has been issued yet.</td></tr>') +
      '</table>';
  }

  artifactKindTable(snap) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.artifactKindTable(). " +
              snap.artifacts.byKind.length +
        " " +
        "kind(s).");
    const body = snap.artifacts.byKind.map(function (row) {
      return '<tr><td>' + self.esc(row.kind) + '</td>' +
        '<td class="num">' + row.issued + '</td>' +
        '<td class="num state-valid">' + row.valid + '</td>' +
        '<td class="num state-expired">' + row.expired + '</td>' +
        '<td class="num">' + row.noExpiry + '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.artifactKindTable().");
    return '<table><tr><th>Artifact</th><th class="num">Issued</th><th ' +
      'class="num">Valid</th><th class="num">Expired</th><th class="num">No ' +
      'expiry</th></tr>' +
      (body || '<tr><td colspan="5">No assertion, ticket or credential has ' +
               'been issued yet.</td></tr>') +
      '</table>';
  }

  // ---------------------------------------------------------------------------
  // GET /admin/tokens, POST /admin/tokens
  // ---------------------------------------------------------------------------



  // Where a form POST sends the browser back to. Revoking the token on page 4
  // and landing on page 1 of an unfiltered list is the paging bug everybody has
  // met, so the row forms carry the view they were rendered in as a `back`
  // field.
  //
  // It is REBUILT rather than echoed, and that is the whole point of doing it
  // here: a redirect target taken from a request body is an open redirect, and
  // one carrying a newline is a header injection. Only the parameters this page
  // understands survive, each of them re-encoded, so the worst a hand-written
  // `back` can produce is a different page of this same table.
  //
  // The list below has to be kept in step with the filter form. A parameter the
  // form offers and this function drops is a filter that silently resets itself
  // the moment somebody revokes a token — which looks like the console losing
  // your place rather than like a missing line here.
  //
  // The users page posts to this same endpoint, so `from` says which of the two
  // pages a button was on. It is read as an ENUM and never as a path: the two
  // paths below are written here, and a `from` naming anything else falls
  // through to the tokens page. That is what keeps the open-redirect property
  // while letting a second page share the handler — a `back` field carrying
  // `//evil.example` would otherwise become a redirect off this service the
  // moment a path came from the body. The drill-down's page parameters, out of
  // a `back` field, for the redirect that follows a revoke.
  //
  // The named parameters above are a WHITELIST, which is the right shape for a
  // redirect target built out of a form field somebody posted — but a whitelist
  // cannot cover this set, because one of the users page's lists has a page
  // parameter per session block and the names therefore depend on which
  // sessions exist. So the rule is a shape rather than a list: a key ending in
  // `Page`, made of the characters a name and a base64url session id are made
  // of, whose value is a positive integer. The value is REBUILT from parseInt
  // rather than passed through, so what lands in the URL is a number this
  // function produced.
  //
  // What it buys is the thing a reader notices immediately: revoking a token
  // from page three of a session's table used to answer with page one of
  // everything, so the row you had just acted on was no longer on screen and
  // neither was its neighbour.
  drillDownPages(params) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.drillDownPages().");
    const out = {};
    params.forEach(function (value, key) {
      if (!/^[A-Za-z0-9_-]+Page$/.test(key)) {
        return;
      }
      const n = parseInt(String(value), 10);
      if (isFinite(n) && n > 0) {
        out[key] = String(n);
      }
    });
    log.debug("Leaving AdminConsole.drillDownPages(). " +
              Object.keys(out).length + " page " +
        "parameter(s).");
    return out;
  }

  backTo(body) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.backTo().");
    let params = null;
    try {
      params = new URLSearchParams(String(body.back || '').replace(/^\?/, ''));
    } catch (e) {
      // Unparseable; the bare page is the right answer and is what the forms
      // that carry no `back` at all get anyway.
      log.debug("Leaving AdminConsole.backTo(). Unparseable back field: " +
                e.message);
      return '/admin/tokens';
    }
    if (String(body.from || '') === 'users') {
      const usersTarget = '/admin/users' + queryWith(Object.assign({
        user: params.get('user') || '',
        q: params.get('q') || '',
        protocol: params.get('protocol') || '',
        per: params.get('per') || '',
        page: params.get('page') || ''
      }, this.drillDownPages(params)), {});
      log.debug("Leaving AdminConsole.backTo(). " + usersTarget);
      return usersTarget;
    }
    // The set page's own buttons, which is the third of the three surfaces that
    // post here. `from` is read as an ENUM and never as a path — the three
    // targets are written out in this function — which is what lets a page
    // share the handler without a `back` field carrying `//evil.example`
    // becoming a redirect off this service.
    if (String(body.from || '') === 'set') {
      const setTarget = '/admin/tokens/set' + queryWith({
        id: params.get('id') || '',
        family: params.get('family') || '',
        kind: params.get('kind') || '',
        state: params.get('state') || '',
        session: params.get('session') || '',
        per: params.get('per') || '',
        page: params.get('page') || ''
      }, {});
      log.debug("Leaving AdminConsole.backTo(). " + setTarget);
      return setTarget;
    }
    const target = '/admin/tokens' + queryWith({
      family: params.get('family') || '',
      kind: params.get('kind') || '',
      state: params.get('state') || '',
      // CARRIED SINCE 2026-09-05, and its absence was a bug rather than a
      // decision: this whitelist has to be kept in step with the filter form
      // above — the comment on this function says so — and `session` was added
      // to that form on 2026-09-04 without being added here. So arriving from
      // /admin/sessions, narrowing to one session and revoking anything sent
      // the reader back to the unfiltered list, which reads as the console
      // losing their place rather than as a missing line here.
      session: params.get('session') || '',
      per: params.get('per') || '',
      page: params.get('page') || ''
    }, {});
    log.debug("Leaving AdminConsole.backTo(). " + target);
    return target;
  }

  // ---------------------------------------------------------------------------
  // MONITORING -> SESSIONS. EVERY SESSION THIS SERVICE IS HOLDING RIGHT NOW.
  //
  // **IT IS THE OTHER HALF OF /admin/tokens AND THE TWO ARE NOT THE SAME
  // LIST.** That page is what this service has HANDED OUT — every one of those
  // outlives the session it came from, several by design and one (a Kerberos
  // service ticket) beyond recall entirely. This page is what it is still
  // HOLDING: the state that makes somebody currently authenticated, which is
  // the question somebody actually arrives with when they ask who is signed in.
  //
  // **THE MODEL IS `logout/logout.ts`'s AND NOT THIS FILE'S**, through the
  // sixth slot. That module is the one answer to "what is a live session"
  // across families — see its CLAUDE.md — and this page walking
  // `authn.sessions`, `boundConnections()` and the ticket register itself would
  // be a SECOND answer, which is the thing rule 3m exists to prevent. It
  // matters more here than on /admin/logout because of the button: a Revoke
  // drawn from one reading and performed by another is a control that acts on
  // something other than the row it is beside.
  //
  // **THE EXPIRY COLUMN IS THE ONE WORTH READING TWICE.** Each session kind
  // works its expiry out differently and the difference is not a detail — a
  // browser session's is absolute and is not extended by use, a TGT's was
  // sealed into the ticket by the KDC and cannot be moved, and an LDAP
  // connection has no expiry at all; since 2026-09-06 an API caller's session
  // is extended by use, and since 2026-09-12 the console's and portal's own
  // sessions are renewed with their refresh token. So the column carries the
  // countdown AND the rule, and the rule comes off `liveSessions()`'s row
  // rather than being written again here.
  //
  // **THE REVOKE BUTTON GOES THROUGH `terminate()`**, the same function
  // /logout's global sign-out uses, with a selection of one — so it writes the
  // same audit row, honours the same two settings and gives the same refusals.
  // What it does NOT do is pretend the three are alike: ending a browser
  // session ends that session, ending an LDAP connection closes a socket, and
  // ending a Kerberos row stamps a sign-out instant on a PRINCIPAL that refuses
  // every ticket it authenticated before now. The last of those does more than
  // the row it is on, and the row says so rather than the button quietly doing
  // it.
  // ---------------------------------------------------------------------------


  // WHEN THIS SESSION ENDS, AND HOW THAT IS WORKED OUT. Several answers rather
  // than one, because the kinds are genuinely different and a column that
  // showed only a timestamp would be read as one rule with several values. The
  // rule is the row's `expiryRule`, which `logout.ts`'s SESSION_EXPIRY_RULES
  // writes.
  sessionExpiryCell(row, nowMs) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.sessionExpiryCell().");
    const rule = row.expiryRule || '';
    if (!row.expiresAt) {
      log.debug("Leaving AdminConsole.sessionExpiryCell().");
      return '<td class="sub" title="' + this.esc(rule) + '"><strong>no ' +
        'expiry</strong><div class="sub">it ends when something ends ' +
        'it</div></td>';
    }
    const left = row.expiresAt - nowMs;
    if (left <= 0) {
      log.debug("Leaving AdminConsole.sessionExpiryCell().");
      // Live rows only reach here in a race — the list was built a moment ago —
      // and saying so is better than a negative countdown.
      return '<td class="state-expired" title="' + this.esc(rule) +
             '">expired' +
        '<div class="sub">' + this.esc(this.whenText(row.expiresAt)) +
        '</div></td>';
    }
    log.debug("Leaving AdminConsole.sessionExpiryCell().");
    return '<td' + (left < 5 * 60 * 1000 ? ' class="state-expired"' : '') +
      ' title="' + this.esc(rule) + '">in ' +
      this.esc(this.durationText(left)) +
      '<div class="sub">' + this.esc(this.whenText(row.expiresAt)) +
      '</div></td>';
  }

  // WHAT CAME OUT OF THIS SESSION, as a link into /admin/tokens.
  //
  // Only a browser sign-on session has a join: a token records the `sessionId`
  // it was issued under, and nothing else here does. A Kerberos row therefore
  // links to its FAMILY — the TGT on this row is in that table and this service
  // keeps no handle on one, which is a fact about Kerberos rather than a gap —
  // and an LDAP connection links nowhere at all, because a bind issues no
  // credential.
  sessionCredentialsCell(row) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.sessionCredentialsCell().");
    if (row.family === 'session') {
      log.debug("Leaving AdminConsole.sessionCredentialsCell().");
      return '<td><a href="' + this.esc('/admin/tokens' +
        queryWith({ session: row.sessionId }, {})) +
        '" title="' + this.esc('Every credential issued under this session') +
        '">issued on it</a></td>';
    }
    if (row.family === 'krb5') {
      log.debug("Leaving AdminConsole.sessionCredentialsCell().");
      return '<td><a href="' + this.esc('/admin/tokens' +
        queryWith({ family: 'ticket' }, {})) +
        '" title="' + this.esc('Every Kerberos ticket this KDC has minted. ' +
          'There is no per-session link: a ticket carries no identifier this ' +
          'service keeps a handle on.') + '">the ticket table</a></td>';
    }
    log.debug("Leaving AdminConsole.sessionCredentialsCell().");
    return '<td class="sub" title="' +
           this.esc('A Bind issues no credential. It sets the authorization ' +
      'state of a connection, and that state is this row.') + '">none</td>';
  }

  sessionRow(row, nowMs, canWrite, back) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.sessionRow(). " + row.id);
    const revoke = row.terminable && canWrite
      ? '<form method="post" action="/admin/sessions">' +
        '<input type="hidden" name="action" value="revoke">' +
        '<input type="hidden" name="key" value="' + this.esc(row.key) + '">' +
        '<input type="hidden" name="select" value="' + this.esc(row.id) + '">' +
        '<input type="hidden" name="back" value="' + this.esc(back) + '">' +
        '<button class="danger"' +
        (row.why ? ' title="' + this.esc(row.why) + '"' : '') +
        '>Revoke</button></form>'
      : (canWrite
          ? '<span class="state-none" title="' + this.esc(row.why) +
            '">cannot</span>'
          : '<span class="state-none" title="' +
            this.esc('Ending a session needs the Admin Write role.') +
            '">—</span>');
    const out = '<tr>' +
      '<td>' + this.esc(row.kind) +
      '<div class="sub">' + this.shortened(row.handle, 28) + '</div></td>' +
      '<td>' + this.esc(row.protocol) +
      (row.acr ? '<div class="sub">acr ' + this.esc(row.acr) +
        (row.amr.length ? ', amr ' + this.esc(row.amr.join(' ')) : '') +
        '</div>' :
       '') +
      '</td>' +
      '<td>' + this.esc(row.username || '(unknown)') +
      (row.sub ? '<div class="sub"><code>' + this.esc(row.sub) +
       '</code></div>' :
       '') +
      '</td>' +
      '<td class="sub">' +
      (row.startedAt ? this.esc(this.whenText(row.startedAt))
                     : '<span title="' +
                       this.esc('Nothing recorded when this one ' +
                       'started. A connection bound before this service ' +
                       'began stamping the instant reads this way.') +
                       '">not ' +
                           'recorded</span>') +
      '</td>' +
      this.sessionExpiryCell(row, nowMs) +
      '<td class="sub">' + this.esc(row.detail || '—') + '</td>' +
      this.sessionCredentialsCell(row) +
      '<td>' + revoke + '</td>' +
      '</tr>';
    log.debug("Leaving AdminConsole.sessionRow().");
    return out;
  }

  // ---------------------------------------------------------------------------
  // GET /admin/audit — what happened here, in order.
  //
  // The other pages on this console are STATE: how many calls, which tokens are
  // still valid, who is in cn=developers. This one is HISTORY, and the
  // difference is the reason it exists. The metrics page can tell you the
  // directory holds eleven entries; only this one can tell you that a twelfth
  // was created at 14:02 and deleted at 14:03, by somebody bound as uid=carol,
  // over LDAPS.
  //
  // Six categories, and every event in the service arrives through one of five
  // funnels rather than from a recording site per feature:
  //
  //   authentication   admin_stats.recordAuthentication(), the single point all
  //                    sixteen protocol families already pass through when a
  //                    credential is ACCEPTED — SCIM being the fifteenth, for
  //                    the three of its schemes that present a credential per
  //                    request, and SPIFFE the sixteenth, for an X509-SVID
  //                    presented over mutual TLS, an agent attesting and a
  //                    JWT-SVID validated
  //   session          authn.js's startSession / endSession, which is where both
  //                    OAuth 2.0 / OIDC and WS-Federation sign in and out
  //   directory        the seven LDAP handlers in ldap_server.js, plus the
  //                    entries this service creates for people who
  //                    authenticated somewhere else
  //   admin / api      app.js's call log, classified by path
  //   protocol         the same call log, everything else
  //
  // **ONE ACT USUALLY PRODUCES SEVERAL ROWS.** A sign-in at /authn/login writes
  // three: the HTTP call, the credential being accepted, and the session that
  // came out of it. They are three facts at three layers rather than one fact
  // three times — and which of them you want depends on the question, which is
  // exactly why the log does not choose. The page says so under the table.
  //
  // **IT OBSERVES ITSELF.** Drawing this page is console access, so it records
  // an `admin.view` row, so the list is one longer than when you asked for it.
  // Suppressing that would put a blind spot exactly where the person reading
  // the audit log stands. It is stated instead, and `?category=` reads past it.
  // ---------------------------------------------------------------------------


  // The outcome, in the same three colours the token states use — so that a
  // page somebody has learned to skim once reads the same way here. `refused`
  // is amber rather than red on purpose: it is this service working correctly
  // and saying no, which is most of what a debugger of a protocol client wants
  // to see, and painting it as a failure would bury the 5xx rows that are one.
  outcomeCell(outcome) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.outcomeCell().");
    const cls = outcome === 'success' ? 'state-valid'
              : (outcome === 'refused' ? 'state-expired' : 'state-revoked');
    log.debug("Leaving AdminConsole.outcomeCell().");
    return '<span class="' + cls + '">' + this.esc(outcome) + '</span>';
  }

  // The detail object as one cell. Rendered as `key=value` pairs rather than as
  // JSON because the column is narrow and a reader is scanning for one fact,
  // not parsing a document; `?format=json` has the object itself for anything
  // that is not a person.
  auditDetailCell(detail) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.auditDetailCell().");
    const keys = Object.keys(detail || {});
    if (!keys.length) {
      log.debug("Leaving AdminConsole.auditDetailCell().");
      return '<span class="state-none">—</span>';
    }
    log.debug("Leaving AdminConsole.auditDetailCell().");
    return keys.map(function (key) {
      return '<code>' + self.esc(key) + '=' + self.esc(detail[key]) + '</code>';
    }).join(' ');
  }

  // Who did it. The console key links to their page where this service has seen
  // them authenticate, following the same three-state rule the groups page uses
  // — a name it knows, a name it could file somebody under but never has, and
  // no name at all. The PRESENTED form is shown underneath when it differs,
  // because the collapse from `uid=alice,ou=users,dc=example,dc=com` to `alice`
  // is a thing an auditor has to be able to see rather than take on trust.
  auditActorCell(row, known) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.auditActorCell().");
    if (!row.actor && !row.actorForm) {
      log.debug("Leaving AdminConsole.auditActorCell().");
      return '<span class="state-none" title="Nothing here names an actor. ' +
        'An unauthenticated protocol call and an anonymous LDAP bind both ' +
        'look like this, and both are ordinary on a service that ' +
        'authenticates nobody.">—</span>';
    }
    const parts = [];
    if (row.actor) {
      parts.push(known[row.actor]
        ? '<a href="' +
          this.esc('/admin/users' + queryWith({ user: row.actor }, {})) +
          '">' + this.esc(row.actor) + '</a>'
        : '<span class="state-none" title="The console has no row for this ' +
          'name: nothing has authenticated as them in this process. A ' +
          'directory bind DN yields a name without there being anybody ' +
          'behind it.">' +
          this.esc(row.actor) + ' <em>(never here)</em></span>');
    }
    if (row.actorForm && row.actorForm !== row.actor) {
      parts.push('<code>' + this.esc(row.actorForm) + '</code>');
    }
    log.debug("Leaving AdminConsole.auditActorCell().");
    return parts.join('<br>');
  }

  // WHERE THE ACT CAME FROM (2026-09-18): the client's address, linked to
  // every row from it. A dash where nobody sent it — a timer, an expiry, a
  // background delivery, a seed at start-up — or where it came over a Unix
  // socket, which has no address; the tooltip says which that can be.
  auditAddressCell(row) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.auditAddressCell().");
    if (!row.address) {
      log.debug("Leaving AdminConsole.auditAddressCell(). None.");
      return '<span class="state-none" title="No client address: this was ' +
        'done by the service on its own (a timer, an expiry, a background ' +
        'delivery, start-up), arrived over a Unix socket, or was recorded ' +
        'before rows carried an address.">—</span>';
    }
    log.debug("Leaving AdminConsole.auditAddressCell().");
    return '<a href="/admin/audit?address=' +
           encodeURIComponent(row.address) + '"><code>' +
           this.esc(row.address) + '</code></a>';
  }

  auditRow(row, known) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.auditRow().");
    log.debug("Leaving AdminConsole.auditRow().");
    return '<tr>' +
      '<td class="num">' + this.esc(row.seq) + '</td>' +
      '<td>' + this.esc(this.whenText(row.at)) + '</td>' +
      '<td>' + this.esc(row.category) + '</td>' +
      '<td><code>' + this.esc(row.action) + '</code></td>' +
      '<td>' + this.outcomeCell(row.outcome) +
        (row.errorCode ? '<br><a href="/admin/audit?code=' +
                         encodeURIComponent(row.errorCode) + '"><code>' +
                         this.esc(row.errorCode) + '</code></a>' : '') +
                             '</td>' +
      '<td class="who">' + this.auditActorCell(row, known) + '</td>' +
      '<td class="who">' + this.auditAddressCell(row) + '</td>' +
      '<td class="who">' +
      (row.target ? '<code>' + this.esc(row.target) + '</code>'
                                       : '<span class="state-none">—</span>') +
        (row.channel ? '<br><span class="state-none">' + this.esc(row.channel) +
                       (row.protocol ? ' — ' + this.esc(row.protocol) : '') +
                       '</span>' : '') +
      '</td>' +
      '<td>' + this.esc(row.summary) + '</td>' +
      '<td class="who">' + this.auditDetailCell(row.detail) + '</td>' +
      '</tr>';
  }

  // ---------------------------------------------------------------------------
  // GET /admin/logout — WHAT ONE IDENTITY IS STILL SIGNED INTO, ANYWHERE, AND
  // THE CONTROLS THAT END IT.
  //
  // The operator's half of `/logout`. The two are one behaviour — both call
  // `logout.ts`'s `inventoryFor()` and `terminate()` and neither decides
  // anything the other does not — and they differ in exactly three ways, each
  // of which is why this page exists rather than a link to the other:
  //
  //   * IT NAMES SOMEBODY ELSE. `/logout` defaults to whoever is holding the
  //     cookie; this page always asks about a `user`, because an operator is
  //     looking AT a person rather than being one.
  //   * IT IS BEHIND THE CONSOLE'S TWO ROLES. Reading it needs Admin Read and
  //     the form needs Admin Write, through the one gate at the top of this
  //     file. `/logout` is behind neither, because signing yourself out must
  //     not require a role.
  //   * IT HAS AN UNDO, and `/logout` deliberately has not. A revoked token can
  //     be restored and a Kerberos sign-out instant can be cleared — NON-SPEC
  //     in both cases, and labelled so, for the reason /admin/tokens gives
  //     about its own restore button: no authorization server could offer it,
  //     and having to restart this service to get back to a working ticket
  //     turns a two-second test into a two-minute one.
  //
  // THE PAGE PAGES AND THE OTHER ONE DOES NOT, which is the standing convention
  // here rather than an inconsistency: a console list page gets paging and a
  // management API resource beside it in the same change. `/logout` groups by
  // family because a person reads it once; this filters and pages because an
  // operator looking at a load generator's identity may have five hundred rows.
  // ---------------------------------------------------------------------------



  logoutNoReaderNote() {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.logoutNoReaderNote().");
    log.debug("Leaving AdminConsole.logoutNoReaderNote().");
    return '<div class="err"><strong>The logout module is not loaded in this ' +
      'process.</strong> That is a require-order fault rather than a ' +
      'configuration one: <code>logout/logout.ts</code> fills this ' +
      'console\'s slot at its own require time, and <code>server.js</code> ' +
      'requires it second to last. Nothing else on this console is ' +
      'affected.</div>';
  }

  // One row of the flattened table. The family is a COLUMN here where /logout
  // makes it a heading, because this table is filtered and paged across
  // families and a heading that appeared and vanished with the filter would be
  // worse than a column that is always there. The opaque `back` field every
  // form on this page carries, so that ending one item does not cost the reader
  // their place in the list. Three other pages here build the same input as a
  // local `const`; this is a function because six forms on this one page need
  // it and a sixth hand-written copy is the one that would forget. It is
  // REBUILT by listViewFromBack() on the way in and never echoed — the
  // guarantee that keeps a hand-written `back` from reaching anything but
  // another page of this same list.
  logoutBackField(back) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.logoutBackField().");
    log.debug("Leaving AdminConsole.logoutBackField().");
    return '<input type="hidden" name="back" value="' + this.esc(back) + '">';
  }

  logoutRowHtml(row, canWrite, back) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.logoutRowHtml().");
    const button = row.terminable && canWrite
      ? '<form method="post" action="/admin/logout" style="display:inline">' +
        '<input type="hidden" name="action" value="end">' +
        '<input type="hidden" name="user" value="' + this.esc(row.user) + '">' +
        '<input type="hidden" name="select" value="' + this.esc(row.id) + '">' +
        this.logoutBackField(back) +
        '<button type="submit">End</button></form>'
      : (row.terminable ? '<span class="state-none">—</span>'
                        : '<span class="state-none" title="' +
                          this.esc(row.why) +
                          '">cannot</span>');
    log.debug("Leaving AdminConsole.logoutRowHtml().");
    return '<tr><td>' + this.esc(row.family) + '</td>' +
      // shortened() emits its OWN <code title=…> wrapper — the title is how the
      // full value stays recoverable — so it must NOT be escaped or wrapped
      // again: esc() around it prints the tags, which is what this cell did.
      '<td>' + this.shortened(row.label, 44) + '<br><span class="sub">' +
      this.esc(row.detail) + '</span>' +
      (row.terminable ? '' :
       '<br><span class="sub">' + this.esc(row.why) + '</span>') + '</td><td>' +
      this.esc(row.kind) + '</td><td ' +
      'class="sub">' + this.esc(this.whenText(row.startedAt)) + '</td>' +
      '<td class="sub">' + this.esc(this.whenText(row.expiresAt)) + '</td>' +
      '<td>' + button + '</td></tr>';
  }

  // ---------------------------------------------------------------------------
  // THE BACK-CHANNEL LOGOUT DELIVERIES AND THEIR DEAD LETTERS (2026-09-17,
  // #36; the cluster-wide, paged list and the retry the same day).
  //
  // A sign-out answers before its Logout Tokens are sent, so every result
  // says `pending`; this is where each one is seen to have been accepted or
  // not. On both halves of the page — the lookup and one person — because a
  // delivery is not only one person's: an operator asking "is the relying
  // party getting these?" has nobody in particular in mind. The rows are the
  // SHARED store's (`oauth-oidc/backchannel_logout.ts`, header point 3), so
  // this node lists every node's deliveries. Filtered by state and a search,
  // paged on `backchannelDeliveriesPage`; a DEAD row carries a Retry button
  // for a holder of Admin Write, which posts `retry-backchannel` — the same
  // action `POST /admin-api/logout/retry-backchannel` calls.
  // ---------------------------------------------------------------------------
  backchannelDeliveriesSection(view, canWrite?, back?, wantedUser?) {
    const { log, queryWith, pageParamsOf } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.backchannelDeliveriesSection().");
    const v = view || {};
    const list = Array.isArray(v.backchannelDeliveries)
      ? v.backchannelDeliveries : [];
    const counts = v.backchannelCounts || { pending: 0, sent: 0, dead: 0 };
    const state = String(v.deliveryState || '');
    const heading = '<h2 id="backchannel">Back-channel Logout Tokens</h2>' +
      this.note('<strong>' + counts.pending + '</strong> pending, <strong>' +
        counts.sent + '</strong> sent, <strong>' + counts.dead + '</strong> ' +
        'dead letter(s) in this realm, across every node. A relying party ' +
        'that is down is retried with backoff by whichever node gets there ' +
        'first, across restarts; one that never accepts — or answers 400, ' +
        'or is refused by the outbound policy — is a DEAD LETTER, sent again ' +
        'only when somebody presses Retry. Each final outcome is also a ' +
        '<code>logout.backchannel</code> row on <a href="/admin/audit">the ' +
        'audit log</a>.') +
      '<form method="get" action="/admin/logout#backchannel"><div ' +
      'class="formrow">' +
      (wantedUser ? '<input type="hidden" name="user" value="' +
                    this.esc(wantedUser) + '">' : '') +
      '<label for="deliveryState">State</label><select id="deliveryState" ' +
      'name="deliveryState"><option value="">any</option>' +
      ['pending', 'sent', 'dead'].map(function (one) {
        return '<option value="' + one + '"' +
               (one === state ? ' selected' : '') + '>' +
               (one === 'dead' ? 'dead letters' : one) + '</option>';
      }).join('') + '</select><label for="deliveryq">Search</label>' +
      '<input id="deliveryq" name="deliveryq" value="' +
      this.esc(v.deliveryq || '') + '" placeholder="client, session, code">' +
      '<button class="secondary">Filter</button></div></form>';
    if (!list.length) {
      log.debug("Leaving AdminConsole.backchannelDeliveriesSection(). None.");
      return heading + this.note(state || v.deliveryq
        ? 'No delivery matches.'
        : 'None yet. A sign-out — or an expiry, while ' +
          '<code>oauth2.backchannelLogoutOnExpiry</code> is on — sends one to ' +
          'every relying party on the ending session that registered a ' +
          '<code>backchannel_logout_uri</code>, while ' +
          '<code>oauth2.backchannelLogout</code> is on.');
    }
    const paging = (v.deliveriesPg && v.deliveriesPg.paging) || null;
    const params = Object.assign({}, pageParamsOf({}), wantedUser
      ? { user: wantedUser } : {}, { deliveryState: state,
                                     deliveryq: v.deliveryq || '' });
    const nav = paging
      ? this.pageNavPair('/admin/logout', params, paging) : { head: '' };
    log.debug("Leaving AdminConsole.backchannelDeliveriesSection(). " +
              list.length + " row(s).");
    return heading + nav.head +
      '<table><thead><tr><th>Queued</th><th>Client</th><th>Session</th>' +
      '<th>State</th><th>Why</th><th></th></tr></thead><tbody>' +
      list.map(function (row) {
        const retry = row.state === 'dead' && canWrite
          ? '<form method="post" action="/admin/logout" ' +
            'style="display:inline">' +
            '<input type="hidden" name="action" value="retry-backchannel">' +
            '<input type="hidden" name="delivery" value="' +
            self.esc(row.id) + '">' +
            '<input type="hidden" name="user" value="' +
            self.esc(wantedUser || '') + '">' +
            self.logoutBackField(back || queryWith(params, {})) +
            '<button type="submit">Retry</button></form>'
          : '';
        return '<tr><td class="sub">' + self.esc(row.queuedAt) + '</td>' +
          '<td><code>' + self.esc(row.clientId) + '</code><br><span ' +
          'class="sub">' + self.esc(row.uri) + '</span></td>' +
          '<td class="sub">' + self.esc(row.sessionId) +
          (row.trigger && row.trigger !== 'sign-out'
            ? '<br>' + self.esc(row.trigger) : '') + '</td>' +
          '<td>' + self.esc(row.state === 'dead' ? 'dead letter' : row.state) +
          (row.attempts ? '<br><span class="sub">' + row.attempts +
                          ' attempt(s)' +
                          (row.status ? ', HTTP ' + row.status : '') +
                          (row.generation > 1
                            ? ', retry ' + (row.generation - 1) : '') +
                          '</span>' : '') +
          (row.encrypted ? '<br><span class="sub">encrypted ' +
                           self.esc(row.encrypted) + '</span>' : '') +
          '</td>' +
          '<td class="sub">' + (row.errorCode
            ? '<code>' + self.esc(row.errorCode) + '</code> ' : '') +
          self.esc(row.why || row.via || '') + '</td><td>' + retry +
          '</td></tr>';
      }).join('') + '</tbody></table>';
  }

  // One route, two answers, and the choice is here rather than in the route so
  // that /admin-api/logout makes the same one — the rule every view in this
  // file follows.
  logoutView(req) {
    const { log, adminViews, gateStateFor, pageParamsOf, queryWith,
            logoutFamilies, krb5Principals } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.logoutView().");
    const view = adminViews.logoutJson(req);
    const wantedUser = String(req.query.user || '').trim();
    const gate = gateStateFor(req);
    const params = pageParamsOf(req.query);
    const back = queryWith(params, {});
    const families = logoutFamilies();

    if (!wantedUser) {
      // No name is not an error and not a 404: this page is a lookup, and the
      // list of everybody is /admin/users' job rather than a second copy here.
      const inner = this.messagesOf(req) +
        (logoutReader ? '' : this.logoutNoReaderNote()) +
        this.note('Name an identity to see everything this service is still ' +
        'holding for them — every browser sign-on session, every token it ' +
        'can still revoke, every outstanding code, every directory ' +
        'connection bound as them, and the Kerberos sign-out instant — and ' +
        'to end any of it.') +
        '<form method="get" action="/admin/logout"><label>Identity <input ' +
        'name="user" value="" placeholder="alice"></label> <button ' +
        'type="submit">Look</button></form><h2>What a logout ' +
        'reaches</h2><table><thead><tr><th>Family</th><th>Protocol</th><th>' +
        'Can ' +
        'it be ended?</th><th>What it is</th></tr></thead><tbody>' +
        families.map(function (family) {
          return '<tr><td>' + self.esc(family.label) + '</td><td>' +
            self.esc(family.protocol) + '</td><td>' +
            (family.terminable ? 'yes' : '<span ' +
                                                  'class="state-none">no' +
                                                  '</span>') + '</td>' +
            // The family's prose is a paragraph on most rows and it is the same
            // prose logout.ts owns (see FAMILIES over there) — so it folds here
            // rather than being shortened, which would have made this file the
            // second place it is written.
            '<td class="sub">' + self.note(self.esc(family.what)) + '<em>' +
              self.esc(
                family.spec) +
            '</em></td></tr>';
        }).join('') + '</tbody></table>' +
        this.note('The families that cannot be ended are listed on purpose. ' +
        'Nothing consults this service when a SAML assertion, a Kerberos ' +
        'service ticket or an X509-SVID is presented, so there is no ' +
        'revocation any issuer could perform — and a page that hid them ' +
        'would make a global logout look complete when it is not.') +
        this.note('A person signing THEMSELVES out uses ' +
        '<code>/logout</code>, which needs no console role and is where the ' +
        'front-channel notifications actually load: those are iframes in the ' +
        'signed-out person\'s own browser, and this console is not that ' +
        'browser. The back-channel Logout Tokens are different — this ' +
        'service sends them, whichever door the sign-out came through — and ' +
        'the list below is where each one ended up.') +
        this.backchannelDeliveriesSection(view, gate.write, back, '') +
        // ON THE LOOKUP PAGE AND NOT ON THE PER-PERSON ONE. These four decide
        // what a logout REACHES, which is a question about the feature; the
        // drill-down is about one person, and a form there would invite
        // somebody to change the rules for everybody while looking at one of
        // them.
        this.configFormsFor('/admin/logout');
      log.debug("Leaving AdminConsole.logoutView(). The lookup form.");
      return { json: Object.assign({}, view.json, {
                 settings: this.configSettingsJson('/admin/logout') }),
               inner: inner, title: 'Sign-out' };
    }

    const key = view.key;
    const inventory = view.inventory;
    const all = view.all;
    const wantedFamily = view.wantedFamily;
    const filtered = view.filtered;
    const pg = view.pg;
    const canWrite = view.canWrite;
    const summary = '<table><thead><tr><th>Family</th><th>Live</th><th>' +
                    'Endable</th><th>Protocol</th></tr></thead><tbody>' +
      inventory.families.map(function (family) {
        return '<tr><td><a href="' +
          self.esc('/admin/logout' +
                   queryWith(params, { user: wantedUser, family: family.id,
                                                         page: '' })) + '">' +
          self.esc(family.label) + '</a></td>' +
          '<td>' + family.held +
          (family.notListed ? ' (' + family.notListed + ' ' +
              'not listed)' : '') +
          '</td>' +
          '<td>' + (family.terminable ? 'yes' : '<span ' +
                                                'class="state-none">no' +
                                                '</span>') +
          '</td><td ' +
          'class="sub">' + self.esc(family.protocol) + '</td></tr>';
      }).join('') + '</tbody></table>';

    const inner = this.messagesOf(req) +
      this.note('<strong>' + inventory.total + '</strong> live item(s) for ' +
        '<code>' +
      this.esc(wantedUser) + '</code>, in ' +
      inventory.families.filter(function (f) { return f.held; }).length + ' ' +
      'family/families. Filed under the key ' +
      '<code>' + this.esc(key) + '</code>, which is what folds ' +
                                                     '<code>' +
      this.esc(wantedUser) + '</code>, <code>' + this.esc(wantedUser) + '@' +
      this.esc(krb5Principals.REALM) + '</code> and a <code>urn:</code> ' +
                                       'subject into one person.') +
      summary +
      (canWrite
        ? '<form method="post" action="/admin/logout">' +
          '<input type="hidden" name="action" value="global">' +
          '<input type="hidden" name="user" value="' + this.esc(wantedUser) +
          '">' +
          this.logoutBackField(back) +
          '<p><button type="submit">Global logout — end everything ' +
          'above</button> <span class="sub">Everything endable, in every ' +
          'family, in one act. What cannot be ended is reported rather than ' +
          'skipped silently.</span></p></form>'
        : this.note('Ending anything needs the Admin Write role.')) +
      '<h2>Live items' + (wantedFamily ? ' — ' + this.esc(wantedFamily) : '') +
      '</h2>' +
      this.perPageForm('/admin/logout', 'family', wantedFamily,
                       pg.paging.perPage,
                       'Filter by family, and choose how many rows a page ' +
                       'holds.',
                       { user: wantedUser }) +
      (pg.shown.length
        ? '<table><thead><tr><th>Family</th><th>What</th><th>Kind</th><th>' +
          'Since</th><th>Until</th><th>End</th></tr></thead><tbody>' +
          pg.shown.map(function (r) { return self.logoutRowHtml(r, canWrite,
              back); })
                  .join('') +
          '</tbody></table>' +
          this.pageNavPair('/admin/logout', params, pg.paging).head
        : this.note('Nothing live' + (wantedFamily ? ' in that family' : '') +
                    '.')) +
      (canWrite
        ? '<h2>Undo — both NON-SPEC</h2>' +
          this.note('Neither of these is an operation any real deployment ' +
          'could offer, and they are here for the reason /admin/tokens\' ' +
          'restore button is: having to restart this service to get back to ' +
          'a working credential turns a two-second test into a two-minute ' +
          'one.') +
          '<form method="post" action="/admin/logout">' +
          '<input type="hidden" name="action" value="restore-kerberos">' +
          '<input type="hidden" name="user" value="' + this.esc(wantedUser) +
          '">' +
          this.logoutBackField(back) +
          '<p><button type="submit">Clear the Kerberos sign-out ' +
          'instant</button> <span class="sub">Tickets issued before it are ' +
          'accepted again. A fresh AS-REQ does this too, and is the ' +
          'supported way back.</span></p></form><form method="post" ' +
          'action="/admin/logout"><input type="hidden" name="action" ' +
          'value="restore-token"><input type="hidden" name="user" ' +
          'value="' + this.esc(wantedUser) + '">' +
          this.logoutBackField(back) +
          '<p><label>Restore a token by jti <input name="jti" ' +
          'placeholder="jti"></label> <button type="submit">Restore</button> ' +
          '<span class="sub">RFC 7009 has no such operation: a resource ' +
          'server may already have cached the refusal.</span></p></form>'
        : '') +
      this.backchannelDeliveriesSection(view, canWrite, back, wantedUser);

    log.debug("Leaving AdminConsole.logoutView(). " + inventory.total +
              " live item(s).");
    return {
      json: view.json,
      inner: inner,
      title: 'Sign-out — ' + wantedUser,
      up: this.upTo('/admin/logout', wantedUser,
                    this.listViewOf('/admin/logout', req.query))
    };
  }

  // ---------------------------------------------------------------------------
  // GET /admin/delegation — WHO ACTED ON WHOSE BEHALF, THROUGH WHAT, TO REACH
  // WHAT.
  //
  // TWO TABLES, and the split between them is the point of the page rather than
  // a layout choice:
  //
  //   * WHAT HAPPENED — one row per delegation ACT, from common/delegation.js.
  //     Every mechanism in three protocol families, in one vocabulary, refusals
  //     included.
  //   * WHO MAY DELEGATE TO WHOM — the CONFIGURED policy, from
  //     krb5_principals.js. It answers *why would this be refused* before
  //     anybody has tried, and it is Kerberos-only because Kerberos is the only
  //     family here that polices delegation at all. That absence is stated on
  //     the page rather than left to be inferred from an empty column.
  //
  // **IT IS DELIBERATELY NOT A PROTOCOL PAGE**, which is why it is in
  // Monitoring beside the tokens it points at and not under Protocols beside
  // SAML and SCIM. A reader arriving here has a chain in their head — *alice
  // hit the portal, the portal called the API* — and wants to know which hop
  // invented which identity. Filing that under one of the three families would
  // mean choosing which two thirds of the answer to hide.
  //
  // **NO FORM, AND THAT IS A DECISION.** Everything on this page is an
  // observation: an act happened or it did not, and a policy row is somewhere
  // else's configuration. There is nothing here to change, so rule 7 is
  // satisfied by `GET /admin-api/delegation` alone — the same shape
  // /admin/audit has, and for a related reason. A control that let somebody
  // TYPE a chain would put invented rows in a table whose whole worth is that
  // its rows are what actually happened.
  // ---------------------------------------------------------------------------

  // One party of a chain — up to two links, because a party can be a person AND
  // an application and routinely is.
  //
  // `HTTP/frontend.example.com` has an entry under ou=users (it authenticates,
  // so the funnel files it with the people) and an entry under ou=applications
  // (a ticket was issued FOR it, so applications.js recorded it). A cell that
  // showed one of them would send half the readers to the wrong page.
  //
  // Both links follow the three-state rule /admin/groups uses for a member: a
  // name this console knows, a name it could file somebody under and never has,
  // and no name at all. An application NOT in the registry is the ordinary case
  // for an RFC 8693 `audience` and is worth seeing rather than hiding — the
  // registry holds what this service was ASKED ABOUT, and a delegation naming
  // something nobody has otherwise mentioned is exactly the row to notice.
  delegationPartyCell(party, known) {
    const { log, applications, queryWith } = this.deps;
    log.debug("Entering AdminConsole.delegationPartyCell().");
    const parts = [];
    if (party.key) {
      parts.push(this.usersPageCell(party.key, known));
      if (party.presented && party.presented !== party.key) {
        parts.push('<code>' + this.esc(party.presented) + '</code>');
      }
    } else if (party.presented) {
      parts.push('<code>' + this.esc(party.presented) + '</code>');
    }
    if (party.application) {
      const registered = !!applications.get(party.application);
      parts.push(registered
        ? '<a href="' + this.esc('/admin/applications' +
            queryWith({ application: party.application }, {})) + '" ' +
          'title="This application is in the registry (ou=applications).">' +
          this.esc(party.application) + '</a>'
        : '<span class="state-none" title="No entry under ou=applications ' +
          'names this. The registry holds what this service has been ASKED ' +
          'ABOUT — a client_id presented, an AppliesTo a token was issued ' +
          'for, an SPN a ticket was cut for — and this delegation named ' +
          'something nobody has otherwise mentioned. That is ordinary for an ' +
          'RFC 8693 audience and is worth seeing rather than ' +
          'hiding.">' + this.esc(party.application) +
          ' <em>(not in the registry)</em></span>');
    }
    if (!parts.length) {
      log.debug("Leaving AdminConsole.delegationPartyCell().");
      return '<span class="state-none" title="Nothing here names this party, ' +
        'and on some mechanisms nothing can: a forwarded ticket-granting ' +
        'ticket is handed to whichever service the client chooses, and this ' +
        'KDC is never told which.">&mdash;</span>';
    }
    log.debug("Leaving AdminConsole.delegationPartyCell().");
    return parts.join('<br>');
  }

  // What a delegation CONSUMED or PRODUCED, as one cell. `key=value` pairs
  // rather than JSON for the reason the audit log's detail cell gives — the
  // column is narrow and a reader is scanning for one fact — and `?format=json`
  // carries the objects for anything that is not a person.
  //
  //
  // Named for the delegation table since #70: the old file declared a second
  // `credentialCell()` — the person's way in, under the users list — and
  // JavaScript's hoisting gave that later one to both of this table's calls,
  // so the cell drew "unknown" for every delegation instead of what it
  // consumed and produced. The TypeScript conversion (#50) kept that
  // behaviour and surfaced it; the calls now reach this method.
  delegationCredentialCell(list, label) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.delegationCredentialCell().");
    if (!list || !list.length) {
      log.debug("Leaving AdminConsole.delegationCredentialCell().");
      // NOTHING rather than a dash, because the two directions share one cell
      // now: a dash under the arrow for a direction that genuinely has no
      // credential reads as a value that failed to load, where an absent line
      // reads as what it is. The empty cell — no credential either way — is the
      // one case that still needs a mark, and it gets one from the caller
      // having drawn neither.
      return '';
    }
    log.debug("Leaving AdminConsole.delegationCredentialCell().");
    // A DIV rather than a run of spans, because the cell holds BOTH directions
    // now and everything in them is inline: without a block the "out" label
    // continued the last note of the "in" list on the same line, which read as
    // one sentence made of two.
    return '<div><span class="state-none">' + label + '</span><br>' +
      list.map(function (one) {
        return '<code>' + self.esc(one.kind) + '</code>' +
          // NOT esc()'d and not wrapped: shortened() returns the <code> element
          // with the whole value in its title. Escaping it printed the markup.
          (one.identifier ? ' ' + self.shortened(one.identifier, 10) : '') +
          (one.note ?
           '<br><span class="state-none">' + self.esc(one.note) +
           '</span>' : '');
      }).join('<br>') + '</div>';
  }

  // Impersonation is drawn as the LOUDER of the two, which is a judgement worth
  // stating rather than leaving in a colour. It is not "worse" — both are
  // ordinary and both are configured on purpose — but it is the one whose
  // consequence is invisible everywhere else: nothing in the credential records
  // that a middle tier was involved, so this table is the only place it will
  // ever be seen. A delegation carries its own chain and can be read off the
  // token later.
  modeCell(mode) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.modeCell().");
    if (mode === 'impersonation') {
      log.debug("Leaving AdminConsole.modeCell().");
      return '<span class="state-expired" title="What came out names the ' +
        'initial identity and nothing else. The far end cannot tell an ' +
        'intermediary was involved, and neither can anybody reading the ' +
        'credential afterwards — which is why the issuer is the only place ' +
        'this is ever visible.">impersonation</span>';
    }
    if (mode === 'delegation') {
      log.debug("Leaving AdminConsole.modeCell().");
      return '<span class="state-valid" title="What came out CARRIES the ' +
        'chain: an `act` claim, a composite ActAs, or S4U_DELEGATION_INFO in ' +
        'the PAC. The far end can see who is really asking.">delegation</span>';
    }
    log.debug("Leaving AdminConsole.modeCell().");
    return '<span class="state-none">&mdash;</span>';
  }

  delegationOutcomeCell(row) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.delegationOutcomeCell().");
    if (row.outcome === 'issued') {
      log.debug("Leaving AdminConsole.delegationOutcomeCell().");
      return '<span class="state-valid">issued</span>';
    }
    log.debug("Leaving AdminConsole.delegationOutcomeCell().");
    return '<span class="state-revoked" title="This service refused the ' +
      'delegation. The reason is the KDC\'s own words — the same text the ' +
      'client was sent — rather than a second wording written for this ' +
      'page.">refused</span>';
  }

  // TEN COLUMNS RATHER THAN TWELVE, and the two that were merged were merged
  // because the table became unreadable rather than merely wide. `.who` breaks
  // a long identifier anywhere (or one DN would widen the whole page), so every
  // extra column costs the ones beside it:
  // `HTTP/frontend.example.com@EXAMPLE.COM` wrapped over five lines at twelve
  // and reads at ten.
  //
  // The protocol went into the mechanism cell because the mechanism id already
  // carries it — every one of them begins `krb5-`, `wstrust-` or `oauth-` — so
  // the column was saying a second time what the cell beside it said first. The
  // two credential columns became one because a row usually has one of each and
  // the arrows say which: what went IN, what came OUT. `options.listView` is
  // what the reader had the table filtered to, carried into the drill-down so
  // that its way back is the page they left rather than the top of an
  // unfiltered list — the rule listViewOf() states. `options.chainLink` is
  // false on the chain page itself, where every row belongs to the chain being
  // drawn and the link would point at the page it is on.
  delegationRow(row, known, options) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.delegationRow().");
    const opts = options || {};
    const chainLink = opts.chainLink === undefined ? true : !!opts.chainLink;
    log.debug("Leaving AdminConsole.delegationRow().");
    return '<tr>' +
      // THE WAY TO THE PICTURE OF THIS ONE RELATIONSHIP, in the narrowest
      // column on the page rather than in an eleventh of its own. The header
      // above says why this table is ten columns and not twelve: `.who` breaks
      // a long identifier anywhere, so every column costs the ones beside it.
      // `#` is the one cell whose content is four digits and can carry a second
      // line for nothing.
      //
      // It goes to the CHAIN and not to the act, which the link says out loud —
      // an act has no picture of its own, and a reader who clicked expecting
      // one would read the times off a diagram that has them taken out.
      '<td class="num">' + this.esc(row.seq) +
        (chainLink
          ? '<br><a href="' + this.esc('/admin/delegation/chain' +
              queryWith(opts.listView || {}, { chain: row.chainKey })) +
            '" title="Draw THIS relationship on its own — the whole chain ' +
            'this act belongs to, with everything else in the service left ' +
            'out.">chain</a>'
          : '') + '</td>' +
      '<td>' + this.esc(this.whenText(row.at)) + '</td>' +
      '<td><code>' + this.esc(row.type) + '</code><br>' +
        '<span class="state-none">' + this.esc(row.typeLabel) + '</span><br>' +
        '<span class="state-none">' + this.esc(row.protocol) +
        (row.spec ? ' &middot; ' + this.esc(row.spec) : '') + '</span></td>' +
      '<td>' + this.modeCell(row.mode) + '</td>' +
      '<td>' + this.delegationOutcomeCell(row) + '</td>' +
      '<td class="who">' + this.delegationPartyCell(row.initial, known) +
      '</td>' +
      '<td class="who">' + this.delegationPartyCell(row.intermediary, known) +
      '</td><td ' +
      'class="who">' + this.delegationPartyCell(row.target, known) + '</td>' +
      '<td>' + (row.outcome === 'refused'
                ? this.esc(row.reason)
                : (row.authorizedBy ? this.esc(row.authorizedBy)
                                    : '<span ' +
                                      'class="state-none">&mdash;</span>')) +
        (row.note ? '<br><span class="state-none">' + this.esc(row.note) +
         '</span>' :
         '') +
      '</td>' +
      '<td class="who">' +
        this.delegationCredentialCell(row.consumed, '&rarr; in') +
        this.delegationCredentialCell(row.produced, '&larr; out') +
        '</td>' +
      '</tr>';
  }

  // One configured pair. `setOn` is the column to read first and is why the two
  // mechanisms are in ONE table rather than two: the messages are identical,
  // the KDC options are identical, and the whole difference is which of the two
  // accounts carries the permission. Two tables would have let a reader learn
  // one of them without ever meeting that fact. FIVE COLUMNS, and `requires` is
  // not one of them although the JSON carries it per pair. It is a property of
  // the MECHANISM rather than of the pair — every classic row has the same
  // sentence and every resource-based row has the other — so as a column it was
  // the same two paragraphs repeated down the table, squeezing the three
  // columns that DO differ per row into unreadable shreds. It is said once
  // above the table instead. The API keeps it on every pair, because a caller
  // reading one pair should not have to know that.
  policyPairRow(pair) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.policyPairRow().");
    log.debug("Leaving AdminConsole.policyPairRow().");
    return '<tr>' +
      '<td><code>' + this.esc(pair.mechanism) +
      '</code><br><span class="state-none">' +
        this.esc(pair.type) + '</span></td>' +
      '<td class="who"><code>' + this.esc(pair.frontEnd) + '</code></td>' +
      '<td class="who"><code>' + this.esc(pair.target) + '</code>' +
        (pair.targetKnown ? ''
          : '<br><span class="state-revoked">no such principal here</span>') +
      '</td>' +
      '<td class="who"><code>' + this.esc(pair.attribute) + '</code><br>' +
        '<span class="state-none">on the ' + this.esc(pair.setOnRole) + ', ' +
          '<code>' +
        this.esc(pair.setOn) + '</code></span></td>' +
      '<td>' + (pair.warning
                ? '<span class="state-expired">' + this.esc(pair.warning) +
                  '</span>'
                : '<span class="state-valid">nothing else is missing</span>') +
      '</td>' +
      '</tr>';
  }

  policyAccountRow(account) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.policyAccountRow().");
    const flags = [];
    if (account.notDelegated) flags.push('NOT_DELEGATED');
    if (account.trustedToAuthenticateForDelegation) {
      flags.push('TRUSTED_TO_AUTHENTICATE_FOR_DELEGATION');
    }
    if (account.okAsDelegate) flags.push('ok-as-delegate');
    log.debug("Leaving AdminConsole.policyAccountRow().");
    return '<tr>' +
      '<td class="who"><code>' + this.esc(account.principal) + '</code></td>' +
      '<td>' + flags.map(function (f) {
        return '<code>' + self.esc(f) + '</code>';
      }).join('<br>') + '</td>' +
      '<td>' + account.effects.map(function (e) {
        return self.esc(e);
      }).join('<br><br>') + '</td>' +
      '</tr>';
  }

  // ---------------------------------------------------------------------------
  // THE CONFIGURED HALF OF /admin/delegation: DELEGATED PERMISSIONS.
  //
  // `common/app_permissions.ts` holds the model and this holds the HTML, the
  // same split every other register on this console has. Five actions, and they
  // are the reason this page HAS a form at all — which reverses a decision
  // stated at length in the route header above, so the reversal is argued here
  // rather than left as an inconsistency somebody re-derives.
  //
  // **THAT HEADER SAID "NO FORM, AND THAT IS A DECISION", AND IT WAS RIGHT
  // ABOUT WHAT IT WAS TALKING ABOUT.** Everything on this page WAS an
  // observation: an act happened or it did not, and a control that let somebody
  // TYPE a chain would put invented rows in a table whose whole worth is that
  // its rows are what actually happened. That sentence is untouched and still
  // governs the acts table, the chains and the picture — none of them has a
  // control and none ever will.
  //
  // What is new is a SECOND REGISTER on the same page, and it is configuration
  // rather than observation: a delegated permission is something somebody
  // DECIDES, like a redirect URI or a federation relationship, and
  // configuration with no way to type it is configuration only an `ldapmodify`
  // can reach. The rule the old header states is therefore intact and sharpened
  // — **nothing that records what HAPPENED has a control, and the thing that
  // records what is ALLOWED is nothing but controls.** The two are drawn under
  // headings that say which is which, because a reader who confused them would
  // draw exactly the wrong conclusion from the difference between them, which
  // is the most useful thing on the page.
  //
  // The five actions are thin: each calls `app_permissions.js`, which calls
  // `applications.updateApplication()`, which is where the RULES are — so the
  // form, `POST /admin-api/permissions/...` and the generic attribute editor on
  // /admin/applications all go through one implementation of "a permission must
  // be defined before it can be granted". See that module's header.
  //
  // **ONE OF THE FIVE FORMS IS NOT DRAWN ON THIS PAGE ANY MORE (2026-09-01).**
  // `grant-permission` is on the CLIENT application's own page — see
  // applicationPermissionsSection() — because a grant is a value on the
  // client's entry, and there the client is the entry the reader is standing on
  // rather than one option in a select of every application here. What this
  // page keeps is the RESOURCE half, which is what it is about: expose an API,
  // define a permission, remove one.
  //
  // `revoke-permission` IS DRAWN IN BOTH PLACES and that is not a leftover. It
  // is a ROW BUTTON, so its two halves are the row it sits on and neither can
  // be got wrong — which is precisely what was wrong with the grant form's two
  // selects. The register here lists every grant in the service and a reader
  // tidying it should not have to open five application pages; the
  // application's own list is the read-back of the grant they just made, and a
  // table with no way to undo the write above it is half a control.
  //
  // None of it touches anything else in this block: every form posts to the
  // same handler, PERMISSION_ACTIONS is still all five, and the paragraph above
  // about the acts half having no control and the configured half being nothing
  // but controls is unchanged. What moved is where one control is DRAWN.
  // ---------------------------------------------------------------------------




  // One row of the permissions table: what a resource EXPOSES.
  // `options.readOnly` swaps the Remove form for a link to the page that has
  // it, for the reason permissionGrantRow() carries the same parameter: the
  // picture pages list what they draw and change nothing, and one row function
  // is what stops two tables coming to disagree about what a permission
  // identifier is.
  permissionDefinitionRow(one, listView, options?) {
    const { log, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.permissionDefinitionRow().");
    const href = '/admin/applications' +
                 queryWith(listView || {}, { application: one.resource });
    log.debug("Leaving AdminConsole.permissionDefinitionRow().");
    return '<tr>' +
      '<td class="who"><a href="' + this.esc(href) + '">' +
      this.esc(one.resourceName) +
      '</a>' +
        (one.resourceName === one.resource ? ''
          : '<br><code>' + this.esc(one.resource) + '</code>') + '</td>' +
      '<td><code>' + this.esc(one.name) + '</code>' +
        (one.description ?
         '<br><span class="state-none">' + this.esc(one.description) + '</span>'
                         : '') + '</td>' +
      // THE IDENTIFIER IS THE COLUMN SOMEBODY COPIES, so it is `<code>` and it
      // is whole rather than shortened. A permission whose entry has no base
      // URI has none at all, and that is said rather than left as an empty cell
      // — it is the one state on this table that means the row cannot work.
      '<td>' + (one.id
        ? '<code>' + this.esc(one.id) + '</code>'
        : '<span class="state-revoked" title="This permission has no ' +
          'identifier because its application has no oauthPermissionBaseUri. ' +
          'A permission is named by its base URI followed by its name, so no ' +
          'client can ever ask for this one. Set the base on the application ' +
          'and it resolves.">no identifier &mdash; the application has no ' +
          'base URI</span>') + '</td>' +
      '<td class="num">' + (one.grantedTo.length
        ? '<span class="state-valid">' + one.grantedTo.length + '</span>'
        : '<span class="state-none" title="Nothing holds this permission. ' +
          'That is the ordinary state of a permission that has just been ' +
          'defined — defining one grants it to nobody.">0</span>') + '</td>' +
      '<td class="who">' + (one.grantedTo.length
        ? one.grantedTo.map(function (who) {
            return self.esc(who.name) + (who.asked ? '' :
              ' <span class="state-none" title="Granted and never asked ' +
              'for.">(unused)</span>');
          }).join('<br>')
        : '<span class="state-none">&mdash;</span>') + '</td>' +
      // THE TWO BRANCHES DREW THE SAME FORM, and they did before this row
      // carried a `back` as well — the `one.id` test above decides the
      // IDENTIFIER cell, not this one, and a permission with no identifier is
      // removed by exactly the same call. One form, once.
      '<td>' + (options && options.readOnly
        ? '<a href="/admin/delegation#allowed" title="This page draws the ' +
          'register and does not change it. The Remove button for this ' +
          'permission is on the register itself.">on the register</a>'
        : '<form method="post" action="/admin/delegation">' +
          this.permissionsBack(listView) + '<div class="formrow">' +
          '<input type="hidden" name="action" value="remove-permission">' +
          '<input type="hidden" name="resource" value="' +
          this.esc(one.resource) +
          '"><input ' +
          'type="hidden" name="name" value="' + this.esc(one.name) + '">' +
          '<button type="submit" class="danger">Remove</button>' +
          '</div></form>') + '</td>' +
      '</tr>';
  }

  // THE LIST STATE, AS ONE FIELD, FOR THE ROW BUTTONS OF THE CONFIGURED
  // REGISTER.
  //
  // It cost nothing while /admin/delegation drew every row it had: a Revoke
  // answered with a redirect to the top of the register and the row that had
  // gone was on the screen anyway. With seven paged tables and two searches on
  // that page it is the difference between a Revoke that answers where you were
  // standing and one that throws you back to page 1 of an unfiltered list,
  // three screens up, with the search you were reading by cleared.
  //
  // One opaque field rather than the parameters loose in the body, for the
  // reason /admin/applications' `carryBack` gives: an action reads its own body
  // BY NAME, and a `q` or a `permissionsPage` loose in there is a field some
  // action added later could pick up by accident. permissionsReturnTo()
  // rebuilds a query from it through listViewOf()'s whitelist rather than
  // echoing it, which is what keeps a hand-written `back` from becoming a
  // redirect somewhere this file did not write.
  permissionsBack(listView) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.permissionsBack().");
    log.debug("Leaving AdminConsole.permissionsBack().");
    return '<input type="hidden" name="back" value="' +
           this.esc(queryWith(listView || {}, {})) + '">';
  }

  // One row of the grants table: the RELATIONSHIP itself. `options.readOnly`
  // swaps the last cell for a link to the page that DOES have the form on it,
  // and is what lets the group picture at /admin/delegation/cluster list the
  // grants it is drawing without becoming a fourth door onto the register
  // (2026-09-02). It is a parameter rather than a second row function for the
  // reason chooserPane() is one function drawn eight times: the six cells
  // before it are the whole of what a grant IS, and two copies of them is two
  // tables that come to disagree about what the access token will say.
  //
  // The picture pages are read-only ON PURPOSE — /admin/delegation/allowed says
  // so in as many words — so a Revoke drawn there would be the one control on a
  // page whose text says it has none, and it would answer by throwing the
  // reader back to a table three screens up on a different page, because
  // permissionsReturnTo() has nowhere else to send it.
  permissionGrantRow(one, listView, options?) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.permissionGrantRow().");
    const clientHref = '/admin/applications' +
      queryWith(listView || {}, { application: one.client });
    const resourceHref = one.resource
      ? '/admin/applications' +
        queryWith(listView || {}, { application: one.resource }) : '';
    log.debug("Leaving AdminConsole.permissionGrantRow().");
    return '<tr>' +
      '<td class="who"><a href="' + this.esc(clientHref) + '">' +
      this.esc(one.clientName) +
      '</a>' +
        (one.clientName === one.client ? ''
          : '<br><code>' + this.esc(one.client) + '</code>') + '</td>' +
      '<td class="who">' + (one.resource
        ? '<a href="' + this.esc(resourceHref) + '">' +
          this.esc(one.resourceName) + '</a>'
        // A DANGLING GRANT NAMES NO RESOURCE, and the cell says why rather than
        // being empty. Both ways it can happen are named, because they are
        // different problems: one is a deleted application and the other is a
        // permission removed from under a grant that was made correctly.
        : '<span class="state-revoked" title="No application in this ' +
          'registry defines this permission. Either the resource\'s entry ' +
          'was deleted, or the permission was removed from it while this ' +
          'grant still named it, or an ldapmodify wrote a grant that never ' +
          'resolved — both console doors refuse to create ' +
          'one.">dangling</span>') + '</td>' +
      '<td>' + (one.permissionName
        ? '<code>' + this.esc(one.permissionName) + '</code>' +
          (one.description ?
           '<br><span class="state-none">' + this.esc(one.description) +
                             '</span>' : '')
        : '<span class="state-none">&mdash;</span>') + '</td>' +
      '<td><code>' + this.esc(one.permissionId) + '</code></td>' +
      // WHAT THE TOKEN WILL SAY, spelled out per row. It is the whole point of
      // the feature and it is two facts a reader would otherwise have to
      // compose from two other columns — which is exactly the arithmetic a
      // table should do for somebody.
      '<td>' + (one.baseUri
        ? '<code>aud: ' + this.esc(one.baseUri) + '</code><br>' +
          '<code>scope: ' + this.esc(one.permissionName) + '</code>'
        : '<span class="state-none">nothing &mdash; the permission does not ' +
          'resolve, so this scope is treated as an ordinary one</span>') +
      '</td><td>' + (one.asked
        ? '<span class="state-valid" title="This client\'s entry records ' +
          'having asked for this scope. It is evidence rather than proof: ' +
          'oauthScope records what was requested, not what was ' +
          'issued.">asked for</span>'
        : '<span class="state-none" title="This client has never asked for ' +
          'it. A configured grant nobody has needed is exactly what this ' +
          'register is here to show.">never asked for</span>') + '</td>' +
      '<td>' + (options && options.readOnly
        ? '<a href="/admin/delegation#allowed" title="This page draws the ' +
          'register and does not change it. The Revoke button for this grant ' +
          'is on the register itself.">on the register</a>'
        : '<form method="post" action="/admin/delegation">' +
          this.permissionsBack(listView) + '<div class="formrow">' +
          '<input type="hidden" name="action" value="revoke-permission">' +
          '<input type="hidden" name="client" value="' + this.esc(one.client) +
          '"><input type="hidden" name="permission" value="' +
          this.esc(one.permissionId) + '"><button ' +
          'type="submit" class="danger">Revoke</button></div></form>') +
          '</td>' +
      '</tr>';
  }

  // WHICH ROWS OF THE CONFIGURED REGISTER THE BROWSER IS BEING SHOWN — the two
  // searches and the two pagings, worked out in ONE PURE FUNCTION.
  //
  // It is a function rather than four lines inside permissionsSection() because
  // the page's `?format=json` has to report the same answer, and there is no
  // way to read it back out of a string of markup. Two hand-written copies of
  // "filter on the client and the resource, then slice" is the disagreement
  // this file keeps warning about, and it would be invisible: the table and the
  // reply would each look right alone.
  //
  // It takes the QUERY and the register rather than the request, so it can be
  // called twice with the same arguments and cannot answer differently.
  permissionsListState(query, register) {
    const { log, queryOne, chooserMatches, pagedRows,
            DELEGATION_PER_PAGE } = this.deps;
    log.debug("Entering AdminConsole.permissionsListState().");
    const permWanted = queryOne(query, 'permq').trim();
    const grantWanted = queryOne(query, 'grantq').trim();
    const permissionsMatched = register.permissions.filter(function (one) {
      return chooserMatches([one.resourceName, one.resource], permWanted);
    });
    // BOTH ENDS OF THE RELATIONSHIP. A dangling grant carries no resource, so
    // it matches on its client alone — which is the only application named on
    // that row and therefore the only one it could be found by.
    const grantsMatched = register.grants.filter(function (one) {
      return chooserMatches([one.clientName, one.client,
                             one.resourceName, one.resource], grantWanted);
    });
    log.debug("Leaving AdminConsole.permissionsListState(). " +
              permissionsMatched.length +
              " permission(s) and " + grantsMatched.length +
              " grant(s) matched.");
    return {
      permWanted: permWanted, grantWanted: grantWanted,
      permPage: pagedRows(query, permissionsMatched,
        { name: 'permissions', noun: 'permissions',
          defaultPer: DELEGATION_PER_PAGE }),
      grantPage: pagedRows(query, grantsMatched,
        { name: 'grants', noun: 'grants', defaultPer: DELEGATION_PER_PAGE })
    };
  }

  // The whole configured section, as it appears on /admin/delegation. Extracted
  // into a function of its own because that page's `inner` is already the
  // longest expression in this console and a fourth screen of string
  // concatenation inside it would be unreadable — not because anything else
  // draws it.
  permissionsSection(req, view, listView) {
    const { log, applications, pageParamsOf } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.permissionsSection().");
    const register = view.register;
    const counts = register.counts;
    // Every application in the registry, for the two selects. A select rather
    // than a text box because BOTH sides of a grant must already be entries —
    // there is nothing to type that is not on this list, and a typed identifier
    // that did not match would be refused with a sentence the reader could have
    // been spared.
    const all = applications.list();
    const applicationOptions = all.map(function (row) {
      return '<option value="' + self.esc(row.identifier) + '">' +
             self.esc(row.name && row.name !== row.identifier
               ? row.name + ' — ' + row.identifier : row.identifier) +
             '</option>';
    }).join('');
    // Only the permissions that HAVE an identifier can be granted, so only
    // those are offered. One with no base URI is on the table above with the
    // reason.
    const grantable = register.permissions.filter(function (
        one) { return !!one.id; });

    // -------------------------------------------------------------------------
    // THE TWO SEARCHES AND THE TWO PAGINGS (2026-09-01).
    //
    // Both tables here are unbounded — one row per permission defined and one
    // row per (client, permission) — and a service driven for an afternoon puts
    // tens of each on a page that already carries five other tables. Ten rows a
    // page is what makes the page navigable at all; DELEGATION_PER_PAGE argues
    // the number.
    //
    // THE SEARCH IS OVER THE APPLICATION AND NOT OVER THE WHOLE ROW, and the
    // two tables answer it differently because a permission has ONE application
    // in it and a grant has TWO. `permq` searches the application that EXPOSES
    // the permission; `grantq` searches BOTH ends of the relationship, for the
    // reason the acts table's own box gives — the fact a reader arrives with
    // names one of the two and they do not know which column it will be in.
    // Both match on the display name AND on the identifier, because half the
    // names in this registry are the identifier and the other half are not, and
    // a reader pastes whichever one the page showed them last.
    //
    // Filter first, then page — pagingOf()'s rule, and for its reason: paging a
    // list and then filtering it gives a page 2 whose length depends on what
    // page 1 happened to hold.
    // -------------------------------------------------------------------------
    const state = this.permissionsListState(req.query, register);
    const permWanted = state.permWanted;
    const grantWanted = state.grantWanted;
    const permPage = state.permPage;
    const grantPage = state.grantPage;
    // EVERY parameter the reader is already carrying, so that paging one table
    // moves nothing else on the page — the six other lists, both chooser
    // searches and the other table's own search all ride along. pageNavPair()
    // overrides only the one name off the paging object it is handed.
    const navParams = pageParamsOf(req.query);
    const permNav = this.pageNavPair('/admin/delegation', navParams,
                                     permPage.paging);
    const grantNav = this.pageNavPair('/admin/delegation', navParams,
                                      grantPage.paging);

    log.debug("Leaving AdminConsole.permissionsSection(). " + counts.grants +
              " grant(s), " +
              permPage.shown.length + " permission row(s) and " +
              grantPage.shown.length + " grant row(s) drawn.");
    return '<h2 id="allowed">What is ALLOWED, decided in advance</h2>' +

      this.note('<strong>Everything above this heading is EVIDENCE and ' +
      'everything below it is INTENT, and the difference is the most useful ' +
      'thing on this page.</strong> The acts, the chains and the picture ' +
      'they are drawn from are things that happened — a credential was ' +
      'issued or refused, at a moment, to somebody. What follows is ' +
      'CONFIGURATION: which client applications may reach which resource ' +
      'applications, typed in before anybody asked for anything. Read one ' +
      'against the other and two questions answer themselves: <em>which ' +
      'grants has nobody ever used</em>, and <em>what has been delegated ' +
      'that nobody granted</em>.') +

      this.note('<strong>A RESOURCE application exposes an API and a CLIENT ' +
      'application is granted permissions on it.</strong> The resource is ' +
      'given a base URI &mdash; anything absolute works here &mdash; and a ' +
      'list of permissions. A permission is identified by the two joined ' +
      'together &mdash; base <code>https://example.com/</code> and name ' +
      '<code>write</code> make <code>https://example.com/write</code> ' +
      '&mdash; and a client application is granted some of them. <strong>A ' +
      'permission must be DEFINED before it can be GRANTED</strong>, which ' +
      'is the one ordering rule this feature has; it is checked in ' +
      '<code>applications.js</code> so that this form, the management API ' +
      'and the attribute editor on <a ' +
      'href="/admin/applications">Applications</a> cannot disagree about it.') +

      this.note('<strong>Then a client asks for it as an OAuth scope, and ' +
      'the access token says both halves.</strong> <code>scope=openid ' +
      'https://example.com/write</code> produces a token audienced to ' +
      '<code>https://example.com/</code> carrying <code>scope: openid ' +
      'write</code> &mdash; the base becomes the <code>aud</code> and the ' +
      'name becomes the scope, which is what a resource server wants: check ' +
      'the audience once, then read bare permission names. That is the same ' +
      'rule a scope naming another application\'s <code>client_id</code> ' +
      'already follows, one step more precise.') +

      this.note('<strong>In product mode an ungranted permission is refused ' +
      '<code>invalid_scope</code>, always. In development a grant refuses ' +
      'nothing by default, and that is the setting at the foot of this ' +
      'page.</strong> With ' +
      '<code>oauth2.delegatedPermissionsEnforced</code> off &mdash; which it ' +
      'is unless somebody turned it on &mdash; an ungranted permission is ' +
      'honoured exactly as a granted one is, logged as ungranted, and marked ' +
      'here. With it on the same request is refused ' +
      '<code>invalid_scope</code> at the authorization endpoint, where the ' +
      'client can still be told. Both answers exercise a client and neither ' +
      'is the right one for every test, which is why the register is fully ' +
      'readable before anybody enforces anything.') +

      '<div class="tiles">' +
        this.tile(counts.resources, 'applications exposing an API') +
        this.tile(counts.permissions, 'permissions defined') +
        this.tile(counts.grants, 'grants') +
        this.tile(counts.clients, 'applications holding one') +
        this.tile(counts.unused, 'granted and never asked for') +
        this.tile(counts.dangling, 'dangling') +
      '</div>' +

      this.note('<a class="btn" href="/admin/delegation/allowed">See the ' +
      'allowed mappings as a picture &rarr;</a> <strong>A SECOND diagram, ' +
      'and it is not the one above.</strong> <a ' +
      'href="/admin/delegation/map">The picture of the acts</a> draws what ' +
      'happened: three layers, a stick figure for the person it happened to, ' +
      'and a hexagon for this service, which issued it. This one draws what ' +
      'is allowed: every box is an application, there is no person on it at ' +
      'all &mdash; a permission says <em>webapp1 may reach the API as ' +
      'whoever is signed in</em>, and there is no whoever yet &mdash; and ' +
      'this service is not on it either, because not one line of it has been ' +
      'issued. A line is DASHED until the client has actually asked for that ' +
      'permission, which is the reading a configured register exists for and ' +
      'the one an acts diagram can never give.') +

      '<h3 id="permissions">Permissions applications expose</h3>' +
      this.note('One row per permission. <strong>Defining one grants it to ' +
      'nobody</strong>, so a row with nothing in the last two columns is the ' +
      'ordinary first step rather than a mistake. Removing a permission does ' +
      'NOT revoke the grants naming it &mdash; they stay on the clients\' ' +
      'entries and become dangling, because tidying them would be this page ' +
      'writing to entries nobody named.') +
      this.sectionSearchForm({
        path: '/admin/delegation', query: req.query,
        param: 'permq', pageParam: 'permissionsPage',
        label: 'Narrow to an application',
        placeholder: 'part of an application name or identifier',
        what: 'It matches the application that EXPOSES the permission ' +
          '&mdash; the first column &mdash; on its name and on its ' +
          'identifier both, and nothing else on the row. A permission ' +
          'belongs to exactly one application, so there is no second column ' +
          'this box could have meant. To find every permission some ' +
          'application HOLDS, search the grants table below instead: that is ' +
          'the relationship, and this table is the definition.'
      }) +
      permNav.head +
      '<table><tr><th>Exposed by</th><th>Permission</th><th>Identifier ' +
      '&mdash; what a client sends</th><th>Held by</th><th>Which ' +
      'applications</th><th></th></tr>' +
      (permPage.shown.map(function (one) {
        return self.permissionDefinitionRow(one, listView);
      }).join('') || '<tr><td colspan="6">' +
        (permWanted
          ? 'No application whose name or identifier contains <code>' +
            this.esc(permWanted) + '</code> exposes a permission. ' +
            (register.permissions.length
              ? register.permissions.length + ' permission(s) are defined ' +
                'here under other applications.'
              : 'None is defined here at all yet.')
          : 'No application here exposes an API yet. Give one a base URI ' +
            'below and then define a permission on it.') +
        '</td></tr>') +
      '</table>' +
      permNav.foot +

      '<h4>Expose an API</h4>' +
      this.note('The base URI is one answer per application and everything ' +
      'it exposes hangs off it. A trailing separator is added where there is ' +
      'none, because the identifier is a plain concatenation and ' +
      '<code>https://example.com</code> + <code>write</code> would otherwise ' +
      'read as one word. Clearing it leaves the permissions on the entry ' +
      'with no identifier at all, which the table above reports rather than ' +
      'hides.') +
      '<form method="post" action="/admin/delegation">' +
      this.permissionsBack(listView) +
        '<div class="formrow">' +
        '<input type="hidden" name="action" value="set-permission-base">' +
        '<label for="base-resource">Application</label>' +
        '<select id="base-resource" name="resource">' + applicationOptions +
      '</select><label ' +
        'for="baseUri">Base URI</label><input type="text" id="baseUri" ' +
        'name="baseUri" size="34" placeholder="https://example.com/"><button ' +
        'type="submit">Set the base URI</button></div></form><h4>Define a ' +
        'permission</h4>' +
      this.note('The name is what ends up on the token\'s <code>scope</code> ' +
      'claim, so it must be a legal OAuth scope token: any printable ASCII ' +
      'except space, double quote and backslash (RFC 6749 section 3.3), and ' +
      'not <code>|</code>, which separates the name from the description in ' +
      'the attribute. The description is optional and is shown wherever the ' +
      'permission is; changing it means removing the permission and defining ' +
      'it again, because a permission has one description and two rows with ' +
      'one name would leave the second unreachable.') +
      '<form method="post" action="/admin/delegation">' +
      this.permissionsBack(listView) +
        '<div class="formrow">' +
        '<input type="hidden" name="action" value="define-permission">' +
        '<label for="perm-resource">Exposed by</label>' +
        '<select id="perm-resource" name="resource">' + applicationOptions +
      '</select><label ' +
        'for="perm-name">Name</label><input type="text" id="perm-name" ' +
        'name="name" size="18" placeholder="write"><label ' +
        'for="perm-description">Description</label><input type="text" ' +
        'id="perm-description" name="description" size="34" ' +
        'placeholder="Change widgets on somebody\'s behalf"><button ' +
        'type="submit">Define it</button></div></form><h3 id="grants">Grants ' +
        '&mdash; the delegation relationships</h3>' +
      this.note('<strong>One row per (client, permission), and that IS the ' +
      'relationship.</strong> A client granted three permissions on one ' +
      'resource is three rows rather than one labelled <em>3</em>, because ' +
      'the permission is what was granted and the pair of applications is ' +
      'what it happens to join. That is also how one-to-many and many-to-one ' +
      'both work here with no store of their own: three clients granted one ' +
      'permission is one value on each of three entries.') +
      this.sectionSearchForm({
        path: '/admin/delegation', query: req.query,
        param: 'grantq', pageParam: 'grantsPage',
        label: 'Narrow to an application',
        placeholder: 'part of an application name or identifier',
        what: 'It matches EITHER END of the relationship &mdash; the client ' +
          'that may ask and the resource that is reached &mdash; on the name ' +
          'and on the identifier both. Both ends deliberately, and it is the ' +
          'same argument the acts table\'s own text box makes: a reader ' +
          'arrives holding one application name and the question <em>what is ' +
          'this thing mixed up in</em>, and they do not know, and should not ' +
          'have to guess, which of the two columns it will turn up in. ' +
          'Searching one end would answer half that question while looking ' +
          'as though it had answered all of it. A DANGLING grant has no ' +
          'resource at all, so it matches only on its client &mdash; which ' +
          'is right: there is no other application in that row to find it by.'
      }) +
      grantNav.head +
      '<table><tr><th>Client &mdash; who may ask</th><th>Resource &mdash; ' +
      'what is reached</th><th>Permission</th><th>Identifier</th><th>What ' +
      'the access token will say</th><th>Ever asked for?</th><th></th></tr>' +
      (grantPage.shown.map(function (one) {
        return self.permissionGrantRow(one, listView);
      }).join('') || '<tr><td colspan="7">' +
        (grantWanted
          ? 'No grant names an application whose name or identifier contains ' +
            '<code>' + this.esc(grantWanted) + '</code>, at either end. ' +
            (register.grants.length
              ? register.grants.length + ' grant(s) are held here between ' +
                'other applications.'
              : 'Nothing is granted here at all yet.')
          : 'Nothing is granted yet. Define a permission above, then grant ' +
            'it on the client\'s own page &mdash; until then every scope ' +
            'this service is sent is an ordinary scope.') +
        '</td></tr>') +
      '</table>' +
      grantNav.foot +

      // -----------------------------------------------------------------------
      // THE GRANT FORM IS NOT HERE ANY MORE, AND THIS PARAGRAPH IS WHAT IS LEFT
      // OF IT (2026-09-01).
      //
      // It was a `<select>` of every application beside a `<select>` of every
      // permission, and it asked the reader to get BOTH right on a page that is
      // about neither of them in particular. That is the one control in this
      // register where picking the wrong option still SUCCEEDS: a grant written
      // to the resource instead of to the client resolves in both directions
      // and reads correctly on this very table, and the only place it shows as
      // wrong is at the token endpoint, later, to somebody else.
      //
      // On an application's own page there is no first select at all: the
      // client is the entry the reader is standing on. So the control that
      // could be half wrong became a control that cannot be. Only this one
      // moved: the three RESOURCE-half controls stay here, because this page is
      // where the register is and `Expose an API`, `Define a permission` and
      // each row's `Remove` all name the resource explicitly.
      // `revoke-permission` is drawn in BOTH places and that is not a leftover
      // — it is a ROW BUTTON, so its two halves are the row it sits on and
      // neither can be got wrong, which is exactly what was wrong with the
      // grant form's two selects. Somebody tidying up works from the register;
      // the application's own list is the read-back of the grant just made, and
      // a table with no way to undo the write above it is half a control.
      //
      // WHAT IS DELIBERATELY UNCHANGED IS THE HANDLER. `POST /admin/delegation`
      // still answers all five actions and PERMISSION_ACTIONS still lists all
      // five, so rule 7's parity check reads the same sentence it always did
      // and `POST /admin-api/permissions/grant-permission` still mirrors it.
      // Moving a FORM is not moving an action, and making it one would have
      // cost this feature an /admin-api operation for no reason a caller would
      // notice.
      // -----------------------------------------------------------------------
      '<h4>Grant a permission</h4>' +
      (grantable.length
        ? this.note('<strong>This one is on the application\'s own ' +
          'page.</strong> A grant lands on the CLIENT\'s entry, as a value ' +
          'of <code>oauthDelegatedPermission</code>, because the client is ' +
          'the party that will name the permission in a <code>scope</code> — ' +
          'so the entry that answers <em>may this request be honoured</em> ' +
          'is the entry the request identifies. That is exactly why the ' +
          'control is drawn where that entry is: open an application under ' +
          '<a href="/admin/applications">Directory &rsaquo; Applications</a> ' +
          'and its <em>Delegated permissions</em> section grants it one, ' +
          'with the client half of the pair already settled by the page you ' +
          'are on rather than chosen out of a list of every application ' +
          'here. An application still cannot be granted its own permission: ' +
          'the token would be addressed to itself, which is what an ID Token ' +
          'already is, and that page offers it none of them.')
        : this.note('<strong>There is nothing to grant yet.</strong> A ' +
          'permission must be defined before it can be granted, so the ' +
          'control appears — on each application\'s own page under <a ' +
          'href="/admin/applications">Directory &rsaquo; Applications</a> — ' +
          'once an application exposes one with an identifier. That ordering ' +
          'is the whole shape of the feature rather than a limitation of ' +
          'either page.')) +

      this.note('<strong>Every one of these is an ordinary attribute on an ' +
      'ordinary directory entry</strong>, and an <code>ldapmodify</code> ' +
      'reaches them exactly as it reaches a redirect URI: ' +
      '<code>oauthPermissionBaseUri</code> and <code>oauthPermission</code> ' +
      'on the resource, <code>oauthDelegatedPermission</code> on the client. ' +
      'What LDAP does not get is the ordering check &mdash; this directory ' +
      'enforces nothing anywhere &mdash; which is why a grant naming a ' +
      'permission nobody defines can exist at all, and why it is shown as ' +
      'dangling rather than treated as an error. <code>GET ' +
      '/admin/ldap/applications</code> publishes all three, and they persist ' +
      'wherever the directory does.');
  }

  // ---------------------------------------------------------------------------
  // WHERE A PERMISSION WRITE SENDS THE READER AFTERWARDS.
  //
  // This handler serves TWO pages since 2026-09-01. Four of its five actions
  // are drawn on /admin/delegation, where the register is; `grant-permission`
  // and the per-row `revoke-permission` beside it are drawn on an APPLICATION's
  // own page, because there the client half of the pair is the entry the reader
  // is standing on rather than an option in a list of everything. See
  // applicationPermissionsSection()'s header for why that move was worth making
  // and why it did not become a sixth action on /admin/applications.
  //
  // `from` IS A NAME AND NOT A URL, and this function is why. configReturnTo()
  // makes the same argument for the settings forms on twenty-one pages: a
  // redirect target taken out of a request body is an open redirect, and one
  // carrying a newline is a header injection. So nothing here is echoed — the
  // name is matched against the two paths this file wrote, and the application
  // page's destination is REBUILT from `client`, which the action has just
  // validated as an identifier in the registry. The worst a hand-written `from`
  // can reach is the delegation page.
  //
  // The FRAGMENT differs between them and that is not decoration. On
  // /admin/delegation the configured half is four screens down, so `#allowed`
  // is the difference between landing on the thing you just did and landing at
  // the top of the longest page in this console; on an application's page the
  // section has its own `#permissions` for exactly the same reason.
  // ---------------------------------------------------------------------------
  permissionsReturnTo(body) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.permissionsReturnTo(). from=" +
              ((body && body.from) || '(none)'));
    const from = String((body && body.from) || '').trim();
    const client = String((body && body.client) || '').trim();
    if (from === '/admin/applications' && client) {
      const listView = this.listViewFromBack('/admin/applications', body.back);
      log.debug("Leaving AdminConsole.permissionsReturnTo(). Back to the " +
                "application page.");
      return '/admin/applications' +
             queryWith(listView, { application: client }) + '#permissions';
    }
    const listView = this.listViewFromBack('/admin/delegation', body.back);
    log.debug("Leaving AdminConsole.permissionsReturnTo(). Back to the " +
              "delegation page.");
    return '/admin/delegation' + queryWith(listView, {}) + '#allowed';
  }

  // ---------------------------------------------------------------------------
  // THE APPLICATION SEARCH OVER THE CONFIGURED REGISTER, AND WHY IT NEEDED
  // PARAMETER NAMES OF ITS OWN.
  //
  // It is `chooserPane()` again — the same control, the same twenty-at-a-time
  // pane, the same "an empty box matches everything" — drawn on
  // /admin/delegation/allowed and again on /admin/delegation/cluster under the
  // group it opened, for the reason `delegationApplicationChooser()` gives
  // about its own second copy: comparing two groups should be one click rather
  // than two.
  //
  // **THE CATALOGUE IS A DIFFERENT LIST FROM THE ACTS CHOOSER'S AND THAT IS THE
  // WHOLE REASON THIS IS A SECOND FUNCTION.** `delegationApplicationChooser()`
  // offers what some act NAMED — what has actually delegated, spellings and
  // all, including an RFC 8693 audience nobody registered. This one offers what
  // the CONFIGURED register touches: every application carrying a base URI or a
  // permission, and every application holding a grant. The two lists overlap
  // and neither contains the other, and a reader searching here for something
  // they saw on the acts picture must be told it is not in this register rather
  // than shown a group it is not in.
  //
  // **IT USES `permappq` / `permappfrom` AND MUST NOT REUSE `appq` /
  // `appfrom`.** Those two belong to the acts chooser, they are in LIST_PARAMS
  // for /admin/delegation, and a drill-down of that page carries them through
  // untouched so that the way back lands on the search the reader left. A
  // second control writing the same two names would overwrite that on every
  // search — the reader would come back to /admin/delegation holding a term
  // they typed into a different list. That is precisely the failure the acts
  // page's own two pairs are split to prevent, made once more between two pages
  // instead of twice on one.
  // ---------------------------------------------------------------------------
  allowedApplicationChooser(view, selectedKey, carry, here) {
    const { log, applications, queryWith } = this.deps;
    log.debug("Entering AdminConsole.allowedApplicationChooser().");
    const groups = view.clusters;
    if (!groups.counts.applications) {
      log.debug("Leaving AdminConsole.allowedApplicationChooser(). Nothing " +
                "configured yet.");
      return this.note('<strong>No application in this registry exposes an ' +
        'API or holds a permission yet</strong>, so there is nothing to ' +
        'search. An application arrives in this list the moment it is given ' +
        'a base URI or a permission of its own — which makes it a RESOURCE — ' +
        'or is granted one somebody else exposes, which makes it a CLIENT. ' +
        'Both are done on <a href="/admin/delegation#allowed">the ' +
        'register</a> or through <code>POST /admin-api/permissions/…</code>.');
    }

    // Everything one row of the pane has to say, worked out from the register
    // ONCE rather than per entry: `register()` is already in hand, and a filter
    // per application over `grants` and `permissions` would be quadratic in a
    // registry whose whole point is that it can be large.
    const holds = {};
    const reached = {};
    const exposes = {};
    view.register.grants.forEach(function (one) {
      holds[one.client] = (holds[one.client] || 0) + 1;
      if (one.resource) {
        reached[one.resource] = (reached[one.resource] || 0) + 1;
      }
    });
    view.register.permissions.forEach(function (one) {
      exposes[one.resource] = (exposes[one.resource] || 0) + 1;
    });

    const entries = [];
    groups.clusters.forEach(function (group) {
      group.members.forEach(function (identifier) {
        const registered = applications.get(identifier);
        const name = registered
          ? (registered.name || registered.dnLabel || identifier) : '';
        const parts = [];
        if (holds[identifier]) {
          parts.push(holds[identifier] + ' grant(s) held');
        }
        if (exposes[identifier]) {
          parts.push(exposes[identifier] + ' permission(s) exposed');
        }
        if (reached[identifier]) {
          parts.push(reached[identifier] + ' grant(s) on it');
        }
        if (!parts.length) {
          parts.push('a base URI and nothing on it yet');
        }
        entries.push({
          key: identifier,
          // The identifier AND the name somebody gave the entry, because the
          // chooser shows one of them and a reader is about as likely to be
          // holding the other — `chooserMatches()`'s whole subject.
          names: name && name !== identifier ? [identifier, name] :
                 [identifier],
          label: identifier,
          detail: parts.join(', ') + ' — ' +
                  (group.counts.applications === 1
                    ? 'in no group but its own'
                    : 'one of ' + group.counts.applications +
                      ' applications joined to each other'),
          // THE GROUP AND NOT THE APPLICATION IS WHAT OPENS, which is the one
          // way this control differs from every other chooser in the console: a
          // result here is not a page about the thing clicked, it is the page
          // about everything that thing is joined to. The application is still
          // the parameter, because it is what the reader chose and it is what
          // makes the URL one somebody can check — a group's own key is the
          // alphabetically first member, which is a name nobody typed.
          href: '/admin/delegation/cluster' +
                queryWith(carry || {}, { application: identifier })
        });
      });
    });
    // ALPHABETICAL AND NOT IN GROUP ORDER. The groups are sorted biggest-first
    // for the table below, and that is the right order for a list of GROUPS; it
    // is the wrong one for a list of applications, where the reader is looking
    // for a name and the group is what they are about to find out.
    entries.sort(function (a, b) { return a.key.localeCompare(b.key); });

    log.debug("Leaving AdminConsole.allowedApplicationChooser(). " +
              entries.length +
              " application(s) to search.");
    return this.chooserPane({
      here: here, param: 'permappq', fromParam: 'permappfrom',
      label: 'Find an application',
      placeholder: 'part of a client_id, a base URI or an application name',
      entries: entries, selectedKey: selectedKey,
      nothing: 'No application in the configured register matches that. This ' +
        'list holds what the PERMISSION register touches — an entry with a ' +
        'base URI or a permission of its own, or one holding a grant — so a ' +
        'name that is on <a href="/admin/applications">the registry</a> and ' +
        'not in here is an application nothing has been configured about yet.'
    });
  }

  // The chooser's own two parameters, carried by hand from the page the search
  // was typed on to the page a result opens.
  //
  // They are deliberately NOT in LIST_PARAMS for /admin/delegation. That table
  // is the state of the acts LIST, spent by the breadcrumb's way back to it,
  // and these two names belong to neither the list nor the page it hangs under
  // — a crumb pointing at /admin/delegation carrying a search that page cannot
  // use would be state nobody can get back to, which is exactly what that
  // table's header says a row is for. So the way BACK to the allowed picture
  // carries them and the way UP to the acts table does not, and each link says
  // which it is.
  allowedChooserState(query) {
    const { log, queryOne } = this.deps;
    log.debug("Entering AdminConsole.allowedChooserState().");
    const out = {};
    ['permappq', 'permappfrom'].forEach(function (name) {
      const value = queryOne(query, name);
      if (value !== '') {
        out[name] = value;
      }
    });
    log.debug("Leaving AdminConsole.allowedChooserState().");
    return out;
  }

  // The groups themselves, as a table: the answer to *what is joined to what*
  // without opening a picture of any of it.
  //
  // One row per group, biggest first, with the applications in it spelled out —
  // because the size of a group is not what makes it interesting, its MEMBERS
  // are, and a table of counts with no names in it would send a reader into
  // every picture in turn to find the one they meant.
  allowedClusterTable(groups, shown, carry) {
    const { log, applications, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.allowedClusterTable(). " + shown.length +
              " row(s).");
    const rows = shown.map(function (group) {
      const members = group.members.map(function (identifier) {
        const registered = applications.get(identifier);
        const name = registered
          ? (registered.name || registered.dnLabel || identifier) : identifier;
        return '<a href="' + self.esc('/admin/delegation/cluster' +
          queryWith(carry || {}, { application: identifier })) + '"><code>' +
          self.esc(identifier) + '</code></a>' +
          (name !== identifier
            ? ' <span class="state-none">' + self.esc(name) + '</span>' : '') +
          (registered ? ''
            : ' <span class="state-none" title="No entry under ' +
              'ou=applications answers to this identifier. It can only have ' +
              'got here through an ldapmodify, since both console doors read ' +
              'the registry.">not in the registry</span>');
      }).join('<br>');
      return '<tr>' +
        '<td class="who"><a href="' + self.esc('/admin/delegation/cluster' +
          queryWith(carry || {}, { application: group.key })) + '">' +
          (group.counts.applications === 1
            ? 'this one alone'
            : self.esc(group.counts.applications) + ' applications') + '</a>' +
          '<br><span class="state-none" title="A group is named after the ' +
          'application whose identifier sorts first, so that adding a grant ' +
          'inside it does not rename it.">named for <code>' +
          self.esc(group.key) +
          '</code></span></td>' +
        '<td>' + members + '</td>' +
        '<td class="num">' + (group.counts.lines
          ? self.esc(group.counts.lines)
          : '<span class="state-none" title="Nothing in this group may reach ' +
            'anything else in it. A group of one with a base URI is an API ' +
            'somebody described and nothing was granted on.">0</span>') +
        '</td><td ' +
        'class="num">' + (group.counts.asked
          ? '<span class="state-valid">' + self.esc(group.counts.asked) +
            '</span>'
          : '<span class="state-none">0</span>') + ' asked for<br>' +
          (group.counts.unused
            ? '<span class="state-expired" title="Granted and never ' +
              'requested. Read off the client\'s own oauthScope — evidence ' +
              'rather than proof.">' + self.esc(group.counts.unused) + '</span>'
            : '<span class="state-none">0</span>') + ' never used</td>' +
        '<td class="num">' + self.esc(group.counts.permissions) + '</td>' +
        '<td class="num">' + (group.counts.dangling
          ? '<span class="state-revoked" title="Naming a permission no ' +
            'application in this registry defines. Not drawn on any picture, ' +
            'because a line to nowhere would be a drawing of a resource that ' +
            'is there.">' + self.esc(group.counts.dangling) + ' dangling</span>'
          : '<span class="state-none">&mdash;</span>') +
          (group.counts.selfGrants
            ? '<br><span class="state-expired" title="An application granted ' +
              'its own permission. Neither console door will create one; an ' +
              'ldapmodify can. No arrow is drawn, because an arrow from a ' +
              'box back to itself is a drawing of nothing.">' +
              self.esc(group.counts.selfGrants) + ' to itself</span>'
            : '') + '</td>' +
        '</tr>';
    }).join('');
    log.debug("Leaving AdminConsole.allowedClusterTable().");
    return '<table><tr><th>The group</th><th>The applications in it</th>' +
      '<th>Lines drawn</th><th>Grants</th><th>Permissions exposed</th>' +
      '<th>Not drawn</th></tr>' +
      (rows || '<tr><td colspan="6">' +
        (groups.counts.clusters
          ? 'No group on this page.'
          : 'Nothing in this registry exposes an API or holds a permission, ' +
            'so there are no groups to draw.') + '</td></tr>') + '</table>';
  }

  // ---------------------------------------------------------------------------
  // GET /admin/delegation/map — THE SAME ACTS, AS A PICTURE.
  //
  // A DRILL-DOWN OF /admin/delegation AND NOT A SECTION OF ITS OWN. It has no
  // NAV row, it passes `up`, and its active tab is the delegation page's —
  // which is what makes the trail read `Admin console › Delegation › The
  // picture` and what makes the way back carry the filter the reader came in
  // with. The test rule 7a states is the one that decided it: a parameter that
  // merely FILTERS a list is not a drill-down, and this is not a filter — it is
  // a second VIEW of the list, which is exactly what a drill-down is. Putting
  // it in the sidebar was considered and would have been a nineteenth tab that
  // shows nothing the tab above it does not already hold.
  //
  // **IT IS THE SAME VIEW FUNCTION.** `delegationView(req.query)` builds it, so
  // every filter on the table filters the picture, and the two can never come
  // to disagree about which acts they are describing. There is one deliberate
  // difference and it is the whole reason to state it: **the picture is drawn
  // from `view.filtered` and the table is drawn from `view.shown`** — paging a
  // picture would draw the boxes that happen to be on page 2 and the lines that
  // happen to join them, which is a diagram of the pagination rather than of
  // the service. The page says so where the count is printed.
  //
  // **THERE IS NO FORM AND THEREFORE NO NEW OPERATION ON /admin-api** — rule 7,
  // satisfied the same way `/admin/delegation` itself satisfies it. What this
  // page shows is reachable without a browser at `?format=json`, and the same
  // graph is on `GET /admin-api/delegation` in the `graph` member, because a
  // picture a test cannot assert against is a picture that can go wrong
  // quietly.
  //
  // **AND `?format=svg` ANSWERS THE DOCUMENT ALONE.** It is the one shape this
  // console has that is worth saving — a diagram is a thing people put in a
  // ticket — and it is not a fourth response format bolted on: `respond()`
  // still answers HTML and JSON, and this route answers SVG before calling it.
  // The standalone document carries NO LINKS, deliberately: `app.js` rewrites
  // root-relative hrefs into the current realm on the way out of a `text/html`
  // response only, so a link inside an `image/svg+xml` body would be a link
  // that silently leaves the realm — and in a saved file it would be a link to
  // somebody's own machine.
  //
  // **`?format=svg` GETS THE 302 AND NOT THE 401, and that is the gate's rule
  // rather than an oversight here.** The guard answers a program with a status
  // and a body and a BROWSER with the sign-in screen, and it decides which by
  // looking for JSON — `?format=json`, a JSON content-type, a JSON-only
  // `Accept`. Nothing else is a program as far as it is concerned, and this
  // format in particular is reached by clicking a link on this page, so a
  // browser that has been signed out should meet the screen and come back to
  // the document. A caller that wants the picture without a session wants
  // `?format=json` and the graph, which is the shape a test can assert against
  // anyway.
  // ---------------------------------------------------------------------------

  // WHAT A BOX IS, WHICH IS THE ONE QUESTION `delegation_map.js` DELIBERATELY
  // CANNOT ANSWER. It is handed this function and asks it once per node.
  //
  // The three states are `delegationPartyCell()`'s three states, and they are
  // the same three on purpose: a name this console can resolve, a name it could
  // file somebody under and never has, and a name for something the registry
  // has never seen. What the picture does with them is what a picture can do
  // and a table cannot — the first two get a SHAPE and the third gets a DASHED
  // one — and the row under the diagram still draws the cell, so nothing that
  // was linkable in the table stops being linkable here.
  //
  // **THE LABEL IS THE CN WHERE THERE IS ONE.** A directory entry's `cn` and an
  // application entry's `appName` are what somebody CALLED this thing, and the
  // identifier is what a protocol spelled it as; on a diagram the first is
  // worth more than the second, and the second is one line below it and in the
  // tooltip. Where there is no entry there is no cn, and the identifier is all
  // there is.
  delegationNodeLook(node, known) {
    const { log, applications, queryWith } = this.deps;
    log.debug("Entering AdminConsole.delegationNodeLook().");
    if (node.kind === 'sts') {
      log.debug("Leaving AdminConsole.delegationNodeLook().");
      // The one box that is not a party. It carries the REALM because a realm
      // is a whole logical copy of this service — two realms' pictures are two
      // different services' pictures — and it says `default` rather than
      // nothing in the realm that has no prefix, since a hexagon labelled only
      // `mock STS` would be silent about the one thing this box is here to say.
      return {
        shape: 'sts',
        label: 'mock STS',
        sublabel: 'realm: ' + (node.realm ? node.realm.name : 'Default') +
                  (node.realm && !node.realm.isDefault ?
                   ' (' + node.realm.id + ')' : ''),
        title: 'THIS SERVICE, in the trust realm ' +
          (node.realm ? node.realm.name + ' (' + node.realm.id + ')' :
           'default') +
          '.\nIssuer: ' + (node.issuer || '(unset)') +
          '\nEvery line in this picture exists because this service issued ' +
          'or refused a credential: ' + node.issued + ' issued, ' +
          node.refused +
          ' refused.\nThe dashed lines leaving it go to whoever ASKED — the ' +
          'intermediary where a chain has one, the initial identity where it ' +
          'does not.',
        href: '/admin/realms',
        dashed: false
      };
    }

    // The directory's own answer about a PERSON. `directoryReader` is a slot
    // and is empty in a build with no `ldap_server.js`, which is a state this
    // console reports everywhere else rather than guessing through — so with no
    // directory every party falls back to the role's shape, dashed, and the
    // page says why.
    let entry = null;
    if (directoryReader && node.key) {
      const info = directoryReader(node.key);
      entry = info && info.found ? info.entry : null;
    }
    // The registry's answer about an APPLICATION. Looked up by the identifier
    // the ACT carried rather than by the node's id: the id is normalised (see
    // `nodeIdOf()` in delegation.js) and `ou=applications` is keyed by what a
    // caller actually presented.
    const application = node.application ? applications.get(node.application) :
                        null;

    const isPerson = !!entry;
    const isApplication = !!application;
    const shape = isPerson && isApplication ? 'both'
                : isApplication ? 'application'
                : isPerson ? 'person'
                // Nothing is known. The ROLE decides, which is the ROLES table
                // read as a drawing: an initial identity is a person, a target
                // is an application, and an intermediary is drawn as an
                // application because that is what a front-end service is.
                : node.chiefRole === 'initial' ? 'person' : 'application';

    const cn = entry ? this.firstAttributeValue(entry, 'cn') : '';
    const appName = application ? (application.name || application.dnLabel) :
                    '';
    const label = cn || appName || node.id;

    // WHAT A PROTOCOL WOULD HAVE TO PRESENT TO REACH THIS BOX. Added
    // 2026-08-27, and it exists because of the paragraph above it: the label is
    // the CN where there is one, so a rectangle reading `Acme Web` said nothing
    // anywhere on the diagram about the string a request would have to carry.
    // That is the fact somebody opens a delegation picture to get — the
    // `client_id` they are about to put in a token request, the `AppliesTo` in
    // the RequestSecurityToken they are about to send — and it was in the
    // tooltip, which is not a place a diagram pasted into a ticket keeps.
    //
    // The list comes from `applications.identifiersOf()` rather than from a
    // walk of the entry here: which attribute is a family's identifier and what
    // the specification calls it are that module's statements, and a picture
    // holding a second opinion about either is drift nothing can see.
    //
    // **THE SPELLING IS OF THE NAME THE ACT ACTUALLY CARRIED**, not of the
    // first identifier the entry happens to hold. An application answering to a
    // client_id AND an entityID is one box, and which of the two is on the line
    // is what the act says; naming the other one would put a string on the
    // picture that nothing in this picture ever presented. The rest are in the
    // tooltip, where "it also answers to" belongs.
    const identifiers = application ? applications.identifiersOf(application) :
                        [];
    const presented = node.application ? String(node.application) : '';
    // GROUPED BY VALUE AND NOT BY ATTRIBUTE, because one string is commonly two
    // families' identifier and the box has room for one line: an application
    // declared for WS-Trust and SAML 2.0 carries `https://esb.example.com` on
    // `wstrustAppliesTo` AND on `samlEntityId`, and drawing the first of those
    // would pick one of two true answers. `AppliesTo / entityID:
    // https://esb.example.com` is the whole fact and is one line.
    //
    // Exact equality throughout, because `applications.js` does not case-fold
    // an identifier anywhere else either — an audience that differs by a
    // character is a different audience, and matching loosely here would be
    // this page deciding a comparison rule on that module's behalf.
    const byValue = [];
    identifiers.forEach(function (row) {
      row.values.forEach(function (value) {
        const already =
            byValue.filter(function (one) { return one.value === value; })[0];
        if (already) {
          if (already.names.indexOf(row.name) < 0) already.names.push(row.name);
          return;
        }
        byValue.push({ value: value, names: [row.name] });
      });
    });
    // WHICH ONE GOES ON THE BOX. The name the ACT carried wins, because that is
    // the string on the line the reader is following; where the act's name is
    // not an identifier attribute at all — the registry key of an entry made by
    // hand, or an application reached through an audience it registered — the
    // first declared identifier is drawn instead, since a box that named only
    // the key would be silent about the one thing somebody opened the picture
    // to get. Everything not drawn is in the tooltip.
    const chosen =
        byValue.filter(function (one) { return one.value === presented; })[0] ||
                   byValue[0] || null;
    // Nothing at all where the drawn name IS the identifier and no family
    // claims it: the string is already on the box, and a second line repeating
    // it is the same fact drawn twice. Where a family DOES claim it, the word
    // alone is drawn — `client_id` under `acme-web` says what kind of name that
    // is, which the box could not otherwise say.
    const identifierLine = chosen
      ? chosen.names.join(' / ') +
        (chosen.value === label ? '' : ': ' + chosen.value)
      : (presented && presented !== label ? presented : '');

    const parts = [];
    if (isPerson) parts.push('person');
    if (isApplication) parts.push('application');
    // WHICH STORE HAS NOT HEARD OF IT, and the two are different sentences. A
    // target drawn as a rectangle is missing from `ou=applications` — the
    // REGISTRY — and an initial identity drawn as a figure is missing from
    // `ou=users` — the DIRECTORY. One word for both would send half the readers
    // to the wrong page to look for it, which is the mistake
    // `delegationPartyCell()` avoids by drawing up to two links rather than
    // one.
    const missing = shape === 'person' ? 'not in the directory' : 'not in ' +
        'the registry';
    const sublabel = parts.length ? parts.join(' + ')
                   : (node.chiefRole ? node.chiefRole + ', ' + missing :
                      missing);

    // Where the box goes when it is clicked. ONE link, where the table draws up
    // to two — an SVG shape can be inside one anchor and the party table under
    // the picture carries both, which is where a reader who wants the other one
    // looks. The person's page wins when this console has SEEN them
    // authenticate, because that page answers the question a delegation raises
    // (what else was issued in their name); the application page otherwise.
    let href = '';
    if (node.key && known[node.key]) {
      href = '/admin/users' + queryWith({ user: node.key }, {});
    } else if (isApplication) {
      href = '/admin/applications' +
             queryWith({ application: node.application }, {});
    }

    const title = [
      label === node.id ? node.id : label + ' — ' + node.id,
      node.presented && node.presented !== node.id
        ? 'presented as ' + node.presented : '',
      node.application && node.application !== node.id
        ? 'named as an application: ' + node.application : '',
      entry ? 'In the directory at ' + entry.dn + '.'
            : (node.key ? 'No entry under ou=users names this.' : ''),
      application ? 'In the applications registry' +
        (application.dn ? ' at ' + application.dn : '') + '.'
        : (node.application ? 'NOT in the applications registry — the ' +
           'registry holds what this service has been ASKED ABOUT, and a ' +
           'delegation naming something nobody has otherwise mentioned is ' +
           'ordinary for an RFC 8693 audience.' : ''),
      // EVERY name it answers to, family by family, because the box has room
      // for one. This is where an application that is a client_id in one
      // protocol and an entityID in another says so.
      identifiers.length
        ? 'It answers to: ' + identifiers.map(function (row) {
            return row.name + ' ' + row.values.join(', ') +
                   ' (' + row.families.join(', ') + ')';
          }).join('; ') + '.'
        : '',
      (presented && chosen && chosen.value !== presented)
        ? 'THE NAME ON THE BOX IS NOT THE NAME THIS ACT PRESENTED. It ' +
          'presented "' +
          presented + '", which none of the identifier attributes above ' +
          'carries — so which family spells it that way cannot be said, and ' +
          'the first declared identifier is drawn instead. An entry created ' +
          'by a protocol sighting carries that family\'s attribute; one made ' +
          'by hand, or reached through an audience it registered, need not.'
        : '',
      'Roles: initial ' + node.roles.initial + ', intermediary ' +
        node.roles.intermediary + ', target ' + node.roles.target + '.',
      node.selfTarget
        ? 'Some act named this party as BOTH the intermediary and the target ' +
          '— a ticket to ITSELF, which is what S4U2Self is. There is no line ' +
          'for it because an arrow leaving a box and coming back is a ' +
          'drawing of nothing.'
        : '',
      node.protocols.length ? 'Seen over: ' + node.protocols.join(', ') + '.' :
      '',
      node.what || ''
    ].filter(Boolean).join('\n');

    log.debug("Leaving AdminConsole.delegationNodeLook().");
    return { shape: shape, label: label, sublabel: sublabel,
             identifier: identifierLine, title: title,
             href: href, dashed: !isPerson && !isApplication };
  }

  // One attribute off an entry the directory reader handed back, canonically
  // spelled or not. `objectFor()` returns them canonically spelled and a caller
  // asking for `cn` should not have to know that.
  firstAttributeValue(entry, name) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.firstAttributeValue().");
    if (!entry || !entry.attributes) {
      log.debug("Leaving AdminConsole.firstAttributeValue().");
      return '';
    }
    const wanted = String(name).toLowerCase();
    const key = Object.keys(entry.attributes).filter(function (one) {
      return String(one).toLowerCase() === wanted;
    })[0];
    const values = key ? entry.attributes[key] : null;
    log.debug("Leaving AdminConsole.firstAttributeValue().");
    return (values && values.length) ? String(values[0]) : '';
  }

  // THE KEY. Drawn out of `delegation_map.js`'s own glyph functions and its own
  // palette rather than out of a second set of shapes written for the legend,
  // because a legend that is drawn separately is a legend that will eventually
  // describe a picture this service no longer draws. `options.issuance` adds
  // the two lines only the person's picture has. It is a parameter rather than
  // two more rows for everybody, because a legend must describe the diagram
  // beside it: a reader of /admin/delegation/map looking for a dotted `signed
  // in` line would never find one, and a key that lists shapes a page does not
  // draw teaches a reader to stop trusting it.
  delegationMapKey(options?) {
    const { log, delegationMap } = this.deps;
    log.debug("Entering AdminConsole.delegationMapKey().");
    const swatch = function (inner, width) {
      log.debug("Entering swatch().");
      log.debug("Leaving swatch().");
      return '<svg width="' + width + '" height="40" viewBox="0 0 ' + width +
        ' 40" aria-hidden="true">' + inner + '</svg>';
    };
    const C = delegationMap.COLOURS;
    // Drawn by `delegation_map.js` rather than here, so that the round end and
    // the pointed end in the key are the ones the picture actually puts on a
    // line. The arrowhead used to be drawn on this side and the tail disc would
    // have made that two shapes to keep in step instead of one.
    const line = function (colour, dash) {
      log.debug("Entering line().");
      log.debug("Leaving line().");
      return delegationMap.edgeSample(colour, dash);
    };
    const items = [
      { art: swatch(delegationMap.personGlyph(9, 3, C.indigo, false, 1), 44),
        what: '<strong>A person.</strong> Something with an entry under ' +
              '<code>ou=users</code> — anybody this service has ' +
              'authenticated, in any of the sixteen families.' },
      { art: swatch('<rect x="3" y="9" width="56" height="22" rx="5" fill="' +
                    C.panel +
                    '" stroke="' + C.indigo + '" stroke-width="1.5"/>', 64),
        what: '<strong>An application.</strong> Something with an entry ' +
              'under <code>ou=applications</code> — an OAuth client, a ' +
              'service provider, a Kerberos service, a WS-Trust relying ' +
              'party.' },
      { art: swatch('<rect x="3" y="4" width="56" height="32" rx="5" fill="' +
                    C.panel +
                    '" stroke="' + C.indigo + '" stroke-width="1.5"/>' +
                    delegationMap.personGlyph(7, 3, C.indigo, false, 0.85), 64),
        what: '<strong>Both, which the middle tier usually is.</strong> ' +
              '<code>HTTP/frontend.example.com</code> authenticates (so the ' +
              'funnel files it with the people) AND has tickets issued FOR ' +
              'it (so the registry has it). Two entries, one party.' },
      { art: swatch('<path d="' + delegationMap.hexPath(3, 6, 58, 28) +
                    '" fill="' +
                    C.wash + '" stroke="' + C.indigo + '" stroke-width="1.8"/>',
                    64),
        what: '<strong>This service, in one trust realm.</strong> Every line ' +
              'here exists because it issued or refused a credential. The ' +
              'realm is on the box because a realm is a whole logical copy ' +
              'of this service.' },
      // Wider than every other swatch here, and it has to be: the whole point
      // of the row is the THIRD line, and an identifier is the one thing on a
      // box that is as long as a protocol allows. At 80 the sample ran out
      // through its own rectangle, which is precisely the mistake this row is
      // teaching a reader to look for.
      { art: swatch('<rect x="3" y="2" width="104" height="36" rx="5" fill="' +
                    C.panel +
                    '" stroke="' + C.indigo + '" stroke-width="1.5"/>' +
                    '<text x="55" y="15" text-anchor="middle" font-size="10" ' +
                    'font-weight="600" fill="' + C.ink + '">Acme ' +
                    'Web</text><text x="55" y="25" text-anchor="middle" ' +
                    'font-size="8" fill="' +
                    C.quiet + '">application</text><text x="55" y="34" ' +
                    'text-anchor="middle" font-size="8" fill="' +
                    C.quiet + '">client_id: acme-web</text>', 110),
        what: '<strong>The two small lines under a name are different ' +
              'sentences.</strong> The first is what the box IS &mdash; ' +
              'which of the two stores knows it, or the role its shape was ' +
              'guessed from. The second is <strong>the identifier a protocol ' +
              'would have to present to reach it, with that protocol\'s own ' +
              'word for it</strong>: a <code>client_id</code> for OAuth 2.0 ' +
              'and OpenID Connect, an <code>entityID</code> for either SAML ' +
              'profile, a <code>wtrealm</code> for WS-Federation, an ' +
              '<code>AppliesTo</code> for WS-Trust, an <code>SPN</code> for ' +
              'Kerberos. The NAME on the box is what somebody CALLED this ' +
              'thing &mdash; a <code>cn</code>, an <code>appName</code> ' +
              '&mdash; and is no use in a request you are about to build, ' +
              'which is why the identifier is on the picture and not only in ' +
              'the tooltip. Where the two are the same string only the word ' +
              'is drawn, because the value is already the label; where ONE ' +
              'string is two families\' identifier both words are drawn ' +
              '(<code>AppliesTo / entityID</code>); and where the ' +
              'application answers to several DIFFERENT names, the one drawn ' +
              'is the one this act actually carried &mdash; or, if the act ' +
              'carried something no identifier attribute holds, the first ' +
              'declared one, with the tooltip saying so. Every name it ' +
              'answers to is in that tooltip either way.' },
      { art: swatch(delegationMap.personGlyph(9, 3, C.grey, true, 1), 44),
        what: '<strong>A dashed outline is something neither store ' +
              'knows.</strong> It is drawn in the shape its ROLE implies and ' +
              'is not an error: an RFC 8693 <code>audience</code> nobody has ' +
              'otherwise mentioned is exactly this.' },
      { art: swatch(line(C.indigo, ''), 64),
        what: '<strong>Which way a line goes is at BOTH of its ' +
              'ends.</strong> It leaves the box with the round end and ' +
              'arrives at the box with the arrowhead &mdash; so the question ' +
              'a reader actually asks, standing at one box: <em>is this line ' +
              'mine, or somebody\'s on me?</em>, is answered where they are ' +
              'standing rather than at the far end of a curve that crosses ' +
              'three others on the way. Every line here has both marks, and ' +
              'no line here is two-way: two applications that reach each ' +
              'other are drawn as two lines.' },
      { art: swatch(line(C.amber, ''), 64),
        what: '<strong>acts for &mdash; an IMPERSONATION.</strong> What came ' +
              'out names the initial identity and nothing else, so nothing ' +
              'at the far end can tell an intermediary was involved. Amber ' +
              'because this picture is the only place that fact will ever ' +
              'exist.' },
      { art: swatch(line(C.green, ''), 64),
        what: '<strong>acts for &mdash; a DELEGATION.</strong> What came out ' +
              'CARRIES the chain: an <code>act</code> claim, a composite ' +
              '<code>ActAs</code>, <code>S4U_DELEGATION_INFO</code> in the ' +
              'PAC.' },
      { art: swatch(line(C.indigo, ''), 64),
        what: '<strong>reaches &mdash; the TRUST relationship.</strong> What ' +
              'the credential is FOR: the back-end service, the ' +
              '<code>AppliesTo</code>, the audience or resource. The label ' +
              'says whose name it carries.' },
      { art: swatch(line(C.indigo, '7 4'), 64),
        what: '<strong>A broken line jumps a party nobody named.</strong> A ' +
              'forwarded ticket-granting ticket has no intermediary and ' +
              'cannot have one — the client gives it to whichever service it ' +
              'chooses and this KDC is never told which.' },
      { art: swatch(line(C.red, '5 3'), 64),
        what: '<strong>Red is a chain nothing was ever issued on.</strong> ' +
              'The tooltip carries the KDC\'s own words for why, which is ' +
              'the same sentence the client was sent.' },
      { art: swatch(line(C.grey, '4 3'), 64),
        what: '<strong>Grey and dashed, from the hexagon: this service ' +
              'ISSUED to that party.</strong> It goes to whoever ASKED — the ' +
              'intermediary where a chain has one, the initial identity ' +
              'where it does not.' }
    ];
    if (options && options.issuance) {
      items.push(
        { art: swatch(line(C.indigo, '2 3'), 64),
          what: '<strong>Dotted, into the hexagon: this person AUTHENTICATED ' +
                'here.</strong> The label is the protocol family and the ' +
                'tooltip is the method — the sign-in screen, an AS-REQ, a ' +
                'UsernameToken, a federated assertion. It is why everything ' +
                'else on the picture was allowed.' },
        { art: swatch(line(C.indigo, ''), 64),
          what: '<strong>Solid indigo: an ORDINARY GRANT, and the label is ' +
                'the exact one</strong> — <code>authorization_code</code>, ' +
                '<code>refresh_token</code>, ' +
                '<code>client_credentials</code>, with the specification ' +
                'section in the tooltip. Out of the PERSON it means a ' +
                'credential naming them went to that application; out of the ' +
                'HEXAGON it means nobody else holds it — a ' +
                '<code>client_credentials</code> token is about the client ' +
                'itself and an X509-SVID has no audience, so the subject and ' +
                'the holder are one box and there is one line rather than ' +
                'two. It takes no amber or green, because impersonation and ' +
                'delegation are properties of a delegation mechanism and a ' +
                'grant claims neither.' },
        { art: swatch(line(C.indigo, ''), 64),
          what: '<strong>Solid indigo out of an APPLICATION is that same ' +
                'relationship one step further on: what the credential it ' +
                'holds is ADDRESSED to.</strong> It is the <em>reaches</em> ' +
                'line above, said about an ordinary grant instead of about a ' +
                'delegation — an access token issued to a web front end and ' +
                'addressed to an API gateway is this service saying the ' +
                'first may reach the second in this person\'s name, with ' +
                'nothing exchanged to get there. The mechanism on the label ' +
                'is what tells the two apart: a grant, or <code>Token ' +
                'exchange</code>. The audience the token actually carries is ' +
                'in the tooltip, because the box is named after whichever ' +
                'application registered that audience. <strong>The line ' +
                'under it names the DELEGATED PERMISSIONS on that ' +
                'token</strong> — the values on its <code>scope</code> claim ' +
                'that the resource at the far end has DEFINED, which is what ' +
                'a client asks for by sending the whole permission ' +
                'identifier (the resource\'s base URI followed by the name) ' +
                'as a scope. <strong><code>default permissions</code> means ' +
                'the token named the resource and asked for none of ' +
                'them</strong>: that is what a scope naming the resource\'s ' +
                'own <code>client_id</code> produces, since that value ' +
                'becomes the audience and comes off the scope claim. It says ' +
                'what was ISSUED and not what was GRANTED — <a ' +
                'href="/admin/delegation/allowed">the configured ' +
                'register</a> is the other question, and ' +
                'in development <code>oauth2.delegatedPermissionsEnforced' +
                '</code> is off by default, so a token can carry a ' +
                'permission its client was never granted (product mode ' +
                'refuses one).' });
    }
    log.debug("Leaving AdminConsole.delegationMapKey().");
    return '<table class="key"><tr><th>Shape</th><th>What it means</th></tr>' +
      items.map(function (one) {
        return '<tr><td class="art">' + one.art + '</td><td>' + one.what +
               '</td></tr>';
      }).join('') + '</table>';
  }

  // One node, as a row of the party index under the picture. The party cell is
  // `delegationPartyCell()`'s, so a box that links to one page in the diagram
  // still offers BOTH links here — see the note in delegationNodeLook().
  delegationNodeRow(node, known, look) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.delegationNodeRow().");
    if (node.kind === 'sts') {
      log.debug("Leaving AdminConsole.delegationNodeRow().");
      return '';
    }
    const party = { key: node.key, presented: node.presented,
                    application: node.application, what: node.what };
    const roles = [];
    if (node.roles.initial) roles.push(node.roles.initial +
                                       ' × initial identity');
    if (node.roles.intermediary) roles.push(node.roles.intermediary + ' × ' +
        'intermediary');
    if (node.roles.target) roles.push(node.roles.target + ' × target');
    log.debug("Leaving AdminConsole.delegationNodeRow().");
    return '<tr>' +
      '<td>' + this.esc(look.label) +
        (look.identifier
          ? '<br><span class="state-none" title="What a protocol would have ' +
            'to present to reach this party, and that protocol\'s own word ' +
            'for it. It is the second line inside the box in the picture ' +
            'above; where the label IS the identifier, only the word is ' +
            'drawn.">' +
            this.esc(look.identifier) + '</span>'
          : '') + '</td>' +
      '<td>' + (look.shape === 'both'
                  ? 'person <strong>and</strong> application'
                  : look.shape === 'person' ? 'person'
                  : look.shape === 'application' ? 'application' : look.shape) +
        (look.dashed
          ? '<br><span class="state-none" title="Neither ou=users nor ' +
            'ou=applications holds an entry for this. The shape is the one ' +
            'its role implies.">drawn from its role &mdash; neither store ' +
            'knows it</span>'
          : '') + '</td>' +
      '<td class="who">' + this.delegationPartyCell(party, known) + '</td>' +
      '<td>' +
      (roles.join('<br>') || '<span class="state-none">&mdash;</span>') +
        (node.selfTarget
          ? '<br><span class="state-expired" title="S4U2Self is a request ' +
            'for a ticket to yourself, so the intermediary and the target ' +
            'are one party. There is no line for it in the picture because ' +
            'an arrow leaving a box and coming back is a drawing of ' +
            'nothing.">also its own target</span>'
          : '') + '</td>' +
      '<td class="num">' + this.esc(node.acts) + ' — ' +
        '<span class="state-valid">' + this.esc(node.issued) + ' ' +
          'issued</span>, ' +
        (node.refused
          ? '<span class="state-revoked">' + this.esc(node.refused) +
            ' refused</span>'
          : '<span class="state-none">0 refused</span>') + '</td>' +
      '<td>' + (node.protocols.map(this.esc.bind(this)).join('<br>') ||
                '<span class="state-none">&mdash;</span>') + '</td>' +
      '</tr>';
  }

  // One edge, as a row of the relationship index. The columns are the picture
  // read as a sentence — who, to whom, meaning what, over what, how it came out
  // — so that everything the diagram says in colour is also said in words. A
  // picture nobody can read as text is one nobody can quote in a bug report.
  delegationEdgeRow(edge, lookOf) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.delegationEdgeRow().");
    const relation = edge.relation === 'issued'
      ? '<span class="state-none" title="This service handed that party a ' +
        'credential. It goes to whoever ASKED.">issued to</span>'
      : edge.relation === 'acts-for'
        ? '<strong>acts for</strong>'
        : '<strong>reaches</strong>' +
          (edge.subject ? '<br><span class="state-none">as ' +
           this.esc(edge.subject) +
                          '</span>' : '');
    log.debug("Leaving AdminConsole.delegationEdgeRow().");
    return '<tr>' +
      '<td class="who">' + this.esc(lookOf(edge.from)) + '</td>' +
      '<td class="who">' + this.esc(lookOf(edge.to)) + '</td>' +
      '<td>' + relation +
        ((edge.skipped || []).length
          ? '<br><span class="state-expired" title="Nobody named this party ' +
            'on these acts, so the line jumps it. A forwarded ' +
            'ticket-granting ticket has no intermediary and cannot have ' +
            'one.">jumps the ' +
            this.esc(edge.skipped.join(' and ')) + '</span>'
          : '') + '</td>' +
      '<td>' + (edge.typeLabel
                  ? '<code>' + this.esc(edge.type) + '</code><br>' +
                    '<span class="state-none">' + this.esc(edge.typeLabel) +
                    '</span><br><span ' +
                    'class="state-none">' + this.esc(edge.protocol) +
                    (edge.spec ? ' &middot; ' + this.esc(edge.spec) : '') +
                    '</span>'
                  : '<span class="state-none">' +
                    this.esc((edge.protocols || []).join(', ') || '&mdash;') +
                    '</span>') +
      '</td>' +
      '<td>' + this.modeCell(edge.mode) + '</td>' +
      '<td class="num">' + this.esc(edge.acts) + ' — ' +
        '<span class="state-valid">' + this.esc(edge.issued) + ' ' +
          'issued</span>, ' +
        (edge.refused
          ? '<span class="state-revoked">' + this.esc(edge.refused) +
            ' refused</span>'
          : '<span class="state-none">0 refused</span>') + '</td>' +
      '<td class="who">' + (edge.produced.length
        ? edge.produced.map(function (one) {
            return '<code>' + self.esc(one.kind) + '</code> × ' +
                   self.esc(one.count) +
              (one.identifiers.length
                ? '<br>' + one.identifiers.map(function (id) {
                    // shortened() brings its own <code title=…> — see the note
                    // in credentialCell().
                    return self.shortened(id, 10);
                  }).join(' ') +
                  (one.moreIdentifiers ? ' <span class="state-none">+' +
                    self.esc(one.moreIdentifiers) + ' more</span>' : '')
                : '');
          }).join('<br>')
        : '<span class="state-none">&mdash;</span>') + '</td>' +
      // WHY, AND THE UNPOLICED FAMILIES ARE COLLAPSED. This is the delegation
      // page's own rule about its *Also requires* column read again: a sentence
      // that is a property of the MECHANISM and identical on every row of its
      // kind is not a column, it is a paragraph. `authorizedBy` on a WS-Trust
      // or an RFC 8693 row is exactly that — *nothing checks this, and here is
      // the paragraph* — repeated down the table it squeezed the four columns
      // that DO differ per row into shreds, which is the failure that took that
      // page from twelve columns to ten.
      //
      // So a REFUSAL prints in full, because it is specific to the act and is
      // most of what this page is for; a POLICED grant prints its attribute,
      // because that is short and names an account; and an unpoliced grant
      // becomes three words with the paragraph in the tooltip. Nothing is lost
      // — the line's own tooltip in the picture carries the same text.
      '<td>' + (edge.reason
                ? this.esc(edge.reason)
                : edge.relation === 'issued'
                  ? '<span class="state-none">&mdash;</span>'
                  : edge.policed
                    ? (edge.authorizedBy ? this.esc(edge.authorizedBy)
                                         : '<span ' +
                                           'class="state-none">&mdash;</span>')
                    : '<span class="state-expired" title="' +
                      this.esc(edge.authorizedBy || 'Nothing in this service ' +
                               'decides who may perform this act.') +
                      '">nothing checks this</span>') +
      '</td>' +
      '</tr>';
  }

  // THE SECTION SEARCHES, AS HIDDEN INPUTS FOR SOMEBODY ELSE'S FORM.
  //
  // The acts table's own filter is a GET form, and a GET form posts its own
  // fields and NOTHING else — so narrowing the table by mechanism would clear
  // every other search on the page, which is a control undoing a control the
  // reader is still using. Each search re-emits the OTHERS through its own
  // hidden inputs; this is for the forms on the page that are none of them.
  //
  // It was two names until 2026-09-01 and is four now: `permq` and `grantq`,
  // the searches over the two tables of the configured register, joined the two
  // chooser searches the day those tables were paged. The function is still
  // called chooserCarry() because sixteen call sites and one name is cheaper
  // than a rename that says nothing new — what it carries is "every search on
  // this page that is not the form asking".
  chooserCarry(query) {
    const { log, queryOne } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.chooserCarry().");
    log.debug("Leaving AdminConsole.chooserCarry().");
    return ['appq', 'appfrom', 'userq', 'userfrom', 'permq', 'grantq'].map(
        function (name) {
      const value = queryOne(query, name);
      return value === ''
        ? ''
        : '<input type="hidden" name="' + self.esc(name) + '" value="' +
          self.esc(value) +
          '">';
    }).join('');
  }

  // The search box, the scrolling pane of results and the line under it, for
  // either kind of party. Written once because it is drawn six times — the two
  // choosers on /admin/delegation, both again on /admin/delegation/map, and one
  // each on the two drill-downs — and six hand-written copies of a control with
  // an offset in it is five chances to page one list with another's parameter.
  //
  // `spec`:
  //   here        { path, query } — THE PAGE THIS IS DRAWN ON, not the page a
  //               result opens. The form submits back to here, so a search
  //               neither leaves the page nor loses the table's filter, its
  //               paging or the other chooser's search: everything in the
  //               current query rides along as hidden inputs.
  //   param       the search box's name — `appq` / `userq`
  //   fromParam   the offset's name — `appfrom` / `userfrom`
  //   label       the box's label; `placeholder` what to type in it
  //   entries     the catalogue, each { key, names, label, detail, href }
  //   selectedKey the entry this page is already showing, marked in the pane
  //   nothing     what to say when the search matched none of them
  chooserPane(spec) {
    const { log, queryOne, chooserMatches, pageParamsOf,
            queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.chooserPane(). param=" + spec.param);
    const query = (spec.here && spec.here.query) || {};
    const path = (spec.here && spec.here.path) || '';
    const wanted = queryOne(query, spec.param).trim();

    // WHERE THE READER IS STANDING, SPELT AS A FRAGMENT SO THAT SEARCHING DOES
    // NOT MOVE THEM.
    //
    // Every control in this pane is a GET that reloads the page, and a reload
    // lands at the top of the document — so a reader who had scrolled past four
    // hundred rows of the table to reach this chooser was thrown back to the
    // heading by the very click that answered them, with the results they asked
    // for now somewhere below the fold. It got worse the longer the page was,
    // which is the same complaint the sidebar had.
    //
    // A fragment on the form's own action is the fix, and there is no other one
    // available here: this console runs no script (see app.js's `script-src
    // 'none'`), so nothing can restore a scroll offset after the navigation.
    // What the fragment CAN do is put this pane back under the reader's eyes,
    // which is what they were looking at when they pressed the button.
    //
    // It works because of a detail of the HTML form algorithm that is easy to
    // doubt and was checked in a browser rather than assumed: submitting a GET
    // form replaces the action URL's QUERY and leaves its FRAGMENT alone, so
    // `action="/admin/delegation#find-appq"` arrives as
    // `/admin/delegation?appq=x#find-appq`. That is the "mutate action URL"
    // step of the HTML standard, which sets `url`'s query and touches nothing
    // else — so a hidden input cannot do this job and there is nowhere else to
    // put the fragment.
    //
    // The name is the search box's own parameter, which is what makes it unique
    // on a page that draws this control twice — `appq` and `userq` never appear
    // in one pane, and the two panes must not send each other's readers to the
    // wrong half of the page.
    const anchor = 'find-' + spec.param;
    const matched = spec.entries.filter(function (entry) {
      return chooserMatches(entry.names, wanted);
    });

    // A STALE OFFSET IS CLAMPED RATHER THAN OBEYED. `?appfrom=40` is a link the
    // reader followed when 57 matched; narrowing the search to 6 would
    // otherwise answer with an empty pane under a line saying 6 matched, which
    // reads as the search being broken by the term that worked.
    let from = parseInt(queryOne(query, spec.fromParam), 10);
    if (!isFinite(from) || from < 0 || from >= matched.length) {
      from = 0;
    }
    const shown = matched.slice(from, from + CHOOSER_HITS);

    // What every control here carries with it. Two names come OUT of it and
    // each for its own reason: the search term, because the text input re-emits
    // it and a hidden input beside it would submit the old one; and the offset,
    // because a NEW search starts at the first twenty — carrying 40 into a
    // two-hit search is the stale-offset case above, arrived at by typing
    // rather than by clicking.
    const carried = pageParamsOf(query);
    delete carried[spec.param];
    delete carried[spec.fromParam];
    const hidden = Object.keys(carried).map(function (name) {
      return '<input type="hidden" name="' + self.esc(name) + '" value="' +
             self.esc(carried[name]) + '">';
    }).join('');

    // The pager's own base keeps the search term — it is paging THESE results —
    // and overrides only the offset. An offset of nothing rather than of 0, so
    // the first page of a search is the same URL whether it was reached by
    // typing or by clicking `previous`.
    const paging = pageParamsOf(query);
    const pageLink = function (at, label, title) {
      log.debug("Entering pageLink().");
      // pageNavPair()'s own idiom, and for its reason: the control's OWN
      // parameter is the only one it sets, so the other chooser's offset and
      // the table's page stay where the reader left them.
      const move = {};
      move[spec.fromParam] = at > 0 ? at : '';
      log.debug("Leaving pageLink().");
      // The anchor for the reason above: paging the results is the same click
      // as searching them, and it is a page reload just as much.
      return '<a href="' + self.esc(path + queryWith(paging, move)) + '#' +
        self.esc(anchor) +
        '" title="' + self.esc(title) + '">' + label + '</a>';
    };

    const rows = shown.map(function (entry) {
      return '<li' + (entry.key && entry.key === spec.selectedKey
                        ? ' class="on"' : '') + '>' +
        '<a href="' + self.esc(entry.href) + '">' + self.esc(entry.label) +
        '</a>' +
        (entry.detail
          ? '<span class="hitwhat">' + self.esc(entry.detail) + '</span>' :
            '') +
        '</li>';
    }).join('');

    const pane = '<div class="chooser">' +
      (rows
        ? '<ul class="hits">' + rows + '</ul>'
        : '<p class="none">' + this.esc(spec.nothing) + '</p>') +
      '</div>';

    const noun = wanted
      ? (matched.length === 1 ? 'match' : 'matches')
      : 'in the list';
    const count = matched.length
      ? (matched.length > CHOOSER_HITS
          ? 'Showing ' + (from + 1) + '&ndash;' + (from + shown.length) +
            ' of ' + matched.length + ' ' + noun + '. '
          : matched.length + ' ' + noun + '. ')
      : '';
    const more = [];
    if (from > 0) {
      more.push(pageLink(from - CHOOSER_HITS, '&larr; previous ' + CHOOSER_HITS,
        'The twenty before these'));
    }
    if (from + CHOOSER_HITS < matched.length) {
      more.push(pageLink(from + CHOOSER_HITS,
                         'next ' + CHOOSER_HITS + ' &rarr;',
        'The twenty after these'));
    }

    log.debug("Leaving AdminConsole.chooserPane(). " + matched.length + " " +
      "matched, " +
              shown.length + " shown from " + from + ".");
    return '<form method="get" id="' + this.esc(anchor) +
           '" class="finder" action="' +
      this.esc(path) + '#' + this.esc(anchor) + '">' +
      '<div class="formrow">' + hidden +
        '<label for="' + this.esc(spec.param) + '">' + this.esc(spec.label) +
        '</label><input type="text" id="' + this.esc(spec.param) + '" name="' +
          this.esc(spec.param) + '" size="32" value="' + this.esc(wanted) +
          '" placeholder="' + this.esc(spec.placeholder) + '">' +
        '<button class="secondary">Search</button>' +
        (wanted
          ? ' <a href="' + this.esc(path + queryWith(carried, {})) + '#' +
            this.esc(anchor) +
            '">clear</a>'
          : '') +
      '</div></form>' +
      pane +
      (count || more.length
        ? '<p class="note">' + count + more.join(' &middot; ') + '</p>'
        : '');
  }

  // ---------------------------------------------------------------------------
  // THE CAEP SESSION CHOOSER — search by PERSON, pick one of their live
  // sessions.
  //
  // **IT WAS A `<select name="session_id">` UNTIL 2026-09-03**, and the
  // argument for replacing it is chooserPane()'s own, one register further on:
  // a control on this page must be the same size whatever is behind it, and
  // this register grows BY ONE ROW PER SIGN-IN for the life of the process and
  // never shrinks — `caep.ts` keeps a row after the session has been signed
  // out, deliberately, because the row is the evidence that it existed and was
  // revoked. So a console left running for an afternoon of testing had a
  // dropdown of several hundred options, sorted by nothing a reader knows, each
  // labelled with a 24-character random identifier.
  //
  // **THE SEARCH IS OVER THE PERSON AND THE RESULTS ARE SESSIONS**, which is
  // the asymmetry worth stating because it is what makes the control useful.
  // Nobody knows a session identifier by heart — it is random and it is the
  // thing they came here to find — but everybody knows who they signed in as.
  // So `names` holds the username and the subject, and the LABEL holds the
  // session, with the protocol that minted it and the state beside it.
  //
  // **ONLY LIVE SESSIONS ARE OFFERED, and that is a deliberate narrowing.** A
  // revoked row stays in the register and stays on /admin/caep-sessions, where
  // it is evidence; it is not offered HERE because this form emits an event
  // ABOUT a session, and the model's one hard refusal is a `session-presented`
  // about a session already revoked. Offering rows that the very next click
  // would be refused for is a control that invites a mistake. The note under
  // the pane says so and points at the page that still lists them.
  //
  // Paging is CHOOSER_HITS — the same twenty every other chooser here uses, and
  // the same clamping of a stale offset, because it is the same function.
  // ---------------------------------------------------------------------------
  caepSessionChooser(here, sessions, selectedId) {
    const { log, pageParamsOf, queryWith } = this.deps;
    log.debug("Entering AdminConsole.caepSessionChooser().");
    const query = (here && here.query) || {};
    const carry = pageParamsOf(query);
    delete carry.session;
    const live = (sessions || []).filter(function (row) {
      return String(row.state || '') !== 'revoked';
    });
    const entries = live.map(function (row) {
      const who = String(row.username || row.sub || '(unnamed)');
      const id = String(row.sessionId || '');
      return {
        key: id,
        // SEARCHED BY PERSON. The session identifier is in here too so that a
        // reader who has one — from a log, from an event they are chasing — can
        // paste it, which is the other half of how this control gets used.
        names: [who, String(row.sub || ''), id],
        label: who + ' — ' + id,
        detail: (row.protocol ? row.protocol + ', ' : '') +
          String(row.state || 'established') +
          (row.total ? ', ' + row.total + ' event(s) sent' : ', nothing sent'),
        href: '/admin/caep' + queryWith(carry, { session: id }) + '#find-sessq'
      };
    });
    log.debug("Leaving AdminConsole.caepSessionChooser(). " + entries.length +
              " live session(s) of " + (sessions || []).length + ".");
    return this.chooserPane({
      here: here, param: 'sessq', fromParam: 'sessfrom',
      label: 'Find a person',
      placeholder: 'part of a username, a subject or a session id',
      entries: entries, selectedKey: selectedId,
      nothing: 'No live session belongs to anybody matching that. This list ' +
        'holds the sessions this service still considers OPEN — a revoked ' +
        'one stays on <a href="/admin/caep-sessions">the sessions page</a> ' +
        'as evidence and is deliberately not offered here, because the state ' +
        'machine refuses a session-presented about a session that has ended.'
    });
  }

  // ---------------------------------------------------------------------------
  // THE RISC ACCOUNT CHOOSER, and the one place it is deliberately more
  // permissive than the CAEP session chooser beside it.
  //
  // That control offers only LIVE sessions, because emitting about a revoked
  // one is the CAEP register's single hard refusal and a chooser that offered
  // rows the next click would refuse is a control inviting a mistake. **This
  // one offers every row, including purged accounts**, and the reason is what
  // the two events mean: a session that has ended cannot be presented, and an
  // account that has been purged can perfectly well have a credential of its
  // discovered in a breach corpus afterwards. Only `account-enabled` on a
  // purged account is refused, and that is one event type rather than a whole
  // row.
  //
  // **AND THE FORM ACCEPTS A NAME THAT IS IN NO ROW AT ALL**, which the CAEP
  // form does not. That is the specifications' difference and not an
  // inconsistency: a CAEP event names a SESSION, and a session identifier this
  // service never minted is one it can compose no subject from. A RISC event
  // names a PERSON, this service can name any person, and RISC is aimed ACROSS
  // providers — so the account a receiver is being warned about is usually one
  // it has never seen. This pane is therefore a convenience over the text field
  // below it rather than the only way in.
  //
  // The search reaches `formerIdentifiers` for the reason riscAccountsState()'s
  // does: the address a reader is holding is routinely the one the account no
  // longer has, because `identifier-changed` is an event about the key itself.
  // ---------------------------------------------------------------------------
  riscAccountChooser(here, accounts, selectedId) {
    const { log, pageParamsOf, queryWith } = this.deps;
    log.debug("Entering AdminConsole.riscAccountChooser().");
    const query = (here && here.query) || {};
    const carry = pageParamsOf(query);
    delete carry.acctq2;
    const entries = (accounts || []).map(function (row) {
      const who = String(row.username || row.accountId || '(unnamed)');
      const id = String(row.accountId || '');
      return {
        key: id,
        names: [who, id, String(row.sub || ''), String(row.email || '')]
          .concat(row.formerIdentifiers || []),
        label: who + (row.email && row.email !== who ? ' — ' + row.email : ''),
        detail: String(row.lifecycle || 'active') + ', ' +
          String(row.optOut || 'opt-in') +
          (row.total ? ', ' + row.total + ' event(s) sent' : ', nothing sent') +
          (row.suppressed ? ', ' + row.suppressed + ' suppressed' : ''),
        href: '/admin/risc' + queryWith(carry, { acctq2: id }) + '#find-acctq2'
      };
    });
    log.debug("Leaving AdminConsole.riscAccountChooser(). " + entries.length +
              " account(s).");
    return this.chooserPane({
      here: here, param: 'acctq2', fromParam: 'acctfrom',
      label: 'Find an account',
      placeholder: 'part of a username, a subject or an email address',
      entries: entries, selectedKey: selectedId,
      nothing: 'No tracked account matches that. This register holds one row ' +
        'per account this service has been told anything about &mdash; ' +
        'including ones that no longer exist &mdash; and it will be empty ' +
        'until the directory changes or somebody emits an event by hand. You ' +
        'can type an account name into the form below regardless: a RISC ' +
        'event names a person, and this service can name any person at all.'
    });
  }

  // ---------------------------------------------------------------------------
  // THE APPLICATION CHOOSER, DRAWN IN THREE PLACES AND THEREFORE A FUNCTION.
  //
  // It is on /admin/delegation (where somebody looking at the table wants to
  // pivot), on /admin/delegation/application with nothing selected (where it IS
  // the page), and again under a selected application (so that comparing two is
  // one click rather than two). Three copies of a `<select>` built from the
  // same list would be three chances for one of them to offer an option the
  // route cannot resolve.
  //
  // **THE OPTION'S VALUE IS A SPELLING AND NOT THE NORMALISED KEY**,
  // deliberately. The route normalises whatever it is given (see its header),
  // so both work — and the identifier is what a reader recognises in the
  // address bar and in a link they paste into a ticket. A URL carrying
  // `HTTP%2Fbackend%40EXAMPLE.COM` is one somebody can check; one carrying a
  // key they have never seen is not.
  //
  // **IT WAS A `<select>` UNTIL 2026-08-26 AND IS A SEARCH NOW** —
  // chooserPane() above carries the whole argument, including why it cannot be
  // a type-ahead. What survives of the old one is the sentence that made it a
  // select rather than a wall of links: a control here must be the same size
  // whatever the register holds, because the table it sits above is what the
  // reader came for. The pane scrolls instead. The full list IS still drawn as
  // links, below, where it is the content rather than a control.
  // ---------------------------------------------------------------------------
  // `carry` is the delegation table's own filter, spelt into every RESULT's
  // href for the reason perPageForm() gives about hidden inputs: without it a
  // click here quietly empties the breadcrumb's way back to the list the reader
  // came from. `here` is the page this control is DRAWN on, which is where its
  // own form submits — a different page from the one a result opens, and
  // conflating the two is what a search added to a chooser gets wrong.
  delegationApplicationChooser(catalogue, selectedKey, carry, here) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.delegationApplicationChooser().");
    if (!catalogue.length) {
      log.debug("Leaving AdminConsole.delegationApplicationChooser(). " +
                "Nothing to choose from.");
      return this.note('<strong>No delegation held here names an application ' +
        'yet</strong>, so there is nothing to choose between. An application ' +
        'arrives in this list the moment something delegates through it or ' +
        'to it: an RFC 8693 <code>audience</code> or the ' +
        '<code>client_id</code> that performed the exchange, a WS-Trust ' +
        '<code>AppliesTo</code>, or the service principal a Kerberos S4U ' +
        'request asked for a ticket to.');
    }
    const entries = catalogue.map(function (entry) {
      const roles = [];
      if (entry.roles.intermediary) roles.push(entry.roles.intermediary + ' ' +
          'as the intermediary');
      if (entry.roles.target) roles.push(entry.roles.target + ' as the target');
      if (entry.roles.initial) roles.push(entry.roles.initial + ' as the ' +
          'initial identity');
      return {
        key: entry.key,
        // EVERY spelling is searched and the newest is shown, which is the same
        // split the option had — see chooserMatches() for why the difference
        // matters more here than anywhere else on the page.
        names: [entry.identifier].concat(entry.spellings || []),
        label: entry.identifier,
        detail: entry.acts + ' act(s): ' + roles.join(', '),
        // THE IDENTIFIER AND NOT THE KEY, for the reason above: the route
        // normalises either, and the identifier is what a reader recognises in
        // the address bar and in a link they paste into a ticket.
        href: '/admin/delegation/application' +
              queryWith(carry || {}, { application: entry.identifier })
      };
    });
    log.debug("Leaving AdminConsole.delegationApplicationChooser(). " +
              catalogue.length +
              " application(s) to search.");
    return this.chooserPane({
      here: here, param: 'appq', fromParam: 'appfrom',
      label: 'Find an application',
      placeholder: 'part of a client_id, an SPN or an audience',
      entries: entries, selectedKey: selectedKey,
      nothing: 'No application any act named matches that. This list holds ' +
        'what some delegation actually presented, spellings and all, so a ' +
        'name that is in the acts table above and not in here is one this ' +
        'register filed as a person rather than as an application.'
    });
  }

  // The same list as CONTENT: every application some act named, with what it
  // did and the way in. It is not the same as `/admin/applications` and the
  // page says so — that page is the REGISTRY, which holds what this service has
  // been asked about; this is what has actually delegated, which can name
  // something the registry has never seen.
  delegationApplicationTable(catalogue, known, carry) {
    const { log, applications, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.delegationApplicationTable().");
    const rows = catalogue.map(function (entry) {
      const roles = [];
      if (entry.roles.intermediary) {
        roles.push(self.esc(entry.roles.intermediary) + ' × intermediary');
      }
      if (entry.roles.target) {
        roles.push(self.esc(entry.roles.target) + ' × target');
      }
      if (entry.roles.initial) {
        roles.push(self.esc(entry.roles.initial) + ' × initial identity');
      }
      const registered = entry.spellings.filter(function (spelling) {
        return !!applications.get(spelling);
      }).length > 0;
      return '<tr>' +
        '<td class="who"><a href="' + self.esc('/admin/delegation/application' +
          queryWith(carry || {}, { application: entry.identifier })) +
        '"><code>' +
          self.esc(entry.identifier) + '</code></a>' +
          (entry.spellings.length > 1
            ? '<br><span class="state-none" title="' +
              self.esc(entry.spellings.join(', ')) + '">' +
              self.esc(entry.spellings.length) +
              ' spellings, collapsed</span>'
            : '') +
          (registered ? ''
            : '<br><span class="state-none" title="No entry under ' +
              'ou=applications names this. The registry holds what this ' +
              'service has been ASKED ABOUT; a delegation can name something ' +
              'nobody has otherwise mentioned.">not in the registry</span>') +
          (entry.identityKey
            ? '<br>' + self.usersPageCell(entry.identityKey, known)
            : '') + '</td>' +
        '<td>' +
        (roles.join('<br>') || '<span class="state-none">&mdash;</span>') +
          '</td>' +
        '<td class="num">' + self.esc(entry.acts) + ' — ' +
          '<span class="state-valid">' + self.esc(entry.issued) + ' ' +
            'issued</span>, ' +
          (entry.refused
            ? '<span class="state-revoked">' + self.esc(entry.refused) + ' ' +
                'refused</span>'
            : '<span class="state-none">0 refused</span>') + '</td>' +
        '<td class="num">' + self.esc(entry.credentials) + '</td>' +
        '<td class="num">' + self.esc(entry.chains) + '</td>' +
        '<td>' + self.esc(self.whenText(entry.lastAt)) + '</td>' +
        '</tr>';
    }).join('');
    log.debug("Leaving AdminConsole.delegationApplicationTable(). " +
              catalogue.length + " " +
        "row(s).");
    return '<table><tr><th>Application</th><th>Roles it has played</th><th>' +
      'Acts</th><th>Credentials</th><th>Relationships</th><th>Last ' +
      'seen</th></tr>' +
      (rows || '<tr><td colspan="6">No delegation held here names an ' +
       'application.</td></tr>') + '</table>';
  }

  // ---------------------------------------------------------------------------
  // THE THREE THINGS EVERY PICTURE PAGE NEEDS, EXTRACTED BECAUSE THERE ARE NOW
  // THREE OF THEM.
  //
  // `/admin/delegation/map` drew the whole graph; `/admin/delegation/chain`
  // draws ONE relationship and `/admin/delegation/application` draws one
  // application's. They differ in which acts they pass to `delegation.graph()`
  // and in nothing else, so the three functions below are shared rather than
  // copied.
  //
  // That is the same argument `delegationView()` makes one layer up and it is
  // worth restating here because the failure it prevents is quiet: three copies
  // of the label rule would be three pages that disagree about what a box is
  // CALLED, and a reader comparing the whole picture with one chain's would
  // have no way to tell that from two boxes that really are different parties.
  // ---------------------------------------------------------------------------

  // What every box is called and how it is drawn, worked out once per page.
  // `labelOf` is separate because a `reaches` line names a party that is
  // NEITHER of its ends — see the map route's call — and the renderer has no
  // `resolve()` answer for it.
  delegationLooks(graph, known) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.delegationLooks().");
    const looks = {};
    graph.nodes.forEach(function (node) {
      looks[node.id] = self.delegationNodeLook(node, known);
    });
    log.debug("Leaving AdminConsole.delegationLooks(). " + graph.nodes.length +
              " box(es).");
    return {
      looks: looks,
      resolve: function (node) {
        log.debug("Entering resolve().");
        log.debug("Leaving resolve().");
        return looks[node.id];
      },
      labelOf: function (id) {
        log.debug("Entering labelOf().");
        log.debug("Leaving labelOf().");
        return looks[id] ? looks[id].label : id;
      }
    };
  }

  // One credential that came out of an act in the picture. `extra` is an
  // optional leading cell — the application page uses it for the ROLE that
  // application played in the act that produced this, which is the whole
  // question that page answers and is a fact about the act rather than about
  // the credential.
  delegationTokenRow(token, labelOf, extra?) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.delegationTokenRow().");
    log.debug("Leaving AdminConsole.delegationTokenRow().");
    return '<tr>' +
      '<td class="num">' + this.esc(token.seq) + '</td>' +
      '<td>' + this.esc(this.whenText(token.at)) + '</td>' +
      (extra === undefined ? '' : '<td>' + extra + '</td>') +
      '<td class="who"><code>' + this.esc(token.kind) + '</code>' +
        (token.identifier
          ? '<br>' + this.shortened(token.identifier, 14)
          : '<br><span class="state-none" title="A Kerberos ticket has no ' +
            'identifier this service could quote — there is no jti and no ' +
            'AssertionID in one.">no identifier</span>') +
        (token.note ? '<br><span class="state-none">' + this.esc(token.note) +
                      '</span>' : '') + '</td>' +
      '<td class="who">' + this.esc(labelOf(token.subject) ||
        '<span class="state-none">&mdash;</span>') + '</td>' +
      '<td class="who">' + (token.actor ? this.esc(labelOf(token.actor))
        : '<span class="state-none" title="A forwarded ticket-granting ' +
          'ticket has no intermediary this KDC was told ' +
          'about.">&mdash;</span>') +
      '</td><td ' +
      'class="who">' + (token.target ? this.esc(labelOf(token.target))
        : '<span class="state-none">&mdash;</span>') + '</td>' +
      '<td><code>' + this.esc(token.type) + '</code><br>' +
        '<span class="state-none">' + this.esc(token.protocol) +
        '</span></td>' +
      '</tr>';
  }

  // The drawing itself, and the two links under it. `path` and `params` are the
  // route's own, because the document-on-its-own link has to come back to the
  // page it was offered on carrying whatever selected these acts — a `chain=`
  // or an `application=`, not the map's filters.
  //
  // The SVG document is rendered a SECOND time with `links: false` rather than
  // the page's markup being reused, and that is not waste: the standalone
  // document carries no links deliberately (see the map route's header —
  // `app.js` rewrites root-relative hrefs into the current realm on the way out
  // of a `text/html` response only, so a link inside an `image/svg+xml` body
  // would silently leave the realm, and in a saved file it would point at
  // somebody's own machine).
  delegationDrawing(graph, look, path, params, label) {
    const { log, delegationMap, queryWith } = this.deps;
    log.debug("Entering AdminConsole.delegationDrawing().");
    const drawn = delegationMap.render(graph, {
      resolve: look.resolve, labelOf: look.labelOf,
      links: true, id: 'delmap', label: label
    });
    const html = '<div class="diagram">' + drawn.svg + '</div>' +
      this.note(drawn.width + '&times;' + drawn.height + ' — ' +
      '<a href="' + this.esc(path + queryWith(params, { format: 'svg' })) +
      '">the document on its own</a> (SVG, no links in it), or ' +
      '<a href="' + this.esc(path + queryWith(params, { format: 'json' })) +
      '">the graph as JSON</a>.' +
      (drawn.failed ? ' <span class="state-revoked">The layout failed: ' +
        this.esc(drawn.failed) + '</span>' : ''));
    log.debug("Leaving AdminConsole.delegationDrawing(). " + drawn.width + "x" +
              drawn.height +
              ".");
    return { drawn: drawn, html: html };
  }

  // `?format=svg` — the document alone. Every picture page answers it the same
  // way, so the answer is one function: a fourth copy of "render it again
  // without links, set no-store, send it as image/svg+xml" is a fourth chance
  // for one of them to keep the links.
  sendDelegationSvg(res, graph, look, label) {
    const { log, delegationMap } = this.deps;
    log.debug("Entering AdminConsole.sendDelegationSvg().");
    const bare = delegationMap.render(graph, {
      resolve: look.resolve, labelOf: look.labelOf,
      links: false, id: 'delmap', label: label
    });
    res.set('Cache-Control', 'no-store').type('image/svg+xml').send(bare.svg);
    log.debug("Leaving AdminConsole.sendDelegationSvg(). " + bare.svg.length +
              " bytes.");
  }

  // What a ROLE is called on this console, off `delegation.ROLES` rather than
  // out of a list here — the same rule the mechanism filter follows. A role
  // that existed in the store and was unnamed on a page would be a blank cell.
  delegationRoleLabel(role) {
    const { log, delegation } = this.deps;
    log.debug("Entering AdminConsole.delegationRoleLabel().");
    const entry = delegation.ROLES.filter(function (one) {
      return one.role === role;
    })[0];
    log.debug("Leaving AdminConsole.delegationRoleLabel().");
    return entry ? entry.label : role;
  }

  // The roles an application played in one act, as cells. Two is possible and
  // is S4U2Self — the requester asks for a ticket to ITSELF, so it is the
  // intermediary and the target of the same act.
  delegationRoleCell(roles) {
    const { log, delegation } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.delegationRoleCell().");
    if (!roles || !roles.length) {
      log.debug("Leaving AdminConsole.delegationRoleCell().");
      return '<span class="state-none">&mdash;</span>';
    }
    log.debug("Leaving AdminConsole.delegationRoleCell().");
    return roles.map(function (role) {
      const entry = delegation.ROLES.filter(function (one) {
        return one.role === role;
      })[0];
      return '<span title="' + self.esc(entry ? entry.what : '') + '">' +
             self.esc(entry ? entry.label : role) + '</span>';
    }).join('<br>');
  }

  // ---------------------------------------------------------------------------
  // THE PERSON CHOOSER, AND IT IS THE APPLICATION CHOOSER'S ARGUMENT MADE AGAIN
  // RATHER THAN COPIED.
  //
  // Drawn in three places for the same reason that one is: on /admin/delegation
  // (where somebody looking at the table wants to pivot), on
  // /admin/delegation/map (where the ask came from — *filter the picture by a
  // person the way it filters by an application*), and on the person's own page
  // (so comparing two is one click rather than two).
  //
  // **THE OPTION'S VALUE IS THE NORMALISED KEY AND NOT A SPELLING**, which is
  // the one place this differs from the application chooser next door, and the
  // difference is the whole reason the two lists are keyed differently
  // (`delegation.js`'s `identityList()` header argues it). An application is
  // chosen by the identifier a protocol NAMED, because that is what a reader
  // recognises in a URL. A person has no such identifier: they arrive as
  // `alice`, as `alice@STS.MOCK` and as `urn:uuid:<entryUUID>`, and the console
  // has filed all three under one name — which is the name on /admin/users, on
  // /admin/audit and in the directory. Putting a spelling in the URL would make
  // a link from this console disagree with every other link in it. The route
  // normalises whatever it is given anyway, so a hand-written
  // `?user=alice@REALM` still lands on the right page.
  //
  // **IT OFFERS IDENTITIES NOTHING WAS EVER ISSUED TO, AND THAT IS THE POINT.**
  // `userGraph.userList()` unions the identity register with the delegation
  // register, so somebody who has never signed in here is in this list if an
  // S4U2Self or an OnBehalfOf named them — which is the case worth finding, and
  // exactly the case a chooser built from /admin/users alone would hide.
  // ---------------------------------------------------------------------------
  delegationUserChooser(catalogue, selectedKey, carry, here) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.delegationUserChooser().");
    if (!catalogue.length) {
      log.debug("Leaving AdminConsole.delegationUserChooser(). Nothing to " +
                "choose from.");
      return this.note('<strong>Nothing has authenticated here and no ' +
        'delegation names anybody</strong>, so there is nobody to choose. An ' +
        'identity arrives in this list the moment a credential is accepted ' +
        'in any of the sixteen families, the moment a token or an assertion ' +
        'is issued naming somebody, or the moment a delegation names them — ' +
        'including one they were never present for.');
    }
    const entries = catalogue.map(function (entry) {
      const facts = [];
      if (entry.authentications) facts.push(entry.authentications +
                                            ' sign-in(s)');
      if (entry.tokens.issued) facts.push(entry.tokens.issued + ' token(s)');
      if (entry.artifacts) facts.push(entry.artifacts + ' artifact(s)');
      if (entry.acts) facts.push(entry.acts + ' delegation act(s)');
      return {
        key: entry.key,
        // The key, the spelling this console shows, and every OTHER spelling
        // the same person arrived under. Searching the key alone would answer
        // "nobody matches" to `alice@STS.MOCK`, which is the form a Kerberos
        // row on this very page prints.
        names: [entry.key, entry.presented].concat(entry.forms || []),
        label: entry.key + (entry.isClient ? ' (a client)' : ''),
        detail: facts.length ? facts.join(', ') : 'nothing yet',
        // THE NORMALISED KEY AND NOT A SPELLING, which is the one place this
        // differs from the application chooser — the header above argues it.
        href: '/admin/delegation/user' +
              queryWith(carry || {}, { user: entry.key })
      };
    });
    log.debug("Leaving AdminConsole.delegationUserChooser(). " +
              catalogue.length +
              " identity(ies) to search.");
    return this.chooserPane({
      here: here, param: 'userq', fromParam: 'userfrom',
      label: 'Find a person',
      placeholder: 'part of a username, a principal or a subject',
      entries: entries, selectedKey: selectedKey,
      nothing: 'Nobody here matches that. This list is the identity register ' +
        'UNIONED with everybody a delegation named, so a name that is on the ' +
        'page above and not in here reached this service as an application ' +
        'rather than as a person.'
    });
  }

  // The same list as CONTENT. It is not /admin/users and the page says so: that
  // page is the identity register, and this one holds everybody that register
  // knows PLUS everybody a delegation named who has never been near this
  // service. The `Where from` column is the only reason to draw it rather than
  // link to the users page, and it is the column worth reading.
  delegationUserTable(catalogue, known, carry) {
    const { log, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.delegationUserTable(). " +
              catalogue.length + " row(s).");
    const rows = catalogue.map(function (entry) {
      const where = [];
      if (entry.authenticated) {
        where.push('<span class="state-valid" title="A credential was ' +
          'accepted for this name here, so the identity register has a row ' +
          'for them.">authenticated here</span>');
      }
      if (entry.delegated) {
        where.push('<span class="state-expired" title="Some delegation act ' +
          'names them, in one of the three roles. That does NOT mean they ' +
          'were present — S4U2Self and OnBehalfOf name somebody who proved ' +
          'nothing.">named in a delegation</span>');
      }
      if (!entry.authenticated) {
        where.push('<span class="state-none" title="Nothing has ever ' +
          'presented a credential under this name in this process. Something ' +
          'was issued in it, or somebody delegated using it, which is ' +
          'exactly the state worth noticing.">never authenticated here</span>');
      }
      return '<tr>' +
        '<td class="who"><a href="' + self.esc('/admin/delegation/user' +
          queryWith(carry || {}, { user: entry.key })) + '"><code>' +
          self.esc(entry.key) + '</code></a>' +
          (entry.isClient
            ? '<br><span class="state-none" title="Something authenticated ' +
              'as this name and said it was a client rather than a person — ' +
              'the client_credentials grant is the usual way.">a client, not ' +
              'a person</span>'
            : '') +
          '<br>' + self.usersPageCell(entry.key, known) + '</td>' +
        '<td>' + where.join('<br>') + '</td>' +
        '<td class="num">' + self.esc(entry.authentications) + '</td>' +
        '<td class="num">' + self.esc(entry.tokens.issued) + ' — ' +
          '<span class="state-valid">' + self.esc(entry.tokens.valid) +
        ' valid</span>' +
          (entry.tokens.revoked
            ? ', <span class="state-revoked">' +
              self.esc(entry.tokens.revoked) +
              ' revoked</span>' : '') + '</td>' +
        '<td class="num">' + self.esc(entry.artifacts) + '</td>' +
        '<td class="num">' + (entry.acts
          ? self.esc(entry.acts) + ' — <span class="state-valid">' +
            self.esc(entry.issued) +
            ' issued</span>' + (entry.refused
              ? ', <span class="state-revoked">' + self.esc(entry.refused) +
                ' refused</span>' : '')
          : '<span class="state-none">0</span>') + '</td>' +
        '<td>' + (entry.protocols.map(self.esc.bind(self)).join('<br>') ||
                  '<span class="state-none">&mdash;</span>') + '</td>' +
        '<td>' + self.esc(self.whenText(entry.lastAt)) + '</td>' +
        '</tr>';
    }).join('');
    log.debug("Leaving AdminConsole.delegationUserTable().");
    return '<table><tr><th>Identity</th><th>Where from</th><th>Sign-ins</th>' +
      '<th>Tokens</th><th>Artifacts</th><th>Delegation acts</th>' +
      '<th>Protocols</th><th>Last seen</th></tr>' +
      (rows || '<tr><td colspan="8">Nobody has authenticated here and no ' +
       'delegation names anybody.</td></tr>') + '</table>';
  }

  // WHAT ISSUED ONE CREDENTIAL, as a cell, and this is the column the whole
  // page was asked for: *label exactly what OAuth2 grant or OIDC authentication
  // flow was used, if not a delegation call.*
  //
  // Three states rather than two, and the third is the one worth keeping apart.
  // A JWT that states a grant gets the grant, with the OIDC name for the same
  // exchange beside it where OpenID Connect gives it one — an *Authorization
  // Code grant* and an *Authorization Code Flow* are one thing under two
  // vocabularies, and somebody debugging an OIDC client is looking for the
  // second while the token endpoint is answering the first. An ARTIFACT gets
  // the mechanism its own specification names, because a SAML assertion was
  // never issued by an OAuth grant and borrowing the word would be a small lie.
  // And a JWT that states NOTHING says so in as many words: `signJwt()` is
  // reached from outside the token endpoint by WS-Trust's JWT token type and by
  // the credential issuer, and an empty cell there would read as a recording
  // failure.
  userFlowCell(credential) {
    const { log, userGraph } = this.deps;
    log.debug("Entering AdminConsole.userFlowCell().");
    if (credential.flowStated) {
      log.debug("Leaving AdminConsole.userFlowCell().");
      return '<code>' + this.esc(credential.flow) + '</code><br>' +
        '<strong>' + this.esc(credential.flowLabel) + '</strong>' +
        (credential.flowOidc && credential.flowOidc !== credential.flowLabel
          ? '<br><span class="state-none" title="OpenID Connect\'s name for ' +
            'the same exchange. One thing, two vocabularies.">OIDC: ' +
            this.esc(credential.flowOidc) + '</span>'
          : '') +
        (credential.flowSpec
          ? '<br><span class="state-none">' + this.esc(credential.flowSpec) +
            '</span>'
          : '');
    }
    if (credential.family !== 'token') {
      log.debug("Leaving AdminConsole.userFlowCell().");
      return '<strong>' + this.esc(credential.flowLabel) + '</strong>' +
        (credential.flowProtocol
          ? '<br><span class="state-none">' +
            this.esc(credential.flowProtocol) +
            (credential.flowSpec ? ' &middot; ' +
             this.esc(credential.flowSpec) : '') +
            '</span>'
          : '') +
        '<br><span class="state-none" title="A SAML assertion, a Kerberos ' +
        'ticket and an SVID are issued by protocols that have never heard of ' +
        'an OAuth grant. The mechanism named here is their own ' +
        'specification\'s.">not an OAuth grant</span>';
    }
    log.debug("Leaving AdminConsole.userFlowCell().");
    return '<span class="state-none" title="' +
      this.esc(userGraph.FLOW_NOT_STATED.what) + '">no grant stated</span>';
  }

  // One credential from the ISSUED register, as a row. `back` carries the list
  // the reader came from, so the revoke button on a token returns them to it —
  // the same `carryBack` rule every form on a drill-down follows.
  userCredentialRow(credential, known) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.userCredentialRow().");
    const holder = credential.holder
      ? this.delegationPartyCell({ key: '', presented: '',
                                   application: credential.holder, what: '' },
                                   known)
      : '<span class="state-none" title="Nothing holds this one. An ' +
        'X509-SVID has no audience, and a token can be minted with no ' +
        'client_id — this service issued it and there is no second party to ' +
        'draw.">&mdash;</span>';
    log.debug("Leaving AdminConsole.userCredentialRow().");
    return '<tr>' +
      '<td>' + this.esc(this.whenText(credential.at)) + '</td>' +
      '<td class="who"><code>' + this.esc(credential.kind) + '</code>' +
        (credential.identifier
          ? '<br>' + this.shortened(credential.identifier, 14)
          : '<br><span class="state-none" title="A Kerberos ticket has no ' +
            'identifier this service could quote — there is no jti and no ' +
            'AssertionID in one.">no identifier</span>') +
        (credential.detail
          ? '<br><span class="state-none">' + this.esc(credential.detail) +
            '</span>'
          : '') + '</td>' +
      '<td>' + this.userFlowCell(credential) + '</td>' +
      '<td class="who">' + holder + '</td>' +
      '<td class="' + this.stateClass(credential.state) + '">' +
      this.esc(credential.state) +
        '</td>' +
      '<td>' + (credential.sessionId
        ? this.shortened(credential.sessionId, 10)
        : '<span class="state-none" title="No browser sign-on session was ' +
          'stated. That is true of every direct grant — client_credentials, ' +
          'password, the pre-authorized code, a token exchange — and of ' +
          'everything issued outside the token ' +
          'endpoint.">none</span>') + '</td>' +
      '</tr>';
  }

  // One box on the person's picture. `delegationNodeRow()`'s columns plus the
  // two this page adds, rather than that function with a flag: half its columns
  // are about ACTS, and a box that received four tokens and took part in no
  // delegation would otherwise be a row of zeroes with the interesting number
  // nowhere on it.
  userNodeRow(node, known, look) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.userNodeRow().");
    if (node.kind === 'sts') {
      log.debug("Leaving AdminConsole.userNodeRow().");
      return '';
    }
    const party = { key: node.key, presented: node.presented,
                    application: node.application, what: node.what };
    const roles = [];
    if (node.roles.initial) roles.push(node.roles.initial +
                                       ' × initial identity');
    if (node.roles.intermediary) roles.push(node.roles.intermediary + ' × ' +
        'intermediary');
    if (node.roles.target) roles.push(node.roles.target + ' × target');
    log.debug("Leaving AdminConsole.userNodeRow().");
    return '<tr>' +
      '<td>' + this.esc(look.label) +
        (node.isSubject
          ? '<br><span class="state-valid" title="This is the person this ' +
            'page is about. Every line on the picture starts or ends ' +
            'here.">this page</span>'
          : '') +
        (look.identifier
          ? '<br><span class="state-none" title="What a protocol would have ' +
            'to present to reach this party, and that protocol\'s own word ' +
            'for it. It is the second line inside the box in the picture ' +
            'above; where the label IS the identifier, only the word is ' +
            'drawn.">' +
            this.esc(look.identifier) + '</span>'
          : '') + '</td>' +
      '<td>' + (look.shape === 'both'
                  ? 'person <strong>and</strong> application'
                  : look.shape === 'person' ? 'person'
                  : look.shape === 'application' ? 'application' : look.shape) +
        (look.dashed
          ? '<br><span class="state-none" title="Neither ou=users nor ' +
            'ou=applications holds an entry for this. The shape is the one ' +
            'its role implies.">drawn from its role &mdash; neither store ' +
            'knows it</span>'
          : '') + '</td>' +
      '<td class="who">' + this.delegationPartyCell(party, known) + '</td>' +
      '<td class="num">' + (node.credentials
        ? '<strong>' + this.esc(node.credentials) + '</strong>'
        : '<span class="state-none">0</span>') + '</td>' +
      '<td>' + (node.flows.map(this.esc.bind(this)).join('<br>') ||
                '<span class="state-none">&mdash;</span>') + '</td>' +
      '<td>' + (roles.join('<br>') || '<span class="state-none" title="This ' +
        'box is in no delegation at all — it holds credentials from an ' +
        'ordinary grant. The delegation roles are a fact about the OTHER ' +
        'register.">not in a delegation</span>') + '</td>' +
      '<td class="num">' + (node.acts
        ? this.esc(node.acts) + ' — <span class="state-valid">' +
          this.esc(node.issued) +
          ' issued</span>' + (node.refused
            ? ', <span class="state-revoked">' + this.esc(node.refused) + ' ' +
                'refused</span>'
            : '')
        : '<span class="state-none">0</span>') + '</td>' +
      '<td>' + (node.protocols.map(this.esc.bind(this)).join('<br>') ||
                '<span class="state-none">&mdash;</span>') + '</td>' +
      '</tr>';
  }

  // One line of the person's picture, in words. `delegationEdgeRow()`'s
  // argument applied to five relations instead of three: everything the diagram
  // says in colour is said here in text, because a picture nobody can quote is
  // a picture nobody can put in a bug report.
  userEdgeRow(edge, lookOf) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.userEdgeRow().");
    const relation =
      edge.relation === 'issued'
        ? '<span class="state-none" title="This service handed that party a ' +
          'credential.">issued to</span>'
      : edge.relation === 'signed-in'
        ? '<strong>signed in</strong><br><span class="state-none">to this ' +
          'service</span>'
      : edge.relation === 'issued-for'
        ? '<strong>issued for</strong><br><span class="state-none">by an ' +
          'ordinary grant</span>'
      : edge.relation === 'acts-for'
        ? '<strong>acts for</strong><br><span class="state-none">a ' +
          'delegation</span>'
        : '<strong>reaches</strong>' +
          (edge.subject ? '<br><span class="state-none">as ' +
           this.esc(edge.subject) +
                          '</span>' : '') +
          // WHAT THE TOKEN MAY DO AT THE FAR END, said here as well as on the
          // picture because the two are drawn from ONE graph and a reader
          // comparing them must not find the line and the row disagreeing. The
          // picture cuts the list at 26 characters and this does not, which is
          // the ordinary division of labour between them.
          //
          // `permissions` is present only on a line built from a CREDENTIAL —
          // see common/user_graph.ts — so a `reaches` line out of the
          // delegation register renders exactly as it did. An EMPTY array is
          // drawn rather than skipped, for the renderer's reason: it means the
          // token named this resource and asked for none of its permissions,
          // which is a state and not a gap.
          (edge.permissions
            ? '<br>' + (edge.permissions.length
                ? edge.permissions.map(function (one) {
                    return '<code>' + self.esc(one) + '</code>';
                  }).join(' ')
                : '<span class="state-none" title="The token names this ' +
                  'resource and none of its delegated permissions \u2014 ' +
                  'which is what a scope naming the resource\'s client_id ' +
                  'produces, because that value becomes the audience and ' +
                  'comes off the scope claim. It is also what a resource ' +
                  'that defines no permissions can ever produce.">default ' +
                  'permissions</span>')
            : '');
    const mechanism = edge.typeLabel
      ? (edge.type ? '<code>' + this.esc(edge.type) + '</code><br>' : '') +
        '<span class="state-none">' + this.esc(edge.typeLabel) + '</span>' +
        (edge.protocol || edge.spec
          ? '<br><span class="state-none">' + this.esc(edge.protocol) +
            (edge.spec ? ' &middot; ' + this.esc(edge.spec) : '') + '</span>'
          : '')
      : '<span class="state-none">' +
        this.esc((edge.protocols || []).join(', ') || '—') + '</span>';
    log.debug("Leaving AdminConsole.userEdgeRow().");
    return '<tr>' +
      '<td class="who">' + this.esc(lookOf(edge.from)) + '</td>' +
      '<td class="who">' + this.esc(lookOf(edge.to)) + '</td>' +
      '<td>' + relation +
        ((edge.skipped || []).length
          ? '<br><span class="state-expired" title="Nobody named this party ' +
            'on these acts, so the line jumps it.">jumps the ' +
            this.esc(edge.skipped.join(' and ')) + '</span>'
          : '') + '</td>' +
      '<td>' + mechanism + '</td>' +
      '<td>' + (edge.mode ? this.modeCell(edge.mode)
        : '<span class="state-none" title="Impersonation and delegation are ' +
          'properties of a DELEGATION mechanism. An ordinary grant makes ' +
          'neither claim, and calling it one would be this console inventing ' +
          'a judgement.">&mdash;</span>') + '</td>' +
      '<td class="num">' + (edge.acts
        ? this.esc(edge.acts) + (edge.relation === 'signed-in' ? ''
            : ' — <span class="state-valid">' + this.esc(edge.issued) + ' ' +
                'issued</span>' +
              (edge.refused
                ? ', <span class="state-revoked">' + this.esc(edge.refused) +
                  ' refused</span>' : ''))
        : '<span class="state-none">&mdash;</span>') + '</td>' +
      '<td class="num">' + (edge.credentials
        ? '<strong>' + this.esc(edge.credentials) + '</strong>'
        : '<span class="state-none">0</span>') + '</td>' +
      '<td class="who">' + ((edge.produced || []).length
        ? edge.produced.map(function (one) {
            return '<code>' + self.esc(one.kind) + '</code> × ' +
                   self.esc(one.count) +
              (one.identifiers.length
                ? '<br>' + one.identifiers.map(function (id) {
                    return self.shortened(id, 10);
                  }).join(' ') +
                  (one.moreIdentifiers ? ' <span class="state-none">+' +
                    self.esc(one.moreIdentifiers) + ' more</span>' : '')
                : '');
          }).join('<br>')
        : '<span class="state-none">&mdash;</span>') + '</td>' +
      '</tr>';
  }

  // The state of the directory's two sockets as a banner, or '' when both are
  // up.
  //
  // Three cases rather than two, because plain 389 and LDAPS 636 are
  // independent sockets and either can be up while the other is not — which is
  // the commonest outcome of a host run, where 636 is privileged and 389 has
  // usually lost the port to the host's own slapd. A page that said "no client
  // can connect" while LDAPS was answering would be wrong in the direction that
  // costs somebody an afternoon looking for a directory that was there all
  // along.
  directoryListenerWarning(info, subject) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.directoryListenerWarning().");
    if (!info.listening && !info.ldapsListening) {
      log.debug("Leaving AdminConsole.directoryListenerWarning().");
      return this.warn('<strong>The directory\'s listeners are not ' +
        'up</strong> — ' +
        this.esc(info.listenError || 'it never bound') + '. ' + subject.upper +
        ' ' +
        subject.verb +
        ' in this process\'s store and ' + subject.verb + ' what an LDAP ' +
        'client WOULD read; right now no client can connect, most likely ' +
        'because TCP ' + this.esc(info.port) +
        ' was already taken. This page is HTTP and answers either way.');
    }
    if (!info.listening) {
      log.debug("Leaving AdminConsole.directoryListenerWarning().");
      return this.warn('<strong>The directory\'s plain listener is not ' +
        'up</strong> — ' +
        this.esc(info.listenError || 'it never bound') + ' — but ' +
          '<strong>LDAPS on ' +
        this.esc(info.ldapsPort) + ' is</strong>, so ' + subject.lower + ' ' +
        subject.verb +
        ' reachable over TLS. TCP ' + this.esc(info.port) + ' was most ' +
                                                            'likely already ' +
                                                                'taken.');
    }
    if (info.ldapsPort && !info.ldapsListening) {
      log.debug("Leaving AdminConsole.directoryListenerWarning().");
      return this.warn('The plain listener on ' + this.esc(info.port) + ' is ' +
        'up; <strong>LDAPS is not</strong>. That affects how ' + subject.lower +
        ' ' +
        'can be reached, not ' +
        'whether ' + subject.pronoun + ' ' + subject.verb + ' there.');
    }
    log.debug("Leaving AdminConsole.directoryListenerWarning().");
    return '';
  }

  // Every attribute of one entry, operational ones included, as the table both
  // the user page and the group page draw. `entry` is what the directory's
  // readers return: canonically spelled names, values already arrays, and
  // `operational` naming which of them a search would have withheld.
  attributeTable(entry) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.attributeTable().");
    const rows = Object.keys(entry.attributes).map(function (name) {
      const values = entry.attributes[name];
      const operational = entry.operational.indexOf(name) >= 0;
      return '<tr><td><code>' + self.esc(name) + '</code>' +
        (operational
          ? ' <span class="state-none" title="An operational attribute. A ' +
            'search returns it only when it is asked for by name (RFC 4511 ' +
            'section 4.5.1.8) — this dump shows it ' +
            'always.">(operational)</span>'
          : '') + '</td>' +
        '<td class="num">' + values.length + '</td>' +
        '<td>' + (values.map(function (value) {
          return '<code>' + self.esc(value) + '</code>';
        }).join('<br>') || '—') + '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.attributeTable().");
    return '<table><tr><th>Attribute</th><th ' +
           'class="num">Values</th><th>Value(s)</th></tr>' +
      rows + '</table>';
  }

  setDirectoryReader(fn) {
    const { log, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setDirectoryReader().");
    directoryReader = fn;
    // AND THE READ LAYER, which needs it for the new-person answer.
    adminViews.setDirectoryReader(fn);
    log.debug("A directory reader was installed; a user's page will now show " +
              "that user's LDAP entry.");
    log.debug("Leaving AdminConsole.setDirectoryReader().");
  }

  setGroupReader(fn) {
    const { log, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setGroupReader().");
    groupReader = fn;
    // AND THE READ LAYER, for the groups answer.
    adminViews.setGroupReader(fn);
    log.debug("A group reader was installed; /admin/groups will now show the " +
              "directory's groups.");
    log.debug("Leaving AdminConsole.setGroupReader().");
  }

  setSpiffeReader(fn) {
    const { log, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setSpiffeReader().");
    spiffeReader = fn;
    // AND THE READ LAYER, which draws the three SPIFFE reports out of the same
    // reader — see admin-core/admin_views.ts.
    adminViews.setSpiffeReader(fn);
    log.debug("A SPIFFE reader was installed; /admin/spiffe will now report " +
              "which gRPC listeners bound.");
    log.debug("Leaving AdminConsole.setSpiffeReader().");
  }

  setCryptoReporter(reporter) {
    const { log, errorCodes, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setCryptoReporter().");
    // ONE OBJECT, VALIDATED WHOLE AND REFUSED WHOLE, which is the rule
    // setLogoutReader() follows and for the same reason: a filler that
    // installed the report and not the export would leave one of the three
    // operations answering 503 while the other two worked, and the parity rule
    // would be failing silently rather than loudly.
    const needed = ['report', 'keys', 'exportKey'];
    const missing = needed.filter(function (name) {
      return !reporter || typeof reporter[name] !== 'function';
    });
    if (missing.length) {
      log.error(errorCodes.tag('STS-ADMIN-0014') + 'admin: ' +
                                                   'setCryptoReporter() was ' +
                                                   'given something without ' +
                missing.join(', ') + ', and was ignored whole. ' +
                '/admin/keys and the two /admin-api crypto resources will ' +
                'say the reporter is not installed rather than half working.');
      log.debug("Leaving AdminConsole.setCryptoReporter(). Refused.");
      return;
    }
    cryptoReporter = reporter;
    // AND THE READ LAYER, which needs the same thing — see the header of
    // admin-core/admin_views.ts. Still one statement and one writer.
    adminViews.setCryptoReporter(reporter);
    log.debug("Leaving AdminConsole.setCryptoReporter(). Installed.");
  }

  // Does this request hold Admin Write? One caller today —
  // `crypto_metadata.js`'s key export, which is the one act in this console
  // that hands over a private key — and it goes through `gateStateFor()` so
  // that the answer is the same one every page's banner is drawn from.
  mayWrite(req) {
    const { log, gateStateFor } = this.deps;
    log.debug("Entering AdminConsole.mayWrite().");
    const state = gateStateFor(req);
    log.debug("Leaving AdminConsole.mayWrite(). write=" + !!state.write);
    return !!state.write;
  }

  setLogoutReader(reader) {
    const { log, errorCodes, adminActions, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setLogoutReader().");
    const complete = reader && typeof reader.inventoryFor === 'function' &&
                     typeof reader.terminate === 'function' &&
                     // liveSessions() joined the three on 2026-09-04, for
                     // /admin/sessions and GET /admin-api/sessions. It is
                     // validated with them rather than tested at each call site
                     // for the reason the whole slot is validated whole: a
                     // reader carrying the inventory and not this would leave
                     // that page saying no reader is installed on a service
                     // that plainly has one.
                     typeof reader.liveSessions === 'function' &&
                     Array.isArray(reader.FAMILIES);
    if (!complete) {
      // A warning and not a throw, for the reason admin_rbac.js's install has:
      // a console that will not start is worse than a console with one page
      // that says why it cannot answer.
      log.warn(errorCodes.tag('STS-ADMIN-0014') + 'admin: a logout reader ' +
               'was offered that does not carry inventoryFor(), terminate(), ' +
               'liveSessions() and FAMILIES. It is refused whole — a partial ' +
               'one would leave /admin/logout and /admin/sessions listing ' +
               'what is live and unable to end any of it, which is the worst ' +
               'of the two halves.');
      log.debug("Leaving AdminConsole.setLogoutReader().");
      return;
    }
    logoutReader = reader;
    // AND THE ACTION LAYER, which needs the same object and is handed it
    // from here rather than asking for it — see the header of
    // admin-core/admin_actions.ts. One statement, two destinations: this is
    // the ONLY place either is written, which is what keeps it one answer.
    adminActions.setLogoutReader(reader);
    // AND THE READ LAYER: sessionsView() reads what is live where
    // sessionsAction() ends it, so both halves need this one.
    adminViews.setLogoutReader(reader);
    log.debug("A logout reader was installed; /admin/logout will now list " +
              "and end live sessions across every protocol family.");
    log.debug("Leaving AdminConsole.setLogoutReader().");
  }

  setXacmlPages(parts) {
    const { log, errorCodes, adminViews, adminActions } = this.deps;
    log.debug("Entering AdminConsole.setXacmlPages().");
    const complete = parts && XACML_PAGE_PARTS.every(function (name) {
      return typeof parts[name] === 'function';
    });
    if (!complete) {
      log.warn(errorCodes.tag('STS-ADMIN-0014') + 'admin: a set of XACML ' +
               'page views was offered that does not carry all seven ' +
               'of ' + XACML_PAGE_PARTS.join(', ') + '. It is ' +
               'refused whole — a partial set would leave /admin-api able to ' +
               'read the policy repository and unable to change it, which ' +
               'reads as a management API that is working and is not.');
      log.debug("Leaving AdminConsole.setXacmlPages().");
      return;
    }
    xacmlPages = parts;
    // AND THE READ LAYER, which needs the same thing — see the header of
    // admin-core/admin_views.ts. Still one statement and one writer.
    adminViews.setXacmlPages(parts);
    // AND THE ACTION LAYER. This is the one collaborator BOTH halves need — the
    // actions dispatch on these pages and the views draw from them — so one
    // statement here has THREE destinations: the variable above, and one in
    // each half. Still exactly one writer, which is the only property that
    // matters; see the header of admin-core/admin_actions.ts.
    adminActions.setXacmlPages(parts);
    log.debug('The XACML page views were installed; /admin-api mirrors them.');
    log.debug("Leaving AdminConsole.setXacmlPages().");
  }

  xacmlActionNames() {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.xacmlActionNames().");
    log.debug("Leaving AdminConsole.xacmlActionNames().");
    return xacmlPages && xacmlPages.actionNames
      ? xacmlPages.actionNames() : [];
  }

  setDirectoryPages(views) {
    const { log, errorCodes, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setDirectoryPages().");
    const complete = views && DIRECTORY_PAGE_NAMES.every(function (name) {
      return typeof views[name] === 'function';
    });
    if (!complete) {
      // A warning and not a throw, for the reason every other install here
      // gives.
      log.warn(errorCodes.tag('STS-ADMIN-0014') + 'admin: a set of directory ' +
               'page views was offered that does not carry all eight ' +
               'of ' + DIRECTORY_PAGE_NAMES.join(', ') + '. It ' +
               'is refused whole — a partial set would leave some of ' +
               '/admin-api\'s directory operations answering as though no ' +
               'directory were loaded on a service that plainly has one.');
      log.debug("Leaving AdminConsole.setDirectoryPages().");
      return;
    }
    directoryPages = views;
    // AND THE READ LAYER, which needs the same thing — see the header of
    // admin-core/admin_views.ts. Still one statement and one writer.
    adminViews.setDirectoryPages(views);
    log.debug('The eight directory page views were installed; /admin-api ' +
              'mirrors them.');
    log.debug("Leaving AdminConsole.setDirectoryPages().");
  }

  setScimReader(fn) {
    const { log, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setScimReader().");
    scimReader = fn;
    // AND THE READ LAYER, which needs the same thing — see the header of
    // admin-core/admin_views.ts. Still one statement and one writer.
    adminViews.setScimReader(fn);
    log.debug("A SCIM reader was installed; /admin/scim will now describe " +
              "the SCIM 2.0 endpoints.");
    log.debug("Leaving AdminConsole.setScimReader().");
  }

  setDirectoryWriter(fn) {
    const { log, adminViews, adminActions } = this.deps;
    log.debug("Entering AdminConsole.setDirectoryWriter().");
    directoryWriter = fn;
    // AND THE READ LAYER, which needs it for the new-person answer.
    adminViews.setDirectoryWriter(fn);
    // AND THE ACTION LAYER, which needs the same object and is handed it
    // from here rather than asking for it — see the header of
    // admin-core/admin_actions.ts. One statement, two destinations: this is
    // the ONLY place either is written, which is what keeps it one answer.
    adminActions.setDirectoryWriter(fn);
    log.debug("A directory writer was installed; /admin/users can now create " +
              "a person in the directory.");
    log.debug("Leaving AdminConsole.setDirectoryWriter().");
  }

  setGroupWriter(fns) {
    const { log, errorCodes, adminViews, adminActions } = this.deps;
    log.debug("Entering AdminConsole.setGroupWriter().");
    const given = fns || {};
    const missing = GROUP_WRITER_MEMBERS.filter(function (name) {
      return typeof given[name] !== 'function';
    });
    if (missing.length) {
      // REFUSED RATHER THAN HALF-INSTALLED, and the warning names what was
      // missing: "the groups page cannot create a group" with no further detail
      // is the kind of message that costs an hour.
      log.error(errorCodes.tag('STS-ADMIN-0014') + "admin: the group writer " +
                                                   "slot was offered an " +
                                                   "object missing " +
                missing.join(', ') + ". It is NOT installed — /admin/groups " +
                "and POST /admin-api/groups will report that no directory is " +
                "loaded, which is true of the writes and not of the reads " +
                "beside them.");
      log.debug("Leaving AdminConsole.setGroupWriter(). Refused.");
      return;
    }
    groupWriter = given;
    // AND THE READ LAYER, for the groups answer.
    adminViews.setGroupWriter(given);
    // AND THE ACTION LAYER, which needs the same object and is handed it
    // from here rather than asking for it — see the header of
    // admin-core/admin_actions.ts. One statement, two destinations: this is
    // the ONLY place either is written, which is what keeps it one answer.
    adminActions.setGroupWriter(given);
    log.debug("Leaving AdminConsole.setGroupWriter(). /admin/groups can now " +
              "create a group and add a member to one.");
  }

  // The whole section, as HTML and as the object that goes into ?format=json.
  // Both come out of one call so that the page and the JSON cannot disagree
  // about what the directory holds, which is the same rule /admin/ldap/*
  // follows for its own five views.
  //
  // `row` is the user record: it is what tells a missing entry apart from an
  // entry that was never going to exist. Four of the five reasons for an
  // absence are facts about the USER rather than about the directory — a client
  // is not a person, an LDAP bind presents a DN and not a user name, an
  // identity that only ever appeared as the subject of something never
  // authenticated at all — and a section that said only "not found" would send
  // a reader to look for a bug in the directory.
  ldapObjectSection(row, key) {
    const { log, adminViews } = this.deps;
    log.debug("Entering AdminConsole.ldapObjectSection(). key=" + key);
    const heading = '<h2>This user in the LDAP directory</h2>';
    if (!directoryReader) {
      log.debug("Leaving AdminConsole.ldapObjectSection(). No directory is " +
                "loaded.");
      return {
        html: heading +
          this.note('No LDAP directory is loaded in this process, so there ' +
          'is no entry to show. That is a build of this service without ' +
          '<code>ldap_server.js</code> and not a failure — every other ' +
          'section of this page is unaffected.'),
        json: null
      };
    }
    const info = directoryReader(key);
    const link = this.note('<a href="/admin/ldap/service">What this ' +
      'directory is</a> &middot; <a href="/admin/ldap/directory">every entry ' +
      'in it</a> &middot; <a href="/admin/ldap/directory?format=json">the ' +
      'same as JSON</a>.');
    // Said on every branch, including the ones with an entry: the entry can be
    // there and the socket down, and a reader who trusts this page to mean "an
    // LDAP client can fetch this" needs to know which.
    const listener = this.directoryListenerWarning(info, ENTRY_SUBJECT);
    const alsoNamed = info.alsoNamed.length
      ? this.note(info.alsoNamed.length + ' other entr' +
        (info.alsoNamed.length === 1 ? 'y names' : 'ies name') + ' this uid: ' +
        this.codeList(info.alsoNamed) + '. Those were written through the ' +
        'protocol rather than seeded by an authentication — this directory ' +
        'has no schema and does not require a uid to be unique, so they are ' +
        'shown rather than merged.')
      : '';

    if (!info.found) {
      // Why not, in the order that makes the first true one the real answer.
      // The DN is quoted with it, EXCEPT where the identity is an LDAP bind DN
      // — there the name this console files the person under is itself a DN, so
      // the place an entry would have gone is `uid=<a whole DN>,ou=users,...`,
      // and leading with that reads as a malformed directory rather than as the
      // explanation that follows it.
      let because;
      let intro = 'There is no entry at <code>' + this.esc(info.dn) +
                  '</code>.';
      if (!info.autoCreateUsers) {
        because = 'An entry per authenticated user is switched OFF in this ' +
          'process (<code>LDAP_AUTOCREATE_USERS</code>), so nothing here ' +
          'seeds one.';
      } else if (row.isClient) {
        because = 'This identity is a <strong>client</strong>, not a person. ' +
          '<code>client_credentials</code> produces tokens with a subject ' +
          'and nobody behind them, and a directory of people is the wrong ' +
          'place for it, so the directory declines to invent one.';
      } else if (!row.authenticated) {
        because = 'This identity has <strong>never authenticated ' +
          'here</strong>. The entry is seeded at the moment a credential is ' +
          'ACCEPTED, and this one is known only as the subject of something ' +
          'that was issued — so there was no such moment.';
      } else if (row.protocols.length === 1 &&
                 row.protocols[0].protocol === 'ldap') {
        intro = 'Nothing was seeded for this identity.';
        because = 'Everything this identity has done here is an <strong>LDAP ' +
          'bind</strong>, which presents a DN rather than a user name. ' +
          'Seeding <code>uid=&lt;a whole DN&gt;</code> under ' +
          '<code>' + this.esc(info.usersDn) + '</code> would put a second, ' +
          'malformed object beside the one the DN already names.';
      } else if (info.full) {
        because = 'The directory holds its maximum of ' +
                  this.esc(info.maxEntries) +
          ' entries, so it stopped creating them. The authentication itself ' +
          'was unaffected — a full directory must never fail one.';
      } else {
        because = 'It was there and is not now: an entry can be ' +
          '<code>delete</code>d or <code>modifyDN</code>&rsquo;d through the ' +
          'protocol like any other, and nothing puts it back until this name ' +
          'authenticates again.';
      }
      log.debug("Leaving AdminConsole.ldapObjectSection(). There is no entry " +
                "at " + info.dn +
                ".");
      return {
        html: heading + listener +
          this.note(intro + ' ' + because) + alsoNamed + link,
        json: adminViews.ldapObjectJson(key)
      };
    }

    const entry = info.entry;

    const html = heading + listener +
      this.note('The entry the embedded directory holds for this person ' +
      '&mdash; the object itself and not a copy of it, so an LDAP client ' +
      'bound to <code>ldap://&lt;host&gt;:' +
      this.esc(info.port) + '</code> reading <code>' + this.esc(entry.dn) +
      '</code> sees exactly this. It appeared the first time they ' +
      'authenticated through any protocol here, and it carries no password: ' +
      '<strong>every bind to this directory succeeds anyway</strong>, ' +
      'whatever DN and whatever password, so nothing in this object is a ' +
      'credential.') +
      '<table><tr><th>DN</th><th>Came from</th><th>Created</th><th>Last ' +
      'modified</th></tr><tr><td><code>' + this.esc(entry.dn) + '</code></td>' +
      '<td>' + this.esc(entry.origin) + '</td>' +
      '<td><code>' + this.esc(entry.createdAt) + '</code></td>' +
      '<td><code>' + this.esc(entry.modifiedAt) + '</code></td></tr></table>' +
      // THE SUBJECT (2026-09-14), which is no longer derivable from the name:
      // what every token issued to this person carries in `sub`.
      '<p>Subject (<code>sub</code>): <code>' +
      this.esc(adminViews.userDetailSubject(key) || '(none)') + '</code></p>' +
      this.note('<code>urn:uuid:</code> and this entry\'s ' +
      '<code>entryUUID</code>, which it keeps through a rename. A person ' +
      'deleted and created again under the same name is a different subject.') +
      this.note('The two timestamps are <em>generalized time</em> ' +
      '(<code>YYYYMMDDHHMMSSZ</code>), which is what a directory shows ' +
      '&mdash; not the ISO 8601 strings the rest of this console uses. The ' +
      'difference is only punctuation and it is kept because a debugger that ' +
      'showed one where the protocol carries the other would be showing the ' +
      'wrong thing.') +
      this.attributeTable(entry) +
      this.note('Every attribute the entry has, operational ones included. ' +
      'This directory is <strong>schemaless</strong>: no ' +
      '<code>objectClass</code> is enforced and no value is checked against ' +
      'a syntax, so an attribute a real directory would refuse is here ' +
      'because something wrote it. The <code>description</code> values are ' +
      'this service\'s own note of which protocols this person has ' +
      'authenticated through &mdash; one line per protocol, added the first ' +
      'time each is seen.') +
      alsoNamed + link;

    log.debug("Leaving AdminConsole.ldapObjectSection(). " +
              Object.keys(entry.attributes).length +
              " attribute(s) dumped.");
    return { html: html, json: adminViews.ldapObjectJson(key) };
  }

  // One user's tokens as a table. The columns are the ones that differ WITHIN a
  // user: their name and subject are the same on every row by construction and
  // are stated once above the table instead of repeated down it.
  userTokenTable(records, back, empty) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.userTokenTable(). " + records.length +
              " row(s).");
    const rows = records.map(function (record) {
      const button = record.revocable
        ? '<form method="post" action="/admin/tokens" class="inline">' +
          '<input type="hidden" name="action" value="' +
          (record.revoked ? 'restore' : 'revoke') + '"><input ' +
          'type="hidden" name="target" value="' + self.esc(record.jti) + '">' +
          '<input type="hidden" name="from" value="users">' +
          '<input type="hidden" name="back" value="' + self.esc(back) + '">' +
          '<button class="' + (record.revoked ? 'secondary' : 'danger') + '">' +
          (record.revoked ? 'Restore' : 'Revoke') + '</button></form>'
        : '<span class="state-none" title="Only access tokens, ID Tokens and ' +
          'refresh tokens can be revoked — the others are replies rather ' +
          'than credentials, or carry no jti to act on.">—</span>';
      return '<tr><td>' + self.esc(record.kind) + '</td>' +
        '<td class="' + self.stateClass(record.state) + '">' +
        self.esc(record.state) +
        '</td><td>' + self.esc(record.grant || '—') + '</td><td>' +
        self.esc(record.client_id || '—') + '</td><td>' +
        self.esc(record.scope || '—') +
        '</td><td>' + (record.jkt ? 'DPoP' : 'Bearer') + '</td><td>' +
        self.esc(self.whenText(record.issuedAt)) + '</td><td>' +
        self.esc(record.exp ? self.whenText(record.exp * 1000) : '—') +
        '</td><td>' +
        self.shortened(record.jti, 12) + '</td><td>' + button + '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.userTokenTable().");
    return '<table><tr><th>Token</th><th>State</th><th>Grant</th><th>' +
      'Client</th><th>Scope</th><th>Presented ' +
      'as</th><th>Issued</th><th>Expires</th><th>jti</th><th></th></tr>' +
      (rows || '<tr><td colspan="10">' + this.esc(empty) + '</td></tr>') +
      '</table>';
  }

  // The artifacts that are not JWTs. One table for all three kinds, with a
  // single Detail column, because an assertion's audience, a ticket's service
  // and enc-type and a credential's configuration id are each the one thing
  // worth reading about that row and giving each its own column would leave two
  // thirds of the table empty.
  userArtifactTable(records) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.userArtifactTable(). " + records.length +
              " row(s).");
    const rows = records.map(function (record) {
      const detail = record.service
        ? 'service ' + record.service +
          (record.etype ? ', ' + record.etype : '') +
          (record.realm ? ', realm ' + record.realm : '')
        : (record.audience ? 'audience ' + record.audience
                           : (record.configId ?
                              'configuration ' + record.configId : ''));
      return '<tr><td>' + self.esc(record.kind) + '</td>' +
        '<td class="' + self.stateClass(record.state) + '">' +
        self.esc(record.state) +
        '</td><td>' + self.esc(detail || '—') + '</td><td>' +
        (record.id ? self.shortened(record.id, 20) : '<span ' +
          'class="state-none" title="A Kerberos ticket has no identifier to ' +
          'quote: it is named by the client and service it is ' +
          'for.">—</span>') + '</td>' +
        '<td>' + self.esc(self.whenText(record.issuedAt)) + '</td><td>' +
        self.esc(record.expiresAt ? self.whenText(record.expiresAt) : '—') +
        '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.userArtifactTable().");
    return '<table><tr><th>Artifact</th><th>State</th><th>Detail</th><th>' +
           'Identifier</th><th>Issued</th><th>Expires</th></tr>' +
      (rows ||
       '<tr><td colspan="6">Nothing that is not a JWT has been issued ' +
               'to this user.</td></tr>') +
      '</table>';
  }

  // How they authenticated, most recent first. The whole point of the table is
  // the Method column: "sign-in screen (password)" and "AS-REQ with
  // PA-ENC-TIMESTAMP" are both authentications and only one of them checked
  // anything.
  authenticationTable(row) {
    const { log, stats } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.authenticationTable(). " +
              row.events.length + " " +
        "event(s).");
    const rows = row.events.slice().reverse().map(function (event) {
      return '<tr><td>' + self.esc(self.whenText(event.at)) + '</td>' +
        '<td>' + self.esc(event.protocol) + '</td>' +
        '<td>' + self.esc(event.method) + '</td>' +
        '<td>' + self.esc(event.presented) + '</td>' +
        // Where it came from (2026-09-19), linked to every audit row from
        // that address. A dash for an event recorded before events carried
        // one, or for an act that came over no socket.
        '<td>' + (event.address
          ? '<a href="/admin/audit?address=' +
            encodeURIComponent(event.address) + '"><code>' +
            self.esc(event.address) + '</code></a>'
          : '<span class="state-none" title="No client address: recorded ' +
            'before authentication events carried one, or not sent over a ' +
            'socket.">—</span>') + '</td>' +
        '<td>' + self.esc(event.amr || '—') + '</td>' +
        '<td>' + self.esc(event.acr || '—') + '</td>' +
        '<td>' + self.esc(event.client_id || '—') + '</td>' +
        '<td>' + (event.sessionId ? self.shortened(event.sessionId, 10) : '—') +
        '</td><td>' + self.esc(event.note || '') + '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.authenticationTable().");
    return '<table><tr><th>When</th><th>Protocol</th><th>Method</th><th>' +
      'Presented ' +
      'as</th><th>From</th><th>amr</th><th>acr</th><th>Client</th>' +
      '<th>Session</th><th>Note</th></tr>' +
      (rows || '<tr><td colspan="10">No authentication was recorded for this ' +
               'identity. See above: they are known here only as the subject ' +
               'of something that was issued.</td></tr>') +
      '</table>' +
      (row.eventsForgotten > 0
        ? this.note('The most recent ' + stats.MAX_EVENTS_PER_USER + ' of ' +
          (row.eventsForgotten + row.events.length) + ' authentication(s); ' +
          'the rest have been forgotten. The counts above them are of all of ' +
          'them and are not capped.')
        : '');
  }

  // One session and everything issued on it. This is the answer to the question
  // the page exists for — "what does this person hold right now, and where did
  // it come from" — so the session's own facts and its tokens are drawn as one
  // block rather than as two tables somebody has to join by eye.
  //
  // Its token table is PAGED, and paged separately from every other block on
  // the page. One browser session can hold most of the five thousand tokens
  // this service remembers — a refresh grant in a loop is all it takes — so the
  // block that answers "what does this person hold right now" is the one table
  // here that can genuinely run away, and a single `page` shared with the block
  // above it would move both. Hence a page parameter named after the session
  // id: it names the block it moves, so a bookmark still moves the same session
  // after the list around it has changed, which an index into the session list
  // would not.
  sessionBlock(session, tokenPage, back, params) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.sessionBlock(). id=" + session.id);
    const nav = this.pageNavPair('/admin/users', params, tokenPage.paging);
    const html = '<h3>Session ' + this.shortened(session.id, 12) + ' &mdash; ' +
      '<span class="' + (session.expired ? 'state-expired' : 'state-valid') +
      '">' +
      (session.expired ? 'expired, not yet swept' : 'active') + '</span></h3>' +
      '<table><tr><th>Signed in</th><th>Last authenticated</th><th>Expires' +
      '</th><th>amr</th><th>acr</th>' +
      '<th>WS-Fed relying parties signed into</th></tr>' +
      '<tr><td>' + this.esc(this.whenText(session.startedAt)) + '</td>' +
      '<td>' + this.esc(this.whenText(session.authTime)) +
      (session.authentications > 1 ? ' (' + session.authentications + ')' :
       '') +
      '</td>' +
      '<td>' + this.esc(this.whenText(session.expires)) + '</td>' +
      '<td>' + this.esc(session.amr || '—') + '</td>' +
      '<td>' + this.esc(session.acr || '—') + '</td>' +
      '<td>' +
      (session.wsfedRealms.length ? this.esc(session.wsfedRealms.join(', ')) :
       '—') +
      '</td></tr></table>' +
      nav.head +
      this.userTokenTable(tokenPage.shown, back,
        'Nothing has been issued on this session yet. A browser can hold a ' +
        'sign-on session and have been given no token at all — it is what ' +
        'the authorization endpoint reads before it issues anything.') +
      nav.foot;
    log.debug("Leaving AdminConsole.sessionBlock().");
    return html;
  }

  // The drill-down. Returns null when the identity is not one this service
  // knows, which the route below turns into an honest empty page rather than a
  // 404: a user can genuinely be forgotten to the cap between the click and the
  // page.
  // ===========================================================================
  // ONE PERSON'S SECOND FACTORS, ON THEIR OWN ROW (2026-09-10).
  //
  // **THIS IS WHERE `/admin/mfa`'s PER-PERSON HALF WENT.** That page listed
  // everybody and gave each row a Clear button; the roster is columns on the
  // list above now and this is the detail. What it buys is the thing a page of
  // forty people could not have: this section can say what the enrolment IS —
  // which digest, how many digits, how long a step, when it was set up, when it
  // was last used, whether the secret is sealed — and it can list a person's
  // security keys one at a time with the role each is in.
  //
  // ---------------------------------------------------------------------------
  // TWO MECHANISMS, AND THE WEBAUTHN HALF IS THE ONE THAT NEEDS EXPLAINING.
  //
  // A WebAuthn key is here only where it is a SECOND FACTOR — the `mfa` role. A
  // `primary` key is a way IN rather than a second factor, so it is reported
  // under what this person can sign in with and not as an MFA credential.
  // `credentials.js`'s ROLES table is what tells them apart, and this section
  // reads it rather than deciding.
  //
  // ---------------------------------------------------------------------------
  // THE TWO REMOVALS ARE NOT THE SAME ACT AND THE BUTTONS SAY SO.
  //
  // Clearing an authenticator app CANNOT lock anybody out: a one-time code is
  // never a primary credential here, so clearing one drops the account to one
  // factor and never to none. Removing a security KEY can — it may be the only
  // credential there is — so it goes through `credentials.removeKey()`, which
  // refuses to remove the last way in. An operator must not be able to do what
  // the person themselves is stopped from doing.
  // ===========================================================================
  mfaSection(row, key, state, back) {
    const { log, credentials, totp, webauthnPolicy, backupCodes,
            adminViews } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.mfaSection(). key=" + key);
    const heading = '<h2>Second factors, and what this person can sign in ' +
                    'with</h2>';
    if (!credentials.storable()) {
      log.debug("Leaving AdminConsole.mfaSection(). No credential store.");
      return {
        html: heading +
          this.note('No credential store is installed in this process, so ' +
          'nothing here can say what this person holds. That is a build of ' +
          'this service without <code>ldap_server.js</code> and not a ' +
          'failure — every other section of this page is unaffected.'),
        json: null
      };
    }

    const mech = credentials.mechanismsFor(key);
    const totpLive = totp.settings();
    const keyLive = webauthnPolicy.settings();
    const recoveryLive = backupCodes.settings();
    const mfaKeys = (mech.keys || []).filter(function (
        one) { return one.role === 'mfa'; });
    const primaryKeys = (mech.keys || []).filter(function (
        one) { return one.role === 'primary'; });
    const carryBack = '<input type="hidden" name="back" value="' +
                      this.esc(back) +
                      '"><input ' +
                      'type="hidden" name="from" value="users">';

    // WHAT THEY CAN GET IN WITH, first and on its own, because it is the
    // question underneath the other two: a person with no password and no
    // primary key cannot sign in at all, however much is enrolled on them, and
    // an operator reading a long list of second factors on such an account
    // would otherwise draw exactly the wrong conclusion.
    const wayIn = '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
      '<tr><th>Password</th><td>' + (mech.password
        ? '<span class="state-valid">set</span> — a scrypt hash on their own ' +
          'entry. It cannot be read back and this console cannot show it; ' +
          'the form below replaces it.'
        : '<span class="state-none">none</span>') + '</td></tr>' +
      '<tr><th>Primary security key</th><td>' + (primaryKeys.length
        ? '<span class="state-valid">' + this.esc(String(primaryKeys.length)) +
          '</span> — signs them in on its own, passwordless. The session ' +
          'then records <code>amr ["hwk"]</code> and <code>acr "1"</code>.'
        : '<span class="state-none">none</span>') + '</td></tr>' +
      '<tr><th>Can sign in</th><td>' + (mech.usable
        ? '<span class="state-valid">yes</span>'
        : '<strong class="state-expired">NO</strong> — no password and no ' +
          'primary key. <strong>A second factor is never a way in by ' +
          'itself</strong>, so nothing below changes this answer. An ' +
          'activation link is how they come to hold a ' +
          'credential.') + '</td></tr>' +
      '<tr><th>Second factor demanded</th><td>' + (mech.mfaRequired
        ? '<span class="state-valid">yes</span> — the sign-in screen asks ' +
          'for ' +
          (mech.secondFactor === 'webauthn'
            ? 'the SECURITY KEY' + (mech.totp
                ? ', with the authenticator app offered as the alternative ' +
                  'for somebody at a machine the key is not plugged into'
                : '')
            : 'a CODE from their authenticator app') +
          '. A password alone will not sign them in.'
        : '<span class="state-none">no</span> — nothing here is configured ' +
          'as a second factor, so a password alone signs them in.') +
          '</td></tr>' +
      '</table>';

    // --- the authenticator app -----------------------------------------------
    const d: any = mech.totpDetail || {};
    const totpBlock = '<h3>Authenticator app (RFC 6238)</h3>' +
      (!mech.totp
        ? this.note('<strong>None enrolled.</strong> ' +
          (totpLive.enabled
            ? 'They set one up themselves at <code>/portal/mfa</code>, or ' +
              'while spending an activation link. <strong>There is ' +
              'deliberately no way to enrol one from here</strong>: ' +
              'enrolling means being shown a shared secret, and an ' +
              'administrative door that handed one out would mint a working ' +
              'second factor for any account — which is not a second factor ' +
              'at all.'
            : 'Authenticator apps are switched off in this realm ' +
              '(<code>totp.enabled</code> on <a href="/admin/totp">TOTP ' +
              'MFA</a>), so nobody new can enrol one.'))
        : (!mech.totpUsable
            ? this.warn('<strong>An enrolment exists and this process cannot ' +
              'read it</strong> — ' +
              this.esc(d.why || 'the stored value is unusable') +
              '. Almost always a shared secret sealed under a key-encryption ' +
              'key that has since been rotated. <strong>They are REFUSED at ' +
              'the code step rather than let through on one factor</strong>, ' +
              'so they cannot sign in at all until this is cleared and set ' +
              'up again.')
            : '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
              '<tr><th>Code</th><td>' + this.esc(String(d.digits || 6)) + ' ' +
                'digits, HMAC-' +
              this.esc(String(d.algorithm || 'SHA1').replace(/^SHA/, 'SHA-')) +
                ', a new one every ' + this.esc(String(d.period || 30)) + ' ' +
                'seconds. <strong>These are the parameters the QR code told ' +
                'the app</strong> and are what verification uses — changing ' +
                '<a href="/admin/totp">the settings</a> affects NEW ' +
                'enrolments only, because this service cannot reach into ' +
                'somebody\'s phone.' +
                ((d.digits && d.digits !== totpLive.digits) ||
                 (d.period && d.period !== totpLive.period) ||
                 (d.algorithm && d.algorithm !== totpLive.algorithm)
                  ? ' <strong>This enrolment differs from the current ' +
                    'settings</strong> (' + this.esc(String(totpLive.digits)) +
                    ' digits, HMAC-' +
                    this.esc(String(totpLive.algorithm)
                      .replace(/^SHA/, 'SHA-')) +
                    ', every ' + this.esc(String(totpLive.period)) + 's), ' +
                    'which is correct and is what that sentence means.'
                  : '') + '</td></tr>' +
              '<tr><th>Skew forgiven</th><td>' +
              this.esc(String(totpLive.window)) +
                ' step(s) either side. <strong>This one is live for ' +
                'everybody</strong> — how much a deployment forgives a ' +
                'drifting clock is a policy rather than something the app ' +
                'was told.</td></tr><tr><th>Shared secret</th><td>' + (d.sealed
                ? '<span class="state-valid">sealed</span> under this ' +
                  'service\'s key-encryption key, so <a ' +
                  'href="/admin/ldap/directory">the directory page</a> shows ' +
                  'ciphertext.'
                : 'stored as <strong>base32, in the clear</strong> — ' +
                  'development mode, where the key-encryption key is ' +
                  'generated per run and sealing would mean an authenticator ' +
                  'that silently stopped working at the next restart. ' +
                  'Verifying a code means COMPUTING it, so unlike a password ' +
                  'the secret cannot be hashed. That is the whole reason ' +
                  'this mechanism can never be a FIRST factor.') +
                  '</td></tr>' +
              '<tr><th>Enrolled</th><td>' +
                this.esc(d.enrolledAt ? this.whenText(d.enrolledAt) :
                         'not recorded') +
                '</td></tr>' +
              '<tr><th>Last code accepted</th><td>' +
                this.esc(d.lastUsedAt ? this.whenText(d.lastUsedAt) : 'never') +
                '. The step it was in is stored with the enrolment, so that ' +
                'code cannot be used again (RFC 6238 section ' +
                '5.2).</td></tr></table>')) +
      (mech.totp && state.write
        ? '<form method="post" action="/admin/users">' +
          '<input type="hidden" name="action" value="clear-totp">' +
          '<input type="hidden" name="user" value="' + this.esc(key) + '">' +
          carryBack +
          '<div class="formrow"><button class="danger" title="' +
          this.esc('Clears the enrolment so this person can set up a new ' +
                   'one. This is the ONLY way back for somebody who has lost ' +
                   'their phone: the secret is on that device and this ' +
                   'service cannot reach it.') +
          '">Clear the authenticator app</button></div></form>' +
          this.note('<strong>This is the only way back for somebody who has ' +
          'lost their phone.</strong> The shared secret lives on that device ' +
          'and this service cannot reach it, and there is deliberately no ' +
          'self-service reset — a second factor anybody can remove is not a ' +
          'second factor. Clearing drops the account to one factor and ' +
          'cannot lock anybody out, because a one-time code is never a way ' +
          'in by itself. They set a new one up at <code>/portal/mfa</code>.')
        : (mech.totp
            ? this.note('Clearing needs <strong>Admin Write</strong>.')
            : ''));

    // --- security keys
    // --------------------------------------------------------
    const keyRow = function (one) {
      log.debug("Entering keyRow().");
      log.debug("Leaving keyRow().");
      return '<tr><td>' + self.esc(one.label || 'security key') + '</td>' +
        '<td>' + (one.role === 'primary'
          ? '<span class="state-valid" title="' +
            self.esc('Signs them in on its own — this is a way IN rather ' +
                     'than a second factor.') + '">primary</span>'
          : 'second factor') + '</td>' +
        // `shortened()` EMITS ITS OWN `<code title=…>` AND ESCAPES THE TEXT, so
        // there is neither an esc() nor a wrapper here. The first version had
        // both, and the cell rendered the literal characters `<code title=…>`.
        '<td>' + self.shortened(one.credentialId || '', 24) + '</td>' +
        '<td class="num">' + self.esc(String(one.signCount || 0)) + '</td>' +
        '<td>' +
        self.esc(one.enrolledAt ? self.whenText(one.enrolledAt) : '—') +
        '</td><td>' + (state.write
          ? '<form method="post" action="/admin/users">' +
            '<input type="hidden" name="action" value="clear-key">' +
            '<input type="hidden" name="user" value="' + self.esc(key) + '">' +
            '<input type="hidden" name="credentialId" value="' +
            self.esc(one.credentialId || '') + '">' + carryBack +
            '<button class="danger" title="' +
            self.esc(one.role === 'primary'
              ? 'This may be the only credential on the account. The removal ' +
                     'is REFUSED if it would leave nobody able to sign in.'
              : 'Removes this key. The account drops to one factor and stays ' +
                     'reachable.') + '">Remove</button></form>'
          : '') + '</td></tr>';
    };
    const allKeys = mfaKeys.concat(primaryKeys);
    const keysBlock = '<h3>Security keys (WebAuthn)</h3>' +
      (allKeys.length
        ? '<table><tr><th>Label</th><th>Role</th><th>Credential id</th>' +
          '<th class="num">Sign count</th><th>Enrolled</th><th></th></tr>' +
          allKeys.map(keyRow).join('') + '</table>' +
          this.note('<strong>The sign count is WebAuthn\'s replay ' +
          'defence</strong>: an authenticator\'s counter only ever goes up, ' +
          'so one that went backwards is a cloned key and the assertion is ' +
          'refused. A counter stuck at zero is an authenticator that does ' +
          'not implement one, which is allowed and is what most platform ' +
          'authenticators do.')
        : this.note('<strong>None enrolled.</strong> ' +
          (keyLive.enabled
            ? 'They enrol one themselves at <code>/portal/keys</code>, at ' +
              'the sign-in screen, or while spending an activation link. ' +
              'There is no way to enrol one from here and there cannot be: a ' +
              'WebAuthn ceremony happens in the person\'s own browser ' +
              'against their own authenticator, and this console is not that ' +
              'browser.'
            : 'Security keys are switched off in this realm ' +
              '(<code>webauthn.enabled</code> on <a ' +
              'href="/admin/webauthn">WebAuthn</a>), so nobody new can enrol ' +
              'one. A key already enrolled goes on working.'))) +
      (allKeys.length && !state.write
        ? this.note('Removing a key needs <strong>Admin Write</strong>.') :
          '') +
      this.note('<strong>What this realm currently allows</strong>: a key ' +
      'may be a primary credential — ' +
      (keyLive.primaryAllowed ? 'yes' : 'NO') + ' — ' +
      'and a second factor — ' + (keyLive.mfaAllowed ? 'yes' : 'NO') + ' — ' +
      'with at ' +
      'most ' + this.esc(String(keyLive.maxKeysPerPerson)) + ' per person. ' +
      'Those refuse an ENROLMENT and never an authentication, so a key above ' +
      'in a role this realm no longer allows goes on working. <a ' +
      'href="/admin/webauthn">WebAuthn</a> is where they are set.');

    // --- the recovery codes
    // --------------------------------------------------- **THE COUNTS AND
    // NEVER THE CODES**, which is why this block reads `mech.backupCodes` — a
    // status object — and there is no call anywhere in this console to the
    // codes themselves. There is no such door anywhere any more — a set is
    // stored as scrypt hashes since 2026-09-11 — and an administrative door
    // that showed somebody's recovery codes would hand a working second factor
    // to whoever holds Admin Read, which is the same refusal this page already
    // makes about enrolling an authenticator app from here. The person's own
    // `/portal/mfa` is the only reader.
    const b = mech.backupCodes || { present: false, total: 0, remaining: 0 };
    const recoveryBlock = '<h3>Recovery codes</h3>' +
      (!b.present
        ? this.note('<strong>None issued.</strong> ' +
          (recoveryLive.enabled
            ? 'A set is created automatically the first time this person ' +
              'enrols a second factor — a confirmed authenticator app, or a ' +
              'security key in the <code>mfa</code> role. <strong>There is ' +
              'no control anywhere that issues one on request</strong>, here ' +
              'or on <code>/portal</code> or on <code>/admin-api</code>: a ' +
              'way back that somebody has to remember to ask for produces ' +
              'exactly the population it exists to protect, one person at a ' +
              'time. Nobody with a second factor enrolled BEFORE 2026-09-10 ' +
              'has a set, which is the ordinary reason this says none — ' +
              'clearing and re-enrolling is what issues one.'
            : 'Recovery codes are switched off in this realm ' +
              '(<code>backupCodes.enabled</code> on <a ' +
              'href="/admin/backup-codes">Recovery codes</a>), so no new set ' +
              'will be issued. A set already issued goes on working.'))
        : (!b.usable
            ? this.warn('<strong>A set exists and this process cannot read ' +
                        'it</strong> — ' +
              this.esc(b.why || 'the stored set is unusable') + '. Almost ' +
              'always a set sealed under a key-encryption key that has since ' +
              'been rotated. <strong>They are REFUSED at the recovery screen ' +
              'rather than let through</strong>, and the codes cannot be ' +
              'shown to them either, so this set is worth nothing until it ' +
              'is cleared — after which the next second factor they enrol ' +
              'issues a new one.')
            : '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
              '<tr><th>Unused</th><td>' +
                (b.remaining === 0
                  ? '<strong class="state-revoked">0</strong> of ' +
                    this.esc(String(b.total)) + ' — <strong>every code has ' +
                    'been used</strong>, so this account has a second factor ' +
                    'and no way back. A set is issued once and is never ' +
                    'topped up; clearing it below is what lets the next ' +
                    'enrolment issue a new one.'
                  : (b.remaining <= 3
                      // `state-expired` is this console's AMBER, despite the
                      // name — see the stylesheet. Nearly out is a warning and
                      // not a failure, which is exactly what that colour says
                      // everywhere else on this page.
                      ? '<span class="state-expired">' +
                        this.esc(String(b.remaining)) +
                        '</span> of ' + this.esc(String(b.total)) +
                        ' — nearly out, and they are not topped up.'
                      : '<span class="state-valid">' +
                        this.esc(String(b.remaining)) +
                        '</span> of ' + this.esc(String(b.total)) + '.')) +
                '</td></tr>' +
              '<tr><th>Issued</th><td>' +
                this.esc(b.generatedAt ? this.whenText(b.generatedAt) :
                         'not recorded') +
                '. <strong>Once.</strong> Enrolling a different second ' +
                'factor does not reissue them, deliberately: somebody who ' +
                'printed a list and later replaced their authenticator app ' +
                'would otherwise be holding strings that had stopped working ' +
                'with nothing having said so.</td></tr>' +
              '<tr><th>Last used</th><td>' +
                this.esc(b.lastUsedAt ? this.whenText(b.lastUsedAt) : 'never') +
                '</td></tr>' +
              '<tr><th>Stored</th><td>' + (b.sealed
                ? '<span class="state-valid">encrypted</span> — AES-256-GCM ' +
                  'under this service\'s key-encryption key, so <a ' +
                  'href="/admin/ldap/directory">the directory page</a> shows ' +
                  'ciphertext.'
                : 'as the <strong>strings they were shown</strong> — ' +
                  'development mode, where the key-encryption key is ' +
                  'generated per run and encrypting them would mean a ' +
                  'printed recovery list that stopped working at the next ' +
                  'restart, which is the precise failure this mechanism ' +
                  'exists to prevent.') +
                ' <strong>ENCRYPTED AND NOT HASHED</strong>, unlike ' +
                '<code>userPassword</code>, and for a product reason rather ' +
                'than an arithmetic one: the person may look at their ' +
                'remaining codes again on <code>/portal/mfa</code>, and a ' +
                'hash cannot be shown.</td></tr></table>' +
              this.note('<strong>This console never shows the codes ' +
              'themselves</strong>, and there is no control here or on ' +
              '<code>/admin-api</code> that does. They are a working second ' +
              'factor; showing them to whoever holds Admin Read would be the ' +
              'same door this page already refuses to open for an ' +
              'authenticator enrolment. The person reads them back on their ' +
              'own <code>/portal/mfa</code>.'))) +
      (b.present && state.write
        ? '<form method="post" action="/admin/users">' +
          '<input type="hidden" name="action" value="clear-backup-codes">' +
          '<input type="hidden" name="user" value="' + this.esc(key) + '">' +
          carryBack +
          '<div class="formrow"><button class="danger" title="' +
          this.esc('Deletes the set. This is the ONLY way to a second set: ' +
                   'the next second factor this person enrols issues a new ' +
                   'one. It cannot lock anybody out — a recovery code is ' +
                   'never a way in by itself — but it does take away their ' +
                   'way back.') +
          '">Clear the recovery codes</button></div></form>' +
          this.note('<strong>Clearing is the only route to a second ' +
          'set.</strong> It cannot lock anybody out — a recovery code is ' +
          'never a way in on its own — but it removes the way BACK, so a ' +
          'person whose set is cleared and who then loses their phone needs ' +
          'you again. The sequence that issues a new set is: clear this, ' +
          'have them clear and re-enrol their second factor, and the ' +
          'enrolment issues one.')
        : (b.present
            ? this.note('Clearing needs <strong>Admin Write</strong>.')
            : ''));

    const html = heading +
      this.note('Everything in this section is about <code>' +
                this.esc(row.name) +
      '</code> and is read from their own directory entry, through the same ' +
      '<code>credentials.mechanismsFor()</code> the sign-in screen and the ' +
      'user portal ask. The mechanisms themselves are configured on <a ' +
      'href="/admin/totp">TOTP MFA</a> and <a ' +
      'href="/admin/webauthn">WebAuthn</a> under Protocols; this is who ' +
      'holds what.') +
      wayIn + totpBlock + keysBlock + recoveryBlock;

    log.debug("Leaving AdminConsole.mfaSection(). totp=" + mech.totp + ", " +
              allKeys.length +
              " key(s).");
    return {
      html: html,
      // ONE SOURCE for what this panel says (2026-09-12): the layer builds it,
      // this draws it. See admin-core/admin_views.ts.
      json: adminViews.mfaJson(key)
    };
  }

  // ---------------------------------------------------------------------------
  // A PERSON'S CREDENTIALS: THEIR RFC 7523 AND RFC 7522 KEY PAIRS, ON THEIR OWN
  // PAGE (2026-09-13).
  //
  // The application page's Credentials section, for a person. Per profile: the
  // key pair on their entry and where it came from, and the controls that
  // REPLACE it — issue one from this realm's certificate authority, or upload a
  // certificate the person already holds (this realm's alone, or another
  // authority's with its whole chain) — and take it off.
  //
  // **THE SAME ACTIONS AND NOT NEW ONES.** Issue is `/admin/pki`'s `issue` with
  // `target=person`, posted to `/admin/pki/person` because what comes back is a
  // PRIVATE KEY and that route answers with a page rather than a redirect;
  // Upload is `upload-certificate` and Take off is `revoke`, both to
  // `/admin/pki`. All three carry `from`, so the handler sends the reader back
  // here through userReturnTo(), and all three are `/admin-api/pki/{action}`
  // with no second operation — moving a form is not moving an action.
  //
  // **WHAT IS DIFFERENT FROM THE APPLICATION'S IS THE PRIVATE KEY.** An
  // application's is readable afterwards through `applications.view()`; a
  // person's is not readable by anybody, so this section says whether one is
  // held and the issue page is the one place it is ever shown.
  //
  // **THE CONTROLS NEED ADMIN WRITE AND ARE DRAWN ONLY FOR IT**, the
  // second-factor section's rule beside it: a button whose only outcome is the
  // gate's refusal is a control that can only fail.
  // ---------------------------------------------------------------------------
  userCredentialsSection(key, state, gate, back) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.userCredentialsSection(). key=" + key);
    const hidden = function (name, value) {
      log.debug("Entering hidden().");
      log.debug("Leaving hidden().");
      return '<input type="hidden" name="' + name + '" value="' +
             self.esc(value) +
             '">';
    };
    const heading = '<h2 id="credentials">Credentials: assertion key ' +
      'pairs</h2>' +
      this.note('What this person can SIGN an authorization grant with: a ' +
      'key pair for RFC 7523&rsquo;s JWT bearer assertion and one for RFC ' +
      '7522&rsquo;s SAML 2.0 bearer assertion, on their own directory entry. ' +
      '<strong>A person&rsquo;s assertion may only be about ' +
      'themselves</strong> &mdash; the token endpoint refuses one naming ' +
      'anybody else as its <code>sub</code> or <code>&lt;Subject&gt;</code>, ' +
      'because a key on one person&rsquo;s entry is that person&rsquo;s ' +
      'credential rather than permission to speak for others. A party that ' +
      'may assert about other people is an APPLICATION with the issuer ' +
      'declared on it. The two profiles&rsquo; key pairs share no attribute, ' +
      'so neither signs for the other and taking one off leaves the other ' +
      'working.');
    if (!state.storable) {
      log.debug("Leaving AdminConsole.userCredentialsSection(). No directory.");
      return heading + this.warn('This process has no directory, so nobody ' +
        'can hold an assertion key pair.', 'No directory');
    }
    if (!state.found) {
      log.debug("Leaving AdminConsole.userCredentialsSection(). No entry.");
      return heading + this.note('<code>' + this.esc(key) + '</code> has no ' +
        'entry under <code>ou=users</code> in this realm, so there is ' +
        'nowhere to put a key pair. An identity this service has seen ' +
        'without a directory entry &mdash; a client, a bind DN, a subject ' +
        'somebody else asserted &mdash; cannot hold one.');
    }
    const username = state.username;
    const algOptions = state.ca.keyAlgorithms.map(function (one) {
      return '<option value="' + self.esc(one.id) + '">' + self.esc(one.label) +
             '</option>';
    }).join('');
    const purposeHtml = state.purposes.map(function (p) {
      const names = p.attributes;
      const anchor = 'credentials-' + p.id;
      const chainCells = p.chain.length
        ? '<ol>' + p.chain.map(function (link) {
            return '<li>' + self.certificateCells(link) + '</li>';
          }).join('') + '</ol>'
        : (p.held ? '<span class="state-none">none stored</span>' : '&mdash;');
      const managed = p.held
        ? '<table><tr><th>Fact</th><th>Value</th></tr>' +
          '<tr><td>Source<div class="sub"><code>' + self.esc(names.source) +
          '</code></div></td><td><code>' + self.esc(p.source) + '</code>' +
          '<div class="sub">' + (PERSON_KEY_SOURCE_SENTENCES[p.source] ||
                                 self.esc(p.source)) + '</div></td></tr>' +
          '<tr><td>Certificate<div class="sub"><code>' +
          self.esc(names.certificate) +
          '</code></div></td><td>' + self.certificateCells(p.certificate) +
          '</td></tr>' +
          '<tr><td>Chain<div class="sub"><code>' + self.esc(names.chain) +
          '</code></div></td><td>' + chainCells + '</td></tr>' +
          '<tr><td>Key handle<div class="sub"><code>' + self.esc(names.handle) +
          '</code></div></td><td><code>' + self.esc(p.handle || '—') +
          '</code> <span class="sub">(' + self.esc(p.handleLabel) +
          ')</span></td></tr>' +
          '<tr><td>Private key<div class="sub"><code>' +
          self.esc(names.privateKey) + '</code></div></td><td>' +
          (p.privateKeyHeld
            ? '<span class="state-valid">held by this service</span>' +
              (state.sealsAtRest ? ' &mdash; sealed at rest' : '') +
              '<div class="sub">It was shown once, when it was issued, and ' +
              'nothing opens it again &mdash; not this page, not ' +
              '<code>/admin-api</code>. Issuing again replaces it.</div>'
            : '<span class="state-none">not held here</span><div class="sub">' +
              'The person keeps their own private key.</div>') +
          '</td></tr>' +
          '<tr><td>Asserts as<div class="sub"><code>' + self.esc(names.issuer) +
          '</code></div></td><td>' + p.effectiveIssuers.map(function (iss) {
            return '<code>' + self.esc(iss) + '</code>';
          }).join('<br>') + (p.issuers.length ? ''
            : ' <span class="sub">(their own name &mdash; nothing is ' +
              'declared)</span>') + '</td></tr></table>'
        : self.note('No key pair is held for this profile. Issue one from ' +
                    'this realm&rsquo;s certificate authority, or upload a ' +
                    'certificate the person already holds.') +
          (p.issuers.length
            ? self.note('An issuer is declared for it (' + p.issuers.map(
                function (iss) {
                  return '<code>' + self.esc(iss) + '</code>';
                }).join(', ') + ') and nothing can be signed under it until ' +
                'a key pair is here.')
            : '');

      if (!gate.write) {
        return '<h3 id="' + anchor + '">' + self.esc(p.label) + '</h3>' +
               managed +
          self.note('Replacing or taking off a key pair needs <strong>Admin ' +
                    'Write</strong>.');
      }
      const issueForm = state.ca.available
        ? '<form method="post" action="/admin/pki/person">' +
          '<div class="formrow">' + hidden('action', 'issue') +
          hidden('target', 'person') + hidden('from', '/admin/users') +
          hidden('back', back) + hidden('identifier', username) +
          hidden('purpose', p.id) +
          '<label for="' + anchor + '-alg">Key algorithm</label>' +
          '<select id="' + anchor + '-alg" name="leafKeyAlg">' +
          '<option value="">(the Issuing CA&rsquo;s: ' +
          self.esc(state.ca.keyAlg) +
          ')</option>' + algOptions + '</select>' +
          '<label for="' + anchor + '-days">Days</label>' +
          '<input type="number" id="' + anchor + '-days" name="days" min="1" ' +
          'value="' + self.esc(String(state.ca.leafLifetimeDays)) + '">' +
          '<label for="' + anchor + '-issuer">Declared issuer</label>' +
          '<input id="' + anchor + '-issuer" name="issuer" placeholder="' +
          '(their username)">' +
          '<button type="submit">' + (p.held
            ? 'Replace it with a key pair from this realm&rsquo;s CA'
            : 'Issue a key pair from this realm&rsquo;s CA') + '</button>' +
          '</div></form>'
        : self.note('This realm has no certificate authority yet, so there ' +
                    'is nothing to issue a key pair from. <a ' +
                    'href="/admin/pki">Build one on the PKI page</a>, or ' +
                    'upload a certificate below.');

      const uploadForm = '<form method="post" action="/admin/pki">' +
        hidden('action', 'upload-certificate') + hidden('target', 'person') +
        hidden('from', '/admin/users') + hidden('back', back) +
        hidden('identifier', username) + hidden('purpose', p.id) +
        '<div class="formrow"><label for="' + anchor + '-cert">Certificate ' +
        '(PEM)</label><textarea id="' + anchor + '-cert" name="certificate" ' +
        'rows="6" required placeholder="-----BEGIN ' +
        'CERTIFICATE-----"></textarea></div><div class="formrow"><label ' +
        'for="' + anchor + '-chain">Chain ' +
        '(PEM, every intermediate and the root)</label><textarea id="' +
        anchor +
        '-chain" name="chain" rows="6" placeholder="-----BEGIN ' +
        'CERTIFICATE-----"></textarea></div><div class="formrow"><button ' +
        'type="submit">' +
        (p.held ? 'Replace it with this certificate' :
         'Upload the certificate') +
        '</button></div></form>';

      const takeOff = p.held || p.issuers.length
        ? '<form method="post" action="/admin/pki">' +
          '<div class="formrow">' + hidden('action', 'revoke') +
          hidden('target', 'person') + hidden('from', '/admin/users') +
          hidden('back', back) + hidden('identifier', username) +
          hidden('purpose', p.id) +
          '<button type="submit" class="danger">Take this key pair ' +
          'off</button><span class="sub">Not revocation: the certificate ' +
          'stays valid and this service stops accepting what it signs. The ' +
          'declared issuer goes with it; the other profile&rsquo;s key pair ' +
          'is untouched.</span></div></form>'
        : '';

      return '<h3 id="' + anchor + '">' + self.esc(p.label) + '</h3>' +
             managed +
        '<h4>Replace it</h4>' +
        self.note('<strong>Either way REPLACES the key pair on this ' +
        'entry.</strong> Issuing generates a new key pair here and signs it ' +
        'with this realm&rsquo;s Issuing CA; the private key is sealed on ' +
        'the entry and <strong>shown once</strong>, on the page the button ' +
        'opens. Uploading registers a certificate the person already holds, ' +
        'and this service keeps no private key for it. A certificate from ' +
        'THIS realm&rsquo;s authority may be uploaded alone, and must have ' +
        'been issued to this person. <strong>One from any other authority ' +
        'must come with its full chain</strong> &mdash; every intermediate ' +
        'and the self-signed root &mdash; and every link is verified. An ' +
        'upload carrying a private key is refused.') +
        issueForm + uploadForm + takeOff;
    }).join('');
    log.debug("Leaving AdminConsole.userCredentialsSection().");
    return heading + purposeHtml +
      (state.selfService
        ? this.note('The person can also issue themselves an RFC 7523 key ' +
                    'pair at <code>/portal/signing-key</code>; that door ' +
                    'offers the JWT profile only.')
        : '');
  }

  // ---------------------------------------------------------------------------
  // RESET A PASSWORD, ISSUE A RESET LINK, DISABLE PASSKEYS, DISABLE OR REQUIRE
  // MFA — ON THE PERSON'S OWN PAGE (2026-09-13).
  //
  // Six controls over six actions on `usersAction()`
  // (`admin-core/admin_actions.ts` argues each), posted to `/admin/users` with
  // `from=user` so the reader lands back here, and mirrored at
  // `POST /admin-api/users/{action}`.
  //
  // **A RESET ANSWERS WITH A PAGE**, because what it hands back exists once —
  // see the users action endpoint. **EVERY CONTROL IS DRAWN FOR ADMIN WRITE
  // ONLY**, the rule the two sections above follow: a button whose only outcome
  // is the gate's refusal is a control that can only fail. And a control that
  // cannot do anything for THIS person is not drawn at all — no Disable
  // passkeys button for somebody with no primary key — with a sentence saying
  // why it is absent.
  // ---------------------------------------------------------------------------
  userCredentialControlsSection(key, factors, gate, back) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.userCredentialControlsSection(). key=" +
              key);
    const heading = '<h2 id="credential-controls">Password and second-factor ' +
                    'controls</h2>';
    if (!factors) {
      log.debug("Leaving AdminConsole.userCredentialControlsSection(). No " +
                "store.");
      return heading + this.note('No credential store is installed in this ' +
        'process, so there is nothing here to reset.');
    }
    const requirement = factors.mfaRequirement ||
      { required: false, byUser: false, byRealm: false };
    const link = factors.passwordResetLink;
    const holdsSecond = factors.totp || factors.mfaKeys > 0 ||
      !!(factors.backupCodes && factors.backupCodes.present);
    const state = '<table class="key"><tr><th>What</th><th>Now</th></tr>' +
      '<tr><th>Account</th><td>' + (factors.disabled
        ? '<strong class="state-expired">DISABLED</strong> — every door ' +
          'refuses them (<code>pwdAccountLockedTime</code>)'
        : '<span class="state-valid">enabled</span>') + '</td></tr>' +
      '<tr><th>Password</th><td>' + (factors.password
        ? '<span class="state-valid">set</span>'
        : '<span class="state-none">none</span>') +
      (factors.passwordChangeRequired
        ? ' — <strong>must be changed at their next sign-in</strong>' : '') +
      '</td></tr><tr><th>Password reset link</th><td>' + (link
        ? (link.expired
            ? '<span class="state-expired">issued and expired</span>'
            : '<span class="state-valid">outstanding</span> until ' +
              this.esc(new Date(link.expires).toISOString()))
        : '<span class="state-none">none</span>') + '</td></tr>' +
      '<tr><th>Passwordless sign-in</th><td>' + (factors.primaryKeys > 0
        ? '<span class="state-valid">' + this.esc(String(factors.primaryKeys)) +
          '</span> primary security key(s)'
        : '<span class="state-none">none</span>') + '</td></tr>' +
      '<tr><th>Second factor required</th><td>' + (requirement.required
        ? '<strong>yes</strong> — ' + [requirement.byUser ? 'on this account'
            : '', requirement.byRealm ? 'by the realm ' +
            '(<code>authn.mfaRequired</code>)' : ''].filter(Boolean)
              .join(' and ') +
          (factors.mfaRequired ? '; they hold one'
            : '; <strong>they hold none, so their next sign-in asks them to ' +
              'enrol one</strong>')
        : '<span class="state-none">no</span>') + '</td></tr></table>';
    const signalsNote = this.note('<strong>Every control below says what it ' +
      'did over Shared Signals</strong>: a CAEP ' +
      '<code>credential-change</code> for each credential set, revoked or ' +
      'removed; RISC <code>account-credential-change-required</code> for a ' +
      'reset or a reset link; RISC <code>recovery-information-changed</code> ' +
      'when recovery codes go. Each goes to every stream that asked for the ' +
      'type and covers this person, subject to ' +
      '<code>caep.autoEmitTypes</code>, <code>risc.autoEmitTypes</code> and ' +
      'the RISC opt-out state.');
    if (!gate.write) {
      log.debug("Leaving AdminConsole.userCredentialControlsSection(). Read " +
                "only.");
      return heading + state + signalsNote +
        this.note('Resetting a password, disabling a credential or requiring ' +
                  'a second factor needs <strong>Admin Write</strong>.');
    }
    // `extra` is markup for the form's own fields (#146): the reset forms'
    // "compromised" box and the disable form's RISC reason — the console half
    // of what `/admin-api/users` takes, per rule 7.
    const form = function (action, label, title, danger, extra?) {
      log.debug("Entering form(). " + action);
      log.debug("Leaving form().");
      return '<form method="post" action="/admin/users">' +
        '<input type="hidden" name="action" value="' + self.esc(action) + '">' +
        '<input type="hidden" name="user" value="' + self.esc(key) + '">' +
        '<input type="hidden" name="from" value="user">' +
        '<input type="hidden" name="back" value="' + self.esc(back) + '">' +
        (extra || '') +
        '<div class="formrow"><button' + (danger ? ' class="danger"' : '') +
        ' title="' + self.esc(title) + '">' + label + '</button></div></form>';
    };
    const compromisedBox = '<div class="formrow"><label><input ' +
      'type="checkbox" name="compromised" value="true"> the password was ' +
      'compromised (RISC <code>credential-compromise</code>)</label></div>';
    const reset = '<h3>Reset the password</h3>' +
      this.note('<strong>Reset password</strong> sets a generated password ' +
                'and shows ' +
      'it to you ONCE — note it and give it to them. They must choose their ' +
      'own at their next sign-in, and they are signed out of everything now. ' +
      '<strong>Generate a reset link</strong> instead REMOVES the password ' +
      'they have, signs them out of everything, and shows you a single-use ' +
      'link to send them, valid for ' +
      '<code>security.passwordResetTtlMinutes</code>; at it they choose a ' +
      'new password under this realm\'s password policy.') +
      this.note('Tick <strong>the password was compromised</strong> when ' +
                'that is WHY you are resetting it: RISC receivers are then ' +
                'also told <code>credential-compromise</code>. A reset link ' +
                'always tells them <code>recovery-activated</code>.') +
      form('reset-password', 'Reset password',
           'A generated password, shown once; they change it at next ' +
           'sign-in; they are signed out everywhere.', true, compromisedBox) +
      form('issue-password-reset', 'Generate a password reset link',
           'Removes their current password, signs them out everywhere, and ' +
           'shows a single-use link to send them.', true, compromisedBox);
    const passkeys = '<h3>Passwordless sign-in</h3>' + (factors.primaryKeys > 0
      ? (factors.password
          ? this.note('Removes all ' + this.esc(String(factors.primaryKeys)) +
                      ' primary security key(s), so a passkey no longer ' +
                      'signs them in without a password. A key they hold as ' +
                      'a second factor is untouched.') +
            form('disable-primary-keys',
                 'Disable passkeys as primary sign-in',
                 'Removes every primary-role security key.', true)
          : this.warn('They have <strong>no password</strong>, so their ' +
                      'primary keys are the only way they can sign in and ' +
                      'disabling them is refused. Reset the password or ' +
                      'issue a reset link first.'))
      : this.note('They hold no primary security key, so there is no ' +
                  'passwordless sign-in to disable.'));
    const mfa = '<h3>Second factors</h3>' + (holdsSecond
      ? this.note('<strong>Disable all MFA</strong> removes their ' +
                  'authenticator app, every security key in the mfa role and ' +
                  'their recovery codes. It cannot lock them out — none of ' +
                  'those is a way in. ' +
                  (requirement.required
                    ? 'A second factor is still required of them, so their ' +
                      'next sign-in asks them to enrol a new one.'
                    : 'A password alone signs them in afterwards.')) +
        form('disable-mfa', 'Disable all MFA',
             'Removes the authenticator app, mfa keys and recovery codes.',
             true)
      : this.note('They hold no second factor, so there is nothing to ' +
                  'disable.')) +
      (requirement.byUser
        ? this.note('A second factor is <strong>required</strong> of them on ' +
                    'their account.') +
          form('stop-requiring-mfa', 'Stop requiring MFA for this person',
               'Takes the per-account requirement off; a realm requirement ' +
               'is unaffected.', false)
        : this.note('<strong>Require MFA</strong> makes the sign-in screen ' +
                    'ask them for a second factor — and, while they hold ' +
                    'none, to enrol an authenticator app or a security key ' +
                    'before it signs them in. A passwordless sign-in is ' +
                    'refused while it is required.' +
                    (requirement.byRealm ? ' The realm already requires it ' +
                      'of everybody (<code>authn.mfaRequired</code>).' : '')) +
          form('require-mfa', 'Require MFA for this person',
               'They must use a second factor, and enrol one at their next ' +
               'sign-in if they hold none.', false));
    // THE ACCOUNT ITSELF (2026-09-17, #36 follow-up): disable, which ends
    // everything the person holds, and enable. `common/account_state.ts`.
    const account = '<h3>The account</h3>' + (factors.disabled
      ? this.note('The account is <strong>disabled</strong>. Enabling it ' +
                  'lets them sign in again; nothing ended by the disable ' +
                  'comes back, and RISC receivers are told ' +
                  '<code>account-enabled</code>.') +
        form('enable', 'Enable the account',
             'Clears pwdAccountLockedTime; they sign in afresh.', false)
      : this.note('<strong>Disable the account</strong> refuses them at ' +
                  'every door — every sign-in, every token grant (a refresh ' +
                  'token included), a Kerberos AS-REQ, an LDAP bind, this ' +
                  'console and the management API — and ends everything ' +
                  'they hold now, as a global logout does: back-channel ' +
                  'Logout Tokens go to their relying parties, CAEP ' +
                  '<code>session-revoked</code> and RISC ' +
                  '<code>account-disabled</code> to the receivers. ' +
                  'Front-channel notifications need their own browser and ' +
                  'are not sent from here. SCIM <code>active: false</code> ' +
                  'is the same act.') +
        form('disable', 'Disable the account',
             'Sets pwdAccountLockedTime and signs them out of everything.',
             true,
             '<div class="formrow"><label>Reason for RISC receivers ' +
             '<select name="riscReason"><option value="">none given' +
             '</option><option value="hijacking">hijacking — the account ' +
             'was taken over</option><option value="bulk-account">' +
             'bulk-account — one of many created in bulk</option>' +
             '</select></label></div>'));
    log.debug("Leaving AdminConsole.userCredentialControlsSection().");
    return heading + state + signalsNote + account + reset + passkeys + mfa;
  }

  userDetailPage(req, key) {
    const { log, adminViews, gateStateFor, queryWith, DEFAULT_BLOCKS_PER_PAGE,
            DEFAULT_PER_PAGE } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.userDetailPage(). key=" + key);
    const view = adminViews.userDetailJson(req, key);
    if (!view) {
      log.debug("Leaving AdminConsole.userDetailPage(). Nothing known.");
      return null;
    }
    const detail = view.detail;
    const row = view.row;
    const sessionRows = view.sessionRows;
    const live = view.live;
    const split = view.split;
    const params = view.params;
    const back = view.back;
    const valid = view.valid;
    const expired = view.expired;
    // THE TWO SECTIONS ARE MARKUP, AND THE LAYER HANDS BACK ONLY THEIR JSON.
    // The first pass of the 2026-09-12 split read `view.directory` and
    // `view.mfa` here and concatenated their `.html` — which the layer never
    // builds — so the directory entry and the second factors vanished from
    // this page while `/admin-api/users?user=` went on answering both. The
    // renderers are drawn here and the JSON still comes from the view, so the
    // page and the resource keep one source for what those panels say.
    const directory = { html: this.ldapObjectSection(row, key).html,
                        json: view.directory.json };
    const mfa = { html: this.mfaSection(row, key, gateStateFor(req), back).html,
                  json: view.mfa.json };
    const sessionPage = view.sessionPage;
    const sessionTokenPages = view.sessionTokenPages;
    const endedPage = view.endedPage;
    const sessionlessPage = view.sessionlessPage;
    const artifactPage = view.artifactPage;
    const sessionsNav = this.pageNavPair('/admin/users', params,
                                         sessionPage.paging);
    // The token paging of each session that is on this page of sessions, kept
    // beside the block rather than recomputed for the JSON below: two calls
    // with the same arguments would be two chances for the page and the reply
    // to disagree about which tokens a reader is looking at.
    const sessionBlocks = sessionPage.shown.map(function (session, index) {
      return self.sessionBlock(session, sessionTokenPages[index], back, params);
    }).join('');

    // ONE NAME PER LIST, and it is the name the reply's array carries: the
    // parameter is `<array>Page` and the paging object beside it is
    // `<array>Paging`. Shortening these two to `endedPage` and
    // `sessionlessPage` read better and was wrong — a caller reading
    // `tokensWithNoSession` in the reply had to be told separately that the
    // parameter moving it was called something else, which is the sort of thing
    // a document gets right and a client author never finds.
    const endedNav = this.pageNavPair('/admin/users', params, endedPage.paging);
    const sessionlessNav = this.pageNavPair('/admin/users', params,
                                            sessionlessPage.paging);
    const artifactNav = this.pageNavPair('/admin/users', params,
                                         artifactPage.paging);

    const inner = this.messagesOf(req) +
      '<div class="tiles">' +
        this.tile(row.authentications, 'authentications') +
        this.tile(row.protocols.length, 'protocols') +
        this.tile(live.length, 'active sessions') +
        this.tile(detail.tokens.length, 'tokens issued') +
        this.tile(valid, 'tokens still valid') +
        this.tile(expired, 'tokens expired') +
        this.tile(detail.artifacts.length, 'assertions, tickets, credentials') +
      '</div>' +
      this.note('Everything below is about the identity <code>' +
                this.esc(row.name) +
      '</code>, ' +
      'which is <a href="/admin/users">one row of the users list</a>. ' +
      (row.authenticated
        ? 'They have presented a credential to this service ' +
            row.authentications + ' ' +
            'time(s).'
        : '<strong>They have never authenticated here.</strong> This ' +
          'identity is known only because something was issued naming them ' +
          'as its subject — an exchanged token from another issuer, a ' +
          'WS-Trust <code>OnBehalfOf</code>, or a Kerberos S4U request made ' +
          'by a service. Nothing about this row says the person was ever ' +
          'present.') +
      (row.isClient
        ? ' <strong>This is a client, not a person</strong>: it appears here ' +
          'because <code>client_credentials</code> produces tokens with a ' +
          'subject and no human behind them.'
        : '')) +

      // THE SAME PERSON AS A PICTURE. This page is the LEDGER — every token
      // with its state and its revoke button, grouped by the session it was
      // issued on — and the diagram is the RELATIONSHIPS: who holds what, by
      // which grant, and who delegated in their name. Neither is the other's
      // summary, so the link says which question the other page answers rather
      // than offering itself as "more detail".
      this.note('<a class="btn" href="' +
        this.esc('/admin/delegation/user' + queryWith({ user: key }, {})) +
        '">See this as a picture &rarr;</a> <strong>Everything issued in ' +
      'their name, drawn.</strong> Every credential as a line to the ' +
      'application holding it, labelled with the exact OAuth 2.0 grant or ' +
      'OpenID Connect flow that produced it; their sign-ins as lines into ' +
      'this service; and any delegation naming them in the same diagram. ' +
      'Tokens are revoked HERE — that page draws and changes nothing.') +

      '<h2>Names this identity has been seen under</h2>' +
      this.note('One person reaches this service under several spellings and ' +
      'they are the same row here — see the note at the top of the users ' +
      'list for what that costs.') +
      '<table><tr><th>As presented</th><th class="num">Times</th></tr>' +
      (row.forms.map(function (form) {
        return '<tr><td><code>' + self.esc(form.form) +
               '</code></td><td class="num">' + form.count + '</td></tr>';
      }).join('') || '<tr><td colspan="2">—</td></tr>') + '</table>' +
      (row.realms.length
        ? this.note('Kerberos realm(s): ' +
          this.codeList(row.realms.map(function (r) { return r.realm; })) + '.')
        : '') +

      '<h2>How they authenticated</h2><table><tr><th>Protocol</th><th ' +
      'class="num">Times</th><th>Methods</th><th>Last</th></tr>' +
      (row.protocols.map(function (family) {
        return '<tr><td>' + self.esc(family.protocol) + '</td>' +
          '<td class="num">' + family.count + '</td>' +
          '<td>' + self.esc(family.methods.map(function (m) {
            return m.method + ' ×' + m.count;
          }).join('; ')) + '</td>' +
          '<td>' + self.esc(self.whenText(family.lastAt)) + '</td></tr>';
      }).join('') || '<tr><td colspan="4">Never, here.</td></tr>') +
        '</table>' +
      this.authenticationTable(row) +

      this.perPageForm('/admin/users', 'user', key, artifactPage.paging.perPage,
                       'The session BLOCKS below start at ' +
                       DEFAULT_BLOCKS_PER_PAGE +
                       ' rather than ' + DEFAULT_PER_PAGE + ', because each ' +
                       'of them is itself a table; setting a size here ' +
                       'applies to those too.',
                       this.filterOnly(this.listViewOf('/admin/users',
                                                       req.query))) +

      '<h2>Sessions, and what was issued on each</h2>' +
      this.note('A <strong>sign-on session</strong> is a browser holding the ' +
      '<code>sts_session</code> cookie, shared between the OAuth 2.0 / OIDC ' +
      'login screen and WS-Federation. A token is placed under a session ' +
      'because the issuance said so — no token this service issues carries a ' +
      'session identifier, and OIDC\'s <code>sid</code> claim is for ' +
      'front-channel logout, so inventing one to make this page easier would ' +
      'change what every client receives. The link is recorded out of band ' +
      'at issuance instead, and it survives a refresh: a refreshed token is ' +
      'looked up by the refresh token\'s <code>jti</code> and lands under ' +
      'the same session.') +
      sessionsNav.head +
      (sessionBlocks || this.note('This user holds no sign-on session. That ' +
        'is the normal state for every identity that never used a browser ' +
        'here — a password grant, a Kerberos client, a WS-Trust requester — ' +
        'and for anyone whose session has expired and been swept.')) +
      sessionsNav.foot +

      (split.ended.length
        ? '<h3>Issued on a session that has since ended</h3>' +
          this.note('These name a session this service no longer holds. It ' +
          'is not an error and it is the ordinary end state: the session ' +
          'expired or was signed out, and the tokens it produced outlived it ' +
          '— which is exactly the position a client is in when its access ' +
          'token still verifies and the browser would be asked to sign in ' +
          'again.') +
          endedNav.head + this.userTokenTable(endedPage.shown, back, '') +
          endedNav.foot
        : '') +

      (split.sessionless.length
        ? '<h3>Issued with no browser session at all</h3>' +
          this.note('The grants that never involve a browser: ' +
          '<code>password</code>, <code>client_credentials</code>, ' +
          'OID4VCI\'s pre-authorized code, and token exchange. The Grant ' +
          'column says which. An empty Grant means the token was minted ' +
          'somewhere that states nothing about how — WS-Trust\'s JWT and the ' +
          'credential issuer both sign directly.') +
          sessionlessNav.head +
          this.userTokenTable(sessionlessPage.shown, back, '') +
          sessionlessNav.foot
        : '') +

      '<h2>Assertions, tickets and credentials</h2>' +
      this.note('None of these can be revoked here and the console does not ' +
      'pretend otherwise: nothing consults this service about a SAML ' +
      'assertion, a Kerberos ticket or a credential, so a button would ' +
      'change a number on this page and nothing at all out there. The only ' +
      'distinction is whether the validity window has closed.') +
      artifactNav.head + this.userArtifactTable(artifactPage.shown) +
      artifactNav.foot +

      directory.html +

      // ---------------------------------------------------------------------
      // AFTER THE DIRECTORY ENTRY AND BEFORE THE TWO SIGN-OUT BUTTONS
      // (2026-09-10), which is where it belongs by what a reader is doing. The
      // entry above says what this person IS; this says what they can sign in
      // WITH; the buttons below end what they currently hold. A reader arriving
      // from the Second factor column of the list lands on the section that
      // column summarises, with the whole account above it for context.
      // ---------------------------------------------------------------------
      mfa.html +

      // What they can SIGN a grant with (2026-09-13), after what they sign IN
      // with and before the buttons that end what they hold.
      this.userCredentialsSection(key, view.credentialsState, gateStateFor(req),
                                  back) +

      // WHAT AN ADMINISTRATOR CAN DO TO THEIR PASSWORD AND SECOND FACTORS
      // (2026-09-13), before the sign-out buttons, which it partly subsumes: a
      // reset signs the person out as well.
      this.userCredentialControlsSection(key, view.mfa.json, gateStateFor(req),
                                         back) +

      // ---------------------------------------------------------------------
      // TWO BUTTONS, AND THE ORDER IS THE ARGUMENT (2026-09-05).
      //
      // There was ONE here and it was labelled "Revoke everything for <name>",
      // which is what somebody reaching for a global sign-out would press. It
      // revoked JWTs and nothing else — so the person stayed SIGNED IN, and the
      // next `/oauth2/authorize`, `/wsfed`, `/saml2/sso` or `/saml11/sso`
      // request minted a fresh set of tokens on the spot. From the outside that
      // is close to a no-op, and the label said the opposite.
      //
      // The narrow act is worth keeping — "take these credentials out of
      // circulation and leave the session alone" is a real thing to want, and
      // it is what `/oauth2/revoke` does — so it stayed, renamed to say what it
      // does. What was ADDED is the act the old label promised, and it is the
      // SAME one `/admin/logout` performs: `logoutReader.terminate()` with an
      // empty selection, which walks every family (eleven since #38) in
      // `endOrder` so the
      // front-channel notifications are built before the session they hang off
      // is destroyed. A second implementation here would be a second answer to
      // "what is a live session", which is the thing rule 3m exists to prevent.
      //
      // The global one is FIRST because it is the one the old label described.
      // ---------------------------------------------------------------------
      '<h2>Sign this person out</h2>' +
      this.note('<strong>Everything, in every protocol.</strong> This is the ' +
      'same act <a ' +
      'href="' + this.esc('/admin/logout' + queryWith({}, { user: key })) +
      '">the sign-out page</a> performs, through the same function: it ends ' +
      'every browser sign-on session, notifies every OpenID Connect relying ' +
      'party, WS-Federation realm and SAML 2.0 service provider they signed ' +
      'into (an OpenID Connect relying party with a ' +
      '<code>backchannel_logout_uri</code> is POSTed a Logout Token by this ' +
      'service), invalidates their authorization codes and OID4VCI ' +
      'pre-authorized codes, closes their bound LDAP connections, stamps the ' +
      'Kerberos sign-out instant on their principal, and revokes their ' +
      'tokens. In that order — the notifications are built off the session, ' +
      'so ending it first would leave every federated partner believing they ' +
      'were still signed in.') +
      this.note('<strong>Two things it cannot do, and neither is a gap in ' +
      'this button.</strong> A front-channel notification is an iframe ' +
      'loaded in the signed-out person\'s OWN browser, and this console is ' +
      'not that browser — so what happens here is that the relying party is ' +
      'forgotten and the notification is reported rather than sent; ' +
      '<code>/logout</code> is where those actually load (the back-channel ' +
      'Logout Token is not one of these: it is sent). And ' +
      '<strong>nothing recalls a SAML assertion, a Kerberos service ticket ' +
      'or an SVID</strong>: each is valid because somebody else can verify ' +
      'it without asking this service, so the only thing that ends one is ' +
      'its own expiry. The Kerberos sign-out instant is the nearest thing ' +
      'that exists, and it refuses a TGS-REQ presenting an older ticket ' +
      'rather than recalling the tickets already issued.') +
      '<form method="post" action="/admin/logout">' +
        '<input type="hidden" name="action" value="global">' +
        '<input type="hidden" name="user" value="' + this.esc(key) + '">' +
        '<input type="hidden" name="from" value="users">' +
        '<input type="hidden" name="back" value="' + this.esc(back) + '">' +
        '<div class="formrow"><button class="danger">Sign ' +
        this.esc(row.name) +
        ' out of everything</button></div></form>' +

      '<h2>Revoke this person\'s tokens, and nothing else</h2>' +
      this.note('The narrower act, and the one this section used to be the ' +
      'whole of. Every access token, ID Token and refresh token held for ' +
      'this identity under any of its spellings, revoked through the same ' +
      'set <code>/oauth2/revoke</code> writes to. <strong>It does not sign ' +
      'them out</strong> — their browser session survives, so the next ' +
      'authorization request mints a fresh set and this will look as though ' +
      'it did nothing. That is the honest behaviour of a revocation and the ' +
      'reason the button above exists; use this one when you want the ' +
      'credentials out of circulation and the session left alone. Assertions ' +
      'and tickets are untouched either way, for the reason above.') +
      '<form method="post" action="/admin/tokens">' +
        '<input type="hidden" name="action" value="revoke-user">' +
        '<input type="hidden" name="user" value="' + this.esc(key) + '">' +
        '<input type="hidden" name="from" value="users">' +
        '<input type="hidden" name="back" value="' + this.esc(back) + '">' +
        '<div class="formrow"><button class="danger">Revoke every token for ' +
        this.esc(row.name) + '</button></div></form>';

    log.debug("Leaving AdminConsole.userDetailPage(). " +
              sessionPage.shown.length + " session(s), " +
              artifactPage.shown.length + " artifact(s) shown of " +
        detail.tokens.length +
              " token(s) held.");
    return {
      inner: inner,
      json: view.json
    };
  }

  // WHERE THIS ROW CAME FROM. Two sources and three answers, and the third —
  // both — is the ordinary one: nearly every door that creates a directory
  // entry also calls `stats.noteKnownIdentity()`. The interesting rows are the
  // ones that are only one, and they mean different things: DIRECTORY alone is
  // somebody the registry has forgotten or never held (it is capped at
  // `stats.MAX_USERS` and the directory is not), and SEEN alone is an identity
  // with no entry — a client, an LDAP bind DN, or a subject something was
  // issued for without anybody being present.
  sourceCell(row) {
    const { log, stats } = this.deps;
    log.debug("Entering AdminConsole.sourceCell().");
    const seen = !!(row.authenticated || row.knownBy !== 'directory');
    if (seen && row.inDirectory) {
      log.debug("Leaving AdminConsole.sourceCell().");
      return '<span class="state-valid" title="' +
        this.esc('This service has seen this identity AND the directory ' +
                 'holds an entry for it. That is the ordinary state.') +
                 '">both</span>';
    }
    if (row.inDirectory) {
      log.debug("Leaving AdminConsole.sourceCell().");
      return '<span title="' +
        this.esc('An entry under ou=users that this service\'s own registry ' +
                 'does not hold. The registry keeps the most recent ' +
                 stats.MAX_USERS +
                 ' identities and the directory is not capped, so a realm ' +
                 'that has been bulk loaded has many of these.') +
                 '">directory</span>';
    }
    log.debug("Leaving AdminConsole.sourceCell().");
    return '<span title="' +
      this.esc('This service has seen this identity and there is no ' +
               'directory entry for it — a client, an LDAP bind DN, or a ' +
               'subject something was issued for with nobody present. Their ' +
               'row says which.') +
      '">seen</span>';
  }

  // WHAT THIS PERSON CAN ACTUALLY GET IN WITH, which is a different question
  // from the Second factor column beside it and is the one an operator asks
  // first. A second factor is never a way in on its own — neither a one-time
  // code nor an `mfa` key — so a person with an entry, no password and no
  // `primary` key cannot sign in at all however much is enrolled on them. That
  // state is the ordinary one for somebody provisioned and not yet activated,
  // and saying "none" plainly is what sends the reader to the activation link
  // on their row.
  credentialCell(factors) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.credentialCell().");
    if (!factors) {
      log.debug("Leaving AdminConsole.credentialCell().");
      return '<span class="state-none">unknown</span>';
    }
    const parts = [];
    if (factors.password) {
      parts.push('password');
    }
    if (factors.primaryKeys) {
      parts.push(factors.primaryKeys + ' primary key' +
                 (factors.primaryKeys > 1 ? 's' : ''));
    }
    if (!parts.length) {
      log.debug("Leaving AdminConsole.credentialCell().");
      return '<span class="state-expired" title="' +
        this.esc('No password and no primary security key, so nobody can ' +
                 'sign in as this person. A second factor is never a way in ' +
                 'by itself. An activation link on their own row is how they ' +
                 'come to hold one.') +
        '">nothing yet</span>';
    }
    log.debug("Leaving AdminConsole.credentialCell().");
    return this.esc(parts.join(' + '));
  }

  // The Second factor cell, drawn the same way in the list and on the
  // drill-down so that the two cannot say different things about one person. It
  // is a function rather than two pieces of markup for the reason every other
  // shared cell here is one: this one carries the `unreadable` state, and a
  // second copy of that branch would be the copy that forgot it.
  secondFactorCell(factors) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.secondFactorCell().");
    if (!factors) {
      log.debug("Leaving AdminConsole.secondFactorCell().");
      return '<span class="state-none">unknown</span>';
    }
    if (!factors.mfaRequired) {
      log.debug("Leaving AdminConsole.secondFactorCell().");
      return '<span class="state-none">none</span>';
    }
    const parts = [];
    if (factors.mfaKeys) {
      parts.push(factors.mfaKeys + ' security key' +
                 (factors.mfaKeys > 1 ? 's' : ''));
    }
    if (factors.totp) {
      parts.push(factors.totpUsable
        ? 'authenticator app'
        : '<span class="state-expired" title="' +
          this.esc('This process cannot read the enrolment: ' +
                   ((factors.totpDetail && factors.totpDetail.why) ||
                    'the stored value is unusable') +
                   '. Their codes cannot be checked, so they are REFUSED ' +
                   'rather than let through on one factor. Clear it and let ' +
                   'them enrol again.') +
          '">authenticator app (unreadable)</span>');
    }
    log.debug("Leaving AdminConsole.secondFactorCell().");
    return parts.join(' + ');
  }

  usersListPage(req) {
    const { log, adminViews, signOnSessionRows, stats, queryWith, vcClaims,
            newUserContainer } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.usersListPage().");
    const view = adminViews.usersListJson(req);
    const wantedText = view.wantedText;
    const wantedProtocol = view.wantedProtocol;
    const population = view.population;
    const all = view.all;
    const wantedFactor = view.wantedFactor;
    const protocolsSeen = view.protocolsSeen;
    const filtered = view.filtered;
    const paging = view.paging;
    const shown = view.shown;
    const filterParams = view.filterParams;
    const authenticatedHere = view.authenticatedHere;
    const factorCounts = view.factorCounts;
    const nav = this.pageNavPair('/admin/users', filterParams, paging);

    // Sessions are counted per user here rather than fetched per row inside the
    // loop: one pass over the session map instead of one per user, and more
    // importantly one definition of "active" for the list and the drill-down
    // both.
    const liveByUser = {};
    signOnSessionRows().forEach(function (session) {
      if (session.expired) return;
      const key = stats.identityKeyOf(session.username || session.sub);
      liveByUser[key] = (liveByUser[key] || 0) + 1;
    });

    const listView = this.listViewOf('/admin/users', req.query);
    const rows = shown.map(function (row) {
      // The link carries the list AS IT IS BEING VIEWED, which is what lets the
      // trail on the other side come back to this page of this filter rather
      // than to the top of everything. See listViewOf().
      const href = '/admin/users' + queryWith(listView, { user: row.key });
      // Shortened for the same reason the metrics page's Who column is, and the
      // Decentralized Identity endpoints are what made it reach this table too:
      // a did:jwk is a couple of hundred characters of base64url with not one
      // place in it a browser will break a line, so drawn in full it sets this
      // cell's minimum width and pushes every column after it off the card —
      // including the rows of people whose names are three letters long. 40
      // keeps an ordinary DID whole, and shortens a `urn:uuid:` subject (45
      // characters); shortened() puts the rest in the title attribute, so
      // nothing is lost, only hidden. THREE CELLS ARRIVED ON 2026-09-10 WITH
      // THE ROSTER `/admin/mfa` USED TO DRAW: where the row came from, what the
      // person can sign in WITH, and what they are asked for as a SECOND
      // factor. They are here rather than on a page of their own because the
      // question *who holds no second factor* is a question about the people
      // this table already lists, and a second table of the same people would
      // be a second answer to who they are.
      return '<tr><td><a href="' + self.esc(href) + '">' +
             self.shortened(row.name, 40) +
        '</a></td><td>' +
        self.sourceCell(row) + '</td><td ' +
        'class="' + (row.authenticated ? 'state-valid' : 'state-none') + '">' +
          (row.authenticated ? row.authentications + '&times;' :
           'never') + '</td><td>' + self.credentialCell(row.factors) +
           '</td><td>' +
        self.secondFactorCell(row.factors) + '</td><td>' +
        self.esc(row.protocols.map(function (f) { return f.protocol; })
          .join(', ') ||
                 '—') + '</td><td>' +
        self.esc(row.realms.map(function (r) { return r.realm; }).join(', ') ||
                 '—') +
        '</td><td ' +
        'class="num">' + (liveByUser[row.key] || 0) + '</td>' +
        '<td class="num">' + row.tokens.issued + '</td>' +
        '<td class="num state-valid">' + row.tokens.valid + '</td>' +
        // EXPIRED IS ITS OWN COLUMN. It was counted in userRows() and shown
        // nowhere, so this table drew "12 issued, 1 valid" and left the other
        // eleven to be read as revoked, forgotten or broken. They are none of
        // those: a token whose lifetime ran out is the ordinary end of a token,
        // and it is the specific thing somebody comes to this page to check
        // after a client started being refused. Same clock as everywhere else —
        // tokenStateOf(), which applies oauth2.clockSkewS.
        '<td class="num state-expired">' + row.tokens.expired + '</td>' +
        '<td class="num state-revoked">' + row.tokens.revoked + '</td>' +
        '<td class="num">' + row.artifacts + '</td>' +
        '<td>' + self.esc(row.firstAt ? self.whenText(row.firstAt) : '—') +
        '</td><td>' + self.esc(self.whenText(row.lastActivityAt)) + '</td>' +
        '<td><a href="' + self.esc(href) + '">sessions and tokens ' +
                                      '&rsaquo;</a></td></tr>';
    }).join('');

    const protocolOptions = [''].concat(Object.keys(protocolsSeen).sort())
                                .map(function (p) {
      return '<option value="' + self.esc(p) + '"' +
             (p === wantedProtocol ? ' ' +
          'selected' : '') + '>' +
             self.esc(p || 'any protocol') + '</option>';
    }).join('');
    const perOptions = this.perPageOptions(paging.perPage);

    // THE SECOND-FACTOR COUNTS (2026-09-10), over the WHOLE population rather
    // than the page — which is why peopleRows() scans rather than reading the
    // rows being shown. A tile that counted one page of twenty would answer a
    // question nobody asked.
    const inner = this.messagesOf(req) +
      '<div class="tiles">' +
        this.tile(all.length, 'people') +
        this.tile(authenticatedHere, 'authenticated here') +
        this.tile(all.length - authenticatedHere, 'never signed in here') +
        this.tile(Object.keys(liveByUser).length, 'with an active session') +
        this.tile(factorCounts.withSecond, 'hold a second factor') +
        this.tile(factorCounts.passwordOnly, 'password only') +
        this.tile(factorCounts.noCredential, 'no way in yet') +
      '</div>' +
      // ---------------------------------------------------------------------
      // WHAT THIS TABLE IS A LIST OF, AND IT CHANGED ON 2026-09-10.
      //
      // It was "every userid this service has been given as part of an
      // interaction that succeeded" — the SEEN registry, and the note said so.
      // It is the UNION of that with this realm's directory people now, because
      // the second-factor roster that used to live on `/admin/mfa` moved onto
      // these columns and that roster's whole value is the people who have
      // NEVER signed in. See peopleRows().
      // ---------------------------------------------------------------------
      this.note('<strong>Everybody this realm knows about</strong>, from two ' +
      'places and the Known from column says which. <strong>SEEN</strong> is ' +
      'every userid this service has been given as part of an interaction ' +
      'that succeeded — the name typed at either sign-in screen, the one on ' +
      'a password grant, the subject of a WS-Security ' +
      '<code>UsernameToken</code>, the client principal in a Kerberos AS-REQ ' +
      'or an accepted AP-REQ, and the subject of an exchanged token; a ' +
      'request that was REFUSED records nothing. <strong>DIRECTORY</strong> ' +
      'is an entry under <code>ou=users</code>, however it got there. Most ' +
      'people are both. Click a name for its sessions, everything issued to ' +
      'it, and the second factors it holds.') +
      (population.store
        ? ''
        : this.warn('<strong>No directory is loaded in this ' +
          'process</strong>, so this is the SEEN half alone and the ' +
          'second-factor columns say <em>unknown</em> rather than ' +
          '<em>none</em>. Those are different answers and the second one is ' +
          'the dangerous one to guess.')) +
      (population.capped
        ? this.warn('This realm holds ' + this.esc(String(population.scanned)) +
                    ' ' +
          'people and only the ' +
          'first ' + this.esc(String(population.limit)) + ' were read ' +
          'for the credential columns. Reading a credential per person ' +
          'happens on the one thread that answers every socket this service ' +
          'holds, so the scan stops rather than stalling the service.')
        : '') +
      (factorCounts.unreadable
        ? this.warn('<strong>' + this.esc(String(factorCounts.unreadable)) +
                    ' ' +
          'authenticator enrolment(s) cannot be read by this ' +
          'process</strong> — almost always a shared secret sealed under a ' +
          'key-encryption key that has since been rotated. Those people are ' +
          'REFUSED at the code step rather than let through on one factor, ' +
          'so they cannot sign in at all until the enrolment is cleared on ' +
          'their own row and set up again. <a href="' +
          this.esc('/admin/users' + queryWith({ factor: 'unreadable' }, {})) +
          '">Show them</a>.')
        : '') +
      // WHERE AN ISSUED SPIFFE IDENTITY LANDS ON THIS TABLE, said here because
      // the row it produces is easy to misread. It has an artifact and NO
      // authentication, so it falls in the "seen only as a subject" tile above
      // — which is exactly right and is what that tile has always been for.
      // Counting an issuance as a sign-in would be the wrong answer twice over:
      // receiving a credential is not presenting one, and an agent holding
      // FetchX509SVID open re-mints every half-lifetime, so one workload left
      // running overnight would read as several hundred authentications.
      this.note('<strong>An identity this trust domain has only ISSUED a ' +
      'certificate to is here with <em>never</em> in the Authenticated ' +
      'column</strong> &mdash; it counts under &ldquo;seen only as a ' +
      'subject&rdquo;. Being issued a credential is not presenting one, and ' +
      'an agent re-mints every half-hour, so counting issuances would turn ' +
      'one workload into hundreds of sign-ins. It also gets a directory ' +
      'entry under <code>ou=users</code> carrying the certificate it ' +
      'currently holds &mdash; see <a href="/admin/ldap/directory">the ' +
      'directory</a> and <a href="/admin/spiffe">SPIFFE</a>.') +
      this.warn('<strong>One row is one local name, across every ' +
      'protocol.</strong> The same person arrives here as <code>alice</code> ' +
      'at the login screen, her <code>urn:uuid:</code> subject in every ' +
      'token and <code>alice@STS.MOCK</code> as a Kerberos principal, and ' +
      'showing three rows for that would be a worse answer than one — the ' +
      'premise of this service is that the name you type is who you are in ' +
      'every protocol at once. What it costs: two different people called ' +
      '<code>alice</code> in two Kerberos realms are one row here. The ' +
      'Realms column is what makes that visible. Case is never collapsed, ' +
      'because nothing in this service treats <code>Alice</code> and ' +
      '<code>alice</code> as one.') +

      '<form method="get" action="/admin/users"><div class="formrow">' +
        '<label for="q">Name contains</label>' +
        '<input type="text" id="q" name="q" size="20" value="' +
        this.esc(wantedText) +
      '"><label ' +
        'for="protocol">Authenticated through</label><select id="protocol" ' +
        'name="protocol">' + protocolOptions + '</select>' +
        // THE SECOND-FACTOR FILTER (2026-09-10), which is what `/admin/mfa` was
        // for. `none` is the one an operator actually comes here to run, and it
        // is the reason this page's population had to widen: the people most
        // likely to hold no second factor are the ones who have never signed
        // in.
        '<label for="factor">Second factor</label>' +
        '<select id="factor" name="factor">' +
        [['', 'any state'], ['any', 'holds one'], ['totp', 'authenticator app'],
         ['key', 'security key'], ['none', 'holds none'],
         ['unreadable', 'enrolment this process cannot read']].map(
             function (pair) {
          return '<option value="' + self.esc(pair[0]) + '"' +
                 (wantedFactor === pair[0] ? ' selected' : '') + '>' +
                 self.esc(pair[1]) + '</option>';
        }).join('') + '</select>' +
        '<label for="per">Per page</label><select id="per" name="per">' +
      perOptions + '</select><button ' +
        'class="secondary">Filter</button>' +
        (wantedText || wantedProtocol || wantedFactor
          ? ' <a href="/admin/users">clear</a>' : '') +
      '</div></form>' +

      // ---------------------------------------------------------------------
      // THE ONE CONTROL ON THIS PAGE, AND SINCE 2026-09-06 IT IS A DOOR RATHER
      // THAN A DEED.
      //
      // It used to be a POST: type a name, press Create, and a person appeared
      // — with a full name, an email address, a date of birth, a street, a
      // locality, a region, a postal code and a nationality, all INVENTED. That
      // was the original design and it had a real justification (an entry has
      // to have values or an issued credential asserts nothing), but it meant
      // the console could create exactly one kind of person: a fictional one,
      // whose every detail then had to be corrected from outside this console.
      //
      // So the button now takes the reader to `/admin/users/new`, where every
      // field a person here has is a box, only the username is required, an
      // empty box records NO VALUE, the invented person is a button rather than
      // the default, and the person can be given a password or an activation
      // link at the moment they are created — none of which fits in a
      // `.formrow` under a table of everybody this service has ever seen.
      //
      // **IT IS A GET FORM AND NOT A POST**, which is what makes it a link with
      // a text box in front of it: nothing is written by pressing it, and the
      // typed name arrives as `?user=` on the page that does the writing. The
      // POST handler on this path is UNCHANGED and still creates — it is what
      // `POST /admin-api/users/create` mirrors (rule 7), and what a test
      // drives.
      // ---------------------------------------------------------------------
      '<form method="get" action="/admin/users/new">' +
        '<div class="formrow">' +
          '<label for="new-username">Create a user</label>' +
          '<input type="text" id="new-username" name="user" size="20" ' +
                 'placeholder="username">' +
          '<button>Create &rsaquo;</button>' +
        '</div>' +
        this.note('<strong>Takes you to <a href="/admin/users/new">New ' +
        'user</a></strong>, carrying whatever name you type here, and ' +
        'creates nobody by itself. There you fill in as few or as many of ' +
        'the ' + vcClaims.personFields().length + ' ' +
        'attributes a person in this directory can carry as you like — only ' +
        'the username is required, and <strong>a box left empty records no ' +
        'value</strong> — and choose how they first get in: a password you ' +
        'type, one generated and shown once, a single-use activation link ' +
        'shown once, or nothing at all. There is a button there that fills ' +
        'the form in with the invented person this service would have made ' +
        'up, which is what this button used to do without asking.') +
        this.note('Puts an entry in the embedded LDAP directory at ' +
        // THE REALM'S OWN CONTAINER, ASKED FOR RATHER THAN BUILT HERE. This
        // read `ou=users,` + the one base DN setting until 2026-08-25,
        // which is the NAMING CONTEXT and is the default realm's container
        // only: under /realm/acme the page named a DN in a different realm than
        // the one the button writes to, and named it on the very control whose
        // whole subject is where the entry goes. `ldap_server.js` answers the
        // question for the ambient realm, and the fallback is the old string
        // for the build with no directory loaded, where the slot is empty and
        // there is no realm to ask about anyway.
        '<code>uid=&lt;name&gt;,' + this.esc(newUserContainer()) + '</code>. ' +
        '<strong>One entry per person:</strong> a name that is already here ' +
        'is refused, whichever protocol brought them and whatever attribute ' +
        'their entry is named by — the same refusal an <code>ldapadd</code>, ' +
        'a SCIM create and <code>POST /admin-api/users/create</code> get, ' +
        'because all of them call one function. The new user will not appear ' +
        'in the table below until they authenticate somewhere: that is who ' +
        'this service has SEEN, and this is what the directory HOLDS.') +
      '</form>' +
      nav.head +
      '<table><tr><th>User</th><th>Known ' +
      'from</th><th>Authenticated</th><th>Can sign in with</th><th>Second ' +
      'factor</th><th>Protocols</th><th>Realms</th><th ' +
      'class="num">Sessions</th><th class="num">Tokens</th><th ' +
      'class="num">Valid</th><th class="num">Expired</th><th ' +
      'class="num">Revoked</th><th class="num">Artifacts</th><th>First ' +
      'seen</th><th>Last activity</th><th></th></tr>' +
      (rows ||
       '<tr><td colspan="16">Nobody matches. Nothing has authenticated ' +
               'here yet unless a filter above is hiding ' +
               'it.</td></tr>') + '</table>' +
      nav.foot +
      this.note(filtered.length + ' identit' +
                (filtered.length === 1 ? 'y' : 'ies') +
      ' match; ' + all.length + ' known in total, most recently active ' +
      'first. An identity marked <em>never</em> under Authenticated has been ' +
      'issued something without ever presenting a credential here: an ' +
      'exchanged token from another issuer, a WS-Trust ' +
      '<code>OnBehalfOf</code>, or a Kerberos S4U request that a service ' +
      'made in their name. Listing them is the point — a users page that ' +
      'showed only the sign-ins would deny the existence of subjects the ' +
      'tokens page is showing at the same moment.') +
      this.note('All of it is in memory and dies with the process, and the ' +
      'registry holds the most recent ' + stats.MAX_USERS + ' identities.');

    log.debug("Leaving AdminConsole.usersListPage(). " + shown.length +
              " row(s) drawn.");
    return {
      inner: inner,
      json: view.json
    };
  }

  usersView(req) {
    const { log, stats, queryWith } = this.deps;
    log.debug("Entering AdminConsole.usersView().");
    const wantedUser = String(req.query.user || '').trim();
    if (wantedUser) {
      const detail = this.userDetailPage(req, wantedUser);
      if (!detail) {
        // Not a 404: this service has simply never seen the name, or has
        // forgotten it to the cap since the link was drawn. Both are answers
        // rather than errors, and a 404 here would send a test looking for a
        // routing problem.
        const inner = this.messagesOf(req) +
          this.note('Nothing here has authenticated as <code>' +
                    this.esc(wantedUser) +
          '</code>, and nothing has been issued naming them. Either the name ' +
          'is not one this service has seen, or it has been forgotten — the ' +
          'registry holds the most recent ' + stats.MAX_USERS +
          ' identities, and everything in it dies with the process.') +
          // The same href the trail's section crumb carries, so the two ways
          // back off this page cannot land in different places.
          this.note('<a href="' +
          this.esc('/admin/users' +
                   queryWith(this.listViewOf('/admin/users', req.query), {})) +
          '">Back to the list</a>.');
        log.debug("Leaving AdminConsole.usersView(). No such user.");
        return { json: { user: wantedUser, known: false }, inner: inner,
                 title: 'User',
                 up: this.upTo('/admin/users', wantedUser,
                               this.listViewOf('/admin/users', req.query)) };
      }
      log.debug("Leaving AdminConsole.usersView(). The drill-down.");
      return { json: Object.assign({ known: true }, detail.json),
               inner: detail.inner, title: 'User ' + wantedUser,
               up: this.upTo('/admin/users', wantedUser,
                             this.listViewOf('/admin/users', req.query)) };
    }
    const list = this.usersListPage(req);
    log.debug("Leaving AdminConsole.usersView(). The list.");
    return { json: list.json, inner: list.inner, title: 'Users' };
  }

  usersPost(req, res, body, held) {
    const { log, gateStateFor, usersAction, baseUrlOf, queryWith } = this.deps;
    log.debug("Entering AdminConsole.usersPost().");
    // The actor is the person whose session got them through the gate, read
    // here rather than inside the action because the management API calls the
    // same function with an actor of its own — a function that reached for a
    // cookie would only work from one of them. It arrived with the two
    // second-factor clears on 2026-09-10, which are the actions on this
    // resource that owe an audit row naming who made them.
    const state = gateStateFor(req);
    const result = usersAction(body, { via: 'console', actor: state.username,
                                       base: baseUrlOf(req) });
    held.settle(!!result.ok);
    // Back to the list carrying whatever filter and page the form came from, so
    // that creating somebody does not cost the reader their place — the rule
    // every form on this console follows. A control drawn on a PERSON'S page
    // (2026-09-13) carries `from=user` and goes back to that page, at the
    // section it was pressed in.
    const who = String(body.user || body.username || '').trim();
    const back = String(body.from || '') === 'user' && who
      ? this.userReturnTo(body, who, '#credential-controls')
      : '/admin/users' +
        queryWith(this.listViewFromBack('/admin/users', body.back), {});
    // A ONE-TIME SECRET IS ANSWERED WITH A PAGE (2026-09-13): a reset password
    // or a reset link exists once, and `respondToAction()` would put it in a
    // redirect's query string — the browser history, every proxy log on the
    // way, and the `Referer` of the next click. `/admin/users/new` made this
    // argument first. A JSON caller still gets JSON.
    const oneTime = result.ok && (result.password || result.resetUrl);
    if (oneTime && !/json/i.test(String(req.headers['content-type'] || ''))) {
      this.respond(req, res, result, 'Credential reset', '/admin/users',
                   this.credentialResetPage(result, back),
                   this.upTo('/admin/users', 'Credential reset',
                             this.listViewFromBack('/admin/users', body.back)));
      log.debug("Leaving AdminConsole.usersPost(). A one-time page.");
      return;
    }
    this.respondToAction(req, res, back, result);
    log.debug("Leaving AdminConsole.usersPost().");
  }

  // THE PAGE A RESET ANSWERS WITH (2026-09-13): the generated password or the
  // reset link, once, and what else the reset did.
  credentialResetPage(result, back) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.credentialResetPage().");
    const who = this.esc(result.username);
    const out = [];
    if (result.password) {
      out.push('<h2>The new password for ' + who + ', shown once</h2>' +
        '<div class="secret">' + this.esc(result.password) + '</div>' +
        this.warn('<strong>Note it now.</strong> This service stores a ' +
        'scrypt hash and cannot show it again — not this console, not ' +
        '<code>/admin-api</code>. ' + (result.forcedChange
          ? 'It works ONCE, at the sign-in screen, where ' + who + ' is made ' +
            'to ' +
            'choose their own before anything is signed in.'
          : 'The forced change could not be recorded, so it will go on ' +
            'working until somebody changes it.')));
    }
    if (result.resetUrl) {
      out.push('<h2>The password reset link for ' + who + ', shown once</h2>' +
        '<div class="secret">' + this.esc(result.resetUrl) + '</div>' +
        this.warn('<strong>Send it to ' + who + ' by a channel you trust. ' +
        'Valid until ' + this.esc(result.expiresAt || 'it expires') +
        '.</strong> Anybody ' +
        'holding it can set this person\'s password, so treat it as the ' +
        'credential it is. It is spent when the new password is stored, not ' +
        'when it is opened. ' + (result.passwordRevoked
          ? 'Their old password was REMOVED, so until the link is used they ' +
            'cannot sign in with a password.'
          : 'They had no password to remove.')));
    }
    out.push('<h2>What else happened</h2>' +
      this.note(this.esc(result.message || '')) +
      this.note('<strong>Shared Signals</strong>: a CAEP credential-change ' +
      'and a RISC account-credential-change-required went to every stream ' +
      'that asked for those types and covers this person, and each session ' +
      'the sign-out ended sent its own CAEP session-revoked. <a ' +
      'href="/admin/caep">CAEP</a> and <a href="/admin/risc">RISC</a> show ' +
      'what was delivered.'));
    out.push(this.note('<a class="btn" href="' + this.esc(back) + '">Back to ' +
                       who +
                       '</a>'));
    log.debug("Leaving AdminConsole.credentialResetPage().");
    return out.join('');
  }

  // ---------------------------------------------------------------------------
  // GET /admin/users/new, POST /admin/users/new — CREATE A PERSON, ON A PAGE OF
  // ITS OWN (2026-09-06).
  //
  // **WHAT THIS REPLACED IS THE POINT OF IT.** The Users list carried a single
  // box and a Create button, and pressing it created somebody whose every
  // attribute was INVENTED — a name, an email address, a date of birth, a
  // street, a nationality, none of which anybody had asked for. That was a
  // deliberate and defensible design while the only thing a directory entry had
  // to do was give an issued credential something to assert: `vc_claims.js`
  // invents a consistent person per username, so the entry and the credential
  // agreed, and nobody had to type twenty-five fields to get a usable test
  // subject.
  //
  // It stopped being enough for two reasons. An operator who wants a person
  // with a PARTICULAR email address or employee number had no way to say so at
  // creation — the entry appeared with fictions on it and had to be corrected
  // afterwards, one `ldapmodify` at a time, from outside this console. And a
  // person created here had no way IN: `credentials.js` has been able to set a
  // password and issue a one-time activation link since it was written, and
  // neither was reachable from any screen. The second was a gap rather than a
  // decision — `POST /admin-api/users/issue-activation` existed and nothing
  // pressed it.
  //
  // So this page does three things the box could not:
  //
  //   * **EVERY FIELD A PERSON HERE HAS, TYPED.** Twenty-five of them, drawn
  //     from `vc_claims.js`'s catalogue — the same list `createUser()` checks a
  //     caller's attribute names against, so this form cannot offer a field the
  //     writer would drop. Only the username is required. **A box left empty
  //     records NO VALUE**; it does not fall back to an invented one.
  //   * **THE INVENTED PERSON IS A BUTTON RATHER THAN THE DEFAULT.** *Fill with
  //     example data* writes exactly what this service WOULD have made up for
  //     that username into the empty boxes, and leaves anything already typed
  //     alone. It is development-mode only, argued below.
  //   * **A CREDENTIAL, CHOSEN AT CREATION.** A typed password, a generated one
  //     shown once, an activation link shown once, or none at all.
  //
  // **IT IS NOT A SECOND DOOR ONTO THE DIRECTORY.** The form posts here and
  // this handler calls `usersAction()` — the same function `POST /admin/users`
  // and `POST /admin-api/users/create` call, reaching `ldap_server.js`'s
  // `createUser()`, which is also where an `ldapadd` and a SCIM create are
  // answered. Two forms over one function are two doors; what would break the
  // one-store rule is a second place the value lives, and there is none. That
  // is the same arrangement `/admin/applications/new` argues at length beside
  // its list page's inline row.
  //
  // **WHY IT POSTS TO ITSELF RATHER THAN TO `/admin/users`, WHICH IS WHERE THE
  // APPLICATIONS PAGE POSTS.** Two reasons, and both are about what a create
  // here ANSWERS WITH.
  //
  //   * A generated password and an activation link EXIST ONCE. Every other
  //     form on this console answers with a 303 back to a list and the message
  //     in a query parameter, sliced to 500 characters — which is right for
  //     "four tokens revoked" and catastrophic for a credential: the value
  //     would be in the browser's history, in any proxy log between here and
  //     the browser, and possibly truncated in half. So a create answers with a
  //     PAGE, and the secret is in the body of a response marked `no-store`.
  //   * A refusal has to come back with the twenty-five boxes still filled in.
  //     A redirect loses them, and losing a form somebody has just typed
  //     because the username had a comma in it is the kind of thing that
  //     teaches people not to use a screen.
  //
  // **NOTHING ON THIS PAGE IS A SECOND OPINION ABOUT WHAT A USERNAME MAY BE.**
  // The DN-syntax rule, the refusal of a name already taken, the DID and SPIFFE
  // shapes that are identities rather than usernames — all of it is in
  // `createUser()`, and this form draws whatever it says. The `required` on the
  // username box is a convenience for a browser and not the check.
  // ---------------------------------------------------------------------------

  // The invented person as form values: what `createUser()` WOULD have written
  // for this username, keyed by the catalogue's own spelling.
  //
  // **IT IS THE SAME PERSONA AND NOT A SECOND RANDOM ONE**, which is the whole
  // value of the button: `vc_claims.js` seeds from the username, so pressing
  // Fill shows an operator exactly what accepting the invented person would
  // have got them — and they can then edit it rather than accept or refuse it
  // whole. A generator of its own here would have produced a plausible-looking
  // person that matched nothing, and the difference would have shown up only in
  // an `ldapsearch` weeks later.
  //
  // The rows with no `from` produce nothing, and there is exactly one:
  // `description`, which this service writes itself to say why the entry
  // exists.
  inventedFieldValues(username) {
    const { log, vcClaims } = this.deps;
    log.debug("Entering AdminConsole.inventedFieldValues(). username=" +
              username);
    const persona = vcClaims.personaFor(String(username || '').trim());
    const out = {};
    vcClaims.personFields().forEach(function (row) {
      if (!row.from) {
        return;
      }
      const value = persona[row.from];
      if (value === undefined || value === null || value === '') {
        return;
      }
      out[row.ldap] = String(value);
    });
    log.debug("Leaving AdminConsole.inventedFieldValues(). " +
              Object.keys(out).length + " " +
        "value(s).");
    return out;
  }

  // One attribute as a row of the form's table. `values` is what to put in the
  // box — a prefill after a refusal, or what Fill wrote — and empty is the
  // ordinary case.
  //
  // **THE FIELD NAME IS `field.<attribute>` AND THE ATTRIBUTE IS THE SCHEMA'S
  // OWN NAME**, which is `declarationFieldRow()`'s rule applied to people. Two
  // reasons, and the second is the load-bearing one: an `ldapsearch` on this
  // entry shows exactly the name that was on the form, and `POST
  // /admin-api/users/create` takes the same names in its `attributes` object —
  // so the console and the management API are one vocabulary and somebody who
  // learns this page can drive the API.
  //
  // The schema reference is drawn beside each one because this directory has NO
  // SCHEMA: nothing here would refuse `schacDateOfBirth` on a group or `l` on
  // an application, so where the name comes from is the only thing that makes
  // it more than a string somebody chose. Three of them are this service's own
  // or SCHAC's rather than an RFC's, and those say so rather than being left to
  // look like the others.
  personFieldRow(row, values) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.personFieldRow().");
    const id = 'field-' + row.ldap;
    const value = (values || {})[row.ldap];
    const hint = this.tip(row.label + ' — ' + row.schema + '. It reaches a ' +
                                                           'credential as ' +
                          row.claim.join('.') + '.');
    log.debug("Leaving AdminConsole.personFieldRow().");
    return '<tr><td><label for="' + this.esc(id) + '"' + hint + '><code>' +
      this.esc(row.ldap) + '</code></label></td>' +
      '<td>' + this.esc(row.label) + '</td>' +
      '<td><input type="text" id="' + this.esc(id) + '" name="field.' +
      this.esc(row.ldap) +
      '"' +
      hint + ' size="36" maxlength="512" placeholder="no value" value="' +
      this.esc(value === undefined ? '' : value) + '"></td>' +
      '<td><code>' + this.esc(row.claim.join('.')) + '</code></td>' +
      '<td class="sub">' + this.esc(row.schema) + '</td></tr>';
  }

  credentialChoiceRow(choice, chosen) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.credentialChoiceRow().");
    const id = 'cred-' + choice.id;
    log.debug("Leaving AdminConsole.credentialChoiceRow().");
    return '<tr><td><input type="radio" id="' + this.esc(id) +
           '" name="credential" ' +
                                                          'value="' +
      this.esc(choice.id) + '"' + (chosen === choice.id ? ' checked' : '') +
      '></td><td><label for="' + this.esc(id) + '"><strong>' +
      this.esc(choice.label) +
                                                          '</strong></label>' +
                                                          '</td><td ' +
      'class="why">' + this.note(choice.what) + '</td></tr>';
  }

  // THE PAGE. `prefill` is what to put back in the boxes — absent on a first
  // visit, and on a refusal it is what was posted, so nobody retypes
  // twenty-five fields because of one bad username.
  newUserPage(req, prefill?) {
    const { log, adminViews, realms, newUserContainer, vcClaims, mode,
            CREDENTIAL_CHOICES, persistence } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.newUserPage().");
    // ONE COMPUTATION, TWO RENDERINGS (2026-09-12) — see
    // admin-core/admin_views.ts. The attribute catalogue below is the one this
    // form draws its boxes from AND the one /admin-api publishes.
    //
    // `prefill` goes WITH the request, because it is what was posted and
    // refused: the answer describes the form as it will be drawn, boxes filled,
    // and a json half that ignored it would describe a form nobody is looking
    // at.
    const json = adminViews.newUserJson(req, prefill);
    const given = prefill || {};
    const values = given.fields || {};
    const username = String(given.username === undefined
      ? (req.query.user || '') : given.username).trim();
    const credential = String(given.credential ||
                              adminViews.DEFAULT_CREDENTIAL);
    const realm = realms.current();
    const container = directoryReader ? newUserContainer() : null;
    const fields = vcClaims.personFields();
    const development = mode.isDevelopment();

    // NO DIRECTORY IN THIS PROCESS. The form is left OUT rather than drawn and
    // refused, which is `newApplicationPage()`'s shape and is right for the
    // same reason: `createUser()` would answer with exactly this sentence, and
    // a form whose only possible outcome is that message is a control that lies
    // about what it does.
    if (!directoryWriter || !container) {
      log.debug("Leaving AdminConsole.newUserPage(). There is no directory.");
      return {
        inner: this.messagesOf(req) +
          this.warn('<strong>There is no embedded LDAP directory loaded in ' +
          'this process</strong>, so there is no <code>ou=users</code> ' +
          'container to put a person in and this form is not drawn. That is ' +
          'a build of this service without <code>ldap/ldap_server.js</code> ' +
          'rather than a fault — every other page is unaffected, and <a ' +
          'href="/admin/users">Users</a> still reports what this service has ' +
          'SEEN, which does not come from the directory.'),
        json: json
      };
    }

    const inner = this.messagesOf(req) +
      '<div class="tiles">' +
        this.tile(fields.length, 'fields you may fill') +
        this.tile(1, 'that is required') +
        this.tile(CREDENTIAL_CHOICES.length, 'ways in to choose from') +
        this.tile(development ? 'yes' : 'no', 'example data offered') +
      '</div>' +

      this.note('<strong>The entry lands in this realm\'s directory, at ' +
                '<code>uid=&lt;name&gt;,' +
      this.esc(container) + '</code>.</strong> The console shows one trust ' +
      'realm at a time and this form writes the one it is showing — ' +
      '<strong>' + this.esc(realm ? realm.name : 'Default') +
      '</strong> — because the realm is taken from the path this request ' +
      'arrived on. People are NOT shared between realms: an ' +
      '<code>ldapsearch</code> with that base DN sees exactly what this ' +
      'creates, and another realm has never heard of them.') +

      this.note('<strong>This is not a second store.</strong> The form posts ' +
      '<code>action=create</code> to this page, which calls the same ' +
      'function <code>POST /admin-api/users/create</code>, a SCIM create and ' +
      'an <code>ldapadd</code> under <code>ou=users</code> all reach — so ' +
      'what a username may be, and the refusal of one that is already here, ' +
      'mean the same thing at every door. <strong>One entry per ' +
      'person</strong> is the rule it keeps, whichever protocol brought them ' +
      'and whatever attribute their entry happens to be named by.') +

      this.warn('<strong>An empty box records NO VALUE. It does not fall ' +
      'back to an invented one.</strong> That is the difference between this ' +
      'page and the single Create box that used to be on <a ' +
      'href="/admin/users">Users</a>: that button invented a whole person — ' +
      'a name, an email address, a date of birth, a street, a nationality — ' +
      'behind whatever username was typed. Here nothing is made up unless ' +
      'you press the button that makes it up. What that costs is that an ' +
      'issued credential asserting <code>birthdate</code> for somebody with ' +
      'no <code>schacDateOfBirth</code> falls back to the invented value at ' +
      'ISSUANCE time (<code>vc_claims.js</code> does that so a credential is ' +
      'never empty), so the credential and the directory will disagree until ' +
      'somebody fills the attribute in. <a href="/admin/vc">Credential ' +
      'claims</a> is where that fallback is described.') +

      '<form method="post" action="/admin/users/new" class="newapp"><input ' +
      'type="hidden" name="action" value="create"><h2>Who they are</h2><div ' +
      'class="formrow"><label for="new-username">Username</label><input ' +
      'type="text" id="new-username" name="username" size="28" ' +
      'maxlength="256" required ' +
      'value="' + this.esc(username) + '" placeholder="the name they ' +
      'sign in as"></div>' +
      this.note('<strong>The only required field.</strong> It is the string ' +
      'they will type at a sign-in screen, the <code>sub</code> of every ' +
      'token issued for them, the <code>uid</code> on the entry and the name ' +
      'of the entry itself. It may not be a DN, a <code>did:</code>, a ' +
      'SPIFFE identity or carry a character RFC 4514 reserves in a DN ' +
      '(<code>, = + &lt; &gt; # ; " \\</code>) — those name entries that ' +
      'reach this directory by being PRESENTED rather than created, and the ' +
      'refusal names which one you sent. Case is never collapsed: nothing in ' +
      'this service treats <code>Alice</code> and <code>alice</code> as one ' +
      'person.') +

      '<h2>How they get in</h2>' +
      '<table><tr><th>Choose</th><th>Option</th><th>What it means</th></tr>' +
      CREDENTIAL_CHOICES.map(function (choice) {
        return self.credentialChoiceRow(choice, credential);
      }).join('') +
      '</table><div class="formrow"><label ' +
      'for="new-password">Password</label><input type="password" ' +
      'id="new-password" name="password" size="28" maxlength="1024" ' +
      'autocomplete="new-password"><label ' +
      'for="new-password-confirm">Again</label><input type="password" ' +
      'id="new-password-confirm" name="passwordConfirm" size="28" ' +
      'maxlength="1024" autocomplete="new-password"></div>' +
      this.note('Read ONLY when <em>A password I type</em> is chosen above; ' +
      'ignored otherwise, so a value left in these boxes cannot become a ' +
      'credential nobody meant to set. The two must match. <strong>It must ' +
      'meet this realm\'s <a href="/admin/policies">password ' +
      'policy</a>' + (json.passwordPolicy && json.passwordPolicy.enforced
        ? '</strong>, which is enforced here: ' +
          this.esc(json.passwordPolicy.rules.join('; ')) + '.'
        : ' in product mode</strong>, and this realm is in development mode, ' +
          'where no password is checked at any door — so the rule is not ' +
          'applied to what you type here. A <em>generated</em> password ' +
          'meets it in both modes.') +
      ' If the password is refused the person is still created, with no ' +
      'credential, and the page says why.') +

      '<h2>What is known about them</h2>' +
      this.note('Every attribute a person in this directory can carry, which ' +
      'is the same catalogue <a href="/admin/vc">Credential claims</a> ' +
      'chooses from — so an attribute filled in here is the value an issued ' +
      'credential asserts, rather than a value that agrees with one by ' +
      'coincidence. <strong>Fill in as few or as many as you like.</strong> ' +
      'The claim column is where each one lands in a token or a credential; ' +
      'the last column is where the attribute name comes from, which matters ' +
      'because <strong>this directory has no schema</strong> and would not ' +
      'refuse any of these on any entry.') +
      (development
        ? this.note('<strong><code>uid</code> is not on this list and that ' +
          'is not an omission.</strong> It is the username, asked for once ' +
          'at the top; a second box for it would let one form create ' +
          '<code>uid=alice</code> whose uid says <code>bob</code>.')
        : '') +
      '<table><tr><th>Attribute</th><th>What it is</th><th>Value</th>' +
      '<th>Reaches a credential as</th><th>Defined by</th></tr>' +
      fields.map(function (row) { return self.personFieldRow(row, values); })
            .join('') +
      '</table>' +

      // TWO SUBMITS, AND ONLY ONE OF THEM CARRIES A NAME. `action=create` is a
      // HIDDEN FIELD, the way every other form on this console spells its
      // action, and Create is an ordinary unnamed button; Fill carries
      // `fill=yes` of its own and the handler reads that FIRST, so a press of
      // it cannot be mistaken for a create even though `action` still says
      // create.
      //
      // **THE ALTERNATIVE — TWO BUTTONS BOTH NAMED `action` — LOOKS TIDIER AND
      // BREAKS TWO THINGS.** A form with two controls of one name has a
      // `RadioNodeList` at `form.elements.action` rather than an element, and
      // `.value` on one of those is empty unless they are radios: so anything
      // that finds a form by the action it posts — which is how this console's
      // own browser suite finds every form it presses — stops finding this one.
      // And a browser that submitted BOTH values (a hidden field plus a named
      // button) would post `action` twice, which is precisely the ambiguity
      // `common/validation.js` refuses everywhere else rather than resolving.
      '<div class="formrow">' +
        '<button type="submit">Create the user</button>' +
        (development
          ? ' <button type="submit" name="fill" value="yes" ' +
            'class="secondary">Fill with example data</button>'
          : '') +
      '</div>' +

      (development
        ? this.note('<strong>Fill with example data</strong> puts the ' +
          'invented person for the username you typed into the boxes you ' +
          'have left EMPTY, and touches nothing you have already filled in. ' +
          'It creates nobody — you land back on this form with the values in ' +
          'it, to edit or clear before pressing Create. <strong>It is the ' +
          'same invented person</strong> this service would have made up on ' +
          'its own, seeded from the username, so it shows you what you would ' +
          'have got rather than a second fiction. Type the username first: ' +
          'change it afterwards and the example person is somebody else. ' +
          '<code>description</code> stays empty because this service writes ' +
          'that one itself.')
        : this.warn('<strong>There is no <em>Fill with example data</em> ' +
          'button because this service is in PRODUCT mode</strong> ' +
          '(<code>global.mode=product</code>). Inventing a person\'s date of ' +
          'birth, address and nationality is a development convenience; on a ' +
          'service running as a product it would put fictions into a ' +
          'directory somebody else reads as fact, and there would be nothing ' +
          'on the entry afterwards to say which values were made up. Type ' +
          'what you know and leave the rest empty. <code>global.mode</code> ' +
          'is on <a href="/admin/config">Configuration</a>, with the rest of ' +
          'the settings that belong to no one protocol.')) +

      // THE INVENTION SWITCH, AND IT IS A HIDDEN FIELD RATHER THAN A CHOICE.
      // This page's whole promise is that nothing is made up unless the button
      // that makes it up was pressed, so `invent=no` is what it always sends;
      // the invented person is reachable from here through Fill, where it is
      // visible and editable, and through the API for a caller that wants the
      // old behaviour.
      '<input type="hidden" name="invent" value="no">' +
      '</form>' +

      this.note('<strong>Nothing about this create is persisted unless this ' +
      'service is persisting the directory.</strong> A person here IS a ' +
      'directory entry, so they survive a restart exactly when it does — see ' +
      '<a href="/admin/persistence">Persistence</a>, which is off by ' +
      'default. That is a property of the whole service rather than of this ' +
      'page.' +
      (persistence.status().persistsDirectory
        ? ' This process is running with <code>persistence.mode=' +
          this.esc(persistence.status().mode) +
          '</code>, so this entry WILL survive.'
        : ' This process is not persisting anything, so it will not.')) +

      this.warn('<strong>A person created here is not on the Users table ' +
      'until they authenticate.</strong> That table is who this service has ' +
      'SEEN; this writes what the directory HOLDS, and the two are different ' +
      'questions on purpose. They are counted under &ldquo;seen only as a ' +
      'subject&rdquo; the moment they exist, and <a ' +
      'href="/admin/ldap/directory">the directory</a> shows the entry ' +
      'immediately.') +

      this.note('<strong>Leaving a field empty is not a promise it stays ' +
      'empty.</strong> The Populate button on <a href="/admin/vc">Credential ' +
      'claims</a> — and the sweep that runs when a trust realm is created — ' +
      'fills every MISSING selected attribute on every person under ' +
      '<code>ou=users</code>, and it does not know which of them were typed ' +
      'by hand. That is the right behaviour for the sweep, whose whole job ' +
      'is that the directory and an issued credential agree; it means ' +
      '&ldquo;no value recorded&rdquo; is a statement about this create ' +
      'rather than a permanent property of the entry.') +

      this.note('<a href="/admin/users">Users</a> &middot; <a ' +
      'href="/admin/ldap/directory">Every entry in the directory</a> ' +
      '&middot; <a href="/admin/vc">Credential claims</a> &middot; <a ' +
      'href="/admin-api/docs#operation/createUser">The same act over ' +
      '/admin-api</a>.');

    log.debug("Leaving AdminConsole.newUserPage(). " + fields.length +
              " field(s) offered.");
    return {
      inner: inner,
      json: json
    };
  }

  newUserView(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.newUserView().");
    const built = this.newUserPage(req);
    log.debug("Leaving AdminConsole.newUserView().");
    return { json: built.json, inner: built.inner, title: 'New user' };
  }

  // ---------------------------------------------------------------------------
  // WHAT A SUCCESSFUL CREATE ANSWERS WITH, and the reason it is a page rather
  // than the 303-with-a-message every other control on this console answers
  // with.
  //
  // **A SECRET THAT EXISTS ONCE MUST NOT TRAVEL IN A URL.** A generated
  // password and an activation link are both produced here and stored only as a
  // hash, so this is the single moment either value exists outside somebody's
  // head. Put in a query parameter it would be in the browser's history, in the
  // referrer of anything clicked next, in every proxy log on the way — and
  // sliced to 500 characters by `respondToAction()`, which for an activation
  // URL is a link that silently does not work.
  //
  // So it is in the BODY of a response this console already marks `no-store`,
  // in a block that says outright that it will not be shown again.
  // ---------------------------------------------------------------------------
  createdUserPage(req, result) {
    const { log, baseUrlOf, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.createdUserPage(). ok=" + result.ok);
    const base = baseUrlOf(req);
    const secret = [];
    if (result.password) {
      secret.push('<h2>The generated password, shown once</h2>' +
        '<div class="secret">' + this.esc(result.password) + '</div>' +
        this.warn('<strong>This is the only time this value exists anywhere ' +
        'but in the hash on the entry.</strong> Copy it now and send it to ' +
        this.esc(result.username) + ' by whatever channel you already use. ' +
        'Nothing in this service can show it again — not this console, not ' +
        '<code>/admin-api</code>, not an <code>ldapsearch</code>, which sees ' +
        'a scrypt hash. If it is lost, set a new one; there is no recovery ' +
        'because there is nothing to recover.'));
    }
    if (result.activationUrl) {
      // ABSOLUTE, and built from the request rather than from a setting: this
      // is a link somebody is about to paste into a message, and a
      // root-relative path is not one. `baseUrlOf()` is what every other
      // outbound URL in this service is built from, so it carries the scheme,
      // host and port this request actually arrived on.
      const absolute = base + result.activationUrl;
      secret.push('<h2>The activation link, shown once</h2>' +
        '<div class="secret">' + this.esc(absolute) + '</div>' +
        this.warn('<strong>Valid until ' +
                  this.esc(result.expiresAt || 'it expires') +
        ', and shown once.</strong> This service stores only a hash of the ' +
        'token in it and cannot produce the link again — issuing another ' +
        'invalidates this one, which is also how a link that never arrived ' +
        'is replaced. <strong>Treat it as the credential it is</strong>: ' +
        'anybody holding it can finish setting up this account, choosing a ' +
        'password or enrolling a security key. It is spent when that setup ' +
        'FINISHES rather than when the link is opened, so a mail scanner or ' +
        'a browser prefetch cannot burn it. There is deliberately no ' +
        'self-service version of this link: with no mail channel here, a ' +
        'form that issued one would hand any visitor an activation link for ' +
        'any unactivated account.'));
    }
    const inner =
      (result.credentialError
        ? this.warn('<strong>The person was created and the credential was ' +
                    'NOT set.</strong> ' +
          this.esc(result.credentialError) + ' The name is taken now, by ' +
          'this entry — so set a credential on it rather than creating them ' +
          'again.')
        : '<div class="ok"><strong>' + this.esc(result.dn) + '</strong> now ' +
                                                        'exists.</div>') +
      secret.join('') +
      '<h2>What was written</h2>' +
      this.note(this.esc(result.message)) +
      '<table><tr><th>Attribute</th><th>Value</th></tr>' +
      Object.keys((result.entry && result.entry.attributes) || {}).sort()
        .map(function (name) {
          // THE TWO VERIFIERS ARE NAMED AND NOT PRINTED. What sits in
          // `userPassword` and `stsActivationToken` is a scrypt hash rather
          // than the value — so this is not a leak of the password or the link
          // — but it is the thing a sign-in and an activation are CHECKED
          // against, and there is no reason for it to be on a page whose
          // subject is what an operator just typed. Saying the attribute is
          // there is the useful half; the hash itself is noise beside the
          // one-time value printed above it, and a reader who saw both would
          // reasonably wonder which one to copy.
          //
          // /admin/ldap/directory still shows every attribute of every entry,
          // deliberately and behind this console's gate — that page's whole
          // subject is what the store holds, and hiding a row there would make
          // it lie about the directory. This page's subject is a create.
          if (name === 'userpassword' || name === 'stsactivationtoken') {
            return '<tr><td><code>' + self.esc(name) + '</code></td><td ' +
              'class="state-none">set — a scrypt hash, not shown ' +
              'here</td></tr>';
          }
          return '<tr><td><code>' + self.esc(name) + '</code></td><td>' +
            self.esc((result.entry.attributes[name] || []).join(', ')) +
            '</td></tr>';
        }).join('') +
      '</table>' +
      this.note('The entry exactly as the store holds it, which is what an ' +
      '<code>ldapsearch</code> under this realm\'s base DN returns. ' +
      'Attribute names are lower-cased because LDAP attribute descriptions ' +
      'are case-insensitive and this store normalises them on the way in; ' +
      'the catalogue\'s own spelling is what the form and ' +
      '<code>/admin-api</code> use. <strong>The two attributes that hold a ' +
      'verifier are named rather than printed</strong> — ' +
      '<code>userPassword</code> and <code>stsActivationToken</code> carry a ' +
      'scrypt hash rather than the value, and the value itself is above, ' +
      'once. <a href="/admin/ldap/directory">The directory page</a> shows ' +
      'both in full, because what that page is FOR is exactly what the store ' +
      'holds.') +
      '<div class="formrow">' +
      '<a href="' + this.esc('/admin/users' +
                             queryWith({ user: result.username }, {})) +
      '">Their row on Users &rsaquo;</a></div>' +
      this.note('<a href="/admin/users/new">Create another</a> &middot; <a ' +
      'href="/admin/ldap/directory">Every entry in the directory</a> ' +
      '&middot; <a href="/admin/users">Back to Users</a>. Remember that they ' +
      'will not appear in the Users TABLE until they authenticate: that list ' +
      'is who this service has SEEN.');
    log.debug("Leaving AdminConsole.createdUserPage().");
    return inner;
  }

  // ---------------------------------------------------------------------------
  // WHERE THIS PAGE SITS IN THE SHELL, AND WHY IT IS ONE FUNCTION RATHER THAN
  // SEVEN LITERALS.
  //
  // This page has no row in `SECTIONS` — the argument is beside the Users row
  // up there — so it is a DRILL-DOWN of `/admin/users`, and every `respond()`
  // below says so twice: `active` is the SECTION's path, which is what marks
  // the Users tab, and `up` is what turns that tab into a link and gives the
  // trail its third crumb (`Admin console › Users › New user`).
  //
  // **THERE ARE SEVEN RESPONSES ON THIS PATH AND EVERY ONE OF THEM NEEDS
  // BOTH.** Two of them are refusals that redraw the form, one is Fill, one is
  // the form itself, and one is the page that shows a generated password ONCE —
  // which is precisely the response a reader must not be stranded on. Writing
  // `upTo()` out seven times is how the seventh comes to be the one that was
  // missed, and the symptom would not be an error: it would be a page drawn
  // with no way back and the Users tab looking as though the reader were
  // somewhere else entirely.
  //
  // The leaf is a parameter because one of the seven is not called `New user`:
  // the success page is `User created`, and the crumb has to agree with the
  // `h1` under it (see `trailBar()`).
  //
  // It carries NO LIST VIEW, deliberately. The Create box on `/admin/users` is
  // a GET form with one field and does not forward the list's filter, so there
  // is nothing to carry — and `upTo()` handed an empty view reports
  // `filtered:false`, which is what makes the crumb's tooltip say `Back to
  // Users` rather than promising a filter that was never here.
  newUserUp(leaf?) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.newUserUp().");
    log.debug("Leaving AdminConsole.newUserUp().");
    return this.upTo('/admin/users', leaf || 'New user');
  }

  // The create half of `POST /admin/users/new`, run with its name claimed.
  newUserCreate(req, res, body, posted, wantsJson, held) {
    const { log, gateStateFor, usersAction, errorCodes } = this.deps;
    log.debug("Entering AdminConsole.newUserCreate().");
    const state = gateStateFor(req);
    const result = usersAction(Object.assign({}, body, {
      action: 'create',
      actor: (state && state.username) || ''
    }));
    held.settle(!!result.ok);
    if (wantsJson) {
      this.respondToAction(req, res, '/admin/users/new', result);
      log.debug("Leaving AdminConsole.newUserCreate(). Answered JSON.");
      return;
    }
    if (!result.ok) {
      // BACK ONTO THE FORM WITH EVERYTHING STILL IN IT. A redirect would lose
      // twenty-five boxes because a username had a comma in it, which is how a
      // screen teaches people not to use it.
      errorCodes.mark(res,
                      result.errorCode || errorCodes.codeOf(result) ||
                      'STS-ADMIN-0012');
      const view = this.newUserPage(req, posted);
      this.respond(req, res,
                   Object.assign({ created: false,
                                   errors: result.errors || [] },
                                           view.json),
                   'New user', '/admin/users',
                   this.warn('<strong>Nobody was created.</strong> ' +
                             this.esc((result.errors || []).join(' ') || String(
                                 result.why || ''))) +
                   view.inner, this.newUserUp());
      log.debug("Leaving AdminConsole.newUserCreate(). Refused.");
      return;
    }
    this.respond(req, res, Object.assign({ created: true }, result),
                 'User created', '/admin/users',
                 this.createdUserPage(req, result),
                 this.newUserUp('User created'));
    log.debug("Leaving AdminConsole.newUserCreate(). Created " + result.dn +
              ".");
  }

  // ---------------------------------------------------------------------------
  // GET /admin/groups — every group in the embedded directory, and one of them
  // in full.
  //
  // The first page in this console whose whole content came from another module
  // (several more do now — /admin/rbac, /admin/roles, the /admin/ldap/* pages).
  // This reads the directory through the slot ldap_server.js fills, and it
  // renders exactly what that returns without deciding anything — including
  // what counts as a group, which is that module's rule and is stated on the
  // page rather than reimplemented here.
  //
  // A GROUP IS NOT AN AUTHORISATION HERE, and the page says so where a reader
  // will see it. This paragraph used to say nothing in this service reads these
  // groups at all; three qualifications have landed since, and GROUPS_CAVEAT
  // below carries the first two: the groups claim (`groups.claim`) CARRIES a
  // membership into tokens and assertions without anything acting on it, the
  // two console role groups grant this console (8b in admin-ui/CLAUDE.md), and
  // a role under `ou=roles` may NAME a group (see /admin/roles). Otherwise they
  // are a directory's objects for a directory client to read, and a console
  // that listed them beside the tokens page without saying that would let
  // somebody conclude that adding a user to `cn=directory-admins` changed what
  // their token could do.
  // ---------------------------------------------------------------------------

  // The heading a group's link carries, and the fallback when it has no cn — an
  // entry can be a group by placement alone, and `(no cn)` is a truer label
  // than an empty cell that reads as a rendering fault.
  groupLabel(group) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.groupLabel().");
    log.debug("Leaving AdminConsole.groupLabel().");
    return group.cn || '(no cn)';
  }

  groupRuleCell(rule) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.groupRuleCell().");
    const rendered = GROUP_RULES[rule];
    if (!rendered) {
      log.debug("Leaving AdminConsole.groupRuleCell().");
      return '<span class="state-none">' + this.esc(rule || 'unstated') +
             '</span>';
    }
    log.debug("Leaving AdminConsole.groupRuleCell().");
    return '<span title="' + this.esc(rendered.title) + '">' +
           this.esc(rendered.label) +
           '</span>';
  }

  // A member's link. To the group page when the member is itself a group, so
  // nesting can be walked; to the users page when this console has actually
  // seen that person authenticate. Neither, and the DN stands on its own —
  // which is the commonest case in a directory nobody has signed in to yet.
  memberLink(member, known) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.memberLink().");
    const label = '<code>' + this.esc(member.dn) + '</code>';
    if (member.kind === 'group') {
      log.debug("Leaving AdminConsole.memberLink().");
      return '<a href="' +
        this.esc('/admin/groups' + queryWith({ group: member.dn }, {})) +
        '">' + label + '</a>';
    }
    if (member.userKey && known[member.userKey]) {
      log.debug("Leaving AdminConsole.memberLink().");
      return '<a href="' +
        this.esc('/admin/users' + queryWith({ user: member.userKey }, {})) +
        '">' + label + '</a>';
    }
    log.debug("Leaving AdminConsole.memberLink().");
    return label;
  }

  // The "On the users page" cell, for a member and for a memberOf claimant
  // alike. Three states and they are all worth telling apart: a name this
  // console has seen authenticate, a name it could file somebody under but
  // never has, and an entry named in a way that yields no user name at all.
  usersPageCell(userKey, known) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.usersPageCell().");
    if (!userKey) {
      log.debug("Leaving AdminConsole.usersPageCell().");
      return '<span class="state-none" title="This entry is not named ' +
        'uid=&lt;name&gt; and carries no uid, so there is no name to look it ' +
        'up by. The console files people under the local name userFor() ' +
        'derives; an entry named some other way — a cn=, or the one a TLS ' +
        'client certificate seeds — has none.">—</span>';
    }
    if (!known[userKey]) {
      log.debug("Leaving AdminConsole.usersPageCell().");
      return '<span class="state-none" title="The directory holds an entry ' +
        'for them and nothing has authenticated as this name in this ' +
        'process, so the users page has no row to link to. The two lists ' +
        'answer different questions: the directory is what somebody wrote ' +
        'into it, the users page is who has actually presented a credential ' +
        'here.">' + this.esc(userKey) +
        ' <em>(never here)</em></span>';
    }
    log.debug("Leaving AdminConsole.usersPageCell().");
    return '<a href="' +
           this.esc('/admin/users' + queryWith({ user: userKey }, {})) +
      '">' +
      this.esc(userKey) + '</a>';
  }

  noGroupDirectorySection() {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.noGroupDirectorySection().");
    log.debug("Leaving AdminConsole.noGroupDirectorySection().");
    return this.note('No LDAP directory is loaded in this process, so there ' +
      'are no groups to show. That is a build of this service without ' +
      '<code>ldap_server.js</code> and not a failure &mdash; every other ' +
      'page of this console is unaffected.');
  }

  // The list.
  groupsListPage(req) {
    const { log, adminViews, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.groupsListPage().");
    const view = adminViews.groupsListJson(req);
    const info = view.info;
    const wantedText = view.wantedText;
    const needle = view.needle;
    const filtered = view.filtered;
    const paging = view.paging;
    const shown = view.shown;
    const filterParams = view.filterParams;
    const totalMembers = view.totalMembers;
    const totalDangling = view.totalDangling;
    const nav = this.pageNavPair('/admin/groups', filterParams, paging);

    const listView = this.listViewOf('/admin/groups', req.query);
    const rows = shown.map(function (group) {
      // The link carries the list AS IT IS BEING VIEWED, which is what lets the
      // trail on the other side come back to this page of this filter rather
      // than to the top of everything. See listViewOf().
      const href = '/admin/groups' + queryWith(listView, { group: group.dn });
      return '<tr><td><a href="' + self.esc(href) + '">' +
             self.esc(self.groupLabel(group)) +
        '</a></td><td ' +
        'class="who"><code>' + self.esc(group.dn) + '</code></td>' +
        '<td>' + self.groupRuleCell(group.rule) + '</td>' +
        '<td class="num">' + group.memberCount + '</td>' +
        '<td class="num">' + (group.presentCount
          ? '<span class="state-valid">' + group.presentCount + '</span>'
          : '<span class="state-none">0</span>') + '</td>' +
        '<td class="num">' + (group.danglingCount
          ? '<span class="state-revoked" title="Membership values naming an ' +
            'entry this directory does not hold. Deleting a user does not ' +
            'remove it from the groups that list it — referential integrity ' +
            'is a directory feature and not a protocol rule, and this ' +
            'directory deliberately does not have it.">' + group.danglingCount +
            '</span>'
          : '<span class="state-none">0</span>') + '</td>' +
        '<td class="num">' + (group.claimedCount
          ? '<span class="state-expired" title="Entries whose own memberOf ' +
            'names this group and which this group does not list back. ' +
            'Nothing here maintains memberOf, so a client that writes it ' +
            'creates exactly this disagreement.">' + group.claimedCount +
            '</span>'
          : '<span class="state-none">0</span>') + '</td>' +
        '<td class="num">' + group.attributeCount + '</td>' +
        '<td>' + self.esc(group.origin) + '</td>' +
        '<td><code>' + self.esc(group.modifiedAt) + '</code></td></tr>';
    }).join('');


    const inner = this.messagesOf(req) +
      this.directoryListenerWarning(info, GROUPS_SUBJECT) +
      '<div class="tiles">' +
      this.tile(info.groupCount, 'Groups') +
      this.tile(totalMembers, 'Membership values') +
      this.tile(totalDangling, 'Dangling') +
      this.tile(info.entryCount, 'Entries in the directory') +
      '</div><form method="get" action="/admin/groups"><div ' +
      'class="formrow"><label for="q">Group</label><input type="text" id="q" ' +
      'name="q" value="' + this.esc(wantedText) +
      '" ' +
      'size="28" placeholder="part of a cn or a DN"><label for="per">Per ' +
      'page</label><select id="per" ' +
      'name="per">' + this.perPageOptions(paging.perPage) +
      '</select><button ' +
      'type="submit">Filter</button>' +
      (wantedText ? ' <a href="/admin/groups">clear</a>' : '') +
      '</div></form>' +
      nav.head +
      '<table><tr><th>Group</th><th>DN</th><th>Counted because</th><th ' +
      'class="num">Members</th><th class="num">Resolve</th><th ' +
      'class="num">Dangling</th><th class="num">Claimed</th><th ' +
      'class="num">Attributes</th><th>Came from</th><th>Last ' +
      'modified</th></tr>' +
      (rows || '<tr><td colspan="10">No group matches. ' +
               (wantedText ? 'The filter above may be hiding some.' : 'This ' +
                'directory holds none — the two it seeds can be deleted ' +
                'through the protocol like any other entry.') +
               '</td></tr>') + '</table>' +
      nav.foot +
      this.note('A group is an entry that sits under <code>' +
                this.esc(info.groupsDn) +
      '</code>, or that carries one of the group object classes ' +
      '(<code>groupOfNames</code>, <code>groupOfUniqueNames</code>, ' +
      '<code>posixGroup</code>, <code>groupOfURLs</code>) wherever it sits ' +
      '&mdash; either rule is enough, and the column above says which one ' +
      'caught each. Both are applied because this directory is ' +
      '<strong>schemaless</strong>: nothing stops a client adding a ' +
      '<code>groupOfNames</code> under <code>' + this.esc(info.usersDn) +
      '</code>, or an entry with no <code>objectClass</code> at all under ' +
      'the groups container, and a page that applied only one rule would ' +
      'answer for one of those and quietly lose the other.') +
      this.note('<strong>Members</strong> counts the values of ' +
      '<code>member</code>, <code>uniqueMember</code> and ' +
      '<code>memberUid</code> together; <strong>Resolve</strong> is how many ' +
      'of them name an entry this directory actually holds and ' +
      '<strong>Dangling</strong> is the rest. The two are shown apart ' +
      'because a group whose seven members resolve to five is this directory ' +
      'doing exactly what it says it does &mdash; deleting a user leaves its ' +
      'DN in every group that listed it &mdash; and one combined number ' +
      'would report that as seven members with nothing wrong. ' +
      '<strong>Claimed</strong> is the disagreement in the other direction: ' +
      'entries whose own <code>memberOf</code> names the group while the ' +
      'group does not list them back.') +
      // THE GROUP CLAIM'S FOUR SETTINGS ARE HERE, and this is the one placement
      // in SETTING_HOMES that is a judgement rather than an obvious fact. They
      // decide what an OAuth2/OIDC token and a SAML assertion carry, so they
      // could be argued onto either protocol's page; they are here because the
      // question they answer — "why is this group not in my token" — occurs to
      // somebody while they are looking at the membership table above, and
      // because what the claim NAMES is a directory group, which is what this
      // page is about.
      // ---------------------------------------------------------------------
      // THE ONE CONTROL ON THIS LIST, AND IT ARRIVED ON 2026-09-06 (rule 7's
      // mirror is POST /admin-api/groups/create). Below the table rather than
      // above it, because the question this page is usually open to answer is
      // "what is in this directory" and a create form at the top would answer a
      // different one first.
      //
      // A REAL BUTTON AND NO SCRIPT, like every other form on this console:
      // `script-src 'none'` is the service-wide policy and this page is not one
      // of the seven exceptions.
      // ---------------------------------------------------------------------
      (groupWriter
        ? '<h2>Create a group</h2>' +
          '<form method="post" action="/admin/groups">' +
          '<input type="hidden" name="action" value="create">' +
          '<input type="hidden" name="back" value="' +
          this.esc(queryWith(listView, {})) + '">' +
          '<div class="formrow">' +
          '<label for="newgroup">cn</label>' +
          '<input type="text" id="newgroup" name="group" size="28" required ' +
          'placeholder="developers">' +
          '<label for="newnote">Description</label>' +
          '<input type="text" id="newnote" name="note" size="40" ' +
          'placeholder="what this group is for">' +
          '</div><div class="formrow">' +
          '<label for="newmembers">Members</label>' +
          '<textarea id="newmembers" name="members" rows="3" cols="60" ' +
          'placeholder="alice&#10;bob&#10;cn=another-group,' +
          this.esc(info.groupsDn) + '"></textarea>' +
          '<button type="submit">Create</button>' +
          '</div></form>' +
          this.note('It goes to <code>cn=&lt;what you typed&gt;,' +
          this.esc(info.groupsDn) + '</code> as a <code>groupOfNames</code>, ' +
          'so it is counted here by <em>both</em> rules. <strong>Members are ' +
          'one per line or comma-separated</strong>, and each may be a user ' +
          'name or any DN — a group can hold another group, and no user name ' +
          'names one. A name with no entry behind it is written as a ' +
          '<strong>dangling</strong> member rather than refused: this ' +
          'directory does no referential integrity in either direction, and ' +
          'refusing here would make the state the Dangling column reports ' +
          'impossible to produce from this page. Leaving the box empty ' +
          'creates a group with no members, which RFC 4519 says a real ' +
          'directory would refuse and this schemaless one does not.')
        : this.note('<strong>No group can be created from here.</strong> The ' +
          'list above is read through one slot and the writes through ' +
          'another, and this process has only the first — a build without ' +
          '<code>ldap_server.js</code>\'s group writer. An ' +
          '<code>ldapadd</code> and <code>POST /scim/v2/Groups</code> are ' +
          'unaffected.')) +
      this.configFormsFor('/admin/groups') +
      GROUPS_CAVEAT + GROUPS_LINKS;

    log.debug("Leaving AdminConsole.groupsListPage(). " + shown.length + " " +
      "row(s) drawn of " +
              info.groupCount + " group(s).");
    return {
      inner: inner,
      json: view.json
    };
  }

  // One group: every attribute it has, and everybody in it.
  groupDetailPage(req, wantedDn) {
    const { log, adminViews, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.groupDetailPage(). dn=" + wantedDn);
    const view = adminViews.groupDetailJson(req, wantedDn);
    const info = view.info;
    const back = this.note('<a href="' +
      this.esc('/admin/groups' +
               queryWith(this.listViewOf('/admin/groups', req.query), {})) +
      '">Back to the groups</a>.');

    if (!info.found) {
      // Three ways to be here and they are different answers, so they get
      // different words. A 404 is none of them: the console linked here from a
      // list it drew from this same store, and the interesting case is
      // precisely that the store has changed since — a client can delete a
      // group, rename it out of ou=groups, or strip its objectClass through the
      // protocol between one click and the next.
      const because = info.notAGroup
        ? 'There <strong>is</strong> an entry at <code>' +
          this.esc(info.entryDn) +
          '</code>, ' +
          'and it is not a group: it sits outside ' +
          '<code>' + this.esc(info.groupsDn) + '</code> ' +
          'and carries no group <code>objectClass</code>. A ' +
          '<code>modifyDN</code> out of the groups container or a ' +
          '<code>modify</code> that deleted the <code>objectClass</code> ' +
          'does exactly this, and neither is refused &mdash; this directory ' +
          'has no schema. <a href="/admin/ldap/directory">The full dump</a> ' +
          'still shows it.'
        : 'Nothing is at <code>' + this.esc(wantedDn) + '</code>. Either it ' +
          'was <code>delete</code>d or <code>modifyDN</code>&rsquo;d through ' +
          'the protocol since the link was drawn, or the DN was typed. DNs ' +
          'are compared case-folded and with the space after each comma ' +
          'ignored, so the spelling is not what is wrong.';
      log.debug("Leaving AdminConsole.groupDetailPage(). Not a group.");
      return {
        inner: this.messagesOf(req) +
               this.directoryListenerWarning(info, GROUP_SUBJECT) +
          this.note(because) + back,
        json: view.json
      };
    }

    const group = view.group;
    const known = view.known;
    const params = view.params;
    const memberPage = view.memberPage;
    const claimedPage = view.claimedPage;
    const pagedGroup = view.pagedGroup;
    const membersNav = this.pageNavPair('/admin/groups', params,
                                        memberPage.paging);
    const claimedNav = this.pageNavPair('/admin/groups', params,
                                        claimedPage.paging);
    const memberRows = memberPage.shown.map(function (member) {
      const state = member.present
        ? '<span class="state-valid">in the directory</span>'
        : '<span class="state-revoked" title="The value names an entry this ' +
          'directory does not hold. It is shown rather than dropped: nothing ' +
          'here enforces referential integrity, so this is the state the ' +
          'protocol leaves behind when a member is deleted.">dangling</span>';
      return '<tr><td class="who">' + self.memberLink(member, known) + '</td>' +
        '<td><code>' + self.esc(member.attribute) + '</code></td>' +
        '<td>' + state + '</td>' +
        '<td>' + self.esc(member.kind === 'group' ? 'a group' :
                          member.kind === 'entry' ? 'an entry' : '—') +
                          '</td>' +
        '<td>' +
        (member.cn ? self.esc(member.cn) :
         '<span class="state-none">—</span>') +
        '</td><td>' +
        (member.mail ? '<code>' + self.esc(member.mail) + '</code>'
                              : '<span class="state-none">—</span>') + '</td>' +
        '<td>' + self.usersPageCell(member.userKey, known) + '</td>' +
        // The raw value, last, because for `member` and `uniqueMember` it is
        // the DN in the first column all over again — and for `memberUid` it is
        // not, which is the whole reason the column is here.
        '<td class="who"><code>' + self.esc(member.value) + '</code></td></tr>';
    }).join('');

    const claimedRows = claimedPage.shown.map(function (entry) {
      return '<tr><td class="who"><code>' + self.esc(entry.dn) +
             '</code></td>' +
        '<td>' +
        (entry.cn ? self.esc(entry.cn) : '<span class="state-none">—</span>') +
        '</td><td>' + (entry.mail ? '<code>' + self.esc(entry.mail) + '</code>'
                             : '<span class="state-none">—</span>') + '</td>' +
        '<td>' + self.usersPageCell(entry.userKey, known) + '</td></tr>';
    }).join('');

    const claimedSection = group.claimed.length
      ? '<h2>Entries that claim this group, and that it does not list</h2>' +
        claimedNav.head +
        '<table><tr><th>DN</th><th>cn</th><th>mail</th><th>On the users ' +
        'page</th></tr>' +
        claimedRows + '</table>' +
        claimedNav.foot +
        this.note('Each of these carries a <code>memberOf</code> naming this ' +
        'group while this group&rsquo;s own <code>member</code> does not ' +
        'name them back. <strong>Nothing here maintains ' +
        '<code>memberOf</code></strong> &mdash; it is not a standard ' +
        'attribute at all (Microsoft&rsquo;s directory and OpenLDAP&rsquo;s ' +
        '<code>memberof</code> overlay both write it, the server keeping it ' +
        'in step with <code>member</code> itself), and a schemaless mock ' +
        'that neither writes nor checks it lets a client create exactly this ' +
        'disagreement in one <code>modify</code>. They are listed apart from ' +
        'the members above rather than merged into them, because which side ' +
        'of the disagreement a name came from is the only interesting thing ' +
        'about it.')
      : '';

    const inner = this.messagesOf(req) +
      this.directoryListenerWarning(info, GROUP_SUBJECT) +
      '<h2>' + this.esc(this.groupLabel(group)) +
      '</h2><table><tr><th>DN</th><th>Counted ' +
      'as a group because</th><th>Came from</th><th>Created</th><th>Last ' +
      'modified</th></tr><tr><td ' +
      'class="who"><code>' + this.esc(group.dn) + '</code></td>' +
      '<td>' + this.groupRuleCell(group.rule) + '</td>' +
      '<td>' + this.esc(group.origin) + '</td>' +
      '<td><code>' + this.esc(group.createdAt) + '</code></td>' +
      '<td><code>' + this.esc(group.modifiedAt) + '</code></td></tr></table>' +
      this.note('The two timestamps are <em>generalized time</em> ' +
      '(<code>YYYYMMDDHHMMSSZ</code>), which is what a directory shows ' +
      '&mdash; not the ISO 8601 strings the rest of this console uses. An ' +
      'LDAP client bound to <code>ldap://&lt;host&gt;:' +
      this.esc(info.port) + '</code> reading <code>' + this.esc(group.dn) +
      '</code> sees exactly the object below, because it <em>is</em> that ' +
      'object and not a copy of it.') +

      this.perPageForm('/admin/groups', 'group', group.dn,
                       memberPage.paging.perPage,
                       '',
                       this.filterOnly(this.listViewOf('/admin/groups',
                                                       req.query))) +

      '<h2>Members</h2>' +
      '<div class="tiles">' +
      this.tile(group.memberCount, 'Membership values') +
      this.tile(group.presentCount, 'Resolve to an entry') +
      this.tile(group.danglingCount, 'Dangling') +
      this.tile(group.claimed.length, 'Claim it back') +
      '</div>' +
      (group.memberCount
        ? membersNav.head +
          '<table><tr><th>Member</th><th>From</th><th>State</th><th>What it ' +
          'is</th><th>cn</th><th>mail</th><th>On the users page</th><th>The ' +
          'value as stored</th></tr>' +
          memberRows + '</table>' + membersNav.foot
        : this.note('This group lists nobody. An empty ' +
          '<code>groupOfNames</code> is something a real directory refuses ' +
          '&mdash; RFC 4519 makes <code>member</code> MUST &mdash; and this ' +
          'one has no schema, so it is here because something wrote it.')) +
      this.note('Membership is read from <code>' +
      this.esc(group.memberAttributes.join('</code>, ' +
      '<code>')) + '</code>. The first two hold a <strong>DN</strong>; ' +
      '<code>memberUid</code> holds a bare user name, which is looked up ' +
      'under <code>' + this.esc(info.usersDn) + '</code> ' +
      '&mdash; treating the three alike is how a page ends up reporting ' +
      'every <code>posixGroup</code> member as dangling. <strong>Nesting is ' +
      'shown and not expanded</strong>: a member that is itself a group ' +
      'links to its own page, and nobody inside it is counted here, because ' +
      'nothing in this service walks a group tree and a flattened list would ' +
      'be claiming a feature that is not here.') +
      this.note('The last column links to the users page only for somebody ' +
      'this service has actually seen <strong>authenticate</strong>. The ' +
      'other members are marked <em>never here</em>, and that is not a ' +
      'fault: the directory holds whatever somebody wrote into it — the ' +
      'three people it seeds at startup, and anything a client has added ' +
      'since — while the users page holds whoever has presented a credential ' +
      'to this process. <code>alice</code> is in this directory from the ' +
      'moment it starts and appears on the users page only once somebody ' +
      'signs in as her. A link that was always drawn would usually land on ' +
      '&ldquo;nothing here has authenticated as alice&rdquo;, which reads as ' +
      'a broken link rather than as the answer it is.') +
      claimedSection +

      // ---------------------------------------------------------------------
      // ADD SOMEBODY (2026-09-06). On the drill-down rather than on the list,
      // because it needs a group in hand and this is the page that has one.
      // `back` carries the list AS IT IS BEING VIEWED, which is the rule every
      // form on a drill-down here follows — rebuilt through listViewOf()'s
      // whitelist on the way back rather than echoed, so a hand-written `back`
      // cannot become a redirect this file did not write.
      // ---------------------------------------------------------------------
      (groupWriter
        ? '<h2>Add a member</h2>' +
          '<form method="post" action="/admin/groups">' +
          '<input type="hidden" name="action" value="add-member">' +
          '<input type="hidden" name="group" value="' + this.esc(group.dn) +
          '"><input type="hidden" name="back" value="' +
          this.esc(queryWith(this.listViewOf('/admin/groups', req.query), {})) +
          '"><div class="formrow"><label for="newmember">User or ' +
          'DN</label><input type="text" id="newmember" name="member" ' +
          'size="44" required placeholder="alice, or cn=another-group,' +
          this.esc(info.groupsDn) + '">' +
          '<button type="submit">Add</button>' +
          '</div></form>' +
          this.note('The value written is that person&rsquo;s OWN entry ' +
          'wherever it is &mdash; somebody whose entry was seeded by a ' +
          'client certificate is at ' +
          '<code>cn=&lt;name&gt;,' + this.esc(info.usersDn) + '</code> and ' +
          'not at <code>uid=</code>, and a membership written in the wrong ' +
          'form would dangle beside the entry it was meant to name. With ' +
          'nobody there yet, the <code>uid=</code> form is written, so the ' +
          'value resolves the moment they authenticate. It goes onto ' +
          '<code>member</code> whatever else this entry carries, and adding ' +
          'somebody already listed changes nothing and says so. ' +
          '<strong>Taking one out is not here</strong>: it is an ' +
          '<code>ldapmodify</code> or a SCIM <code>PATCH</code>, and <a ' +
          'href="/admin/rbac">Admin roles</a> for the two groups that grant ' +
          'this console.')
        : '') +

      '<h2>Every attribute this group has</h2>' +
      this.attributeTable(group) +
      this.note('The whole object, operational attributes included &mdash; a ' +
      'search returns those only when they are asked for by name (RFC 4511 ' +
      '&sect;4.5.1.8), and this is a dump rather than a search. The ' +
      'membership attributes are in here too, as the raw values the store ' +
      'holds; the table above is the same values resolved. This directory is ' +
      '<strong>schemaless</strong>: no <code>objectClass</code> is enforced ' +
      'and no value is checked against a syntax, so an attribute a real ' +
      'directory would refuse is here because something wrote it.') +
      GROUPS_CAVEAT + back + GROUPS_LINKS;

    log.debug("Leaving AdminConsole.groupDetailPage(). " +
              memberPage.shown.length + " of " +
              group.memberCount + " member value(s) shown, " +
              Object.keys(group.attributes).length + " attribute(s).");
    // The entry is copied rather than sliced in place. groupsFor() builds a
    // fresh object per call today, so mutating it would work — but that is a
    // fact about another module, and a reader of this one cannot see it.
    // `members` and `claimed` go out as THE PAGE, like every other list here;
    // the paging objects sit beside `group` rather than inside it because they
    // describe this reply and not the directory entry, whose own counts are
    // untouched next to them.
    return { inner: inner, json: view.json };
  }

  groupsView(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.groupsView().");
    if (!groupReader) {
      // A build without ldap_server.js. Answered rather than 404'd, and with
      // the nav intact, for the same reason the user page's directory section
      // says it in words: the page exists, the directory does not, and those
      // are different facts about this process.
      log.debug("Leaving AdminConsole.groupsView(). No directory is loaded.");
      return { json: { directory: false, groups: [] }, title: 'Groups',
               inner: this.messagesOf(req) + this.noGroupDirectorySection() };
    }
    const wantedDn = String(req.query.group || '').trim();
    if (wantedDn) {
      const detail = this.groupDetailPage(req, wantedDn);
      log.debug("Leaving AdminConsole.groupsView(). The drill-down.");
      return { json: detail.json, inner: detail.inner,
               title: 'Group ' + wantedDn,
               // The whole DN is the leaf and shortCrumb() cuts it if it has
               // to, rather than being cut to its first RDN here: a leaf
               // trimmed at the shell keeps its full text in the tooltip, and a
               // DN cut here would lose the container it is in with nowhere
               // left to say so. What the cut drops is the tail, which is the
               // part every entry shares.
               up: this.upTo('/admin/groups', wantedDn,
                             this.listViewOf('/admin/groups', req.query)) };
    }
    const list = this.groupsListPage(req);
    log.debug("Leaving AdminConsole.groupsView(). The list.");
    return { json: list.json, inner: list.inner, title: 'Groups' };
  }

  // One application's kinds as cells, since a record commonly carries two — an
  // OAuth client that asked for the openid scope is also a relying party, and a
  // wtrealm handed a SAML 2.0 assertion in one request and the 1.1 default in
  // the next is both of those. The registry accumulates rather than choosing,
  // so the cell has to.
  kindCells(kinds) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.kindCells().");
    if (!kinds.length) {
      log.debug("Leaving AdminConsole.kindCells().");
      return '<span class="state-none">unstated</span>';
    }
    log.debug("Leaving AdminConsole.kindCells().");
    return kinds.map(function (kind) {
      return '<code>' + self.esc(kind) + '</code>';
    }).join('<br>');
  }

  // AN APPLICATION'S KINDS, RECORDED AND DECLARED TOGETHER (2026-09-18). The
  // Kind column read `row.kinds` alone, which a create does not write — so an
  // application declared on /admin/applications/new for OAuth 2.0 and SAML
  // 2.0 showed "unstated" beside that declaration. It is known; the entry just
  // keeps the two apart (see `declaredKinds` in applications.js's view()).
  applicationKindCells(row) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.applicationKindCells().");
    const kinds = (row.kinds || []).slice(0);
    (row.declaredKinds || []).forEach(function (kind) {
      if (kinds.indexOf(kind) < 0) {
        kinds.push(kind);
      }
    });
    log.debug("Leaving AdminConsole.applicationKindCells().");
    return this.kindCells(kinds);
  }

  // THE PROTOCOLS CELL: WHAT IT IS FOR FIRST, THEN WHAT HAS HAPPENED
  // (2026-09-18). It used to lead with the observed list, so a new
  // application read "none recorded" and then "declared: oauth2, oidc, …" —
  // two lines that looked like they disagreed. A declared application now
  // shows its families by name, with what has been seen (or that nothing has
  // yet) under them; one that was never declared shows what was seen.
  applicationProtocolCell(row) {
    const { log, applications } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.applicationProtocolCell().");
    const declared = (row.allowedProtocols || []).map(function (id) {
      const known = (applications.PROTOCOLS || []).filter(function (p) {
        return p.id === id;
      })[0];
      return known ? known.label : id;
    });
    const seen = row.protocols || [];
    if (!declared.length) {
      log.debug("Leaving AdminConsole.applicationProtocolCell(). Observed " +
                "only.");
      return seen.length ? this.esc(seen.join(', '))
                         : '<span class="state-none">none</span>';
    }
    log.debug("Leaving AdminConsole.applicationProtocolCell().");
    return this.esc(declared.join(', ')) +
      '<div class="sub">' + (seen.length
        ? 'seen: ' + self.esc(seen.join(', '))
        : 'not used yet') + '</div>';
  }

  // REGISTERED MEANS SOMEBODY PUT IT HERE ON PURPOSE (2026-09-18) — an
  // administrator, RFC 7591, or this service's own seeding — as against an
  // identifier that merely turned up. It read `row.registered`, which is RFC
  // 7591's flag, and so said "no" about an application just created on
  // /admin/applications/new. The flag itself is unchanged: it is what RFC
  // 9700 mode and RFC 7592 turn on (see appRegisteredBy's schema row).
  applicationRegisteredCell(row) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.applicationRegisteredCell().");
    const by = String(row.registeredBy || (row.registered ? 'rfc7591' : ''));
    if (!by) {
      log.debug("Leaving AdminConsole.applicationRegisteredCell(). No.");
      return '<span class="state-none">no</span>';
    }
    const how = by === 'administrator' ? 'by an administrator'
      : by === 'rfc7591' ? 'RFC 7591'
      : by === 'startup' ? 'at startup'
      : by;
    log.debug("Leaving AdminConsole.applicationRegisteredCell(). " + by);
    return '<span class="state-valid">yes</span><div class="sub">' +
           this.esc(how) + '</div>';
  }

  // WHERE A FINISHED APPLICATION ACTION LANDS. Extracted from the route on
  // 2026-08-27 when `refresh-metadata` became asynchronous and needed the same
  // answer from a callback — two copies of this would be two opinions about
  // where a form goes back to, and the one added later would be the one that
  // forgot the list state.
  respondToApplicationAction(req, res, body, result) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.respondToApplicationAction().");
    // Back to the drill-down the form was posted from, when there was one, so a
    // reader who has just added a redirect URI is looking at the entry that now
    // carries it rather than at the top of the list — and carrying the list
    // state that came in on the form's `back` field either way, so the
    // breadcrumb on the page they land on still offers the filter and page they
    // came from. Without that, editing an application silently costs the reader
    // their place in the list, which is exactly what the trail exists to keep.
    //
    // `create` IS THE ONE ACTION THAT CANNOT NAME ITS APPLICATION IN THE BODY:
    // its field is `identifier`, because the entry does not exist yet. So a
    // create went to the top of the list and left the reader hunting for the
    // row they had just described — which /admin/applications/new made worse,
    // since that page has nothing else on it to go back to. The identifier is
    // taken off the RESULT rather than off the body, so this cannot point at an
    // entry that was refused, and `forget` still lands where it always did (on
    // a drill-down that says the application is gone, which is the honest
    // answer to "what did that do").
    const listView = this.listViewFromBack('/admin/applications', body.back);
    const made = result.ok && result.application
      ? String(result.application.identifier || '') : '';
    const named = made ||
      (result.ok !== false ? String(body.application || '').trim() : '');
    const back = named
      ? '/admin/applications' + queryWith(listView, { application: named }) +
        // Back to the section the button was in, which is four screens down.
        (String(body.action || '') === 'regenerate-secret' ||
         String(body.action || '') === 'rotate-secret' ? '#credentials'
          : (String(body.action || '') === 'issue-software-statement'
            ? '#software-statements'
            : (String(body.action || '') === 'revoke-tls-client-certificate' ||
               String(body.action || '') === 'issue-tls-client-certificate'
              ? '#credentials-tls-client' : '')))
      : '/admin/applications' + queryWith(listView, {});
    this.respondToAction(req, res, back, result);
    log.debug("Leaving the admin applications action endpoint.");
  }

  // ---------------------------------------------------------------------------
  // THE ONE-TIME ANSWER TO AN ISSUED APPLICATION TLS CLIENT CERTIFICATE
  // (RFC 8705, 2026-09-13).
  //
  // Three downloads — the PKCS#12, the encrypted PEM key and the chain — as
  // `data:` links, which is how `/portal/signing-key` hands a person theirs and
  // needs no script. `no-store`, for the rule every document here that carries
  // a key follows. What the application does with it is said beside it: present
  // it at the token endpoint with `token_endpoint_auth_method=tls_client_auth`,
  // and every access token it is issued is bound to it.
  // ---------------------------------------------------------------------------
  answerIssuedTlsClientCertificate(req, res, body, answer) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.answerIssuedTlsClientCertificate().");
    const files = answer.files;
    const cert = answer.certificate;
    const identifier = String((answer.application &&
                               answer.application.identifier) ||
                              body.application || '');
    const dataUri = function (mime, base64) {
      log.debug("Entering dataUri().");
      log.debug("Leaving dataUri().");
      return 'data:' + mime + ';base64,' + base64;
    };
    const b64 = function (text) {
      log.debug("Entering b64().");
      log.debug("Leaving b64().");
      return Buffer.from(String(text), 'utf8').toString('base64');
    };
    const listView = this.listViewFromBack('/admin/applications', body.back);
    const back = '/admin/applications' +
      queryWith(listView, { application: identifier }) +
      '#credentials-tls-client';
    const html = this.warn('<p><strong>This is the only time these files can ' +
      'be downloaded.</strong> The private key is not kept by this service — ' +
      'not on the application&rsquo;s entry, not in the certificate register ' +
      '— and nothing on this console or on <code>/admin-api</code> hands it ' +
      'out again. If it is lost, revoke the certificate and issue ' +
      'another.</p><p><a class="btn" download="' + this.esc(files.pkcs12.name) +
      '" href="' +
      this.esc(dataUri(files.pkcs12.mime, files.pkcs12.base64)) +
      '">Download ' +
      this.esc(files.pkcs12.name) + '</a> <a class="btn" download="' +
      this.esc(files.key.name) + '" href="' +
      this.esc(dataUri(files.key.mime, b64(files.key.text))) + '">Download ' +
      this.esc(files.key.name) + '</a> <a class="btn" download="' +
      this.esc(files.chain.name) + '" href="' +
      this.esc(dataUri(files.chain.mime, b64(files.chain.text))) +
      '">Download ' +
      this.esc(files.chain.name) + '</a></p>', 'The private key, once') +
      '<table><tr><th>Issued to</th><td><code>' + this.esc(cert.subject) +
      '</code><div class="sub">subjectAltName <code>' +
      this.esc(cert.implicitName) + '</code></div></td></tr>' +
      '<tr><th>Serial</th><td><code>' + this.esc(cert.serialHex) +
      '</code></td></tr><tr><th>SHA-256 thumbprint</th><td><code>' +
      this.esc(cert.thumbprint) + '</code></td></tr><tr><th>Key</th><td>' +
      this.esc(cert.keyAlg) + '</td></tr><tr><th>Good until</th><td><code>' +
      this.esc(cert.notAfter) + '</code></td></tr></table>' +
      this.note('<strong>How the application uses it.</strong> Present it on ' +
      'the TLS connection to the token endpoint with <code>client_id=' +
      this.esc(identifier) + '</code> and a <code>token_endpoint_auth_method' +
      '</code> of <code>tls_client_auth</code> on the entry: RFC 8705 ' +
      'section 2.1 authenticates it because this realm issued it to this ' +
      'application, with nothing else registered. Every access token issued ' +
      'on that connection carries <code>cnf["x5t#S256"]</code> of this ' +
      'certificate (section 3), so the application presents the same ' +
      'certificate to the resource servers. <code>curl --cert ' +
      this.esc(files.chain.name) +
      ' --key ' + this.esc(files.key.name) + ' --pass &lt;file password&gt; ' +
      '-d grant_type=client_credentials -d client_id=' + this.esc(identifier) +
      ' &lt;base&gt;/oauth2/token</code>') +
      '<p><a class="btn" href="' + this.esc(back) + '">Back to ' +
      this.esc(identifier) +
      '</a></p>';
    res.set('Cache-Control', 'no-store');
    this.respond(req, res, { ok: true }, 'TLS client certificate',
                 '/admin/applications', html,
                 this.upTo('/admin/applications', 'TLS client certificate',
                           listView));
    log.debug("Leaving AdminConsole.answerIssuedTlsClientCertificate().");
  }

  // One value off an application view's fields, whichever shape it is in. The
  // registry hands `multi` attributes back as arrays and `single` ones as
  // strings, and a page that assumed either would be wrong for half the schema.
  firstFieldValue(row, attribute) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.firstFieldValue().");
    const value = (row && row.fields && row.fields[attribute]);
    const one = Array.isArray(value) ? value[0] : value;
    log.debug("Leaving AdminConsole.firstFieldValue().");
    return String(one == null ? '' : one).trim();
  }

  // The two selects the edit forms offer, built from applications.js's EDITABLE
  // table so that a form cannot offer a field the action would refuse — the
  // same reason the audit page's filters are built from the audit vocabulary.
  //
  // `row` IS THE ENTRY BEING EDITED, and it is here because that rule acquired
  // a second half on 2026-09-01. An attribute whose SCHEMA row carries
  // `families` may only be written onto an entry declared for one of them —
  // `oauthTokenExchangeRefreshToken` and, since 2026-09-12, `ssfAllowedEvents`
  // — and `updateApplication()` refuses it. Offering it in this select on a
  // SAML-only application would be exactly the drift this function exists to
  // prevent: a control that looks like a control and answers with a refusal.
  // Both halves come off the same SCHEMA member, so a second family-scoped
  // attribute needs nothing here.
  //
  // The entry is optional: `editableOptions(mode, selected)` with no entry
  // offers everything, which is what a caller with no application in hand
  // should get.
  editableOptions(mode, selected, row?) {
    const { log, applications } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.editableOptions().");
    const declared = row ? applications.declaredFamiliesOf(row) : null;
    log.debug("Leaving AdminConsole.editableOptions().");
    return applications.editableAttributes(mode).filter(function (attribute) {
      if (!declared) {
        return true;
      }
      return !applications.familyRefusal(attribute.name, declared,
                                         (row && row.identifier) || '');
    }).map(function (attribute) {
      return '<option value="' + self.esc(attribute.name) + '"' +
        (attribute.name === selected ? ' selected' : '') + '>' +
             self.esc(attribute.name) +
        (attribute.sensitive ? ' (credential)' : '') + '</option>';
    }).join('');
  }

  // What one attribute of an application entry IS, as the drill-down's third
  // column. Split out of that page because the entry carries FOUR kinds of
  // attribute and the table only ever described one of them — so everything
  // else came out as "not in the published schema", which is true of
  // `objectClass` and `createTimestamp` in the narrowest sense and useless as
  // an explanation.
  //
  // The order is the order of certainty: the registry's own table first, since
  // it is the same table the entry was written from; then the operational ones,
  // which the DIRECTORY sets and no schema of this module's would ever mention;
  // then the object classes, published one heading further down
  // `/admin/ldap/applications`; and only then the honest "somebody wrote this
  // by hand", which is a real state — this directory is schemaless and an
  // ldapmodify can put anything on an entry.
  //
  // No description is invented for an attribute nothing here knows. Saying
  // something confident about a name written by hand is how a page starts
  // lying.
  applicationAttributeNote(name, operational) {
    const { log, applications } = this.deps;
    log.debug("Entering AdminConsole.applicationAttributeNote().");
    const lower = String(name).toLowerCase();
    const spec = applications.SCHEMA.attributes.filter(function (one) {
      return one.name.toLowerCase() === lower;
    })[0];
    if (spec) {
      log.debug("Leaving AdminConsole.applicationAttributeNote().");
      return { text: spec.what, sensitive: !!spec.sensitive };
    }
    if (lower === 'entrydn') {
      log.debug("Leaving AdminConsole.applicationAttributeNote().");
      return { text: 'WHERE THE ENTRY IS. RFC 5020, and the directory ' +
                     'synthesises it rather than storing it: the DN is the ' +
                     'key the entry is held under, so a stored copy would be ' +
                     'a second definition of the same fact and the one that ' +
                     'goes stale the moment the entry is renamed. It is the ' +
                     'name an ldapsearch filter matches this by, which is ' +
                     'why the dump calls it the same thing.' };
    }
    if (lower === 'createtimestamp' || lower === 'modifytimestamp') {
      log.debug("Leaving AdminConsole.applicationAttributeNote().");
      return { text: 'The directory\'s own, not the registry\'s: when this ' +
                     'ENTRY was ' +
                     (lower === 'createtimestamp' ? 'created' :
                      'last written') +
                     '. Different from appFirstSeen and appLastSeen one row ' +
                     'up, which are when the APPLICATION was seen — an ' +
                     'ldapmodify moves this one and not those.' };
    }
    if (lower === 'objectclass') {
      log.debug("Leaving AdminConsole.applicationAttributeNote().");
      return { text: 'The classes this entry claims, from the registry\'s ' +
                     'vocabulary: ' +
                     applications.SCHEMA.objectClasses.map(function (one) {
                       return one.name;
                     }).join(', ') + '. A VOCABULARY and not a constraint — ' +
                     'node-ldapjs has no schema subsystem and this directory ' +
                     'is schemaless on purpose, so nothing rejects an entry ' +
                     'for disobeying it.' };
    }
    if (operational) {
      log.debug("Leaving AdminConsole.applicationAttributeNote().");
      // An operational attribute this function has no sentence for, which means
      // ldap_server.js's OPERATIONAL list grew and this one did not. Saying so
      // is better than the "written by hand" answer below, which would be
      // flatly wrong about an attribute the directory sets itself.
      return { text: 'An operational attribute the directory sets. A search ' +
                     'returns it only when it is asked for by name (RFC 4511 ' +
                     'section 4.5.1.8); this dump is not a search, so it is ' +
                     'here. This page has nothing more specific to say about ' +
                     'it.' };
    }
    log.debug("Leaving AdminConsole.applicationAttributeNote().");
    return { text: 'Not in the published schema and not one the directory ' +
                   'sets — written by hand into this entry, which nothing ' +
                   'here prevents and which is what a schemaless directory ' +
                   'means. The registry\'s own writes REPLACE the entry, so ' +
                   'a value here survives only until the next time this ' +
                   'application is seen.' };
  }

  // THE LIST'S EXPIRING-SECRET MARK (#49 P5): a client secret that has
  // expired, or expires within oauth2.clientSecretExpiryWarningDays — the
  // same two the daily job oauth2.client-secret-expiry warns about.
  secretExpiryMark(row) {
    const { log, applications, config } = this.deps;
    log.debug("Entering AdminConsole.secretExpiryMark().");
    const record = applications.get(row.identifier);
    const fields = (record && record.fields) || {};
    const expiresAt = fields.oauthClientSecret
      ? applications.secretExpiryOf(fields) : 0;
    if (!expiresAt) {
      log.debug("Leaving AdminConsole.secretExpiryMark(). None.");
      return '';
    }
    const nowS = Math.floor(Date.now() / 1000);
    const warnS = Number(config.value('oauth2.clientSecretExpiryWarningDays')) *
                  86400;
    log.debug("Leaving AdminConsole.secretExpiryMark().");
    return expiresAt <= nowS
      ? '<div class="sub warn">Client secret EXPIRED ' +
        this.esc(new Date(expiresAt * 1000).toISOString()) + '</div>'
      : expiresAt - nowS <= warnS
        ? '<div class="sub warn">Client secret expires ' +
          this.esc(new Date(expiresAt * 1000).toISOString()) + '</div>'
        : '';
  }

  applicationsListPage(req) {
    const { log, adminViews, queryWith, applications } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.applicationsListPage().");
    const view = adminViews.applicationsListJson(req);
    const all = view.all;
    const wantedText = view.wantedText;
    const wantedKind = view.wantedKind;
    const needle = view.needle;
    const filtered = view.filtered;
    const paged = view.paged;
    const paging = view.paging;
    const filterParams = view.filterParams;
    const registeredCount = view.registeredCount;
    const nav = this.pageNavPair('/admin/applications', filterParams, paging);

    const listView = this.listViewOf('/admin/applications', req.query);
    const rows = paged.shown.map(function (row) {
      // The link carries the list AS IT IS BEING VIEWED, which is what lets the
      // trail on the other side come back to this page of this filter rather
      // than to the top of everything. See listViewOf().
      const href = '/admin/applications' +
                   queryWith(listView, { application: row.identifier });
      return '<tr><td><a href="' + self.esc(href) + '"><code>' +
             self.esc(row.identifier) +
        '</code></a>' +
        // The DN on every row rather than only where the RDN is a digest. These
        // entries ARE the registry, so the DN is what an ldapsearch or
        // ldapmodify is aimed at; showing it only in the odd case made it look
        // like a note about a special entry instead of the address of every one
        // of them.
        (row.dn ? '<div class="sub"><code>' + self.esc(row.dn) + '</code>' +
          (row.identifier === row.dnLabel ? '' :
            ' &mdash; the identifier is too long for a readable RDN, so the ' +
            '<code>cn</code> is a digest of it') + '</div>' : '') +
        '</td><td>' + self.esc(row.name) + self.secretExpiryMark(row) +
        '</td>' +
        '<td>' + self.applicationKindCells(row) + '</td>' +
        // BOTH PROTOCOL LISTS IN ONE CELL, and the declared half is labelled
        // rather than run in with the other. An application created by hand has
        // no observed protocols at all — it has never connected — so this cell
        // was blank on exactly the entries somebody had just finished
        // describing, which reads as the create having lost what was ticked.
        // The two are not the same claim, so they are not the same line: the
        // labels are what HAPPENED and the ids under them are what was
        // DECLARED.
        // DECLARED FIRST since 2026-09-18 — applicationProtocolCell() says why.
        '<td>' + self.applicationProtocolCell(row) + '</td>' +
        '<td>' + self.applicationRegisteredCell(row) + '</td>' +
        '<td class="num">' + row.authentications + '</td>' +
        '<td class="num">' + row.sessions + '</td>' +
        '<td class="num">' + row.users + '</td>' +
        '<td><code>' + self.esc(row.lastSeen) + '</code></td></tr>';
    }).join('');

    const kindOptions = ['<option value=""' + (wantedKind ? '' : ' selected') +
                         '>any kind</option>']
      .concat(applications.KINDS.map(function (one) {
        // Counted over EVERYTHING rather than over the filtered set, so the
        // numbers do not change as the reader narrows the list — a select whose
        // options renumber themselves on every Filter is one nobody can use to
        // find out where the rows went.
        const n = all.filter(function (row) {
          return row.kinds.indexOf(one.kind) >= 0 ||
                 (row.declaredKinds || []).indexOf(one.kind) >= 0;
        }).length;
        return '<option value="' + self.esc(one.kind) + '"' +
               (one.kind === wantedKind ? ' selected' : '') + '>' +
               self.esc(one.label) + ' (' + n + ')</option>';
      })).join('');


    const inner = this.messagesOf(req) +
      '<div class="tiles">' +
      this.tile(all.length, 'Applications') +
      this.tile(registeredCount, 'Registered') +
      this.tile(all.reduce(function (n, r) { return n + r.authentications; },
                           0),
                'Authentications') +
      this.tile(applications.maxApplications ? applications.maxApplications() :
                '',
                'Maximum ' +
          'held') +
      '</div><form method="get" action="/admin/applications"><div ' +
      'class="formrow"><label for="q">Application</label><input type="text" ' +
      'id="q" name="q" value="' + this.esc(wantedText) +
      '" ' +
      'size="28" placeholder="client_id, wtrealm, entityID, SPN or ' +
      'name"><label for="kind">Kind</label><select id="kind" ' +
      'name="kind">' + kindOptions + '</select>' +
      '<label for="per">Show</label>' +
      '<select id="per" name="per">' + this.perPageOptions(paging.perPage) +
      '</select><button ' +
      'type="submit">Filter</button>' +
      ((wantedText || wantedKind)
        ? ' <a href="/admin/applications">clear</a>' : '') +
      '</div></form>' +
      nav.head +
      '<table><tr><th>Identifier</th><th>Name</th><th>Kind</th><th>' +
      'Protocols</th><th>Registered</th><th class="num">Auth</th><th ' +
      'class="num">Sessions</th><th class="num">Users</th><th>Last ' +
      'seen</th></tr>' +
      (rows || '<tr><td colspan="9">No application matches. ' +
               ((wantedText || wantedKind)
                 ? 'The filter above may be hiding some.'
                 : 'One appears the first time a client_id, wtrealm, ' +
                   'AppliesTo, entityID or service principal name is ' +
                   'accepted here.') + '</td></tr>') +
      '</table>' +
      nav.foot +
      '<h2>Add an application</h2>' +
      // ---------------------------------------------------------------------
      // THE DOOR TO THE FULLER FORM IS A BUTTON, AND SINCE 2026-09-06 IT IS THE
      // ONLY WAY TO IT.
      //
      // `/admin/applications/new` had a row in the sidebar and lost it (see
      // SECTIONS): it is the longer of two forms on this page rather than a
      // place in this console. What that leaves is this control, so it cannot
      // go on being a link inside a sentence — a `<p class="sub">` is what a
      // reader skims past, and skipping it now means not finding the protocol
      // families, the per-protocol identifiers or the redirect URIs at all.
      //
      // `a.btn` rather than a form with a submit in it, because nothing is
      // written by pressing it: it is a link that looks like the next action,
      // which is exactly what it is. `/admin/users/new`'s door is a GET FORM
      // instead, and the difference is a real one rather than an inconsistency
      // — that box carries the typed username onward and this page has no field
      // to carry, since the short row below is where a bare identifier goes.
      // ---------------------------------------------------------------------
      '<p><a class="btn" href="/admin/applications/new">New application ' +
      '&rsaquo;</a></p>' +
      this.note('<strong>The button opens the fuller form</strong> &mdash; ' +
      'the same action, with the PROTOCOL FAMILIES this application is ' +
      'declared for and a sentence about each, its per-protocol identifiers ' +
      'and its redirect URIs. It has no tab of its own in the sidebar ' +
      'because it is not a place in this console: it is the long way in from ' +
      'this page, and the row below is the short one. Both post here and ' +
      'reach one function.') +
      this.note('For a relying party that has not connected yet. An entry ' +
      'usually appears because an identifier was ACCEPTED — a client_id at ' +
      'the token endpoint, a wtrealm on a sign-in response — and this is how ' +
      'to get one in ahead of that, which is what RFC 9700 mode needs if it ' +
      'is to judge a client against its own redirect URIs rather than ' +
      'against the <code>oauth2.redirectUris</code> setting. It records that ' +
      'it was created by hand, so it cannot be mistaken for one that turned ' +
      'up once and never came back.') +
      '<form method="post" action="/admin/applications"><div ' +
      'class="formrow"><input type="hidden" name="action" ' +
      'value="create"><label for="identifier">Identifier</label><input ' +
      'type="text" id="identifier" name="identifier" size="30" required ' +
      'placeholder="client_id, wtrealm, entityID or SPN"><label ' +
      'for="newname">Name</label><input type="text" id="newname" name="name" ' +
      'size="18" placeholder="optional"><button ' +
      'type="submit">Add</button></div></form>' +
      this.note('This row takes the identifier and a name and nothing else ' +
      '&mdash; it is the short way in for somebody already looking at the ' +
      'list. The <em>Kind</em> select that used to sit in it is gone for the ' +
      'reason <a href="/admin/applications/new">New application</a> gives at ' +
      'length: it asked the same question the protocol families do, in a ' +
      'vocabulary that does not line up with theirs, and it is DERIVED ' +
      'rather than declared &mdash; a kind is written when a protocol ' +
      'actually recognises the identifier. The fuller form is where the ' +
      'families, the per-protocol identifiers and the redirect URIs are, and ' +
      'an entry made here can be given all of them afterwards from its own ' +
      'page.') +
      this.note('<strong>One entry per identifier, whatever protocol brought ' +
      'it.</strong> The key is the identifier exactly as it arrived &mdash; ' +
      'not lower-cased and not namespaced by protocol &mdash; so an ' +
      'application appearing under one name in two protocols is one row with ' +
      'two kinds rather than two rows. That is the same rule that makes ' +
      '<code>alice</code>, her <code>urn:uuid:</code> subject and ' +
      '<code>alice@REALM</code> one person on the users page.') +
      this.note('<strong>Sessions and Users are counts of CHANGES, not of ' +
      'distinct sets.</strong> The ids themselves are deliberately not kept ' +
      'on the entry &mdash; an application used by two thousand people would ' +
      'otherwise carry two thousand values &mdash; so the count moves when ' +
      'the id differs from the last one recorded. Right for the ordinary ' +
      'case, and it undercounts somebody alternating between two ' +
      'applications.') +
      // The two applications.* rows: how many entries this registry remembers,
      // and whether the console and the management API are seeded into it as
      // applications of their own.
      this.configFormsFor('/admin/applications') +
      APPLICATIONS_CAVEAT + APPLICATIONS_LINKS;

    log.debug("Leaving AdminConsole.applicationsListPage(). " +
              paged.shown.length + " row(s) of " +
              filtered.length + " matched.");
    return {
      inner: inner,
      json: view.json
    };
  }

  // THE TWO PROTOCOL LISTS AN APPLICATION ENTRY CARRIES, SIDE BY SIDE.
  //
  // `appAllowedProtocol` is DECLARED — ticked on /admin/applications/new, or
  // added here — and what the entry has been RECORDED as is derived from its
  // KINDS. Both are in the attribute table below like everything else; this
  // section exists because comparing them is the whole question a reader has,
  // and the comparison cannot be made by eye: one list holds ids (`oauth2`) and
  // the entry's own `Protocols` row holds the prose labels /admin/users spells
  // protocols with (`OAuth 2.0 / OIDC`).
  //
  // **THE MATCH IS ON KINDS AND NOT ON THOSE LABELS**, which is the one thing
  // to know before editing this. Matching on labels is what this was written as
  // and it was wrong: a FEDERATION partner's sighting is recorded under
  // whichever protocol its relationship speaks, so by label every ordinary
  // OAuth client read as a federation partner. applications.js's
  // `protocolIdsForKinds()` is the translation and its header carries the
  // argument; a second copy of that map in this file would be a second answer
  // to "has this family been seen".
  //
  // **A family with no kind at all is marked**, and that is the row this
  // section is really for. LDAP, SCIM, SPIFFE, mutual TLS and OpenID4VCI record
  // no application identifier anywhere in this service, so nothing will EVER
  // record one — a bare "no" beside them would read as an application that has
  // not been used, when it means the question cannot be answered here.
  //
  // **"Recorded" IS NOT "HAS AUTHENTICATED"**, and the column is named for what
  // it actually reads. A kind is usually written when a protocol recognises the
  // identifier, but createApplication() takes one, so a hand-made entry can
  // carry a kind and no authentications at all. The page says so rather than
  // letting a reader take the column for evidence of traffic; the
  // Authentications tile is the figure that is.
  //
  // Only the families that are declared or recorded are listed. Sixteen rows of
  // "no, no" on every drill-down would be a table nobody reads, and the ones
  // that say nothing are exactly the ones with nothing to say.
  // ---------------------------------------------------------------------------
  // THE CORS ORIGINS ON AN APPLICATION'S PAGE (2026-09-18).
  //
  // `appCorsOrigin` configures CORS on every endpoint this service publishes
  // (`common/cors.js`), and it is a LIST — so it is edited the way this
  // console edits every other list a person maintains by hand (the claims on
  // /admin/claims, the trust anchors on /admin/tls): one row per value, a
  // Remove button ON the row, and one box beneath to add another. The generic
  // Set / Add to / Remove from controls further down reach the same attribute
  // and still do; they make a person retype a value to remove it, which for
  // an origin is exactly the string most likely to be mistyped.
  //
  // **THE REMOVE BUTTON POSTS THE VALUE AS STORED, NOT AS NORMALISED.** A
  // write through this service stores the normalised origin, but an
  // `ldapmodify` stores what it was given, and `updateApplication()` removes
  // the value as typed or, failing that, its normalised spelling. Posting the
  // normalised form of a value that was stored in mixed case would match
  // neither, and the button would do nothing while looking as if it worked.
  //
  // THIS PAGE DOES NOT DECIDE ANYTHING: both forms post `add` and `remove` to
  // /admin/applications, which is `updateApplication()` — the origin check
  // (`STS-REG-0150`) and the normalisation are there, and
  // `POST /admin-api/applications/add|remove` reaches the same function.
  // ---------------------------------------------------------------------------
  applicationCorsSection(row, carryBack) {
    const { log, applications } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.applicationCorsSection().");
    const held = [].concat((row.fields && row.fields.appCorsOrigin) || [])
      .map(function (one) { return String(one); })
      .filter(function (one) { return one !== ''; });
    const rows = held.map(function (stored) {
      // A value an `ldapmodify` wrote in another spelling is shown with the
      // origin a browser would actually send, because that is the string the
      // Origin header is compared against — and a reader wondering why a
      // listed origin is refused needs to see the two side by side. Asked of
      // the registry's own reader one value at a time, so this is the
      // normalisation `common/cors.js` compares against and not a copy of it.
      const canonical = applications.corsOriginsOf(
        { appCorsOrigin: [stored] })[0] || '';
      const differs = canonical && canonical !== stored;
      return '<tr><td><code>' + self.esc(stored) + '</code>' +
        (differs ? '<br><span class="sub">matched as <code>' +
                   self.esc(canonical) + '</code></span>' : '') +
        '</td><td><form method="post" action="/admin/applications" ' +
        'class="inline">' + carryBack +
        '<input type="hidden" name="action" value="remove">' +
        '<input type="hidden" name="application" value="' +
        self.esc(row.identifier) + '">' +
        '<input type="hidden" name="attribute" value="appCorsOrigin">' +
        '<input type="hidden" name="value" value="' + self.esc(stored) + '">' +
        '<button type="submit" class="secondary" title="' +
        self.esc('A page on ' + stored + ' stops being able to read this ' +
                 'service\'s answers for this application on its next ' +
                 'request.') + '">Remove</button></form></td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.applicationCorsSection(). " +
              held.length + " origin(s).");
    return '<h2>Web origins allowed to call it (CORS)</h2>' +
      '<p><strong>These origins configure CORS on every published protocol ' +
      'endpoint of this service</strong> for this application &mdash; a ' +
      'browser page on one of them may read the answers to requests that ' +
      'name this application as their client, whatever the protocol. They ' +
      'are this entry\'s <code>appCorsOrigin</code>.</p>' +
      this.note('A request that names NO client &mdash; discovery, a JWKS, a ' +
      'DID document, a CORS preflight &mdash; is answered for an origin ' +
      'listed on ANY application in this realm, so an origin added here ' +
      'also lets that page read those. Empty allows no third-party origin, ' +
      'in both modes; this service\'s own origins and ' +
      '<code>global.corsOrigins</code> never need listing.') +
      '<table><tr><th>Origin</th><th></th></tr>' +
      (rows || '<tr><td colspan="2"><span class="state-none">None &mdash; ' +
       'no page on another origin may read this service\'s answers for ' +
       'this application.</span></td></tr>') +
      '</table>' +
      '<form method="post" action="/admin/applications">' + carryBack +
      '<div class="formrow">' +
      '<input type="hidden" name="action" value="add">' +
      '<input type="hidden" name="application" value="' +
      this.esc(row.identifier) + '">' +
      '<input type="hidden" name="attribute" value="appCorsOrigin">' +
      '<label for="add-cors-origin">Add an origin</label>' +
      '<input type="text" id="add-cors-origin" name="value" size="42" ' +
      'required placeholder="https://app.example.com">' +
      '<button type="submit">Add</button></div></form>' +
      this.note('One exact origin &mdash; a scheme, a host and an optional ' +
      'port, with no path and no wildcard. It is stored normalised, so ' +
      '<code>HTTPS://App.Example.com:443</code> is kept as ' +
      '<code>https://app.example.com</code>, and one that is not an origin ' +
      'is refused with the reason (<code>STS-REG-0150</code>).');
  }

  protocolFamilySection(row) {
    const { log, applications } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.protocolFamilySection().");
    const declared = row.allowedProtocols || [];
    const seen = row.recordedProtocols || [];
    const shown = applications.PROTOCOL_IDS.filter(function (id) {
      return declared.indexOf(id) >= 0 || seen.indexOf(id) >= 0;
    });

    const rows = shown.map(function (id) {
      const meta = applications.protocolRow(id) ||
                   { label: id, kinds: [], kind: '' };
      const isDeclared = declared.indexOf(id) >= 0;
      const isSeen = seen.indexOf(id) >= 0;
      const seenCell = isSeen
        ? '<span class="state-valid">yes</span>'
        : ((meta.kinds || []).length
            ? '<span class="state-none">no</span>'
            : '<span class="state-none" title="This service records no ' +
              'application identifier in that family, so no entry will ever ' +
              'carry a kind for it.">never recorded here</span>');
      return '<tr><td>' + self.esc(meta.label) + '</td><td><code>' +
             self.esc(id) +
        '</code></td><td>' + (isDeclared ? '<span ' +
          'class="state-valid">yes</span>'
                             : '<span class="state-none">no</span>') + '</td>' +
        '<td>' + seenCell + '</td></tr>';
    }).join('');

    log.debug("Leaving AdminConsole.protocolFamilySection(). " + shown.length +
              " row(s).");
    return '<h2>Protocol families</h2>' +
      this.note('<strong>Declared</strong> is ' +
      '<code>appAllowedProtocol</code> &mdash; what somebody said this ' +
      'application is FOR, on <a href="/admin/applications/new">New ' +
      'application</a> or through the Add and Remove forms below. ' +
      '<strong>Recorded</strong> is read off the KINDS in the table above, ' +
      'and not off the protocol labels beside them, because a federation ' +
      'partner is recorded under the protocol its relationship speaks and by ' +
      'label would be indistinguishable from an ordinary client.') +
      this.note('<strong>Recorded is not the same as "has ' +
      'authenticated".</strong> A kind is usually written when a protocol ' +
      'recognises the identifier &mdash; but a create takes one too, so an ' +
      'entry made by hand can be recorded in a family it has never connected ' +
      'in. The <em>Authentications</em> count above is the figure that ' +
      'answers whether anything has actually happened. <strong>And neither ' +
      'column grants anything:</strong> a family declared and not recorded ' +
      'is usually an application that has not connected yet, one recorded ' +
      'and never declared is the ordinary case, and nothing asks a protocol ' +
      'to check either &mdash; a mock that refused a protocol would remove a ' +
      'test case rather than add one.') +
      '<table><tr><th>Family</th><th>Value</th><th>Declared</th><th>' +
      'Recorded</th></tr>' +
      (rows || '<tr><td colspan="4">Neither list has anything on it. Nothing ' +
               'has been declared for this application and it carries no ' +
               'kind &mdash; which for an entry created by hand with no kind ' +
               'chosen is the state it is created in.</td></tr>') +
      '</table>';
  }

  applicationPermissionsSection(req, row, carryBack) {
    const { log, applicationPermissionsState, pageParamsOf,
            queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.applicationPermissionsSection(). " +
      "identifier=" +
              row.identifier);
    const identifier = row.identifier;
    const state = applicationPermissionsState(req.query, identifier);
    const register = state.register;
    const held = state.held;
    const exposes = state.exposes;
    const offerable = state.offerable;
    const heldPage = state.heldPage;
    const exposedPage = state.exposedPage;
    const navParams = pageParamsOf(req.query);
    const heldNav = this.pageNavPair('/admin/applications', navParams,
                                     heldPage.paging);
    const exposedNav = this.pageNavPair('/admin/applications', navParams,
                                        exposedPage.paging);
    const options = offerable.map(function (one) {
      return '<option value="' + self.esc(one.id) + '">' + self.esc(one.id) +
             ' — exposed by ' + self.esc(one.resourceName) + '</option>';
    }).join('');

    const heldRows = heldPage.shown.map(function (one) {
      return '<tr>' +
        '<td>' + (one.resource
          ? '<a href="' + self.esc('/admin/applications' +
              queryWith(self.listViewOf('/admin/applications', req.query),
                        { application: one.resource })) + '">' +
            self.esc(one.resourceName) + '</a>'
          : '<span class="state-revoked" title="No application in this ' +
            'registry defines this permission — a deleted resource, a ' +
            'permission removed from under the grant, or an ldapmodify. Both ' +
            'console doors refuse to create one.">dangling</span>') + '</td>' +
        '<td>' + (one.permissionName
          ? '<code>' + self.esc(one.permissionName) + '</code>' +
            (one.description
              ? '<br><span class="state-none">' + self.esc(one.description) +
                '</span>'
              : '')
          : '<span class="state-none">&mdash;</span>') + '</td>' +
        '<td><code>' + self.esc(one.permissionId) + '</code></td>' +
        '<td>' + (one.baseUri
          ? '<code>aud: ' + self.esc(one.baseUri) + '</code><br>' +
            '<code>scope: ' + self.esc(one.permissionName) + '</code>'
          : '<span class="state-none">nothing &mdash; the permission does ' +
            'not resolve, so this scope is treated as an ordinary one</span>') +
        '</td><td>' + (one.asked
          ? '<span class="state-valid" title="This entry\'s oauthScope ' +
            'records having asked for this scope. Evidence rather than ' +
            'proof: it records what was requested, not what was ' +
            'issued.">asked for</span>'
          : '<span class="state-none" title="It has never asked for this ' +
            'one. A configured grant nobody has needed is exactly what this ' +
            'register is here to show.">never asked for</span>') + '</td>' +
        // THE ROW BUTTON POSTS TO /admin/delegation TOO, for the reason the
        // header gives — and it needs no `client` select either, because the
        // row IS the pair.
        '<td><form method="post" action="/admin/delegation">' + carryBack +
          '<div class="formrow">' +
          '<input type="hidden" name="action" value="revoke-permission">' +
          '<input type="hidden" name="from" value="/admin/applications">' +
          '<input type="hidden" name="client" value="' + self.esc(identifier) +
          '"><input type="hidden" name="permission" value="' +
            self.esc(one.permissionId) + '">' +
          '<button type="submit" class="danger">Revoke</button>' +
          '</div></form></td>' +
        '</tr>';
    }).join('');

    const exposedRows = exposedPage.shown.map(function (one) {
      return '<tr><td><code>' + self.esc(one.name) + '</code>' +
        (one.description
          ? '<br><span class="state-none">' + self.esc(one.description) +
            '</span>' :
         '') +
        '</td><td>' + (one.id
          ? '<code>' + self.esc(one.id) + '</code>'
          : '<span class="state-revoked" title="This application has no ' +
            'oauthPermissionBaseUri, and a permission is named by its base ' +
            'URI followed by its name — so no client can ever ask for this ' +
            'one. Set the base on the Delegation page and it resolves.">no ' +
            'identifier &mdash; this application has no base ' +
            'URI</span>') + '</td>' +
        '<td class="num">' + (one.grantedTo.length
          ? '<span class="state-valid">' + one.grantedTo.length + '</span>'
          : '<span class="state-none" title="Nothing holds it. That is the ' +
            'ordinary state of a permission that has just been defined — ' +
            'defining one grants it to nobody.">0</span>') + '</td>' +
        '<td class="who">' + (one.grantedTo.length
          ? one.grantedTo.map(function (who) {
              return self.esc(who.name) + (who.asked ? '' :
                ' <span class="state-none" title="Granted and never asked ' +
                'for.">(unused)</span>');
            }).join('<br>')
          : '<span class="state-none">&mdash;</span>') + '</td></tr>';
    }).join('');

    log.debug("Leaving AdminConsole.applicationPermissionsSection(). " +
              held.length +
              " held (" + heldPage.shown.length + " drawn), " + exposes.length +
              " exposed (" + exposedPage.shown.length + " drawn), " +
              offerable.length + " offerable.");
    return '<h2 id="permissions">Delegated permissions</h2>' +

      this.note('<strong>Which OTHER applications\' APIs this one may reach, ' +
      'on behalf of whoever is signed in.</strong> The model is Microsoft ' +
      'Entra ID\'s: a RESOURCE application is given a base URI and exposes ' +
      'named permissions on it, and a CLIENT application is granted some of ' +
      'them. The grant lands on the CLIENT\'s entry — this one — as a value ' +
      'of <code>oauthDelegatedPermission</code>, because the client is the ' +
      'party that will name the permission in a <code>scope</code>. <a ' +
      'href="/admin/delegation#allowed">The Delegation page</a> is the whole ' +
      'register across every application, with the picture of it; this is ' +
      'the one entry\'s half of it, where the client is settled by the page ' +
      'rather than chosen out of a list.') +

      this.note('<strong>Then it asks for one as an ordinary OAuth ' +
      'scope.</strong> <code>scope=openid https://example.com/write</code> ' +
      'produces an access token audienced to ' +
      '<code>https://example.com/</code> carrying <code>scope: openid ' +
      'write</code> — the base becomes the <code>aud</code> and the name ' +
      'becomes the scope, which is what a resource server wants: check the ' +
      'audience once, then read bare permission names. <strong>In product ' +
      'mode an ungranted permission is refused; in development it REFUSES ' +
      'nothing by default.</strong> With ' +
      '<code>oauth2.delegatedPermissionsEnforced</code> off — which it is ' +
      'unless somebody turned it on, at the foot of the Delegation page — an ' +
      'ungranted permission is honoured exactly as a granted one is and ' +
      'merely recorded as ungranted.') +

      '<h3>What it holds</h3>' +
      heldNav.head +
      '<table><tr><th>Resource &mdash; what is ' +
      'reached</th><th>Permission</th><th>Identifier</th><th>What the access ' +
      'token will say</th><th>Ever asked for?</th><th></th></tr>' +
      (heldRows || '<tr><td colspan="6">It holds none. That is the ordinary ' +
        'state of an application nobody has granted anything to, and it ' +
        'changes nothing about what this service will issue it &mdash; until ' +
        '<code>oauth2.delegatedPermissionsEnforced</code> is set, an ' +
        'ungranted scope is honoured exactly as a granted one is.</td></tr>') +
      '</table>' +
      heldNav.foot +

      '<h3>Grant it a permission</h3>' +
      (options
        ? this.note('The select offers every permission this service knows ' +
          'about EXCEPT the ones this application exposes itself and the ' +
          'ones it already holds. Its own are left out because a token ' +
          'audienced to the application that asked for it is what an ID ' +
          'Token already is, and <code>app_permissions.js</code> refuses ' +
          'that grant however it arrives; the ones it holds are left out ' +
          'because granting a value an entry already carries writes nothing.') +
          '<form method="post" action="/admin/delegation">' + carryBack +
          '<div class="formrow">' +
            '<input type="hidden" name="action" value="grant-permission">' +
            // WHERE TO GO AFTERWARDS. Not a URL — a NAME, checked against a
            // table in permissionsReturnTo() and spent by rebuilding the path
            // from `client`. A redirect target taken out of a request body is
            // an open redirect, and one carrying a newline is a header
            // injection.
            '<input type="hidden" name="from" value="/admin/applications">' +
            '<input type="hidden" name="client" value="' +
            this.esc(identifier) +
          '"><label ' +
            'for="grant-permission">Permission</label><select ' +
            'id="grant-permission" name="permission">' + options +
              '</select>' +
            '<button type="submit">Grant it</button>' +
          '</div></form>'
        : this.note('<strong>There is nothing to offer it.</strong> ' +
          (register.permissions.length
            ? 'Every permission defined here is either one this application ' +
              'exposes itself, one it already holds, or one with no ' +
              'identifier for a client to ask for.'
            : 'No application in this registry exposes an API yet. A ' +
              'permission must be DEFINED before it can be GRANTED, which is ' +
              'the one ordering rule this feature has.') +
          ' <a href="/admin/delegation#allowed">Expose an API and define a ' +
          'permission on the Delegation page</a>, and it appears here.')) +

      '<h3>What it exposes</h3>' +
      this.note('The other half of the same question, and it is READ-ONLY ' +
      'here on purpose. Giving this application a base URI and defining ' +
      'permissions on it are configuration of the RESOURCE, which is one ' +
      'form each on <a href="/admin/delegation#allowed">the Delegation ' +
      'page</a> — where the reader is looking at the register rather than at ' +
      'one entry. Everything below is <code>oauthPermissionBaseUri</code> ' +
      'and <code>oauthPermission</code> on this entry, and the attribute ' +
      'table above shows both raw.') +
      exposedNav.head +
      '<table><tr><th>Permission</th><th>Identifier &mdash; what a client ' +
      'sends</th><th>Held by</th><th>Which applications</th></tr>' +
      (exposedRows || '<tr><td colspan="4">It exposes none. An application ' +
        'exposes an API by being given a base URI and then having ' +
        'permissions defined on it, both on the Delegation page.</td></tr>') +
      '</table>' +
      exposedNav.foot;
  }

  // ---------------------------------------------------------------------------
  // RETURN ADDRESSES A DEVELOPMENT-MODE REQUEST PUT ON THIS ENTRY (2026-09-12).
  //
  // A SAML ACS URL, a SAML 1.1 `shire`, a WS-Federation `wreply` or this
  // service's own learnt callback, written by a sighting and marked OBSERVED on
  // `appReturnAddressObserved`. Product mode refuses a marked address until it
  // is confirmed — `applications.returnAddressesOf()` is the rule — and this is
  // where an operator decides which way each one goes, BEFORE switching a realm
  // to product rather than after its first refused sign-in.
  //
  // **TWO FORMS PER ROW AND NOT ONE WITH TWO BUTTONS.** Two submit buttons in
  // one form would both have to be named `action`, and `form.elements.action`
  // is then a RadioNodeList whose `.value` is empty — which is how
  // `tests/vendored/sts_admin_console.js` finds a form, so the pair would read
  // as controls that reach nothing (the trap `/admin/users/new` met). Each form
  // names its verb in one hidden field, so it does one thing however submitted.
  //
  // Drawn only as markup here; the rows and their paging are
  // `adminViews.applicationDetailJson()`'s, so the table and
  // `GET /admin-api/applications?application=` are one computation.
  // ---------------------------------------------------------------------------
  applicationObservedAddressesSection(req, view, carryBack) {
    const { log, pageParamsOf, mode } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.applicationObservedAddressesSection().");
    const row = view.row;
    const shown = view.observedPaged.shown;
    const nav = this.pageNavPair('/admin/applications', pageParamsOf(req.query),
                                 view.observedPaged.paging);
    const product = !mode.acceptsUnregisteredAddresses();
    const rows = shown.map(function (one) {
      function button(action, label, cls?) {
        log.debug("Entering button().");
        log.debug("Leaving button().");
        return '<form method="post" action="/admin/applications">' + carryBack +
          '<input type="hidden" name="action" value="' + action + '">' +
          '<input type="hidden" name="application" value="' +
          self.esc(row.identifier) + '"><input ' +
          'type="hidden" name="attribute" ' +
          'value="' + self.esc(one.attribute) + '"><input ' +
          'type="hidden" name="value" value="' + self.esc(one.value) + '">' +
          '<button type="submit"' + (cls ? ' class="' + cls + '"' : '') + '>' +
          label +
          '</button></form>';
      }
      return '<tr><td><code>' + self.esc(one.attribute) + '</code></td>' +
        '<td><code>' + self.esc(one.value) + '</code>' +
        (one.held ? '' : '<div class="sub">no longer on the entry; only the ' +
                         'mark is left</div>') + '</td>' +
        '<td>' + (one.trusted
          ? '<span class="state-valid">used &mdash; development believes ' +
            'it</span>'
          : '<span class="state-revoked">refused &mdash; product withholds ' +
            'it</span>') +
        '</td>' +
        '<td>' + button('confirm-address', 'Confirm') +
        button('discard-address', 'Discard', 'danger') + '</td></tr>';
    }).join('');
    const html = '<h2>Return addresses nobody registered</h2>' +
      this.note('Each address below is on this entry because a request NAMED ' +
      'it while the realm was in DEVELOPMENT mode, which believes any ' +
      'address and writes it down. <strong>Product mode refuses every one of ' +
      'them</strong> exactly as it refuses an address that is not on the ' +
      'entry at all' +
      (product ? ' &mdash; and this realm is in product mode now' : '') + '. ' +
      '<strong>Confirm</strong> an address that really is this ' +
      'application\'s: the mark goes and the address stays. ' +
      '<strong>Discard</strong> one that is not: both go. Adding the address ' +
      'with the form below confirms it too. Addresses recorded before this ' +
      'service marked sightings carry no mark and cannot be told apart from ' +
      'registered ones &mdash; review the return-address attributes in the ' +
      'table above by hand before switching a realm to product.') +
      nav.head +
      '<table><tr><th>Attribute</th><th>Address</th><th>In this realm\'s ' +
      'mode</th><th>Decide</th></tr>' +
      (rows || '<tr><td colspan="4">Nothing on this entry is marked as ' +
               'observed.</td></tr>') +
      '</table>' +
      nav.foot;
    log.debug("Leaving AdminConsole.applicationObservedAddressesSection(). " +
              shown.length +
        " " +
        "row(s).");
    return html;
  }

  // ---------------------------------------------------------------------------
  // WHERE A FORM POSTED FROM AN APPLICATION'S PAGE TO ANOTHER HANDLER GOES BACK
  // TO (2026-09-13). The Credentials section below draws `/admin/pki`'s issue,
  // upload and take-off controls, which post to that page's handler — moving a
  // FORM is not moving an ACTION — and that handler asks this for the way back.
  // Exported for `admin-ui/pki_admin.ts`; the destination is REBUILT from the
  // identifier and the list state `back` carries, never echoed, for
  // `permissionsReturnTo()`'s reason one section up.
  // ---------------------------------------------------------------------------
  applicationReturnTo(body, identifier, anchor) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.applicationReturnTo(). identifier=" +
              identifier);
    const listView = this.listViewFromBack('/admin/applications',
                                           body && body.back);
    log.debug("Leaving AdminConsole.applicationReturnTo().");
    return '/admin/applications' +
           queryWith(listView, { application: String(identifier) }) +
           (anchor === '#credentials' ? anchor : '');
  }

  // And a PERSON's page (2026-09-13), whose Credentials section draws the same
  // `/admin/pki` controls for that person's RFC 7523 and RFC 7522 key pairs.
  // The same rule, the same shape: the name arrives in the body and the
  // destination is rebuilt around it, never echoed.
  userReturnTo(body, key, anchor) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.userReturnTo(). key=" + key);
    const listView = this.listViewFromBack('/admin/users', body && body.back);
    log.debug("Leaving AdminConsole.userReturnTo().");
    return '/admin/users' + queryWith(listView, { user: String(key) }) +
           (['#credentials', '#credential-controls'].indexOf(anchor) >= 0
             ? anchor : '');
  }

  certificateCells(summary) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.certificateCells().");
    if (!summary) {
      log.debug("Leaving AdminConsole.certificateCells(). None.");
      return '<span class="state-none">none</span>';
    }
    if (summary.unreadable) {
      log.debug("Leaving AdminConsole.certificateCells(). Unreadable.");
      return '<span class="state-revoked">unreadable: ' +
             this.esc(summary.unreadable) + '</span>';
    }
    log.debug("Leaving AdminConsole.certificateCells().");
    return '<code>' + this.esc(summary.subject) + '</code>' +
      '<div class="sub">issued by <code>' + this.esc(summary.issuer) +
      '</code>' +
      (summary.selfSigned ? ' (self-signed)' : '') + ' &middot; serial <code>' +
      this.esc(summary.serialHex) + '</code> &middot; valid until <code>' +
      this.esc(summary.notAfter) + '</code>' +
      (summary.expired ? ' <span class="state-revoked">expired</span>' : '') +
      ' &middot; ' + this.esc(summary.keyType) + '</div>';
  }

  // ---------------------------------------------------------------------------
  // CREDENTIALS: THE CLIENT SECRET AND THE KEY PAIRS, ON THE APPLICATION'S OWN
  // PAGE (2026-09-13).
  //
  // Every value here was already on the entry and in the attribute table below;
  // what this adds is the READING — which certificate, issued by whom, through
  // what chain, and whether this service holds the private half — and the
  // controls that REPLACE a key pair, which lived only on `/admin/pki` where
  // the application had to be typed into a box.
  //
  // **THREE CONTROLS PER PROFILE, AND NONE OF THEM IS NEW AS AN ACTION BUT
  // ONE.** Issue and Take off post to `/admin/pki`'s existing `issue` and
  // `revoke`; Upload posts `upload-certificate` beside them. All three carry
  // `from`, so that page's handler sends the reader back here
  // (`applicationReturnTo()`), and all three reach `/admin-api/pki/{action}`
  // with no second operation — rule 7 read the way `/admin/delegation`'s grant
  // form reads it.
  //
  // **THE ISSUE CONTROL IS NOT DRAWN IN A REALM WITH NO CERTIFICATE
  // AUTHORITY**, because its only outcome there is a refusal. Upload still is:
  // a certificate from somebody else's authority needs nothing from this
  // realm's.
  //
  // **NO SCRIPT.** The secret is behind a `<details>`, which is how this
  // console folds everything, and the upload is two textareas.
  // ---------------------------------------------------------------------------
  applicationCredentialsSection(req, view, carryBack) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.applicationCredentialsSection().");
    const row = view.row;
    const state = view.credentialsState;
    const id = row.identifier;
    const hidden = function (name, value) {
      log.debug("Entering hidden().");
      log.debug("Leaving hidden().");
      return '<input type="hidden" name="' + name + '" value="' +
             self.esc(value) +
             '">';
    };
    const secret = state.clientSecret;
    const secretHtml = '<h3 id="credentials-secret">Client secret</h3>' +
      this.note('<code>oauthClientSecret</code> is what ' +
      '<code>client_secret_basic</code>, <code>client_secret_post</code> and ' +
      '<code>client_secret_jwt</code> authenticate with. The token endpoint ' +
      'CHECKS it in RFC 9700 mode and in product mode. <strong>Regenerating ' +
      'replaces it at once</strong>: the old secret stops authenticating on ' +
      'the next request, so the client has to be given the new one before it ' +
      'next asks for a token. The new value is minted here &mdash; ' +
      '<code>oauth2.registeredSecretBytes</code> random bytes, as a ' +
      'registration mints one &mdash; and never typed.') +
      '<table><tr><th>Credential</th><th>Held</th></tr>' +
      '<tr><td><code>oauthClientSecret</code>' +
      (secret.authMethod
        ? '<div class="sub">token endpoint auth method <code>' +
          this.esc(secret.authMethod) + '</code></div>' : '') + '</td><td>' +
      (secret.held
        ? '<details class="fold"><summary>Show the client secret</summary>' +
          '<code>' + this.esc(secret.value) + '</code></details>'
        : '<span class="state-none">none</span>') + '</td></tr>' +
      '<tr><td><code>appRegistrationAccessToken</code><div class="sub">RFC ' +
      '7592&rsquo;s credential for reading and changing the registration ' +
      '&mdash; not a client secret</div></td><td>' +
      (secret.registrationAccessTokenHeld
        ? '<details class="fold"><summary>Show the registration access token' +
          '</summary><code>' + this.esc(secret.registrationAccessToken) +
          '</code></details>'
        : '<span class="state-none">none</span>') + '</td></tr></table>' +
      '<form method="post" action="/admin/applications">' + carryBack +
      '<div class="formrow">' + hidden('action', 'regenerate-secret') +
      hidden('application', id) +
      '<button type="submit"' + (secret.held ? ' class="danger"' : '') + '>' +
      (secret.held ? 'Regenerate the client secret'
                   : 'Generate a client secret') + '</button>' +
      '<span class="sub">' + (secret.held
        ? 'The current secret stops working immediately.'
        : 'This application holds none yet.') + '</span></div></form>' +
      // ROTATION WITH AN OVERLAP (#49 P5): the one to use for a client in
      // service — the old secret keeps working while it changes over.
      (secret.held
        ? '<form method="post" action="/admin/applications">' + carryBack +
          '<div class="formrow">' + hidden('action', 'rotate-secret') +
          hidden('application', id) +
          '<button type="submit">Rotate the client secret</button>' +
          '<span class="sub">A new secret; the current one goes on working ' +
          'for oauth2.clientSecretOverlapS so the client can change over.' +
          (secret.previousUntil
            ? ' The secret an earlier rotation replaced works until ' +
              this.esc(new Date(secret.previousUntil).toISOString()) + '.'
            : '') +
          (secret.expiresAt
            ? ' The current secret expires at ' +
              this.esc(new Date(secret.expiresAt * 1000).toISOString()) + '.'
            : '') + '</span></div></form>'
        : '');

    const algOptions = state.ca.keyAlgorithms.map(function (one) {
      return '<option value="' + self.esc(one.id) + '">' + self.esc(one.label) +
             '</option>';
    }).join('');
    const purposeHtml = state.purposes.map(function (p) {
      const names = p.attributes;
      const anchor = 'credentials-' + p.id;
      const chainCells = p.chain.length
        ? '<ol>' + p.chain.map(function (link) {
            return '<li>' + self.certificateCells(link) + '</li>';
          }).join('') + '</ol>'
        : (p.held
          ? '<span class="state-none">none stored</span>' : '&mdash;');
      const managed = p.held
        ? '<table><tr><th>Fact</th><th>Value</th></tr>' +
          '<tr><td>Source<div class="sub"><code>' + self.esc(names.source) +
          '</code></div></td><td><code>' + self.esc(p.source) + '</code>' +
          '<div class="sub">' + (KEY_SOURCE_SENTENCES[p.source] ||
                                 self.esc(p.source)) + '</div></td></tr>' +
          '<tr><td>Certificate<div class="sub"><code>' +
          self.esc(names.certificate) +
          '</code></div></td><td>' + self.certificateCells(p.certificate) +
          '</td></tr>' +
          '<tr><td>Chain<div class="sub"><code>' + self.esc(names.chain) +
          '</code></div></td><td>' + chainCells + '</td></tr>' +
          '<tr><td>Key handle<div class="sub"><code>' + self.esc(names.handle) +
          '</code></div></td><td><code>' + self.esc(p.handle || '—') +
          '</code> <span class="sub">(' + self.esc(p.handleLabel) +
          ')</span></td></tr>' +
          '<tr><td>Private key<div class="sub"><code>' +
          self.esc(names.privateKey) + '</code></div></td><td>' +
          (p.privateKeyHeld
            ? '<span class="state-valid">held by this service</span>' +
              (p.sealedAtRest ? ' &mdash; sealed at rest' : '') +
              '<div class="sub">Collect it from the attribute table below.' +
              '</div>'
            : '<span class="state-none">not held here</span><div class="sub">' +
              'The application keeps its own private key.</div>') +
          '</td></tr><tr><td>Declared issuer<div class="sub"><code>' +
          self.esc(names.issuer) +
          '</code></div></td><td>' + (p.issuers.length
            ? p.issuers.map(function (iss) {
                return '<code>' + self.esc(iss) + '</code>';
              }).join('<br>')
            : '<em>none &mdash; it can authenticate, and cannot present an ' +
              'authorization grant</em>') + '</td></tr></table>'
        : self.note('No key pair is managed for this profile. Issue one from ' +
                    'this realm&rsquo;s certificate authority, or upload a ' +
                    'certificate the application already holds.');

      const issueForm = state.ca.available
        ? '<form method="post" action="/admin/pki">' + carryBack +
          '<div class="formrow">' + hidden('action', 'issue') +
          hidden('from', '/admin/applications') + hidden('identifier', id) +
          hidden('purpose', p.id) +
          '<label for="' + anchor + '-alg">Key algorithm</label>' +
          '<select id="' + anchor + '-alg" name="leafKeyAlg">' +
          '<option value="">(the Issuing CA&rsquo;s: ' +
          self.esc(state.ca.keyAlg) +
          ')</option>' + algOptions + '</select>' +
          '<label for="' + anchor + '-days">Days</label>' +
          '<input type="number" id="' + anchor + '-days" name="days" min="1" ' +
          'value="' + self.esc(String(state.ca.leafLifetimeDays)) + '">' +
          '<button type="submit">' + (p.held
            ? 'Replace it with a key pair from this realm&rsquo;s CA'
            : 'Issue a key pair from this realm&rsquo;s CA') + '</button>' +
          '</div></form>'
        : self.note('This realm has no certificate authority yet, so there ' +
                    'is nothing to issue a key pair from. <a ' +
                    'href="/admin/pki">Build one on the PKI page</a>, or ' +
                    'upload a certificate below.');

      const uploadForm = '<form method="post" action="/admin/pki">' +
                         carryBack +
        hidden('action', 'upload-certificate') +
        hidden('from', '/admin/applications') + hidden('identifier', id) +
        hidden('purpose', p.id) +
        '<div class="formrow"><label for="' + anchor + '-cert">Certificate ' +
        '(PEM)</label><textarea id="' + anchor + '-cert" name="certificate" ' +
        'rows="6" required placeholder="-----BEGIN ' +
        'CERTIFICATE-----"></textarea></div><div class="formrow"><label ' +
        'for="' + anchor + '-chain">Chain ' +
        '(PEM, every intermediate and the root)</label><textarea id="' +
        anchor +
        '-chain" name="chain" rows="6" placeholder="-----BEGIN ' +
        'CERTIFICATE-----"></textarea></div><div class="formrow"><button ' +
        'type="submit">' +
        (p.held ? 'Replace it with this certificate' :
         'Upload the certificate') +
        '</button></div></form>';

      const takeOff = p.held
        ? '<form method="post" action="/admin/pki">' + carryBack +
          '<div class="formrow">' + hidden('action', 'revoke') +
          hidden('from', '/admin/applications') + hidden('identifier', id) +
          hidden('purpose', p.id) +
          '<button type="submit" class="danger">Take this key pair ' +
          'off</button><span class="sub">Not revocation: the certificate ' +
          'stays valid and this service stops accepting what it signs. The ' +
          'other profile&rsquo;s key pair is untouched.</span></div></form>'
        : '';

      const registered = p.registered;
      const registeredRows = p.id === 'jwt'
        ? (registered.keys.length
          ? '<table><tr><th>kid</th><th>Key</th><th>Certificate</th></tr>' +
            registered.keys.map(function (key) {
              return '<tr><td><code>' + self.esc(key.kid || '—') +
                     '</code></td>' +
                '<td><code>' + self.esc(key.kty) + '</code>' +
                (key.alg ? ' <code>' + self.esc(key.alg) + '</code>' : '') +
                '</td><td>' + (key.certificate
                  ? self.certificateCells(key.certificate)
                  : '<span class="state-none">bare key</span>') + '</td></tr>';
            }).join('') + '</table>'
          : '<p class="sub">' + (registered.problem
            ? '<span class="state-revoked">' + self.esc(registered.problem) +
              '</span>'
            : 'None registered.') + '</p>')
        : (registered.certificates.length
          ? '<table><tr><th>Certificate</th></tr>' +
            registered.certificates.map(function (cert) {
              return '<tr><td>' + self.certificateCells(cert) + '</td></tr>';
            }).join('') + '</table>'
          : '<p class="sub">' + (registered.problem
            ? '<span class="state-revoked">' + self.esc(registered.problem) +
              '</span>'
            : 'None registered.') + '</p>');

      return '<h3 id="' + anchor + '">' + self.esc(p.label) + '</h3>' +
        managed +
        '<h4>Regenerate it</h4>' +
        self.note('<strong>Either way REPLACES the key pair on this ' +
        'entry.</strong> Issuing generates a new key pair here and signs it ' +
        'with this realm&rsquo;s Issuing CA; the private key is sealed on ' +
        'the entry. Uploading registers a certificate the application ' +
        'already holds, and this service keeps no private key for it. A ' +
        'certificate from THIS realm&rsquo;s authority may be uploaded ' +
        'alone. <strong>One from any other authority must come with its full ' +
        'chain</strong> &mdash; every intermediate and the self-signed root ' +
        '&mdash; and every link is verified: signatures, names, validity, ' +
        'and that each issuer is a CA allowed to sign. An upload carrying a ' +
        'private key is refused.') +
        issueForm + uploadForm + takeOff +
        '<h4>Keys the application registered itself</h4>' +
        self.note('<code>' + self.esc(registered.attribute) + '</code>, ' +
        'registered by value and verified beside the key pair above &mdash; ' +
        'replacing that pair leaves these alone. Change them with the ' +
        'attribute editor below.') +
        registeredRows;
    }).join('');

    // THE ASSERTION PROFILES ONLY FOR AN OAUTH 2.0 CLIENT (2026-09-13). See
    // `oauthDeclared` in admin-core/admin_views.ts. The markup above is still
    // built and then dropped rather than guarded, so the two branches cannot
    // drift in what a profile section says.
    const heldElsewhere = state.purposes.filter(function (p) {
      return p.held || p.issuers.length;
    });
    const assertionHtml = state.oauthDeclared
      ? purposeHtml
      : '<h3 id="credentials-assertions">Assertion key pairs</h3>' +
        this.note('The RFC 7523 (JWT bearer) and RFC 7522 (SAML 2.0 bearer) ' +
        'sections are shown only for an application declared as ' +
        '<strong>OAuth 2.0</strong> or <strong>OpenID Connect</strong>, ' +
        'because both profiles are used at the token endpoint. Declare one ' +
        'of those families under <em>Protocol families</em> to manage its ' +
        'key pairs here.') +
        (heldElsewhere.length
          ? this.warn('This entry already carries ' +
                      heldElsewhere.map(function (p) {
              return self.esc(p.label);
            }).join(' and ') + ' material, and hiding the section does not ' +
            'take it off: the token endpoint still verifies what it signs. ' +
            'Its attributes are in the table below.', 'Still in effect')
          : '');

    log.debug("Leaving AdminConsole.applicationCredentialsSection(). " +
      "oauthDeclared=" +
              state.oauthDeclared);
    return '<h2 id="credentials">Credentials</h2>' +
      this.note('What this application authenticates and signs with: its ' +
      'client secret, and for each assertion profile the key pair this ' +
      'service manages beside the keys the application registered itself. ' +
      'The two profiles&rsquo; key pairs are separate on purpose &mdash; ' +
      'neither can sign for the other &mdash; so each has its own controls.') +
      secretHtml + assertionHtml +
      (state.oauthDeclared
        ? this.applicationMtlsSection(state.mtls, id, carryBack)
        : '');
  }

  // ---------------------------------------------------------------------------
  // MUTUAL TLS — RFC 8705, ON THE APPLICATION'S OWN PAGE (2026-09-13).
  //
  // Both halves of the RFC read off one model (`applicationMtlsState()` in
  // admin-core/admin_views.ts): how the token endpoint will authenticate this
  // application by certificate, and whether its tokens are bound to one.
  //
  // **THE ISSUE CONTROL IS THE IMPLICIT MAPPING'S REGISTRATION.** A certificate
  // issued here names the application in its subjectAltName and is listed on
  // the application's record, and that is everything `tls_client_auth` needs —
  // so the subject parameters below are for a certificate from somebody ELSE's
  // authority, and are set with the attribute editor like every other
  // attribute. The issue answers with a page of downloads rather than a
  // redirect; see `answerIssuedTlsClientCertificate()`.
  //
  // **NO SCRIPT**: a form per certificate to revoke it, and two password
  // fields.
  // ---------------------------------------------------------------------------
  applicationMtlsSection(state, id, carryBack) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.applicationMtlsSection().");
    const hidden = function (name, value) {
      log.debug("Entering hidden().");
      log.debug("Leaving hidden().");
      return '<input type="hidden" name="' + name + '" value="' +
             self.esc(value) +
             '">';
    };
    const methodSentence = state.certificateMethod
      ? 'This application authenticates with <code>' +
        this.esc(state.authMethod) +
        '</code>, and is held to it <strong>in every mode</strong>: a token ' +
        'request from it that does not authenticate by certificate is ' +
        'refused <code>invalid_client</code>.'
      : 'This application&rsquo;s <code>oauthTokenEndpointAuthMethod</code> ' +
        'is ' +
        (state.authMethod ? '<code>' + this.esc(state.authMethod) + '</code>'
                          : 'not set') + ', so nothing here authenticates it ' +
        'yet. Set it to <code>tls_client_auth</code> (or ' +
        '<code>self_signed_tls_client_auth</code>) with the attribute editor ' +
        'below to use a certificate in place of a secret.';
    const rows = state.certificates.length
      ? '<table><tr><th>Certificate</th><th>State</th><th></th></tr>' +
        state.certificates.map(function (cert) {
          const revokeForm = cert.state === 'valid'
            ? '<form method="post" action="/admin/applications">' + carryBack +
              hidden('action', 'revoke-tls-client-certificate') +
              hidden('application', id) + hidden('serialHex', cert.serialHex) +
              '<div class="formrow"><select name="reason" ' +
              'aria-label="Reason">' +
              state.revocationReasons.map(function (reason) {
                return '<option value="' + self.esc(reason) + '">' +
                       self.esc(reason) +
                       '</option>';
              }).join('') + '</select><button type="submit" class="danger">' +
              'Revoke</button></div></form>'
            : '';
          return '<tr><td><code>' + self.esc(cert.subject) + '</code><div ' +
            'class="sub">' +
            (cert.label ? self.esc(cert.label) + ' &middot; ' : '') +
            'serial <code>' + self.esc(cert.serialHex) + '</code> &middot; ' +
            self.esc(cert.keyAlg) + ' &middot; valid until <code>' +
            self.esc(cert.notAfter) + '</code><br>thumbprint <code>' +
            self.esc(cert.thumbprint) + '</code></div></td><td><span ' +
              'class="state-' +
            (cert.state === 'valid' ? 'valid' : 'revoked') + '">' +
            self.esc(cert.state) + '</span>' + (cert.reason
              ? '<div class="sub">' + self.esc(cert.reason) + '</div>' : '') +
            '</td><td>' + revokeForm + '</td></tr>';
        }).join('') + '</table>'
      : '<p class="sub">This realm has issued this application no TLS client ' +
        'certificate.</p>';
    const issueForm = state.caAvailable
      ? (state.active >= state.max
        ? this.note('This application holds ' + state.active + ' valid TLS ' +
                    'client certificates, which is <code>' +
                    'pki.applicationTlsClientCertificateMax</code>. Revoke ' +
                    'one to issue another.')
        : '<form method="post" action="/admin/applications">' + carryBack +
          hidden('action', 'issue-tls-client-certificate') +
          hidden('application', id) +
          '<div class="formrow"><label for="mtls-label">Name it</label>' +
          '<input id="mtls-label" name="label" maxlength="40" ' +
          'placeholder="instance 1"><label for="mtls-alg">Key</label>' +
          '<select id="mtls-alg" name="keyAlg">' +
          state.keyAlgorithms.map(function (alg) {
            return '<option value="' + self.esc(alg) + '"' +
                   (alg === state.defaultKeyAlg ? ' selected' : '') + '>' +
                   self.esc(alg) + '</option>';
          }).join('') + '</select></div>' +
          '<div class="formrow"><label for="mtls-password">File password' +
          '</label><input type="password" id="mtls-password" name="password" ' +
          'minlength="' + state.passwordMin + '" required autocomplete=' +
          '"new-password"><label for="mtls-confirm">Again</label><input ' +
          'type="password" id="mtls-confirm" name="confirm" minlength="' +
          state.passwordMin + '" required autocomplete="new-password"></div>' +
          '<div class="formrow"><button type="submit">Issue a TLS client ' +
          'certificate from this realm&rsquo;s CA</button><span class="sub">' +
          'The private key is shown once, as downloads, and is not kept.' +
          '</span></div></form>')
      : this.note('This realm has no certificate authority yet, so there is ' +
                  'nothing to issue a TLS client certificate from. <a ' +
                  'href="/admin/pki">Build one on the PKI page</a>.');
    const subjectRows = '<table><tr><th>Parameter</th><th>Attribute</th>' +
      '<th>Registered</th></tr>' + state.subjects.map(function (subject) {
        return '<tr><td><code>' + self.esc(subject.member) + '</code><div ' +
          'class="sub">' + self.esc(subject.label) + '</div></td><td><code>' +
          self.esc(subject.attribute) + '</code></td><td>' + (subject.value
            ? '<code>' + self.esc(subject.value) + '</code>'
            : '<span class="state-none">none</span>') + '</td></tr>';
      }).join('') + '</table>';
    const html = '<h3 id="credentials-tls-client">Mutual TLS (RFC 8705)</h3>' +
      this.note(methodSentence) +
      '<h4>TLS client certificates this realm issued it</h4>' +
      this.note('The <strong>implicit</strong> mapping for ' +
      '<code>tls_client_auth</code>: a certificate from this realm&rsquo;s ' +
      'TLS client Issuing CA naming <code>' + this.esc(state.implicitName) +
      '</code>, still listed here, authenticates this application with ' +
      'nothing registered. A certificate this service issued to anybody else ' +
      'never authenticates it, whatever subject is registered below.') +
      rows + issueForm +
      '<h4>A certificate from another authority</h4>' +
      this.note('The <strong>explicit</strong> mapping, RFC 8705 section ' +
      '2.1.2: register <strong>one</strong> of these five, and a certificate ' +
      'whose chain verified against the client truststore (<a ' +
      'href="/tls/trust">/tls/trust</a>) and carries it authenticates this ' +
      'application. A DN is compared as a name, not a string. ' +
      '<code>self_signed_tls_client_auth</code> instead matches ' +
      '<code>oauthTlsClientCertificateThumbprint</code>' +
      (state.selfSignedThumbprint ? ' (<code>' +
        this.esc(state.selfSignedThumbprint) + '</code>)' : ' (none)') + ' ' +
      'or a key&rsquo;s <code>x5c</code> in its registered <code>jwks</code>' +
      '.') +
      subjectRows +
      '<h4>Certificate-bound access tokens</h4>' +
      this.note((state.bindingAvailable
        ? 'The main port is TLS, so every access token and refresh token ' +
          'issued on a connection that presented a client certificate ' +
          'carries <code>cnf["x5t#S256"]</code>, and a resource server here ' +
          'refuses it on a connection without that certificate. '
        : 'The main port is <strong>not</strong> TLS ' +
          '(<code>global.https</code>), so no token can be bound. ') +
      '<code>' + this.esc(state.boundTokensAttribute) + '</code> is <code>' +
      (state.boundTokens ? 'TRUE' : 'FALSE') + '</code>' + (state.boundTokens
        ? ': this application declared bound tokens, so a token request from ' +
          'it with no client certificate is refused in every mode.'
        : ': a token request with no certificate gets an unbound token.'));
    log.debug("Leaving AdminConsole.applicationMtlsSection(). " +
              state.certificates.length + " certificate(s).");
    return html;
  }

  // ---------------------------------------------------------------------------
  // SOFTWARE STATEMENTS (RFC 7591 section 2.3), ON THE APPLICATION'S OWN PAGE
  // (2026-09-13).
  //
  // Three halves, each drawn only where it says something: the issuers this
  // application vouches for as a PUBLISHER, the statement this realm ISSUED for
  // it with the one control on the section, and — for a client that registered
  // with a statement — how that statement let it in. The declaration is an
  // ordinary attribute, set with the attribute editor further down; the issue
  // is `issue-software-statement` on `/admin/applications`, which `POST
  // /admin-api/applications/issue-software-statement` mirrors.
  //
  // **NO SCRIPT.** The statement is behind a `<details>`, and the metadata a
  // new one fixes is a textarea of JSON.
  // ---------------------------------------------------------------------------
  applicationSoftwareStatementSection(req, view, carryBack) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.applicationSoftwareStatementSection().");
    const row = view.row;
    const state = view.softwareStatementState;
    const settings = state.settings;
    const hidden = function (name, value) {
      log.debug("Entering hidden().");
      log.debug("Leaving hidden().");
      return '<input type="hidden" name="' + name + '" value="' +
             self.esc(value) +
             '">';
    };

    const publisherHtml = '<h3 id="software-statements-publisher">As a ' +
      'publisher</h3>' +
      this.note('A statement whose <code>iss</code> is one of these is ' +
      'TRUSTED at <code>POST /oauth2/register</code>, verified against this ' +
      'application&rsquo;s <code>jwks</code> or its RFC 7523 key pair from ' +
      '<em>Credentials</em> above. Add or remove an issuer with ' +
      '<code>oauthSoftwareStatementIssuer</code> in the attribute editor ' +
      'below. A statement this realm issued needs no declaration.') +
      '<table><tr><th>Declared issuer</th><th>Keys a statement verifies ' +
      'under</th></tr>' +
      (state.issuers.length
        ? state.issuers.map(function (iss) {
            return '<tr><td><code>' + self.esc(iss) + '</code></td><td>' +
              (state.usableKeys
                ? '<span class="state-valid">' + state.usableKeys + ' key' +
                  (state.usableKeys === 1 ? '' : 's') + '</span>'
                : '<span class="state-revoked">none &mdash; every statement ' +
                  'from this issuer is refused</span>') + '</td></tr>';
          }).join('')
        : '<tr><td colspan="2"><span class="state-none">none declared' +
          '</span></td></tr>') + '</table>';

    const issued = state.issued;
    const issuedRows = issued
      ? (issued.readable
        ? '<table><tr><th>Fact</th><th>Value</th></tr>' +
          '<tr><td>Issuer</td><td><code>' + this.esc(issued.issuer) +
          '</code></td></tr>' +
          '<tr><td>Issued</td><td><code>' +
          this.esc(this.whenText(issued.issuedAt)) +
          '</code></td></tr>' +
          '<tr><td>Expires</td><td><code>' +
          this.esc(issued.expiresAt ? this.whenText(issued.expiresAt) :
                   'never') +
          '</code></td></tr>' +
          '<tr><td>Verifies now</td><td>' + (issued.verifies
            ? '<span class="state-valid">yes, under this realm&rsquo;s key' +
              '</span>'
            : '<span class="state-revoked">no</span><div class="sub">' +
              this.esc(issued.why) + ' &mdash; issue a new one.</div>') +
          '</td></tr>' +
          '<tr><td>Fixes</td><td><code>' +
          this.esc(JSON.stringify(issued.metadata)) + '</code></td></tr>' +
          '<tr><td>Statement</td><td><details class="fold"><summary>Show the ' +
          'statement</summary><code>' + this.esc(state.issuedToken) +
          '</code></details></td></tr></table>'
        : this.warn(this.esc(issued.why), 'Unreadable'))
      : '<p class="sub"><span class="state-none">This realm has issued no ' +
        'statement for this application.</span></p>';

    const issueForm = '<form method="post" action="/admin/applications">' +
      carryBack + hidden('action', 'issue-software-statement') +
      hidden('application', row.identifier) +
      '<div class="formrow"><label for="software-statement-metadata">Client ' +
      'metadata it fixes (JSON)</label><textarea ' +
      'id="software-statement-metadata" name="metadata" rows="6" ' +
      'placeholder="{&quot;redirect_uris&quot;: ' +
      '[&quot;https://app.example/cb&quot;], &quot;grant_types&quot;: ' +
      '[&quot;authorization_code&quot;]}"></textarea></div>' +
      '<div class="formrow"><label ' +
      'for="software-statement-lifetime">Lifetime (seconds, 0 for ' +
      'none)</label><input id="software-statement-lifetime" ' +
      'name="lifetimeSeconds" type="number" min="0" placeholder="' +
      this.esc(String(settings.lifetimeSeconds)) + '"></div>' +
      '<div class="formrow"><button type="submit">' +
      (issued ? 'Issue a new statement' : 'Issue a statement') + '</button>' +
      '<span class="sub">' + (issued
        ? 'Replaces the one shown. The earlier statement still verifies ' +
          'until it expires.'
        : 'Signed with this realm&rsquo;s key.') + '</span></div></form>';

    const issuedHtml = '<h3 id="software-statements-issued">Issued by this ' +
      'realm</h3>' +
      this.note('Hand the statement to whoever ships the software. A client ' +
      'that presents it is registered with the members it fixes, and they ' +
      'take precedence over the registration&rsquo;s own JSON; ' +
      '<strong>whatever it does not fix, the registering client ' +
      'chooses</strong> &mdash; so fix <code>grant_types</code> and ' +
      '<code>redirect_uris</code> when those matter. ' +
      '<code>software_id</code> defaults to this application&rsquo;s ' +
      'identifier. The <code>iss</code> is the issuer published at the ' +
      'address this page was reached on. It is not a secret, and it stops ' +
      'verifying when the realm&rsquo;s signing key is replaced &mdash; in ' +
      'development mode, every restart.') +
      issuedRows + issueForm;

    const facts = state.registeredWith;
    const registeredHtml = facts
      ? '<h3 id="software-statements-registered">How it registered</h3>' +
        '<table><tr><th>Fact</th><th>Value</th></tr>' +
        '<tr><td>Statement issuer<div class="sub"><code>' +
        'appSoftwareStatementIssuer</code></div></td><td><code>' +
        this.esc(facts.issuer) + '</code></td></tr>' +
        '<tr><td>Trusted<div class="sub"><code>appSoftwareStatementTrusted' +
        '</code></div></td><td>' + (facts.trusted
          ? '<span class="state-valid">yes</span>'
          : '<span class="state-revoked">no &mdash; accepted unverified' +
            '</span>') + '</td></tr>' +
        '<tr><td>Through<div class="sub"><code>appSoftwareStatementPublisher' +
        '</code></div></td><td>' + (facts.publisher
          ? '<a href="/admin/applications?application=' +
            encodeURIComponent(facts.publisher) + '"><code>' +
            this.esc(facts.publisher) + '</code></a>'
          : '&mdash;') + '</td></tr></table>'
      : '';

    // AN OAUTH 2.0 CLIENT'S SECTION, for the Credentials section's reason: a
    // statement is presented at POST /oauth2/register, so an application not
    // declared for OAuth 2.0 or OpenID Connect gets a sentence rather than
    // controls — unless it already carries something, which stays in effect.
    if (!view.credentialsState.oauthDeclared && !state.issuers.length &&
        !issued && !facts) {
      log.debug("Leaving AdminConsole.applicationSoftwareStatementSection(). " +
                "Not an OAuth client.");
      return '<h2 id="software-statements">Software statements</h2>' +
        this.note('RFC 7591 software statements are presented at the OAuth ' +
        '2.0 registration endpoint, so this section is drawn for an ' +
        'application declared as <strong>OAuth 2.0</strong> or ' +
        '<strong>OpenID Connect</strong>. Declare one of those families ' +
        'under <em>Protocol families</em> to issue a statement for it or ' +
        'name the issuers it vouches for.');
    }
    log.debug("Leaving AdminConsole.applicationSoftwareStatementSection().");
    return '<h2 id="software-statements">Software statements</h2>' +
      this.note('RFC 7591 section 2.3: a signed JWT of client metadata, ' +
      'presented at registration. In this realm a statement from an ' +
      'undeclared issuer is ' +
      (settings.requireTrustedIssuer ? '<strong>refused</strong>'
        : '<strong>accepted unverified</strong>') + ', a trusted one ' +
      (settings.opensRegistration ? '<strong>opens</strong>' :
       'does not open') +
      ' a registration endpoint closed to everybody else, and a registration ' +
      (settings.required ? '<strong>must</strong>' : 'need not') + ' carry ' +
      'one. Those are <code>oauth2.softwareStatement*</code> on ' +
      '<a href="/admin/oauth2">/admin/oauth2</a>.') +
      publisherHtml + issuedHtml + registeredHtml;
  }

  // The drill-down. Its one list is the ATTRIBUTE table, which is paged under a
  // name of its own (`attributesPage`) rather than the bare `page` — the
  // convention pagingOf()'s header describes for a view that holds more than
  // the list views do, and the shape to grow into when this page gains a second
  // list.
  applicationDetailPage(req, identifier) {
    const { log, adminViews, queryWith, pageParamsOf } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.applicationDetailPage(). identifier=" +
              identifier);
    const view = adminViews.applicationDetailJson(req, identifier);
    const row = view.row;
    const carryBack = '<input type="hidden" name="back" value="' +
      this.esc(queryWith(this.listViewOf('/admin/applications', req.query),
                         {})) + '">';
    if (!row) {
      log.debug("Leaving AdminConsole.applicationDetailPage(). No such " +
                "application.");
      return {
        missing: true,
        inner: this.messagesOf(req) +
          '<p class="warn">No application called <code>' +
          this.esc(identifier) +
          '</code> is recorded here. That is not the same as one this ' +
          'service has refused: an entry appears the first time an ' +
          'identifier is ACCEPTED, so a client whose every request was ' +
          'turned away has none.</p>' + APPLICATIONS_LINKS,
        json: view.json
      };
    }
    // EVERY attribute the entry carries, operational ones and entryDN included.
    // This used to be the schema half minus the twelve names applications.js
    // reads into named members, under a heading that said "every attribute the
    // entry carries" — so objectClass, cn, appIdentifier, both timestamps and
    // the DN itself were all missing from the one table on this service whose
    // whole job is to be complete.
    const attributeRows = view.attributeRows;
    const paged = view.paged;
    const paging = view.paging;
    const nav = this.pageNavPair('/admin/applications', pageParamsOf(req.query),
                                 paging);

    const attrHtml = paged.shown.map(function (attr) {
      // What each attribute MEANS rather than only what it holds. The
      // registry's own table is the first answer and is the same table the
      // entry was written from — a second description here would be the one
      // that went stale — and applicationAttributeNote() carries the other
      // three cases.
      const note = self.applicationAttributeNote(attr.name, attr.operational);
      return '<tr><td><code>' + self.esc(attr.name) + '</code>' +
        (note.sensitive ? ' <span class="state-revoked">credential</span>' :
         '') +
        (attr.sealedAtRest
          ? ' <span class="state-valid" title="The entry holds this ' +
            'encrypted under the key-encryption key — the same one this ' +
            'service\'s signing keys and the certificate authority are ' +
            'sealed with. An ldapsearch, an ldif file, a database row or a ' +
            'backup shows the ciphertext; this page opened it.">sealed at ' +
            'rest</span>'
          : '') +
        (attr.operational
          ? ' <span class="state-none" title="An operational attribute. A ' +
            'search returns it only when it is asked for by name (RFC 4511 ' +
            'section 4.5.1.8) — this dump shows it ' +
            'always.">(operational)</span>'
          : '') +
        '</td><td>' + attr.values.map(function (v) {
          return '<code>' + self.esc(v) + '</code>';
        }).join('<br>') + '</td><td class="sub">' + self.esc(note.text) +
          '</td></tr>';
    }).join('');

    const inner = this.messagesOf(req) +
      '<h2><code>' + this.esc(row.identifier) + '</code></h2>' +
      '<div class="tiles">' +
      this.tile(row.authentications, 'Authentications') +
      this.tile(row.sessions, 'Sessions') +
      this.tile(row.users, 'Users') +
      this.tile(row.registered || row.registeredBy ? 'yes' : 'no',
                'Registered') +
      '</div>' +
      '<table><tr><th>Thing</th><th>Value</th></tr>' +
      // FIRST, because it is the thing this page could not previously answer:
      // where in the tree this application lives. The registry is the
      // directory, so the DN is what an ldapsearch or an ldapmodify is aimed
      // at, and a console that showed only the cn made an operator reconstruct
      // it.
      '<tr><td>Distinguished name</td><td>' + (row.dn
        ? '<code>' + this.esc(row.dn) + '</code>' +
          (row.identifier === row.dnLabel ? '' :
            '<div class="sub">The RDN is a digest &mdash; <code>' +
            this.esc(row.identifier) +
            '</code> is longer than 64 characters, which is not a readable ' +
            'RDN. <code>appIdentifier</code> is the identity, not the ' +
            '<code>cn</code>.</div>')
        : '<span class="state-none">no directory is loaded in this process, ' +
          'so there is no entry and no registry</span>') + '</td></tr>' +
      '<tr><td>Name</td><td>' + this.esc(row.name) + '</td></tr>' +
      '<tr><td>Kind</td><td>' + this.applicationKindCells(row) +
      '</td></tr>' +
      '<tr><td>Protocols</td><td>' + this.applicationProtocolCell(row) +
      '</td></tr>' +
      '<tr><td>Registered</td><td>' + this.applicationRegisteredCell(row) +
      '</td></tr><tr><td>First ' +
      'seen</td><td><code>' + this.esc(row.firstSeen) +
      '</code></td></tr><tr><td>Last ' +
      'seen</td><td><code>' + this.esc(row.lastSeen) +
      '</code></td></tr><tr><td>How ' +
      'it got here</td><td>' + (row.descriptions.length
        ? row.descriptions.map(function (d) { return self.esc(d); })
          .join('<br>')
        : '<span class="state-none">nothing recorded</span>') + '</td></tr>' +
      '</table>' +
      this.protocolFamilySection(row) +
      // WHAT BROWSER PAGES MAY CALL IT, beside what protocols it is for: both
      // are what this application is allowed to do, and the CORS list is the
      // one of the two a person edits value by value.
      this.applicationCorsSection(row, carryBack) +
      // THE CREDENTIALS, above the raw entry because they are what a reader
      // most often opens this page to find — see the section's header.
      this.applicationCredentialsSection(req, view, carryBack) +
      this.applicationSoftwareStatementSection(req, view, carryBack) +
      '<h2>Its directory entry</h2><p class="sub">Every attribute the entry ' +
      'carries &mdash; the operational ones and <code>entryDN</code> ' +
      'included, which a SEARCH would return only when asked for by name ' +
      '(RFC 4511 section 4.5.1.8) &mdash; with what each one is. This IS the ' +
      'entry &mdash; the registry is the <code>ou=applications</code> ' +
      'container and nothing caches it &mdash; so an <code>ldapmodify</code> ' +
      'shows here on the next refresh, and changes what RFC 9700 mode ' +
      'enforces at the same moment.</p>' +
      (row.dn
        ? '<p class="sub"><code>' + this.esc(row.dn) + '</code>' +
          (row.createdAt ?
           ' &middot; created <code>' + this.esc(row.createdAt) +
           '</code>' : '') +
          (row.modifiedAt ?
           ' &middot; last written <code>' + this.esc(row.modifiedAt) +
                            '</code>' : '') +
          (row.origin ?
           ' &middot; written by <code>' + this.esc(row.origin) +
           '</code>' : '') +
          '</p>'
        : '') +
      nav.head +
      '<table><tr><th>Attribute</th><th>Value</th><th>What it is</th></tr>' +
      // Reachable only where no directory is loaded in this process. Every real
      // entry carries objectClass, cn, appIdentifier and its two timestamps at
      // the least, so "no attributes" is now a statement about the STORE rather
      // than about this application — which is what it says.
      (attrHtml || '<tr><td colspan="3">No directory is loaded in this ' +
       'process, so there is no <code>ou=applications</code> container and ' +
       'no entry to show. This module keeps no store of its own on ' +
       'purpose.</td></tr>') +
      '</table>' +
      nav.foot +

      this.applicationObservedAddressesSection(req, view, carryBack) +

      '<h2>Change what it is allowed to do</h2>' +
      this.note('These write the same entry an <code>ldapmodify</code> ' +
      'writes, through the same functions &mdash; the console is a set of ' +
      'controls in front of this registry and not a second copy of it. A ' +
      'change here is what RFC 9700 mode enforces on the very next request: ' +
      'add to <code>oauthRedirectUri</code> and that URI is accepted by ' +
      'exact match, set <code>oauthTokenEndpointAuthMethod</code> to ' +
      '<code>none</code> and the client becomes public, so PKCE is required ' +
      'of it and its secret stops being checked.') +
      '<form method="post" action="/admin/applications">' + carryBack +
      '<div ' +
      'class="formrow"><input type="hidden" name="action" value="set"><input ' +
      'type="hidden" name="application" value="' + this.esc(row.identifier) +
      '"><label for="setattr">Set</label><select id="setattr" ' +
      'name="attribute">' + this.editableOptions('set', '', row) +
      '</select><label ' +
      'for="setval">to</label><input type="text" id="setval" name="value" ' +
      'size="34" placeholder="empty clears it"><button ' +
      'type="submit">Set</button></div></form><form method="post" ' +
      'action="/admin/applications">' + carryBack + '<div ' +
      'class="formrow"><input type="hidden" name="action" value="add"><input ' +
      'type="hidden" name="application" value="' + this.esc(row.identifier) +
      '"><label for="addattr">Add to</label><select id="addattr" ' +
      'name="attribute">' +
      this.editableOptions('multi', 'oauthRedirectUri', row) +
      '</select><label for="addval">the value</label><input type="text" ' +
      'id="addval" name="value" size="34" required><button ' +
      'type="submit">Add</button></div></form><form method="post" ' +
      'action="/admin/applications">' + carryBack +
      '<div ' +
      'class="formrow"><input type="hidden" name="action" ' +
      'value="remove"><input type="hidden" name="application" ' +
      'value="' + this.esc(row.identifier) + '">' +
      '<label for="remattr">Remove from</label>' +
      // NO `row` HERE, AND THAT IS THE RULE READ EXACTLY: the family scope
      // refuses a SET and an ADD and never a REMOVE, for updateApplication()'s
      // reason — a value can arrive by `ldapmodify` or be left behind when a
      // family is untimed from the entry, and a console that would not offer to
      // remove it would be the one door that could tidy it up, shut. So this
      // select keeps offering everything editable. Nothing family-scoped is
      // `multi` today; the asymmetry is here so that the first one that is
      // behaves correctly.
      '<select id="remattr" name="attribute">' +
      this.editableOptions('multi', 'oauthRedirectUri') +
      '</select>' +
      '<label for="remval">the value</label>' +
      '<input type="text" id="remval" name="value" size="34" required>' +
      '<button type="submit">Remove</button>' +
      '</div></form>' +
      this.note('<strong>What these will not change, and why.</strong> The ' +
      'counters, the first and last sighting, the kinds and the protocols ' +
      'are DERIVED &mdash; they are what happened rather than what this ' +
      'application may do &mdash; and a form that could rewrite them would ' +
      'make this page lie about the service\'s own behaviour, in a way ' +
      'indistinguishable from the recording being broken. ' +
      '<code>ldapmodify</code> still reaches every one of them: an operator ' +
      'with an LDAP client is doing something deliberate, and refusing them ' +
      'HERE is the difference between offering an operation and merely not ' +
      'preventing it. <code>appRegistrationJson</code> is not offered either ' +
      '&mdash; edit the attributes beside it instead, which is what the ' +
      'registration is rebuilt from.') +

      // AFTER the generic attribute editor and before the metadata refresh. The
      // editor can already write `oauthDelegatedPermission` by hand — it is an
      // ordinary multi-valued attribute in the EDITABLE table — so this section
      // is a second DOOR onto it and not a second place it lives, which is the
      // one-store rule /admin/token-lifetimes' header argues. What it adds is
      // the resolution: the raw attribute is a bare identifier, and every
      // column beside it here (which application exposes it, what the token
      // will say, whether it has ever been asked for) is something only the
      // register can answer.
      this.applicationPermissionsSection(req, row, carryBack) +


      // THE METADATA REFRESH, and the only control on this page that reaches
      // off this machine. It is a button rather than something issuing does,
      // for the reason sp_metadata.ts argues at length: an assertion that had
      // to wait on somebody else's web server would make every sign-in as
      // reliable as that server. It is drawn only for an application that names
      // a URL — a button whose only possible outcome is "there is no URL" is
      // not a control.
      (this.firstFieldValue(row, 'samlSpMetadataUrl')
        ? '<h2>Service provider metadata</h2>' +
          this.note('Fetches <code>' +
                    this.esc(this.firstFieldValue(row, 'samlSpMetadataUrl')) +
          '</code> and CONSUMES it: the document on ' +
          '<code>samlSpMetadata</code>, its AssertionConsumerService and ' +
          'SingleLogoutService endpoints as REGISTERED return addresses, its ' +
          'signing certificates as what its requests are verified against, ' +
          'its <code>use="encryption"</code> certificate on ' +
          '<code>samlEncryptionCertificate</code>, and its NameIDFormats, ' +
          'AuthnRequestsSigned and WantAssertionsSigned. A fetch that fails ' +
          'changes NOTHING, so whatever is registered stays in force. <a ' +
          'href="/admin/saml2?sp=' + encodeURIComponent(row.identifier) +
          '">The SAML 2.0 page</a> shows what was consumed and takes an ' +
          'uploaded document.') +
          '<form method="post" action="/admin/applications">' + carryBack +
          '<div class="formrow">' +
          '<input type="hidden" name="action" value="refresh-metadata">' +
          '<input type="hidden" name="application" value="' +
          this.esc(row.identifier) + '"><button ' +
          'type="submit">Refresh the metadata</button><span class="sub">' +
          (this.firstFieldValue(row, 'samlSpMetadataConsumedAt')
            ? 'Metadata was last consumed ' +
              this.esc(this.firstFieldValue(row, 'samlSpMetadataConsumedAt')) +
              '; this replaces what it registered.'
            : 'No metadata has been consumed yet.') +
          ' This is one of two places in this service that dials anything at ' +
          'all.</span></div></form>'
        : '') +

      '<h2>Take it out of the registry</h2>' +
      (row.registered
        ? '<form method="post" action="/admin/applications">' + carryBack +
          '<div class="formrow"><input type="hidden" name="action" ' +
          'value="revoke-registration"><input type="hidden" ' +
          'name="application" value="' + this.esc(row.identifier) +
          '"><button type="submit">Revoke the RFC 7591 ' +
          'registration</button><span class="sub">The entry and its history ' +
          'stay; the client_secret, the registration access token and the ' +
          'registration itself go. Afterwards RFC 9700 mode treats it as an ' +
          'unregistered, public client.</span></div></form>'
        : this.note('It has no RFC 7591 registration to revoke &mdash; it is ' +
          'an identifier this service has seen rather than a client that ' +
          'registered, which RFC 9700 mode already treats as public.')) +
      '<form method="post" action="/admin/applications">' + carryBack +
      '<div ' +
      'class="formrow"><input type="hidden" name="action" ' +
      'value="forget"><input type="hidden" name="application" ' +
      'value="' + this.esc(row.identifier) + '"><button ' +
      'type="submit" class="danger">Delete this entry</button><span ' +
      'class="sub">The only control here that LOSES a fact: the entry goes ' +
      'and takes its ' +
      row.authentications + ' recorded authentication(s) with it. It will ' +
      'reappear, empty, the next time this identifier is accepted by a ' +
      'protocol.</span></div></form>' +
      APPLICATIONS_CAVEAT +
      '<p class="sub"><a href="' +
      this.esc('/admin/applications' +
               queryWith(this.listViewOf('/admin/applications', req.query),
                         {})) +
      '">back to the list</a> &middot; <a ' +
      'href="/admin/ldap/applications">the registry as the directory sees ' +
      'it</a></p>';

    // The same two lists the section above drew, for the reply. One pure
    // function, called twice with the same arguments — see its header.
    const permissionState = view.permissionState;
    log.debug("Leaving AdminConsole.applicationDetailPage(). " +
              paged.shown.length +
              " attribute row(s), " + permissionState.held.length +
              " delegated permission(s) held.");
    return { inner: inner, json: view.json };
  }

  applicationsView(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.applicationsView().");
    const wanted = String(req.query.application || '').trim();
    if (wanted) {
      const detail = this.applicationDetailPage(req, wanted);
      log.debug("Leaving AdminConsole.applicationsView(). The drill-down.");
      return { missing: !!detail.missing, json: detail.json,
               inner: detail.inner,
               title: 'Application ' + wanted,
               up: this.upTo('/admin/applications', wanted,
                             this.listViewOf('/admin/applications',
                                             req.query)) };
    }
    const list = this.applicationsListPage(req);
    log.debug("Leaving AdminConsole.applicationsView(). The list.");
    return { json: list.json, inner: list.inner, title: 'Applications' };
  }

  // ---------------------------------------------------------------------------
  // GET /admin/applications/new — CREATE ONE, ON A PAGE OF ITS OWN.
  //
  // The Applications list has carried an *Add an application* form since it
  // grew its six actions, and this page does not replace it or duplicate it:
  // BOTH POST TO `/admin/applications` WITH `action=create`, both reach
  // `applications.createApplication()`, and there is exactly one store behind
  // them. That is the same arrangement four doors onto one group membership
  // have (rule 8a) and the same one `/admin/token-lifetimes` argues for four
  // settings that `/admin/config` can already write: **two forms over one
  // function are two doors; what breaks the one-store rule is a second PLACE
  // THE VALUE LIVES**, and there is none here.
  //
  // So the question this page has to answer is the one that file's header asks
  // — what is different about the READER'S TASK — and there are three answers:
  //
  //   * **THE PROTOCOL FAMILIES.** Sixteen checkboxes with a sentence each do
  //     not fit in a `.formrow` at the foot of a table of every application
  //     this service has ever seen; they are a table of their own, and the row
  //     on the list page would have had to become a link to somewhere anyway.
  //   * **THE IDENTIFIERS AND THE REDIRECT URIS.** Two dozen more fields (as of
  //     2026-09-16), which is the 2026-08-25 change and the reason this page is
  //     now the only place a whole application can be configured in one post.
  //     Before it, a create took an identifier and a name and nothing else:
  //     every attribute that actually CONFIGURES the application — the
  //     client_id RFC 9700 mode reads, the redirect URIs it matches against,
  //     the entityID, the wtrealm — had to be added afterwards, one `add` at a
  //     time, from a different page.
  //   * **Creating one is a DIFFERENT ERRAND from reading the list.** Somebody
  //     configuring a relying party before it connects has not come to look at
  //     what has already connected, and on the list page that form is below the
  //     paging — so on a service with forty applications it is off the bottom
  //     of the screen, and the one control a person came for is the one they
  //     have to hunt for.
  //
  // **THE KIND SELECT IS GONE, AND ITS ABSENCE IS THE POINT RATHER THAN A
  // TIDY-UP.** This page used to ask for a KIND as well as for the families,
  // and they were two vocabularies for one question that did not line up: eight
  // kinds against fourteen families, five of those families having no kind at
  // all, and a reader made to choose in both. Worse, the two are on opposite
  // sides of the line `applications.js`'s EDITABLE header draws — a family is
  // DECLARED and a kind is DERIVED, written by `seen()` when a protocol
  // actually recognises the identifier — so the select let a form assert a
  // sighting that had not happened, and `view()`'s `recordedProtocols` had to
  // carry a paragraph saying it was not evidence of traffic. The families won
  // because they are what an operator is actually declaring.
  // `createApplication()` still TAKES a kind, because `saml2Action()` and
  // `saml11Action()` pass one when they register a service provider from its
  // own page, and that is a protocol module's statement rather than a person's
  // guess in a select.
  //
  // The inline form STAYS, for the same reason the sign-out page did not remove
  // `/oauth2/logout`: it is where somebody already looking at the list will
  // reach for, and it is one line of markup pointing at the same function.
  //
  // **WHAT LANDS IS AN ORDINARY DIRECTORY ENTRY IN THE REALM THE CONSOLE IS
  // SHOWING.** Nothing on this page knows that — the realm is ambient (rule 3m)
  // and `ou=applications` is resolved by `ldap_server.js` under whichever realm
  // this request arrived in — which is why the container DN is printed rather
  // than described: it is the answer this process gives, not one this page
  // works out, and on `/realm/acme/admin/applications/new` it says `acme`'s.
  // ---------------------------------------------------------------------------

  // One protocol family as a row of the checkbox table. `kind` is what the
  // registry WOULD record this application as when a protocol of that family
  // finally recognises the identifier — shown rather than written, because a
  // declaration is not a sighting (see createApplication()) — and a family with
  // no kind at all is marked, because the alternative is a reader wondering for
  // the third time why the LDAP row never fills that column in. `checked` is
  // the one argument this row did not have until the RFC 9728 import: a
  // document describes an OAuth protected resource, so OAuth 2.0 comes ticked,
  // and a refused create redraws the boxes the reader had ticked. It is a
  // separate argument and not `Array.prototype.map`'s index, which a bare
  // `.map(protocolChoiceRow)` would have passed as a truthy number.
  protocolChoiceRow(row, checked) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.protocolChoiceRow().");
    const kindCell = row.kind
      ? '<code>' + this.esc(row.kind) + '</code>'
      : '<span class="state-none">none &mdash; this service records no ' +
        'application identifier in that family</span>';
    log.debug("Leaving AdminConsole.protocolChoiceRow().");
    // The family's own sentence appears TWICE and neither is the only copy: as
    // a tooltip on the checkbox's label, where somebody deciding whether to
    // tick it is already pointing, and folded in the last column, where it can
    // be read from a keyboard and on a touch screen. See tip() for why nothing
    // is ever said only in a title attribute.
    return '<tr><td><input type="checkbox" id="proto-' + this.esc(row.id) +
           '" ' +
      'name="protocol" value="' + this.esc(row.id) + '"' +
      (checked === true ? ' checked' : '') + this.tip(row.what) + '></td>' +
      '<td><label for="proto-' + this.esc(row.id) + '"' + this.tip(row.what) +
      '>' +
      this.esc(row.label) + '</label></td>' +
      '<td><code>' + this.esc(row.id) + '</code></td>' +
      '<td>' + kindCell + '</td>' +
      '<td class="why">' + this.note(this.esc(row.what)) + '</td></tr>';
  }

  // ---------------------------------------------------------------------------
  // ONE DECLARED ATTRIBUTE AS A ROW OF ONE OF THE DECLARATION TABLES
  // (`declarationFieldRow()`, below `familyClasses()`).
  //
  // The rows come from `applications.declarationAttributes()`, which walks the
  // PROTOCOLS table and dedupes by ATTRIBUTE — so this renders one field per
  // attribute rather than one per family, and names the families each field
  // serves underneath it. Building that list here instead was the obvious
  // thing and would have been a second opinion about which attribute a family's
  // identifier goes in; `createApplication()` has the first.
  //
  // **THE FIELD NAME IS `field.<attribute>` AND THE ATTRIBUTE IS THE SCHEMA'S
  // OWN NAME**, not a friendly one. Two reasons, and the second is the
  // load-bearing one: an `ldapsearch` on this entry shows exactly the name that
  // was on the form, and `POST /admin-api/applications/create` takes the same
  // names in its `fields` object — so the console and the management API are
  // one vocabulary and a person who learns the page can drive the API.
  //
  // A `multi` attribute gets a TEXTAREA, one value per line, and a `single` one
  // gets an input. Newline-separated rather than comma-separated because a
  // redirect URI may legally contain a comma and may not contain a newline;
  // splitting on commas would silently cut one URI into two that both fail to
  // match. There was once exactly one single-valued field here — mutual TLS's
  // `oauthTlsClientAuthSubjectDn` — and the row's note says why that one is a
  // different shape. There are four now (`oauthClientSecret`, `gnapInstanceId`
  // and `gnapSymmetricKey` joined it), and the note under the input still gives
  // the RFC 8705 reason on every one of them.
  // ---------------------------------------------------------------------------
  // The classes that make a block appear only when one of its families is
  // ticked. `pf` is what the stylesheet hides; each `pf-<id>` is what a checked
  // box shows. A block serving several families carries several, and appears
  // when ANY of them is ticked — which is what `samlEntityId` needs, since it
  // is the identifier of both SAML profiles.
  //
  // A block with NO families is not given `pf` at all: it is unconditional, and
  // tagging it would hide it forever.
  familyClasses(ids) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.familyClasses().");
    const list = (ids || []).filter(function (id) { return !!id; });
    if (!list.length) {
      log.debug("Leaving AdminConsole.familyClasses().");
      return '';
    }
    log.debug("Leaving AdminConsole.familyClasses().");
    return 'pf ' + list.map(function (id) { return 'pf-' + id; }).join(' ');
  }

  declarationFieldRow(row, draft?) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.declarationFieldRow().");
    // WHAT WAS POSTED, drawn back — `newApplicationPage()`'s `draft`, present
    // only when this page is redrawn from a round trip of its own form.
    const posted = draft ? draft['field.' + row.attribute] : undefined;
    const value = posted == null ? '' : String(posted);
    // A row that belongs to EVERY family (`appCorsOrigin`, 2026-09-18) has an
    // empty `families` on purpose — that is what keeps its section
    // unconditional — so the cell says what the emptiness means rather than
    // being blank, which would read as "applies to nothing".
    const families = row.everyFamily
      ? '<strong>every family</strong> &mdash; every published endpoint'
      : row.families.map(function (one) {
        return self.esc(one.label);
      }).join(', ');
    // The attribute's sentence, on the control itself. The schema's name is
    // unfriendly on purpose (see the header above) — it is the name an
    // ldapsearch and the management API both use — so the one place a reader
    // can find out what `appWsFedRealm` is for without leaving the field is a
    // tooltip on it. The same sentence is in the last column, folded.
    const hint = this.tip(row.what);
    const control = row.kind === 'multi'
      ? '<textarea id="field-' + this.esc(row.attribute) + '" name="field.' +
        this.esc(row.attribute) +
        '"' + hint + ' rows="3" cols="42" placeholder="one per line; leave ' +
                     'empty for none">' + this.esc(value) + '</textarea>'
      : '<input type="text" id="field-' + this.esc(row.attribute) +
        '" name="field.' +
        this.esc(row.attribute) + '"' + hint +
        ' size="42" placeholder="optional" value="' + this.esc(value) + '">';
    // GENERATE SECRET (2026-09-18), beside `oauthClientSecret` and no other
    // field — the `secret` role also holds GNAP's `gnapSymmetricKey`, which
    // is a key and not a client secret. A SUBMIT BUTTON AND NOT A SCRIPT:
    // `script-src 'none'` holds on this page, and the test for an exception
    // is that the page cannot work without one, which a round trip answers.
    // Its `formaction` sends the whole form to `/admin/applications/new`
    // with `action=generate-secret` — the button's pair comes after the
    // hidden `action=create` in the body and `parseBody()` keeps the last —
    // and that page is drawn again with every box as it was and a new
    // secret in this one. `formnovalidate`, because the Identifier is
    // `required` and a person may want the secret before they have named
    // the application.
    const generate = row.attribute === 'oauthClientSecret'
      ? ' <button type="submit" class="secondary" name="action" ' +
        'value="generate-secret" formaction="/admin/applications/new" ' +
        'formnovalidate' +
        this.tip('Mint a client secret the way POST /oauth2/register does ' +
                 'and put it in this box. Nothing is written until the ' +
                 'application is created. Everything else you have typed ' +
                 'on this page is kept.') +
        '>Generate Secret</button>'
      : '';
    const shape = row.kind === 'multi'
      ? '<span class="note">a list &mdash; one value per line</span>'
      : '<span class="state-none">one value only &mdash; an RFC 8705 check ' +
        'compares this string to a certificate\'s subject by exact equality, ' +
        'so it cannot hold a list until somebody decides what &ldquo;any of ' +
        'these&rdquo; should mean to that check</span>';
    log.debug("Leaving AdminConsole.declarationFieldRow().");
    // The row is conditional on the families it serves — see familyClasses().
    return '<tr class="' +
           this.esc(this.familyClasses(row.families.map(function (one) {
        return one.id;
      }))) + '"><td><label for="field-' + this.esc(row.attribute) + '"' + hint +
      '><code>' +
      this.esc(row.attribute) + '</code></label></td>' +
      '<td>' + families + '</td>' +
      '<td>' + control + generate + '<br>' + shape + '</td>' +
      '<td class="why">' + this.note(this.esc(row.what)) + '</td></tr>';
  }

  // ---------------------------------------------------------------------------
  // THE PER-APPLICATION SAML SETTINGS, AS FORM FIELDS.
  //
  // THIS DECLARATION IS DEAD: an identical `samlOverrideFieldRow()` is declared
  // again under *THE PER-APPLICATION SETTINGS* below, and the later function
  // declaration is the one every caller gets. Its header is the current one.
  // A class cannot hold two methods of one name, so under #50 this one became
  // `unreachedSamlOverrideFieldRow()`, which nothing calls.
  //
  // Written when there were ten attributes, five per SAML profile, each
  // overriding one `config.js` setting for this application alone. They are
  // drawn from `applications.overridableSettings()` — the same table
  // `saml2_sso.ts` resolves through and `/admin/saml-assertions` names the
  // attribute from — so a setting added there reaches this form without anybody
  // editing it.
  //
  // **EVERY FIELD IS EMPTY BY DEFAULT AND EMPTY MEANS INHERIT.** That is the
  // whole of the interaction and it is why there is no "use the default"
  // checkbox beside each one: the absence of the attribute IS the default, so a
  // blank box and an unticked box would be two spellings of one state. The
  // placeholder carries the value that would be used, read live off the
  // setting, so somebody can see what they are overriding without opening
  // another page.
  //
  // A `select` for a bool rather than a checkbox, for `configRow()`'s reason —
  // an unticked checkbox posts NOTHING, which here is indistinguishable from
  // "leave it alone". The empty option is what makes "inherit" expressible.
  unreachedSamlOverrideFieldRow(row) {
    const { log, configSettingFor, config } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.unreachedSamlOverrideFieldRow().");
    const setting = configSettingFor(row.setting);
    if (!setting) {
      log.debug("Leaving AdminConsole.unreachedSamlOverrideFieldRow().");
      return '';
    }
    const described = config.describe(setting);
    const id = 'field-' + row.attribute;
    const hint = this.tip(described.description);
    const inherits = 'inherit — currently ' + described.text;
    // A BOOL AND AN ENUM ARE THE SAME CONTROL WITH DIFFERENT OPTIONS, which is
    // why the enum joined this branch rather than getting one of its own: both
    // are a closed vocabulary plus the empty option that means inherit, and a
    // bool is the two-word case of it. The options come off `enumValues` on the
    // setting's own row, so a value added there reaches this form with nothing
    // edited here — the same rule the section itself follows about
    // `overridableSettings()`. A text box would have been the alternative and
    // is the wrong one: this attribute is read back through `config.parseAs()`,
    // so a typo is a warning in the log and the service-wide value silently in
    // force, which is the failure a select cannot have.
    const choices = described.type === 'bool' ? ['true', 'false']
      : (described.type === 'enum' ? (described.enumValues || []) : null);
    const control = choices
      ? '<select id="' + this.esc(id) + '" name="field.' +
        this.esc(row.attribute) + '"' +
        hint + '><option ' +
        'value="">' + this.esc(inherits) + '</option>' +
        choices.map(function (option) {
          return '<option value="' + self.esc(option) + '">' +
                 self.esc(option) +
                 '</option>';
        }).join('') + '</select>'
      : '<input type="' + (described.type === 'int' ? 'number' : 'text') + '"' +
        ' id="' + this.esc(id) + '" name="field.' + this.esc(row.attribute) +
        '"' + hint +
        (typeof described.min === 'number' ? ' min="' + described.min + '"' :
         '') +
        (typeof described.max === 'number' ? ' max="' + described.max + '"' :
         '') +
        (typeof described.step === 'number' ? ' step="' + described.step + '"' :
         '') +
        ' size="' + (described.type === 'int' ? '10' : '42') + '"' +
        ' placeholder="' + this.esc(inherits) + '">';
    log.debug("Leaving AdminConsole.unreachedSamlOverrideFieldRow().");
    return '<tr><td><label for="' + this.esc(id) + '"' + hint + '><code>' +
      this.esc(row.attribute) + '</code></label></td>' +
      '<td><code>' + this.esc(row.setting) + '</code></td>' +
      '<td>' + control + '</td>' +
      '<td class="why">' + this.note(this.esc(described.description)) +
      '</td></tr>';
  }

  // ---------------------------------------------------------------------------
  // THE PER-APPLICATION SETTINGS, AS FORM FIELDS.
  //
  // Twenty-six attributes as of 2026-09-16 — twenty-five across the five
  // sections and `gnap.accessTokenLifetimeS`, which matches none — each
  // overriding one `config.js` setting for this application alone. They are
  // drawn from `applications.overridableSettings()` — the same table the
  // protocol modules resolve through and each defaults page names the
  // attribute from — so a setting that becomes per-application reaches this
  // form without anybody editing it. What it DOES need is a row in
  // OVERRIDE_SECTIONS above, and a setting whose prefix matches none of them is
  // drawn in a section of its own at the end rather than silently dropped.
  //
  // **EVERY FIELD IS EMPTY BY DEFAULT AND EMPTY MEANS INHERIT.** That is the
  // whole of the interaction and it is why there is no "use the default"
  // checkbox beside each one: the absence of the attribute IS the default, so a
  // blank box and an unticked box would be two spellings of one state. The
  // placeholder carries the value that would be used, read live off the
  // setting.
  //
  // A `select` for a bool rather than a checkbox, for `configRow()`'s reason —
  // an unticked checkbox posts NOTHING, which here cannot be told from "leave
  // it alone". The empty option is what makes "inherit" expressible.
  samlOverrideFieldRow(row, draft?) {
    const { log, configSettingFor, config } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.samlOverrideFieldRow().");
    // What was posted, on a redraw of this page's own form — see
    // declarationFieldRow().
    const posted = draft ? draft['field.' + row.attribute] : undefined;
    const value = posted == null ? '' : String(posted);
    const setting = configSettingFor(row.setting);
    if (!setting) {
      log.debug("Leaving AdminConsole.samlOverrideFieldRow().");
      return '';
    }
    const described = config.describe(setting);
    const id = 'field-' + row.attribute;
    const hint = this.tip(described.description);
    const inherits = 'inherit — currently ' + described.text;
    // A BOOL AND AN ENUM ARE THE SAME CONTROL WITH DIFFERENT OPTIONS, which is
    // why the enum joined this branch rather than getting one of its own: both
    // are a closed vocabulary plus the empty option that means inherit, and a
    // bool is the two-word case of it. The options come off `enumValues` on the
    // setting's own row, so a value added there reaches this form with nothing
    // edited here — the same rule the section itself follows about
    // `overridableSettings()`. A text box would have been the alternative and
    // is the wrong one: this attribute is read back through `config.parseAs()`,
    // so a typo is a warning in the log and the service-wide value silently in
    // force, which is the failure a select cannot have.
    const choices = described.type === 'bool' ? ['true', 'false']
      : (described.type === 'enum' ? (described.enumValues || []) : null);
    const control = choices
      ? '<select id="' + this.esc(id) + '" name="field.' +
        this.esc(row.attribute) + '"' +
        hint + '><option ' +
        'value="">' + this.esc(inherits) + '</option>' +
        choices.map(function (option) {
          return '<option value="' + self.esc(option) + '"' +
                 (option === value ? ' selected' : '') + '>' +
                 self.esc(option) +
                 '</option>';
        }).join('') + '</select>'
      : '<input type="' + (described.type === 'int' ? 'number' : 'text') + '"' +
        ' id="' + this.esc(id) + '" name="field.' + this.esc(row.attribute) +
        '"' + hint + ' value="' + this.esc(value) + '"' +
        (typeof described.min === 'number' ? ' min="' + described.min + '"' :
         '') +
        (typeof described.max === 'number' ? ' max="' + described.max + '"' :
         '') +
        (typeof described.step === 'number' ? ' step="' + described.step + '"' :
         '') +
        ' size="' + (described.type === 'int' ? '10' : '42') + '"' +
        ' placeholder="' + this.esc(inherits) + '">';
    log.debug("Leaving AdminConsole.samlOverrideFieldRow().");
    return '<tr><td><label for="' + this.esc(id) + '"' + hint + '><code>' +
      this.esc(row.attribute) + '</code></label></td>' +
      '<td><code>' + this.esc(row.setting) + '</code></td>' +
      '<td>' + control + '</td>' +
      '<td class="why">' + this.note(this.esc(described.description)) +
      '</td></tr>';
  }

  samlKeySourceSection(draft?) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.samlKeySourceSection().");
    const rows = SAML_KEY_SOURCE_FIELDS.map(function (row) {
      const id = 'field-' + row.attribute;
      const hint = self.tip(row.what);
      // What was posted, on a redraw — see declarationFieldRow().
      const posted = draft ? draft['field.' + row.attribute] : undefined;
      const value = posted == null ? '' : String(posted);
      const control = row.multi
        ? '<textarea id="' + self.esc(id) + '" name="field.' +
          self.esc(row.attribute) +
          '"' + hint +
          ' rows="4" cols="42" placeholder="paste the document, or leave it ' +
          'to Refresh">' + self.esc(value) + '</textarea>'
        : '<input type="text" id="' + self.esc(id) + '" name="field.' +
          self.esc(row.attribute) + '"' +
          hint + ' size="42" placeholder="optional" value="' +
          self.esc(value) + '">';
      return '<tr><td><label for="' + self.esc(id) + '"' + hint + '><code>' +
        self.esc(row.attribute) + '</code></label></td>' +
        '<td>' + self.esc(row.label) + '</td><td>' + control + '</td>' +
        '<td class="why">' + self.note(self.esc(row.what)) + '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.samlKeySourceSection().");
    return '<div class="' + this.esc(this.familyClasses(['saml2'])) + '">' +
      '<h3>Where SAML 2.0 encryption gets this service provider\'s key</h3>' +
      this.note('Encrypting an assertion needs the RECIPIENT\'S certificate, ' +
      'and this service does not consume metadata unless it is told to. It ' +
      'looks in three places, most specific first: ' +
      '<code>samlEncryptionCertificate</code> below, then a REGISTERED ' +
      '<code>samlSigningCertificate</code>, then — in development mode only ' +
      '— the certificate a signed AuthnRequest carried, which is recorded as ' +
      'observed and not trusted. With none, an assertion that was meant to ' +
      'be encrypted goes out <strong>in clear</strong> in development and ' +
      'says so in the log, and is refused in product. A metadata document ' +
      'pasted below is CONSUMED when the application is created.') +
      '<table><tr><th>Attribute</th><th>What</th><th>Value</th><th>Notes</th>' +
      '</tr>' +
      rows + '</table></div>';
  }

  samlOverrideFieldsSection(draft?) {
    const { log, applications } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.samlOverrideFieldsSection().");
    const rows = applications.overridableSettings();
    if (!rows.length) {
      log.debug("Leaving AdminConsole.samlOverrideFieldsSection(). Nothing " +
                "to offer.");
      return '';
    }
    const placed = {};
    const sections = OVERRIDE_SECTIONS.map(function (group) {
      const mine = rows.filter(function (one) {
        return one.setting.indexOf(group.prefix) === 0;
      });
      mine.forEach(function (one) { placed[one.attribute] = true; });
      if (!mine.length) return '';
      return '<div class="' + self.esc(self.familyClasses(group.families)) +
             '">' +
        '<h3>' + self.esc(group.heading) + '</h3>' + self.note(group.intro) +
        '<table><tr><th>Attribute</th><th>Overrides</th><th>Value</th>' +
        '<th>What it is</th></tr>' +
        mine.map(function (row) {
          return self.samlOverrideFieldRow(row, draft);
        }).join('') +
        '</table></div>';
    }).join('');

    // A SETTING THAT MATCHED NO SECTION IS DRAWN ANYWAY, unconditionally, and
    // that is the safe direction rather than a loose end: the alternative is a
    // per-application setting that exists, is resolved by some protocol module,
    // and has no box on the one form meant to offer every one of them. It is
    // also visible, which is how somebody notices a row is missing from
    // OVERRIDE_SECTIONS.
    const leftovers =
        rows.filter(function (one) { return !placed[one.attribute]; });
    const extra = leftovers.length
      ? '<h3>Other per-application settings</h3>' +
        this.warn('These ' + leftovers.length + ' override a setting whose ' +
        'family this page does not know, so they are shown always rather ' +
        'than with a protocol. Add a row to <code>OVERRIDE_SECTIONS</code> ' +
        'in <code>admin-ui/admin.ts</code> naming the families they belong ' +
        'to.') +
        '<table><tr><th>Attribute</th><th>Overrides</th><th>Value</th>' +
        '<th>What it is</th></tr>' +
        leftovers.map(function (row) {
          return self.samlOverrideFieldRow(row, draft);
        }).join('') +
        '</table>'
      : '';

    log.debug("Leaving AdminConsole.samlOverrideFieldsSection(). " +
              rows.length + " field(s), " +
              leftovers.length + " unplaced.");
    return '<h2>What each protocol issues for this application</h2>' +
      this.note('These are OPTIONAL and every one of them means ' +
      '<em>inherit</em> when left alone. Each overrides one service-wide ' +
      'setting for this application only — the box says which, and its ' +
      'placeholder says what would be used instead. The defaults live on <a ' +
      'href="/admin/token-lifetimes">Token lifetimes</a> and <a ' +
      'href="/admin/saml-assertions">SAML assertions</a>, and changing one ' +
      'there moves every application that has not been given an answer of ' +
      'its own.') +
      this.note('<strong>They apply whether or not the protocol is ticked ' +
      'above &mdash; with one exception, named below.</strong> A declaration ' +
      'grants and refuses nothing here &mdash; an application declared for ' +
      'nothing at all still gets a token if it asks for one &mdash; so these ' +
      'are read whenever this service issues to this application. Ticking a ' +
      'family only decides what this page SHOWS you. What ticking SAML 2.0 ' +
      'or SAML 1.1 does do is give the entry a <code>samlEntityId</code> if ' +
      'you leave that blank, so its metadata is publishable immediately.') +
      this.note('<strong>The exception is ' +
      '<code>oauthTokenExchangeRefreshToken</code>, and it is REFUSED rather ' +
      'than inert on an entry declared for neither OAuth 2.0 nor OpenID ' +
      'Connect.</strong> Every other field here is a default something reads ' +
      'if it ever gets the chance, so writing one onto an application that ' +
      'never reaches that protocol costs nothing and says nothing false. ' +
      'That one decides what the <em>token endpoint</em> does for one ' +
      '<code>client_id</code> &mdash; so on an entry no token request could ' +
      'ever name, it would sit there reading like a policy that was in ' +
      'force. Tick OAuth 2.0 or OpenID Connect and it is accepted. An ' +
      '<code>ldapmodify</code> reaches the attribute either way, as it ' +
      'reaches every attribute here.') +
      sections + extra + this.samlKeySourceSection(draft);
  }

  // The tables, one per ROLE (`identifier`, `redirect`, `logout`, `secret`,
  // `delivery`, `events`), split by what the attribute IS rather than drawn as
  // one list. A reader filling this in is answering different questions — "what
  // is this application called", "where does a response go back to" — and each
  // of the later ones exists only for the families that have one. `omit` names
  // attributes another part of the page already draws a box for — the RFC 9728
  // pane draws `oauthClientId` — because two fields with one name in one form
  // post the name twice and `parseBody()` keeps whichever came LAST, which
  // would be the empty box below the one the reader filled in.
  declarationFieldsSection(role, heading, intro, omit?, draft?) {
    const { log, applications } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.declarationFieldsSection(). role=" + role);
    const skip = omit || [];
    const rows = applications.declarationAttributes().filter(function (one) {
      return one.role === role && skip.indexOf(one.attribute) < 0;
    });
    if (!rows.length) {
      // Not reachable with the table as it stands, and drawn as nothing rather
      // than as an empty table if a family is ever the last of its kind to be
      // removed. An empty <table> with a header row reads as data having gone
      // missing.
      log.debug("Leaving AdminConsole.declarationFieldsSection(). Nothing to " +
                "offer.");
      return '';
    }
    // THE SECTION carries the UNION of its rows' families, so that a table
    // whose every row is hidden does not leave a heading and a bare header row
    // behind — which reads as data having gone missing rather than as nothing
    // applying.
    const families = [];
    rows.forEach(function (one) {
      one.families.forEach(function (family) {
        if (families.indexOf(family.id) < 0) families.push(family.id);
      });
    });
    log.debug("Leaving AdminConsole.declarationFieldsSection(). " +
              rows.length +
              " field(s).");
    return '<div class="' + this.esc(this.familyClasses(families)) + '">' +
      '<h2>' + heading + '</h2>' + intro +
      '<table><tr><th>Attribute</th><th>Families it serves</th><th>Value</th>' +
      '<th>What it is</th></tr>' +
      rows.map(function (row) {
        return self.declarationFieldRow(row, draft);
      }).join('') +
      '</table></div>';
  }

  // The form that gives the document. The checkbox shows the three sources; it
  // has no name, so it is never posted, and it is ticked whenever a document is
  // on the page or was just refused, so the reader is not left hunting for the
  // form their error is about.
  resourceMetadataLoadSection(state) {
    const { log, resourceMetadata, mode } = this.deps;
    log.debug("Entering AdminConsole.resourceMetadataLoadSection().");
    const given = (state && state.loadInput) || {};
    const open = !!(state && (state.loaded || state.loadError));
    log.debug("Leaving AdminConsole.resourceMetadataLoadSection().");
    return '<h2>Configure it from OAuth 2.0 Protected Resource Metadata ' +
      '(RFC 9728)</h2>' +
      this.note('A protected resource publishes a JSON document describing ' +
      'itself &mdash; at <code>' + this.esc(resourceMetadata.WELL_KNOWN) +
      '</code> under its own host. Give this page that document and it fills ' +
      'in the form below: the <code>resource</code> becomes the default ' +
      'NAME, the permission base URI (<code>oauthPermissionBaseUri</code>) ' +
      'and the AUDIENCE of tokens issued for it ' +
      '(<code>oauthAudience</code>); <code>scopes_supported</code> becomes ' +
      'the permissions it exposes; a client_id is generated at random; and ' +
      'OAuth 2.0 is ticked. Nothing is created until you press Create, and ' +
      'every value can be changed first.') +
      '<form method="post" action="/admin/applications/new" ' +
      'enctype="multipart/form-data" class="prm-load">' +
      '<input type="hidden" name="action" value="load-resource-metadata">' +
      '<input type="checkbox" id="prm-use" class="prm-use"' +
      (open ? ' checked' : '') + '> <label for="prm-use"><strong>Use a ' +
      'protected resource metadata document</strong></label>' +
      '<div class="prm-source">' +
      this.note('Give it ONE way. A document fetched from a URL is held to ' +
      'RFC 9728 section 3.3 &mdash; its <code>resource</code> must be the ' +
      'identifier the well-known URL was built from &mdash; which ' +
      (mode.acceptsNonconformingResourceMetadata()
        ? 'this development-mode service reports and does not refuse'
        : 'this product-mode service refuses') + '. The fetch follows the ' +
      'federation outbound policy: no redirects, a size cap, a timeout, ' +
      'https unless <code>federation.outboundAllowInsecure</code> is on' +
      (mode.dialsInternalAddresses()
        ? '.'
        : ', and never to a loopback, private or link-local address.')) +
      '<div class="formrow"><label for="prm-document">Paste it</label>' +
      '<textarea id="prm-document" name="document" rows="8" cols="60" ' +
      'placeholder="{&quot;resource&quot;: ' +
      '&quot;https://api.example.com&quot;, ' +
      '&quot;scopes_supported&quot;: [&quot;read&quot;]}">' +
      this.esc(String(given.document || '')) + '</textarea></div>' +
      '<div class="formrow"><label for="prm-file">or upload it</label>' +
      '<input type="file" id="prm-file" name="file" ' +
      'accept="application/json,.json"></div>' +
      '<div class="formrow"><label for="prm-url">or fetch it from</label>' +
      '<input type="text" id="prm-url" name="url" size="60" value="' +
      this.esc(String(given.url || '')) + '" ' +
        'placeholder="https://api.example.com' +
      this.esc(resourceMetadata.WELL_KNOWN) + '"></div>' +
      '<div class="formrow"><button type="submit">Load the document</button>' +
      '</div></div></form>';
  }

  // What the third tab's boxes hold: the plan's defaults, or — on a redraw
  // after a refused create — what the reader had typed. A key present in the
  // posted body is what they typed, including an emptied box.
  resourceMetadataValues(loaded, draft) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.resourceMetadataValues().");
    const plan = loaded.plan;
    const posted = draft || null;
    const pick = function (key, fallback) {
      log.debug("Entering pick().");
      log.debug("Leaving pick().");
      return posted && Object.prototype.hasOwnProperty.call(posted, key)
        ? String(posted[key]) : fallback;
    };
    const members = {};
    loaded.members.forEach(function (row) {
      if (!row.known || row.type === 'jwt') {
        return;
      }
      const fallback = Array.isArray(row.value) ? row.value.join('\n')
                                                : String(row.value);
      members[row.member] = pick('metadata.' + row.member, fallback);
    });
    log.debug("Leaving AdminConsole.resourceMetadataValues().");
    return {
      identifier: pick('identifier', plan.identifier),
      name: pick('name', plan.name),
      clientId: pick('field.oauthClientId', plan.clientId),
      baseUri: pick('field.oauthPermissionBaseUri', plan.baseUri),
      audience: pick('field.oauthAudience', plan.audience),
      permissions: pick('field.oauthPermission',
                        plan.permissionLines.join('\n')),
      detailsTypes: pick('field.oauthAuthorizationDetailsType',
                         (plan.detailsTypeLines || []).join('\n')),
      url: pick('field.oauthResourceMetadataUrl', loaded.url),
      members: members
    };
  }

  // One JSON value, readable in a table cell.
  resourceMetadataValueCell(value) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.resourceMetadataValueCell().");
    log.debug("Leaving AdminConsole.resourceMetadataValueCell().");
    if (Array.isArray(value)) {
      return value.map(function (one) {
        return '<code>' + self.esc(typeof one === 'string' ? one :
                                   JSON.stringify(one)) + '</code>';
      }).join('<br>') || '<span class="state-none">(empty)</span>';
    }
    return '<code>' + this.esc(typeof value === 'string' ? value :
                               JSON.stringify(value)) + '</code>';
  }

  // The banners above the tabs: the authorization-server comparison, section
  // 3.3, and every warning the reading produced. ABOVE the tabs rather than on
  // one of them, because a reader on the raw JSON tab must not miss that none
  // of the authorization servers is this realm's.
  resourceMetadataBanners(loaded) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.resourceMetadataBanners().");
    const servers = loaded.authorizationServers;
    let html = '';
    if (!servers.listed) {
      html += this.note('<strong>The document names no authorization ' +
        'server.</strong> <code>authorization_servers</code> is optional, so ' +
        'there is nothing to compare with this realm\'s.');
    } else if (servers.allMatched) {
      html += '<div class="ok"><strong>Every authorization server it names ' +
        'is one this trust realm publishes</strong> &mdash; ' +
        servers.rows.map(function (row) {
          return '<code>' + self.esc(row.issuer) + '</code> is <code>' +
                 self.esc(row.authorizationServer) + '</code>';
        }).join(', ') + '.</div>';
    } else {
      const unmatched = servers.rows.filter(function (row) {
        return !row.matched;
      });
      // NOT warn(), which folds prose longer than a line behind its opening
      // sentence: the issuers that did not match ARE this banner, and a fold
      // would put them where a reader skimming for the colour does not look.
      html += '<div class="warn"><strong>' + unmatched.length + ' of the ' +
        servers.listed +
        ' authorization server(s) this document names ' +
        (unmatched.length === 1 ? 'is' : 'are') + ' not one this trust realm ' +
        'publishes:</strong> ' + unmatched.map(function (row) {
          return '<code>' + self.esc(row.issuer) + '</code>';
        }).join(', ') + '. The resource expects tokens from ' +
        (unmatched.length === 1 ? 'an issuer' : 'issuers') + ' this service ' +
        'is not, at the address this page was reached on. You can still ' +
        'create the application; a token this realm issues for it will carry ' +
        'an <code>iss</code> the resource was not told to trust. This realm ' +
        'publishes ' + servers.realm.map(function (row) {
          return '<code>' + self.esc(row.issuer) + '</code>';
        }).join(', ') + '.</div>';
    }
    const check = loaded.resourceCheck;
    if (check.checked && check.matches) {
      html += '<div class="ok"><strong>RFC 9728 section 3.3:</strong> ' +
              this.esc(check.why) + '</div>';
    } else if (!check.checked) {
      html += this.note('<strong>RFC 9728 section 3.3 was not ' +
        'checked.</strong> ' +
                        this.esc(check.why));
    }
    if (loaded.warnings.length) {
      html += '<div class="warn"><strong>' + loaded.warnings.length +
        ' thing(s) to know before creating it:</strong><ul>' +
        loaded.warnings.map(function (one) {
          return '<li>' + self.esc(one) + '</li>';
        }).join('') + '</ul></div>';
    }
    log.debug("Leaving AdminConsole.resourceMetadataBanners().");
    return html;
  }

  // THE THREE TABS. `tab` picks which one is open: the raw JSON on a fresh
  // load, and the editable fields when a refused create is redrawn, because
  // those are what the refusal is about.
  resourceMetadataPane(loaded, draft, tab) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.resourceMetadataPane().");
    const values = this.resourceMetadataValues(loaded, draft);
    const open = tab || 'json';
    const radio = function (id) {
      log.debug("Entering radio().");
      log.debug("Leaving radio().");
      return '<input type="radio" name="prm-tab" id="prm-tab-' + id + '" ' +
             'form="prm-tabs-not-a-form"' + (open === id ? ' checked' : '') +
             '>';
    };
    const source = loaded.source === 'url'
      ? 'fetched from <code>' + this.esc(loaded.url) + '</code>'
      : (loaded.source === 'upload'
        ? 'uploaded' +
          (loaded.filename ? ' as <code>' + this.esc(loaded.filename) +
                        '</code>' : '')
        : 'pasted');

    const matchOf = {};
    loaded.authorizationServers.rows.forEach(function (row) {
      matchOf[row.issuer] = row;
    });
    const tableRows = loaded.members.map(function (row) {
      let value = self.resourceMetadataValueCell(row.value);
      if (row.member === 'authorization_servers' && Array.isArray(row.value)) {
        value = row.value.map(function (issuer) {
          const match = matchOf[issuer];
          return '<code>' + self.esc(issuer) + '</code> ' +
                 (match && match.matched
            ? '<span class="state-valid">matches this realm\'s <code>' +
              self.esc(match.authorizationServer) + '</code></span>'
            : '<span class="state-expired">not an authorization server of ' +
              'this realm</span>');
        }).join('<br>');
      }
      return '<tr><td><code>' + self.esc(row.member) + '</code></td><td>' +
             value +
        '</td><td>' + (row.known ? self.esc(row.type)
                                 : '<span class="state-none">extension' +
                                   '</span>') +
        '</td><td class="why">' + self.esc(row.maps || '') + '</td>' +
        '<td class="why">' + self.note(self.esc(row.what)) + '</td></tr>';
    }).join('');
    const permissionRows = loaded.plan.permissions.map(function (one) {
      return '<tr><td><code>' + self.esc(one.scope) + '</code></td><td><code>' +
        self.esc(one.name) + '</code></td><td><code>' + self.esc(one.id) +
        '</code></td><td>' + (one.problem
          ? '<span class="state-expired">not usable</span>'
          : (one.duplicate
            ? '<span class="state-none">a duplicate</span>'
            : (one.sameAsAdvertised
              ? '<span class="state-valid">the scope as advertised</span>'
              : '<span class="state-none">a client asks for the identifier, ' +
                'not the scope</span>'))) + '</td></tr>';
    }).join('');
    const signed = loaded.signedMetadata
      ? '<h3>signed_metadata, decoded and not verified</h3>' +
        this.note('Shown so it can be read. This service holds no key for ' +
        'the resource to verify it with, and none of its claims were ' +
        'applied.') +
        '<pre><code>' + this.esc(JSON.stringify({
          header: loaded.signedMetadata.header,
          claims: loaded.signedMetadata.claims
        }, null, 2)) + '</code></pre>'
      : '';

    const memberRows = loaded.members.filter(function (row) {
      return row.known && row.type !== 'jwt';
    }).map(function (row) {
      const id = 'prm-member-' + row.member.replace(/[^A-Za-z0-9_-]/g, '_');
      const name = 'metadata.' + row.member;
      const current = values.members[row.member];
      let control = '';
      if (row.type === 'bool') {
        control = '<select id="' + self.esc(id) + '" name="' + self.esc(name) +
                  '">' +
          ['true', 'false', ''].map(function (option) {
            return '<option value="' + option + '"' +
              (current === option ? ' selected' : '') + '>' +
              (option || '(remove the member)') + '</option>';
          }).join('') + '</select>';
      } else if (row.type === 'string-list' || row.type === 'uri-list') {
        control = '<textarea id="' + self.esc(id) + '" name="' +
                  self.esc(name) + '" ' +
          'rows="3" cols="50" placeholder="one per line; empty removes the ' +
          'member">' + self.esc(current) + '</textarea>';
      } else {
        control = '<input type="text" id="' + self.esc(id) + '" name="' +
          self.esc(name) + '" size="50" value="' + self.esc(current) + '" ' +
          'placeholder="empty removes the member">';
      }
      return '<tr><td><label for="' + self.esc(id) + '"><code>' +
             self.esc(row.member) +
        '</code></label></td><td>' + control + '</td><td class="why">' +
        self.note(self.esc(row.what)) + '</td></tr>';
    }).join('');

    const field = function (id, name, label, value, what, multi?) {
      log.debug("Entering field().");
      log.debug("Leaving field().");
      return '<tr><td><label for="' + id + '">' + label + '</label></td><td>' +
        (multi
          ? '<textarea id="' + id + '" name="' + self.esc(name) +
            '" rows="4" cols="50">' + self.esc(value) + '</textarea>'
          : '<input type="text" id="' + id + '" name="' + self.esc(name) +
            '" size="50" value="' + self.esc(value) + '">') +
        '</td><td class="why">' + self.note(what) + '</td></tr>';
    };

    const html = this.resourceMetadataBanners(loaded) +
      '<div class="prm-tabs">' +
      radio('json') + radio('table') + radio('fields') +
      '<div class="prm-tablist" role="tablist">' +
      '<label for="prm-tab-json">Raw JSON</label>' +
      '<label for="prm-tab-table">Table of values</label>' +
      '<label for="prm-tab-fields">Editable fields</label></div>' +

      '<div class="prm-panel prm-panel-json">' +
      '<p class="sub">The document as it was read, ' + source + '.</p>' +
      '<pre><code>' + this.esc(loaded.pretty) + '</code></pre></div>' +

      '<div class="prm-panel prm-panel-table">' +
      '<table><tr><th>Member</th><th>Value</th><th>Type</th>' +
      '<th>Used for</th><th>What it is</th></tr>' + tableRows + '</table>' +
      '<h3>The scopes, as permissions</h3>' +
      (permissionRows
        ? '<table><tr><th>Scope</th><th>Permission</th>' +
          '<th>Identifier a client asks for</th><th></th></tr>' +
          permissionRows + '</table>'
        : this.note('The document names no <code>scopes_supported</code>, so ' +
                    'the application is created exposing no permissions.')) +
      '<h3>This trust realm\'s authorization servers</h3>' +
      '<table><tr><th>Authorization server</th><th>Issuer</th></tr>' +
      loaded.authorizationServers.realm.map(function (row) {
        return '<tr><td><code>' + self.esc(row.id) + '</code></td><td><code>' +
               self.esc(row.issuer) + '</code></td></tr>';
      }).join('') + '</table>' + signed + '</div>' +

      '<div class="prm-panel prm-panel-fields">' +
      this.note('These are fields of the form below, whichever tab is ' +
      'showing: edit them here and press <strong>Create the ' +
      'application</strong> at the foot of the page. Everything else on the ' +
      'page below still applies.') +
      '<input type="hidden" name="metadata" value="' +
      this.esc(loaded.compact) +
      '">' +
      '<input type="hidden" name="metadataSource" value="' +
      this.esc(loaded.source) + '">' +
      '<input type="hidden" name="metadataFilename" value="' +
      this.esc(loaded.filename) + '">' +
      '<table><tr><th>Setting</th><th>Value</th><th>Where it came from</th>' +
      '</tr>' +
      field('prm-identifier', 'identifier', 'Identifier', values.identifier,
            'The key this registry files the entry under. It defaults to the ' +
            'generated client_id, which is what a protocol sighting would ' +
            'file an OAuth client under.') +
      field('prm-name', 'name', 'Name', values.name,
            'What pages call it &mdash; the document\'s ' +
            '<code>resource</code>.') +
      field('prm-client-id', 'field.oauthClientId',
            '<code>oauthClientId</code>', values.clientId,
            'Generated at random, in the shape a dynamic client registration ' +
            'mints.') +
      field('prm-base', 'field.oauthPermissionBaseUri',
            '<code>oauthPermissionBaseUri</code>', values.baseUri,
            'The document\'s <code>resource</code>. A permission is this ' +
            'followed by its name.') +
      field('prm-audience', 'field.oauthAudience',
            '<code>oauthAudience</code>', values.audience,
            'The document\'s <code>resource</code>: the audience a token for ' +
            'this application carries. One per line.', true) +
      field('prm-permissions', 'field.oauthPermission',
            '<code>oauthPermission</code>', values.permissions,
            'One per line, <code>name</code> or ' +
            '<code>name|description</code>, from ' +
            '<code>scopes_supported</code> with the resource prefix taken ' +
            'off. A scope that cannot be a permission is left out.', true) +
      field('prm-details-types', 'field.oauthAuthorizationDetailsType',
            '<code>oauthAuthorizationDetailsType</code>', values.detailsTypes,
            'One per line, from <code>authorization_details_types_supported' +
            '</code> (RFC 9396): the types a rich authorization request may ' +
            'name for this API. A line may be a JSON definition with a ' +
            '<code>schema</code>; <code>openid_credential</code> and a type ' +
            'another application declares are left out.', true) +
      (values.url
        ? field('prm-url-field', 'field.oauthResourceMetadataUrl',
                '<code>oauthResourceMetadataUrl</code>', values.url,
                'Where the document was fetched from. Recorded, never ' +
                'fetched again.')
        : '') +
      '</table>' +
      '<h3>The document\'s members</h3>' +
      this.note('What the document said, editable. The document stored on ' +
      'the entry as <code>oauthResourceMetadata</code> is the one you loaded ' +
      'with these edits applied; an emptied box removes that member. ' +
      (loaded.extensions.length
        ? 'Its ' + loaded.extensions.length + ' extension member(s) &mdash; ' +
          loaded.extensions.map(function (one) {
            return '<code>' + self.esc(one) + '</code>';
          }).join(', ') + ' &mdash; are kept as they were. '
        : '') +
      'Changing a member here does not change the settings above: the name, ' +
      'base URI and audience were filled in from <code>resource</code> when ' +
      'the document was loaded.') +
      (memberRows
        ? '<table><tr><th>Member</th><th>Value</th><th>What it is</th></tr>' +
          memberRows + '</table>'
        : '') +
      '</div></div>';
    log.debug("Leaving AdminConsole.resourceMetadataPane().");
    return html;
  }

  newApplicationPage(req, state) {
    const { log, adminViews, realms, applications } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.newApplicationPage().");
    const given = state || {};
    const loaded = given.loaded || null;
    // THE FORM AS IT WAS POSTED, on a redraw from one of this page's own
    // round trips — a refused create, or Generate Secret — so that no box
    // comes back empty. Absent on a plain GET.
    const draft = given.draft || null;
    const drafted = function (name) {
      const value = draft ? draft[name] : undefined;
      return value == null ? '' : String(value);
    };
    // WHAT THIS PAGE SHOWS IS WHAT /admin-api ANSWERS (2026-09-12). These four
    // facts used to be computed here and again, in the same shape, in the json
    // half at the bottom of this function; they come off ONE call now, so the
    // vocabulary this form offers cannot drift from the one that document
    // publishes. admin-core/admin_views.ts carries the rest of the argument.
    const json = adminViews.newApplicationJson(req);
    const container = json.container;
    const max = json.max;
    const held = json.applicationCount;
    const realm = realms.current();
    if (!container) {
      log.debug("Leaving AdminConsole.newApplicationPage(). There is no " +
                "directory.");
      return {
        inner: this.messagesOf(req) +
          '<p class="warn"><strong>There is no embedded directory loaded in ' +
          'this process</strong>, so there is no ' +
          '<code>ou=applications</code> container to create an entry in and ' +
          'this form is not drawn. The applications registry has no store of ' +
          'its own on purpose &mdash; the directory IS the registry &mdash; ' +
          'so this is a build without <code>ldap/ldap_server.js</code> ' +
          'rather than a fault.</p>' + APPLICATIONS_LINKS,
        json: json
      };
    }

    // The identifier and the name, as the form draws them with no document
    // loaded. With one loaded they are on the pane's third tab instead, and
    // drawing them here as well would post each name twice.
    const calledSection =
      '<h2>What it is called</h2><div class="formrow"><label ' +
      'for="identifier">Identifier</label><input type="text" id="identifier" ' +
      'name="identifier" size="42" required placeholder="client_id, wtrealm, ' +
      'AppliesTo, entityID or SPN" value="' + this.esc(drafted('identifier')) +
      '"></div>' +
      this.note('THE KEY, exactly as the protocol will present it. At most ' +
      '512 characters, and no line break: an entry whose <code>cn</code> ' +
      'would be longer than 64 characters is filed under <code>app-&lt;12 ' +
      'hex&gt;</code> instead, and <code>appIdentifier</code> is the ' +
      'attribute to search on either way.') +
      '<div class="formrow"><label for="newname">Name</label><input ' +
      'type="text" id="newname" name="name" size="24" ' +
      'placeholder="optional" value="' + this.esc(drafted('name')) +
      '"></div>' +
      this.note('The name is what pages call it; with none given the ' +
      'identifier is the name, because inventing a friendly name for an ' +
      'opaque id would be inventing a fact.') +
      this.note('<strong>There is no <em>Kind</em> to choose and that is ' +
      'deliberate.</strong> It used to be a select here, and it asked the ' +
      'same question the protocol families below do in a vocabulary that ' +
      'does not line up with theirs &mdash; eight kinds against fourteen ' +
      'families, five of which have no kind at all. It is also the wrong ' +
      'side of the line this registry draws: a family is DECLARED and a kind ' +
      'is DERIVED, written when a protocol actually recognises the ' +
      'identifier, so choosing one here was a form asserting a sighting that ' +
      'had not happened. Tick the families instead; the kinds fill ' +
      'themselves in as this application is used.');
    // The families ticked: the ones a refused create had ticked, or OAuth 2.0
    // for a document describing an OAuth protected resource.
    const ticked = given.protocols ||
                   (loaded ? loaded.plan.protocols : []);

    const inner = this.messagesOf(req) +
      (given.error && given.error.length
        ? '<div class="warn"><strong>' +
          this.esc(given.errorTitle || 'That was refused.') + '</strong><ul>' +
          given.error.map(function (one) {
            return '<li>' + self.esc(one) + '</li>';
          }).join('') + '</ul></div>'
        : '') +
      (loaded ? '<div class="ok">' + this.esc(loaded.message) + '</div>' : '') +
      (given.notice ? '<div class="ok">' + this.esc(given.notice) + '</div>' :
       '') +
      '<div class="tiles">' +
      this.tile(held, 'In the registry') +
      this.tile(applications.PROTOCOLS.length, 'Protocol families') +
      this.tile(applications.KINDS.length, 'Kinds') +
      '</div>' +
      this.note('<strong>The entry lands in this realm\'s directory, at ' +
        '<code>' +
      this.esc(container) + '</code></strong>' +
      (max ? ', which holds at most ' + this.esc(String(max)) +
      ' application(s)' : '') + '. The console shows one trust realm at a ' +
      'time and this form writes the one it is showing &mdash; ' +
      '<strong>' + this.esc(realm ? realm.name : 'Default') +
      '</strong> &mdash; because the realm is taken from the path this ' +
      'request arrived on. Applications are NOT shared between realms: an ' +
      '<code>ldapsearch</code> with that base DN is the same entry this ' +
      'creates, and another realm\'s registry has never heard of it.') +
      this.note('<strong>This is not a second door onto the ' +
      'registry.</strong> The form below posts to ' +
      '<code>/admin/applications</code> with <code>action=create</code> ' +
      '&mdash; the same action the list page\'s own <em>Add an ' +
      'application</em> row posts, calling the same function in ' +
      '<code>applications.js</code> that a protocol endpoint and an ' +
      '<code>ldapmodify</code> reach. Two forms over one function are two ' +
      'doors; there is one store behind them and nothing caches it.') +

      this.resourceMetadataLoadSection(given) +

      '<form method="post" action="' +
      (loaded ? '/admin/applications/new' : '/admin/applications') +
      '" class="newapp">' +
      '<input type="hidden" name="action" value="create">' +
      // THE DEFAULT BUTTON. Enter in a text box submits with the FIRST submit
      // button in the form, and since 2026-09-18 that would otherwise be
      // Generate Secret, halfway down — so a person who pressed Enter in the
      // Identifier would get a secret instead of an application. This one is
      // first, creates, and is off-screen and out of the tab order; the
      // visible Create button at the foot does the same thing.
      '<button type="submit" class="default-submit" tabindex="-1" ' +
      'aria-hidden="true">Create the application</button>' +
      (loaded
        ? '<h2>The protected resource metadata that was loaded</h2>' +
          this.resourceMetadataPane(loaded, given.draft, given.tab) +
          '<h2>What it is called</h2>' +
          this.note('<strong>The identifier, the name and the client_id are ' +
          'on the <em>Editable fields</em> tab above</strong>, filled in ' +
          'from the document: the name is its <code>resource</code> and the ' +
          'identifier is the client_id generated for it.')
        : calledSection) +

      '<h2>Protocol families it is declared for</h2>' +
      this.note('Tick as many as apply. The list is CLOSED &mdash; a value ' +
      'that is not one of these is refused rather than recorded, because a ' +
      'typo that silently became a new family is how one application comes ' +
      'to be declared for two spellings of one thing.') +
      '<table><tr><th>For</th><th>Family</th><th>Value</th>' +
      '<th>Recorded as, when it turns up</th><th>What it means</th></tr>' +
      applications.PROTOCOLS.map(function (row) {
        return self.protocolChoiceRow(row, ticked.indexOf(row.id) >= 0);
      }).join('') +
      '</table>' +

      // THE CORS ORIGINS, ABOVE THE LINE (2026-09-18). Every section below
      // the hint depends on which families are ticked; this one does not —
      // its row belongs to every family, so it carries no `pf` class and is
      // always shown — and putting it below the hint would make the hint's
      // first sentence false.
      this.declarationFieldsSection('cors',
                                    'Which web origins may call it (CORS)',
                                    NEW_APPLICATION_CORS_INTRO, [], draft) +

      // THE PROMPT THAT STANDS IN FOR THE HIDDEN FIELDS. It is inside the form
      // so the `:has()` rule that hides it can reach it, and it is always in
      // the markup: a browser without `:has()` shows it beside every field,
      // where it reads as a description of the page rather than as a broken
      // instruction.
      '<div class="pf-hint">Everything below this line depends on which ' +
      'families you tick above &mdash; a field appears when the protocol it ' +
      'belongs to is selected, so an OAuth client is not asked for a SAML ' +
      'entityID. Tick a family to see its fields. <strong>If you can see the ' +
      'fields already</strong>, this browser does not support the ' +
      '<code>:has()</code> selector and the form is showing everything, ' +
      'which is the same form it was before this page could hide anything ' +
      '&mdash; nothing you type is affected either way, because the server ' +
      'reads what was posted and not what was visible.</div>' +

      this.declarationFieldsSection('identifier',
                                    'What each protocol will call it',
                                    NEW_APPLICATION_IDENTIFIERS_INTRO,
                                    loaded ? RESOURCE_METADATA_OWNED : [],
                                    draft) +
      this.declarationFieldsSection('redirect', 'Where responses go back to',
                                    NEW_APPLICATION_REDIRECTS_INTRO, [],
                                    draft) +
      this.declarationFieldsSection('logout', 'Where a sign-out goes',
                                    NEW_APPLICATION_LOGOUT_INTRO, [], draft) +
      this.declarationFieldsSection('secret', 'The client secret',
                                    NEW_APPLICATION_SECRET_INTRO, [], draft) +
      this.declarationFieldsSection('delivery', 'Where events are pushed',
                                    NEW_APPLICATION_DELIVERY_INTRO, [],
                                    draft) +
      this.declarationFieldsSection('events', 'Which Shared Signals events ' +
                                              'it may receive',
                                    NEW_APPLICATION_EVENTS_INTRO, [], draft) +
      this.samlOverrideFieldsSection(draft) +

      '<div class="formrow"><button type="submit">Create the ' +
      'application</button>' +
      this.note('It is created with zero counters and a description saying ' +
      'it was made by hand, so it cannot be mistaken for one that turned up ' +
      'once and never came back. You land on its entry.') + '</div></form>' +

      NEW_APPLICATION_NOTES + APPLICATIONS_CAVEAT + APPLICATIONS_LINKS;

    log.debug("Leaving AdminConsole.newApplicationPage(). " +
              applications.PROTOCOLS.length +
              " protocol family/families offered.");
    return {
      inner: inner,
      // The loaded document and what it was read as, beside the vocabulary the
      // GET answers — `load-resource-metadata`'s reply, which is what an API
      // caller of that action gets.
      json: loaded ? Object.assign({}, json, { resourceMetadata: loaded }) :
            json
    };
  }

  newApplicationView(req, state?) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.newApplicationView().");
    const built = this.newApplicationPage(req, state);
    log.debug("Leaving AdminConsole.newApplicationView().");
    return { json: built.json, inner: built.inner, title: 'New application' };
  }

  // ---------------------------------------------------------------------------
  // POST /admin/applications/new — THE RFC 9728 IMPORT'S TWO ROUND TRIPS.
  //
  // `load-resource-metadata` answers with this page and the pane on it;
  // `create` creates through the same `applicationsAction()` the list page
  // posts to, lands on the entry it made, and on a refusal redraws THIS page
  // with the document still loaded and every box as the reader left it. Any
  // other action is handed to that switch and answered as `/admin/applications`
  // answers it, so the unknown-action sentence a caller meets here is the same
  // one — which is the sentence the parity checks read.
  //
  // A JSON caller gets JSON throughout, as everywhere on this console.
  // ---------------------------------------------------------------------------
  newApplicationRedraw(req, res, state, code?) {
    const { log, errorCodes } = this.deps;
    log.debug("Entering AdminConsole.newApplicationRedraw().");
    if (code) {
      errorCodes.mark(res, code);
    }
    const view = this.newApplicationView(req, state);
    this.respond(req, res, view.json, view.title, '/admin/applications',
                 view.inner,
                 this.upTo('/admin/applications', view.title));
    log.debug("Leaving AdminConsole.newApplicationRedraw().");
  }

  asMemberOptions(selected) {
    const { log, authorizationServers } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.asMemberOptions().");
    log.debug("Leaving AdminConsole.asMemberOptions().");
    return authorizationServers.GROUPS.map(function (group) {
      const inGroup = authorizationServers.MEMBERS.filter(function (row) {
        return row.group === group;
      });
      return '<optgroup label="' + self.esc(group) + '">' +
             inGroup.map(function (row) {
        return '<option value="' + self.esc(row.name) + '"' +
          (row.name === selected ? ' selected' : '') + '>' +
          self.esc(row.name) +
               '</option>';
      }).join('') + '</optgroup>';
    }).join('');
  }

  asListPage(req) {
    const { log, adminViews, asDriftRows, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.asListPage().");
    const view = adminViews.asListJson(req);
    const all = view.all;
    const paged = view.paged;
    const paging = view.paging;
    const nav = this.pageNavPair('/admin/authorization-servers',
                        { per: req.query.per ? paging.perPage : '' }, paging);

    const listView = this.listViewOf('/admin/authorization-servers', req.query);
    const rows = paged.shown.map(function (row) {
      const drift = asDriftRows(row.id);
      // The link carries the list AS IT IS BEING VIEWED, which is what lets the
      // trail on the other side come back to this page of this filter rather
      // than to the top of everything. See listViewOf().
      const href = '/admin/authorization-servers' +
                   queryWith(listView, { profile: row.id });
      return '<tr><td><a href="' + self.esc(href) + '"><code>' +
             self.esc(row.id) +
        '</code></a></td><td>' + self.esc(row.label || '') + '</td><td ' +
        'class="num">' + Object.keys(row.overrides).length + '</td>' +
        '<td class="num">' + row.removed.length + '</td>' +
        '<td class="num">' + (drift.length
          ? '<span class="state-expired" title="Members whose published ' +
            'value disagrees with what this service would ' +
            'publish.">' + drift.length + '</span>'
          : '<span class="state-none">0</span>') + '</td>' +
        '<td><code>' + self.esc(row.urls.authorize) + '</code><br><code>' +
        self.esc(
            row.urls.token) +
        '</code><div class="sub">metadata at <code>' + self.esc(
            row.urls.oidc) + '</code></div></td><td>' + (row.autoCreated
          ? '<span class="sub">asked for</span>' : '<span ' +
                                                   'class="sub">configured' +
                                                   '</span>') +
        '</td><td class="num">' + self.esc(row.seen) + '</td></tr>';
    }).join('');

    const inner = this.messagesOf(req) +
      '<div class="tiles">' +
      this.tile(all.length, 'Profiles') +
      this.tile(all.reduce(function (n, r) {
        return n + Object.keys(r.overrides).length;
      },
                           0), 'Overrides') +
      this.tile(all.reduce(function (n,
                                     r) { return n +
                                          asDriftRows(r.id).length; },
                           0), 'Drifting ' +
          'members') +
      '</div>' +
      this.note('<strong>One process, several authorization ' +
      'servers.</strong> The path component the two discovery shapes already ' +
      'carry now selects a CONFIGURATION as well as an issuer identifier ' +
      '&mdash; RFC 8414 section 3.1 <em>inserts</em> it after the well-known ' +
      'segment and OpenID Connect Discovery section 4 <em>appends</em> the ' +
      'well-known segment to it, which is the commonest reason a discovery ' +
      'fetch 404s, and this service has answered both for a long time. ' +
      '<strong>A path nobody has configured publishes the document this ' +
      'service always published</strong>, so nothing that worked before this ' +
      'page existed behaves differently.') +
      nav.head +
      '<table><tr><th>Authorization server</th><th>Label</th><th ' +
      'class="num">Overrides</th><th class="num">Removed</th><th ' +
      'class="num">Drift</th><th>Its endpoints</th><th>Came from</th><th ' +
      'class="num">Asked for</th></tr>' +
      (rows || '<tr><td colspan="8">No authorization server has been named. ' +
               'Every discovery URL answers with the document this service ' +
               'builds for itself, which is what RFC 9700 section 2.6 asks ' +
               'for &mdash; these are for when you need it to say something ' +
               'else.</td></tr>') +
      '</table>' +
      nav.foot +
      '<h2>Add an authorization server</h2><form method="post" ' +
      'action="/admin/authorization-servers"><div class="formrow"><input ' +
      'type="hidden" name="action" value="create"><label ' +
      'for="asid">Id</label><input type="text" id="asid" name="id" size="18" ' +
      'required placeholder="tenant1"><label ' +
      'for="aslabel">Label</label><input type="text" id="aslabel" ' +
      'name="label" size="20" placeholder="optional"><label ' +
      'for="asdesc">Note</label><input type="text" id="asdesc" ' +
      'name="description" size="28" placeholder="what it is for"><button ' +
      'type="submit">Add</button></div></form>' +
      this.note('The id is a single URL path segment &mdash; letters, ' +
      'digits, dot, dash, underscore or tilde &mdash; because it has to ' +
      'appear in a URL without being escaped. One that had to be escaped ' +
      'would be one nobody could find again.') +
      AS_CAVEAT + AS_LINKS;

    log.debug("Leaving AdminConsole.asListPage(). " + paged.shown.length +
              " row(s).");
    return {
      inner: inner,
      json: view.json
    };
  }

  asDetailPage(req, id) {
    const { log, adminViews, queryWith, authorizationServers } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.asDetailPage(). id=" + id);
    const view = adminViews.asDetailJson(req, id);
    const profile = view.profile;
    const carryBack = '<input type="hidden" name="back" value="' +
      this.esc(queryWith(this.listViewOf('/admin/authorization-servers',
                                         req.query), {})) +
      '">';
    if (!profile) {
      log.debug("Leaving AdminConsole.asDetailPage(). No such profile.");
      return {
        missing: true,
        inner: this.messagesOf(req) +
          '<p class="warn">There is no authorization server profile called ' +
          '<code>' + this.esc(id) +
          '</code>. Its discovery URLs still answer &mdash; with the ' +
          'document this service builds and the issuer taken from the path ' +
          '&mdash; because an unconfigured path component has always been ' +
          'served that way rather than 404\'d.</p>' + AS_LINKS,
        json: view.json
      };
    }
    const drift = view.drift;
    const capabilities = view.capabilities;
    const memberRows = Object.keys(profile.overrides)
                             .sort()
                             .map(function (member) {
      const spec = authorizationServers.MEMBERS.filter(function (row) {
        return row.name === member;
      })[0];
      const bad = drift.filter(function (d) { return d.member === member; })[0];
      return '<tr><td><code>' + self.esc(member) + '</code></td>' +
        '<td><code>' + self.esc(JSON.stringify(profile.overrides[member])) +
        '</code></td><td>' + (bad
          ? '<span class="state-expired">' + self.esc(bad.kind) +
            '</span><div ' +
              'class="sub">' +
            self.esc(bad.what) + '</div>'
          : '<span class="state-valid">agrees</span>') + '</td>' +
        '<td class="sub">' + self.esc(spec ? spec.what : 'Not a member this ' +
          'service recognises — which is allowed, and is half the point: ' +
          'publishing something a client did not expect is what this page is ' +
          'for.') + '</td>' +
        '<td><form method="post" action="/admin/authorization-servers">' +
        carryBack +
        '<input type="hidden" name="action" value="reset">' +
        '<input type="hidden" name="profile" value="' + self.esc(id) + '">' +
        '<input type="hidden" name="member" value="' + self.esc(member) + '">' +
        '<button type="submit">Reset</button></form></td></tr>';
    }).join('');

    const removedRows = profile.removed.map(function (member) {
      return '<tr><td><code>' + self.esc(member) + '</code></td><td ' +
        'class="sub">Not published at all. A client reading this document ' +
        'cannot tell that this server supports it &mdash; which is not the ' +
        'same as learning that it does not, and is the difference RFC 9700 ' +
        'section 2.6 is arguing about.</td><td><form method="post" ' +
        'action="/admin/authorization-servers">' + carryBack +
        '<input type="hidden" name="action" value="reset">' +
        '<input type="hidden" name="profile" value="' + self.esc(id) + '">' +
        '<input type="hidden" name="member" value="' + self.esc(member) + '">' +
        '<button type="submit">Put it back</button></form></td></tr>';
    }).join('');

    const inner = this.messagesOf(req) +
      '<h2><code>' + this.esc(profile.id) + '</code>' +
      (profile.label ? ' &mdash; ' + this.esc(profile.label) : '') + '</h2>' +
      (profile.description ? this.note(this.esc(profile.description)) : '') +
      '<table><tr><th>Thing</th><th>Value</th></tr>' +
      '<tr><td>RFC 8414 document</td><td><a href="' +
      this.esc(profile.urls.oauth) +
      '"><code>' +
      this.esc(profile.urls.oauth) + '</code></a></td></tr>' +
      '<tr><td>OpenID Provider Configuration</td><td><a href="' +
      this.esc(profile.urls.oidc) +
      '"><code>' + this.esc(profile.urls.oidc) + '</code></a></td></tr>' +
      '<tr><td>Last changed</td><td><code>' + this.esc(profile.changedAt) +
      '</code></td></tr></table>' +
      (drift.length
        ? this.warn('<strong>' + drift.length + ' member(s) of this document ' +
          'do not describe this service.</strong> That is allowed and is ' +
          'often the point &mdash; but a client configured from this ' +
          'document will behave as though these were true.')
        : '<div class="ok">Every member of this document agrees with what ' +
          'this service would publish. A client configured from it is ' +
          'configured correctly.</div>') +
      '<h2>What this authorization server does</h2><p class="sub">Its ' +
      'effective capabilities — the defaults every authorization server here ' +
      'starts with, plus whatever this one has been given. This IS the ' +
      'document it publishes and it IS what its endpoints enforce; there is ' +
      'no second table that could disagree with ' +
      'it.</p><table><tr><th>Capability</th><th>This ' +
      'server</th><th>Enforced</th></tr>' +
      authorizationServers.MEMBERS
        .filter(function (row) { return row.enforces; })
        .map(function (row) {
          const value = capabilities[row.name];
          return '<tr><td><code>' + self.esc(row.name) + '</code></td>' +
            '<td>' + (value === undefined
              ? '<span class="state-none">not published — the check does not ' +
                'run</span>'
              : '<code>' + self.esc(JSON.stringify(value)) + '</code>') +
                '</td>' +
            '<td class="sub">' + self.esc(row.enforces) + '</td></tr>';
        }).join('') +
      '</table><h2>Overridden ' +
      'members</h2><table><tr><th>Member</th><th>Published ' +
      'as</th><th>Agreement</th><th>What it is</th><th></th></tr>' +
      (memberRows || '<tr><td colspan="5">Nothing is overridden, so this ' +
                     'document says exactly what this service says about ' +
                     'itself.</td></tr>') + '</table>' +
      (removedRows
        ? '<h2>Removed members</h2><table><tr><th>Member</th><th>What that ' +
          'means</th><th></th></tr>' +
          removedRows + '</table>'
        : '') +
      '<h2>Publish a member</h2>' +
      this.note('The value is read as JSON first and as a plain string if ' +
      'that fails, so <code>["S256"]</code> is a list, <code>false</code> is ' +
      'a boolean and <code>https://example.com/token</code> is a string. ' +
      '<strong>Any member name is accepted</strong> &mdash; the list below ' +
      'is help rather than a schema, and one this service has never heard of ' +
      'is published just the same.') +
      '<form method="post" action="/admin/authorization-servers">' + carryBack +
      '<div ' +
      'class="formrow"><input type="hidden" name="action" value="set"><input ' +
      'type="hidden" name="profile" value="' + this.esc(id) + '">' +
      '<label for="asmember">Member</label>' +
      '<select id="asmember" name="member">' + this.asMemberOptions('') +
      '</select><label ' +
      'for="asvalue">as</label><input type="text" id="asvalue" name="value" ' +
      'size="36" placeholder=\'["S256"]\'><button ' +
      'type="submit">Publish</button></div></form><form method="post" ' +
      'action="/admin/authorization-servers">' + carryBack + '<div ' +
      'class="formrow"><input type="hidden" name="action" value="set"><input ' +
      'type="hidden" name="profile" value="' + this.esc(id) + '">' +
      '<label for="asother">Or any member</label>' +
      '<input type="text" id="asother" name="member" size="30" required ' +
      'placeholder="a name this service has never heard of">' +
      '<label for="asothervalue">as</label>' +
      '<input type="text" id="asothervalue" name="value" size="26">' +
      '<button type="submit">Publish</button>' +
      '</div></form>' +
      '<h2>Stop publishing a member</h2>' +
      '<form method="post" action="/admin/authorization-servers">' + carryBack +
      '<div ' +
      'class="formrow"><input type="hidden" name="action" ' +
      'value="remove"><input type="hidden" name="profile" ' +
      'value="' + this.esc(id) + '">' +
      '<label for="asrem">Remove</label>' +
      '<select id="asrem" name="member">' +
      this.asMemberOptions('code_challenge_methods_supported') +
      '</select><button type="submit">Remove it from the ' +
      'document</button><span class="sub">Different from resetting it: reset ' +
      'undoes an override, this publishes an ' +
      'ABSENCE.</span></div></form><h2>Delete this authorization ' +
      'server</h2><form method="post" ' +
      'action="/admin/authorization-servers">' + carryBack + '<div ' +
      'class="formrow"><input type="hidden" name="action" ' +
      'value="delete"><input type="hidden" name="profile" ' +
      'value="' + this.esc(id) + '"><button ' +
      'type="submit" class="danger">Delete</button><span class="sub">The two ' +
      'URLs go on answering &mdash; with this service\'s own document ' +
      '&mdash; because an unconfigured path has always been served that ' +
      'way.</span></div></form>' +
      AS_CAVEAT + AS_LINKS;

    log.debug("Leaving AdminConsole.asDetailPage(). " + drift.length +
              " drifting member(s).");
    return { inner: inner, json: view.json };
  }

  authorizationServersView(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.authorizationServersView().");
    const wanted = String(req.query.profile || '').trim();
    if (wanted) {
      const detail = this.asDetailPage(req, wanted);
      log.debug("Leaving AdminConsole.authorizationServersView(). The " +
                "drill-down.");
      return { missing: !!detail.missing, json: detail.json,
               inner: detail.inner,
               title: 'Authorization server ' + wanted,
               up: this.upTo('/admin/authorization-servers', wanted,
                             this.listViewOf('/admin/authorization-servers',
                                             req.query)) };
    }
    const list = this.asListPage(req);
    log.debug("Leaving AdminConsole.authorizationServersView(). The list.");
    return { json: list.json, inner: list.inner,
             title: 'Authorization servers' };
  }

  // The settings that decide what a service provider receives are DRAWN ON THIS
  // PAGE, and until 2026-08-27 they were readings here with a link to
  // /admin/config. What changed is not the rule they were following — one
  // store, one function — but where the door is:
  // `configFormsFor('/admin/saml2')` posts `set-many` to that same endpoint
  // against that same override map, exactly as /admin/token-lifetimes has since
  // it was written. See SETTING_HOMES, which is where the keys are now named; a
  // list of them here would be the second list that disagrees with the first.
  //
  // This page draws TWO groups, and the second is worth knowing about: `SAML`
  // holds `saml.issuer`, the Issuer of every assertion this service builds —
  // 2.0, 1.1 and WS-Federation's, which come out of the same two functions — so
  // it is drawn on the SAML 1.1 page as well and configFormsFor() says so on
  // both.

  saml2ListPage(req) {
    const { log, adminViews, saml2Facts, queryWith, SAML2_SP_KIND } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.saml2ListPage().");
    // ONE PASS OVER THE REGISTRY, TWO RENDERINGS — admin-core/admin_views.ts.
    const view = adminViews.saml2ListJson(req);
    const base = view.base;
    const all = view.all;
    const needle = view.needle;
    const filtered = view.filtered;
    const paged = view.paged;
    const paging = view.paging;
    const filterParams = view.filterParams;
    const nav = this.pageNavPair('/admin/saml2', filterParams, paging);
    const listView = this.listViewOf('/admin/saml2', req.query);
    const rows = paged.shown.map(function (row) {
      const facts = saml2Facts(base, row.identifier);
      const href = '/admin/saml2' + queryWith(listView, { sp: row.identifier });
      const acs = row.fields.samlAssertionConsumerService;
      const slo = row.fields.samlSingleLogoutService;
      return '<tr><td><a href="' + self.esc(href) + '"><code>' +
             self.esc(row.identifier) +
        '</code></a><div ' +
        'class="sub">its identity provider: ' +
        '<code>' + self.esc(facts.idpEntityId) + '</code></div></td><td><a ' +
        'href="' + self.esc(facts.metadataUrl) + '">metadata</a></td>' +
        '<td>' +
        (acs ? self.codeList(Array.isArray(acs) ? acs : [acs]) : '<span ' +
            'class="sub">none seen</span>') +
        '</td>' +
        '<td>' + (slo ? self.codeList(Array.isArray(slo) ? slo : [slo])
                      : '<span class="sub">not declared &mdash; ' +
                        'guessed</span>') +
        '</td><td>' +
        self.esc(String(row.fields.samlAuthnRequestVerification || '')
                   .split(' ')[0] || '—') +
        '</td><td>' + self.esc(String(row.authentications)) + '</td><td>' +
        self.esc(row.lastSeen ? row.lastSeen.replace('T', ' ').slice(0, 19) :
                 '') +
        '</td></tr>';
    }).join('');

    const inner = '<h1>SAML 2.0 identity provider</h1><p class="sub">The Web ' +
      'Browser SSO profile, all three bindings, and Single Logout. This page ' +
      'holds nothing: every row is an entry in ' +
      '<code>ou=applications</code>.</p>' +
      this.note('<strong>Every service provider gets its own metadata ' +
      'document.</strong> The identity provider names itself differently to ' +
      'each one and publishes endpoints scoped to it, which is what Okta and ' +
      'Ping do. <strong>And it is minted for anything asked for</strong> — a ' +
      'service provider does not have to appear here before it can be ' +
      'pointed at this service, because asking for its metadata is what ' +
      'creates it. The unscoped document at <a ' +
      'href="/saml2/metadata">/saml2/metadata</a> works too and names one ' +
      'identity provider for everybody.') +
      '<p class="sub"><a href="/saml2">what the profile is</a> &middot; <a ' +
      'href="/saml2/sp">the mock service provider</a> &middot; <a ' +
      'href="/admin/saml-attributes">what goes into an assertion</a> ' +
      '&middot; <a ' +
      'href="/admin/applications?kind=' + SAML2_SP_KIND + '">these entries ' +
      'on the applications page</a></p><form method="get" ' +
      'action="/admin/saml2"><div class="formrow"><label ' +
      'for="q">Search</label><input type="text" id="q" name="q" ' +
      'value="' + this.esc(String(req.query.q || '')) + '" ' +
      'placeholder="an entityID or a name">' +
      (req.query.per ?
       '<input type="hidden" name="per" value="' + this.esc(paging.perPage) +
       '">' :
       '') +
      '<button class="secondary">Filter</button>' +
      (String(req.query.q || '') ? ' <a href="/admin/saml2">clear</a>' : '') +
      '</div></form>' +
      nav.head +
      (rows
        ? '<table><thead><tr><th>Service provider (entityID)</th><th>Its ' +
          'metadata</th><th>Assertion consumer service</th><th>Single logout ' +
          'service</th><th>Last request\'s signature</th><th>Responses</th>' +
          '<th>Last ' +
          'seen</th></tr></thead><tbody>' + rows + '</tbody></table>' + nav.foot
        : this.note('No service provider has used this profile yet' +
          (needle ? ' under that filter' : '') + '. Start one at <a ' +
          'href="/saml2/sp">the mock service provider</a>, or register an ' +
          'entityID below.')) +
      '<h2>Register a service provider</h2><p class="sub">Optional, and it ' +
      'changes nothing about whether a request is accepted — an entityID is ' +
      'accepted whether or not it is here. What it buys is a metadata ' +
      'document to hand somebody before they have sent anything.</p><form ' +
      'method="post" action="/admin/saml2"><div class="formrow"><input ' +
      'type="hidden" name="action" value="register"><label ' +
      'for="new_sp">entityID</label><input type="text" id="new_sp" name="sp" ' +
      'placeholder="https://sp.example.com/saml"><button>Register</button>' +
      '<span class="note">The same thing a request or a metadata fetch would ' +
      'do.</span></div></form>' +
      '<h2>Import one from the Metadata Query responder</h2><p ' +
      'class="sub">Asks <code>saml2.mdqBaseUrl</code> for the entity by ' +
      'name (<code>&lt;base&gt;/entities/&lt;entityID&gt;</code>), creates ' +
      'the entry if the answer describes it, and consumes the document — ' +
      'held to the realm\'s metadata trust anchors when it has any. A ' +
      'request from a service provider with no metadata starts the same ' +
      'lookup in the background.</p><form method="post" ' +
      'action="/admin/saml2"><div class="formrow"><input type="hidden" ' +
      'name="action" value="mdq-import"><label for="mdq_sp">entityID</label>' +
      '<input type="text" id="mdq_sp" name="sp" ' +
      'placeholder="https://sp.example.com/saml"><button>Import</button>' +
      '</div></form>' +
      this.configFormsFor('/admin/saml2') +
      this.note('These decide the SHAPE of an assertion — who issued it, how ' +
      'long it is good for, what is signed. <a ' +
      'href="/admin/saml-attributes">Custom SAML attributes</a> is the page ' +
      'that changes what one CONTAINS, and its SAML 2.0 set reaches this ' +
      'profile through the same assertion builder that serves WS-Trust and ' +
      'WS-Federation.') +
      this.perPageForm('/admin/saml2', 'q', String(req.query.q || ''),
                       paging.perPage,
                       '', {});

    log.debug("Leaving AdminConsole.saml2ListPage(). " + paged.shown.length +
              " row(s) of " +
              filtered.length + ".");
    return {
      inner: inner,
      json: view.json
    };
  }

  saml2DetailPage(req, identifier) {
    const { log, adminViews, queryWith, config, valuesFor } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.saml2DetailPage(). identifier=" +
              identifier);
    const view = adminViews.saml2DetailJson(req, identifier);
    const base = view.base;
    const facts = view.facts;
    const row = view.row;
    const fields = view.fields;
    const acs = view.acs;
    const slo = view.slo;
    const verification = view.json.lastRequestVerification;
    const required = view.json.signedRequestsRequired;
    const listView = this.listViewOf('/admin/saml2', req.query);
    const carryBack = '<input type="hidden" name="back" value="' +
      this.esc(queryWith(listView, {})) + '">';

    const endpointRows = [
      ['entityID of the identity provider', facts.idpEntityId,
       config.value('saml2.perApplicationEntityId')
         ? 'Unique to this service provider. saml2.perApplicationEntityId ' +
           'turns that off, and then every document names the same identity ' +
           'provider.'
         : 'The same for every service provider, because ' +
           'saml2.perApplicationEntityId is off. The ENDPOINTS below are ' +
           'still this service provider\'s own.'],
      ['Metadata', facts.metadataUrl, 'Signed, and served no-store because ' +
       'the signing key is regenerated on every start. This is the URL to ' +
       'configure the service provider from.'],
      ['Single Sign-On', facts.ssoUrl, 'HTTP Redirect and HTTP POST both. ' +
       'Which binding the RESPONSE comes back on is the AuthnRequest\'s own ' +
       'ProtocolBinding.'],
      ['Single Logout', facts.sloUrl, 'A LogoutRequest arriving from this ' +
       'service provider, and a bare GET to start one from here.'],
      ['Artifact Resolution', facts.arsUrl, 'SOAP over HTTP, and a back ' +
       'channel: the browser never touches it. An artifact resolves exactly ' +
       'once.']
    ].map(function (r) {
      return '<tr><td>' + self.esc(r[0]) + '</td><td><code>' + self.esc(r[1]) +
        '</code></td><td ' +
        'class="sub">' + self.esc(r[2]) + '</td></tr>';
    }).join('');

    const inner = '<h1><code>' + this.esc(identifier) + '</code></h1>' +
      '<p class="sub">A SAML 2.0 service provider. Its entry is ' +
      (row ?
       '<a href="/admin/applications?application=' +
             encodeURIComponent(identifier) +
             '">in the applications registry</a>'
           : 'NOT in the registry yet — this page is showing what it WOULD ' +
             'be given') + '.</p><h2>The endpoints it is configured from</h2>' +
      '<table><thead><tr><th>What</th><th>Where</th><th></th></tr></thead>' +
      '<tbody>' +
      endpointRows + '</tbody></table>' +
      '<p class="sub">The path segment is <code>' + this.esc(facts.slug) +
      '</code>' +
      (facts.slug === identifier ? '' :
        ', which is a digest of the entityID because the entityID is not ' +
        'safe in a URL path segment. The percent-encoded entityID works in ' +
        'the same place') + '.</p><h2>What ' +
      'this service has recorded</h2><table><tbody><tr><td>Assertion ' +
      'consumer services seen</td><td>' +
        (acs.length ? this.codeList(acs) : '<span class="sub">none</span>') +
      '</td></tr><tr><td>NameID ' +
      'formats asked for</td><td>' +
        (valuesFor(fields.samlNameIdFormat).length
          ? this.codeList(valuesFor(fields.samlNameIdFormat))
          : '<span class="sub">none — it has never named one, so it gets ' +
            'saml2.nameIdFormat</span>') +
        '</td></tr>' +
      '<tr><td>Response bindings asked for</td><td>' +
        (valuesFor(fields.samlResponseBinding).length
          ? this.codeList(valuesFor(fields.samlResponseBinding)) : '<span ' +
              'class="sub">none</span>') +
        '</td></tr>' +
      '<tr><td>Its last AuthnRequest\'s signature</td><td>' +
        (verification.outcome
          ? '<strong>' + this.esc(verification.outcome) + '</strong>' +
            (verification.binding
              ? ' <span class="sub">(' + this.esc(verification.binding) +
                ' binding, ' + this.esc(verification.signatureMethod ||
                                        'no SigAlg') +
                (verification.weak ? ', SHA-1 — weak' : '') + ')</span>'
              : '')
          : '<span class="sub">unknown</span>') +
        ' <span class="sub">&mdash; VERIFIED against the registered signing ' +
        'certificates below, never against the one a request carries. ' +
        '<code>no-certificate</code> means it was signed and nothing is ' +
        'registered to check it against.</span></td></tr>' +
        '<tr><td>Signed requests required</td><td>' +
        (required.required ? '<strong>yes</strong>' : 'no') +
        ' <span class="sub">&mdash; ' + this.esc(required.why) +
        '. An unsigned AuthnRequest or LogoutRequest is refused when this ' +
        'is yes; a signature that does not verify is refused ' +
        'always.</span></td></tr><tr><td>Responses issued to ' +
        'it</td><td>' + this.esc(String((row && row.authentications) || 0)) +
        '</td></tr>' +
      '</tbody></table>' +
      '<h2>Where its LogoutResponse goes</h2>' +
      this.note('A <code>&lt;samlp:LogoutRequest&gt;</code> carries no ' +
      'return address — only SP metadata does. The SingleLogoutService ' +
      'endpoints of its CONSUMED metadata (below) are used first; with none, ' +
      'what is declared here; then <code>saml2.defaultSingleLogoutService' +
      '</code>; and then the assertion consumer service URL this service ' +
      'provider last used, <strong>which is a guess and is logged as ' +
      'one</strong>. Consuming its metadata, or declaring an address here, ' +
      'removes the guess.') +
      (slo.length
        ? '<table><thead><tr><th>Declared</th><th></th></tr></thead><tbody>' +
          slo.map(function (one) {
            return '<tr><td><code>' + self.esc(one) + '</code></td><td>' +
              '<form method="post" action="/admin/saml2">' + carryBack +
              '<input type="hidden" name="action" ' +
              'value="remove-logout-service"><input type="hidden" name="sp" ' +
              'value="' + self.esc(identifier) + '">' +
              '<input type="hidden" name="value" value="' + self.esc(one) +
              '"><button class="secondary">Remove</button></form></td></tr>';
          }).join('') + '</tbody></table>'
        : this.note('Nothing is declared, so the fallback above applies' +
          (acs.length ?
           ' — and it would guess <code>' + this.esc(acs[acs.length - 1]) +
           '</code>' :
           '') +
          '.')) +
      '<form method="post" action="/admin/saml2">' + carryBack + '<div ' +
      'class="formrow"><input type="hidden" name="action" ' +
      'value="set-logout-service"><input type="hidden" name="sp" ' +
      'value="' + this.esc(identifier) + '"><label ' +
      'for="slo">Add one</label><input type="text" id="slo" name="value" ' +
      'placeholder="https://sp.example.com/saml/slo"><button>Add</button>' +
      '<span ' +
      'class="note">Writes <code>samlSingleLogoutService</code> on the ' +
      'entry. An <code>ldapmodify</code> of the same attribute does exactly ' +
      'this.</span></div></form>' +
      this.saml2SigningCertificatesSection(identifier, view.json,
                                           carryBack) +
      this.saml2MetadataSection(identifier, view.json, carryBack) +
      '<p class="sub"><a ' +
      'href="' + this.esc(facts.metadataUrl) + '">its ' +
      'metadata</a> &middot; <a href="/saml2">the profile</a>' +
      (row ? ' &middot; <a href="/admin/applications?application=' +
             encodeURIComponent(identifier) + '">its registry entry, with ' +
                                              'every attribute</a>' : '') +
      '</p>';

    log.debug("Leaving AdminConsole.saml2DetailPage().");
    return {
      inner: inner,
      json: view.json
    };
  }

  // ---------------------------------------------------------------------------
  // THE SIGNING CERTIFICATES (#37): what this service provider's signatures
  // are verified against, and the one a request carried that nobody has
  // vouched for. Each registered value has a Remove; the observed one has
  // Confirm and Discard. Values on ONE entry, bounded by what a person or a
  // metadata document registered, so the list is drawn whole — the same as
  // the endpoint lists above it.
  // ---------------------------------------------------------------------------
  saml2SigningCertificatesSection(identifier, json, carryBack) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.saml2SigningCertificatesSection().");
    const hidden = function (action) {
      log.debug("Entering hidden().");
      log.debug("Leaving hidden().");
      return '<form method="post" action="/admin/saml2">' + carryBack +
        '<input type="hidden" name="action" value="' + action + '">' +
        '<input type="hidden" name="sp" value="' + self.esc(identifier) +
        '">';
    };
    const certs = json.signingCertificates || [];
    const shown = function (der) {
      log.debug("Entering shown().");
      log.debug("Leaving shown().");
      return '<pre>' + self.esc(String(der).replace(/(.{72})/g, '$1\n')) +
             '</pre>';
    };
    const html = '<h2>Its signing certificates</h2>' +
      this.note('What this service provider\'s AuthnRequests, ' +
      'LogoutRequests and LogoutResponses are <strong>VERIFIED</strong> ' +
      'against, in every mode — a signature that verifies against none of ' +
      'them is refused. Written by consuming its metadata, by the form ' +
      'below, or by confirming an observed certificate. The certificate a ' +
      'request carries in its <code>ds:KeyInfo</code> is never trusted by ' +
      'arriving: it is shown as OBSERVED until you confirm it. RSA ' +
      'certificates only, because the verifier here is RSA.') +
      (certs.length
        ? '<table><thead><tr><th>Registered</th><th></th></tr></thead>' +
          '<tbody>' + certs.map(function (der) {
            return '<tr><td>' + shown(der) + '</td><td>' +
              hidden('remove-signing-certificate') +
              '<input type="hidden" name="value" value="' + self.esc(der) +
              '"><button class="secondary">Remove</button></form></td></tr>';
          }).join('') + '</tbody></table>'
        : this.note('None registered, so a signed request from this service ' +
                    'provider is recorded as <code>no-certificate</code> ' +
                    'and not verified.')) +
      hidden('set-signing-certificate') + '<div class="formrow"><label ' +
      'for="cert">Replace them with</label><input type="text" id="cert" ' +
      'name="value" placeholder="base64 DER or PEM"><button>Set</button>' +
      '<span class="note">The list becomes this one certificate; empty ' +
      'clears it.</span></div></form>' +
      '<h3>Observed</h3>' +
      (json.observedSigningCertificate
        ? this.note('The last signed request carried this certificate, and ' +
          'it is not registered. It verifies <strong>nothing</strong>. ' +
          'Development mode encrypts an assertion to it when nothing else ' +
          'is on the entry; product does not. Confirm it only if it is ' +
          'genuinely this service provider\'s.') +
          shown(json.observedSigningCertificate) +
          '<div class="formrow">' + hidden('confirm-signing-certificate') +
          '<button>Confirm — trust it</button></form> ' +
          hidden('discard-signing-certificate') +
          '<button class="secondary">Discard</button></form></div>'
        : this.note('No request has carried a certificate that is not ' +
                    'already registered.'));
    log.debug("Leaving AdminConsole.saml2SigningCertificatesSection().");
    return html;
  }

  // ---------------------------------------------------------------------------
  // THE CONSUMED METADATA (#37): what the last refresh or upload registered,
  // and the upload form. The refresh button is on the application's own page,
  // beside the URL it dials, and this links there rather than drawing a second
  // copy of it.
  // ---------------------------------------------------------------------------
  saml2MetadataSection(identifier, json, carryBack) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.saml2MetadataSection().");
    const meta = json.metadata || {};
    const yes = function (flag) {
      log.debug("Entering yes().");
      log.debug("Leaving yes().");
      return flag ? 'true' : 'false';
    };
    const acsRows = (meta.assertionConsumerServices || []).map(function (e) {
      return '<tr><td>' + self.esc(e.index || '—') + '</td><td>' +
        (e.isDefault === true ? 'yes' : (e.isDefault === false ? 'no' : '—')) +
        '</td><td><code>' + self.esc(e.binding) + '</code></td><td><code>' +
        self.esc(e.location) + '</code></td></tr>';
    }).join('');
    const sloRows = (meta.singleLogoutServices || []).map(function (e) {
      return '<tr><td><code>' + self.esc(e.binding) + '</code></td><td><code>' +
        self.esc(e.location) + '</code></td><td>' +
        (e.responseLocation ? '<code>' + self.esc(e.responseLocation) +
                              '</code>' : '—') + '</td></tr>';
    }).join('');
    const facts = meta.consumed
      ? '<table><tbody>' +
        '<tr><td>Consumed</td><td>' + self.esc(meta.consumedAt) + ' (' +
          self.esc(meta.how) + ')' +
          (meta.url ? ' from <code>' + self.esc(meta.url) + '</code>' : '') +
          '</td></tr>' +
        '<tr><td>The document\'s own signature</td><td>' +
          self.esc(meta.signature || 'unknown') + '</td></tr>' +
        '<tr><td>State</td><td><strong>' +
          self.esc(String(meta.state || '').toUpperCase()) + '</strong>' +
          (meta.stateWhy ? ' <span class="sub">— ' + self.esc(meta.stateWhy) +
                           '</span>' : '') +
          (meta.state === 'expired'
            ? ' <span class="sub">Every request from this service provider ' +
              'is REFUSED until a newer document is consumed.</span>' : '') +
          '</td></tr>' +
        '<tr><td>validUntil (effective)</td><td>' +
          (meta.validUntil
            ? self.esc(meta.validUntil) + ' <span class="sub">— enforced: ' +
              'past it this service provider\'s requests are refused</span>'
            : '<span class="sub">none stated</span>') + '</td></tr>' +
        '<tr><td>cacheDuration (effective)</td><td>' +
          (meta.cacheDuration ? self.esc(meta.cacheDuration) : '<span ' +
           'class="sub">none stated</span>') +
          (meta.staleAt ? ' <span class="sub">— stale from ' +
                          self.esc(meta.staleAt) + '</span>' : '') +
          '</td></tr>' +
        '<tr><td>Background refresh</td><td>' +
          (!meta.refreshable
            ? '<span class="sub">not possible: the document was ' +
              'uploaded, and a stale one keeps working until its ' +
              'validUntil</span>'
            : (meta.refresherEnabled ? 'on' : '<strong>off</strong> ' +
               '(saml2.spMetadataRefresh)') +
              (meta.refresh
                ? ' — last attempt ' + self.esc(meta.refresh.lastAttemptAt) +
                  (meta.refresh.ok ? ', succeeded'
                    : ', <strong>FAILING</strong> since ' +
                      self.esc(meta.refresh.failingSince) + ' (' +
                      self.esc(String(meta.refresh.failures)) +
                      ' attempt(s)): ' + self.esc(meta.refresh.why))
                : ' <span class="sub">— not attempted in this ' +
                  'process</span>')) +
          '</td></tr>' +
        '<tr><td>AuthnRequestsSigned</td><td>' +
          yes(meta.authnRequestsSigned) + '</td></tr>' +
        '<tr><td>WantAssertionsSigned</td><td>' +
          yes(meta.wantAssertionsSigned) + '</td></tr>' +
        '<tr><td>Encrypted assertions wanted</td><td>' +
          yes(meta.wantAssertionsEncrypted) + ' <span class="sub">— true ' +
          'when the document publishes a use="encryption" key; the ' +
          'assertion is then encrypted to it in every mode</span></td></tr>' +
        '<tr><td>NameIDFormats</td><td>' +
          ((meta.nameIdFormats || []).length
            ? this.codeList(meta.nameIdFormats) + ' <span class="sub">— a ' +
              'NameIDPolicy asking for another is answered ' +
              'InvalidNameIDPolicy</span>'
            : '<span class="sub">none declared — any format asked for is ' +
              'answered</span>') + '</td></tr>' +
        '<tr><td>Encryption certificate</td><td>' +
          (meta.encryptionCertificate ? 'on the entry' : 'none') +
          '</td></tr>' +
        '</tbody></table>' +
        '<h3>Registered assertion consumer services</h3>' +
        (acsRows
          ? '<table><thead><tr><th>index</th><th>isDefault</th>' +
            '<th>Binding</th><th>Location</th></tr></thead><tbody>' +
            acsRows + '</tbody></table>'
          : this.note('The document registered none.')) +
        '<h3>Registered single logout services</h3>' +
        (sloRows
          ? '<table><thead><tr><th>Binding</th><th>Location</th>' +
            '<th>ResponseLocation</th></tr></thead><tbody>' + sloRows +
            '</tbody></table>'
          : this.note('The document registered none.'))
      : this.note('No metadata has been consumed for this service provider, ' +
                  'so its return addresses and signing certificates are ' +
                  'whatever was recorded or declared above.');
    const html = '<h2>Its metadata</h2>' +
      this.note('Consuming a service provider\'s metadata REGISTERS what it ' +
      'says: its AssertionConsumerService endpoints (a request is answered ' +
      'only at one of them, in every mode), its SingleLogoutService ' +
      'endpoints, its signing certificates, its encryption certificate, its ' +
      'NameIDFormats, AuthnRequestsSigned and WantAssertionsSigned. It ' +
      'happens only when you refresh the URL on <a ' +
      'href="/admin/applications?application=' +
      encodeURIComponent(identifier) + '">its application page</a> or ' +
      'upload a document here — never while somebody is signing in.') +
      facts +
      '<form method="post" action="/admin/saml2" ' +
      'enctype="multipart/form-data">' + carryBack +
      '<input type="hidden" name="action" value="upload-metadata">' +
      '<input type="hidden" name="sp" value="' + self.esc(identifier) + '">' +
      '<div class="formrow"><label for="md-doc">Upload a document</label>' +
      '<textarea id="md-doc" name="document" rows="4" cols="60" ' +
      'placeholder="paste &lt;md:EntityDescriptor&gt;…"></textarea></div>' +
      '<div class="formrow"><label for="md-file">or a file</label>' +
      '<input type="file" id="md-file" name="file" accept=".xml,' +
      'application/samlmetadata+xml,application/xml,text/xml">' +
      '<button>Consume it</button><span class="note">Its entityID must be ' +
      'this service provider\'s. Nothing changes if it is refused.</span>' +
      '</div></form>' +
      '<form method="post" action="/admin/saml2">' + carryBack +
      '<div class="formrow"><input type="hidden" name="action" ' +
      'value="refresh-metadata"><input type="hidden" name="sp" value="' +
      self.esc(identifier) + '"><button>Refresh it now</button>' +
      '<span class="note">From its samlSpMetadataUrl, or — with none — ' +
      (meta.mdqUrl ? 'from the MDQ responder, <code>' +
                     self.esc(meta.mdqUrl) + '</code>'
                   : 'from the MDQ responder (saml2.mdqBaseUrl, not set ' +
                     'here)') + '.</span></div></form>' +
      '<h3>The certificate its metadata must be signed with</h3>' +
      this.note((meta.signingCertificateConfigured
        ? 'Set: a document that is unsigned, or not signed with this key ' +
          'or a realm trust anchor, is refused.'
        : (meta.trustAnchors
          ? 'Not set on the entry, and this realm has ' + meta.trustAnchors +
            ' metadata trust anchor(s) (saml2.metadataTrustAnchors): a ' +
            'document that verifies against none of them is refused.'
          : 'Not set, and no realm trust anchor: a signed document is ' +
            'consumed and recorded as <code>signed-not-verified</code>, and ' +
            'the trust act is your choice of URL or document.')) +
        ((meta.trustAnchorProblems || []).length
          ? ' <strong>' + self.esc(meta.trustAnchorProblems.join('; ')) +
            '.</strong>' : '')) +
      '<form method="post" action="/admin/saml2">' + carryBack +
      '<div class="formrow"><input type="hidden" name="action" ' +
      'value="set-metadata-signing-certificate"><input type="hidden" ' +
      'name="sp" value="' + self.esc(identifier) + '"><label ' +
      'for="md-cert">Metadata signing certificate</label><input type="text" ' +
      'id="md-cert" name="value" placeholder="base64 DER or PEM">' +
      '<button>Set</button><span class="note">Empty clears it.</span>' +
      '</div></form>';
    log.debug("Leaving AdminConsole.saml2MetadataSection().");
    return html;
  }

  saml2View(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.saml2View().");
    const wanted = String(req.query.sp || '').trim();
    if (wanted) {
      const detail = this.saml2DetailPage(req, wanted);
      log.debug("Leaving AdminConsole.saml2View(). The drill-down.");
      return { json: detail.json, inner: detail.inner,
               title: 'SAML 2.0 service provider ' + wanted,
               up: this.upTo('/admin/saml2', wanted,
                             this.listViewOf('/admin/saml2', req.query)) };
    }
    const list = this.saml2ListPage(req);
    log.debug("Leaving AdminConsole.saml2View(). The list.");
    return { json: list.json, inner: list.inner, title: 'SAML 2.0 identity ' +
                                                        'provider' };
  }

  // Which of the two browser profiles a recorded binding value names, in words.
  // The registry holds the profile URI, which is what the metadata publishes
  // and what a person reading a table does not want to compare character by
  // character.
  saml11ProfileLabel(value) {
    const { log, saml11 } = this.deps;
    log.debug("Entering AdminConsole.saml11ProfileLabel().");
    if (value === saml11.PROFILE_POST) {
      log.debug("Leaving AdminConsole.saml11ProfileLabel().");
      return 'Browser/POST';
    }
    if (value === saml11.PROFILE_ARTIFACT) {
      log.debug("Leaving AdminConsole.saml11ProfileLabel().");
      return 'Browser/Artifact';
    }
    log.debug("Leaving AdminConsole.saml11ProfileLabel().");
    return value;
  }

  // The settings that decide what a relying party receives are DRAWN ON THIS
  // PAGE, for the reason the SAML 2.0 page's equivalent comment gives and
  // through the same function. The `SAML` group appears on both pages because
  // `saml.issuer` governs both profiles; configFormsFor() says so where it
  // draws it, so a reader who sets it here is told it is the same value the 2.0
  // page shows.

  saml11ListPage(req) {
    const { log, adminViews, saml11Facts, queryWith, valuesFor, saml11,
            SAML11_RP_KIND } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.saml11ListPage().");
    const view = adminViews.saml11ListJson(req);
    const base = view.base;
    const all = view.all;
    const needle = view.needle;
    const filtered = view.filtered;
    const paged = view.paged;
    const paging = view.paging;
    const filterParams = view.filterParams;
    const nav = this.pageNavPair('/admin/saml11', filterParams, paging);
    const listView = this.listViewOf('/admin/saml11', req.query);

    const rows = paged.shown.map(function (row) {
      const facts = saml11Facts(base, row.identifier);
      const href = '/admin/saml11' +
                   queryWith(listView, { rp: row.identifier });
      const acs = row.fields.samlAssertionConsumerService;
      const profiles = valuesFor(row.fields.samlResponseBinding)
        .filter(function (v) {
          return v === saml11.PROFILE_POST || v === saml11.PROFILE_ARTIFACT;
        })
        .map(self.saml11ProfileLabel.bind(self));
      return '<tr><td><a href="' + self.esc(href) + '"><code>' +
             self.esc(row.identifier) +
        '</code></a><div ' +
        'class="sub">its identity provider: ' +
        '<code>' + self.esc(facts.idpProviderId) + '</code></div></td><td><a ' +
        'href="' + self.esc(facts.metadataUrl) + '">metadata</a></td>' +
        '<td>' + (acs ? self.codeList(Array.isArray(acs) ? acs : [acs])
                      : '<span class="sub">none seen</span>') + '</td>' +
        '<td>' + (profiles.length ? self.esc(profiles.join(', '))
                                  : '<span class="sub">none &mdash; it has ' +
                                    'only ever been handed a 1.1 assertion ' +
                                    'through another door</span>') +
                                    '</td><td>' +
        self.esc(String(row.authentications)) + '</td><td>' +
        self.esc(row.lastSeen ? row.lastSeen.replace('T', ' ').slice(0, 19) :
                 '') +
        '</td></tr>';
    }).join('');

    const inner = '<h1>SAML 1.1 identity provider</h1><p class="sub">Both ' +
      'browser profiles, and the SAML responder behind one of them. This ' +
      'page holds nothing: every row is an entry in ' +
      '<code>ou=applications</code>.</p>' +
      this.note('<strong>SAML 1.1 has no request message</strong>, which is ' +
      'where most of the differences from <a href="/admin/saml2">the SAML ' +
      '2.0 page</a> come from. A relying party cannot identify itself in the ' +
      'protocol, so it is named by Shibboleth\'s <code>providerId</code> ' +
      'parameter, by the path segment of a scoped endpoint, or it is GUESSED ' +
      'from the origin of the <code>TARGET</code>. There is also no Single ' +
      'Logout to configure &mdash; it arrived with SAML 2.0 &mdash; and no ' +
      'request signature to record.') +
      this.note('<strong>Every relying party gets its own metadata ' +
      'document</strong>, minted for anything asked for, exactly as the 2.0 ' +
      'profile does. It is a SAML 2.0 metadata document describing a SAML ' +
      '1.1 identity provider, which is what every relying party actually ' +
      'consumes: SAML 1.1 never had a metadata specification. The unscoped ' +
      'document at <a href="/saml11/metadata">/saml11/metadata</a> works too ' +
      'and names one identity provider for everybody.') +
      '<p class="sub"><a href="/saml11">what the profile is</a> &middot; <a ' +
      'href="/saml11/rp">the mock relying party</a> &middot; <a ' +
      'href="/admin/saml-attributes">what goes into an assertion</a> ' +
      '&middot; <a ' +
      'href="/admin/applications?kind=' + SAML11_RP_KIND + '">these entries ' +
      'on the applications page</a></p><form method="get" ' +
      'action="/admin/saml11"><div class="formrow"><label ' +
      'for="q">Search</label><input type="text" id="q" name="q" ' +
      'value="' + this.esc(String(req.query.q || '')) + '" ' +
      'placeholder="an identifier or a name">' +
      (req.query.per ?
       '<input type="hidden" name="per" value="' + this.esc(paging.perPage) +
       '">' :
       '') +
      '<button class="secondary">Filter</button>' +
      (String(req.query.q || '') ? ' <a href="/admin/saml11">clear</a>' : '') +
      '</div></form>' +
      nav.head +
      (rows
        ? '<table><thead><tr><th>Relying party</th><th>Its metadata</th>' +
          '<th>Assertion consumer (shire)</th><th>Profiles used</th>' +
          '<th>Assertions</th><th>Last seen</th></tr></thead><tbody>' + rows +
          '</tbody></table>' + nav.foot
        : this.note('No relying party has taken a SAML 1.1 assertion yet' +
          (needle ? ' under that filter' : '') + '. Start one at <a ' +
          'href="/saml11/rp">the mock relying party</a>, or register an ' +
          'identifier below.')) +
      '<h2>Register a relying party</h2><p class="sub">Optional, and it ' +
      'changes nothing about whether a flow is accepted &mdash; any ' +
      'identifier is accepted whether or not it is here. What it buys is a ' +
      'metadata document to hand somebody before they have sent anything, ' +
      'and a name to use in <code>providerId</code> so that nothing has to ' +
      'be guessed.</p><form method="post" action="/admin/saml11"><div ' +
      'class="formrow"><input type="hidden" name="action" ' +
      'value="register"><label for="new_rp">Identifier</label><input ' +
      'type="text" id="new_rp" name="rp" ' +
      'placeholder="urn:example:app"><button>Register</button><span ' +
      'class="note">The same thing a flow or a metadata fetch would ' +
      'do.</span></div></form>' +
      this.configFormsFor('/admin/saml11') +
      this.note('These decide the SHAPE of an assertion. <a ' +
      'href="/admin/saml-attributes">Custom SAML attributes</a> is the page ' +
      'that changes what one CONTAINS, and its SAML 1.1 set reaches this ' +
      'profile through the same assertion builder that serves WS-Trust and ' +
      'WS-Federation.') +
      this.perPageForm('/admin/saml11', 'q', String(req.query.q || ''),
                       paging.perPage,
                       '', {});

    log.debug("Leaving AdminConsole.saml11ListPage(). " + paged.shown.length +
              " row(s) of " +
              filtered.length + ".");
    return {
      inner: inner,
      json: view.json
    };
  }

  saml11DetailPage(req, identifier) {
    const { log, adminViews, config, valuesFor } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.saml11DetailPage(). identifier=" +
              identifier);
    const view = adminViews.saml11DetailJson(req, identifier);
    const base = view.base;
    const facts = view.facts;
    const row = view.row;
    const fields = view.fields;
    const acs = view.acs;
    const profiles = view.profiles;
    const endpointRows = [
      ['providerID of the identity provider', facts.idpProviderId,
       config.value('saml11.perApplicationProviderId')
         ? 'Unique to this relying party. saml11.perApplicationProviderId ' +
           'turns that off, and then every document names the same identity ' +
           'provider — and every artifact carries the same SourceID, because ' +
           'that is a hash of this value.'
         : 'The same for every relying party, because ' +
           'saml11.perApplicationProviderId is off. The ENDPOINTS below are ' +
           'still this relying party\'s own.'],
      ['Metadata', facts.metadataUrl,
       'Signed, and served no-store because the signing key is regenerated ' +
       'on every start. This is the URL to configure the relying party from. ' +
       'It carries an IDPSSODescriptor for the browser profiles AND an ' +
       'AttributeAuthorityDescriptor for the responder, because a Shibboleth ' +
       'service provider looks for its attribute authority in the second.'],
      ['Inter-site transfer', facts.ssoUrl,
       'Where a browser is sent to be signed in. SAML 1.1\'s name for what ' +
       'SAML 2.0 calls the Single Sign-On service. Send TARGET, and shire ' +
       'for where the assertion goes.'],
      ['SAML responder', facts.responderUrl,
       'SOAP over HTTP POST, and a back channel: the browser never touches ' +
       'it. It resolves artifacts (once each), returns assertions by ' +
       'AssertionID, and answers AttributeQuery and AuthenticationQuery — ' +
       'which is SAML 1.1\'s attribute authority.']
    ].map(function (r) {
      return '<tr><td>' + self.esc(r[0]) + '</td><td><code>' + self.esc(r[1]) +
        '</code></td><td ' +
        'class="sub">' + self.esc(r[2]) + '</td></tr>';
    }).join('');

    // A relying party whose identifier looks like a bare origin is very likely
    // one this service GUESSED, and saying so here is the whole reason the
    // guess is survivable. It is a heuristic and is worded as one: somebody may
    // perfectly well have registered `https://app.example.com` on purpose.
    const looksGuessed = /^https?:\/\/[^/]+$/i.test(identifier);

    const inner = '<h1><code>' + this.esc(identifier) + '</code></h1>' +
      '<p class="sub">A SAML 1.1 relying party. Its entry is ' +
      (row ?
       '<a href="/admin/applications?application=' +
             encodeURIComponent(identifier) +
             '">in the applications registry</a>'
           : 'NOT in the registry yet — this page is showing what it WOULD ' +
             'be given') + '.</p>' +
      (looksGuessed
        ? this.note('<strong>This identifier is a bare origin, which is what ' +
          'this service writes down when nobody told it the relying party\'s ' +
          'name.</strong> SAML 1.1 has no request message, so a flow that ' +
          'sent no <code>providerId</code> and used no scoped endpoint ' +
          'leaves the audience to be guessed from the origin of the TARGET. ' +
          'The assertion is issued to THIS string, so a relying party ' +
          'expecting a different audience refuses it inside a signature ' +
          'check with nothing saying why. Send <code>providerId</code>, or ' +
          'use the scoped endpoint above, to make it exact.')
        : '') +
      '<h2>The endpoints it is configured from</h2>' +
      '<table><thead><tr><th>What</th><th>Where</th><th></th></tr></thead>' +
      '<tbody>' +
      endpointRows + '</tbody></table>' +
      '<p class="sub">The path segment is <code>' + this.esc(facts.slug) +
      '</code>' +
      (facts.slug === identifier ? '' :
        ', which is a digest of the identifier because the identifier is not ' +
        'safe in a URL path segment. The percent-encoded identifier works in ' +
        'the same place') +
      '. It is THE SAME SLUG <a href="/admin/saml2">the SAML 2.0 profile</a> ' +
      'uses for this application, deliberately: one application has one ' +
      'handle, or the console would show one entry as two.</p><h2>What this ' +
      'service has recorded</h2><table><tbody><tr><td>Assertion consumers ' +
      'seen</td><td>' +
        (acs.length ? this.codeList(acs) : '<span class="sub">none</span>') +
        ' <span class="sub">&mdash; the <code>shire</code> parameter. Not ' +
        'checked against any registration, like every other return URL ' +
        'here.</span></td></tr><tr><td>Browser profiles used</td><td>' +
        (profiles.length ?
         this.esc(profiles.map(this.saml11ProfileLabel.bind(this)).join(', '))
                         : '<span class="sub">none &mdash; it has only ever ' +
                           'been handed a 1.1 assertion through ' +
                           'WS-Federation or WS-Trust</span>') +
      '</td></tr><tr><td>NameIdentifier ' +
      'formats asked for</td><td>' +
        (valuesFor(fields.samlNameIdFormat).length
          ? this.codeList(valuesFor(fields.samlNameIdFormat))
          : '<span class="sub">none &mdash; and in this protocol that is the ' +
            'ordinary case, because there is no NameIDPolicy to ask in. It ' +
            'gets saml11.nameIdFormat unless the non-spec ' +
            '<code>format</code> parameter says otherwise.</span>') +
      '</td></tr><tr><td>Assertions ' +
      'issued to it</td><td>' +
      this.esc(String((row && row.authentications) || 0)) +
        '</td></tr></tbody></table><h2>What is NOT here, and why</h2><p ' +
      'class="sub">The SAML 2.0 page has three rows this one does not, and ' +
      'none of them is missing work. <strong>No logout return ' +
      'address</strong>: SAML 1.1 has no Single Logout, so there is nothing ' +
      'to send anywhere. <strong>No request signature and no signing ' +
      'certificate</strong>: there is no request, so there is nothing for a ' +
      'relying party to sign. <strong>No response binding</strong>: which of ' +
      'the two browser profiles is used is chosen HERE, by ' +
      '<code>saml11.defaultProfile</code> or the non-spec ' +
      '<code>profile</code> parameter, because nothing in SAML 1.1 lets a ' +
      'relying party ask.</p><p class="sub"><a href="/saml11/rp">The mock ' +
      'relying party</a> will run either profile against this service and ' +
      'show every check.</p>';

    log.debug("Leaving AdminConsole.saml11DetailPage().");
    return {
      inner: inner,
      json: view.json
    };
  }

  saml11View(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.saml11View().");
    const wanted = String(req.query.rp || '').trim();
    if (wanted) {
      const detail = this.saml11DetailPage(req, wanted);
      log.debug("Leaving AdminConsole.saml11View(). The drill-down.");
      return { json: detail.json, inner: detail.inner,
               title: 'SAML 1.1 relying party ' + wanted,
               up: this.upTo('/admin/saml11', wanted,
                             this.listViewOf('/admin/saml11', req.query)) };
    }
    const list = this.saml11ListPage(req);
    log.debug("Leaving AdminConsole.saml11View(). The list.");
    return { json: list.json, inner: list.inner, title: 'SAML 1.1 identity ' +
                                                        'provider' };
  }

  groupsPost(req, res, body, held) {
    const { log, groupsAction, queryWith } = this.deps;
    log.debug("Entering AdminConsole.groupsPost().");
    const result = groupsAction(body);
    held.settle(result.ok !== false);
    // WHERE THE READER GOES AFTERWARDS. A create lands on the group it just
    // made, because the next thing anybody does with a new group is put
    // somebody in it and that control is on the drill-down; an add-member stays
    // where it was, on the group it was adding to. A refusal goes back to the
    // page that asked, so the message is next to the form that produced it.
    const listView = this.listViewFromBack('/admin/groups', body.back);
    const landOn = result.ok !== false
      ? String(result.dn || '')
      : String(body.group && /,/.test(String(body.group)) ? body.group : '');
    const back = landOn
      ? '/admin/groups' + queryWith(listView, { group: landOn })
      : '/admin/groups' + queryWith(listView, {});
    this.respondToAction(req, res, back, result);
    log.debug("Leaving AdminConsole.groupsPost().");
  }

  // A membership value that names an entry which is not there. It is a normal
  // state here rather than a fault — see the grant form's note — so it is
  // marked and explained rather than hidden or repaired.
  rbacMemberCell(row, knownKeys) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.rbacMemberCell().");
    const name = this.esc(row.username || row.value);
    if (row.userKey && knownKeys[row.userKey]) {
      log.debug("Leaving AdminConsole.rbacMemberCell().");
      return '<a href="' +
             this.esc('/admin/users?user=' + encodeURIComponent(row.userKey)) +
             '">' +
             name + '</a>';
    }
    if (row.present) {
      log.debug("Leaving AdminConsole.rbacMemberCell().");
      // In the directory, but this service has never seen them authenticate.
      // The same distinction /admin/groups draws on its member rows, and drawn
      // the same way so the two pages cannot be read as disagreeing.
      return name + ' <span class="state-none" title="This person has an ' +
             'entry in the directory, but nothing here has authenticated as ' +
             'them yet, so there is no page about them on Users.">never ' +
             'here</span>';
    }
    log.debug("Leaving AdminConsole.rbacMemberCell().");
    return name + ' <span class="state-expired" title="Nothing is at this ' +
           'DN. The role still counts — it resolves the moment somebody ' +
           'authenticates under this name or the entry is created — but ' +
           'until then no directory client can see who it ' +
           'names.">dangling</span>';
  }

  // The mark on a row whose membership is on the PERSON'S entry rather than in
  // the group. It really grants the role — `groupsOfUser()` reads both
  // directions, so admin_rbac.js merges these in — which is why it is on this
  // list at all; and it cannot be taken away from here, so the row says that
  // too rather than offering a button that would report success and change
  // nothing.
  rbacClaimedMark(row) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.rbacClaimedMark().");
    if (row.kind !== 'claimed') {
      log.debug("Leaving AdminConsole.rbacClaimedMark().");
      return '';
    }
    log.debug("Leaving AdminConsole.rbacClaimedMark().");
    return ' <span class="state-expired" title="Their own entry&#39;s ' +
           'memberOf names this group and the group does not list them back. ' +
           'Nothing here maintains memberOf — a client wrote it — and it ' +
           'grants the role all the same, so it is on this list. The Revoke ' +
           'button cannot remove it: the value is on the person, and this ' +
           'console writes only to groups.">via their own memberOf</span>';
  }

  rbacListPage(req) {
    const { log, adminViews, queryWith, pageParamsOf, rbac,
            queryOne } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.rbacListPage().");
    // ONE COMPUTATION, TWO RENDERINGS (2026-09-12) — admin-core/admin_views.ts.
    // Everything this page draws comes off this call, and so does what
    // GET /admin-api/rbac answers.
    const view = adminViews.rbacListJson(req);
    const info = view.info;
    const state = view.state;
    const grants = view.grants;
    const wantedText = view.wantedText;
    const wantedRole = view.wantedRole;
    const paging = view.paging;
    const shown = view.shown;
    const filterParams = view.filterParams;
    const knownKeys = view.knownKeys;
    const candidates = view.candidates;
    const nav = this.pageNavPair('/admin/rbac', filterParams, paging);
    const carryBack = '<input type="hidden" name="back" value="' +
      this.esc(queryWith(this.listViewOf('/admin/rbac', req.query), {})) + '">';

    const rows = shown.map(function (row) {
      return '<tr><td>' + self.rbacMemberCell(row, knownKeys) +
             self.rbacClaimedMark(row) +
        '</td><td>' + self.esc(row.roleLabel) + '</td><td><a ' +
        'href="' + self.esc('/admin/groups?group=' +
                            encodeURIComponent(row.dn)) + '"><code>' +
          self.esc(row.dn) + '</code></a></td>' +
        '<td><code>' + self.esc(row.attribute) + '</code>: <code>' + self.esc(
            row.value) + '</code></td><td>' +
          (row.kind === 'claimed'
            ? '<span class="state-expired" title="The membership is on their ' +
              'own entry as memberOf, so there is nothing in the group to ' +
              'remove. An ldapmodify or a SCIM PATCH of the PERSON takes it ' +
              'away.">not from here</span>'
            : state.write
            ? '<form class="inline" method="post" action="/admin/rbac">' +
              '<input type="hidden" name="action" value="revoke">' +
              '<input type="hidden" name="username" value="' +
              self.esc(row.username) +
              '"><input ' +
              'type="hidden" name="role" ' +
              'value="' + self.esc(row.role) + '">' + carryBack +
              '<button class="danger">Revoke</button></form>'
            : '<span class="state-none">read-only</span>') +
        '</td></tr>';
    }).join('');

    // WHO CAN BE PICKED. `stats.userRows()` is who has authenticated and the
    // directory is who has an entry; admin_rbac.js unions them, because a list
    // built from either alone would silently omit half the people somebody
    // wants to grant a role to.
    //
    // **IT WAS A `<select>` OF ALL OF THEM UNTIL 2026-09-13 AND IS A SEARCH
    // NOW**, for the reason /admin/delegation's person chooser stopped being
    // one: a default realm bulk loaded with thousands of people made the select
    // a control nobody could scroll, find a name in, or load quickly. It is
    // chooserPane() — the same search, the same twenty-at-a-time pane, the same
    // clamped offset — and a RESULT IS A LINK that picks that person, which
    // opens the grant form for them below the pane. So granting is search,
    // click, choose the role, Grant: one step more than the select, and the one
    // that makes the list usable at any size.
    const whereFrom = function (row) {
      log.debug("Entering whereFrom().");
      log.debug("Leaving whereFrom().");
      return row.inDirectory && row.seen ? 'directory, and has signed in'
        : (row.inDirectory ? 'in the directory' : 'has signed in');
    };
    const pickCarry = pageParamsOf(req.query);
    delete pickCarry.person;
    const personPane = this.chooserPane({
      here: { path: '/admin/rbac', query: req.query },
      param: 'personq', fromParam: 'personfrom',
      label: 'Find a person',
      placeholder: 'part of a username',
      entries: candidates.map(function (row) {
        return {
          key: row.username.toLowerCase(),
          names: [row.username],
          label: row.username,
          detail: whereFrom(row),
          href: '/admin/rbac' + queryWith(pickCarry, { person: row.username }) +
                '#grant-picked'
        };
      }),
      selectedKey: view.picked ? view.picked.username.toLowerCase() : '',
      nothing: candidates.length
        ? 'Nobody in the directory or among the people who have signed in ' +
          'matches that. To grant a role to a name this service has never ' +
          'seen, use the form below the results.'
        : 'Nobody is in the directory and nobody has signed in yet, so there ' +
          'is nobody to pick. The form below takes a typed name.'
    });
    const roleOptions = rbac.ROLES.map(function (role) {
      return '<option value="' + self.esc(role.id) + '">' +
             self.esc(role.label) +
             '</option>';
    }).join('');

    const tiles = '<div class="tiles">' +
      info.roles.map(function (role) {
        return self.tile(role.memberCount, role.label);
      }).join('') +
      this.tile(candidates.length, 'People who could hold one') +
      '</div>';

    const status = info.closedToEveryone
      ? '<div class="err"><strong>Nobody can use this console.</strong> The ' +
        'gate is on, no role has a member, and ' +
        (info.bootstrap && info.bootstrap.seeded && info.bootstrap.claimedAt
          ? 'the bootstrap administrator has already signed in. '
          : '<code>admin.openWhenEmpty</code> is off. ') +
        'Anything you are reading ' +
        'here you are reading through <code>/admin-api</code> or with the ' +
        'gate off.</div>'
      : (info.openToAnyone && info.bootstrap && info.bootstrap.seeded
          ? this.warn('<strong><code>' + this.esc(info.bootstrap.username) +
                      '</code> ' +
            'has not signed in to this console yet, so anybody who signs in ' +
            'has the whole console.</strong> This service\'s bootstrap ' +
            'administrator holds both roles already; its first sign-in here ' +
            'ends the open console — for everybody who holds no role. ' +
            '<strong>Grant yourself a role now</strong> if you will need the ' +
            'console after that.')
      : (info.openToAnyone
          ? this.warn('<strong>No role has a member, so anybody who signs in ' +
            'has the whole console.</strong> The first grant made on this ' +
            'page ends that — for everybody, including whoever makes it. ' +
            '<strong>Grant yourself a role before you grant anybody else ' +
            'one</strong>, or the next page you click will be a 403.')
          : (info.enforced
              ? '<div class="ok">The roster is enforced. ' + info.grantCount +
                ' grant(s) across two roles; everybody else is refused at ' +
                'every page of this console.</div>'
              : this.warn('<strong>None of this is in force.</strong> The ' +
                'gate is OFF, so the console is open to anybody who can ' +
                'reach this port and these roles decide nothing. They are ' +
                'still real directory groups and can be granted now. ' +
                '(UNREACHABLE since 2026-09-06: the gate is ' +
                'unconditional.)'))));

    const noDirectory = info.available ? '' :
      '<div class="err">No LDAP directory is loaded in this process, so ' +
      'there is nowhere to hold these roles and nothing on this page can be ' +
      'granted. That is a build of this service without ' +
      '<code>ldap_server.js</code> rather than a failure — but the console ' +
      'gate is unconditional, so it leaves this console reachable only while ' +
      '<code>admin.openWhenEmpty</code> is on.</div>';

    const forms = state.write && info.available
      ? '<h2 id="grant">Grant a role</h2>' +
        this.note('Search for the person, then pick them from the results. ' +
        'The list is everybody with an entry in the directory and everybody ' +
        'this service has seen authenticate — two different sets, which is ' +
        'why both are searched and why each result says which it came from. ' +
        'An empty search lists everybody, twenty at a time.') +
        personPane +
        (view.picked
          ? '<form method="post" action="/admin/rbac" id="grant-picked">' +
            '<div class="formrow">' +
            '<input type="hidden" name="action" value="grant">' + carryBack +
            '<input type="hidden" name="username" value="' +
              this.esc(view.picked.username) + '">' +
            '<span>Person: <strong>' + this.esc(view.picked.username) +
              '</strong> <span class="state-none">' +
              this.esc(whereFrom(view.picked)) + '</span></span>' +
            '<label for="role">Role</label>' +
            '<select id="role" name="role">' + roleOptions + '</select>' +
            '<button type="submit">Grant</button>' +
            ' <a href="' + this.esc('/admin/rbac' + queryWith(pickCarry, {})) +
              '#find-personq">pick somebody else</a>' +
            '</div></form>'
          : (view.personAsked
              ? '<div class="err" id="grant-picked"><strong>' +
                this.esc(view.personAsked) + '</strong> is not in the ' +
                'directory and has not signed in, so there is nobody by that ' +
                'name to pick. Search again, or grant to the name as typed ' +
                'with the form below.</div>'
              : '')) +
        '<h3>Grant to a name that is not listed</h3>' +
        '<form method="post" action="/admin/rbac"><div class="formrow">' +
        '<input type="hidden" name="action" value="grant">' + carryBack +
        '<label for="typed">Name</label><input type="text" id="typed" ' +
        'name="username" size="24" placeholder="the name they will sign in ' +
        'as"><label for="typedrole">Role</label><select id="typedrole" ' +
        'name="role">' + roleOptions + '</select>' +
        '<button type="submit" class="secondary">Grant</button>' +
        '</div></form>' +
        this.note('The membership will DANGLE until that person exists — it ' +
        'names a DN this directory does not hold yet — and the role counts ' +
        'from the moment they first sign in. That is the interesting case ' +
        'for a mock and is why this form is here: nothing about a grant ' +
        'requires the person to have been seen. A name carrying a character ' +
        'RFC 4514 reserves in a DN is refused, the same refusal creating a ' +
        'person gets.')
      : (info.available && info.enforced && !state.write
          ? this.note('Granting and revoking need <strong>Admin ' +
            'Write</strong>. The table above is what you can see with ' +
            '<strong>Admin Read</strong>.')
          : '');

    // The grant pane's search, carried through the table's filter form: a GET
    // form posts its own fields and nothing else, so without these narrowing
    // the table would clear the search the reader is still using below it.
    const personCarry = ['personq', 'personfrom', 'person']
      .map(function (name) {
      const value = queryOne(req.query, name);
      return value === ''
        ? ''
        : '<input type="hidden" name="' + name + '" value="' + self.esc(value) +
          '">';
    }).join('');
    const inner = this.messagesOf(req) + noDirectory + status + tiles +
      '<form method="get" action="/admin/rbac"><div class="formrow">' +
      personCarry +
      '<label for="q">Person</label>' +
      '<input type="text" id="q" name="q" value="' + this.esc(wantedText) +
      '" size="22" placeholder="part of a name"><label ' +
      'for="rolefilter">Role</label><select id="rolefilter" ' +
      'name="role"><option value="">both</option>' +
      rbac.ROLES.map(function (role) {
        return '<option value="' + self.esc(role.id) + '"' +
               (wantedRole === role.id ? ' selected' : '') + '>' +
               self.esc(role.label) + '</option>';
      }).join('') + '</select>' +
      '<label for="per">Per page</label>' +
      '<select id="per" name="per">' + this.perPageOptions(paging.perPage) +
      '</select><button ' +
      'type="submit">Filter</button>' +
      (wantedText || wantedRole ? ' <a href="/admin/rbac">clear</a>' : '') +
      '</div></form>' +
      nav.head +
      '<table><tr><th>Person</th><th>Role</th><th>Group</th><th>Membership ' +
      'value</th><th>Take it away</th></tr>' +
      (rows || '<tr><td colspan="5">' +
        (wantedText || wantedRole
          ? 'No grant matches. The filter above may be hiding some.'
          : 'Nobody holds either role.' +
            (info.openToAnyone ? ' Which is why anybody who signs in can ' +
                                 'read this page.' : '')) +
        '</td></tr>') +
      '</table>' + nav.foot +
      (info.roles.some(function (r) { return r.claimedCount; })
        ? this.note('<strong>Some of those grants are on the PERSON rather ' +
          'than in the group.</strong> An entry whose own ' +
          '<code>memberOf</code> names a role group holds the role — the ' +
          'directory is asked in both directions — and nothing here ' +
          'maintains <code>memberOf</code>, so a client wrote it. They are ' +
          'listed because a page answering &ldquo;who has access&rdquo; that ' +
          'omitted them would be showing a console somebody could use and a ' +
          'list they were not on. They cannot be revoked from here: the ' +
          'value is on their entry, and this console writes only to groups. ' +
          'One edge worth knowing — a <code>memberOf</code> naming a role ' +
          'group that has <em>never been created</em> grants nothing, and ' +
          'starts granting the moment the first ordinary grant creates it.')
        : '') + forms +
      '<h2>What the two roles ' +
      'are</h2><table><tr><th>Role</th><th>Group</th><th>What it ' +
      'allows</th><th class="num">Members</th></tr>' +
      info.roles.map(function (role) {
        return '<tr><td><strong>' + self.esc(role.label) + '</strong></td>' +
          '<td><code>' + self.esc(role.dn || ('cn=' + role.cn)) + '</code>' +
          (role.exists ? '' : ' <span class="state-none" title="The group is ' +
            'created by the first grant rather than at startup, so &quot;no ' +
            'group&quot; and &quot;no members&quot; are the same state ' +
            'here.">not created yet</span>') + '</td><td>' +
            self.esc(role.what) +
          '</td><td ' +
          'class="num">' + role.memberCount +
          (role.claimedCount
            ? ' <span class="state-expired" title="Of which ' +
              role.claimedCount +
              ' are claimed by the person&#39;s own memberOf rather than ' +
              'listed by the group.">(' + role.claimedCount + ')</span>'
            : '') + '</td></tr>';
      }).join('') + '</table>' +
      this.note('<strong>Write implies read.</strong> A member of <code>' +
      this.esc(info.roles[1] ? info.roles[1].cn : '') + '</code> does not ' +
                                                        'also need <code>' +
      this.esc(info.roles[0] ? info.roles[0].cn : '') + '</code>: a role ' +
      'that could post a form to a page it was not allowed to look at would ' +
      'be a trap rather than a permission.') +
      // THE FOUR SETTINGS THEMSELVES, AND NOT A TABLE OF READINGS BESIDE THEM.
      // This page carried its own four-row table saying what each one was set
      // to and what it did, above a link to /admin/config; the descriptions in
      // that table and the ones in config.js's own rows had already begun to
      // differ. The form below is drawn from config.js, so there is one
      // description and it is the one the API answers with. The two sentences
      // that were ONLY in that table are the note under it — they are about
      // this console rather than about the settings, which is why they are not
      // in config.js either.
      this.configFormsFor('/admin/rbac') +
      this.note('<strong>Renaming a role group does not move ' +
      'anybody.</strong> The members stay in the group they were put in, ' +
      'which stops granting anything the moment the name changes — and the ' +
      'new name grants nothing until somebody is put in it. ' +
      '<code>/admin-api</code> is not gated by any of these four, on ' +
      'purpose: it is the way back in when nobody who holds a role can sign ' +
      'in, and it is why turning the gate on does not break a test suite ' +
      'driving the management API.') +
      RBAC_CAVEAT;

    log.debug("Leaving AdminConsole.rbacListPage(). " + shown.length + " " +
      "row(s) drawn of " +
              grants.length + " grant(s).");
    return {
      inner: inner,
      json: view.json
    };
  }

  // ===========================================================================
  // `/admin/mfa` WAS HERE AND IT IS GONE (2026-09-10).
  //
  // It arrived that same day and lasted hours, which is the shortest life any
  // page in this console has had — so this marker is worth more than the usual
  // silence: a reader following a link, a bookmark or a sentence in an older
  // file needs to be told where the two halves went rather than meeting a 404.
  //
  // The page did TWO things and could only be filed by one of them. It edited
  // the eight `totp.*` settings, and it drew a roster of who held a second
  // factor with a Clear button on every row. Its own header argued at length
  // that it belonged under Identities because it answered a question about
  // PEOPLE — right about the roster, wrong about the settings, and a reader
  // looking for the skew window and a reader looking for *who has no second
  // factor* landed on one screen and read past each other.
  //
  //   * **THE SETTINGS ARE `/admin/totp` UNDER PROTOCOLS**, beside a new
  //     `/admin/webauthn` — the other second factor, which had no settings at
  //     all until that day. Both are rows in PROTOCOL_SETTINGS_PAGES.
  //   * **THE ROSTER IS COLUMNS ON `/admin/users`** — Known from, Can sign in
  //     with, Second factor — with a `factor` filter and the counts as tiles.
  //     That page's POPULATION had to widen to carry it: see peopleRows().
  //   * **THE PER-PERSON DETAIL AND BOTH CLEAR BUTTONS ARE ON THAT PERSON'S
  //     OWN ROW** — see mfaSection(), which can say what an enrolment IS
  //     rather than only that there is one, because it is drawn for one person
  //     rather than for forty.
  //   * **`mfaAction()` IS `usersAction()`'s `clear-totp` AND `clear-key`.**
  //     Same two acts, same audit rows, same refusals.
  //
  // `GET /admin-api/mfa` and `POST /admin-api/mfa/:action` still exist and
  // answer out of the views above, because an API operation that worked is not
  // worth breaking to tidy a table.
  // ===========================================================================

  rbacView(req): Record<string, any> {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.rbacView().");
    const list = this.rbacListPage(req);
    log.debug("Leaving AdminConsole.rbacView().");
    return { json: list.json, inner: list.inner, title: 'Admin roles' };
  }

  // ---------------------------------------------------------------------------
  // GET /admin/consent, POST /admin/consent — WHAT PEOPLE HAVE AGREED TO.
  //
  // THE THIRD REGISTER IN THIS CONSOLE THAT LOOKS LIKE THE OTHER TWO AND
  // ANSWERS A DIFFERENT QUESTION, and saying which is which is most of what
  // this page is for. `/admin/delegation` holds ACTS (what happened) beside
  // INTENT (what an operator allowed between two applications). This page holds
  // neither: it holds what a PERSON said yes to, and the override an operator
  // wrote so that nobody would be asked.
  //
  // It is a page of its own rather than a fourth heading on /admin/delegation
  // for two reasons and the first is enough. Every row there is about two
  // APPLICATIONS; every row here has a person in it, and the whole argument for
  // keeping the acts picture and the permissions picture on separate canvases
  // is that a drawing with a person in it and a drawing without one must not
  // share a frame. The second is arithmetic: that page already carries seven
  // tables.
  //
  // TWO SECTIONS, AND THEY ARE NOT THE SAME KIND OF THING:
  //
  //   * GLOBAL CONSENT is CONFIGURATION. One row per (application, scope), held
  //     as `oauthGlobalConsent` on the application's own entry. Nobody is asked
  //     about a scope named there, and nothing is written about anybody — so
  //     REMOVING one asks everybody again, including the people who would have
  //     said yes.
  //   * RECORDED CONSENT is a RECORD. One row per (person, application, scope),
  //     held as `oauthConsent` on the person's own entry. Removing one asks
  //     that person again and nobody else.
  //
  // Both headings say so, because a reader who confused them would draw exactly
  // the wrong conclusion from an empty second table — which is what a service
  // with everything under global consent correctly looks like.
  //
  // FOUR ACTIONS, and two of them go through `applications.updateApplication()`
  // like every other attribute write in this console, so the ordering rules,
  // the audit row and the `ldapmodify` equivalence all come for free.
  // ---------------------------------------------------------------------------




  consentBack(listView) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.consentBack().");
    log.debug("Leaving AdminConsole.consentBack().");
    return '<input type="hidden" name="back" value="' +
           this.esc(queryWith(listView || {}, {})) + '">';
  }

  // One row of the overrides table.
  globalConsentRow(one, listView) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.globalConsentRow().");
    const href = '/admin/applications' +
                 queryWith(listView || {}, { application: one.client });
    log.debug("Leaving AdminConsole.globalConsentRow().");
    return '<tr>' +
      '<td class="who"><a href="' + this.esc(href) + '">' +
      this.esc(one.clientName) +
      '</a>' +
        (one.clientName === one.client ? '' :
         '<br><code>' + this.esc(one.client) + '</code>') +
      '</td>' +
      '<td><code>' + this.esc(one.scope) + '</code></td>' +
      // WHAT THE SCOPE IS, in three states rather than two, which is the same
      // honesty the grants table applies to a name it cannot resolve. Most
      // scopes are not permissions and saying "not a permission" about `openid`
      // would be reporting a fault where there is none.
      '<td>' + (one.resource
        ? 'the permission <code>' + this.esc(one.permission) + '</code> ' +
                                                          'exposed by <code>' +
          this.esc(one.resource) + '</code>' +
          (one.granted ? '' : '<br><span class="state-none">and this client ' +
            'has NOT been granted it &mdash; in product mode, or with ' +
            '<code>oauth2.delegatedPermissionsEnforced</code> on, the ' +
            'request is refused anyway, consented or not</span>')
        : '<span class="state-none">an ordinary scope &mdash; no application ' +
          'here defines a permission by that name</span>') + '</td>' +
      '<td class="act"><form method="post" action="/admin/consent">' +
      this.consentBack(listView) +
        '<input type="hidden" name="action" value="revoke-global-consent">' +
        '<input type="hidden" name="client" value="' + this.esc(one.client) +
        '"><input type="hidden" name="scope" value="' + this.esc(one.scope) +
        '"><button type="submit" class="danger">Remove</button>' +
      '</form></td></tr>';
  }

  // One row of the recorded table.
  recordedConsentRow(one, listView) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.recordedConsentRow().");
    const userHref = '/admin/users' +
                     queryWith(listView || {}, { user: one.username });
    const appHref = '/admin/applications' +
                    queryWith(listView || {}, { application: one.client });
    if (one.unreadable) {
      log.debug("Leaving AdminConsole.recordedConsentRow().");
      return '<tr><td class="who"><a href="' + this.esc(userHref) + '">' +
        this.esc(one.username) +
        '</a></td><td colspan="2"><span class="state-none">This value is not ' +
        'in the shape this service writes &mdash; something put it on the ' +
        'entry by hand: <code>' +
        this.esc(one.raw) + '</code>. It is shown rather than dropped, ' +
        'because a value the page silently ignored would be one somebody ' +
        'wrote on purpose and could not find out was being ignored. It ' +
        'consents nothing.</span></td><td class="act"></td></tr>';
    }
    log.debug("Leaving AdminConsole.recordedConsentRow().");
    return '<tr>' +
      '<td class="who"><a href="' + this.esc(userHref) + '">' +
      this.esc(one.username) +
      '</a></td><td ' +
      'class="who"><a ' +
      'href="' + this.esc(appHref) + '">' + this.esc(one.client) +
      '</a></td><td><code>' +
      this.esc(one.scope) + '</code>' +
        (one.at ?
         '<br><span class="state-none">agreed at ' + this.esc(one.at) +
         '</span>' :
         '') +
      '</td>' +
      '<td class="act"><form method="post" action="/admin/consent">' +
      this.consentBack(listView) +
        '<input type="hidden" name="action" value="revoke-consent">' +
        '<input type="hidden" name="username" value="' +
        this.esc(one.username) +
      '"><input ' +
        'type="hidden" name="client" value="' + this.esc(one.client) + '">' +
        '<input type="hidden" name="scope" value="' + this.esc(one.scope) +
        '"><button type="submit" class="danger">Revoke</button>' +
      '</form></td></tr>';
  }

  setRolePreviewer(hooks) {
    const { log, errorCodes, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setRolePreviewer().");
    if (!hooks || typeof hooks.preview !== 'function' ||
        typeof hooks.policy !== 'function') {
      log.error(errorCodes.tag('STS-ADMIN-0014') + 'admin: ' +
                'setRolePreviewer() was offered an object without both ' +
                'preview() and policy(), so it was refused and /admin/roles ' +
                'will say the decision cannot be previewed. Installing half ' +
                'of it would leave the page able to ask a question and ' +
                'unable to say which policy answered.');
      log.debug("Leaving AdminConsole.setRolePreviewer(). Refused.");
      return false;
    }
    rolePreviewer = hooks;
    // AND THE READ LAYER, which needs the same thing — see the header of
    // admin-core/admin_views.ts. Still one statement and one writer.
    adminViews.setRolePreviewer(hooks);
    log.info('admin: /admin/roles can preview an issuance decision — the ' +
             'same call the nine issuance sites make, through the same ' +
             'policy.');
    log.debug("Leaving AdminConsole.setRolePreviewer(). Installed.");
    return true;
  }

  rolesBack(listView) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.rolesBack().");
    log.debug("Leaving AdminConsole.rolesBack().");
    return '<input type="hidden" name="back" value="' +
           this.esc(queryWith(listView || {}, {})) + '">';
  }

  // One row of the configured table. The three membership lists are drawn as
  // one column each rather than one list of "members", because the three are
  // looked up in three different places and a reader tracking down why somebody
  // holds a role needs to know which one to look in.
  roleRow(one, listView) {
    const { log, ROLE_MEMBER_KINDS } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.roleRow().");
    const members = ROLE_MEMBER_KINDS.map(function (kindRow) {
      const list = one[kindRow.field];
      if (!list.length) {
        return '<td><span class="state-none">none</span></td>';
      }
      return '<td>' + list.map(function (member) {
        return '<div><code>' + self.esc(member) + '</code> ' +
          '<form method="post" action="/admin/roles" class="inline">' +
          self.rolesBack(listView) +
          '<input type="hidden" name="action" value="remove-member">' +
          '<input type="hidden" name="role" value="' + self.esc(one.name) +
          '">' +
          '<input type="hidden" name="kind" value="' + self.esc(kindRow.kind) +
          '"><input type="hidden" name="member" value="' + self.esc(member) +
          '"><button type="submit" class="danger">Remove</button></form></div>';
      }).join('') + '</td>';
    }).join('');
    log.debug("Leaving AdminConsole.roleRow().");
    return '<tr><td class="who"><code>' + this.esc(one.name) + '</code>' +
      (one.description ? '<br><span class="sub">' + this.esc(one.description) +
                         '</span>' : '') +
      '</td>' + members +
      '<td class="act"><form method="post" action="/admin/roles">' +
        this.rolesBack(listView) +
        '<input type="hidden" name="action" value="delete-role">' +
        '<input type="hidden" name="role" value="' + this.esc(one.name) + '">' +
        '<button type="submit" class="danger">Delete</button>' +
      '</form></td></tr>';
  }

  // ---------------------------------------------------------------------------
  // /admin/policies (2026-09-12) — DIRECTORY → POLICIES, and the PASSWORD
  // POLICY is the first kind of policy on it.
  //
  // ONE COMPUTATION, TWO RENDERINGS: `adminViews.passwordPoliciesView()` is the
  // model, this route draws it, and `GET /admin-api/policies` hands the same
  // model back. The two writes are `adminActions.passwordPoliciesAction()`,
  // which `POST /admin-api/policies/{action}` calls too — rule 7 by
  // construction.
  //
  // **THE FORM CARRIES EVERY FIELD**, because a save replaces the whole
  // profile; the action is told it came from the console so that an unticked
  // checkbox — which posts nothing — reads as "no" here and as a refusal from
  // an API caller that forgot one.
  //
  // **THE RESET IS ITS OWN FORM, NOT A SECOND BUTTON IN THE SAVE FORM**, for
  // `/admin/users/new`'s reason: two submit buttons named `action` make
  // `form.elements.action` a RadioNodeList whose value is empty, and the
  // console suite finds every form it presses by the action it posts.
  // ---------------------------------------------------------------------------
  passwordPolicyFieldRow(field) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.passwordPolicyFieldRow().");
    const id = 'pp-' + field.key;
    const hint = this.tip(field.what, Infinity);
    let control;
    if (field.type === 'bool') {
      control = '<input type="checkbox" id="' + this.esc(id) + '" name="' +
        this.esc(field.key) + '" value="TRUE"' +
        (field.value ? ' checked' : '') +
        hint + '>';
    } else {
      control = '<input type="number" id="' + this.esc(id) + '" name="' +
        this.esc(field.key) + '" min="' + this.esc(field.min) + '" max="' +
        this.esc(field.max) +
        '" step="1" required value="' + this.esc(field.value) + '"' + hint +
        '> <span class="sub">' + this.esc(field.unit) + '</span>';
    }
    log.debug("Leaving AdminConsole.passwordPolicyFieldRow().");
    return '<tr><td><label for="' + this.esc(id) + '"' + hint + '>' +
      this.esc(field.label) +
      '</label></td><td>' + control + '</td>' +
      '<td><code>' + this.esc(field.attribute) + '</code></td>' +
      '<td>' + this.esc(field.type === 'bool' ? (field.default ? 'yes' : 'no')
                                              : String(field.default)) +
                                                '</td>' +
      '<td class="' + (field.source === 'directory' ? '' : 'state-none') +
      '">' +
      this.esc(field.source === 'directory' ? 'this profile' :
               'built-in default') +
      '</td></tr>';
  }

  // The page's own URL with the preview user on it. Every form on this page
  // posts to THIS rather than to the bare path, so that the 303 after an action
  // lands back on the person the reader was looking at. A form that dropped the
  // parameter would answer "what did that do?" with somebody else's values,
  // which reads as the action having done something it did not.
  claimsPageUrl(query) {
    const { log, claimsPreviewUser } = this.deps;
    log.debug("Entering AdminConsole.claimsPageUrl().");
    log.debug("Leaving AdminConsole.claimsPageUrl().");
    return '/admin/claims?user=' + encodeURIComponent(claimsPreviewUser(query));
  }

  // The same for the SAML page. Two functions rather than one taking a path,
  // because these are the two strings the two POST handlers redirect to and a
  // caller that passed the wrong path would send a reader to the page their
  // form was NOT on — which is the one failure this helper exists to prevent.
  // The preview user is read by the same function for both, deliberately: the
  // two pages preview the same person unless somebody says otherwise, exactly
  // as /admin/vc already does.
  samlAttributesPageUrl(query) {
    const { log, claimsPreviewUser } = this.deps;
    log.debug("Entering AdminConsole.samlAttributesPageUrl().");
    log.debug("Leaving AdminConsole.samlAttributesPageUrl().");
    return '/admin/saml-attributes?user=' +
           encodeURIComponent(claimsPreviewUser(query));
  }

  // ---------------------------------------------------------------------------
  // THE DIRECTORY ATTRIBUTE HALF OF ONE SET.
  //
  // A checkbox per attribute type in the catalogue, a column saying what it
  // would put in a token for the person being previewed, and three buttons. It
  // is repeated for each of the four sets rather than being one table with four
  // checkbox columns, because the four sets are chosen for different reasons —
  // an access token goes to a resource server and an ID Token goes to a client,
  // and the interesting configuration is usually the one where they DIFFER. A
  // single grid would make four independent decisions look like one, and would
  // have to post all four sets at once, so changing the ID Token would rewrite
  // the access token's selection as a side effect.
  //
  // THE THREE BUTTONS ARE THREE FORMS, and unticking a box in one of the other
  // three sets' tables does nothing to this one: only the form that is
  // submitted sends anything, so each Update button carries exactly its own
  // set's boxes. That is worth stating because a page with four checkbox tables
  // and one Update button would be the obvious design and would be wrong in the
  // direction nobody notices — it would clear the three sets whose tables were
  // rendered before the reader ticked anything.
  // ---------------------------------------------------------------------------
  claimAttributeSection(setId, previewUser, values, pageUrl) {
    const { log, claimAttributes } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.claimAttributeSection(). setId=" + setId);
    const selected = claimAttributes.selectedNames(setId);

    const rows = claimAttributes.CATALOGUE.map(function (row) {
      const on = claimAttributes.isSelected(setId, row.ldap);
      const found = values.byLdap[row.ldap.toLowerCase()];
      // `description` is the one row with no generator: this service writes it
      // on every entry itself, to record the protocols that person has used. So
      // it is the one attribute whose value is a real fact, and it is absent
      // rather than invented for somebody with no entry.
      const valueCell = found
        ? '<td><code>' + self.esc(found.value) + '</code></td><td>' +
          self.esc(found.source) + '</td>'
        : '<td><span class="state-none">—</span></td><td>' +
          (row.from ? 'would be generated' : 'the entry\'s own') + '</td>';
      return '<tr><td><input type="checkbox" name="attribute" value="' +
        self.esc(row.ldap) + '"' +
        (on ? ' checked' : '') + '></td>' +
        '<td><code>' + self.esc(row.ldap) + '</code></td>' +
        '<td>' + self.esc(row.schema) + '</td>' +
        '<td><code>' + self.esc(row.claim.join('.')) + '</code></td>' +
        valueCell + '</tr>';
    }).join('');

    log.debug("Leaving AdminConsole.claimAttributeSection(). " +
              selected.length + " of " +
              claimAttributes.CATALOGUE.length + " selected.");
    return this.note((selected.length
        ? 'Carries ' + selected.length + ' directory attribute(s): ' +
          this.codeList(selected) + '.'
        : 'Carries no directory attribute. Tick some and press Update.')) +
      '<form method="post" action="' + this.esc(pageUrl) + '">' +
      '<input type="hidden" name="action" value="attributes">' +
      '<input type="hidden" name="set" value="' + this.esc(setId) + '">' +
      '<table><tr><th>In</th><th>LDAP attribute</th><th>Defined by</th>' +
      '<th>' +
      (setId === 'saml2' || setId === 'saml11' ? 'Attribute name' : 'Claim') +
      '</th><th>For ' + this.esc(previewUser) + '</th><th>Source</th></tr>' +
      rows + '</table><div class="formrow"><button>Update</button><span ' +
      'class="note">The ticked boxes become the whole selection for this ' +
      'set: unticking is how an attribute is ' +
      'removed.</span></div></form><div class="formrow"><form method="post" ' +
      'action="' + this.esc(pageUrl) + '" class="inline">' +
      '<input type="hidden" name="action" value="attributes-all">' +
      '<input type="hidden" name="set" value="' + this.esc(setId) + '">' +
      '<button class="secondary">Select all</button></form> ' +
      '<form method="post" action="' + this.esc(pageUrl) + '" class="inline">' +
      '<input type="hidden" name="action" value="attributes-clear">' +
      '<input type="hidden" name="set" value="' + this.esc(setId) + '">' +
      '<button class="secondary">Delete all</button></form>' +
      this.note('Both act immediately — there is no script on this page, so ' +
      'these are form posts and not a way of ticking the boxes above.') +
      '</div>';
  }

  // One set, rendered: what is in it, a way to remove each, and a way to add
  // another. The three sets differ in the extra field each needs, which is why
  // the form is built from the set's kind rather than being one form four
  // times.
  //
  // IT IS THE SAME FUNCTION ON BOTH PAGES and only the NOUN changes: a JWT set
  // carries claims and a SAML set carries attributes, which is what each
  // protocol's own readers call them, and a page headed "Custom SAML
  // attributes" whose tables said "claim" would be teaching the wrong word for
  // the thing it is about to put in an assertion. Everything else — the two
  // halves, the three buttons, the precedence — is one behaviour and is drawn
  // once.
  claimSetSection(setId, previewUser, values, pageUrl) {
    const { log, stats } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.claimSetSection(). setId=" + setId);
    const set = stats.CLAIM_SETS[setId];
    const claims = stats.claimSet(setId);
    const isSaml2 = setId === 'saml2';
    const isSaml11 = setId === 'saml11';
    const isSaml = isSaml2 || isSaml11;
    const isUserinfo = setId === 'userinfo';
    const noun = isSaml ? 'attribute' : 'claim';
    // Three carriers now, and the third is the one that is not ISSUED: a
    // UserInfo response is built on every call, so the sentence this word ends
    // up in ("these tokens carry only what the protocol puts in them") has to
    // say `responses` or it will be describing a signed document that does not
    // exist.
    const carrier = isSaml ? 'assertions' :
                    (isUserinfo ? 'UserInfo responses' : 'tokens');
    const extraHeader = isSaml2 ? '<th>NameFormat</th>' :
                        (isSaml11 ? '<th>AttributeNamespace</th>' : '');

    const rows = claims.map(function (claim) {
      const extraCell = isSaml2 ? '<td>' + self.esc(claim.nameFormat || '—') +
                        '</td>'
                      : (isSaml11 ?
                         '<td>' + self.shortened(claim.namespace, 44) +
                         '</td>' : '');
      return '<tr><td><code>' + self.esc(claim.name) + '</code></td>' +
             extraCell +
        '<td><code>' + self.esc(claim.value) + '</code></td>' +
        '<td><form method="post" action="' + self.esc(pageUrl) +
        '" class="inline">' +
        '<input type="hidden" name="action" value="remove">' +
        '<input type="hidden" name="set" value="' + self.esc(setId) + '">' +
        '<input type="hidden" name="name" value="' + self.esc(claim.name) +
        '"><button class="secondary">Remove</button></form></td></tr>';
    }).join('');

    const extraInput = isSaml2
      ? '<label for="nf-' + setId + '">NameFormat</label>' +
        '<input type="text" id="nf-' + setId +
        '" name="nameFormat" size="28" ' +
                                               'placeholder="(optional)">'
      : (isSaml11
        ? '<label for="ns-' + setId + '">Namespace</label>' +
          '<input type="text" id="ns-' + setId +
          '" name="namespace" size="34" ' +
                                                 'placeholder="' +
          this.esc(stats.DEFAULT_SAML11_NAMESPACE) + '">'
        : '');

    log.debug("Leaving AdminConsole.claimSetSection(). " + claims.length +
              " claim(s).");
    return '<h3>' + this.esc(set.label) + ' <code>' + this.esc(setId) +
           '</code></h3>' +
      // The two halves in this order because the second is the one that changes
      // per person, and a reader who has just pressed Update wants to see the
      // table they pressed it under rather than scroll past a form they did not
      // touch. The headings say which half is which: they are configured
      // separately, audited separately, and only one of them can be wrong in a
      // way the directory explains.
      '<p class="sub">Typed ' + noun + 's &mdash; a name and a value, the ' +
      'same for everybody.</p><table><tr><th>Name</th>' + extraHeader +
      '<th>Value</th><th></th></tr>' +
      (rows ||
       '<tr><td colspan="' + (extraHeader ? 4 : 3) + '">No custom ' + noun +
       ' ' +
               'is configured; ' +
               'these ' + carrier + ' carry only what the protocol puts in ' +
                                    'them.</td></tr>') + '</table><form ' +
      'method="post" action="' + this.esc(pageUrl) + '"><div class="formrow">' +
        '<input type="hidden" name="action" value="add">' +
        '<input type="hidden" name="set" value="' + this.esc(setId) + '">' +
        '<label for="n-' + setId + '">Name</label>' +
        '<input type="text" id="n-' + setId + '" name="name" size="20">' +
        extraInput +
        '<label for="v-' + setId + '">Value</label>' +
        '<input type="text" id="v-' + setId + '" name="value" size="28">' +
        '<button>Add</button>' +
        '</div></form>' +
      (claims.length
        ? '<form method="post" action="' + this.esc(pageUrl) +
          '" class="inline">' +
          '<input type="hidden" name="action" value="clear">' +
          '<input type="hidden" name="set" value="' + this.esc(setId) + '">' +
          '<button class="secondary">Clear this set</button></form>'
        : '') +
      '<p class="sub">Directory attributes &mdash; a value read off each ' +
      'person\'s own entry.</p>' +
      this.claimAttributeSection(setId, previewUser, values, pageUrl);
  }

  // ---------------------------------------------------------------------------
  // THE GROUPS CLAIM, on the page that already answers "what will the next
  // token carry".
  //
  // READ-ONLY here, and that is a decision rather than an omission. Its four
  // settings live in config.js's table, which means /admin/config and POST
  // /admin-api/config/set already change them — a second form here would be a
  // second door to one setting, which is exactly the two-stores mistake this
  // service keeps out of everything else. So this section reports and links;
  // the parity rule (a control gets an API operation in the same commit) is
  // satisfied by there being no new control.
  //
  // What it does add is the thing no configuration page can: what the claim
  // WOULD say about the person being previewed, built by groupsOf() — the same
  // function the issuance path calls — so a preview that agreed with the page
  // and disagreed with the token is not possible.
  // ---------------------------------------------------------------------------
  groupClaimSection(previewUser) {
    const { log, groupClaims } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.groupClaimSection(). user=" + previewUser);
    const state = groupClaims.state();
    const answer = groupClaims.groupsOf(previewUser);
    const rows = answer.groups.map(function (group) {
      // WHY this group is in the list, which is the whole of the memberOf
      // disagreement in one cell: `member` and its two siblings are the GROUP
      // saying so, memberOf is the PERSON saying so, and this directory
      // maintains neither from the other.
      const how = group.via.length
        ? group.via.map(function (name) {
          return '<code>' + self.esc(name) + '</code>';
        }).join(', ')
        : '';
      const counted = group.via.length || state.memberOfCounts;
      return '<tr><td><code>' + self.esc(group.dn) + '</code></td>' +
        '<td>' + self.esc(group.cn) + '</td>' +
        '<td>' + (how || '&mdash;') +
        (group.viaMemberOf
          ? (how ? ', ' : '') + 'the person\'s own <code>memberOf</code>' +
            (state.memberOfCounts ? '' : ' <em>(not counted)</em>')
          : '') + '</td>' +
        '<td>' + (counted ? 'yes' : 'no') + '</td></tr>';
    }).join('');

    const values = answer.values.length
      ? this.note('The claim <code>' + this.esc(state.claim) + '</code> ' +
        'would carry ' +
        this.codeList(answer.values) + '.')
      : this.note('No claim at all for this person &mdash; not an empty ' +
        'list, absent. ' +
        this.esc(answer.reason));

    log.debug("Leaving AdminConsole.groupClaimSection(). " +
              answer.values.length + " " +
        "value(s).");
    return '<h2>The groups claim</h2>' +
      this.note('The one thing on this page that is <strong>not</strong> ' +
      'chosen per set: with <code>groups.claim</code> on, all four carry it, ' +
      'for anybody who is a member of a group in <a href="/admin/groups">the ' +
      'embedded directory</a>. The membership is read at the moment a token ' +
      'is minted, so an <code>ldapmodify</code> changes the next one, and ' +
      'somebody in no group gets no claim rather than an empty list &mdash; ' +
      'which is why this can be on by default without changing what an ' +
      'existing client receives.') +

      '<div class="' + (state.enabled && !state.problem ? 'note' : 'warn') +
      '">' +
      (state.enabled
        ? (state.problem
            ? '<strong>On, and not arriving.</strong> ' +
              this.esc(state.problem)
            : '<strong>On.</strong> Every access token, ID Token and both ' +
              'SAML assertions carry ' +
              '<code>' + this.esc(state.claim) + '</code>, each value being ' +
              (state.valueForm === 'dn' ? 'the group\'s whole DN' : 'the ' +
                  'group\'s <code>cn</code>') +
              '. A person\'s own <code>memberOf</code> ' +
              (state.memberOfCounts ? 'counts' : 'does NOT count') + ' as ' +
                  'membership.')
        : '<strong>Off.</strong> No token or assertion carries a groups ' +
          'claim. Turn it on with <code>groups.claim</code>.') +
      ' Change any of it on <a href="/admin/groups">the groups page</a>, ' +
      'which draws all four: ' + this.codeList(state.settings) + '.' +
      (state.loaded ? '' : ' <strong>The embedded directory is not loaded in ' +
                           'this process</strong>, so there are no groups to ' +
                           'read.') +
      '</div>' +

      this.warn('<strong>Carrying a group is not granting one.</strong> No ' +
      'endpoint here reads this claim and nothing decides anything on it ' +
      '&mdash; the same sentence <a href="/admin/groups">the groups page</a> ' +
      'has always carried, and the half of it that changed is that a token ' +
      'now says so out loud.') +

      this.note('A typed claim above, and a ticked directory attribute ' +
      'above, both win over this one where the names collide: those were ' +
      'named on this page about this service, and this comes from a setting ' +
      'and a directory.') +

      '<h3>What ' + this.esc(previewUser) + ' would get</h3>' +
      values +
      (answer.groups.length
        ? '<table><tr><th>Group</th><th>cn</th><th>Named ' +
          'by</th><th>Counted</th></tr>' +
          rows + '</table>'
        : this.note(this.esc(previewUser) + ' is named by no group here' +
          (answer.entryFound ? '' :
           ', and has no entry in the directory either') + '. ' +
          'The entry would be at <code>' + this.esc(answer.dn) + '</code>.'));
  }

  // ---------------------------------------------------------------------------
  // THE PROSE BOTH PAGES NEED, WRITTEN ONCE.
  //
  // /admin/claims and /admin/saml-attributes are the same two halves — a typed
  // entry and a ticked directory attribute — put into two different
  // vocabularies, so most of what each page has to explain is one explanation.
  // It is factored into these four helpers rather than copied, because the copy
  // that is not edited alongside the other is the one a reader believes: two
  // pages disagreeing about what `${username}` does, or about whether a typed
  // entry wins, would be worse than either page saying nothing.
  //
  // `family` is 'jwt' or 'saml' and is the ONLY thing that varies. Where the
  // two genuinely differ — a JWT value is typed and a SAML one is always text,
  // a JWT nests and an assertion cannot — the difference is stated rather than
  // generalised away, because that pair of facts is exactly what somebody
  // comparing an ID Token with an assertion has come here to find.
  // ---------------------------------------------------------------------------
  claimHalvesNote(family) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.claimHalvesNote().");
    const noun = family === 'saml' ? 'attribute' : 'claim';
    const carrier = family === 'saml' ? 'assertion'
                  : (family === 'userinfo' ? 'UserInfo response' : 'token');
    log.debug("Leaving AdminConsole.claimHalvesNote().");
    return this.note('Each set has <strong>two halves</strong>. A <em>typed ' +
                     noun +
      '</em> is a name and a value somebody wrote here, the same for ' +
      'everybody except where it carries a <code>${placeholder}</code>. A ' +
      '<em>directory attribute</em> is ticked from the catalogue below and ' +
      'its value is whatever that person\'s entry under ' +
      '<code>ou=users</code> says — so an <code>ldapmodify</code> changes ' +
      'the next ' + carrier + ', ' +
      'and an LDAP client and a relying party pointed at this service are ' +
      'shown the same person. That is the half worth exercising, and until ' +
      'the catalogue existed only a Verifiable Credential could do it.');
  }

  // The "show me somebody" form. It is a GET form posting to the page's own
  // path, so the preview user survives in the URL and every action form on the
  // page can carry it into its redirect — see claimsPageUrl().
  claimPreviewForm(path, previewUser, values) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.claimPreviewForm().");
    log.debug("Leaving AdminConsole.claimPreviewForm().");
    return '<form method="get" action="' + this.esc(path) +
           '"><div class="formrow">' +
      '<label for="user">Show the values for</label>' +
      '<input type="text" id="user" name="user" size="20" value="' +
      this.esc(previewUser) + '"><button ' +
      'class="secondary">Show</button>' +
      this.note((values.entryFound
        ? 'This person has an entry in the directory, so the values marked ' +
          '<em>directory</em> are what an LDAP client reads from it.'
        : 'This person has no entry in the directory — nobody has ' +
          'authenticated as them and nothing was added by hand — so every ' +
          'value below is generated. It will be the same one next time: the ' +
          'invented person is seeded from the username.')) + '</div></form>';
  }

  attributeCatalogueNotes(family) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.attributeCatalogueNotes().");
    const saml = family === 'saml';
    // THE OTHER PAGES, AS A LIST, because there are three of them now and there
    // were two when this was written. It said "the other page" and named one,
    // which was a sentence that could only ever be right while the number was
    // two — and the day a third arrived it would have gone on reading correctly
    // while telling a reader that one of the two places their selection does
    // NOT apply is the only one. Derived from `family` in one place so that a
    // fourth page is one entry rather than three sentences to find.
    const OTHER_PAGES = {
      jwt: ['<a href="/admin/userinfo-claims">the UserInfo claims page</a>',
            '<a href="/admin/saml-attributes">the SAML attributes page</a>'],
      userinfo: ['<a href="/admin/claims">the custom claims page</a>',
                 '<a href="/admin/saml-attributes">the SAML attributes ' +
                 'page</a>'],
      saml: ['<a href="/admin/claims">the custom claims page</a>',
             '<a href="/admin/userinfo-claims">the UserInfo claims page</a>']
    };
    const OTHER_THINGS = {
      jwt: 'a UserInfo response or an assertion carries',
      userinfo: 'an access token, an ID Token or an assertion carries',
      saml: 'an access token, an ID Token or a UserInfo response carries'
    };
    const otherPage = (OTHER_PAGES[family] || OTHER_PAGES.jwt).join(' and ');
    const otherThing = OTHER_THINGS[family] || OTHER_THINGS.jwt;
    log.debug("Leaving AdminConsole.attributeCatalogueNotes().");
    return '<h2>Where a directory attribute comes from, and what it does not ' +
           'do</h2>' +
      this.note('The catalogue is of <strong>LDAP attribute types</strong> ' +
        'and not of ' +
      (saml ? 'attribute names' : 'claim names') + ', and it is the same ' +
                                                        'catalogue ' +
      otherPage + ' and <a href="/admin/vc">the credential claims page</a> ' +
      'choose from — one list of spellings, because two would eventually ' +
      'disagree about what <code>schacDateOfBirth</code> is called while ' +
      'both looked right. The value is the one on that person\'s entry under ' +
      '<code>ou=users</code>; where the entry has nothing, it is invented ' +
      'from the username — the same invented person every time, across ' +
      'restarts, in obviously fictional ranges. Three rows are not RFC ' +
      '4519/4524/2798: there is no standard attribute type for a birthdate ' +
      'or a nationality, so the SCHAC schema\'s names are borrowed rather ' +
      'than invented.') +
      this.note('The <strong>five selections are independent</strong>, and ' +
      'that is the point of having five: an access token carrying ' +
      '<code>employee_number</code> and a SAML 2.0 assertion carrying ' +
      '<code>email</code> is a normal arrangement and a single list could ' +
      'not express it. What is on this page is independent of ' +
      'what ' + otherThing + ' ' +
          '— ticked on ' +
      otherPage + ' — and of what a <a href="/admin/vc">credential</a> ' +
      'carries and what the <a href="/admin/vc-verifier-config">Verifier ' +
      'asks for</a>, deliberately: that is what keeps "issue a credential ' +
      'carrying a claim the access token does not" reachable.') +
      (saml
        ? this.note('A <strong>nested</strong> claim cannot stay nested ' +
          'here. A SAML Attribute\'s content model is a name and text ' +
          'values, so <code>address.locality</code> arrives as an attribute ' +
          'whose NAME is the dotted path, where a JWT would carry a ' +
          '<code>locality</code> member of an <code>address</code> object ' +
          '(OIDC Core 5.1.1). Both families then call one claim by one name, ' +
          'which is the property somebody comparing an ID Token with an ' +
          'assertion needs.')
        : this.note('A <strong>nested</strong> claim stays nested in a JWT: ' +
          '<code>address.locality</code> is a <code>locality</code> member ' +
          'of an <code>address</code> object, which is what OIDC Core 5.1.1 ' +
          'defines. A SAML Attribute has no way to spell that — the content ' +
          'model is a name and text values — so the assertion carries the ' +
          'dotted path as the attribute\'s name. Both families then call one ' +
          'claim by one name, which is the property somebody comparing an ID ' +
          'Token with an assertion needs.')) +
      this.note('<strong>A typed ' + (saml ? 'attribute' : 'claim') + ' of ' +
      'the same name wins.</strong> Somebody who wrote <code>email</code> by ' +
      'hand on the set that also has <code>mail</code> ticked has said ' +
      'something specific, and the specific thing beats the general one. In ' +
      'an assertion that has to be a filter rather than an overwrite: two ' +
      '<code>&lt;Attribute&gt;</code> elements with one name would leave a ' +
      'relying party reading whichever the builder emitted first.') +
      (saml
        ? this.note('<strong>And the protocol\'s own attribute beats ' +
          'both</strong>, which is worth knowing before it is discovered in ' +
          'an assertion. A SAML 2.0 assertion sets <code>name</code> from ' +
          'the sign-in and a WS-Federation one sets the whole identity claim ' +
          'list, so ticking <code>cn</code>, <code>givenName</code>, ' +
          '<code>sn</code>, <code>uid</code> or <code>mail</code> may change ' +
          'nothing a relying party sees. The rule is not this page\'s: a ' +
          'configured attribute is ADDED to an assertion and never ' +
          'substituted into one, because an attribute a relying party keys ' +
          'off that a web form could displace would break a sign-in ' +
          'somewhere that looks nothing like this page.')
        : this.note('<strong>And the protocol\'s own claim beats ' +
          'both</strong>, which is worth knowing before it is discovered on ' +
          'a token. An ID Token always carries <code>name</code>, ' +
          '<code>given_name</code>, <code>family_name</code>, ' +
          '<code>preferred_username</code> and <code>email</code> built from ' +
          'the sign-in, so ticking <code>cn</code>, <code>givenName</code>, ' +
          '<code>sn</code>, <code>uid</code> or <code>mail</code> <em>on ' +
          'that set</em> changes nothing the client sees — the same five ' +
          'reach an access token, where the protocol sets none of them, and ' +
          'reach it from the directory. The rule is not new and is not this ' +
          'page\'s: a configured claim is added to a token and never ' +
          'substituted into one, because a claim a relying party keys off ' +
          'that a web form could displace would break a sign-in somewhere ' +
          'that looks nothing like this page.')) +
      this.note('<strong>None of it is verified and none of it grants ' +
      'anything.</strong> This service authenticates nobody — the username ' +
      'typed at the sign-in screen is the identity in everything it issues — ' +
      'so a birthdate from here is a birthdate from a web form. No endpoint ' +
      'here reads one of these back or decides anything on one. That is true ' +
      'of the groups claim as well: it is carried, and a group on that ' +
      'person\'s entry still grants them nothing &mdash; see <a ' +
      'href="/admin/groups">the groups page</a>.');
  }

  claimValueNotes(family) {
    const { log, stats } = this.deps;
    log.debug("Entering AdminConsole.claimValueNotes().");
    const saml = family === 'saml';
    log.debug("Leaving AdminConsole.claimValueNotes().");
    return '<h2>Values</h2>' +
      this.note('A value may contain <code>${placeholders}</code>, because a ' +
      'value that can only be a constant cannot exercise the thing worth ' +
      'testing — that an ' +
      (saml ? 'attribute' : 'claim') + ' carrying the signed-in user\'s ' +
      'identity reaches the relying party. ' +
      (saml
        ? 'The ones an ASSERTION expands are ' +
          this.codeList(SAML_PLACEHOLDERS) +
          ', and that is a shorter list than <a href="/admin/claims">the ' +
          'token page\'s</a> on purpose rather than by oversight: an ' +
          'assertion is built from a subject and an audience, so the names a ' +
          'JWT context carries — <code>${username}</code>, ' +
          '<code>${email}</code> and the rest — have nothing to expand ' +
          'against here and arrive as the characters they were written as. ' +
          '<code>${subject}</code> is the one that carries the signed-in ' +
          'identity.'
        : 'The ones understood are ' + this.codeList(stats.PLACEHOLDERS) +
          '.') +
      ' An unknown one is left as it was written rather than replaced with ' +
      'nothing: <code>${dept}</code> that silently became an empty string is ' +
      'a bug that looks like a configuration mistake, and one that still ' +
      'says <code>${dept}</code> names itself.') +
      (saml
        ? this.note('<strong>A SAML attribute value is never typed.</strong> ' +
          'The XML content model is text, so <code>true</code> and ' +
          '<code>{"a":1}</code> reach the relying party as the characters ' +
          'they were written as — which is the opposite of what the same ' +
          'value does in a JWT, where it would arrive as a boolean and an ' +
          'object. That difference is worth knowing rather than discovering: ' +
          'a client library that parses an assertion attribute into a ' +
          'boolean is doing that on its own.')
        : this.note('A ' + (family === 'userinfo' ? 'UserInfo' : 'JWT') + ' ' +
          'claim value is typed: text that unambiguously looks like JSON — ' +
          'an object, an array, a bare ' +
          '<code>true</code>/<code>false</code>/<code>null</code>, or a ' +
          'number — is used as that JSON, and anything else is a string. One ' +
          'consequence, stated rather than left to be discovered: a claim ' +
          'whose value is genuinely the four characters <code>true</code> ' +
          'cannot be configured, because a text field cannot tell the two ' +
          'apart. Write <code>"true"</code>, which parses as the JSON ' +
          'string. <a href="/admin/saml-attributes">SAML attribute ' +
          'values</a> are never typed — the XML content model is text.'));
  }

  // The "replace a whole set" form, which is the one a test wants: POST the
  // same thing as JSON and get JSON back. The SELECT is built from the ids the
  // page carries, so the SAML page cannot offer a token set and the claims page
  // cannot offer an assertion one — the same restriction claimsAction()'s
  // `allowed` enforces on the way in, said in the markup so it is not only a
  // refusal.
  replaceSetForm(ids, pageUrl, family) {
    const { log, stats } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.replaceSetForm().");
    const options = ids.map(function (id) {
      return '<option value="' + self.esc(id) + '">' +
             self.esc(stats.CLAIM_SETS[id].label) + '</option>';
    }).join('');
    const noun = family === 'saml' ? 'attributes' : 'claims';
    log.debug("Leaving AdminConsole.replaceSetForm().");
    return '<h2>Replace a whole set</h2>' +
      this.note('The form a test wants. POST the same thing as JSON to get ' +
      'JSON back. This replaces the ' +
      '<em>typed</em> ' + noun + ' only; the directory attributes ' +
      'ticked above are a separate action (<code>attributes</code>) and are ' +
      'left alone by it.') +
      '<form method="post" action="' + this.esc(pageUrl) + '">' +
        '<input type="hidden" name="action" value="replace">' +
        '<div class="formrow"><label for="set">Set</label>' +
        '<select id="set" name="set">' + options + '</select></div><textarea ' +
        'name="claims" spellcheck="false">[{"name": "dept", "value": ' +
        '"engineering"}, {"name": "on_behalf_of", "value": ' +
        '"${username}"}]</textarea><div ' +
        'class="formrow"><button>Replace</button></div></form>';
  }

  // ---------------------------------------------------------------------------
  // GET /admin/userinfo-claims, POST /admin/userinfo-claims
  //
  // THE THIRD PAGE ONTO THE ONE CLAIM-SET STORE, added 2026-08-26, and the
  // first one whose subject is not something this service ISSUES.
  //
  // Everything structural about it is /admin/claims: the same two halves (a
  // typed claim and a ticked directory attribute), the same claimsAction() with
  // an `allowed` list, the same setClaimSet() and the same audit row. What it
  // does NOT share with that page is the sentence that page is built around —
  // "nothing already issued changes" — and that difference is the whole
  // argument for it being a page rather than a third section over there.
  //
  // **A UserInfo response is BUILT ON EVERY CALL.** An access token, an ID
  // Token and both assertions are signed documents: a claim added to one of
  // those sets reaches a client at its next sign-in and never reaches the
  // tokens it already holds. A claim added HERE reaches the very next `GET
  // /oauth2/userinfo` from a client that signed in an hour ago and has done
  // nothing since. That is a different thing to be able to demonstrate, and a
  // reader who came to this console to demonstrate it should not have to infer
  // it from a page whose every warning says the opposite.
  //
  // **AND IT IS THE ONE CLAIM SET A CLIENT CAN ADD TO.** OpenID Connect Core
  // section 5.5 lets a client name individual claims in the `userinfo` member
  // of the `claims` request parameter, and this service answers them off the
  // same LDAP catalogue this page ticks from. So this page has a section no
  // other claims page has: what a client may ask for, what the four layers of
  // precedence are, and a PREVIEW of what a given request would return for a
  // given person — built by the functions oauth2.js's UserInfo endpoint itself
  // calls, for the reason every preview here is built that way.
  //
  // THE RESERVED LIST APPLIES HERE and does not apply to the SAML page, which
  // is the one rule a reader coming from /admin/saml-attributes will get wrong:
  // `sub` is required in this response by section 5.3.2 and a client MUST check
  // it against the ID Token's, and the SIGNED form of this response (a client
  // that registered `userinfo_signed_response_alg`) is a JWT carrying `iss`,
  // `aud` and `exp`. admin_stats.js's reservedNames() is where that is decided.
  // ---------------------------------------------------------------------------

  // The page's own URL with the preview user and any claims request on it, for
  // the same reason claimsPageUrl() carries the user: every form here posts to
  // THIS, so the 303 after an action lands back on what the reader was looking
  // at. Dropping the request would answer "what did that do?" with a page that
  // had forgotten the question.
  userinfoClaimsPageUrl(query) {
    const { log, claimsRequestParameter, claimsPreviewUser } = this.deps;
    log.debug("Entering AdminConsole.userinfoClaimsPageUrl().");
    const request = claimsRequestParameter(query);
    log.debug("Leaving AdminConsole.userinfoClaimsPageUrl().");
    return '/admin/userinfo-claims?user=' +
           encodeURIComponent(claimsPreviewUser(query)) +
      (request ? '&request=' + encodeURIComponent(request) : '');
  }

  // The "what would this request return" form and its answer. A GET form onto
  // the page's own path, exactly as the preview-user form is and for the same
  // reason: the request survives in the URL, so every action form on the page
  // carries it into its redirect and a reader who ticks a box does not lose
  // what they typed.
  claimsRequestSection(previewUser, raw, preview) {
    const { log, oauth2 } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.claimsRequestSection().");
    const form = '<form method="get" action="/admin/userinfo-claims">' +
      '<input type="hidden" name="user" value="' + this.esc(previewUser) +
      '"><div class="formrow"><label for="request">A claims ' +
      'request</label><input type="text" id="request" name="request" ' +
      'size="72" spellcheck="false" value="' +
      this.esc(raw) + '" ' +
      'placeholder=\'{"userinfo":{"birthdate":null,"address":null}}\'>' +
      '<button ' +
      'class="secondary">Show what it returns</button></div></form>';

    if (!preview.asked) {
      log.debug("Leaving AdminConsole.claimsRequestSection(). Nothing asked " +
                "for.");
      return form + this.note('Nothing asked for yet. Paste the ' +
        '<code>claims</code> parameter a client would send &mdash; the whole ' +
        'section 5.5 object, <code>userinfo</code> member and all &mdash; ' +
        'and this shows what ' +
        this.esc(previewUser) + ' would get back, computed by the same two ' +
        'functions the <code>/oauth2/userinfo</code> endpoint calls.');
    }
    if (!preview.ok) {
      log.debug("Leaving AdminConsole.claimsRequestSection(). The request " +
                "was refused.");
      return form + this.warn('<strong>This request would be ' +
        'refused</strong> at the authorization endpoint with ' +
        '<code>invalid_request</code>, and the client would be told: ' +
        this.esc(preview.error) + ' That is the answer a client gets, shown ' +
        'here rather than corrected &mdash; a console that quietly fixed a ' +
        'malformed request would be the one place this mistake is invisible.');
    }

    const rows = preview.report.map(function (item) {
      return '<tr><td><code>' + self.esc(item.requested) + '</code></td>' +
        '<td><code>' + self.esc(item.claim) + '</code></td><td>' +
        (item.ldap ? '<code>' + self.esc(item.ldap) + '</code>' : '&mdash;') +
        '</td><td><code>' + self.esc(item.value) + '</code></td><td>' +
        self.esc(item.source) + '</td></tr>';
    }).join('');

    log.debug("Leaving AdminConsole.claimsRequestSection(). " +
              preview.report.length + " " +
        "row(s).");
    return form +
      (preview.ignoredMembers.length
        ? this.warn('This request carries the top-level member(s) ' +
          this.codeList(preview.ignoredMembers) +
          ', which section 5.5 does not define. They are <strong>ignored and ' +
          'not refused</strong>: the section says other members MAY be ' +
          'defined, so one this service has never heard of is a client that ' +
          'knows something this one does not. The two acted on are ' +
          this.codeList(oauth2.CLAIMS_REQUEST_MEMBERS) + '.')
        : '') +
      (preview.idTokenNames.length
        ? this.note('The <code>id_token</code> member asks for ' +
          this.codeList(preview.idTokenNames) +
          '. Those are answered where the ID Token is built and are not part ' +
          'of the table below, which is the <code>userinfo</code> member ' +
          'alone.')
        : '') +
      (rows
        ? '<table><tr><th>Asked for</th><th>Claim returned</th><th>From ' +
          'attribute</th><th>Value ' +
          'for ' + this.esc(previewUser) + '</th><th>Source</th></tr>' + rows +
          '</table>'
        : this.note('Nothing in this request resolves to a claim this ' +
                    'service can produce.')) +
      (preview.unresolvable.length
        ? this.note('<strong>Absent, and not an error.</strong> ' +
          this.codeList(preview.unresolvable) + ' &mdash; neither the ' +
          'attribute catalogue nor the sign-in can ' +
          'produce ' + (preview.unresolvable.length > 1 ? 'those' : 'that') +
          '. Section 5.5.1 says a server MUST NOT return an error because a ' +
          'requested claim is unavailable, so the response simply lacks it ' +
          'and the log says so.')
        : '') +
      (preview.essentialAndAbsent.length
        ? this.warn('<strong>Marked essential and still absent:</strong> ' +
          this.codeList(preview.essentialAndAbsent) + '. That is still not ' +
          'an error &mdash; <code>essential</code> is a statement about what ' +
          'the CLIENT will do without the claim, not an instruction to this ' +
          'server. It is logged at warn level so it can be found.')
        : '') +
      (preview.valueMismatches.length
        ? this.warn('<strong>A value was asked for and a different one is ' +
                    'held:</strong> ' +
          this.esc(preview.valueMismatches.join('; ')) + '. The value HELD ' +
          'is what is returned. This service could echo back whatever a ' +
          'request asked it to assert and deliberately does not: everything ' +
          'it says about a person comes from the directory or the invented ' +
          'persona, and a mock that agreed with the request could not be ' +
          'used to test anything.')
        : '') +
      this.note((preview.entryFound
        ? this.esc(previewUser) + ' has an entry under ' +
          '<code>ou=users</code>, so a value marked <em>directory</em> is ' +
          'what an <code>ldapsearch</code> reads.'
        : this.esc(previewUser) + ' has no entry in the directory, so every ' +
          'value above is invented from the username &mdash; the same ' +
          'invented person every time, across restarts.'));
  }

  // The vocabulary table: every name a client may put in a claims request.
  requestableClaimsSection() {
    const { log, claimAttributes, oauth2 } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.requestableClaimsSection().");
    const rows = claimAttributes.requestableClaims().map(function (row) {
      return '<tr><td><code>' + self.esc(row.claim) + '</code></td><td>' +
        (row.grouped ? '<em>the whole claim</em>' : self.esc(row.label)) +
        '</td><td><code>' + self.esc(row.ldap) + '</code></td></tr>';
    }).join('');
    const persona = oauth2.PERSONA_CLAIMS.map(function (name) {
      return '<tr><td><code>' + self.esc(name) + '</code></td><td>Invented ' +
        'from the username at sign-in</td><td><span ' +
        'class="state-none">&mdash;</span></td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.requestableClaimsSection().");
    return '<h3>What a client may ask for</h3>' +
      this.note('A request may name a claim by its <strong>flat ' +
      'name</strong> (<code>birthdate</code>, <code>address.locality</code>) ' +
      'or, for a nested one, by its <strong>top-level name</strong> alone ' +
      '(<code>address</code>) &mdash; which is the spelling section 5.5.1\'s ' +
      'own example uses and which returns the whole Address Claim of OIDC ' +
      'Core 5.1.1 as one object. A <strong>language tag</strong> is part of ' +
      'the name (Core 5.2): <code>family_name#ja-Kana-JP</code> is answered ' +
      'under exactly that name, with the value this service holds &mdash; it ' +
      'keeps one value per attribute, so the tag changes the spelling of the ' +
      'member and not the content.') +
      '<table><tr><th>Claim</th><th>What it is</th><th>From ' +
      'attribute</th></tr>' +
      rows + persona + '</table>';
  }

  // ---------------------------------------------------------------------------
  // GET /admin/vc, POST /admin/vc
  //
  // WHICH CLAIMS AN ISSUED CREDENTIAL CARRIES. The page is a list of LDAP
  // attribute types rather than of claim names, and vc_claims.js says at length
  // why; the short version is that this service has a directory, so a claim can
  // have a value that something other than the credential can see.
  //
  // It is the second page here that CHANGES what the protocol endpoints do, and
  // the first that writes to the directory: saving a selection sweeps every
  // person under ou=users and fills in what they are missing. That sweep is the
  // whole point of the page rather than a side effect — without it, ticking
  // `title` would change every future credential and change nothing an LDAP
  // client could see, and the two halves of this service would quietly stop
  // describing the same people.
  // ---------------------------------------------------------------------------

  // The values of a field that may appear MORE THAN ONCE in the body — which is
  // what a list of checkboxes is, and what nothing else on this console needed
  // until now.
  //
  // helpers.parseBody() cannot answer it: it builds a plain object, so a
  // repeated field keeps only its LAST value and a form with ten boxes ticked
  // would arrive as a selection of one. That is not a bug there — every other
  // form on this console has scalar fields, and changing the shape of that
  // function would change what eight other handlers see — so the repetition is
  // read here, from the raw body, beside the parsed one.
  listField(req, body, name) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.listField(). name=" + name);
    const type = String(req.headers['content-type'] || '');
    if (/json/i.test(type)) {
      const value = body[name];
      const out = Array.isArray(value) ? value.map(String)
                : (value == null || value === '' ? [] : [String(value)]);
      log.debug("Leaving AdminConsole.listField(). " + out.length + " " +
                                                       "value(s) from a JSON " +
                                                       "body.");
      return out;
    }
    const raw = typeof req.body === 'string' ? req.body : '';
    const out = new URLSearchParams(raw).getAll(name);
    log.debug("Leaving AdminConsole.listField(). " + out.length +
              " value(s) from a form body.");
    return out;
  }

  // The one preview row: what this attribute would put in a credential for the
  // person the page is previewing, and where that value came from.
  vcExampleCell(row, persona, byLdap) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.vcExampleCell().");
    const found = byLdap[row.ldap.toLowerCase()];
    if (found) {
      log.debug("Leaving AdminConsole.vcExampleCell().");
      return '<td><code>' + this.esc(found.value) + '</code></td><td>' +
             this.esc(found.source) + '</td>';
    }
    if (!row.from) {
      log.debug("Leaving AdminConsole.vcExampleCell().");
      // The only row with no generator: `description`, which this service
      // already writes on every entry to record the protocols that person has
      // used. Saying so is the point — it is the one attribute whose value is a
      // real fact.
      return '<td>—</td><td>the entry\'s own</td>';
    }
    // Shown in the form the CLAIM would take rather than the form the attribute
    // holds, because that is what the column above it is showing for the
    // selected rows and a table with two conventions in one column is a table
    // nobody can read. The two differ for exactly two attributes — a date and a
    // postal address — and both differences are punctuation.
    const raw = persona[row.from] == null ? '' : String(persona[row.from]);
    log.debug("Leaving AdminConsole.vcExampleCell().");
    return '<td><code>' + this.esc(row.toClaim ? row.toClaim(raw) : raw) +
           '</code></td><td>would be generated</td>';
  }

  vcAttributeTable(previewUser) {
    const { log, vcClaims } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.vcAttributeTable(). previewUser=" +
              previewUser);
    const persona = vcClaims.personaFor(previewUser);
    const built = vcClaims.subjectClaimsFor(previewUser, {});
    const byLdap = {};
    built.report.forEach(function (item) {
      byLdap[item.ldap.toLowerCase()] = item;
    });

    const rows = vcClaims.VC_ATTRIBUTES.map(function (row) {
      const on = vcClaims.isSelected(row.ldap);
      return '<tr><td><input type="checkbox" name="attribute" value="' +
        self.esc(row.ldap) + '"' +
        (on ? ' checked' : '') + '></td>' +
        '<td><code>' + self.esc(row.ldap) + '</code></td>' +
        '<td>' + self.esc(row.schema) + '</td>' +
        '<td><code>' + self.esc(row.claim.join('.')) + '</code></td>' +
        '<td>' + (row.ldpTerm ? '<code>' + self.esc(row.ldpTerm) + '</code>' :
                  '<span ' +
            'class="state-none">—</span>') +
        '</td>' + self.vcExampleCell(row, persona, byLdap) + '</tr>';
    }).join('');

    log.debug("Leaving AdminConsole.vcAttributeTable(). " +
              vcClaims.VC_ATTRIBUTES.length +
        " " +
        "row(s).");
    return '<form method="post" action="/admin/vc"><input type="hidden" ' +
      'name="action" value="select"><table><tr><th>In</th><th>LDAP ' +
      'attribute</th><th>Defined by</th><th>Claim</th><th>ldp_vc ' +
      'term</th><th>In a credential ' +
      'for ' + this.esc(previewUser) + '</th><th>Source</th></tr>' +
      rows + '</table><div class="formrow"><button>Save this ' +
      'selection</button><span class="note">Saving also populates the ' +
      'directory: every person under <code>ou=users</code> gains the ' +
      'attributes they are missing.</span></div></form><div ' +
      'class="formrow"><form method="post" action="/admin/vc" ' +
      'class="inline"><input type="hidden" name="action" ' +
      'value="defaults"><button class="secondary">Restore the six default ' +
      'claims</button></form> <form method="post" action="/admin/vc" ' +
      'class="inline"><input type="hidden" name="action" ' +
      'value="populate"><button class="secondary">Populate the directory ' +
      'now</button></form></div>';
  }

  // What a credential for this person would actually assert, claim by claim. It
  // is built by the same function the issuer calls, not by a second walk of the
  // catalogue — a preview that agreed with the page and disagreed with the
  // credential would be worse than no preview.
  vcPreviewSection(previewUser) {
    const { log, vcClaims } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.vcPreviewSection(). previewUser=" +
              previewUser);
    const built = vcClaims.subjectClaimsFor(previewUser, {});
    const rows = built.report.map(function (item) {
      return '<tr><td><code>' + self.esc(item.claim) + '</code></td>' +
        '<td><code>' + self.esc(item.value) + '</code></td>' +
        '<td>' + self.esc(item.source) + '</td>' +
        '<td>' + (item.ldpTerm ? 'yes' : '<span class="state-none">no</span>') +
        '</td></tr>';
    }).join('');
    const omitted = vcClaims.ldpOmitted();

    log.debug("Leaving AdminConsole.vcPreviewSection(). " +
              built.report.length +
              " claim(s).");
    return '<form method="get" action="/admin/vc"><div class="formrow">' +
      '<label for="user">Preview the credential for</label>' +
      '<input type="text" id="user" name="user" size="20" value="' +
      this.esc(previewUser) + '"><button ' +
      'class="secondary">Show</button></div></form>' +
      this.note((built.entryFound
        ? 'This person has an entry in the directory, so the values below ' +
          'marked <em>directory</em> are what an LDAP client reads from it.'
        : 'This person has no entry in the directory — nobody has ' +
          'authenticated as them and nothing was added by hand — so every ' +
          'value below is generated. It will be the same one next time: the ' +
          'invented person is seeded from the username.')) +
      '<table><tr><th>Claim</th><th>Value</th><th>From</th><th>In ' +
      'ldp_vc</th></tr>' +
      (rows ||
       '<tr><td colspan="4">No attribute is selected, so a credential ' +
               'carries nothing but its subject identifier. That is a ' +
               'legitimate thing to test and is not a mistake this page will ' +
               'correct.</td></tr>') + '</table>' +
      (omitted.length
        ? this.note('<strong>' + this.codeList(omitted) + '</strong> ' +
          (omitted.length === 1 ? 'is selected and does' :
           'are selected and do') +
          ' not appear in an <code>ldp_vc</code> credential. That format is ' +
          'signed over canonicalized JSON-LD, so it can only carry terms the ' +
          'vendored context defines, and the context is vendored precisely ' +
          'because editing it would invalidate every credential already ' +
          'issued against it. The two JOSE-secured formats carry all of them.')
        : '');
  }

  // Whether the ISSUER currently mints the claim this Verifier is asking for.
  // The two pages are separate settings on purpose (see the header), so the
  // disagreement is a state to report rather than one to prevent — and
  // reporting it is what stops "the wallet disclosed nothing" being
  // investigated as a wallet bug.
  vpIssuedCell(claimName) {
    const { log, vpConfig } = this.deps;
    log.debug("Entering AdminConsole.vpIssuedCell().");
    const carried = vpConfig.carriedNow(claimName);
    if (!carried.known) {
      log.debug("Leaving AdminConsole.vpIssuedCell().");
      return '<td><span class="state-none">not a claim this service ' +
             'issues</span></td>';
    }
    if (!carried.carried.length) {
      log.debug("Leaving AdminConsole.vpIssuedCell().");
      return '<td><span class="state-expired">no — not selected on ' +
             '<a href="/admin/vc">/admin/vc</a></span></td>';
    }
    if (carried.missing.length) {
      log.debug("Leaving AdminConsole.vpIssuedCell().");
      return '<td><span class="state-expired">partly — ' +
             this.codeList(carried.missing) +
             ' not selected</span></td>';
    }
    log.debug("Leaving AdminConsole.vpIssuedCell().");
    return '<td><span class="state-valid">yes</span></td>';
  }

  // One catalogue row. The DCQL path column is shown for the format the next
  // unqualified request will use, because a path is not a property of the
  // claim: the same claim is ["given_name"], ["credentialSubject","given_name"]
  // or ["credentialSubject","birthDate"] depending on what is being asked for,
  // and a column that picked one silently would be wrong two-thirds of the
  // time.
  vpClaimRow(row, format) {
    const { log, vpConfig } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.vpClaimRow().");
    const on = vpConfig.isRequested(row.claim);
    const paths = vpConfig.dcqlPathsFor(format, row.claim);
    const attributes = row.members.map(function (member) {
      return '<code>' + self.esc(member.ldap) + '</code> <span ' +
        'class="state-none">(' +
             self.esc(member.schema) + ')</span>';
    }).join('<br>');
    log.debug("Leaving AdminConsole.vpClaimRow().");
    return '<tr><td><input type="checkbox" name="claim" value="' +
      this.esc(row.claim) + '"' +
      (on ? ' checked' : '') + '></td>' +
      '<td><code>' + this.esc(row.claim) + '</code>' +
      (row.nested ?
       '<br><span class="state-none">one object, ' + row.members.length +
                    ' attributes</span>' : '') + '</td>' +
      '<td>' + this.esc(row.label) + '</td>' +
      '<td>' + attributes + '</td>' +
      '<td>' + (paths.length
        ? paths.map(function (path) {
          return '<code>' + self.esc(JSON.stringify(path)) + '</code>';
        }).join('<br>')
        : '<span class="state-expired">cannot be asked for in this ' +
          'format</span>') + '</td><td>' +
          (row.ldpTerms.length ? this.codeList(row.ldpTerms)
                                    : '<span class="state-none">—</span>') +
      '</td>' +
      this.vpIssuedCell(row.claim) + '</tr>';
  }

  // The claims being asked for that are NOT in the catalogue. Rendered as
  // ticked checkboxes in the same form rather than as a separate list with its
  // own Save, because a form that dropped them the moment somebody saved the
  // table above would silently undo a deliberate configuration.
  vpExtraRows(format) {
    const { log, vpConfig } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.vpExtraRows().");
    const extras = vpConfig.requestedRows()
                           .filter(function (row) { return !row.inCatalogue; });
    if (!extras.length) {
      log.debug("Leaving AdminConsole.vpExtraRows().");
      return '';
    }
    log.debug("Leaving AdminConsole.vpExtraRows().");
    return '<h3>Asked for, and not in the catalogue</h3>' +
      this.note('Nothing this service issues carries these, which is what ' +
      'makes them worth asking for: it is the only way to see what a wallet ' +
      'does with a request it cannot satisfy, and what this Verifier says ' +
      'when it checks. They are ticked below so that saving the table above ' +
      'keeps them — untick one to stop asking for it.') +
      '<table><tr><th>In</th><th>Claim</th><th>DCQL path (' + this.esc(format) +
      ')</th></tr>' +
      extras.map(function (row) {
        const paths = vpConfig.dcqlPathsFor(format, row.claim);
        return '<tr><td><input type="checkbox" name="claim" value="' +
          self.esc(row.claim) + '" ' +
          'checked></td><td><code>' + self.esc(row.claim) + '</code></td>' +
          '<td>' + paths.map(function (path) {
            return '<code>' + self.esc(JSON.stringify(path)) + '</code>';
          }).join('<br>') + '</td></tr>';
      }).join('') + '</table>';
  }

  vpClaimsSection(format) {
    const { log, vpConfig } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.vpClaimsSection(). format=" + format);
    const rows = vpConfig.REQUESTABLE.map(function (
        row) { return self.vpClaimRow(row, format); }).join('');
    const omitted = format === 'ldp_vc' ? vpConfig.ldpOmitted() : [];
    log.debug("Leaving AdminConsole.vpClaimsSection(). " +
              vpConfig.REQUESTABLE.length + " " +
        "row(s).");
    return '<form method="post" action="/admin/vc-verifier-config"><input ' +
      'type="hidden" name="action" value="select"><table><tr><th>Ask</th><th>' +
      'Claim</th><th>Label</th><th>LDAP attribute (defined by)</th><th>DCQL ' +
      'path (' + this.esc(format) + ')</th><th>ldp_vc term</th>' +
      '<th>Issued now</th></tr>' + rows + '</table>' +
      this.vpExtraRows(format) +
      '<div class="formrow"><button>Save this request</button>' +
      this.note('It applies to the next Authorization Request. One already ' +
      'in flight keeps the claims it was built with — a Verifier that judged ' +
      'a presentation against a list changed after it asked would refuse a ' +
      'wallet for answering the question it was really ' +
      'asked.') + '</div></form>' +
      (omitted.length
        ? this.note('<strong>' + this.codeList(omitted) + '</strong> ' +
          (omitted.length === 1 ? 'is asked for and is' :
           'are asked for and are') +
          ' dropped from an <code>ldp_vc</code> query. That format is signed ' +
          'over canonicalized JSON-LD, so only terms the vendored context ' +
          'defines can be named at all, and asking under a name it does not ' +
          'define would fail canonicalization rather than return less. The ' +
          'two JOSE-secured formats ask for all of them.')
        : '') +
      '<div class="formrow"><form method="post" ' +
      'action="/admin/vc-verifier-config" class="inline"><input ' +
      'type="hidden" name="action" value="add"><label for="claim">Also ask ' +
      'for a claim that is not in the catalogue</label><input type="text" ' +
      'id="claim" name="claim" size="24" ' +
      'placeholder="drivers_licence_number"><button ' +
      'class="secondary">Add</button></form> <form method="post" ' +
      'action="/admin/vc-verifier-config" class="inline"><input ' +
      'type="hidden" name="action" value="defaults"><button ' +
      'class="secondary">Back to what this process started ' +
      'with</button></form></div>';
  }

  // The credential types a wallet may submit, and which one an unqualified
  // request asks for. One request is for ONE of them: a presentation cannot
  // convert between formats, so a wallet holding a jwt_vc_json credential has
  // nothing to answer a dc+sd-jwt query with — and the honest outcome is that
  // it says so rather than that this page pretends the choice does not matter.
  vpFormatsSection(format) {
    const { log, vpConfig } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.vpFormatsSection(). format=" + format);
    const rows = vpConfig.FORMATS.map(function (item) {
      const configs = item.configs.map(function (id) {
        return '<code>' + self.esc(id) + '</code>';
      }).join('<br>');
      return '<tr><td><input type="radio" name="format" value="' +
             self.esc(item.id) +
        '"' +
        (item.id === format ? ' checked' : '') + '></td>' +
        '<td><code>' + self.esc(item.id) + '</code><br>' +
        self.esc(item.label) + '</td>' +
        '<td><code>' + self.esc(item.identifiedBy) + '</code><br><code>' +
        self.esc(item.identifierText) + '</code></td>' +
        '<td>' + self.esc(item.selectiveDisclosure) + '</td>' +
        '<td>' + self.esc(item.holderBinding) + '</td>' +
        '<td>' + configs + '</td>' +
        '<td><a href="' + self.esc('/oid4vp/verifier?format=' +
                                   encodeURIComponent(item.id)) +
        '">Present one</a></td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.vpFormatsSection(). " +
              vpConfig.FORMATS.length + " " +
        "row(s).");
    return '<form method="post" action="/admin/vc-verifier-config"><input ' +
      'type="hidden" name="action" ' +
      'value="format"><table><tr><th>Default</th><th>Format</th><th>' +
      'Identified ' +
      'in DCQL by</th><th>Selective disclosure</th><th>Holder ' +
      'binding</th><th>Issued here as</th><th></th></tr>' +
      rows + '</table>' +
      '<div class="formrow"><button>Ask for this one by default</button>' +
      this.note('The default is what <code>/oid4vp/start</code> asks for ' +
      'when the link that reached it names no format. The bar door\'s three ' +
      'format buttons name one explicitly, so they are unaffected — a button ' +
      'saying "present an SD-JWT VC" that asked for something else would be ' +
      'lying in the one place a reader is most likely to trust it.') +
      '</div></form>' +
      this.note(vpConfig.FORMATS.map(function (item) {
        return '<strong>' + self.esc(item.id) + '</strong> — ' +
               self.esc(item.what);
      }).join('<br><br>')) +
      this.note('The identifying values are not settable here. They are what ' +
      'this service\'s own issuer mints (<code>vc_configs.js</code>), and a ' +
      'Verifier asking for a <code>vct</code> nobody here issues would be a ' +
      'request no wallet in this stack could ever satisfy — a negative worth ' +
      'having, but one that belongs to the issuer\'s configuration rather ' +
      'than to a text box on this page.');
  }

  // HOW a family is separated, in the words the row itself carries. This used
  // to print the literal "by path" for every `full` row, which was true of all
  // of them until the embedded directory became per realm: LDAP is separated by
  // DN — a subtree per realm inside one naming context — and a table that
  // called that "by path" would be describing the one family whose separation
  // is NOT a path segment as though it were.
  separatedBy(by) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.separatedBy().");
    const how = String(by || 'path');
    log.debug("Leaving AdminConsole.separatedBy().");
    return 'by ' + (how === 'dn' ? 'DN' : how);
  }

  realmSupportTable() {
    const { log, realms } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.realmSupportTable().");
    const rows = realms.realmSupport().map(function (row) {
      const state = row.state === 'full'
        ? '<span class="m">' + self.esc(self.separatedBy(row.by)) + '</span>'
        : (row.state === 'partial'
            ? '<span class="eff" title="Realm-aware, but not wholly">' +
              self.esc(row.by) + '</span>'
            : '<span class="none">shared</span>');
      // The note is realms.js's own prose and runs to a paragraph on the rows
      // that matter most — the directory's is 1,700 characters — so it folds.
      // What stays on the row is the family and whether it is separated, which
      // is the question somebody scans this table to answer.
      return '<tr><td>' + self.esc(row.family) + '</td><td>' + state +
             '</td><td>' +
             self.note(self.esc(row.note)) + '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.realmSupportTable().");
    return '<table><tr><th>Family</th><th>Separated</th><th>What that ' +
           'means</th></tr>' +
           rows + '</table>';
  }

  realmsListPage(req) {
    const { log, realmsJson, pagedRows, queryWith, realms, config,
            pageParamsOf } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.realmsListPage().");
    const json = realmsJson(req);
    const listView = this.listViewOf('/admin/realms', req.query);
    const paged = pagedRows(req.query, json.realms, { path: '/admin/realms' });
    const pg = paged.paging;
    const rows = paged.shown.map(function (row) {
      const href = '/admin/realms' + queryWith(listView, { realm: row.id });
      return '<tr><td><a href="' + self.esc(href) + '"><code>' +
             self.esc(row.id) +
        '</code></a>' +
        (row.builtin ? ' <span class="why">built in</span>' : '') +
        '</td><td>' + self.esc(row.name) + '</td>' +
        '<td><code>' + self.esc(row.domain) + '</code></td>' +
        '<td><code>' + self.esc(row.pathPrefix || '/') + '</code></td>' +
        '<td><code>' + self.esc(row.kid) + '</code></td>' +
        '<td class="num">' + row.settings.length + '</td></tr>';
    }).join('');

    const carryBack = '<input type="hidden" name="back" value="' +
      this.esc(queryWith(listView, {})) + '">';

    const inner =
      '<p class="sub">' + realms.count() + ' realm(s). Everything under a ' +
      'realm\'s prefix is that realm; everything under no prefix is the ' +
      'default one.</p>' +
      REALMS_CAVEAT +

      // `realms.active()` IS FALSE FOR TWO DIFFERENT REASONS AND THIS USED TO
      // NAME ONLY ONE OF THEM. It is `realms.size > 0 &&
      // config.value('realms.enabled')`, and the banner here said
      // "`realms.enabled` is false" whenever it came back false — which on a
      // service with the setting ON and no realm yet defined is a page
      // asserting something untrue about a setting a reader can go and look at.
      // That is the worst shape a console message can take: it sent somebody to
      // Configuration to turn on a thing that was already on.
      //
      // So the two states are told apart, and the second is not a warning at
      // all. "The flag is on and nothing has been defined" is the ORDINARY
      // state of this service — the contract `common/realms.js` states is that
      // a service with no realms defined behaves exactly as it did before
      // realms existed — so it is a note saying what to do next, not a yellow
      // box saying something is wrong.
      (realms.active()
        ? ''
        : !config.value('realms.enabled')
          ? this.warn('<strong>Trust realms are switched off.</strong> ' +
            '<code>realms.enabled</code> is false, so every prefix below ' +
            'answers 404 and this whole service is the default realm. The ' +
            'definitions are untouched — that is what this setting is for: ' +
            'it lets a realm be ruled out as the cause of something without ' +
            'anything being deleted. Turn it back on in the settings at the ' +
            'foot of this page.')
          : this.note('<strong>Trust realms are switched ON and none has ' +
            'been defined, which is this service\'s ordinary state rather ' +
            'than something to fix.</strong> <code>realms.enabled</code> is ' +
            '<code>true</code>; the feature does nothing until a realm ' +
            'exists, because the built-in <code>default</code> realm has an ' +
            'empty prefix and IS this service. That is a property rather ' +
            'than a coincidence — a service with no realm defined behaves ' +
            'exactly as it did before realms existed, which is what keeps ' +
            'every client, container and test that predates them working ' +
            'unchanged. <strong>Define one below</strong> and its prefix ' +
            'starts answering immediately: a switcher appears on every page ' +
            'of this console, and <code>GET /realms</code> starts reporting ' +
            '<code>active: true</code>.')) +

      '<h2>The realms</h2><table><tr><th>Id</th><th>Name</th>' +
      '<th>Domain</th><th>Path prefix</th><th>Signing key</th><th ' +
      'class="num">Settings</th></tr>' + rows + '</table>' +
      this.pageNavPair('/admin/realms', pageParamsOf(req.query), pg).head +
      this.perPageForm('/admin/realms', 'per', req.query.per, pg.perPage, '',
                       listView) +

      '<h2>Define a realm</h2>' +
      this.note('The id becomes a path segment, so it is lower-case letters, ' +
      'digits and hyphens. It may not be <code>default</code> and it may not ' +
      'be the first segment of a path this service already serves — ' +
      (json.reserved.length ? this.codeList(json.reserved.slice(0, 12)) +
        (json.reserved.length > 12 ? ' and ' + (json.reserved.length - 12) +
         ' ' +
            'more' : '')
        : 'nothing is registered yet') +
      ' — whatever <code>realms.pathSegment</code> is set to, precisely so ' +
      'that clearing that setting cannot turn an existing realm into a ' +
      'shadow over the console or the authorization server.') +
      this.note('<strong>The domain</strong> — <code>iyasec.io</code>, ' +
      '<code>dev.iyasec.io</code> — is the root of every NAME the realm ' +
      'invents: its directory is a tree of its own at the RFC 2247 mapping ' +
      'of it (<code>iyasec.io</code> is <code>dc=iyasec,dc=io</code>), its ' +
      'Kerberos realm is the domain in capitals, its SPIFFE trust domain is ' +
      'the domain, and its identity providers call themselves ' +
      '<code>urn:&lt;domain&gt;:idp</code>. It is not where the realm is ' +
      'REACHED — the issuer and every URL still come from the host a request ' +
      'arrived on. No two realms may share one; one inside another\'s is ' +
      'allowed and is a separate tree. <strong>It is fixed once the realm is ' +
      'created.</strong> Left empty it is <code>&lt;id&gt;.' +
      this.esc(realms.domainOf(realms.DEFAULT_ID)) + '</code>.') +
      '<form method="post" action="/admin/realms">' + carryBack +
      '<input type="hidden" name="action" value="create"><div ' +
      'class="formrow"><label for="rid">Id</label><input type="text" ' +
      'id="rid" name="id" size="16" placeholder="acme" required><label ' +
      'for="rname">Name</label><input type="text" id="rname" name="name" ' +
      'size="22" placeholder="Acme Corporation"><label ' +
      'for="rdomain">Domain</label><input type="text" id="rdomain" ' +
      'name="domain" size="22" placeholder="iyasec.io" ' +
      'autocapitalize="off" spellcheck="false"><label ' +
      'for="rdesc">Description</label><input type="text" id="rdesc" ' +
      'name="description" size="40"><button type="submit">Define ' +
      'it</button></div></form><h2>What is separated, and what is ' +
      'shared</h2><p class="lead">A realm separates what this service ISSUES ' +
      'and everything it is holding while it issues it — keys, sessions, ' +
      'codes, tokens, offers, artifacts, statistics and the audit log — and ' +
      'since each realm has a directory of its own, the people, groups, ' +
      'applications and policies in it. The families on sockets with no ' +
      'path in them are told apart some other way, and a few things belong ' +
      'to the process and are shared. This table is the whole list, ' +
      'and <code>GET /realms</code> answers the same thing to a client that ' +
      'cannot read a console.</p>' +
      this.realmSupportTable() +
      // The two realms.* rows. `realms.enabled` is the one that makes every
      // prefixed path in this service answer or not, which is worth being able
      // to see beside the list of realms it governs.
      this.configFormsFor('/admin/realms');

    log.debug("Leaving AdminConsole.realmsListPage().");
    json.settings = this.configSettingsJson('/admin/realms');
    log.debug("Leaving AdminConsole.realmsListPage().");
    return { json: json, inner: inner };
  }

  realmDetailPage(req, wanted) {
    const { log, realms, realmJson, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.realmDetailPage(). realm=" + wanted);
    const realm = realms.get(wanted);
    if (!realm) {
      log.debug("Leaving AdminConsole.realmDetailPage(). No such realm.");
      return { missing: true, json: { error: 'no_such_realm', realm: wanted },
               inner: '<div class="err">No realm called <code>' +
                      this.esc(wanted) +
                      '</code> is defined. <a href="/admin/realms">The ' +
                      'list</a> is what there is.</div>' };
    }
    const json = realmJson(req, realm);
    const listView = this.listViewOf('/admin/realms', req.query);
    const carryBack = '<input type="hidden" name="back" value="' +
      this.esc(queryWith(listView, {})) + '">';
    const inRealm = realms.currentId() === realm.id;

    const settingRows = json.settings.length
      ? json.settings.map(function (row) {
          return '<tr><td><code>' + self.esc(row.key) + '</code></td>' +
                 '<td>' + self.esc(row.label) + '</td>' +
                 '<td><code>' + self.esc(row.value) + '</code></td>' +
                 '<td><form method="post" action="/admin/realms" ' +
                 'class="inline">' +
                 carryBack + '<input type="hidden" name="action" ' +
                 'value="unset"><input type="hidden" name="id" value="' +
                 self.esc(realm.id) + '">' +
                 '<input type="hidden" name="key" value="' + self.esc(row.key) +
                 '"><button type="submit" ' +
                 'class="secondary">Unset</button></form></td></tr>';
        }).join('')
      : '<tr><td colspan="4" class="none">Nothing. This realm is configured ' +
        'exactly as the process is — which is a realm that differs only in ' +
        'its key, its sessions and what it has issued.</td></tr>';

    const endpointRows = Object.keys(json.endpoints).map(function (name) {
      return '<tr><td>' + self.esc(name) + '</td><td class="who"><a href="' +
             self.esc(json.endpoints[name]) + '"><code>' +
             self.esc(json.endpoints[name]) +
             '</code></a></td></tr>';
    }).join('');

    const inner =
      '<p class="sub">' + this.esc(realm.name) +
      (realm.builtin ? ' — the built-in realm' : '') + '</p>' +
      (realm.description ? '<p class="lead">' + this.esc(realm.description) +
       '</p>' :
       '') +

      (inRealm
        ? '<div class="ok">You are reading this console ' +
          '<strong>inside</strong> this realm. Every settings form in this ' +
          'console writes here.</div>'
        : this.warn('You are reading this console in the <strong>' +
          this.esc(realms.current().name) + '</strong> realm. The switcher ' +
          'on the left moves to this one; until then a settings form writes ' +
          'to the realm you are in, not to this one.')) +

      '<h2>Its domain</h2>' +
      this.note('<code>' + this.esc(json.domain) + '</code>' +
      (realm.builtin ? ', from <code>global.domain</code>' : '') +
      ', fixed ' + (realm.builtin ? 'until a restart with another value'
                                  : 'since the realm was created') +
      '. Its directory is the tree at <code>' + this.esc(json.baseDn) +
      '</code>, a naming context of its own on the shared LDAP socket, and ' +
      'the names it invents — Kerberos realm, SPIFFE trust domain, entity ' +
      'IDs, the address a development-mode person is given — are built from ' +
      'it; the ones seeded when it was created are among its settings ' +
      'below.') +
      '<h2>Where it answers</h2>' +
      this.note('Path prefix <code>' +
                this.esc(json.pathPrefix || '(none — this is ' +
      'the default realm)') + '</code>. Every HTTP endpoint this service has ' +
      'is under it, unchanged: what is <code>/oauth2/token</code> in the ' +
      'default realm is ' +
      '<code>' + this.esc(json.pathPrefix) + '/oauth2/token</code> here.') +
      '<table><tr><th>Document</th><th>URL</th></tr>' + endpointRows +
      '</table>' +
      this.note('The signing key is <code>' + this.esc(json.kid) + '</code>, ' +
      'generated for this realm and held only in memory. A token minted here ' +
      'does not verify against any other realm\'s JWKS, which is what makes ' +
      'two realms two authorization servers rather than one served twice.') +

      '<h2>What this realm sets</h2>' +
      this.note('Every setting in <a href="/admin/config">this service\'s ' +
      'table</a> can be set per realm, above whatever the process as a whole ' +
      'is configured with and below nothing. The two exceptions are ' +
      '<code>realms.enabled</code> and <code>realms.pathSegment</code>: a ' +
      'realm that could switch realms off, or move the prefix it was found ' +
      'under, would be doing it half way through the request that found it.') +
      '<table><tr><th>Key</th><th>Setting</th><th>Value</th><th></th></tr>' +
      settingRows + '</table>' +
      '<form method="post" action="/admin/realms">' + carryBack +
      '<input type="hidden" name="action" value="set">' +
      '<input type="hidden" name="id" value="' + this.esc(realm.id) +
      '"><div class="formrow"><label for="skey">Key</label><input ' +
      'type="text" id="skey" name="key" size="30" ' +
      'placeholder="saml2.entityId" required><label ' +
      'for="sval">Value</label><input type="text" id="sval" name="value" ' +
      'size="30"><button type="submit">Set it ' +
      'here</button></div></form><h2>Name and description</h2><form ' +
      'method="post" action="/admin/realms">' + carryBack +
      '<input type="hidden" name="action" value="update">' +
      '<input type="hidden" name="id" value="' + this.esc(realm.id) + '">' +
      '<div class="formrow"><label for="uname">Name</label>' +
      '<input type="text" id="uname" name="name" size="22" value="' +
      this.esc(realm.name) + '"><label ' +
      'for="udesc">Description</label><input type="text" id="udesc" ' +
      'name="description" size="46" value="' +
      this.esc(realm.description) + '">' +
      '<button type="submit">Save</button></div></form>' +

      (realm.builtin
        ? '<h2>It cannot be removed</h2>' +
          this.note('Every URL this service published before trust realms ' +
          'existed is a URL in this realm, so removing it would remove the ' +
          'service. There is deliberately no button.')
        : '<h2>Remove it</h2>' +
          this.note('<strong>Everything it holds goes with it</strong> — its ' +
          'sessions, its authorization codes, its tokens, its offers, its ' +
          'service providers, its statistics, its audit log and its signing ' +
          'key. That is deliberate rather than thorough: a realm re-created ' +
          'with the same id inheriting the last one\'s sessions would be the ' +
          'single most surprising thing a re-created realm could do. Nothing ' +
          'is removed from the shared directory, because nothing there ' +
          'belongs to a realm.') +
          '<form method="post" action="/admin/realms">' + carryBack +
          '<input type="hidden" name="action" value="remove">' +
          '<input type="hidden" name="id" value="' + this.esc(realm.id) + '">' +
          '<button type="submit" class="danger">Remove ' + this.esc(realm.id) +
          '</button></form>');

    log.debug("Leaving AdminConsole.realmDetailPage().");
    return { json: json, inner: inner };
  }

  realmsView(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.realmsView().");
    const wanted = String(req.query.realm || '').trim();
    if (wanted) {
      const detail = this.realmDetailPage(req, wanted);
      log.debug("Leaving AdminConsole.realmsView(). The drill-down.");
      return { missing: !!detail.missing, json: detail.json,
               inner: detail.inner,
               title: 'Trust realm ' + wanted,
               up: this.upTo('/admin/realms', wanted,
                             this.listViewOf('/admin/realms', req.query)) };
    }
    const list = this.realmsListPage(req);
    log.debug("Leaving AdminConsole.realmsView(). The list.");
    return { json: list.json, inner: list.inner, title: 'Trust realms' };
  }

  // ---------------------------------------------------------------------------
  // /admin/config — THE SETTINGS THAT BELONG TO NO PROTOCOL, AND THE INDEX OF
  // WHERE EVERY OTHER ONE IS EDITED.
  //
  // It was every setting this service has, in one page, until 2026-08-27. What
  // that page got right is that the sections were the PROTOCOLS, in the order
  // config.js's table declares them, because that is where a reader looks:
  // somebody who wants the Kerberos realm thinks of Kerberos. What it got wrong
  // follows from the same sentence — they think of Kerberos, so they open the
  // console's Kerberos page, and until that day the answer there was a table of
  // readings and a link to here. A hundred and fifty-four rows on one page is
  // also a page nobody reads: the group somebody wants is one of twenty-two,
  // and the twenty-one they scroll past are noise every time.
  //
  // So the settings live on the page for the family they configure, and what is
  // left here is the two things that have nowhere else to be:
  //
  //   * **The groups that belong to no protocol** — `Global` (it was five rows,
  //     a bind address, a port, the scheme, the proxy header and the log level,
  //     and has grown the mode, the proxy and worker settings since), and
  //     `Key material` and `Web security` beside it. Facts about the PROCESS
  //     rather than about anything it speaks, so there is no family page they
  //     would be less surprising on. They are drawn by the same
  //     `configSection()` every protocol page draws; SETTING_HOMES is the list.
  //   * **The INDEX**: every group, how many settings are in it, how many of
  //     those are overridden right now, and the page that edits it. Derived
  //     from SETTING_HOMES and `config.groups()`, so a group cannot be added to
  //     config.js and go unlisted here, and a page cannot be renamed out from
  //     under a link.
  //
  // Two things did NOT move, and both are deliberate. `Reset all` stays here,
  // because clearing every runtime override in the service is not an act about
  // one protocol and a button that did it from the Kerberos page would be the
  // only control in this console whose blast radius was invisible from where it
  // was pressed. And `?format=json` still answers the WHOLE table — `GET
  // /admin-api/config` is the API's configuration resource and a caller asking
  // it for the settings should not have to visit twenty-one pages to assemble
  // them. The page narrowed; the resource did not.
  //
  // EVERY ROW SAYS WHERE ITS VALUE CAME FROM. That is the question this page
  // exists to answer and it is the one that used to require a grep: a value can
  // arrive from a runtime override, from an environment variable (its own or
  // the legacy STS_ISSUER), from the appconfig file this process was started
  // with, or from env/defaults.js under it — and the four are indistinguishable
  // once they have been read.
  //
  // THERE IS NO FIFTH, since 2026-08-24: a setting with a value in none of them
  // stops this service from starting rather than quietly taking a constant out
  // of a module. So a value shown here is a value somebody can find in a file,
  // which is what makes the Source column worth reading at all.
  //
  // A restart-only row is SHOWN and its input is DISABLED, with the reason
  // beside it. Both halves matter. Hiding it would answer "what is this service
  // configured with?" with three quarters of the answer; letting it be typed
  // into would accept a change that does nothing, which reads as having worked.
  //
  // No script here, like every other page in this console: each section is a
  // plain form that posts the whole section at once (`set-many`), and each row
  // has a Reset button of its own. See the shell's note above — `script-src
  // 'none'` is what makes reflected content moot for this service, and the API
  // explorer (at `/admin/api-explorer` since 2026-09-09) is the one console
  // page that is an exception.
  // ---------------------------------------------------------------------------

  // The five sources, as a phrase a reader can act on. `env-legacy` is its own
  // case rather than being folded into `env`, because the variable it names is
  // not the one the rest of the row talks about — being told the value comes
  // from "the environment" while STS_SAML_ISSUER is unset is the kind of true
  // answer that costs twenty minutes.
  //
  // `defaults` is a case for exactly the same reason. The appconfig layer is
  // TWO files unioned — env/defaults.js, and whatever CONFIG_FILE names over it
  // — and telling somebody a value comes from "the appconfig file" when their
  // file does not mention it would send them to edit a line that is not there.
  // So the two halves of that layer are named separately and each names its own
  // file.
  //
  // The last line is now unreachable for anything but a `derived` setting:
  // config.js refuses to start when a non-derived setting has no value in
  // either file and no environment variable. It stays because the three derived
  // ones DO resolve through their `dflt`, which is a function of a neighbour
  // rather than a literal anybody could have written in a file.
  sourceNote(setting) {
    const { log, config } = this.deps;
    log.debug("Entering AdminConsole.sourceNote().");
    if (setting.source === 'override') {
      log.debug("Leaving AdminConsole.sourceNote().");
      return 'set here, in memory only';
    }
    if (setting.source === 'env') {
      log.debug("Leaving AdminConsole.sourceNote().");
      return 'from ' + setting.env;
    }
    if (setting.source === 'env-legacy') {
      log.debug("Leaving AdminConsole.sourceNote().");
      return 'from ' + setting.legacyEnv + ' (the legacy variable)';
    }
    if (setting.source === 'appconfig') {
      log.debug("Leaving AdminConsole.sourceNote().");
      return 'from ' + (process.env.CONFIG_FILE || 'the appconfig file');
    }
    if (setting.source === 'defaults') {
      log.debug("Leaving AdminConsole.sourceNote().");
      return 'from ' + config.DEFAULTS_FILE + ' (the default appconfig file)';
    }
    log.debug("Leaving AdminConsole.sourceNote().");
    return 'derived from another setting';
  }

  // ---------------------------------------------------------------------------
  // THE SETTINGS BLOCK EVERY PROTOCOL PAGE DRAWS, AND THE ONE FUNCTION THAT
  // DRAWS IT.
  //
  // `configFormsFor('/admin/kerberos')` is the whole of what a page has to say
  // to own its settings: it looks the page up in SETTING_HOMES, renders one
  // `configSection()` per group that lives there, and returns the markup. A
  // page therefore names no key, decides no order and repeats no prose — which
  // is the property that keeps twenty-one pages from coming to describe the
  // same table twenty-one ways, and it is why the block reads identically
  // wherever it appears.
  //
  // WHAT IT SAYS IS SAID ONCE, HERE. Each of these pages used to carry its own
  // sentence about the configuration page owning its settings, and the three
  // that existed had already drifted into three different claims. The lead note
  // below is the only one now, and everything in it is derived: which groups
  // these are, how many rows cannot be changed while the service runs, whether
  // the group is shared with another page, and which file to put a value in to
  // make it survive a restart.
  //
  // A PAGE WITH NO ROW IN SETTING_HOMES GETS AN EMPTY STRING, deliberately. The
  // alternative is a page that draws an empty *Settings* heading because
  // somebody added the call before adding the table row, which reads as "this
  // family has no settings" — a claim no page here should make by accident.
  // ---------------------------------------------------------------------------

  // The row for a group, or null. A page asks the other way round; both are
  // looked up in the same table.
  settingHomeRowOf(group) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.settingHomeRowOf().");
    log.debug("Leaving AdminConsole.settingHomeRowOf().");
    return SETTING_HOMES.filter(function (row) {
      return row.group === group;
    })[0] || null;
  }

  // The described settings this page owns, grouped, in `config.js`'s
  // declaration order rather than SETTING_HOMES's. Two orders would be two
  // answers to "which comes first" and the table that already decides it is the
  // one that also carries the reasoning for what sits beside what.
  settingsGroupsFor(path) {
    const { log, config } = this.deps;
    log.debug("Entering AdminConsole.settingsGroupsFor(). path=" + path);
    const mine = SETTING_HOMES.filter(function (row) {
      return row.pages.indexOf(path) >= 0;
    }).map(function (row) { return row.group; });
    const out = config.groups().filter(function (group) {
      return mine.indexOf(group.group) >= 0;
    });
    log.debug("Leaving AdminConsole.settingsGroupsFor(). " + out.length +
              " group(s).");
    return out;
  }

  // What `?format=json` and the management API answer for a page's settings.
  // The same described rows the form is drawn from — including `source`,
  // `editable` and `restartReason` — so a caller can see WHY a value is what it
  // is without fetching the whole table from /admin-api/config and filtering it
  // themselves.
  configSettingsJson(path) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.configSettingsJson(). path=" + path);
    const groups = this.settingsGroupsFor(path);
    const all = groups.reduce(function (rows, group) {
      return rows.concat(group.settings);
    }, []);
    const json = {
      page: path,
      groups: groups,
      settingCount: all.length,
      editableCount:
        all.filter(function (setting) { return setting.editable; }).length,
      overridden: all.filter(function (setting) { return setting.overridden; })
                     .map(function (setting) { return setting.key; }),
      // Where a caller POSTs a change. Named rather than left to be inferred:
      // these settings are edited through the configuration resource wherever
      // they are DRAWN, which is the whole of why there is no second store.
      setWith: 'POST /admin-api/config/set-many'
    };
    log.debug("Leaving AdminConsole.configSettingsJson(). " +
              json.settingCount + " " +
        "setting(s).");
    return json;
  }

  // The other pages a group of these settings is also drawn on, as a sentence,
  // or '' when there are none. Only `SAML` has any today; the sentence is
  // derived so that a second shared group cannot arrive without being
  // announced.
  sharedSettingNote(groupName, path) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.sharedSettingNote().");
    const row = this.settingHomeRowOf(groupName);
    if (!row || row.pages.length < 2) {
      log.debug("Leaving AdminConsole.sharedSettingNote().");
      return '';
    }
    const others =
      row.pages.filter(function (other) { return other !== path; });
    log.debug("Leaving AdminConsole.sharedSettingNote().");
    return '<strong>These are the same settings ' +
      others.map(function (other) {
        return '<a href="' + self.esc(other) + '">' +
               self.esc(self.labelOfPath(other)) + '</a>';
      }).join(' and ') + ' draws.</strong> One setting, shown in both places ' +
      'because it governs both: a value saved here is saved there. Nothing ' +
      'is copied — both forms post to the same action against the same ' +
      'override map.';
  }

  // A page's own label, off NAV, so a cross-reference cannot name a tab that
  // has been renamed. `upTo()` does the same thing for a breadcrumb and for the
  // same reason; this one answers with the label alone.
  labelOfPath(path) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.labelOfPath().");
    const item = NAV.filter(function (row) { return row.path === path; })[0];
    log.debug("Leaving AdminConsole.labelOfPath().");
    return item ? item.label : path;
  }

  // The block itself.
  configFormsFor(path) {
    const { log, persistence } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.configFormsFor(). path=" + path);
    const groups = this.settingsGroupsFor(path);
    if (!groups.length) {
      log.debug("Leaving AdminConsole.configFormsFor(). No settings live on " +
                path + ".");
      return '';
    }

    const all = groups.reduce(function (rows, group) {
      return rows.concat(group.settings);
    }, []);
    const fixed =
        all.filter(function (setting) { return !setting.editable; }).length;
    const overridden =
        all.filter(function (setting) { return setting.overridden; })
                          .map(function (setting) { return setting.key; });
    const configFile = process.env.CONFIG_FILE || 'env/local.js';

    const shared = groups.map(function (group) {
      return self.sharedSettingNote(group.group, path);
    }).filter(Boolean).map(function (text) { return self.note(text); })
      .join('');

    const inner = '<h2>Settings</h2>' +

      this.note('The appconfig rows that decide what this family does, on ' +
      'the page for the family rather than on <a ' +
      'href="/admin/config">Configuration</a>. They are the same settings, ' +
      'written through the same function against the same override map — ' +
      'this is a second DOOR onto them and not a second place they live, ' +
      'which is the rule <a href="/admin/token-lifetimes">Token ' +
      'lifetimes</a> was the first page here to apply. The <em>Source</em> ' +
      'column says where each value came from: a runtime override set on a ' +
      'page like this one, an environment variable, the appconfig file this ' +
      'process was started with, or the default appconfig file under it.') +

      shared +

      // ---------------------------------------------------------------------
      // THIS PARAGRAPH USED TO BE ONE SENTENCE AND IT WAS TRUE FOR THE WHOLE
      // LIFE OF THIS SERVICE UNTIL 2026-08-27.
      //
      // "Changes here are in memory and are gone on restart" is now true only
      // in the default mode, and it is drawn on EVERY settings page in this
      // console — so leaving it would have made the most-repeated sentence in
      // the console the wrong one, on a service whose premise is that the prose
      // is more trustworthy than the code.
      //
      // Both branches are written out rather than one being patched with a
      // clause, because they are different advice: with no persistent store the
      // reader is told to edit the appconfig file, and with one they are told
      // that this IS the durable door and where the value went.
      //
      // WHAT DID NOT CHANGE is the reason nothing here rewrites the appconfig
      // FILE, which is the same in both modes and is worth keeping said: a
      // service that edited a file checked into a repository would leave a
      // test's forgotten change behind permanently. The durable overrides go to
      // the persistent store instead, which is a place nothing is checked in
      // from.
      // ---------------------------------------------------------------------
      (persistence.status().persistsAppconfig
        ? '<div class="ok"><strong>Changes here SURVIVE A RESTART.</strong> ' +
          'This process is running with <code>persistence.mode=' +
          this.esc(persistence.status().mode) + '</code>, so a value set ' +
          'here is written to the persistent store and applied again the ' +
          'next time this service starts. It is still a runtime override ' +
          'rather than a new layer — the same setting, the same override ' +
          'map, put back through the same function — so <em>Reset</em> still ' +
          'means "fall back to the file or the environment variable", and ' +
          'the reset is written down too. Nothing rewrites ' +
          '<code>' + this.esc(configFile) + '</code>, ' +
          'deliberately: a service that edited a file checked into a ' +
          'repository would leave a test\'s forgotten change behind ' +
          'permanently. See <a href="/admin/persistence">Persistence</a>.</div>'
        : this.warn('<strong>Changes here are in memory and are gone on ' +
          'restart.</strong> Nothing writes to the appconfig file, ' +
          'deliberately: a service that edited a file checked into a ' +
          'repository would leave a test\'s forgotten change behind ' +
          'permanently. To make something stick, put it in ' +
          '<code>' + this.esc(configFile) + '</code>, in ' +
          'the setting\'s environment variable, or turn on a persistent ' +
          'store — see <a href="/admin/persistence">Persistence</a>, which ' +
          'is off by default.')) +

      (fixed
        ? this.warn('<strong>' + this.esc(String(fixed)) + ' of these ' +
          this.esc(String(all.length)) + ' cannot be changed while this ' +
          'service runs.</strong> They are shown with their inputs disabled ' +
          'and the reason beside each, rather than hidden: they were ' +
          'consumed by the time this service was listening — a bound socket, ' +
          'a certificate\'s names, the Kerberos principal database and its ' +
          'long-term keys, the directory\'s base DN — and accepting a change ' +
          'to one would do nothing and read as having worked.')
        : '') +

      (overridden.length
        ? '<div class="ok">' + this.esc(String(overridden.length)) + ' of ' +
          'these has a runtime override in ' +
          'force: ' + this.codeList(overridden) + '. Each ' +
          'row\'s Reset puts it back to the value its file or environment ' +
          'variable gives it.</div>'
        : '') +

      groups.map(function (group) { return self.configSection(group, path); })
            .join('') +

      this.note('<a href="/admin/config">Configuration</a> holds the whole ' +
      'table — every setting this service has, whichever page edits it — and ' +
      'the rows that belong to no protocol. The same settings over JSON are ' +
      'at <code>' + this.esc(path) + '?format=json</code> and ' +
      '<code>GET /admin-api/config</code>; the four actions are ' +
      '<code>POST /admin-api/config/set</code>, <code>/set-many</code>, ' +
      '<code>/reset</code> and <code>/reset-all</code>.');

    log.debug("Leaving AdminConsole.configFormsFor(). " + all.length + " " +
      "setting(s) in " +
              groups.length + " group(s).");
    return inner;
  }

  // WHERE A SAVE GOES BACK TO. Every settings form posts to /admin/config
  // wherever it was drawn, so the page a person was on has to travel with it —
  // otherwise saving the Kerberos realm from the Kerberos page lands them on
  // the configuration page, which is exactly the journey this change removed.
  //
  // It is checked against SETTING_HOMES rather than merely being escaped, and
  // that is not ceremony: what comes out of here is put into a Location header,
  // so the set of values has to be one this file wrote. An unknown `from` is
  // not an error — it is the configuration page, which is where the form used
  // to send everybody.
  configReturnTo(body) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.configReturnTo().");
    const asked = String((body && body.from) || '').trim();
    const known = SETTING_HOMES.some(function (row) {
      return row.pages.indexOf(asked) >= 0;
    });
    log.debug("Leaving AdminConsole.configReturnTo().");
    return known ? asked : '/admin/config';
  }

  configRow(setting, from) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.configRow().");
    const id = 'cfg-' + setting.key.replace(/\./g, '-');
    // The control carries the description as a tooltip, at the length a tooltip
    // holds. See the comment above the return.
    const hint = this.tip(setting.description, Infinity);
    const input = setting.type === 'enum'
      ? '<select name="' + this.esc(setting.key) + '" id="' + this.esc(id) +
        '"' + hint +
        (setting.editable ? '' : ' disabled') + '>' +
        setting.enumValues.map(function (option) {
          return '<option value="' + self.esc(option) + '"' +
            (option === setting.text ? ' selected' : '') + '>' +
            self.esc(option) +
                 '</option>';
        }).join('') + '</select>'
      : (setting.type === 'bool'
        ? '<select name="' + this.esc(setting.key) + '" id="' + this.esc(id) +
          '"' + hint +
          (setting.editable ? '' : ' disabled') + '>' +
          ['true', 'false'].map(function (option) {
            return '<option value="' + option + '"' +
              (option === setting.text ? ' selected' : '') + '>' + option +
                   '</option>';
          }).join('') + '</select>'
        : '<input type="text" name="' + this.esc(setting.key) + '" id="' +
          this.esc(id) +
          '"' +
          hint + ' size="34" value="' + this.esc(setting.text) + '"' +
          (setting.editable ? '' : ' disabled') + '>');

    // THE RESET BUTTON IS A `formaction`, AND IT USED TO BE A NESTED `<form>`
    // THAT NO BROWSER EVER CREATED. This row is inside the section's form, and
    // the HTML parser DROPS a `<form>` start tag inside another form — the
    // element is never created and its children are adopted by the outer form.
    // So the row's `action=reset` and `key` hidden inputs became fields of the
    // SECTION's form, `parseBody()` takes the last value of a repeated name,
    // and the section's Save button therefore performed a RESET of the last
    // overridden key instead of saving. Nothing failed: the page reloaded with
    // a cheerful message about the thing it had just done instead of the thing
    // it was asked to do. It was found by dumping the parsed DOM rather than by
    // reading the markup, which is the only way this class of defect is ever
    // found.
    //
    // `formaction` is the fix and it needs no script: the button submits the
    // same form to a different URL, and the key rides in that URL where it
    // cannot be confused with a field. THE KEY IS IN THE QUERY STRING AND NOT
    // IN A HIDDEN INPUT for exactly that reason.
    //
    // It also fixes what pressing ENTER in a text box does. A form with no
    // hidden `action` and two named submit buttons would submit the FIRST one
    // on Enter — a Reset — so the hidden `action=set-many` stays and the
    // buttons carry no name at all: Enter posts to the form's own action and
    // saves.
    //
    // `form-action` is deliberately absent from this service's CSP (see
    // `common/app.js`), so nothing here is relaxed to allow it.
    const reset = setting.overridden
      ? '<button class="secondary" formaction="/admin/config?reset=' +
        this.esc(encodeURIComponent(setting.key)) + '">Reset</button>'
      : '';

    const provenance = setting.overridden
      ? '<strong>' + this.esc(this.sourceNote(setting)) + '</strong>'
      : this.esc(this.sourceNote(setting));

    // Named `restart` and not `note`, which it was until the folds arrived: a
    // local called `note` shadows the helper of that name for the whole
    // function, and the row then fails to render with `Cannot access 'note'
    // before initialization` — at request time, on one page, which is the
    // slowest possible way to find out.
    const restart = setting.editable
      ? ''
      : this.note('<strong>Restart to apply:</strong> ' +
        this.esc(setting.restartReason) + '.');

    log.debug("Leaving AdminConsole.configRow().");
    // THE DESCRIPTION IS THE TOOLTIP AND THERE IS NO LONGER A FOLD
    // (2026-09-05).
    //
    // It was a fold with the setting's short label as its summary, and the
    // input carried a 190-character teaser of the same text. That was the right
    // shape while a tooltip was a PREVIEW of something the reader could go and
    // read — but it meant 152 summary lines on /admin/config and a summary line
    // per setting on every protocol page's Settings block, which is the bulk of
    // what was left visible after the 2026-08-26 folds.
    //
    // Both the key's label and the control now carry the WHOLE description as a
    // title, and nothing is drawn under them. **This is the one place in this
    // console where something is said only in a tooltip**, and it is deliberate
    // rather than an oversight — see the paragraph in `admin-ui/CLAUDE.md` that
    // used to say the opposite. What pays for it is that a setting's
    // description is also on `/admin/config`'s own JSON view, in
    // `GET /admin-api/config`, and in README.md's table, so the text has three
    // other doors that a keyboard or a screen reader can reach. A field whose
    // prose has NO other door does not get this treatment.
    return '<tr>' +
      '<td><label for="' + this.esc(id) + '"' +
      this.tip(setting.description, Infinity) +
      '><code>' + this.esc(setting.key) + '</code></label>' +
      restart + '</td>' +
      '<td>' + input + '</td>' +
      '<td>' + provenance + '</td>' +
      '<td>' + reset + '</td></tr>';
  }

  configSection(group, from) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.configSection(). group=" + group.group);
    const rows = group.settings.map(function (setting) {
      return self.configRow(setting, from);
    }).join('');
    const anyEditable = group.settings.some(function (
        setting) { return setting.editable; });
    const save = anyEditable
      ? '<p><button>Save ' + this.esc(group.group) + '</button> ' +
        '<span class="note">Applies to the next token, assertion, ticket or ' +
        'search — nothing already issued changes.</span></p>'
      : this.note('Every setting in this section is read at startup, so ' +
        'there is nothing here to save. Change them in ' +
        this.esc(process.env.CONFIG_FILE || 'the appconfig file') + ' or in ' +
        'the environment and restart.');
    log.debug("Leaving AdminConsole.configSection(). " + group.settings.length +
              " setting(s).");
    return '<h3>' + this.esc(group.group) + '</h3>' +
      '<form method="post" action="/admin/config">' +
      '<input type="hidden" name="action" value="set-many">' +
      '<input type="hidden" name="from" value="' +
      this.esc(from || '/admin/config') +
      '"><table><tr><th>Setting</th><th>Value</th><th>Source</th><th></th>' +
      '</tr>' +
      rows + '</table>' + save + '</form>';
  }

  // The whole table, plus WHERE EACH GROUP IS EDITED. The snapshot is untouched
  // — this resource is still every setting this service has, because a caller
  // asking the API for the configuration should not have to visit twenty-one
  // pages to assemble it — and `homes` is what a caller needs to send a person
  // to the right console page, or to notice that a group has none.
  configJson() {
    const { log, config } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.configJson().");
    const json = config.snapshot();
    json.homes = SETTING_HOMES.map(function (row) {
      return { group: row.group, pages: row.pages,
               labels: row.pages.map(self.labelOfPath.bind(self)) };
    });
    // Empty on a service whose table and console agree, which is every service
    // that starts from a commit where they did. Present either way, so a caller
    // can assert on it.
    json.homeProblems = SETTING_HOME_PROBLEMS;
    log.debug("Leaving AdminConsole.configJson(). " + json.settingCount +
              " setting(s).");
    return json;
  }

  // Seconds as a person reads them. Deliberately approximate above an hour and
  // exact below one: the interesting settings on this page are the short ones,
  // and "90 minutes" is the answer somebody wants for 5400 while "1.04 days" is
  // nobody's answer for 90000. The exact number is always beside it in its own
  // column, so this is a gloss rather than the value.
  humanSeconds(seconds) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.humanSeconds().");
    const n = Number(seconds) || 0;
    if (n === 0) {
      log.debug("Leaving AdminConsole.humanSeconds().");
      return 'no allowance at all';
    }
    if (n < 60) {
      log.debug("Leaving AdminConsole.humanSeconds().");
      return n + ' second' + (n === 1 ? '' : 's');
    }
    if (n < 3600) {
      const minutes = n / 60;
      log.debug("Leaving AdminConsole.humanSeconds().");
      return (Number.isInteger(minutes) ? minutes : minutes.toFixed(1)) +
             ' minute' + (minutes === 1 ? '' : 's');
    }
    if (n < 86400) {
      const hours = n / 3600;
      log.debug("Leaving AdminConsole.humanSeconds().");
      return (Number.isInteger(hours) ? hours : hours.toFixed(1)) +
             ' hour' + (hours === 1 ? '' : 's');
    }
    const days = n / 86400;
    log.debug("Leaving AdminConsole.humanSeconds().");
    return (Number.isInteger(days) ? days :
            days.toFixed(1)) + ' day' + (days === 1 ? '' : 's');
  }

  // One row of the form. A `number` input rather than the text box
  // /admin/config draws, with `min`, `max` and `step` off the setting itself —
  // the bounds are declared in config.js's table and are rendered here rather
  // than repeated, so the browser's own refusal and the server's are the same
  // three numbers. The server still checks: an input attribute is a convenience
  // for a person and no constraint at all on a JSON body or a curl.
  tokenLifetimeRow(setting, snapshot) {
    const { log, applications } = this.deps;
    log.debug("Entering AdminConsole.tokenLifetimeRow().");
    const id = 'tl-' + setting.key.replace(/\./g, '-');
    const kind = TOKEN_LIFETIME_KINDS[setting.key];
    const counts = kind
      ? (snapshot.tokens.byKind.filter(function (row) {
        return row.kind === kind;
      })[0] || null)
      : null;
    const issued = counts
      ? '<span class="state-valid">' + counts.valid + ' valid</span>, ' +
        '<span class="state-expired">' + counts.expired + ' expired</span>, ' +
        '<span class="state-revoked">' + counts.revoked + ' revoked</span>'
      : '<span class="state-none">&mdash;</span>';
    // The setting's own description, on the label and on the box. It is the
    // same sentence every settings form carries as its row's tooltip since
    // 2026-09-05 (configRow()), where it used to be a fold; here it was always
    // a tooltip, because this page is a short list of rows somebody sets a
    // number in, so a paragraph under each would be most of the page.
    const hint = this.tip(setting.description, Infinity);
    // TWO CONTROLS, BY TYPE. It was one — a `number` input — until
    // `oauth2.revokeRefreshOnLogout` joined this page on 2026-08-27, and a bool
    // gets the `select` of true/false that /admin/config's configRow() draws
    // rather than a checkbox: an unticked checkbox posts NOTHING, and this form
    // would read that as "the field was not sent" rather than as false.
    //
    // The bounds on the number input come off the setting itself, so the
    // browser's own refusal and the server's are the same three numbers. The
    // server still checks: an input attribute is a convenience for a person and
    // no constraint at all on a JSON body or a curl.
    const control = setting.type === 'bool'
      ? '<select name="' + this.esc(setting.key) + '" id="' + this.esc(id) +
        '"' + hint +
        '>' +
        ['true', 'false'].map(function (option) {
          return '<option value="' + option + '"' +
            (option === setting.text ? ' selected' : '') + '>' + option +
                 '</option>';
        }).join('') + '</select>'
      : '<input type="number" name="' + this.esc(setting.key) + '" id="' +
        this.esc(id) +
        '"' +
        hint + ' value="' + this.esc(setting.text) + '"' +
        (typeof setting.min === 'number' ? ' min="' + setting.min + '"' : '') +
        (typeof setting.max === 'number' ? ' max="' + setting.max + '"' : '') +
        (typeof setting.step === 'number' ? ' step="' + setting.step + '"' :
         '') +
        ' size="10">';
    // WHICH ATTRIBUTE A CLIENT OVERRIDES THIS WITH, or a plain no. Read from
    // applications.js's own table rather than listed here, so a setting that
    // becomes per-client cannot reach this page without this column saying so.
    const override = applications.overridableSettings().filter(function (row) {
      return row.setting === setting.key;
    })[0];
    const per = override
      ? '<code>' + this.esc(override.attribute) + '</code>'
      : '<span class="state-none">not per client</span>';
    log.debug("Leaving AdminConsole.tokenLifetimeRow().");
    return '<tr>' +
      '<td><label for="' + this.esc(id) + '"' + hint + '>' +
      this.esc(setting.label) +
      '</label><div ' +
      'class="note"><code>' + this.esc(setting.key) + '</code>' +
      (setting.env ? ', <code>' + this.esc(setting.env) + '</code>' : '') +
      '</div></td><td>' + control + '</td><td>' +
      this.esc(setting.type === 'bool' ? '' :
               this.humanSeconds(setting.value)) +
      '</td><td>' + per + '</td><td>' + (setting.overridden
        ? '<strong>' + this.esc(this.sourceNote(setting)) + '</strong>'
        : this.esc(this.sourceNote(setting))) + '</td>' +
      '<td>' + issued + '</td></tr>';
  }

  // The two ways these four can be set to something legal and surprising.
  // Neither is refused — this service exists to be pointed at a client and made
  // to misbehave on purpose, and both of these are reachable states a real
  // deployment can get into — but a page that showed the numbers and not the
  // consequence would leave the consequence to be discovered from a client that
  // stopped working.
  tokenLifetimeWarnings() {
    const { log, config } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.tokenLifetimeWarnings().");
    const access = config.value('oauth2.accessTokenTtlS');
    const refresh = config.value('oauth2.refreshTokenTtlS');
    const skew = config.value('oauth2.clockSkewS');
    const notes = [];
    if (access >= refresh) {
      notes.push('<strong>The access token lives at least as long as the ' +
                 'refresh token</strong> (' +
        this.esc(this.humanSeconds(access)) + ' against ' +
        this.esc(this.humanSeconds(refresh)) +
                 '). ' +
        'That is legal and it is issued exactly as configured, but the grant ' +
        'can never usefully be renewed: by the time a client needs a new ' +
        'access token, the credential it would renew with has expired too. ' +
        'It is a good way to watch a client discover that it has no way back.');
    }
    if (skew >= access) {
      notes.push('<strong>The clock skew is at least as long as the access ' +
        'token’s own lifetime</strong> ' +
        '(' + this.esc(this.humanSeconds(skew)) + ' against ' +
        this.esc(this.humanSeconds(access)) +
        '). Every endpoint that reads one back will accept it for its whole ' +
        'life and then for as long again, so an expired access token is ' +
        'never refused anywhere here — including at introspection, which ' +
        'will keep reporting it active. Lower the skew, or raise the ' +
        'lifetime, unless that is the thing being tested.');
    }
    if (!notes.length) {
      log.debug("Leaving AdminConsole.tokenLifetimeWarnings().");
      return '';
    }
    log.debug("Leaving AdminConsole.tokenLifetimeWarnings().");
    return notes.map(function (note) { return self.warn(note); }).join('');
  }

  // ---------------------------------------------------------------------------
  // /admin/saml-assertions — HOW LONG AN ASSERTION IS VALID, AND HOW FAR THE
  // WINDOW IS WIDENED FOR SOMEBODY ELSE'S CLOCK.
  //
  // Three settings when it was written — sixteen now (`SAML_ASSERTION_SETTINGS`
  // in admin-core/admin_actions.ts), since it became the page for the
  // per-application SAML defaults on 2026-08-27 — all of them `config.js` rows,
  // on a page of their own under Protocols > SAML. It is the THIRD page of this
  // shape — /admin/token-lifetimes was the second and argues the form at length
  // — so the test that header sets is the one this page has to pass rather than
  // cite: a page like this earns its place when the reader's task is not the
  // one /admin/config serves, and it costs a reader nothing only while it
  // writes through the same function.
  //
  // It passes on both counts, and the specific reasons are:
  //
  //   * These three are a QUANTITY somebody sets to a number to watch something
  //     happen — "make it a minute and see whether that service provider checks
  //     NotOnOrAfter at all". Two of them are already drawn on /admin/saml2 and
  //     /admin/saml11, one on each, which means comparing them or changing both
  //     costs two pages and a scroll through everything else those pages
  //     configure. This is the one place both are visible at once.
  //   * They INTERACT, and interact in a way a flat table cannot say. A skew as
  //     long as the lifetime is an assertion valid for twice as long as its
  //     lifetime claims; a skew of zero against a relying party whose clock is
  //     behind is an assertion refused as not-yet-valid, which is the single
  //     most misdiagnosed failure in this protocol family because it reads from
  //     both ends as a signature or trust-store problem.
  //   * The two lifetimes are SEPARATE settings, and a page that shows them
  //     together is the only place that fact is visible. SAML 2.0 and SAML 1.1
  //     are separate implementations here, not one with a version flag.
  //
  // THERE IS NO STORE. This page holds nothing and writes through
  // `config.setOverride()` — the same function /admin/config's Save, the two
  // identity provider pages' own forms, and `POST /admin-api/config/set` all
  // call, against the same override map. A change made on any of them is one
  // change. That is what keeps the one-store rule intact while the same setting
  // appears on two pages.
  //
  // WHY THE SKEW IS ONE SETTING AND THE LIFETIMES ARE TWO. The lifetimes are
  // per profile because the two profiles are consumed differently and this
  // repository has argued that at length in config.js. The skew is not about a
  // profile at all: it is how far out the clocks in the estate this service
  // issues into are allowed to be, which a deployment decides once. It is
  // applied in both builders, so WS-Trust and WS-Federation — whose assertions
  // come out of those same two functions — get it without either module knowing
  // it exists.
  //
  // AND IT IS NOT oauth2.clockSkewS. That one is a TOLERANCE applied when this
  // service READS something back, including an inbound federation partner's
  // assertion (federation/federation_sp.ts, which argues there that a reading
  // tolerance is decided once). This one is what this service WRITES into a
  // document it issues. A deployment wanting a strict reading and a forgiving
  // issuance has to be able to say so, and with one setting it could not.
  //
  // NOTHING ALREADY ISSUED CHANGES, and the page says so. A validity window is
  // stamped into an assertion when it is signed.
  // ---------------------------------------------------------------------------





  // One row of the form. The same shape tokenLifetimeRow() draws and for the
  // same reasons — a `number` input with `min`, `max` and `step` off the
  // setting itself, so the browser's refusal and the server's are the same
  // three numbers — differing only in that the unit is per row here rather than
  // seconds throughout, and that what sits in the last column is a count of
  // assertions rather than of tokens.
  samlAssertionSettingRow(setting, snapshot) {
    const { log, samlAssertionRowFor, samlAssertionSeconds } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.samlAssertionSettingRow().");
    const id = 'sa-' + setting.key.replace(/\./g, '-');
    const row = samlAssertionRowFor(setting.key);
    const counts = (row && row.kind)
      ? (snapshot.assertions.byKind.filter(function (
          k) { return k.kind === row.kind; })[0] || null)
      : null;
    const issued = counts
      ? '<span class="state-valid">' + counts.valid + ' valid</span>, ' +
        '<span class="state-expired">' + counts.expired + ' expired</span>'
      : '<span class="state-none">&mdash;</span>';
    const hint = this.tip(setting.description, Infinity);
    // THREE CONTROLS, BY TYPE, and they are the ones `/admin/config`'s
    // configRow() draws — a `select` of true/false for a bool rather than a
    // checkbox, because an unticked checkbox sends NOTHING and this form would
    // then read "sign no assertion" as "the field was not posted". That is the
    // same reason configRow() uses a select, and getting it wrong here would be
    // a silent one. THREE CONTROLS NOW, not two: `enum` joined bool and int
    // when the two encryption algorithm rows arrived. It draws the setting's
    // own enumValues, so a value added to that list in config.js appears here
    // without this file being touched — and a value REMOVED there stops being
    // offerable, which is what keeps the form from proposing something the
    // action would refuse.
    const control = setting.type === 'enum'
      ? '<select name="' + this.esc(setting.key) + '" id="' + this.esc(id) +
        '"' + hint +
        '>' +
        (setting.enumValues || []).map(function (option) {
          return '<option value="' + self.esc(option) + '"' +
            (option === setting.text ? ' selected' : '') + '>' +
            self.esc(option) +
                 '</option>';
        }).join('') + '</select>'
      : (setting.type === 'bool'
      ? '<select name="' + this.esc(setting.key) + '" id="' + this.esc(id) +
        '"' + hint +
        '>' +
        ['true', 'false'].map(function (option) {
          return '<option value="' + option + '"' +
            (option === setting.text ? ' selected' : '') + '>' + option +
                 '</option>';
        }).join('') + '</select>'
      : (setting.type === 'int'
        ? '<input type="number" name="' + this.esc(setting.key) + '" id="' +
          this.esc(id) +
          '"' +
          hint + ' value="' + this.esc(setting.text) + '"' +
          (typeof setting.min === 'number' ? ' min="' + setting.min + '"' :
           '') +
          (typeof setting.max === 'number' ? ' max="' + setting.max + '"' :
           '') +
          (typeof setting.step === 'number' ? ' step="' + setting.step + '"' :
           '') +
          ' size="10"> ' + this.esc(row ? row.unit : '')
        : '<input type="text" name="' + this.esc(setting.key) + '" id="' +
          this.esc(id) +
          '"' +
          hint + ' size="40" value="' + this.esc(setting.text) + '">'));
    // What an application would override this with. Named rather than
    // described, because the whole point of the column is that somebody reading
    // this page can go and type the exception on an application entry — and the
    // attribute name is what they have to type.
    const per = (row && row.field)
      ? '<code>' + this.esc(row.field) + '</code>'
      : '<span class="state-none">not per application</span>';
    log.debug("Leaving AdminConsole.samlAssertionSettingRow().");
    return '<tr>' +
      '<td><label for="' + this.esc(id) + '"' + hint + '>' +
      this.esc(setting.label) +
      '</label><div ' +
      'class="note"><code>' + this.esc(setting.key) + '</code>' +
      (setting.env ? ', <code>' + this.esc(setting.env) + '</code>' : '') +
      '</div></td><td>' + control + '</td><td>' +
      this.esc(setting.type === 'int' && row && row.unit
        ? this.humanSeconds(samlAssertionSeconds(setting.key))
        : '') + '</td>' +
      '<td>' + per + '</td>' +
      '<td>' + (setting.overridden
        ? '<strong>' + this.esc(this.sourceNote(setting)) + '</strong>'
        : this.esc(this.sourceNote(setting))) + '</td>' +
      '<td>' + issued + '</td></tr>';
  }

  // The states these settings can legally be in that are worth being told
  // about. Nothing here is refused — this service exists to be pointed at a
  // relying party and made to misbehave on purpose — but a page that showed
  // three numbers and not their consequence would leave the consequence to be
  // found from a relying party that stopped working.
  samlAssertionWarnings() {
    const { log, samlAssertionSeconds, SAML_ASSERTION_SETTINGS } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.samlAssertionWarnings().");
    const skew = samlAssertionSeconds('saml.clockSkewS');
    const notes = [];
    SAML_ASSERTION_SETTINGS.filter(function (row) { return row.kind; })
                           .forEach(function (row) {
      const lifetime = samlAssertionSeconds(row.key);
      if (skew > 0 && skew >= lifetime) {
        notes.push('<strong>The clock skew is at least as long as the ' +
          self.esc(row.kind) +
          ' assertion&rsquo;s own lifetime</strong> (' + self.esc(
              self.humanSeconds(skew)) + ' ' +
              'against ' +
          self.esc(self.humanSeconds(lifetime)) + '). The window written ' +
          'into the assertion is the lifetime plus the skew at EACH end, so ' +
          'it is valid for at least three times as long as ' +
          '<code>' + self.esc(row.key) + '</code> says. If the point was to ' +
          'watch a relying party refuse a stale assertion, it will not: ' +
          'lower the skew, or raise the lifetime.');
      }
    });
    if (skew === 0) {
      notes.push('<strong>The skew is 0, which is what this service has ' +
        'always done</strong> &mdash; <code>NotBefore</code> is stamped at ' +
        'exactly the moment of issue. That is the strict reading, and it is ' +
        'the one that fails against a relying party whose clock is a few ' +
        'seconds behind: the assertion is not yet valid when it arrives, and ' +
        'the refusal reads as a signature or trust-store problem from both ' +
        'ends. If a service provider is refusing assertions that look ' +
        'correct, this is the first setting to raise.');
    }
    if (!notes.length) {
      log.debug("Leaving AdminConsole.samlAssertionWarnings().");
      return '';
    }
    log.debug("Leaving AdminConsole.samlAssertionWarnings().");
    return notes.map(function (note) { return self.warn(note); }).join('');
  }

  // ---------------------------------------------------------------------------
  // THE AUTHENTICATION SECTION OF /admin/scim.
  //
  // Two tables and a list, and the division between them is the one this page
  // already draws everywhere else: the SCHEMES come from scim.js's
  // description() — which is scim_auth.js's table, the same one that builds the
  // WWW-Authenticate challenge and the ServiceProviderConfig — while the COUNTS
  // come from admin_stats.js. So a scheme that is offered cannot be missing
  // from this page and a count cannot be attributed to a scheme that does not
  // exist.
  //
  // Every scheme is drawn INCLUDING the ones at zero and the ones turned off,
  // for the reason the operations table below draws its zeroes: "can I use
  // Digest against this server" is the question somebody arrives with, and a
  // table that listed only what had been used would answer it by omission.
  //
  // There are no CONTROLS in this section, which is what keeps rule 7 satisfied
  // with only a GET on /admin-api/scim: every one of these is a config.js row,
  // drawn in the page's settings block (`configFormsFor('/admin/scim')`), which
  // posts to /admin/config — and POST /admin-api/config/set already has the
  // operation. A second form here would be a second door to one setting.
  // ---------------------------------------------------------------------------
  authenticationSection(auth, counters) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.authenticationSection().");
    const counts = (counters && counters.byAuthScheme) || {};
    const rows = auth.schemes.map(function (row) {
      return '<tr><td>' + self.esc(row.name) +
        (row.primary ? ' <span class="sub">(primary)</span>' : '') +
        // The scheme's description is scim_auth.js's own and is a paragraph on
        // every row, so five of them made this the longest table on the page
        // while the column somebody scans — the scheme's NAME — was one line.
        self.note(self.esc(row.description)) + '</td>' +
        '<td><code>' + self.esc(row.type) + '</code>' +
        (row.canonical ? '' : '<div class="sub">no canonical value in RFC ' +
          '7643 section 5 — published beside the four that have one</div>') +
          '</td>' +
        '<td>' + (row.enabled
          ? '<span class="state-valid">offered</span>'
          : '<span class="state-none">off</span>') +
        '<div class="sub"><code>' + self.esc(row.setting) +
        '</code></div></td>' +
        '<td>' + (row.scoped ? 'what its scopes say' : 'everything') + '</td>' +
        '<td class="num">' + (counts[row.id] || 0) + '</td></tr>';
    }).join('');
    const extra = ['anonymous', 'refused'].map(function (name) {
      return '<tr><td>' + self.esc(name === 'anonymous'
        ? 'Nothing (an open discovery call, or authentication turned off)'
        : 'Refused before any handler ran') +
          '</td><td></td><td></td><td></td>' +
        '<td class="num">' + (counts[name] || 0) + '</td></tr>';
    }).join('');
    const policy = auth.policy.map(function (text) {
      return self.bullet(self.esc(text));
    }).join('');
    const out = '<h2>Authentication</h2>' +
      this.note('RFC 7644 section 2 defines no credential of its own — it ' +
      'delegates to TLS and RFC 7235 and NAMES six schemes, and all six are ' +
      'here. Its one SHALL is that the schemes be indicated in ' +
      '<code>WWW-Authenticate</code>, which every 401 from these endpoints ' +
      'carries; its one MUST is that an authenticated client be mappable to ' +
      'an access control policy, which is the list below. Realm <code>' +
      this.esc(auth.realm) + '</code>. Discovery is ' +
      (auth.discoveryOpen
        ? 'OPEN, because a client has to be able to read which schemes exist ' +
          'before it can use one'
        : 'closed as well (<code>scim.authDiscovery</code>)') + '. Every ' +
      'switch here is in the settings at the foot of this page.') +
      '<table><tr><th>Scheme</th><th>type</th><th>State</th><th>May do</th>' +
      '<th class="num">Requests</th></tr>' + rows + extra + '</table>' +
      this.note('The two OAuth scopes are <code>' + this.esc(auth.scopes.read) +
      '</code> and <code>' + this.esc(auth.scopes.write) + '</code> — the ' +
      'first scope requirement anywhere in this service — and they are ' +
      'published in <code>scopes_supported</code> in both discovery ' +
      'documents. Digest offers ' +
      this.esc(auth.digestAlgorithms.join(', ')) + '; HOBA keys are ' +
      'registered at <code>' + this.esc(auth.hobaRegistration) + '</code> ' +
      'and land on the person\'s own directory entry, so <a ' +
      'href="/admin/users">Users</a> shows them.') +
      '<h3>The access control policy</h3><ul class="note">' + policy + '</ul>';
    log.debug("Leaving AdminConsole.authenticationSection(). " +
              auth.schemes.length + " " +
        "scheme(s).");
    return out;
  }

  // One row of the by-scheme table. Not folded into the page body because the
  // same rows are wanted in two orders — the schemes the surface declares, then
  // anything counted under a name it does not declare — and a second copy of
  // the markup is how the two come to be formatted differently.
  scimSchemeRow(id, name, enabled, count, note) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.scimSchemeRow().");
    log.debug("Leaving AdminConsole.scimSchemeRow().");
    return '<tr><td><code>' + this.esc(id) + '</code>' +
      (name ? ' ' + this.esc(name) : '') + '</td>' +
      '<td>' + (enabled === null ? '<span class="sub">—</span>'
        : (enabled ? '<span class="state-valid">on</span>'
                   : '<span class="state-none">off</span>')) + '</td>' +
      '<td class="num">' + count + '</td>' +
      '<td class="sub">' + (note || '') + '</td></tr>';
  }

  // A round step for the value axis: 1, 2 or 5 times a power of ten, never
  // below one letter, so the ticks read 0 / 5 / 10 rather than 0 / 3.25 / 6.5.
  deadLetterAxisStep(peak) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.deadLetterAxisStep(). " + peak);
    const rough = Math.max(1, peak / 4);
    const power = Math.pow(10, Math.floor(Math.log10(rough)));
    const step = [1, 2, 5, 10].map(function (m) {
      return m * power;
    }).filter(function (candidate) {
      return candidate >= rough;
    })[0];
    log.debug("Leaving AdminConsole.deadLetterAxisStep(). " + step);
    return step;
  }

  // A window length for an axis label: `60m`, `12h`, `30d`.
  deadLetterSpan(seconds) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.deadLetterSpan().");
    let out = Math.round(seconds / 60) + 'm';
    if (seconds >= 172800 && seconds % 86400 === 0) {
      out = (seconds / 86400) + 'd';
    } else if (seconds >= 7200) {
      out = Math.round(seconds / 3600) + 'h';
    }
    log.debug("Leaving AdminConsole.deadLetterSpan(). " + out);
    return out;
  }

  deadLetterSwatch(colour) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.deadLetterSwatch().");
    log.debug("Leaving AdminConsole.deadLetterSwatch().");
    return '<svg width="12" height="12" viewBox="0 0 12 12" ' +
      'aria-hidden="true"><rect x="0" y="0" width="12" height="12" rx="3" ' +
      'fill="' + this.esc(colour) + '"/></svg>';
  }

  // One stacked column's segments, bottom up in the report's cause order, with
  // a 2px gap of the card's white between touching segments and the top one
  // rounded. `geometry` is the column's x, width, baseline and pixels per
  // letter.
  deadLetterColumn(bucket, causes, geometry) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.deadLetterColumn().");
    const present = causes.filter(function (cause) {
      return bucket.counts[cause.id] > 0;
    });
    let base = geometry.baseline;
    const parts = present.map(function (cause, index) {
      const full = bucket.counts[cause.id] * geometry.perLetter;
      const gap = index > 0 ? 2 : 0;
      const height = Math.max(1, full - gap);
      const bottom = base - gap;
      const top = bottom - height;
      base = base - full;
      const colour = DEAD_LETTER_COLOURS[cause.id] || '#8a8a99';
      const x = geometry.x;
      const w = geometry.width;
      if (index < present.length - 1) {
        return '<rect x="' + x.toFixed(1) + '" y="' + top.toFixed(1) +
          '" width="' + w.toFixed(1) + '" height="' + height.toFixed(1) +
          '" fill="' + colour + '"/>';
      }
      const r = Math.min(4, height, w / 2);
      return '<path d="M' + x.toFixed(1) + ' ' + bottom.toFixed(1) +
        'V' + (top + r).toFixed(1) +
        'Q' + x.toFixed(1) + ' ' + top.toFixed(1) + ' ' +
        (x + r).toFixed(1) + ' ' + top.toFixed(1) +
        'H' + (x + w - r).toFixed(1) +
        'Q' + (x + w).toFixed(1) + ' ' + top.toFixed(1) + ' ' +
        (x + w).toFixed(1) + ' ' + (top + r).toFixed(1) +
        'V' + bottom.toFixed(1) + 'Z" fill="' + colour + '"/>';
    });
    log.debug("Leaving AdminConsole.deadLetterColumn(). " + parts.length +
              " segment(s).");
    return parts.join('');
  }

  // The whole timeline: axes, gridlines, one column per bucket, and the text a
  // reader who cannot see the colours still gets.
  deadLetterTimeline(timeline, causes) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.deadLetterTimeline(). " +
              timeline.buckets.length +
              " bucket(s).");
    const W = 760;
    const H = 230;
    const left = 46;
    const right = 14;
    const top = 12;
    const bottom = 34;
    const plotW = W - left - right;
    const plotH = H - top - bottom;
    const baseline = top + plotH;
    const n = timeline.buckets.length;
    const band = plotW / n;
    const width = Math.max(2, Math.min(24, band - 2));
    const step = this.deadLetterAxisStep(timeline.peak);
    const ceiling = Math.max(step, Math.ceil(timeline.peak / step) * step);
    const perLetter = plotH / ceiling;

    const grid = [];
    for (let v = 0; v <= ceiling; v += step) {
      const y = baseline - v * perLetter;
      grid.push('<line x1="' + left + '" x2="' + (W - right) + '" y1="' +
        y.toFixed(1) + '" y2="' + y.toFixed(1) + '" stroke="' +
        (v === 0 ? '#c9c9d3' : '#ececf2') + '" stroke-width="1"/>' +
        '<text x="' + (left - 8) + '" y="' + (y + 4).toFixed(1) +
        '" text-anchor="end" font-size="11" fill="#666">' +
        this.esc(v.toLocaleString('en-US')) + '</text>');
    }

    const ticks = [0, 0.25, 0.5, 0.75, 1].map(function (fraction) {
      const x = left + plotW * fraction;
      const ago = Math.round(timeline.windowS * (1 - fraction));
      const anchor = fraction === 0 ? 'start' : (fraction === 1 ? 'end'
                                                                 : 'middle');
      return '<text x="' + x.toFixed(1) + '" y="' + (baseline + 20) +
        '" text-anchor="' + anchor + '" font-size="11" fill="#666">' +
        self.esc(ago ? self.deadLetterSpan(ago) + ' ago' : 'now') + '</text>';
    }).join('');

    const columns = timeline.buckets.map(function (bucket, i) {
      const x = left + i * band;
      const from = Date.parse(bucket.start);
      const until = from + timeline.bucketS * 1000;
      const what = causes.filter(function (cause) {
        return bucket.counts[cause.id] > 0;
      }).map(function (cause) {
        return cause.label + ' ' + bucket.counts[cause.id];
      }).join(', ');
      const title = self.whenText(from) + ' to ' + self.whenText(until) + ': ' +
        (bucket.total
          ? bucket.total + ' dead-lettered (' + what + ')'
          : 'nothing dead-lettered');
      return '<g><title>' + self.esc(title) + '</title>' +
        '<rect x="' + x.toFixed(1) + '" y="' + top + '" width="' +
        band.toFixed(1) + '" height="' + plotH + '" fill="#fff" ' +
        'fill-opacity="0"/>' +
        (bucket.total
          ? self.deadLetterColumn(bucket, causes, { x: x + (band - width) / 2,
              width: width, baseline: baseline, perLetter: perLetter })
          : '') +
        '</g>';
    }).join('');

    const empty = timeline.peak
      ? ''
      : '<text x="' + (left + plotW / 2) + '" y="' + (top + plotH / 2) +
        '" text-anchor="middle" font-size="13" fill="#666">' +
        'Nothing held was dead-lettered in this window.</text>';

    const label = timeline.peak
      ? 'Dead letters held, by when they were dead-lettered, in ' +
        this.deadLetterSpan(timeline.bucketS) + ' columns over the last ' +
        this.deadLetterSpan(timeline.windowS) + '; the busiest column holds ' +
        timeline.peak + '. The same numbers are in the table below.'
      : 'No dead letter held was dead-lettered in the last ' +
        this.deadLetterSpan(timeline.windowS) + '.';
    log.debug("Leaving AdminConsole.deadLetterTimeline().");
    return '<div class="chart"><svg xmlns="http://www.w3.org/2000/svg" ' +
      'viewBox="0 0 ' + W + ' ' + H + '" role="img" aria-label="' +
      this.esc(label) +
      '">' + grid.join('') + columns + ticks + empty + '</svg></div>';
  }

  // The letters list's rows. Called once per page load over at most one page.
  deadLetterRows(json, causeById, listView) {
    const { log, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.deadLetterRows(). " + json.letters.length +
              ".");
    const rows = json.letters.map(function (row) {
      const cause = causeById[row.cause] || { label: row.cause };
      const event = row.event;
      return '<tr>' +
        '<td class="sub">' + self.esc(self.whenText(Date.parse(row.deadAt))) +
        (row.ageS !== null
          ? '<div>' + self.esc(self.durationText(row.ageS * 1000)) +
            ' ago</div>' : '') +
        '</td>' +
        '<td class="who"><a href="' + self.esc('/admin/ssf/dead-letters' +
          queryWith(listView, { dlstream: row.stream_id, lettersPage: '' })) +
        '#find-dlq"><code>' + self.esc(row.stream_id) + '</code></a>' +
        (row.streamKnown
          ? ''
          : '<div class="sub">no such stream here</div>') + '</td>' +
        '<td>' + (event
          ? self.esc(event.name) + '<div class="sub"><code>' +
            self.esc(event.types[0] || '') + '</code></div>' +
            (event.subject
              ? '<div class="sub">' + self.esc(event.subject) + '</div>' : '')
          : '<span class="sub">unreadable</span>') + '</td><td>' +
        self.deadLetterSwatch(DEAD_LETTER_COLOURS[row.cause] || '#8a8a99') +
        ' ' + self.esc(cause.label) +
        (row.errorCode
          ? '<div><a href="/admin/error-codes#' + self.esc(row.errorCode) +
            '"><code class="ec">' + self.esc(row.errorCode) +
            '</code></a></div>'
          : '') + '</td>' +
        '<td class="num">' + (row.status ? self.esc(String(row.status))
          : '<span class="sub" title="' +
            self.esc('No HTTP answer: the push was ' +
              'never made, or nothing answered it.') + '">&mdash;</span>') +
        '</td>' +
        '<td>' + self.esc(row.reason) + '</td>' +
        '<td class="sub">' + (row.signed ? 'signed' : 'not signed') +
        '<div><code>' + self.esc(row.jti) + '</code></div></td>' +
        '</tr>';
    }).join('');
    log.debug("Leaving AdminConsole.deadLetterRows().");
    return rows;
  }

  setSignalsReporter(reporter) {
    const { log, errorCodes, adminActions, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setSignalsReporter().");
    const needed = ['report', 'deadLetters', 'action', 'actions', 'eventTypes',
                    'statuses', 'subjectFormats'];
    const missing = needed.filter(function (name) {
      return !reporter || reporter[name] === undefined;
    });
    if (missing.length) {
      log.error(errorCodes.tag('STS-ADMIN-0014') + 'admin: ' +
                                                   'setSignalsReporter() was ' +
                                                   'given something without ' +
                missing.join(', ') + ', and was ignored whole. /admin/ssf ' +
                'and /admin-api/ssf will say the reporter is not installed ' +
                'rather than half working.');
      log.debug("Leaving AdminConsole.setSignalsReporter(). Refused.");
      return;
    }
    signalsReporter = reporter;
    // AND THE ACTION LAYER, which needs the same object and is handed it
    // from here rather than asking for it — see the header of
    // admin-core/admin_actions.ts. One statement, two destinations: this is
    // the ONLY place either is written, which is what keeps it one answer.
    adminActions.setSignalsReporter(reporter);
    // AND THE READ LAYER: the /admin-api report of this stream is drawn there
    // while the controls beside it are actions. One statement, one writer.
    adminViews.setSignalsReporter(reporter);
    log.debug("Leaving AdminConsole.setSignalsReporter(). Installed.");
  }

  setTruststore(value) {
    const { log, errorCodes, adminActions, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setTruststore().");
    const needed = ['list', 'add', 'remove'];
    const missing = needed.filter(function (name) {
      return !value || typeof value[name] !== 'function';
    });
    if (missing.length) {
      log.error(errorCodes.tag('STS-ADMIN-0014') + 'admin: setTruststore() ' +
                                                   'was given something ' +
                                                   'without ' +
                missing.join(', ') + ', and was ignored whole. ' +
                '/admin/tls/trust and /admin-api/tls/trust will say the ' +
                'truststore is not installed rather than half working.');
      log.debug("Leaving AdminConsole.setTruststore(). Refused.");
      return false;
    }
    truststore = value;
    // One statement, two destinations, and this is the only place either is
    // written — the arrangement every forwarded collaborator here keeps, and
    // what tests/admin_actions_layer.js asserts.
    adminActions.setTruststore(value);
    adminViews.setTruststore(value);
    log.debug("Leaving AdminConsole.setTruststore(). Installed.");
    return true;
  }

  ssfStreamCard(row) {
    const { log, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.ssfStreamCard().");
    const subjects = row.subjects.length
      ? row.subjects.map(function (one) {
          return '<tr><td>' + self.esc(one.text) + '</td><td>' +
            (one.verified ? 'verified' : 'unverified') + '</td><td ' +
              'class="sub">' +
            self.esc(one.addedAt) + '</td></tr>';
        }).join('')
      : '<tr><td colspan="3">No subjects. What that MEANS is ' +
        '<code>default_subjects</code> on the transmitter metadata: with ALL ' +
        'this stream is about everybody, with NONE about nobody.</td></tr>';
    const queue = row.queue.length
      ? row.queue.map(function (one) {
          return '<tr><td><code>' + self.esc(one.jti) + '</code></td><td>' +
            self.esc(one.summary.name) + '</td><td class="sub">' +
            self.esc(one.queuedAt) + '</td><td class="sub">' +
            self.esc(one.deliveredAt || 'not yet') + '</td></tr>';
        }).join('')
      : '<tr><td colspan="4">Nothing waiting.</td></tr>';
    const history = row.log.slice(0, 12).map(function (one) {
      return '<tr><td class="sub">' + self.esc(one.at) + '</td><td>' +
        self.esc(one.kind) + '</td><td>' + self.esc(one.text) + '</td></tr>';
    }).join('') || '<tr><td colspan="3">Nothing recorded yet.</td></tr>';
    // THE DEAD-LETTER QUEUE (2026-09-14): what could not be delivered, newest
    // first, with the reason. The first twenty-five; the JSON has them all.
    const dead = row.deadLetters || [];
    const deadRows = dead.length
      ? dead.slice(0, 25).map(function (one) {
          return '<tr><td><code>' + self.esc(one.jti) + '</code></td><td>' +
            self.esc(one.summary ? one.summary.name : '') + '</td><td ' +
              'class="sub">' +
            self.esc(one.deadAt) + '</td><td>' + self.esc(one.reason) +
            (one.errorCode ? ' <code>' + self.esc(one.errorCode) + '</code>' :
             '') +
            '</td><td class="sub">' + (one.signed ? 'signed' : 'not signed') +
            '</td></tr>';
        }).join('') + (dead.length > 25
          ? '<tr><td colspan="5">&hellip; and ' + (dead.length - 25) +
            ' more, in this page\'s JSON.</td></tr>' : '')
      : '<tr><td colspan="5">Nothing undeliverable.</td></tr>';

    log.debug("Leaving AdminConsole.ssfStreamCard().");
    // The heading carries an id so Monitoring → Shared Signals → Dead letters
    // can link each stream row straight to this card, where its controls are.
    return '<h3 id="stream-' + this.esc(row.stream_id) + '"><code>' +
      this.esc(row.stream_id) + '</code> &mdash; ' +
      this.esc(row.status) + (row.dead
        ? ' <span class="state-invalid">&mdash; DEAD</span>' : '') + '</h3>' +
      (row.dead
        ? '<p class="state-invalid">Declared dead at ' +
          this.esc(row.deadSince) +
          ': its pushes all failed for <code>ssf.deadStreamTimeoutS</code>. ' +
          'Nothing is pushed to it; its SETs go to the dead-letter queue, ' +
          'and one is pushed as a probe at ' +
          this.esc(row.nextProbeAt || 'the next ' +
          'sweep') + '. Last failure: ' + this.esc(row.deadReason) + '</p>'
        : '') +
      '<table class="key">' +
      '<tr><th>Issuer</th><td><code>' + this.esc(row.iss) +
      '</code></td></tr>' +
      '<tr><th>Audience</th><td><code>' +
      this.esc(Array.isArray(row.aud) ? row.aud.join(', ') : row.aud) +
      '</code></td></tr>' +
      '<tr><th>Delivery</th><td><code>' + this.esc(row.delivery.method) +
      '</code>' + (row.delivery.endpoint_url
        ? ' &rarr; <code>' + this.esc(row.delivery.endpoint_url) + '</code>'
        : '') + '</td></tr>' +
      '<tr><th>Delivers</th><td>' + (row.events_delivered.length
        ? row.events_delivered.map(function (uri) {
            return '<code>' + self.esc(uri) + '</code>';
          }).join('<br>')
        : 'nothing &mdash; the intersection of what the receiver asked for ' +
          'and what this transmitter supports is empty') + '</td></tr>' +
      '<tr><th>Created</th><td class="sub">' + this.esc(row.createdAt) + ' ' +
        'by ' +
      this.esc(row.createdBy || '(unauthenticated)') + '</td></tr>' +
      '<tr><th>Counters</th><td>' + row.counters.queued + ' queued, ' +
      row.counters.delivered + ' delivered, ' + row.counters.failed +
      ' failed, ' + row.counters.acknowledged + ' acknowledged, ' +
      row.counters.receiverErrors + ' refused by the receiver</td></tr>' +
      (row.lastPushError
        ? '<tr><th>Last push</th><td class="state-invalid">' +
          this.esc(row.lastPushError) + '</td></tr>'
        : '') +
      '</table>' +
      '<h4>Subjects</h4>' +
      '<table><tr><th>Subject</th><th>State</th><th>Added</th></tr>' +
      subjects + '</table>' +
      '<h4>Waiting to be delivered</h4>' +
      '<table><tr><th>jti</th><th>Event</th><th>Queued</th><th>Delivered</th>' +
      '</tr>' + queue + '</table>' +
      '<h4>Dead letters</h4>' +
      this.note('SETs that could not be delivered, with the reason, kept for ' +
                '<code>ssf.deadLetterRetentionS</code> and then deleted. ' +
                'Nothing resends them. <a href="' +
                this.esc('/admin/ssf/dead-letters' +
                queryWith({}, { dlstream: row.stream_id })) +
                '#find-dlq">Every ' +
                'one, counted and searchable</a>.') +
      '<table><tr><th>jti</th><th>Event</th><th>Dead since</th><th>Why</th>' +
      '<th></th></tr>' + deadRows + '</table>' +
      '<h4>What has happened on this stream</h4>' +
      '<table><tr><th>When</th><th>What</th><th>Detail</th></tr>' + history +
      '</table>' +
      (row.dead
        ? '<form method="post" action="/admin/ssf"><div class="formrow">' +
          '<input type="hidden" name="stream_id" value="' +
          this.esc(row.stream_id) +
          '">' +
          '<input type="hidden" name="action" value="revive">' +
          '<button>Revive this stream</button>' +
          '</div></form>'
        : '') +
      (dead.length
        ? '<form method="post" action="/admin/ssf"><div class="formrow">' +
          '<input type="hidden" name="stream_id" value="' +
          this.esc(row.stream_id) +
          '">' +
          '<input type="hidden" name="action" value="clear-dead-letters">' +
          '<button class="secondary">Drop its dead letters</button>' +
          '</div></form>'
        : '') +
      // A TRANSMITTER-INITIATED VERIFICATION EVENT (#144, SSF 1.0 section
      // 8.1.4), with no state — the receiver did not ask, so there is none to
      // echo. The same act as POST /admin-api/ssf/verify.
      (row.status !== 'disabled'
        ? '<form method="post" action="/admin/ssf"><div class="formrow">' +
          '<input type="hidden" name="stream_id" value="' +
          this.esc(row.stream_id) +
          '">' +
          '<input type="hidden" name="action" value="verify">' +
          '<button class="secondary">Send a verification event</button>' +
          '</div></form>'
        : '') +
      '<form method="post" action="/admin/ssf"><div class="formrow">' +
      '<input type="hidden" name="stream_id" value="' +
      this.esc(row.stream_id) +
      '"><input type="hidden" name="action" value="status"><label ' +
      'for="status-' + this.esc(row.stream_id) +
      '">Set the status</label>' +
      '<select id="status-' + this.esc(row.stream_id) + '" name="status">' +
      (signalsReporter ? signalsReporter.statuses : []).map(function (one) {
        return '<option value="' + self.esc(one) + '"' +
          (one === row.status ? ' selected' : '') + '>' + self.esc(one) +
          '</option>';
      }).join('') + '</select>' +
      '<input type="text" name="reason" size="28" placeholder="reason (shown ' +
      'to the receiver)">' +
      '<button>Set status</button>' +
      '</div></form>' +
      '<form method="post" action="/admin/ssf"><div class="formrow">' +
      '<input type="hidden" name="stream_id" value="' +
      this.esc(row.stream_id) +
      '"><input type="hidden" name="action" value="transmit"><label ' +
      'for="type-' + this.esc(row.stream_id) +
      '">Transmit an event</label>' +
      '<select id="type-' + this.esc(row.stream_id) + '" name="type">' +
      (signalsReporter ? signalsReporter.eventTypes() : [])
        .map(function (one) {
          return '<option value="' + self.esc(one.uri) + '">' +
                 self.esc(one.name) +
            (one.offered ? '' : ' (not offered)') + '</option>';
        }).join('') + '</select>' +
      '<input type="text" name="payload" size="30" value="{}" ' +
      'placeholder="the event payload, as JSON">' +
      '<input type="text" name="subject" size="30" ' +
      'placeholder="sub_id, as JSON — optional">' +
      '<button>Transmit</button>' +
      '</div></form>' +
      '<form method="post" action="/admin/ssf"><div class="formrow">' +
      '<input type="hidden" name="stream_id" value="' +
      this.esc(row.stream_id) +
      '">' +
      '<input type="hidden" name="action" value="delete">' +
      '<button class="secondary">Delete this stream</button>' +
      '</div></form>';
  }

  setCaepReporter(reporter) {
    const { log, errorCodes, adminActions, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setCaepReporter().");
    const needed = ['report', 'action', 'actions', 'eventTypes'];
    const missing = needed.filter(function (name) {
      return !reporter || reporter[name] === undefined;
    });
    if (missing.length) {
      log.error(errorCodes.tag('STS-ADMIN-0014') + 'admin: setCaepReporter() ' +
                                                   'was given something ' +
                                                   'without ' +
                missing.join(', ') + ', and was ignored whole. /admin/caep ' +
                'and /admin/caep-sessions will say the reporter is not ' +
                'installed rather than half working.');
      log.debug("Leaving AdminConsole.setCaepReporter(). Refused.");
      return;
    }
    caepReporter = reporter;
    // AND THE ACTION LAYER, which needs the same object and is handed it
    // from here rather than asking for it — see the header of
    // admin-core/admin_actions.ts. One statement, two destinations: this is
    // the ONLY place either is written, which is what keeps it one answer.
    adminActions.setCaepReporter(reporter);
    // AND THE READ LAYER: the /admin-api report of this stream is drawn there
    // while the controls beside it are actions. One statement, one writer.
    adminViews.setCaepReporter(reporter);
    log.debug("Leaving AdminConsole.setCaepReporter(). Installed.");
  }

  // The short name of every CAEP event type, in catalogue order, so that the
  // count columns on the monitoring table are in the same order as the emit
  // form's menu. Derived from the reporter rather than written out, because a
  // list typed here would be the one place RISC's arrival is forgotten.
  caepShortNames(json) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.caepShortNames().");
    const out = (json.eventTypes || []).map(function (row) {
      return row.short;
    });
    log.debug("Leaving AdminConsole.caepShortNames(). " + out.length + ".");
    return out;
  }

  // What a session's CAEP state is called on the screen, with the class that
  // colours it. `revoked` is the one that must not read as ordinary: it is the
  // whole point of the profile.
  caepStateCell(state) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.caepStateCell(). " + state);
    const cls = state === 'revoked' ? 'state-invalid'
      : (state === 'presented' ? 'state-valid' : 'sub');
    const out = '<td class="' + cls + '">' + this.esc(state) + '</td>';
    log.debug("Leaving AdminConsole.caepStateCell().");
    return out;
  }

  // ONE ROW OF THE SESSIONS TABLE, and its first cell is the way IN since
  // 2026-09-04. What used to be under this table — a card per session listing
  // every event sent about it — is /admin/caep-sessions/session now, one
  // session at a time. `listView` is what the reader was looking at when they
  // clicked, so the drill-down's trail comes back to the search and the page
  // they left rather than to the top of an unfiltered list.
  //
  // `back` is the same query as a POSTable field, for the Reset button in the
  // last cell: resetting a row on page three used to answer with page one, so
  // the row somebody had just acted on was off screen and so was its neighbour.
  caepSessionRow(row, shorts, prefix, listView, back) {
    const { log, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.caepSessionRow().");
    const counts = shorts.map(function (short) {
      const n = row.counts[prefix + short] || 0;
      return '<td class="' + (n ? '' : 'sub') + '">' + self.esc(String(n)) +
             '</td>';
    }).join('');
    const href = '/admin/caep-sessions/session' +
      queryWith(listView || {}, { id: row.sessionId });
    const out = '<tr>' +
      '<td><a href="' + this.esc(href) + '" title="' +
      this.esc('Everything that has been said about this session, in order') +
      '"><code>' + this.esc(row.sessionId) + '</code></a>' +
      '<div class="sub">' + this.esc(row.protocol || '') + '</div></td>' +
      '<td>' + this.esc(row.username || row.sub || '(unknown)') +
      '<div class="sub"><code>' + this.esc(row.sub) + '</code></div></td>' +
      this.caepStateCell(row.state) +
      '<td class="sub">' + this.esc(row.assurance.level
        ? (row.assurance.namespace + ' ' + row.assurance.level) : '—') +
      '</td>' +
      '<td class="' + (row.compliance === 'not-compliant'
        ? 'state-invalid' : 'sub') + '">' +
      this.esc(row.compliance || '—') + '</td>' +
      '<td class="' + (row.risk.level === 'HIGH' ? 'state-invalid' : 'sub') +
      '">' + this.esc(row.risk.level || '—') + '</td>' +
      counts +
      '<td><strong>' + this.esc(String(row.total)) + '</strong></td>' +
      // IT POSTS TO /admin/caep AND NOT TO THE PAGE IT IS ON, which is the
      // arrangement the applications page's permission forms already have with
      // /admin/delegation: a form may live on one page and post to another's
      // handler, and MOVING A FORM IS NOT MOVING AN ACTION. There is one CAEP
      // action handler, it has one operation on /admin-api, and a second route
      // here would have wanted a second operation over the same function —
      // which is the duplication rule 7 is trying to prevent rather than
      // require. `from` and `back` are what bring the reader back to this page.
      '<td><form method="post" action="/admin/caep">' +
      '<input type="hidden" name="action" value="reset-session">' +
      '<input type="hidden" name="session_id" value="' +
      this.esc(row.sessionId) + '">' +
      '<input type="hidden" name="from" value="sessions">' +
      '<input type="hidden" name="back" value="' + this.esc(back || '') + '">' +
      '<button class="secondary">Reset</button></form></td>' +
      '</tr>';
    log.debug("Leaving AdminConsole.caepSessionRow().");
    return out;
  }

  // One session, opened out: what has actually been sent about it, in order,
  // with the findings the register made as each one was applied. The counts on
  // the table above say HOW MANY and this says WHICH, and the two are different
  // questions — see caep.ts on why the ring and the counters are separate.
  //
  // **IT IS A PAGE OF ITS OWN SINCE 2026-09-04 AND USED TO BE A CARD PER
  // SESSION UNDER THE TABLE.** That block was drawn for EVERY session the
  // register held, each with a table of its own, so a service driven for an
  // afternoon answered /admin/caep-sessions with a couple of hundred nested
  // tables under the one table anybody had come to read — and the sessions
  // table itself was then off the top of the screen for the whole of it. One
  // session at a time, reached by clicking the identifier, is the arrangement
  // /admin/tokens and its credential drill-down already have.
  caepEventRow(one) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.caepEventRow().");
    log.debug("Leaving AdminConsole.caepEventRow().");
    return '<tr><td class="sub">' + this.esc(one.at) + '</td>' +
      '<td>' + this.esc(one.name) + '</td>' +
      '<td><code>' + this.esc(one.jti) + '</code></td>' +
      '<td><code>' + this.esc(one.streamId || '(none)') + '</code></td>' +
      '<td class="sub">' + this.esc((one.warnings || []).join(' ') || '—') +
      '</td></tr>';
  }

  // The facts about one session that are not events: who it belongs to, what
  // state it is in, and the three CAEP dimensions a receiver acts on. Drawn as
  // a table rather than as the run of `<div class="sub">` lines the card used,
  // because on a page of its own this is the summary somebody reads first and a
  // paragraph of eight facts is not read at all.
  caepSessionFacts(row) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.caepSessionFacts().");
    function line(label, value, cls?) {
      log.debug("Entering line().");
      log.debug("Leaving line().");
      return '<tr><th>' + self.esc(label) + '</th><td' +
        (cls ? ' class="' + cls + '"' : '') + '>' + value + '</td></tr>';
    }
    const out = '<table>' +
      line('Session', '<code>' + this.esc(row.sessionId) + '</code>') +
      line('Who', this.esc(row.username || row.sub || '(unknown)') +
        ' <span class="sub"><code>' + this.esc(row.sub) + '</code></span>') +
      line('Subject',
           '<span class="sub">' + this.esc(row.subject) + '</span>') +
      line('Issuer', '<span class="sub"><code>' + this.esc(row.iss || '—') +
        '</code></span>') +
      line('Protocol', '<span class="sub">' + this.esc(row.protocol || '—') +
        '</span>') +
      line('State', this.esc(row.state),
        row.state === 'revoked' ? 'state-invalid'
          : (row.state === 'presented' ? 'state-valid' : 'sub')) +
      line('Established', '<span class="sub">' + this.esc(row.establishedAt) +
        '</span>') +
      line('Last changed', '<span class="sub">' + this.esc(row.updatedAt) +
        '</span>') +
      line('Assurance', '<span class="sub">' + this.esc(row.assurance.level
        ? (row.assurance.namespace + ' ' + row.assurance.level) : '—') +
        '</span>') +
      line('Device compliance', this.esc(row.compliance || '—'),
        row.compliance === 'not-compliant' ? 'state-invalid' : 'sub') +
      line('Risk', this.esc(row.risk.level || '—') +
        (row.risk.subject ? ' <span class="sub">' + this.esc(row.risk.subject) +
          '</span>' : ''),
        row.risk.level === 'HIGH' ? 'state-invalid' : 'sub') +
      line('acr / amr', '<span class="sub"><code>' + this.esc(row.acr || '—') +
        '</code> / <code>' + this.esc((row.amr || []).join(' ') || '—') +
        '</code></span>') +
      (Object.keys(row.claims).length
        ? line('Claims changed',
               '<code>' + this.esc(JSON.stringify(row.claims)) +
          '</code>')
        : '') +
      ((row.credentials || []).length
        ? line('Credentials', '<span class="sub">' +
          this.esc(row.credentials.join(', ')) + '</span>')
        : '') +
      ((row.notes || []).length
        ? line('Notes', '<span class="sub">' + this.esc(row.notes.join(' ')) +
          '</span>')
        : '') +
      '</table>';
    log.debug("Leaving AdminConsole.caepSessionFacts().");
    return out;
  }

  // What has been said about this session PER TYPE, which is the row of the
  // sessions table the reader clicked, drawn the long way round. It is here and
  // not only there because the eight columns of that table are headed by an
  // abbreviation — `revoked`, `established`, `credential` — and this is the one
  // place there is room for the type's whole name and its URI.
  caepSessionCounts(row, types) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.caepSessionCounts().");
    const rows = types.map(function (type) {
      const n = row.counts[type.uri] || 0;
      return '<tr><td>' + self.esc(type.name) + '</td>' +
        '<td class="sub"><code>' + self.esc(type.short) + '</code></td>' +
        '<td class="' + (n ? '' : 'sub') + '">' + self.esc(String(n)) +
        '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.caepSessionCounts(). " + types.length +
              " type(s).");
    return '<table><tr><th>Event type</th><th>Short ' +
      'name</th><th>Sent</th></tr>' +
      rows + '</table>';
  }

  // One row of it. The eight count columns are in the SAME ORDER as the
  // sessions table's, off the same `caepShortNames()`, because a reader moving
  // between the two tables is reading one vocabulary — and a column that moved
  // between them would be worse than a column too many.
  caepApplicationRow(row, shorts, prefix) {
    const { log, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.caepApplicationRow(). " + row.identifier);
    const counts = shorts.map(function (short) {
      const n = row.counts[prefix + short] || 0;
      return '<td class="' + (n ? '' : 'sub') + '">' + self.esc(String(n)) +
             '</td>';
    }).join('');
    // WHERE IT IS IN THE REGISTRY, when it is there at all. A receiver seen
    // when a stream was created has an entry; one that agreed a stream while
    // the stream endpoint was not gated has none, and that row is the collected
    // total for all of them rather than an application. The gate was
    // `ssf.authRequired` until 2026-09-06 and is `mode.gatesSharedSignals()`
    // now (ssf/ssf_auth.ts); the page's note below still names the old setting.
    const who = row.registered
      ? '<a href="' + this.esc('/admin/applications' +
          queryWith({ application: row.identifier }, {})) + '">' +
        this.esc(row.name || row.identifier) + '</a>' +
        (row.name && row.name !== row.identifier
          ? '<div class="sub"><code>' + this.esc(row.identifier) +
            '</code></div>'
          : '')
      : '<span class="sub">' + this.esc(row.name || row.identifier) + '</span>';
    const streams = row.streamCount
      ? this.esc(String(row.streamCount)) +
        (row.enabled === row.streamCount
          ? ''
          : ' <span class="state-invalid">(' + this.esc(String(row.enabled)) +
            ' enabled)</span>') +
        '<div class="sub">' + this.esc(row.deliveries.join(', ')) + '</div>'
      : '<span class="state-none" title="' +
        this.esc('Declared for Shared Signals and no stream has been agreed. ' +
                 'That is the ordinary state of a receiver that has not ' +
                 'connected yet, and it is the first thing to check when ' +
                 'nothing arrives.') +
        '">none</span>';
    // THE AUDIENCE, BESIDE THE IDENTIFIER RATHER THAN INSTEAD OF IT. They are
    // different fields — one is what the receiver authenticated as and the
    // other is what it asked its SETs to be addressed to — and a receiver whose
    // `aud` is not its own name is doing something legitimate that is invisible
    // anywhere else in this console. shortened() rather than the whole string,
    // for the reason the tokens page uses it on a jti: an `aud` is routinely a
    // URL, `code` is `word-break: break-all`, and a fifteen-column table gives
    // this one about three characters of width — so the untruncated value
    // wrapped to six lines and made every row on the page that tall. The whole
    // value is the tooltip.
    const audience = row.audiences.length
      ? (row.audiences.length === 1 && row.audiences[0] === row.identifier
          ? '<span class="sub" title="' +
            this.esc('The receiver asked its Security Event Tokens to be ' +
                     'addressed to the same name it authenticated as. That ' +
                     'is the ordinary case and not a requirement: `aud` is ' +
                     'the receiver\'s own choice and this service never ' +
                     'defaults it.') +
            '">the same</span>'
          : this.shortened(row.audiences.join(', '), 20))
      : '<span class="sub" title="' +
        this.esc('No stream, so nothing is addressed anywhere yet.') +
        '">&mdash;</span>';
    const out = '<tr>' +
      '<td>' + who + '</td>' +
      '<td>' + streams + '</td>' +
      '<td class="sub">' + audience + '</td>' +
      '<td class="' + (row.takes.length ? '' : 'state-invalid') + '">' +
      this.esc(row.takes.length ? String(row.takes.length) + ' of 8'
                                : 'none of CAEP\'s eight') + '</td>' +
      counts +
      '<td><strong>' + this.esc(String(row.total)) + '</strong></td>' +
      '<td class="sub">' + this.esc(String(row.sessions)) + '</td>' +
      '<td class="sub">' + this.esc(String(row.delivered)) + ' / ' +
      '<span class="' + (row.failed ? 'state-invalid' : 'sub') + '">' +
      this.esc(String(row.failed)) + '</span>' +
      (row.lastPushError
        ? '<div class="sub" title="' + this.esc(row.lastPushError) +
          '">last error recorded</div>'
        : '') + '</td>' +
      '</tr>';
    log.debug("Leaving AdminConsole.caepApplicationRow().");
    return out;
  }

  // WHERE A CAEP ACTION ANSWERS BACK TO. Three pages post to one handler — the
  // settings page, the sessions table and the one-session drill-down — and
  // `from` says which, read as an ENUM and never as a path, which is the rule
  // backTo() follows on the tokens page and the reason an open redirect is not
  // reachable from this field. The list view is REBUILT out of `back` by
  // listViewFromBack(), from the whitelist in LIST_PARAMS, so the worst a
  // hand-written field can produce is another page of the same table.
  caepSessionsBackTo(body) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.caepSessionsBackTo().");
    const from = String((body || {}).from || '');
    if (from === 'session') {
      const target = '/admin/caep-sessions/session' +
        queryWith(this.listViewFromBack('/admin/caep-sessions', body.back),
                  { id: String(body.session_id || '') });
      log.debug("Leaving AdminConsole.caepSessionsBackTo(). " + target);
      return target;
    }
    if (from === 'sessions') {
      const target = '/admin/caep-sessions' +
        queryWith(this.listViewFromBack('/admin/caep-sessions', body.back), {});
      log.debug("Leaving AdminConsole.caepSessionsBackTo(). " + target);
      return target;
    }
    log.debug("Leaving AdminConsole.caepSessionsBackTo(). The CAEP page.");
    return '/admin/caep';
  }

  setRiscReporter(reporter) {
    const { log, errorCodes, adminActions, adminViews } = this.deps;
    log.debug("Entering AdminConsole.setRiscReporter().");
    const needed = ['report', 'action', 'actions', 'eventTypes'];
    const missing = needed.filter(function (name) {
      return !reporter || reporter[name] === undefined;
    });
    if (missing.length) {
      log.error(errorCodes.tag('STS-ADMIN-0014') + 'admin: setRiscReporter() ' +
                                                   'was given something ' +
                                                   'without ' +
                missing.join(', ') + ', and was ignored whole. /admin/risc ' +
                'and /admin/risc-accounts will say the reporter is not ' +
                'installed rather than half working.');
      log.debug("Leaving AdminConsole.setRiscReporter(). Refused.");
      return;
    }
    riscReporter = reporter;
    // AND THE ACTION LAYER, which needs the same object and is handed it
    // from here rather than asking for it — see the header of
    // admin-core/admin_actions.ts. One statement, two destinations: this is
    // the ONLY place either is written, which is what keeps it one answer.
    adminActions.setRiscReporter(reporter);
    // AND THE READ LAYER: the /admin-api report of this stream is drawn there
    // while the controls beside it are actions. One statement, one writer.
    adminViews.setRiscReporter(reporter);
    log.debug("Leaving AdminConsole.setRiscReporter(). Installed.");
  }

  // The short name of every RISC event type, in catalogue order, so that the
  // count columns on the monitoring table are in the same order as the emit
  // form's menu. Derived from the reporter rather than written out.
  riscShortNames(json) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.riscShortNames().");
    const out = (json.eventTypes || []).map(function (row) {
      return row.short;
    });
    log.debug("Leaving AdminConsole.riscShortNames(). " + out.length + ".");
    return out;
  }

  // A RISC event type's column heading, which has to be SHORT and has to stay
  // distinguishable — and those two pull against each other harder here than
  // they do for CAEP. Four of the fourteen begin `opt-out-` and three begin
  // `account-`, so dropping a common prefix the way the CAEP table drops
  // `session-` would leave three columns headed `-in`, `-initiated` and
  // `-cancelled`. The rule is therefore: drop `account-`, which leaves four
  // distinct words, and keep everything else whole. The whole URI is the title.
  riscColumnLabel(short) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.riscColumnLabel(). " + short);
    const out = String(short || '').replace(/^account-/, '');
    log.debug("Leaving AdminConsole.riscColumnLabel(). " + out);
    return out;
  }

  // What an account's RISC lifecycle is called on the screen, with the class
  // that colours it. `purged` is the one that must not read as ordinary: RISC
  // defines it as permanently deleted and it is the only terminal state in the
  // vocabulary.
  riscLifecycleCell(state) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.riscLifecycleCell(). " + state);
    const cls = (state === 'purged' || state === 'disabled') ? 'state-invalid'
      : 'sub';
    const out = '<td class="' + cls + '">' + this.esc(state) + '</td>';
    log.debug("Leaving AdminConsole.riscLifecycleCell().");
    return out;
  }

  // And its opt-out state, which is a SECOND dimension and not a variant of the
  // first. An account can be opted out and perfectly healthy, or compromised
  // and still exchanging — so the two are two columns, and folding them into
  // one word would mean choosing which of the two questions this page answers.
  riscOptCell(state) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.riscOptCell(). " + state);
    const cls = state === 'opt-out' ? 'state-invalid'
      : (state === 'opt-out-initiated' ? '' : 'sub');
    const title = state === 'opt-out'
      ? 'RISC section 2.8: this account is not participating in event ' +
        'exchange, so nothing but an opt-out event is sent about it while ' +
        'risc.honourOptOut is on.'
      : (state === 'opt-out-initiated'
          ? 'The person asked to stop and exchange CARRIES ON for a while. ' +
            'That delay exists to stop a hijacker opting out the moment they ' +
            'take an account over and silencing the events that would report ' +
            'them.'
          : 'Participating in RISC event exchange.');
    const out = '<td class="' + cls + '" title="' + this.esc(title) + '">' +
      this.esc(state) + '</td>';
    log.debug("Leaving AdminConsole.riscOptCell().");
    return out;
  }

  // ONE ROW OF THE ACCOUNTS TABLE, and its first cell is the way in — the
  // arrangement /admin/caep-sessions has, for its reason.
  riscAccountRow(row, shorts, prefix, listView, back) {
    const { log, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.riscAccountRow().");
    const counts = shorts.map(function (short) {
      const n = row.counts[prefix + short] || 0;
      return '<td class="' + (n ? '' : 'sub') + '">' + self.esc(String(n)) +
             '</td>';
    }).join('');
    const href = '/admin/risc-accounts/account' +
      queryWith(listView || {}, { id: row.accountId });
    const out = '<tr>' +
      '<td><a href="' + this.esc(href) + '" title="' +
      this.esc('Everything that has been said about this account, in order') +
      '"><code>' + this.esc(row.accountId) + '</code></a>' +
      (row.email
        ? '<div class="sub">' + this.esc(row.email) + '</div>' : '') + '</td>' +
      '<td class="sub">' + this.esc(row.subject || '—') + '</td>' +
      this.riscLifecycleCell(row.lifecycle) +
      this.riscOptCell(row.optOut) +
      '<td class="' + (row.credentialStanding === 'compromised'
        ? 'state-invalid' : 'sub') + '">' +
      this.esc(row.credentialStanding || '—') + '</td>' +
      counts +
      '<td><strong>' + this.esc(String(row.total)) + '</strong></td>' +
      '<td class="' + (row.suppressed ? 'state-invalid' : 'sub') + '" title="' +
      this.esc('Events this transmitter built and did NOT send, because the ' +
               'account is in the RISC opt-out state. A count here is the ' +
               'one number on this page that says a receiver heard nothing ' +
               'ON PURPOSE.') +
      '">' + this.esc(String(row.suppressed)) + '</td>' +
      // IT POSTS TO /admin/risc AND NOT TO THE PAGE IT IS ON, for the reason
      // the CAEP table's Reset does: there is one RISC action handler and one
      // operation on /admin-api over it, and moving a form is not moving an
      // action.
      '<td><form method="post" action="/admin/risc">' +
      '<input type="hidden" name="action" value="reset-account">' +
      '<input type="hidden" name="account_id" value="' +
      this.esc(row.accountId) + '">' +
      '<input type="hidden" name="from" value="accounts">' +
      '<input type="hidden" name="back" value="' + this.esc(back || '') + '">' +
      '<button class="secondary">Reset</button></form></td>' +
      '</tr>';
    log.debug("Leaving AdminConsole.riscAccountRow().");
    return out;
  }

  riscEventRow(one) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.riscEventRow().");
    log.debug("Leaving AdminConsole.riscEventRow().");
    return '<tr><td class="sub">' + this.esc(one.at) + '</td>' +
      '<td>' + this.esc(one.name) + '</td>' +
      '<td><code>' + this.esc(one.jti) + '</code></td>' +
      '<td><code>' + this.esc(one.streamId || '(none)') + '</code></td>' +
      '<td class="sub">' + this.esc((one.warnings || []).join(' ') || '—') +
      '</td></tr>';
  }

  // The facts about one account that are not events. It is longer than the CAEP
  // equivalent by exactly the amount RISC's model is larger: a session has one
  // state and an account has three that move independently, and the identifiers
  // it has been known by are a list rather than a value — because
  // `identifier-changed` is an event about the key itself.
  riscAccountFacts(row) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.riscAccountFacts().");
    function line(label, value, cls?) {
      log.debug("Entering line().");
      log.debug("Leaving line().");
      return '<tr><th>' + self.esc(label) + '</th><td' +
        (cls ? ' class="' + cls + '"' : '') + '>' + value + '</td></tr>';
    }
    const out = '<table>' +
      line('Account', '<code>' + this.esc(row.accountId) + '</code>') +
      line('Subject', '<span class="sub">' + this.esc(row.subject || '—') +
        '</span>') +
      line('Issuer', '<span class="sub"><code>' + this.esc(row.iss || '—') +
        '</code></span>') +
      line('Directory entry',
           '<span class="sub"><code>' + this.esc(row.dn || '—') +
        '</code></span>') +
      line('Lifecycle', this.esc(row.lifecycle),
        (row.lifecycle === 'purged' || row.lifecycle === 'disabled')
          ? 'state-invalid' : 'sub') +
      line('Opt-out state', this.esc(row.optOut),
        row.optOut === 'opt-out' ? 'state-invalid' : 'sub') +
      line('Credential standing', this.esc(row.credentialStanding || '—'),
        row.credentialStanding === 'compromised' ? 'state-invalid' : 'sub') +
      line('Credential change required', row.credentialChangeRequired
        ? 'yes' : '<span class="sub">no</span>') +
      line('Recovery activated', row.recoveryActivated
        ? 'yes' : '<span class="sub">no</span>') +
      line('Email',
           '<span class="sub">' + this.esc(row.email || '—') + '</span>') +
      line('Phone',
           '<span class="sub">' + this.esc(row.phone || '—') + '</span>') +
      ((row.formerIdentifiers || []).length
        ? line('Known by, formerly', '<span class="sub" title="' +
          this.esc('An event naming one of these still counts against this ' +
                   'row. That is what keeps identifier-changed from ' +
                   'splitting one person into two rows at the moment their ' +
                   'identifier moves.') +
          '">' + this.esc(row.formerIdentifiers.join(', ')) + '</span>')
        : '') +
      line('First seen', '<span class="sub">' + this.esc(row.createdAt) +
        '</span>') +
      line('Last changed', '<span class="sub">' + this.esc(row.updatedAt) +
        '</span>') +
      line('Suppressed', this.esc(String(row.suppressed || 0)),
        row.suppressed ? 'state-invalid' : 'sub') +
      ((row.identifierChanges || []).length
        ? line('Identifier changes', '<span class="sub">' +
          this.esc(row.identifierChanges.map(function (one) {
            return (one.from || '(none)') + ' → ' + (one.to || '(not said)');
          }).join(', ')) + '</span>')
        : '') +
      ((row.credentials || []).length
        ? line('Compromised credentials', '<span class="sub">' +
          this.esc(row.credentials.map(function (one) {
            return one.credentialType || '(unstated)';
          }).join(', ')) + '</span>')
        : '') +
      ((row.notes || []).length
        ? line('Notes', '<span class="sub">' + this.esc(row.notes.join(' ')) +
          '</span>')
        : '') +
      '</table>';
    log.debug("Leaving AdminConsole.riscAccountFacts().");
    return out;
  }

  // What has been said about this account PER TYPE, written the long way round.
  // It carries a DEPRECATED column that the CAEP equivalent has no need of: one
  // of RISC's fourteen is deprecated by its own specification in favour of a
  // CAEP event, and a count in that row is a fact about the receiver's future
  // rather than about this account.
  riscAccountCounts(row, types) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.riscAccountCounts().");
    const rows = types.map(function (type) {
      const n = row.counts[type.uri] || 0;
      return '<tr><td>' + self.esc(type.name) + '</td>' +
        '<td class="sub"><code>' + self.esc(type.short) + '</code></td>' +
        '<td class="' + (type.deprecated ? 'state-invalid' : 'sub') + '">' +
        (type.deprecated
          ? 'deprecated &mdash; use <code>' +
            self.esc(String(type.deprecated).split('/').pop()) + '</code>'
          : '&mdash;') + '</td>' +
        '<td class="' + (n ? '' : 'sub') + '">' + self.esc(String(n)) +
        '</td></tr>';
    }).join('');
    log.debug("Leaving AdminConsole.riscAccountCounts(). " + types.length +
              " type(s).");
    return '<table><tr><th>Event type</th><th>Short name</th><th></th>' +
      '<th>Sent</th></tr>' + rows + '</table>';
  }

  // WHERE A RISC ACTION ANSWERS BACK TO. The same enum-not-a-path rule
  // caepSessionsBackTo() follows, for its reason.
  riscAccountsBackTo(body) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.riscAccountsBackTo().");
    const from = String((body || {}).from || '');
    if (from === 'account') {
      const target = '/admin/risc-accounts/account' +
        queryWith(this.listViewFromBack('/admin/risc-accounts', body.back),
                  { id: String(body.account_id || '') });
      log.debug("Leaving AdminConsole.riscAccountsBackTo(). " + target);
      return target;
    }
    if (from === 'accounts') {
      const target = '/admin/risc-accounts' +
        queryWith(this.listViewFromBack('/admin/risc-accounts', body.back), {});
      log.debug("Leaving AdminConsole.riscAccountsBackTo(). " + target);
      return target;
    }
    log.debug("Leaving AdminConsole.riscAccountsBackTo(). The RISC page.");
    return '/admin/risc';
  }

  riscApplicationRow(row, shorts, prefix) {
    const { log, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.riscApplicationRow(). " + row.identifier);
    const counts = shorts.map(function (short) {
      const n = row.counts[prefix + short] || 0;
      return '<td class="' + (n ? '' : 'sub') + '">' + self.esc(String(n)) +
             '</td>';
    }).join('');
    const who = row.registered
      ? '<a href="' + this.esc('/admin/applications' +
          queryWith({ application: row.identifier }, {})) + '">' +
        this.esc(row.name || row.identifier) + '</a>' +
        (row.name && row.name !== row.identifier
          ? '<div class="sub"><code>' + this.esc(row.identifier) +
            '</code></div>'
          : '')
      : '<span class="sub">' + this.esc(row.name || row.identifier) + '</span>';
    const streams = row.streamCount
      ? this.esc(String(row.streamCount)) +
        (row.enabled === row.streamCount
          ? ''
          : ' <span class="state-invalid">(' + this.esc(String(row.enabled)) +
            ' enabled)</span>') +
        '<div class="sub">' + this.esc(row.deliveries.join(', ')) + '</div>'
      : '<span class="state-none" title="' +
        this.esc('Declared for Shared Signals and no stream has been agreed.') +
        '">none</span>';
    const audience = row.audiences.length
      ? (row.audiences.length === 1 && row.audiences[0] === row.identifier
          ? '<span class="sub">the same</span>'
          : this.shortened(row.audiences.join(', '), 20))
      : '<span class="sub">&mdash;</span>';
    const out = '<tr>' +
      '<td>' + who + '</td>' +
      '<td>' + streams + '</td>' +
      '<td class="sub">' + audience + '</td>' +
      '<td class="' + (row.takes.length ? '' : 'state-invalid') + '">' +
      this.esc(row.takes.length ? String(row.takes.length) + ' of 14'
                                : 'none of RISC\'s fourteen') + '</td>' +
      counts +
      '<td><strong>' + this.esc(String(row.total)) + '</strong></td>' +
      '<td class="sub">' + this.esc(String(row.accounts)) + '</td>' +
      '<td class="sub">' + this.esc(String(row.delivered)) + ' / ' +
      '<span class="' + (row.failed ? 'state-invalid' : 'sub') + '">' +
      this.esc(String(row.failed)) + '</span>' +
      (row.lastPushError
        ? '<div class="sub" title="' + this.esc(row.lastPushError) +
          '">last error recorded</div>'
        : '') + '</td>' +
      '</tr>';
    log.debug("Leaving AdminConsole.riscApplicationRow().");
    return out;
  }

  // ONE ROW OF THE INDEX: a group, its size, what is overridden in it, and the
  // page that edits it. Built from `config.groups()` and SETTING_HOMES
  // together, which is what makes a group with no home visible here rather than
  // merely absent — see checkSettingHomes(), whose findings the page prints
  // above this table.
  configHomeRow(group) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.configHomeRow().");
    const row = this.settingHomeRowOf(group.group);
    const overridden = group.settings.filter(function (setting) {
      return setting.overridden;
    }).length;
    const fixed = group.settings.filter(function (setting) {
      return !setting.editable;
    }).length;
    const where = row
      ? row.pages.map(function (path) {
          return '<a href="' + self.esc(path) + '">' +
                 self.esc(self.labelOfPath(path)) + '</a>';
        }).join(' and ')
      : '<span class="state-invalid">nowhere — this group has no row in ' +
        'SETTING_HOMES, so nothing draws it</span>';
    log.debug("Leaving AdminConsole.configHomeRow().");
    return '<tr><td>' + this.esc(group.group) + '</td>' +
      '<td class="num">' + group.settings.length + '</td>' +
      '<td class="num">' + (fixed || '') + '</td>' +
      '<td class="num">' + (overridden || '') + '</td>' +
      '<td>' + where + '</td></tr>';
  }

  // ---------------------------------------------------------------------------
  // THE EIGHT PROTOCOL SETTINGS PAGES ADDED ON 2026-08-27, AND WHY THEY ARE A
  // TABLE AND A LOOP RATHER THAN EIGHT ROUTE HANDLERS.
  //
  // Five protocol families here had settings and no page in this console —
  // OAuth 2.0 / OIDC, WS-Trust, WS-Federation, Kerberos, LDAP, TLS, OpenID4VCI
  // and OpenID4VP, eight pages across six of them — so their configuration was
  // only ever reachable at /admin/config among a hundred and fifty-four rows.
  // Every one of these pages is therefore the SAME page: a paragraph or two
  // saying what the family is and where it answers, the links to the surfaces
  // it already has, and `configFormsFor()`. The differences between them are
  // PROSE.
  //
  // Eight handlers would have been eight copies of one four-line body, and the
  // copy nobody edited is the one a reader believes — the defect this
  // repository warns about everywhere else. So the prose is a table and the
  // handler is written once. Three things follow and each is deliberate:
  //
  //   * **REGISTRATION IS STILL IN THE ONE REGISTRATION PASS** (rule 1). The
  //     loop runs inside `registerRoutes()` — while this module was being
  //     required until #50's R1, and when `common/protocol_stack.ts` calls
  //     that method since — so these routes are registered in the order they
  //     appear here, are behind the gate registered above them, and are
  //     visible to `sts_metadata.js` reading the router — a page built in a
  //     loop is not a page built differently.
  //   * **THE SETTINGS ARE NOT IN THIS TABLE.** A row names a `path`, and
  //     SETTING_HOMES says what lives there. Naming keys here would be the
  //     second list that disagrees with the first.
  //   * **A ROW MAY SAY WHAT ITS FAMILY DOES NOT DO**, and several do. These
  //     are the pages a person lands on when they are deciding whether this
  //     service can stand in for a real one, and the answer "it speaks the
  //     protocol" without "it checks nothing" is the misleading half of a true
  //     sentence — which is the same rule `sts_metadata.js`'s coverage notes
  //     follow.
  //
  // WHAT IS DELIBERATELY NOT ON THEM IS AN ENDPOINT LIST. Every one of these
  // families answers on paths this file would have to keep in step by hand, and
  // `/admin/sts-metadata` already derives exactly that list from the running
  // router. A hand-written table here would be the drift that page exists to
  // catch, on a page that cannot be checked. The links below are to the
  // family's own surfaces — pages that explain themselves — and to the
  // service metadata for the rest.
  //
  // **REVERSED 2026-09-13, AND STILL NOT IN THIS TABLE**: every Protocols page
  // now lists its realm's endpoints, drawn by `respond()` from
  // `admin-core/protocol_endpoints.ts`, which names ROUTES and takes names and
  // methods from `sts_metadata.js` and the router, so both drifts fail
  // `tests/protocol_endpoints.js`. admin-ui/CLAUDE.md argues it.
  // ---------------------------------------------------------------------------
  // WHAT THE PERSISTENT STORE IS ACTUALLY DOING, as HTML and as JSON at once.
  //
  // ONE FUNCTION RETURNING BOTH, for the reason `config.describe()` is one
  // shape: a console and a management API that compute the same answer
  // separately are a console and an API that will one day disagree about
  // whether the last write worked, and that is the single worst thing either of
  // them could disagree about.
  //
  // **IT COMPUTES NOTHING.** Every number and every sentence comes out of
  // `persistence.status()`, which is that module's own account of itself and is
  // the same object `GET /admin/ldap/service` publishes. This function chooses
  // what to SHOW and how to phrase it; it does not decide anything, so there is
  // no second opinion here to go stale.
  // ---------------------------------------------------------------------------
  persistenceStatusBlock() {
    const { log, persistence } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.persistenceStatusBlock().");
    const info = persistence.status();
    const off = info.mode === 'memory';

    const rows = [
      ['Mode', off
        ? '<strong>memory</strong> — nothing is written down. This is what ' +
          'this service did until 2026-08-27 and is still the default.'
        : '<strong>' + this.esc(info.mode) + '</strong>' +
          (info.configuredMode !== info.mode
            ? ' — though <code>persistence.mode</code> is set to <code>' +
              this.esc(info.configuredMode) + '</code>. IT FELL BACK, which ' +
              'means the store could not be opened or read; the reason is ' +
              'below and in the log. Nothing is being written down. ' +
              '<strong>THIS SHOULD NOT BE REACHABLE</strong>: since ' +
              '2026-08-28 a configured store that cannot be opened stops the ' +
              'process at startup rather than letting it run as something it ' +
              'is not, so a service drawing this page has one. Seeing it ' +
              'means something re-introduced the fallback — the row is kept ' +
              'as the net under that.'
            : '')],
      ['Where', off ? 'nowhere'
        : info.mode === 'ldif'
          ? '<code>' + this.esc(info.dataDir || '') + '</code> — one RFC ' +
            '2849 LDIF file per realm, plus <code>realms.json</code> and ' +
            '<code>appconfig.json</code>'
          : info.database
            ? 'PostgreSQL <code>' + this.esc(String(info.database.host)) + ':' +
              this.esc(String(info.database.port)) + '/' +
              this.esc(String(info.database.database)) + '</code> as <code>' +
              this.esc(String(info.database.user ||
                              'the connection string\'s user')) +
              '</code>. The connection string itself is never shown here: it ' +
              'carries a password and this page is one fetch away from a log.'
            : 'a PostgreSQL connection string'],
      // WHERE THE PASSWORD COMES FROM (2026-09-12), as its own row and never
      // the password itself. A deployment that moved it into a secret store has
      // no other way to see that this process agreed: the row above looks
      // identical either way, because it never printed the password.
      ['Password', off || info.mode !== 'postgres'
        ? 'no connection is dialled, so there is no credential'
        : (info.database
            ? (info.database.passwordProvider &&
               info.database.passwordProvider !== 'none'
                ? '<strong>' + this.esc(String(info.database.passwordFrom)) +
                  '</strong> &mdash; read once at startup and put into the ' +
                  'connection string. The same mechanism, and by default the ' +
                  'same file or secret, as the key-encryption key on ' +
                  '<a href="/admin/encryption">the encryption report</a>.'
                : 'the connection string ' +
                  '(<code>persistence.databaseUrl</code>), in plain text. ' +
                  '<code>persistence.databasePasswordProvider</code> reads ' +
                  'it from a mounted file, AWS Secrets Manager, Google ' +
                  'Secret Manager, Azure Key Vault or HashiCorp Vault instead.')
            : 'unknown')],
      // TLS TO THE DATABASE, as its own row rather than folded into the one
      // above. Encryption and authentication are two answers and this page has
      // room to give both — which matters here more than in most places,
      // because the honest state of the compose stack is "encrypted and not
      // authenticated" and a single tick would have to round that one way or
      // the other.
      ['Transport', off || info.mode !== 'postgres'
        ? (info.mode === 'ldif'
            ? 'a file on this machine — no connection, so nothing to encrypt'
            : 'nothing is written, so nothing is dialled')
        : (info.database
            ? (info.database.encrypted
                ? '<strong>TLS</strong> &mdash; ' +
                  this.esc(String(info.database.tls))
                : '<strong class="bad">NOT TLS</strong> &mdash; ' +
                  this.esc(String(info.database.tls)))
            : 'unknown')],
      ['What is written', off ? 'nothing' : [
          info.persistsDirectory ? 'the embedded directory' : null,
          info.persistsRealms ? 'the trust realm registry' : null,
          info.persistsAppconfig ? 'runtime setting changes' : null,
          info.minted && info.minted.persisting
            ? '<strong>and everything this process mints</strong> — ' +
              'sessions, tokens, codes, artifacts, Kerberos principals, the ' +
              'replay caches, the counters and the audit log, across ' +
              this.esc(String(info.minted.stores)) +
              ' stores, every row encrypted'
            : null
        ].filter(Boolean).join(', ')],
      // -----------------------------------------------------------------------
      // THE NEGATIVE ROW, AND IT IS NOW CONDITIONAL — which is the whole of
      // what changed here on 2026-09-06. It used to be a constant, because the
      // answer used to be the same in every configuration. Keeping it as a row
      // rather than deleting it is deliberate: "what is never written" is the
      // question an operator actually has, and a page that answered it only
      // when the answer was long would be a page that stopped answering it
      // exactly when the answer got interesting.
      // -----------------------------------------------------------------------
      ['What is NEVER written', off
        ? 'everything — nothing at all is written in this mode'
        : (info.minted && info.minted.persisting
            ? 'nothing, beyond the two caches that are re-derivable ' +
              '(the signed-metadata cache and the parsed-policy cache) and ' +
              'the plaintext of the signing keys, which are held encrypted. ' +
              '<strong>The private keys themselves are never written in the ' +
              'clear</strong>, and neither is any minted row.'
            : 'sessions, access tokens, ID Tokens, refresh tokens, ' +
              'authorization codes, pre-authorized codes, SAML artifacts, ' +
              'Kerberos tickets, the replay caches, the statistics and the ' +
              'audit log' +
              (info.minted && info.minted.unsupportedReason
                ? ' — because ' + this.esc(info.minted.unsupportedReason)
                : ' — because the signing key is regenerated on every start ' +
                  'in development mode, so a token that outlived it would ' +
                  'verify against nothing') + '.')],
      // -----------------------------------------------------------------------
      // AND WHETHER THIS PROCESS IS ALONE WITH ITS COPY. This row is the
      // reversal of the sentence that used to be two paragraphs of prose above:
      // "one process per database".
      // -----------------------------------------------------------------------
      ['Other processes', off || !info.replication
        ? '—'
        : info.replication.coordinating
          ? '<strong>coordinating</strong> — applied up to change ' +
            this.esc(String(info.replication.appliedSeq)) + ', ' +
            this.esc(String(info.replication.rowsApplied)) + ' row(s) taken ' +
              'from ' +
            this.esc(String(info.replication.otherProcesses)) + ' other ' +
            'process(es), polling ' +
            'every ' + this.esc(String(info.replication.pollIntervalMs)) +
            'ms with a LISTEN/NOTIFY nudge on top' +
            (info.replication.lastError
              ? '. <strong class="bad">THE LAST PULL FAILED</strong> — ' +
                this.esc(info.replication.lastError) + '. This process is ' +
                'BEHIND and is serving its own copy until it catches up.'
              : '.')
          : '<strong>not coordinating</strong> — this process holds its own ' +
            'copy and will not see another\'s writes until it restarts. ' +
            (info.replication.supported
              ? 'Set <code>persistence.coordinate</code> to change that.'
              : 'The ' + this.esc(info.mode) + ' store cannot coordinate: a ' +
                'change log needs a transaction and a sequence, and that ' +
                'store writes whole files.')],
      ['When', off ? '—'
        : info.writeDelayMs === 0
          ? 'immediately — every change made while handling one request ' +
            'commits as one transaction the moment that request is done, ' +
            'plus a final flush on SIGTERM and SIGINT'
          : this.esc(String(info.writeDelayMs)) + 'ms after a change, so a ' +
            'burst costs one file write, plus a final flush on SIGTERM and ' +
            'SIGINT. A <code>kill -9</code> cannot be trapped and would lose ' +
            'up to that much.'],
      ['Health', off ? '—'
        : info.lastError
          ? '<strong>THE LAST WRITE FAILED</strong> — ' +
            this.esc(info.lastError) +
            '. This service is unaffected and is still answering everything ' +
            'out of memory. The next change will recompute the same ' +
            'difference and try again, so nothing has been lost yet.'
          : 'writing normally'],
      ['Written so far', off ? '—'
        : this.esc(String(info.writes)) + ' flush(es), ' +
          this.esc(String(info.failures)) +
          ' failure(s), ' + this.esc(String(info.entriesTracked)) + ' ' +
          'entry/entries across ' + this.esc(String(info.realmsTracked)) + ' ' +
            'realm(s). Last write ' +
          (info.lastWriteAt ? this.esc(info.lastWriteAt) : 'not yet') +
          (info.pending ? '; a change is waiting to be written.' : '.')],
      ['Restored at startup', off ? '—'
        : info.restoredAt
          ? this.esc(String(info.restored.entries)) + ' directory ' +
            'entry/entries, ' +
            this.esc(String(info.restored.realms)) + ' trust realm(s) and ' +
            this.esc(String(info.restored.overrides)) + ' setting ' +
              'override(s), at ' +
            this.esc(info.restoredAt) + '. Zero across the board means this ' +
            'was the first run against an empty store, which then had the ' +
            'seeded directory written into it.'
          : 'nothing was restored'],
      ['Coordination', '<strong>NO.</strong> ' + this.esc(info.note)]
    ];

    const html = '<h2>Right now</h2>' +
      (off
        ? this.note('Nothing on this page is in force: ' +
                    '<code>persistence.mode</code> is <code>memory</code>, ' +
                    'so this service is holding everything in memory and ' +
                    'will lose all of it when the process ends — which is ' +
                    'exactly what it has always done. The settings below ' +
                    'change that, and all but one of them are restart-only, ' +
                    'because the store is opened and READ before the ' +
                    'listener binds.')
        : (info.lastError
            ? this.warn('<strong>The persistent store is not currently ' +
                        'accepting writes.</strong> Everything below still ' +
                        'describes what this service is trying to do; the ' +
                        'Health row says what went wrong. Nothing has been ' +
                        'lost — see the note above about a failed write.')
            : '')) +
      '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
      rows.map(function (row) {
        return '<tr><th>' + self.esc(row[0]) + '</th><td>' + row[1] +
               '</td></tr>';
      }).join('') +
      '</table>';

    log.debug("Leaving AdminConsole.persistenceStatusBlock(). mode=" +
              info.mode);
    return { html: html, json: info };
  }

  // ===========================================================================
  // /admin/cluster's STATUS BLOCK (2026-09-14, #46).
  //
  // The `status` member of a row in PROTOCOL_SETTINGS_PAGES, for the reason
  // `/admin/persistence` has one: `cluster.mode` SET and a cluster WORKING are
  // two facts. It computes nothing — `cluster.status()` and
  // `cluster.snapshot()` are that module's own account of itself — and it draws
  // four answers: what this node is, who else is a member and who holds which
  // lease, what active-active mode is still waiting for, and which secrets
  // every node shares.
  //
  // **THE NODE AND LEASE TABLES ARE A SNAPSHOT**, read on every heartbeat by a
  // front process and at most once a heartbeat by the process drawing this
  // page, and the page says how old it is. A synchronous page cannot wait for a
  // query, and a page that silently showed an hour-old membership would be
  // worse than one that says "as of two seconds ago".
  // ===========================================================================
  clusterStatusBlock() {
    const { log, cluster, clusterSecrets, clusterBarrier } = this.deps;
    const consoleSelf = this;
    log.debug("Entering AdminConsole.clusterStatusBlock().");
    const self = cluster.status();
    const snap = cluster.snapshot();
    const state = snap.state;
    const secrets = clusterSecrets.describe();
    const barrier = clusterBarrier.report();
    const off = self.mode === 'off';
    const nameOf = {};
    ((state && state.nodes) || []).forEach(function (node) {
      nameOf[node.nodeId] = node.name;
    });
    // A TIME AGAINST THE DATABASE'S CLOCK, never this process's. Tenths of a
    // second up to two minutes, because every lifetime and every heartbeat on
    // this page is seconds long and a tenth is the difference between a node
    // that is late and one that is dead; past two minutes it is a duration,
    // because "7200s ago" is a number a reader has to divide (2026-09-17, the
    // roster — a node that left hours ago is on this page now).
    const ago = function (at) {
      if (!state || !at) {
        return '—';
      }
      const ms = state.now - at;
      const size = Math.abs(ms);
      const much = size >= 120000
        ? consoleSelf.durationText(size)
        : String(Math.round(size / 100) / 10) + 's';
      return ms >= 0 ? consoleSelf.esc(much) + ' ago'
                     : 'in ' + consoleSelf.esc(much);
    };

    const rows = [
      ['Mode',
       '<strong>' + this.esc(self.mode) + '</strong> — ' + this.esc(self.why) +
        (self.refused ? '. ' + this.warn(this.esc(self.refused)) : '')],
      ['This node', off ? 'not a member of anything'
        : '<code>' + this.esc(self.nodeId) + '</code> (' + this.esc(self.name) +
          '), ' +
          this.esc(self.role === 'worker' ? 'a request worker of that node'
                                          : 'the front process') +
          (self.joinedAt ? ', joined ' + this.esc(self.joinedAt) : '')],
      ['Heartbeat', off ? '—'
        : 'every ' + this.esc(String(self.heartbeatMs)) + 'ms, lifetime ' +
          this.esc(String(self.ttlMs)) + 'ms by the database clock' +
          (self.msSinceRenewal !== null
            ? '; last renewed ' + this.esc(String(self.msSinceRenewal)) +
              'ms ago'
            : '') +
          (self.heartbeatFailures
            ? '. <strong>' + this.esc(String(self.heartbeatFailures)) +
              ' consecutive failure(s)</strong>: ' +
              this.esc(String(self.lastHeartbeatError)) +
              '. A node that cannot renew within its lifetime exits.'
            : '')],
      ['Leases held here', off || !self.leases.length ? 'none'
        : self.leases.map(function (lease) {
          return '<code>' + consoleSelf.esc(lease.name) + '</code> at token ' +
                 consoleSelf.esc(String(lease.token));
        }).join(', ')],
      ['Read barrier', barrier.active
        ? this.esc(String(barrier.requests)) + ' request(s) waited a mean ' +
          this.esc(String(Math.round(barrier.meanWaitMs * 10) / 10)) + 'ms ' +
          'to catch up; ' + this.esc(String(barrier.gaveUp)) + ' were served ' +
          'before catching up; ' + this.esc(String(barrier.heldForCommit)) +
          ' response(s) were held a mean ' +
          this.esc(String(Math.round(barrier.meanHoldMs * 10) / 10)) + 'ms ' +
          'for their writes to commit' + (barrier.commitFailures
            ? ', <strong>' + this.esc(String(barrier.commitFailures)) +
              ' of which failed</strong>' : '')
        : 'off — only active-active nodes wait for each other\'s writes']
    ];

    // =======================================================================
    // THE ROSTER (2026-09-17): WHO IS RUNNING, AND WHEN ANYTHING LAST HEARD
    // FROM THEM.
    //
    // The first question an operator brings to this page is whether the other
    // container is up and when it was last seen, and until this it was
    // answered by one table that mixed the live members in with every row the
    // store has kept and left out everything the membership row's `info`
    // carries. So the LIVE members come first, one row each, with what only
    // that node can say about itself — where it runs, how long its process has
    // been up, how many processes answer requests there, whether its event
    // loop has stalled — and the rows that have left or expired are folded
    // underneath rather than thrown away, because #46's own failure mode is a
    // node whose row expired while its process kept running and the operator
    // needs to see that it was here.
    //
    // **EVERY TIME HERE IS THE DATABASE'S CLOCK** (`state.now`), never this
    // process's: two containers' clocks differ, and `cluster.js` measures a
    // lease against the store's clock for exactly that reason. `info.uptimeMs`
    // is the one exception and is labelled as the node's own, because no other
    // clock can state how long a process has been running.
    //
    // **AND IT IS DRAWN EVEN WHEN THIS PROCESS IS NOT CLUSTERED.** A section
    // that disappears in `off` mode reads as a page that has not loaded; one
    // that says there is no membership to list answers the question.
    // =======================================================================
    const nodes = state ? state.nodes : [];
    const now = state ? state.now : 0;
    const isLive = function (node) {
      return !node.leftAt && node.expiresAt > now;
    };
    const live = nodes.filter(isLive);
    const gone = nodes.filter(function (node) {
      return !isLive(node);
    });

    // WHICH LEASES EACH NODE STILL HOLDS, by holder. A lapsed row is left out:
    // a released lease is expired and never deleted (the fencing token must
    // never go back to 1), so a page that listed every row would say a node
    // holds what it gave up.
    const leasesOf = {};
    ((state && state.leases) || []).forEach(function (lease) {
      if (lease.expiresAt <= now) {
        return;
      }
      if (!leasesOf[lease.holder]) {
        leasesOf[lease.holder] = [];
      }
      leasesOf[lease.holder].push(lease);
    });

    // WHAT A LIVE NODE IS DOING is not the same question as whether it is
    // alive, and the difference is the whole of active-passive mode: one
    // member holds the service lease and serves, and every other one is a
    // standby that has restored nothing and bound nothing. The LEASE TABLE is
    // the only thing that says which, so it is read rather than guessed from
    // the node's own mode.
    const roleOf = function (node) {
      const mine = leasesOf[node.nodeId] || [];
      const serving = node.mode === 'active-active' || mine.some(
        function (lease) {
          return lease.name === cluster.SERVICE_LEASE;
        });
      return serving ? { label: 'serving', cls: 'state-valid' }
                     : { label: 'standby', cls: 'state-none' };
    };

    // WHERE THE CONTAINER IS, out of the membership row's `info` — the only
    // channel a node has to tell the others anything about itself, rewritten
    // on its join and on every heartbeat (`cluster/cluster.js`, `nodeInfo()`).
    // A row written by an older build simply has fewer members in it, so
    // nothing below assumes any one of them is there.
    const whereOf = function (node) {
      const info = node.info || {};
      const parts = [];
      if (info.host) {
        parts.push('<code>' + consoleSelf.esc(String(info.host)) +
                   (info.port ? ':' + consoleSelf.esc(String(info.port)) : '') +
                   '</code>');
      }
      if (info.pid) {
        parts.push('pid ' + consoleSelf.esc(String(info.pid)));
      }
      if (info.workers) {
        parts.push(consoleSelf.esc(String(info.workers)) +
                   ' request worker(s)');
      }
      return parts.length ? parts.join('<br>') : '—';
    };

    // HOW LONG THE PROCESS HAS BEEN UP, which the node states itself, falling
    // back to how long its membership row has existed. They are different
    // facts and the second is the weaker one — a row is written after the
    // process starts and survives a restart that reuses no node id — so it is
    // only used where the node said nothing.
    const upOf = function (node) {
      const info = node.info || {};
      if (info.uptimeMs) {
        return consoleSelf.esc(consoleSelf.durationText(info.uptimeMs));
      }
      return 'joined ' + ago(node.startedAt);
    };

    // WHEN ANYTHING LAST HEARD FROM IT, and — where the node reported one —
    // the stall that explains a late heartbeat. A blocked event loop cannot
    // heartbeat, so the last stall a node saw is the first thing to look at
    // when its row is close to expiring, and it is the reason `cluster.js`
    // logs `STS-CLUSTER-0025` at all. It is the MOST RECENT stall rather than
    // the worst one, which is what `status()` reports as `lastStallMs` too.
    const seenOf = function (node) {
      const info = node.info || {};
      const stall = Number(info.lastStallMs) || 0;
      return ago(node.heartbeatAt) + (stall
        ? '<br><strong>last stall ' +
          consoleSelf.esc(String(Math.round(stall / 100) / 10)) + 's</strong>'
        : '');
    };

    const leasesCell = function (node) {
      const mine = leasesOf[node.nodeId] || [];
      if (!mine.length) {
        return 'none';
      }
      return mine.map(function (lease) {
        return '<code>' + consoleSelf.esc(lease.name) + '</code> at token ' +
               consoleSelf.esc(String(lease.token));
      }).join('<br>');
    };

    const agreesCell = function (node) {
      if (node.agrees === null) {
        return 'not known here';
      }
      return node.agrees ? 'yes'
        : '<strong>NO</strong> — two nodes with different values answer ' +
          'the same request two ways';
    };

    const liveRows = live.map(function (node) {
      const role = roleOf(node);
      return '<tr><td><strong>' + consoleSelf.esc(node.name) +
        '</strong>' + (node.nodeId === self.nodeId
          ? ' <em>(this node)</em>' : '') +
        '<br><code>' + consoleSelf.esc(node.nodeId) + '</code></td><td>' +
        whereOf(node) + '</td><td><span class="' + role.cls + '">' +
        consoleSelf.esc(role.label) + '</span><br>' +
        consoleSelf.esc(node.mode) + '</td><td>' +
        consoleSelf.esc(node.version || 'unknown') + '</td><td>' +
        upOf(node) + '</td><td>' + seenOf(node) + '</td><td>' +
        ago(node.expiresAt) + '</td><td>' + leasesCell(node) + '</td><td>' +
        agreesCell(node) + '</td></tr>';
    }).join('');

    const goneRows = gone.map(function (node) {
      return '<tr><td><strong>' + consoleSelf.esc(node.name) +
        '</strong><br><code>' + consoleSelf.esc(node.nodeId) +
        '</code></td><td>' + whereOf(node) + '</td><td>' +
        consoleSelf.esc(node.mode) + '</td><td>' +
        consoleSelf.esc(node.version || 'unknown') + '</td><td>' +
        ago(node.startedAt) + '</td><td>' + ago(node.heartbeatAt) +
        '</td><td>' + (node.leftAt
          ? 'left cleanly ' + ago(node.leftAt)
          : '<strong>expired</strong> ' + ago(node.expiresAt)) +
        '</td></tr>';
    }).join('');

    const goneTable = !gone.length ? ''
      : '<details class="fold"><summary>' + this.esc(String(gone.length)) +
        ' node(s) that have left or expired</summary><div class="foldbody">' +
        '<p>A node that stopped cleanly released its leases on the way out, ' +
        'so another member took them over within one heartbeat. A node that ' +
        'EXPIRED did not, and its leases waited out their lifetime — and it ' +
        'is dead for good either way: the heartbeat refuses to renew an ' +
        'expired row, so its process exits rather than come back quietly.</p>' +
        this.wideTable('Nodes that have left or expired',
          '<table><tr><th>Node</th><th>Where</th><th>Mode</th>' +
          '<th>Version</th><th>Started</th><th>Last seen</th><th>Ended</th>' +
          '</tr>' + goneRows + '</table>') + '</div></details>';

    // TWO DIFFERENT REASONS FOR NO LIST, AND THEY MUST NOT SHARE A SENTENCE
    // (2026-09-18). `off` is a fact about the configuration. No `state` on a
    // clustered process is a fact about THIS PROCESS: it has not read the
    // member list yet — a request worker reads it when it attaches and then
    // at most once a heartbeat, and a page drawn in the gap has nothing to
    // show. The first version said "cluster.mode resolved to off" for both,
    // and on testidp, where `/admin` is served by a worker, told somebody
    // looking at a healthy three-node cluster that it was not clustered.
    const noList = off
      ? this.note('There is no membership to list: <code>cluster.mode</code> ' +
                  'resolved to <code>off</code>, so this process is not a ' +
                  'node of anything and writes nothing another node could ' +
                  'fence. What runs here is one container, which is correct ' +
                  'for one container and WRONG for several against one store.')
      : this.note('<strong>This process has not read the member list ' +
                  'yet.</strong> The node is clustered (' +
                  this.esc(self.mode) + '); the ' +
                  (self.role === 'worker' ? 'request worker' : 'process') +
                  ' drawing this page reads the membership from the store ' +
                  'when it starts and then at most once a heartbeat, and ' +
                  'this page was drawn before the first read came back. ' +
                  'Reload it.');
    const nodeTable = '<h2>Members</h2>' + (off || !state
      ? noList
      : '<div class="tiles">' +
        this.tile(live.length, 'running') +
        this.tile(Object.keys(leasesOf).length, 'nodes holding a lease') +
        this.tile(gone.length, 'left or expired') +
        '</div><p>As of ' +
        this.esc(String(Math.round((snap.ageMs || 0) / 100) / 10)) +
        's ago, read at most one heartbeat apart by whichever process drew ' +
        'this page. <strong>Every time below is the database\'s ' +
        'clock</strong>, which a lifetime is measured against — two ' +
        'containers\' clocks differ, and a lease that expires by ' +
        'whoever-is-asking\'s ' +
        'clock is a lease two nodes both hold. <em>Up</em> is the exception: ' +
        'it is the node\'s own process uptime, which no other clock can ' +
        'state.</p>' +
        this.wideTable('Running cluster members',
          '<table><tr><th>Node</th><th>Where</th><th>State</th>' +
          '<th>Version</th><th>Up</th><th>Last seen</th><th>Expires</th>' +
          '<th>Leases</th><th>Settings agree</th></tr>' +
          (liveRows || '<tr><td colspan="9">no live member rows — this ' +
           'node\'s own row arrives on its first heartbeat</td></tr>') +
          '</table>') + goneTable +
        '<h2>Leases</h2><p>A lease is a named role ONE node holds, with a ' +
        'fencing token that goes up every time it changes hands. A released ' +
        'lease is expired and never deleted, so the token never goes back ' +
        'to 1.</p><table><tr><th>Lease</th><th>Holder</th>' +
        '<th>Token</th><th>Expires</th></tr>' +
        (state.leases.length ? state.leases.map(function (lease) {
          const lapsed = lease.expiresAt <= now;
          return '<tr><td><code>' + consoleSelf.esc(lease.name) +
                 '</code></td><td>' +
            consoleSelf.esc(nameOf[lease.holder] || lease.holder) +
            '</td><td>' +
            consoleSelf.esc(String(lease.token)) + '</td><td>' +
            (lapsed ? 'released or expired' : ago(lease.expiresAt)) +
            '</td></tr>';
        }).join('') : '<tr><td colspan="4">none</td></tr>') + '</table>');

    const caps = self.capabilities;
    const capabilityTable = '<h2>What active-active depends on</h2><p>' +
      (caps.ready
        ? 'Every capability is provided' + (caps.acceptedMissing.length
          ? ' or accepted as missing (<code>' +
            this.esc(caps.acceptedMissing.join(', ')) + '</code>)' : '') + '.'
        : '<strong>' + this.esc(String(caps.missing.length)) + ' of ' +
          this.esc(String(caps.rows.length)) + ' are missing</strong>, so ' +
          '<code>cluster.mode=active-active</code> refuses to start. Each is ' +
          'a way two nodes give different answers, described under the ' +
          'section of issue #46 named beside it.') +
      (caps.unknownAccepted.length
        ? ' <strong>cluster.acceptMissingCapabilities names ids this build ' +
          'does not know:</strong> <code>' +
          this.esc(caps.unknownAccepted.join(', ')) + '</code>.' : '') +
      '</p><table><tr><th>Capability</th><th>#46</th><th>State</th>' +
      '<th>What</th><th>Where</th></tr>' +
      caps.rows.map(function (row) {
        return '<tr><td><code>' + consoleSelf.esc(row.id) + '</code></td><td>' +
          consoleSelf.esc(row.section) + '</td><td>' +
          (row.provided ? 'provided'
            : (row.accepted ? '<strong>accepted as missing</strong>'
                            : '<strong>missing</strong>')) + '</td><td>' +
          consoleSelf.esc(row.what) + '</td><td><code>' +
          consoleSelf.esc(row.by) + '</code></td></tr>';
      }).join('') + '</table>';

    const secretTable = '<h2>Shared secrets</h2><table><tr><th>Secret</th>' +
      '<th>Where this process\'s value came from</th><th>What</th></tr>' +
      secrets.secrets.map(function (one) {
        return '<tr><td><code>' + consoleSelf.esc(one.name) +
               '</code></td><td>' +
          consoleSelf.esc(one.source) + '</td><td>' +
          consoleSelf.esc(one.what) + '</td></tr>';
      }).join('') + '</table>';

    const html = '<h2>Right now</h2>' +
      (off ? this.note('This process is not clustered: ' +
                       '<code>cluster.mode</code> resolved to ' +
                       '<code>off</code>. That is correct for one container ' +
                       'and WRONG for several against one store — see the ' +
                       'first paragraph above.') : '') +
      '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
      rows.map(function (row) {
        return '<tr><th>' + consoleSelf.esc(row[0]) + '</th><td>' + row[1] +
               '</td></tr>';
      }).join('') + '</table>' + nodeTable + capabilityTable + secretTable;

    log.debug("Leaving AdminConsole.clusterStatusBlock(). mode=" + self.mode);
    return {
      html: html,
      json: { self: self, snapshotAgeMs: snap.ageMs,
              nodes: state ? state.nodes : [],
              leases: state ? state.leases : [],
              databaseNow: state ? state.now : null,
              secrets: secrets, barrier: barrier }
    };
  }

  // ===========================================================================
  // THE TWO SECOND-FACTOR PAGES' STATUS BLOCKS (2026-09-10).
  //
  // Each is the `status` member of a row in PROTOCOL_SETTINGS_PAGES below — the
  // same optional function `/admin/persistence` has, and for the same reason it
  // was invented: a settings page describes what this service is CONFIGURED to
  // do, and these two also have to say what the MECHANISM is, which is a
  // different fact. Which digests exist as against which one is in use; which
  // COSE algorithms this relying party can verify as against which two it is
  // offering. **Every table below is READ FROM THE MODULE THAT PERFORMS THE
  // ALGORITHM** — `common/totp.ts`'s `report()` and
  // `authn/webauthn_policy.ts`'s — which is the rule `/admin/crypto-metadata`
  // is built on, one layer down. A page that wrote the list out would describe
  // something this service does not do the first time one was added.
  // ===========================================================================
  totpMechanismBlock() {
    const { log, totp } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.totpMechanismBlock().");
    const info = totp.report();
    const algorithmRows = info.algorithms.map(function (alg) {
      return '<tr><td><code>' + self.esc(alg.name) + '</code></td>' +
        '<td>' + (alg.inUse
          ? '<span class="state-valid">in use</span>'
          : '<span class="state-none">available</span>') + '</td>' +
        '<td>' + self.esc(alg.note || '') + '</td></tr>';
    }).join('');
    const html = '<h2>The mechanism</h2>' +
      (info.offered
        ? ''
        : this.warn('<strong><code>totp.enabled</code> is off</strong>, so ' +
          'nobody new can enrol an authenticator app. <strong>It does not ' +
          'disable a secret somebody already holds</strong> — that account ' +
          'is still configured for two factors and the sign-in screen still ' +
          'asks for the code, because a switch that silently downgraded it ' +
          'would be a security control whose off position does something ' +
          'other than what it says. Clearing an existing enrolment is on ' +
          'that person\'s row under <a href="/admin/users">Users</a>.')) +
      '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
      '<tr><th>Code</th><td>' + this.esc(String(info.digits)) + ' digits, a ' +
        'new one every ' + this.esc(String(info.period)) + ' ' +
      'seconds</td></tr><tr><th>Skew forgiven</th><td>' +
      this.esc(String(info.window)) + ' step(s) ' +
        'either side, so a code lives about ' +
        this.esc(String(info.period * (1 + 2 * info.window))) + ' seconds. ' +
        '<strong>This one applies to everybody</strong>, existing enrolments ' +
        'included.</td></tr>' +
      '<tr><th>Shared secret</th><td>' + this.esc(String(info.secretBits)) +
      ' bits, ' +
        this.esc(info.encoding) + '</td></tr>' +
      '<tr><th>Truncation</th><td>' + this.esc(info.truncation) + '</td></tr>' +
      '</table>' +
      '<h3>Digests</h3>' +
      '<table><tr><th>Algorithm</th><th>State</th><th>Note</th></tr>' +
      algorithmRows + '</table>';
    log.debug("Leaving AdminConsole.totpMechanismBlock(). offered=" +
              info.offered);
    return { html: html, json: info };
  }

  webauthnMechanismBlock() {
    const { log, webauthnPolicy } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.webauthnMechanismBlock().");
    const info = webauthnPolicy.report();
    const algorithmRows = info.algorithms.map(function (alg) {
      return '<tr><td><code>' + self.esc(alg.name) + '</code></td>' +
        '<td class="num"><code>' + self.esc(String(alg.coseAlg)) +
        '</code></td><td>' + (alg.offered
          ? '<span class="state-valid">offered</span>'
          : '<span class="state-none">verifiable, not offered</span>') +
        '</td></tr>';
    }).join('');
    const html = '<h2>The ceremony</h2>' +
      (info.offered
        ? ''
        : this.warn('<strong><code>webauthn.enabled</code> is off</strong>, ' +
          'so no new security key can be enrolled here. <strong>It does not ' +
          'disable a key somebody already holds</strong>, for ' +
          '<code>totp.enabled</code>\'s reason — and there is a sharper ' +
          'edge: somebody whose only credential is a <code>primary</code> ' +
          'key would be locked out of their own account by this switch. ' +
          'Removing a key is on that person\'s row under <a ' +
          'href="/admin/users">Users</a>.')) +
      '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
      '<tr><th>RP ID</th><td>' +
        (info.rpId
          ? '<code>' + this.esc(info.rpId) + '</code> — configured. It is ' +
            'used only where it is a <strong>registrable domain ' +
            'suffix</strong> of the host this service was reached on; ' +
            'anything else is refused here, with the reason in the log, ' +
            'because a browser would refuse it with an error ' +
            'indistinguishable from a hardware failure.'
          : 'the host this service was reached on. That is the default and ' +
            'it is what a credential is bound to.') + '</td></tr>' +
      '<tr><th>RP name</th><td><code>' + this.esc(info.rpName) + '</code> — ' +
        'what a browser shows while somebody decides. It has no security ' +
        'meaning: WebAuthn binds a credential to the RP ID and to nothing ' +
        'else.</td></tr><tr><th>User ' +
        'verification</th><td><code>' + this.esc(info.userVerification) +
        '</code> — ' + (info.userVerificationEnforced
          ? '<strong>requested AND CHECKED</strong>. The UV flag is inside ' +
            'the bytes the authenticator signed, so an authenticator that ' +
            'did not verify the person is refused rather than quietly ' +
            'accepted. It is the only ceremony option on this page this ' +
            'service can check, because it is the only one anything signed ' +
            'says anything about.'
          : 'requested only. Nothing is refused on it.') + '</td></tr>' +
      '<tr><th>Attestation</th><td><code>' + this.esc(info.attestation) +
      '</code>, ' +
        'conveyance requested. <strong>NO ATTESTATION STATEMENT IS VERIFIED ' +
        'HERE whatever is asked for</strong> — there is no metadata service, ' +
        'no vendor trust anchor and no model allow-list — so the statement ' +
        'is parsed, reported and believed. Formats recognised: ' +
        this.esc(info.attestationFormats.join(', ')) + '.</td></tr>' +
      '<tr><th>Timeout</th><td>' + this.esc(String(info.timeoutMs)) + 'ms, ' +
        'and it is a HINT: the specification lets a client clamp it and ' +
        'browsers do. The pending step this service holds expires on its own ' +
        'five-minute clock regardless.</td></tr><tr><th>Signature ' +
        'counter</th><td>' + this.esc(info.signatureCounter) +
        '</td></tr>' +
      '</table>' +
      '<h3>CTAP</h3>' +
      this.note('These three are what a browser translates into what it asks ' +
      'the AUTHENTICATOR for. <strong>They are requests and not ' +
      'checks</strong>: nothing signed says what the browser was asked for, ' +
      'so a check here would be a comparison against a value this service ' +
      'itself supplied. What this service does instead is RECORD what came ' +
      'back, beside the key.') +
      '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
      '<tr><th>Attachment</th><td>' +
        (info.authenticatorAttachment === 'any'
          ? 'no preference sent, so any authenticator may answer — the ' +
            'member is omitted from the options rather than sent as a wide ' +
            'value, because the dictionary has no value meaning ' +
            '&ldquo;any&rdquo;'
          : '<code>' + this.esc(info.authenticatorAttachment) + '</code> ' +
            'only. The browser filters; this service does not refuse a ' +
            'credential whose attachment turned out to be the other one.') +
            '</td></tr>' +
      '<tr><th>Discoverable credential</th><td><code>' +
      this.esc(info.residentKey) +
        '</code> — a CTAP2 <em>resident key</em>, stored on the ' +
        'authenticator itself. That is what a passkey is and what a ' +
        'usernameless sign-in needs. <strong>This service offers no ' +
        'usernameless flow</strong>, so <code>required</code> consumes one ' +
        'of the small number of slots a roaming authenticator has — which ' +
        'cannot always be freed again — and buys nothing here beyond seeing ' +
        'what a client does when the browser prompts differently.</td></tr>' +
      '<tr><th>credProps</th><td>' + (info.credProps
        ? 'asked for. It is the only way to find out whether a ' +
          '<code>preferred</code> ceremony actually produced a discoverable ' +
          'credential — nothing in the attestation says. The answer is ' +
          'recorded beside the key and decides nothing.'
        : 'not asked for, so nothing here knows whether an enrolled ' +
          'credential is discoverable.') + '</td></tr></table><h3>What a key ' +
      'may BE here</h3>' +
      this.note('The three rows below are <strong>not WebAuthn</strong>. ' +
      'They are what THIS service will do with a key once the ceremony is ' +
      'over, decided here rather than by any specification — and all three ' +
      'refuse an <strong>enrolment</strong> and never an authentication. A ' +
      'key already on somebody\'s entry goes on working when the role that ' +
      'produced it is switched off.') +
      '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
      '<tr><th>Primary (passwordless)</th><td>' + (info.primaryAllowed
        ? '<span class="state-valid">allowed</span> — a key may be the only ' +
          'credential on an account. The session then records <code>amr ' +
          '["hwk"]</code> and <code>acr "1"</code>.'
        : '<span class="state-none">not allowed</span> — the passwordless ' +
          'box is off the sign-in screen and the <code>primary</code> choice ' +
          'is off the portal.') + '</td></tr>' +
      '<tr><th>Second factor</th><td>' + (info.mfaAllowed
        ? '<span class="state-valid">allowed</span> — beside a password. The ' +
          'session then records <code>amr ["pwd","hwk"]</code> and <code>acr ' +
          '"mfa"</code>.'
        : '<span class="state-none">not allowed</span> — the other second ' +
          'factor is <a href="/admin/totp">an authenticator ' +
          'app</a>.') + '</td></tr>' +
      '<tr><th>Keys per person</th><td>' +
      this.esc(String(info.maxKeysPerPerson)) +
        '. Several is the ordinary case and the specification expects it: an ' +
        'assertion NAMES the credential that produced it, so there is none ' +
        'of the ambiguity two shared secrets would have.</td></tr>' +
      '</table>' +
      '<h3>Algorithms</h3>' +
      this.note('<code>pubKeyCredParams</code> is built from the OFFERED ' +
      'rows, in order. The list is filtered against what ' +
      '<code>authn/webauthn.js</code> can actually verify — a name outside ' +
      'that table is dropped with a warning rather than sent, because ' +
      'offering an algorithm this service cannot check produces a credential ' +
      'that enrols perfectly and then fails every assertion it is ever used ' +
      'for, at sign-in rather than at enrolment.') +
      '<table><tr><th>Algorithm</th><th ' +
      'class="num">COSE</th><th>State</th></tr>' +
      algorithmRows + '</table>' +
      '<p class="sub">Curves: ' +
      info.curves.map(function (curve) {
        return '<code>' + self.esc(curve.name) + '</code>';
      }).join(', ') + '.</p>';
    log.debug("Leaving AdminConsole.webauthnMechanismBlock(). offered=" +
              info.offered);
    return { html: html, json: info };
  }

  // ---------------------------------------------------------------------------
  // THE RECOVERY CODE MECHANISM, for `/admin/backup-codes` (2026-09-10).
  //
  // Read from `common/backup_codes.ts` — the module that generates and compares
  // a code — rather than written down here, which is the design every `status`
  // block on this page follows: the table lives with the code that performs the
  // thing, so this cannot describe something the service does not do.
  //
  // **IT IS THE ONE BLOCK HERE WITH NO SPECIFICATION COLUMN**, because there is
  // no specification. Everything in it is a decision this service made, and the
  // page says which decision and why rather than citing a document.
  // ---------------------------------------------------------------------------
  backupCodesMechanismBlock() {
    const { log, backupCodes } = this.deps;
    log.debug("Entering AdminConsole.backupCodesMechanismBlock().");
    const info = backupCodes.report();
    const html =
      '<h3>What a code is</h3>' +
      '<table class="key"><tr><th>What</th><th>Answer</th></tr>' +
      '<tr><th>Offered</th><td>' + (info.offered
        ? '<span class="state-valid">yes</span> — a set is issued the first ' +
          'time somebody enrols a second factor.'
        : '<span class="state-none">no</span> — no NEW set will be issued. ' +
          '<strong>A set already issued goes on working</strong>, which is ' +
          'the contract <code>totp.enabled</code> and ' +
          '<code>webauthn.enabled</code> both keep: a switch that took away ' +
          'the only way back into an account whose phone is lost would be ' +
          'the worst one on this console.') +
        '</td></tr>' +
      '<tr><th>Set</th><td>' + this.esc(String(info.count)) + ' codes of ' +
        this.esc(String(info.length)) + ' characters, printed in groups of ' +
        this.esc(String(info.groupSize || 0)) + '.</td></tr>' +
      '<tr><th>Strength</th><td><strong>' + this.esc(String(info.bitsPerCode)) +
        ' bits</strong> per code, out of an alphabet of ' +
        this.esc(String(info.alphabetSize)) + '. That is the number that ' +
        'matters rather than the length, and it is what makes the rate limit ' +
        'on the recovery screen a belt rather than the whole ' +
      'trousers.</td></tr><tr><th>Alphabet</th><td><code>' +
      this.esc(info.alphabet) + '</code> — the ' +
        'same thirty-two characters RFC 4648 base32 uses, and <strong>not ' +
        'shared with <a href="/admin/totp">TOTP</a></strong>. That one is ' +
        'base32 because the <code>otpauth</code> URI says so; this one is ' +
        'these characters because <strong>no pair of them is ' +
        'confusable</strong> — no <code>0</code> beside <code>O</code>, no ' +
        '<code>1</code> beside <code>I</code> — and a recovery code is the ' +
        'one credential here that somebody writes on paper and types back ' +
        'months later.</td></tr><tr><th>Generated</th><td>' +
        this.esc(info.source) +
      '</td></tr><tr><th>Compared</th><td>' + this.esc(info.comparison) + ' ' +
        'Every code in the set is compared even after a match, so the time ' +
        'taken does not depend on WHICH one matched.</td></tr><tr><th>At ' +
        'rest</th><td>' + this.esc(info.atRest) + '</td></tr>' +
      '</table>' +
      '<h3>What this service will not do with them</h3>' +
      this.note('<strong>There is no control anywhere that issues a set on ' +
      'request.</strong> Not on this console, not on ' +
      '<code>/admin-api</code>, not on <code>/portal</code>. A set is ' +
      'created by the ACT of enrolling a second factor and by nothing else, ' +
      'because a way back that somebody has to remember to ask for produces ' +
      'exactly the population it exists to protect — the people who did not ' +
      'ask are the people who will need it.') +
      this.note('<strong>A set is issued ONCE and is never topped ' +
      'up.</strong> Enrolling a different second factor does not reissue: ' +
      'somebody who printed a list in March and replaced their authenticator ' +
      'app in June would otherwise be holding a page of strings that had ' +
      'stopped working with nothing having said so. The only route to a ' +
      'second set is an operator\'s Clear on that person\'s row under <a ' +
      'href="/admin/users">Users</a>, after which the next enrolment issues ' +
      'one.') +
      this.note('<strong>This console never shows a code.</strong> The ' +
      'person reads their own set back on <code>/portal/mfa</code> and ' +
      'nowhere else. Showing them here would hand a working second factor to ' +
      'whoever holds Admin Read, which is the same door this console already ' +
      'refuses to open for an authenticator enrolment.') +
      this.note('<strong>A recovery code is never a FIRST factor and never ' +
      'the factor a sign-in asks for.</strong> ' +
      '<code>credentials.mechanismsFor().secondFactor</code> answers ' +
      '<code>webauthn</code> or <code>totp</code> and never this; the ' +
      'recovery screen is reachable only as a way OUT of one of those two, ' +
      'with a step id the person already holds.');
    log.debug("Leaving AdminConsole.backupCodesMechanismBlock(). offered=" +
              info.offered);
    return { html: html, json: info };
  }

  // What one of these pages answers over `?format=json`, and what the
  // management API mirrors. The settings are `configSettingsJson()` — the same
  // described rows the form is drawn from — and the rest is what the page SAYS,
  // in the text it says it in: a caller driving this console without a browser
  // should be able to read the caveats too, since on these pages the caveats
  // are most of the content.
  protocolSettingsJson(row) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.protocolSettingsJson(). path=" + row.path);
    const json: Record<string, any> = {
      page: row.path,
      title: row.title,
      what: this.plainTextOf(row.lead),
      notes: (row.also || []).map(this.plainTextOf.bind(this)),
      links: (row.links || []).map(function (link) {
        return { href: link[0], what: link[1] };
      }),
      settings: this.configSettingsJson(row.path)
    };
    // ONE OPTIONAL MEMBER, invented for `/admin/persistence` and carried by
    // five rows now (persistence, cluster and the three second-factor mechanism
    // pages). A settings page describes what this service is CONFIGURED to do;
    // `/admin/persistence` also has to say what it is ACTUALLY doing right now
    // — which store is open, whether the last write worked, how much is in it —
    // because a persistence setting that is set and a persistence store that is
    // working are two different facts and the gap between them is the whole
    // failure mode. It is a function on the row rather than a second kind of
    // page, so the JSON and the HTML stay one thing. See
    // `persistenceStatusBlock()`.
    if (typeof row.status === 'function') {
      json.status = row.status().json;
    }
    log.debug("Leaving AdminConsole.protocolSettingsJson(). " +
              json.settings.settingCount +
              " setting(s).");
    return json;
  }

  // The page, written once. `configFormsFor()` is the whole of the second half;
  // everything above it is the row.
  protocolSettingsPage(req, row) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.protocolSettingsPage(). path=" + row.path);
    const inner = this.messagesOf(req) +
      this.note(row.lead) +
      (row.also || []).map(function (text) { return self.warn(text); })
        .join('') +
      // ABOVE the settings forms, deliberately: what the store is doing right
      // now is what somebody came to this page to find out, and the settings
      // that produced it are the answer to the follow-up question. See the
      // `status` member in protocolSettingsJson() above.
      (typeof row.status === 'function' ? row.status().html : '') +
      this.configFormsFor(row.path) +
      // A `<p class="sub">` AND NOT A `note()`, which is the rule bullet()
      // states for a list item that opens with a link, applied one helper
      // across. A row of links is longer than a line and note() would therefore
      // FOLD it — and the summary of that fold is a truncation of the first two
      // link texts, so the only controls in the row end up behind a summary
      // made of their own words. Every other link row in this console is a `<p
      // class="sub">` for the same reason.
      '<p class="sub">' + (row.links || []).map(function (link) {
        return '<a href="' + self.esc(link[0]) + '">' + self.esc(link[1]) +
               '</a>';
      }).concat(['<a href="' + this.esc(row.path) +
                 '?format=json">this page as ' +
                                               'JSON</a>',
                 '<a href="/admin/sts-metadata">every endpoint this service ' +
                 'registers</a>']).join(' &middot; ') + '</p>';
    log.debug("Leaving AdminConsole.protocolSettingsPage().");
    return inner;
  }

  // What `mgmt-api/admin_api.ts` calls for each of these. It takes the PATH
  // rather than an index or a title, because that is what SETTING_HOMES and NAV
  // are keyed by and it is what the API's own route already carries.
  protocolSettingsJsonFor(path) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.protocolSettingsJsonFor().");
    const row = PROTOCOL_SETTINGS_PAGES.filter(function (item) {
      return item.path === path;
    })[0];
    if (!row) {
      // A caller asking for a page this table does not have is a wiring mistake
      // in this repository rather than anything a request can cause, so it
      // throws rather than answering an empty object that would read as "this
      // family has no settings".
      throw new Error('No protocol settings page at ' + path + '.');
    }
    log.debug("Leaving AdminConsole.protocolSettingsJsonFor().");
    return this.protocolSettingsJson(row);
  }

  // ---------------------------------------------------------------------------
  // /admin/spiffe, /admin/spiffe/entries, /admin/spiffe/agents — THE SPIFFE
  // SECTION.
  //
  // Three pages rather than one, and the split is by what the reader is doing
  // rather than by what the data is:
  //
  //   /admin/spiffe           the TRUST DOMAIN — its authorities, the bundle,
  //                           the four gRPC listeners, the federated bundles.
  //                           The forms here rotate an authority and set or
  //                           remove a federated bundle.
  //   /admin/spiffe/entries   the REGISTRATION ENTRIES: a list with a filter and
  //                           paging, a drill-down per entry, and the forms
  //                           that create, change and delete one.
  //   /admin/spiffe/agents    the ATTESTED AGENTS: the same shape, and the forms
  //                           ban, unban and delete.
  //
  // **The second and third are separate sections rather than drill-downs**,
  // which is why each has its own NAV row and its own LIST_PARAMS whitelist. A
  // drill-down's section crumb points at the list it came from, and an entries
  // list hanging under /admin/spiffe would make the crumb point at a page that
  // does not hold that list — the exact defect rule 7a describes.
  //
  // **THIS PAGE DECIDES NOTHING.** Every form posts to an action function that
  // calls into `spiffe_registry.js` or `spiffe_ca.js` — the same functions the
  // SPIRE Server API's `BatchCreateEntry`, `BanAgent` and
  // `BatchSetFederatedBundle` call, and the same store an `ldapmodify` under
  // `ou=spiffe` writes to. Three doors, one store, which is the one-store rule
  // this service already applies to revocation, to the applications registry
  // and to SCIM.
  //
  // **AND IT SAYS, ON EVERY PAGE, THAT NOTHING IS ATTESTED.** A console that
  // listed registration entries beside the tokens page without saying so would
  // let somebody conclude that a selector on an entry restricts who can get
  // that identity. Nothing here restricts anything: any caller that reaches the
  // Workload API socket is handed every identity in the trust domain.
  // ---------------------------------------------------------------------------

  // The warning that goes at the top of all three, written once. It is the
  // SPIFFE analogue of the "a group here grants nothing" line on /admin/groups,
  // and it matters more, because what comes out of these pages is a credential
  // another service will believe. The banner every SPIFFE page carries. It is a
  // FUNCTION rather than a constant now, because half of what it says depends
  // on a setting that can be off: a fixed string would go on describing mutual
  // TLS on a port that had been bound plain, which is the silent disagreement
  // this repository keeps warning about.
  //
  // TWO PARAGRAPHS, and the split is the point — the two surfaces are
  // authenticated differently because their specifications say opposite things,
  // and a single sentence covering both was what made the old note wrong in one
  // direction as soon as one of them changed.
  spiffePostureNote() {
    const { log, spiffeAuth } = this.deps;
    log.debug("Entering AdminConsole.spiffePostureNote().");
    const enforced = spiffeAuth.authRequired();
    log.debug("Leaving AdminConsole.spiffePostureNote().");
    return this.warn('<strong>Nothing here is attested.</strong> A real ' +
      'SPIFFE agent reads the peer credentials of its socket — pid, uid, ' +
      'gid, and from those the executable, the container, the pod — and ' +
      'hands a workload only the identities those selectors match. Node ' +
      'cannot read them at all, so this service identifies a Workload API ' +
      'caller by the transport it arrived on, the endpoint it reached and ' +
      'its peer address, and nothing else. Those DO now decide which entries ' +
      'answer (<code>spiffe.attestWorkloads</code>), and they prove nothing ' +
      'about who is calling: anybody who can reach the socket can still get ' +
      'an identity. Node attestation is not: an agent below attested with a ' +
      'type its realm accepts and an attestor here verified, or it was ' +
      'refused.') +
      '<div class="' + (enforced ? 'note' : 'warn') + '">' +
      (enforced
        ? '<strong>The SPIRE Server API is the exception.</strong> Its TCP ' +
          'port is mutual TLS: a caller presents an X509-SVID from this ' +
          'trust domain, and every method is authorized against SPIRE\'s own ' +
          'table — so an entry marked <code>admin</code> or ' +
          '<code>downstream</code> below now decides what its holder may do. ' +
          'Its Unix socket is the <code>local</code> entity and needs no ' +
          'credential. The Workload API is deliberately untouched: its ' +
          'specification says a client MUST NOT be required to authenticate.'
        : '<strong>And nobody is authenticated on the SPIRE Server API ' +
          'either.</strong> That port is plain gRPC, any caller can create a ' +
          'registration entry granting any identity here and then collect an ' +
          'SVID for it, and the <code>admin</code> and ' +
          '<code>downstream</code> flags below are recorded and read by ' +
          'nothing. It is restart-only, because it decides how the socket is ' +
          'bound.') +
      ' <a href="/spiffe">GET /spiffe</a> has the whole table and the full ' +
      'list of what is and is not checked.</div>';
  }

  // WORKLOAD ATTESTATION ON THE UNIX SOCKET (#40 phase four): whether the
  // kernel can be asked at all, which attestors run, and each connection
  // open now with what it was attested as.
  spiffeWorkloadAttestation(state) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.spiffeWorkloadAttestation().");
    if (!state) {
      log.debug("Leaving AdminConsole.spiffeWorkloadAttestation(). None.");
      return '';
    }
    const kernel = state.nativeModule
      ? 'The native module is loaded: each connection to the Workload ' +
        'API\'s Unix socket is attested when it is accepted, and every call ' +
        'on it checks that the process is still the one attested.'
      : (state.unattestedSocketServed
        ? '<strong>The native module is not loaded, so the Unix socket is ' +
          'served UNATTESTED</strong> (development): ' +
          this.esc(state.problem)
        : '<strong>The native module is not loaded, so the Unix socket is ' +
          'NOT SERVED</strong> (product): ' + this.esc(state.problem));
    const out = '<h2>Workload attestation</h2>' + this.note(kernel +
      ' A TCP caller is never attested. Which attestors run is ' +
      '<code>spiffe.workloadAttestors</code>' +
      (state.unknownConfigured.length
        ? '; it names ' + state.unknownConfigured.map(function (t) {
            return '<code>' + self.esc(t) + '</code>';
          }).join(', ') + ', which nothing here implements'
        : '') + '.') +
      '<table><tr><th>Attestor</th><th>Runs</th><th>What it verifies</th>' +
      '</tr>' + state.attestors.map(function (a) {
        return '<tr><td><code>' + self.esc(a.type) + '</code></td><td>' +
          (a.enabled ? 'yes' : 'no') + '</td><td>' + self.esc(a.verifies) +
          '</td></tr>';
      }).join('') + '</table>' +
      (state.connections.length
        ? '<table><tr><th>Connection</th><th>pid</th><th>uid</th>' +
          '<th>gid</th><th>Selectors</th><th>State</th></tr>' +
          state.connections.map(function (c) {
            return '<tr><td><code>' + self.esc(c.tag) + '</code></td><td>' +
              self.esc(String(c.pid)) + '</td><td>' + self.esc(String(c.uid)) +
              '</td><td>' + self.esc(String(c.gid)) + '</td><td>' +
              self.esc(String(c.selectors)) + '</td><td>' +
              self.esc(c.error ? 'refused: ' + c.error
                               : (c.note || 'attested')) + '</td></tr>';
          }).join('') + '</table>'
        : this.note('No connection is open on the socket now.'));
    log.debug("Leaving AdminConsole.spiffeWorkloadAttestation().");
    return out;
  }

  // A listener row, and the fourth column is WHAT A CALLER HAS TO PRESENT ON
  // IT. Not decoration: the four sockets have three different postures — plain,
  // plain-and-trusted-as-local, and mutual TLS — and a reader who cannot see
  // which is which meets the difference as a handshake failure with no message.
  // The same courtesy /tls says about which port needs verification turned off.
  // **THE REALM IS A COLUMN SINCE 2026-09-12**, and it is the first thing a
  // reader of this table needs: the listeners are per realm now, so two rows
  // with the same surface and different addresses are two TRUST DOMAINS rather
  // than one surface on two transports. A table without it would report four
  // Workload API rows and leave the reader to work out which service they
  // belong to from the port.
  spiffeListenerRows(bindings, what) {
    const { log } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.spiffeListenerRows().");
    if (!bindings.length) {
      log.debug("Leaving AdminConsole.spiffeListenerRows().");
      return '<tr><td colspan="5">Nothing bound for ' + this.esc(what) + '. ' +
        'Either both transports are off in configuration, or the process has ' +
        'not finished starting.</td></tr>';
    }
    log.debug("Leaving AdminConsole.spiffeListenerRows().");
    return bindings.map(function (binding) {
      return '<tr><td>' + self.esc(what) + '</td><td>' +
        self.esc(binding.realm || 'default') + '</td><td><code>' +
        self.esc(binding.address) +
        '</code>' +
        (binding.tls ? ' <span class="note">(mutual TLS)</span>' : '') +
        '</td><td>' + (binding.listening ? 'listening'
          : '<strong>did not bind</strong> &mdash; ' +
            self.esc(binding.error)) +
        '</td><td>' + self.esc(binding.authentication || '') + '</td></tr>';
    }).join('');
  }

  spiffePage(req) {
    const { log, spiffeJson, spiffeCa } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.spiffePage().");
    const json: any = spiffeJson(req);
    const state = spiffeCa.state();
    const x509Rows = state.x509Authorities.map(function (authority) {
      return '<tr><td><code>' + self.esc(authority.id) + '</code></td><td>' +
        (authority.active ? '<strong>active</strong>' :
         'retired, still published') +
        '</td><td>' + self.esc(authority.keyType) + '</td><td>' +
        self.esc(authority.notAfter) + '</td><td><code>' +
        self.esc(authority.subject) +
        '</code></td></tr>';
    }).join('');
    const jwtRows = state.jwtAuthorities.map(function (authority) {
      return '<tr><td><code>' + self.esc(authority.id) + '</code></td><td>' +
        (authority.active ? '<strong>active</strong>' :
         'retired, still published') +
        '</td><td>' + self.esc(authority.keyType) + ' / ' +
        self.esc(authority.alg) +
        '</td><td>' + self.esc(new Date(authority.createdAt).toISOString()) +
        '</td><td>&mdash;</td></tr>';
    }).join('');
    const federatedRows = state.federated.map(function (entry) {
      return '<tr><td><code>' + self.esc(entry.trustDomainId) +
             '</code></td><td>' +
        entry.x509Keys + ' x509, ' + entry.jwtKeys + ' jwt</td><td>' +
        self.esc(entry.bundleEndpointProfile) + '<br><span class="note">' +
        self.esc(entry.bundleEndpointUrl || '(no endpoint URL recorded)') +
        '</span></td><td>' + self.esc(entry.sequence) + '</td><td>' +
        '<form method="post" action="/admin/spiffe" class="inline">' +
        '<input type="hidden" name="action" value="federation-remove">' +
        '<input type="hidden" name="trustDomain" value="' +
        self.esc(entry.trustDomain) + '"><button ' +
        'class="danger">Remove</button></form> <a ' +
        'href="/spiffe/federated/' + encodeURIComponent(entry.trustDomain) +
        '">document</a></td></tr>';
    }).join('') ||
      '<tr><td colspan="5">None. This trust domain federates with ' +
      'nobody.</td></tr>';

    const inner = this.messagesOf(req) + this.spiffePostureNote() +
      (json.enabled ? '' : this.warn('SPIFFE is turned OFF ' +
        '(<code>spiffe.enabled</code>): the bundle endpoint answers 404 and ' +
        'every gRPC call is refused with <code>Unavailable</code>. Turn it ' +
        'back on in the settings at the foot of this page; it needs no ' +
        'restart.')) +
      (json.ready ? '' : this.warn((json.error
        ? 'The issuing authority could not be built, so nothing here will ' +
          'issue an SVID: ' + this.esc(json.error)
        : 'The issuing authority is still being generated &mdash; an ' +
          'RSA-4096 key takes a few seconds. Reload.'))) +

      '<h2>The trust domain</h2>' +
      this.note('This service is the issuing authority for <code>' +
      this.esc(json.trustDomainId) + '</code>. Its own identity as a SPIFFE ' +
      'server is <code>' + this.esc(json.serverId || '(not yet)') +
      '</code>, and every ' +
      'registration entry hangs beneath that by default. The trust domain is ' +
      'restart-only (<code>spiffe.trustDomain</code>): every authority ' +
      'certificate names it.') +
      this.note('The bundle is published at <a href="' +
                this.esc(json.bundle.path) +
      '"><code>' + this.esc(json.bundle.path) +
      '</code></a> &mdash; sequence <code>' +
      this.esc(json.bundle.sequence) + '</code>, refresh hint ' +
      this.esc(json.bundle.refreshHint) + ' seconds. The sequence changes ' +
      'whenever the bundle does and never otherwise, which is what lets a ' +
      'consumer tell &ldquo;I have the current bundle&rdquo; from &ldquo;I ' +
      'have a bundle&rdquo;.') +

      '<h2>Authorities</h2>' +
      // ---------------------------------------------------------------------
      // THE X.509 AUTHORITY CAME FROM ONE OF TWO PLACES AND THIS PAGE SAYS
      // WHICH (2026-09-11).
      //
      // The two differ in the one thing an operator has to ACT on — what to
      // install as a trust anchor and how often — so the note is branched
      // rather than generalised into a sentence true of both. A page that read
      // the same either way would be describing a self-signed authority's
      // maintenance burden to somebody who no longer has one, or hiding it from
      // somebody who does.
      // ---------------------------------------------------------------------
      (json.authorities.source === 'pki'
        ? this.note('The X.509 authority is this realm\'s <strong>SPIFFE ' +
          'Issuing CA</strong>, under this service\'s own Root &mdash; ' +
          '<a href="/admin/pki">manage it on the PKI page</a>, where it is ' +
          'one of the Issuing CAs in this realm\'s branch. So the trust ' +
          'anchor a consumer installs is the <strong>Root</strong>, which ' +
          'every realm shares and which also covers LDAPS 636, the main port ' +
          'and every token this service signs: one anchor, installed once. ' +
          'An X509-SVID carries the Issuing CA and this realm\'s ' +
          'Intermediate in its own chain' +
          (json.authorities.chainSubjects.length
            ? ' (' + json.authorities.chainSubjects.map(function (subject) {
                return '<code>' + self.esc(subject) + '</code>';
              }).join(' &rarr; ') + ')'
            : '') + '. <strong>Rotating it re-issues that Issuing CA and ' +
          'leaves the anchor alone</strong>, so the bundle does not change, ' +
          'nothing has to be re-fetched, and SVIDs minted under the old ' +
          'authority go on verifying &mdash; which is the whole difference ' +
          'from the self-signed arrangement this replaced. The JWT authority ' +
          'has no certificate and no hierarchy to hang from: it is generated ' +
          'per start and rotating it still prepends, keeping at most ' +
          this.esc(json.authorities.maxRetained) + '.')
        : this.warn('This realm has <strong>no certificate ' +
          'authority</strong>, so its X.509 authority is ' +
          '<strong>self-signed</strong> and IS the trust anchor &mdash; ' +
          'generated per start and held in memory, exactly like the STS ' +
          'signing key and the TLS certificate, so a workload holding a ' +
          'bundle from before a restart will fail to verify every SVID ' +
          'minted after it. Rotating PREPENDS a new authority and keeps the ' +
          'old one published: an SVID minted a minute ago has to go on ' +
          'verifying, which is what a bundle is for. At most ' +
          this.esc(json.authorities.maxRetained) + ' are retained, and past ' +
          'that the oldest is dropped &mdash; anything it signed stops ' +
          'verifying at that moment. <a href="/admin/pki">Build this ' +
          'realm\'s certificate authority</a> to put the SPIFFE authority ' +
          'under this service\'s Root instead.')) +
      '<table><tr><th>Id</th><th>State</th><th>Key</th><th>Until</th>' +
      '<th>Subject</th></tr>' + x509Rows + jwtRows + '</table>' +
      // **WHAT SIGNS AND WHAT IS TRUSTED ARE TWO TABLES.** They were one, and
      // could be, while a self-signed authority was both. See `/spiffe`'s own
      // version of this note.
      '<h3>What a consumer trusts</h3>' +
      '<table><tr><th>Anchor</th><th>Subject</th><th>Until</th></tr>' +
      (json.authorities.trustAnchors || []).map(function (anchor) {
        return '<tr><td><code>' + self.esc(anchor.id) +
               '</code></td><td><code>' +
          self.esc(anchor.subject) + '</code></td><td>' +
          self.esc(anchor.notAfter) +
          '</td></tr>';
      }).join('') + '</table>' +
      '<form method="post" action="/admin/spiffe"><div class="formrow">' +
      '<input type="hidden" name="action" value="rotate">' +
      '<label for="which">Rotate</label>' +
      '<select id="which" name="which">' +
      '<option value="x509">the X.509 authority</option>' +
      '<option value="jwt">the JWT authority</option>' +
      '<option value="both">both</option></select>' +
      '<button>Rotate</button>' +
      '<span class="note">New SVIDs are signed with the new authority ' +
      'immediately; existing ones keep verifying until they expire.</span>' +
      '</div></form>' +

      '<h2>The gRPC listeners</h2>' +
      this.note('Neither <code>/admin/sts-metadata</code> nor this page can ' +
      'see a socket, so this table is the only place that reports whether ' +
      'each one actually bound. The DEFAULT realm\'s four are bound when the ' +
      'process starts and stay bound with <code>spiffe.enabled</code> off — ' +
      'they answer <code>Unavailable</code>, because a socket that vanished ' +
      'would read as a service that had stopped. EVERY OTHER REALM\'S are ' +
      'bound when <code>spiffe.enabled</code> is turned on for it, on the ' +
      'address <code>spiffe.grpcHost</code> names for that realm — the ' +
      'endpoint address is the only thing a SPIFFE client has to name a ' +
      'tenant with, because the gRPC method name is fixed by the ' +
      'specification.') +
      '<table><tr><th>Surface</th><th>Realm</th><th>Address</th><th>State' +
      '</th>' +
      '<th>What a caller presents</th></tr>' +
      this.spiffeListenerRows(json.listeners.workloadApi, 'Workload API') +
      this.spiffeListenerRows(json.listeners.serverApi, 'SPIRE Server API') +
      '</table>' +

      this.spiffeWorkloadAttestation(json.workloadAttestation) +

      '<h2>Who may call the SPIRE Server API</h2>' +
      '<p>' + this.esc(json.authentication.what || '') + '</p>' +
      this.note('A caller may be several of these at once and the check asks ' +
      'whether it is <em>any</em> of the ones a method allows, which is what ' +
      'SPIRE\'s own policy does: the <code>spire-server</code> CLI on this ' +
      'host is <code>local</code>, and an agent that also holds an entry ' +
      'marked <code>admin</code> is both.') +
      '<table><tr><th>Entity</th><th>What it means</th></tr>' +
      json.authentication.entities.map(function (entity) {
        return '<tr><td><code>' + self.esc(entity.id) + '</code></td><td>' +
          self.esc(entity.what) + '</td></tr>';
      }).join('') + '</table>' +
      this.note('Administrators by configuration ' +
      '(<code>spiffe.adminIds</code>, in the ' +
      'settings at the foot of this page): ' +
      (json.authentication.adminIds.length
        ? json.authentication.adminIds.map(function (id) {
            return '<code>' + self.esc(id) + '</code>';
          }).join(', ') + '. '
        : 'none. ') +
      'The other way to make one is to mark a registration entry ' +
      '<code>admin</code> on <a href="/admin/spiffe/entries">the entries ' +
      'page</a>; SPIRE has both, and neither is cached, so either takes ' +
      'effect on the next call.') +
      this.note('Workload API selectors: a caller there is identified as ' +
      '<code>transport:</code>, <code>endpoint:</code>, ' +
      '<code>peer:</code> over TCP, and on the Unix socket by what the ' +
      'workload attestors established (above), and ' +
      (json.authentication.attestWorkloads
        ? 'those decide which entries answer it ' +
          '(<code>spiffe.attestWorkloads</code>).'
        : 'that decides nothing at the moment &mdash; ' +
          '<code>spiffe.attestWorkloads</code> is off, so every caller is ' +
          'answered with every entry.') +
      ' Asserted selectors (<code>' +
      this.esc(json.authentication.assertedSelectorHeader) + '</code>) are ' +
      (json.authentication.acceptAssertedSelectors
        ? '<strong>believed</strong>, and nothing verifies them.'
        : 'ignored (<code>spiffe.acceptAssertedSelectors</code> is off).')) +
      '<h3>The per-method table</h3>' +
      this.note('Copied from SPIRE\'s own <code>policy_data.json</code> ' +
      'rather than reasoned out: a table derived from what each method ' +
      '&ldquo;obviously&rdquo; needs disagrees with SPIRE in two or three ' +
      'places, and the client author who meets the disagreement cannot tell ' +
      'which end is wrong. <code>any</code> means the method is open here ' +
      'and in a real server too &mdash; <code>AttestAgent</code> because an ' +
      'agent has no SVID until that call gives it one, ' +
      '<code>GetBundle</code> because a trust bundle is public.') +
      '<table><tr><th>Method</th><th>Allowed to</th></tr>' +
      json.authentication.policy.map(function (row) {
        return '<tr><td><code>' + self.esc(row.method) + '</code></td><td>' +
          self.esc(row.allow.join(', ')) + '</td></tr>';
      }).join('') + '</table>' +

      '<h2>Federated trust domains</h2>' +
      this.note('<strong>A foreign bundle is given to this service and never ' +
      'fetched by it.</strong> The federation specification has a bundle ' +
      'endpoint URL in the relationship and a real implementation polls it; ' +
      'this one records the URL and refuses to follow it, because fetching a ' +
      'URL somebody registered in order to obtain a credential-verification ' +
      'key is a server-side request forgery with a citation attached &mdash; ' +
      'the same refusal this service gives WS-Federation\'s ' +
      '<code>wreqptr</code> and a client\'s <code>jwks_uri</code>. Paste the ' +
      'bundle in below, or push it with <code>BatchSetFederatedBundle</code>' +
      '.') +
      '<table><tr><th>Trust domain</th><th>Keys</th><th>Profile / ' +
      'endpoint</th><th>Sequence</th><th></th></tr>' + federatedRows +
      '</table><form ' +
      'method="post" action="/admin/spiffe"><div class="formrow"><input ' +
      'type="hidden" name="action" value="federation-set"><label ' +
      'for="fed-td">Trust domain</label><input id="fed-td" ' +
      'name="trustDomain" placeholder="other.example" size="24"><label ' +
      'for="fed-url">Bundle endpoint URL</label><input id="fed-url" ' +
      'name="bundleEndpointUrl" placeholder="https://other.example/bundle" ' +
      'size="34"><label for="fed-profile">Profile</label><select ' +
      'id="fed-profile" name="bundleEndpointProfile"><option ' +
      'value="https_web">https_web</option><option ' +
      'value="https_spiffe">https_spiffe</option></select></div><div ' +
      'class="formrow"><label for="fed-doc">Bundle document</label><textarea ' +
      'id="fed-doc" name="document" rows="6" cols="80" ' +
      'placeholder=\'{"keys":[{"kty":"EC","use":"x509-svid","x5c":["..."]}],' +
      '"spiffe_sequence":1,"spiffe_refresh_hint":300}\'></textarea><button>' +
      'Set</button>' +
      this.note('A JWK Set. Every key needs <code>use</code> of ' +
      '<code>x509-svid</code>, <code>jwt-svid</code> or ' +
      '<code>wit-svid</code>: a consumer MUST IGNORE one without it, so a ' +
      'bundle of keys missing that member verifies nothing and reports no ' +
      'error, which is why it is refused here rather than stored.') +
      '</div></form>' +

      '<h2>Elsewhere</h2><ul>' +
      '<li><a href="/admin/spiffe/entries">Registration entries</a> &mdash; ' +
      this.esc(json.counts.entries) + ' of at most ' +
      this.esc(json.counts.maxEntries) +
      '</li>' +
      '<li><a href="/admin/spiffe/agents">Attested agents</a> &mdash; ' +
      this.esc(json.counts.agents) + ' of at most ' +
      this.esc(json.counts.maxAgents) +
      '</li><li><a href="/spiffe">What this is, and what it does not ' +
      'check</a></li><li><a href="/admin/ldap/spiffe">The containers and ' +
      'their schema</a></li><li><a href="/admin-api/spiffe">The same, over ' +
      'JSON</a></li></ul>' +
      // The spiffe.* rows (thirty-four as of 2026-09-16), on the page about
      // the trust domain rather than on /admin/config. The two lists below
      // them — the registration entries and the agents — are a STORE and are
      // edited on their own pages; these are the settings that decide what an
      // SVID minted against any entry looks like.
      this.configFormsFor('/admin/spiffe');
    log.debug("Leaving AdminConsole.spiffePage().");
    json.settings = this.configSettingsJson('/admin/spiffe');
    log.debug("Leaving AdminConsole.spiffePage().");
    return { json: json, inner: inner, title: 'SPIFFE' };
  }

  spiffeEntriesListPage(req) {
    const { log, spiffeEntriesJson, queryWith, spiffeSelectorText } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.spiffeEntriesListPage().");
    const view = spiffeEntriesJson(req);
    const json = view.json;
    const listView = this.listViewOf('/admin/spiffe/entries', req.query);
    const rows = json.entries.map(function (entry) {
      return '<tr><td><a href="/admin/spiffe/entries' +
        queryWith(listView, { entry: entry.id }) + '"><code>' +
        self.esc(entry.spiffeId) + '</code></a>' +
        (entry.expired ? ' <strong>(expired)</strong>' : '') +
        '<br><span class="note"><code>' + self.esc(entry.id) +
        '</code></span></td>' +
        '<td>' + self.esc(entry.selectors.map(spiffeSelectorText).join(', ') ||
                          '(none — matches every workload)') + '</td>' +
        '<td>' + self.esc(entry.origin) + '</td>' +
        '<td>' + self.esc(entry.hint || '—') + '</td>' +
        '<td>' + entry.svidsIssued + '</td>' +
        '<td>rev ' + entry.revisionNumber + '</td></tr>';
    }).join('') ||
      '<tr><td colspan="6">No registration entry matches.</td></tr>';
    const originOptions = ['<option value="">every origin</option>'].concat(
      json.origins.map(function (name) {
        return '<option value="' + self.esc(name) + '"' +
          (json.filter.origin === name ? ' selected' : '') + '>' +
          self.esc(name) +
          '</option>';
      })).join('');
    const inner = this.messagesOf(req) + this.spiffePostureNote() +
      this.note(this.esc(json.total) + ' registration entry/entries, of at ' +
        'most ' +
      this.esc(json.max) + ' (<code>spiffe.maxEntries</code>). The store is ' +
      'the embedded directory under <code>' + this.esc(json.container) +
      '</code>: an <code>ldapmodify</code> there, a form here and the SPIRE ' +
      'Server API\'s <code>BatchUpdateEntry</code> are three doors onto one ' +
      'entry, and nothing caches it &mdash; so a change takes effect on the ' +
      'next SVID.') +
      '<form method="get" action="/admin/spiffe/entries"><div ' +
      'class="formrow"><label for="q">Search</label>' +
      '<input id="q" name="q" value="' + this.esc(json.filter.q) +
      '" size="28" ' +
      'placeholder="a SPIFFE ID, a selector, an entry id">' +
      '<label for="origin">Origin</label>' +
      '<select id="origin" name="origin">' + originOptions + '</select>' +
      '<label for="per">Rows</label>' +
      '<select id="per" name="per">' +
      this.perPageOptions(view.paging.perPage) +
      '</select><button class="secondary">Filter</button>' +
      this.note('Origin is how the entry got here: <code>seed</code> ' +
      'at startup, <code>console</code>, <code>api</code>, ' +
      '<code>grpc</code>, <code>auto</code> (invented for a workload that ' +
      'matched nothing) or <code>ldap</code>.') +
      '</div></form><table><tr><th>SPIFFE ID / entry ' +
      'id</th><th>Selectors</th><th>Origin</th><th>Hint</th><th>SVIDs</th>' +
      '<th>' +
      'Revision</th></tr>' + rows + '</table>' +
      this.pageNavPair('/admin/spiffe/entries', this.filterOnly(listView),
                       view.paging).head +
      this.spiffeCreateEntryForm() ;
    log.debug("Leaving AdminConsole.spiffeEntriesListPage().");
    return { json: json, inner: inner };
  }

  spiffeCreateEntryForm() {
    const { log, spiffeCa } = this.deps;
    log.debug("Entering AdminConsole.spiffeCreateEntryForm().");
    log.debug("Leaving AdminConsole.spiffeCreateEntryForm().");
    return '<h2>Create a registration entry</h2>' +
      this.note('The SPIFFE ID must be in this trust domain and outside the ' +
      'reserved <code>/spire</code> path &mdash; those two refusals are the ' +
      'whole of what is checked. The parent defaults to this server\'s own ' +
      'identity, which is what SPIRE uses for an entry describing a workload ' +
      'rather than a node.') +
      '<form method="post" action="/admin/spiffe/entries"><div ' +
      'class="formrow"><input type="hidden" name="action" value="create">' +
      '<label for="e-id">SPIFFE ID</label>' +
      '<input id="e-id" name="spiffeId" size="40" placeholder="spiffe://' +
      this.esc(spiffeCa.trustDomain()) + '/ns/default/sa/web"><label ' +
      'for="e-parent">Parent</label><input id="e-parent" name="parentId" ' +
      'size="34" placeholder="(this server)"></div><div ' +
      'class="formrow"><label for="e-sel">Selectors</label><input id="e-sel" ' +
      'name="selectors" size="40" placeholder="unix:uid:1000, ' +
      'k8s:ns:default"><label for="e-dns">DNS names</label><input id="e-dns" ' +
      'name="dnsNames" size="26" placeholder="web.default.svc"></div><div ' +
      'class="formrow"><label for="e-x509ttl">X509-SVID TTL</label><input ' +
      'id="e-x509ttl" name="x509SvidTtl" size="6" placeholder="3600"><label ' +
      'for="e-jwtttl">JWT-SVID TTL</label><input id="e-jwtttl" ' +
      'name="jwtSvidTtl" size="6" placeholder="300"><label ' +
      'for="e-hint">Hint</label><input id="e-hint" name="hint" size="12" ' +
      'placeholder="internal"><label for="e-fed">Federates ' +
      'with</label><input id="e-fed" name="federatesWith" size="20" ' +
      'placeholder="other.example"><button>Create</button>' +
      this.note('Selectors, DNS names and trust domains are ' +
      'comma-separated. A selector is <code>type:value</code>, split on the ' +
      'FIRST colon only &mdash; so <code>docker:label:app:web</code> is type ' +
      '<code>docker</code>.') + '</div></form>';
  }

  spiffeEntryDetailPage(req, id) {
    const { log, spiffeRegistry, queryWith, spiffeSelectorText,
            config } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.spiffeEntryDetailPage(). id=" + id);
    const entry = spiffeRegistry.entryById(id);
    const listView = this.listViewOf('/admin/spiffe/entries', req.query);
    const back = queryWith(listView, {});
    if (!entry) {
      log.debug("Leaving AdminConsole.spiffeEntryDetailPage(). Not here.");
      return { json: { error: 'No registration entry has the id ' + id },
               inner: this.messagesOf(req) +
                 this.note('No registration entry has the id <code>' +
                           this.esc(id) +
                 '</code>. It may have been deleted &mdash; from this page, ' +
                 'with <code>BatchDeleteEntry</code>, or with an ' +
                 '<code>ldapdelete</code> under ' +
                 '<code>ou=entries,ou=spiffe</code>, which are three doors ' +
                 'onto one store.') +
                 '<p><a href="/admin/spiffe/entries' + this.esc(back) +
                 '">Back to the entries</a>.</p>' };
    }
    const attributeRows = Object.keys(entry.attributes || {}).sort()
      .map(function (name) {
        const value = entry.attributes[name];
        return '<tr><td><code>' + self.esc(name) + '</code></td><td>' +
          self.esc(Array.isArray(value) ? value.join(' | ') : String(value)) +
          '</td></tr>';
      }).join('');
    const json = { entry: entry, editable: spiffeRegistry.EDITABLE };
    const carried = '<input type="hidden" name="back" value="' +
                    this.esc(back) +
                    '"><input ' +
                    'type="hidden" name="entry" value="' + this.esc(entry.id) +
                    '">';
    const inner = this.messagesOf(req) + this.spiffePostureNote() +
      '<h2><code>' + this.esc(entry.spiffeId) + '</code></h2>' +
      this.note('Entry <code>' + this.esc(entry.id) + '</code>, revision ' +
      this.esc(entry.revisionNumber) + ', created by <code>' +
      this.esc(entry.origin) +
      '</code>. It lives at <code>' + this.esc(entry.dn) + '</code>' +
      (entry.expired ? ' and <strong>has expired</strong> &mdash; it is kept ' +
        'and reported rather than deleted, because an entry that vanished is ' +
        'indistinguishable from one nobody created' : '') + '.') +
      '<table><tr><th>Field</th><th>Value</th></tr>' +
      '<tr><td>Parent</td><td><code>' + this.esc(entry.parentId) +
      '</code></td></tr><tr><td>Selectors</td><td>' +
      this.esc(entry.selectors.map(spiffeSelectorText).join(', ') ||
               '(none — this entry matches every workload)') + '</td></tr>' +
      '<tr><td>DNS names</td><td>' +
      this.esc(entry.dnsNames.join(', ') || '—') +
      '</td></tr>' +
      '<tr><td>Federates with</td><td>' +
      this.esc(entry.federatesWith.join(', ') || '—') + '</td></tr>' +
      '<tr><td>X509-SVID TTL</td><td>' +
      (entry.x509SvidTtl ||
       ('default (' + this.esc(config.text('spiffe.svidTtl')) + ')')) +
      '</td></tr>' +
      '<tr><td>JWT-SVID TTL</td><td>' +
      (entry.jwtSvidTtl ||
       ('default (' + this.esc(config.text('spiffe.jwtSvidTtl')) + ')')) +
      '</td></tr>' +
      '<tr><td>Hint</td><td>' + this.esc(entry.hint || '—') + '</td></tr>' +
      '<tr><td>admin / downstream / storeSvid</td><td>' +
      (entry.admin ? 'admin' : '') + (entry.downstream ? ' downstream' : '') +
      (entry.storeSvid ? ' storeSvid' : '') +
      ((entry.admin || entry.downstream || entry.storeSvid) ? '' : '—') +
      ' <span class="note">recorded and never read &mdash; nothing here ' +
      'decides anything on one</span></td></tr><tr><td>SVIDs ' +
      'issued</td><td>' + entry.svidsIssued +
      (entry.lastSvidAt ? ', most recently ' + this.esc(entry.lastSvidAt) :
       '') +
      '</td></tr></table>' +

      '<h3>Change it</h3>' +
      this.note('Only the DECLARED half is editable here &mdash; what the ' +
      'entry may DO. The derived half (the revision number, the SVID ' +
      'counter, when it was created) is what HAPPENED, and a form that could ' +
      'rewrite it would make this page lie about the service\'s own ' +
      'behaviour. <code>ldapmodify</code> reaches everything: refusing it ' +
      'here is the difference between offering an operation and merely not ' +
      'preventing it.') +
      '<form method="post" action="/admin/spiffe/entries"><div ' +
      'class="formrow"><input type="hidden" name="action" value="update">' +
      carried +
      '<label for="u-field">Field</label>' +
      '<select id="u-field" name="field">' +
      ['spiffeId', 'parentId', 'selectors', 'dnsNames', 'federatesWith',
       'x509SvidTtl', 'jwtSvidTtl', 'hint', 'expiresAt', 'admin', 'downstream',
       'storeSvid'].map(function (name) {
        return '<option value="' + self.esc(name) + '">' + self.esc(name) +
               '</option>';
      }).join('') + '</select>' +
      '<label for="u-value">Value</label>' +
      '<input id="u-value" name="value" size="40">' +
      '<button>Set</button>' +
      '<span class="note">A list field takes comma-separated values and an ' +
      'empty value clears it. A boolean takes true or false.</span>' +
      '</div></form>' +
      '<form method="post" action="/admin/spiffe/entries"><div ' +
      'class="formrow"><input type="hidden" name="action" value="delete">' +
      carried +
      '<button class="danger">Delete this entry</button>' +
      this.note('Whatever holds an SVID minted from it keeps that SVID until ' +
      'it expires. SPIFFE has no revocation &mdash; the answer is a short ' +
      'lifetime, which is why the default is an hour.') + '</div></form>' +

      '<h3>The directory entry</h3>' +
      '<p>Every attribute, operational ones included. This is the store ' +
      'rather than a description of it.</p>' +
      '<table><tr><th>Attribute</th><th>Value</th></tr>' + attributeRows +
      '</table>';
    log.debug("Leaving AdminConsole.spiffeEntryDetailPage().");
    return { json: json, inner: inner };
  }

  spiffeEntriesView(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.spiffeEntriesView().");
    const wanted = String(req.query.entry || '').trim();
    if (wanted) {
      const detail = this.spiffeEntryDetailPage(req, wanted);
      log.debug("Leaving AdminConsole.spiffeEntriesView(). The drill-down.");
      return { json: detail.json, inner: detail.inner,
               title: 'Registration entry ' + wanted,
               up: this.upTo('/admin/spiffe/entries', wanted,
                             this.listViewOf('/admin/spiffe/entries',
                                             req.query)) };
    }
    const list = this.spiffeEntriesListPage(req);
    log.debug("Leaving AdminConsole.spiffeEntriesView(). The list.");
    return { json: list.json, inner: list.inner,
             title: 'Registration entries' };
  }

  spiffeAgentsListPage(req) {
    const { log, spiffeAgentsJson, queryWith } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.spiffeAgentsListPage().");
    const view = spiffeAgentsJson(req);
    const json = view.json;
    const listView = this.listViewOf('/admin/spiffe/agents', req.query);
    const rows = json.agents.map(function (agent) {
      return '<tr><td><a href="/admin/spiffe/agents' +
        queryWith(listView, { agent: agent.id }) + '"><code>' +
        self.esc(agent.id) + '</code></a></td>' +
        '<td>' + self.esc(agent.attestationType) + '</td>' +
        '<td>' + (agent.banned ? '<strong>banned</strong>' : 'active') +
        '</td><td>' + agent.attestations + '</td>' +
        '<td>' + self.esc(agent.lastSeen || '—') + '</td></tr>';
    }).join('') || '<tr><td colspan="5">No agent has attested here. An agent ' +
      'appears when it calls <code>AttestAgent</code> on the SPIRE Server ' +
      'API.</td></tr>';
    const inner = this.messagesOf(req) + this.spiffePostureNote() +
      this.note(this.esc(json.total) + ' agent(s), of at most ' +
                this.esc(json.max) +
      ' (<code>spiffe.maxAgents</code>). These entries are a RECORD rather ' +
      'than configuration &mdash; everything on them was written by this ' +
      'service when an agent attested &mdash; which is why nothing about an ' +
      'agent is editable and only the ban is.') +
      this.note('<strong>Node attestation is verified or refused.</strong> ' +
      'An agent here attested with a type its realm names in ' +
      '<code>spiffe.nodeAttestors</code> and an attestor verified, and its ' +
      'selectors are the ones that attestor derived.') +
      '<form method="get" action="/admin/spiffe/agents"><div class="formrow">' +
      '<label for="q">Search</label>' +
      '<input id="q" name="q" value="' + this.esc(json.filter.q) +
      '" size="30" placeholder="an agent id, an attestor, a selector"><label ' +
      'for="per">Rows</label><select id="per" name="per">' +
      this.perPageOptions(view.paging.perPage) +
      '</select><button class="secondary">Filter</button></div></form>' +
      '<table><tr><th>Agent</th><th>Attestor</th><th>State</th>' +
      '<th>Attestations</th><th>Last seen</th></tr>' + rows + '</table>' +
      this.pageNavPair('/admin/spiffe/agents', this.filterOnly(listView),
                       view.paging).head;
    log.debug("Leaving AdminConsole.spiffeAgentsListPage().");
    return { json: json, inner: inner };
  }

  spiffeAgentDetailPage(req, id) {
    const { log, spiffeRegistry, queryWith, spiffeSelectorText } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.spiffeAgentDetailPage(). id=" + id);
    const agent = spiffeRegistry.agentById(id);
    const listView = this.listViewOf('/admin/spiffe/agents', req.query);
    const back = queryWith(listView, {});
    if (!agent) {
      log.debug("Leaving AdminConsole.spiffeAgentDetailPage(). Not here.");
      return { json: { error: 'No agent has attested here as ' + id },
               inner: this.messagesOf(req) +
                 '<p>No agent has attested here as <code>' + this.esc(id) +
                 '</code>.</p><p><a href="/admin/spiffe/agents' +
                 this.esc(back) +
                 '">Back to the agents</a>.</p>' };
    }
    const attributeRows = Object.keys(agent.attributes || {}).sort()
      .map(function (name) {
        const value = agent.attributes[name];
        return '<tr><td><code>' + self.esc(name) + '</code></td><td>' +
          self.esc(Array.isArray(value) ? value.join(' | ') : String(value)) +
          '</td></tr>';
      }).join('');
    const carried = '<input type="hidden" name="back" value="' +
                    this.esc(back) +
                    '"><input ' +
                    'type="hidden" name="agent" value="' + this.esc(agent.id) +
                    '">';
    const inner = this.messagesOf(req) + this.spiffePostureNote() +
      '<h2><code>' + this.esc(agent.id) + '</code></h2>' +
      this.note('Attested with <code>' + this.esc(agent.attestationType) +
                '</code>, ' +
      this.esc(agent.attestations) + ' time(s), first at ' +
      this.esc(agent.firstSeen) +
      ' and most recently at ' + this.esc(agent.lastSeen) + '. It lives at ' +
        '<code>' +
      this.esc(agent.dn) + '</code> &mdash; the RDN is a digest of the ' +
      'SPIFFE ID, because a SPIFFE ID is too long for a readable one, so ' +
      '<strong>the cn is not the identity here</strong>: ' +
      '<code>spiffeAgentId</code> is.') +
      '<table><tr><th>Field</th><th>Value</th></tr>' +
      '<tr><td>State</td><td>' + (agent.banned
        ? '<strong>banned</strong> — AttestAgent refuses it, which is one of ' +
          'the few refusals in this service and is what keeps the button ' +
          'below from being a lie'
        : 'active') + '</td></tr>' +
      '<tr><td>Its directory entry</td><td>' +
      'Every identity this trust domain issues an X509-SVID to has one under ' +
      '<code>ou=users</code>, carrying the current certificate as the same ' +
      'six <code>x509*</code> attributes a verified TLS client certificate ' +
      'writes. Banning or deleting this agent marks that entry ' +
      '<code>spiffeCredentialStatus: revoked</code> and never removes it; ' +
      'unbanning marks it active again. <span class="note">That is not a ' +
      'certificate status. SPIFFE has no revocation, nothing reads the flag ' +
      'back, and whatever SVID this agent holds keeps working until it ' +
      'expires.</span></td></tr>' +
      '<tr><td>Can reattest</td><td>' + (agent.canReattest ? 'yes' : 'no') +
      '</td></tr>' +
      '<tr><td>Selectors</td><td>' +
      this.esc(agent.selectors.map(spiffeSelectorText).join(', ') || '—') +
      ' <span class="note">claimed, never verified</span></td></tr>' +
      '<tr><td>SVID</td><td>' + this.esc(agent.svidHash || '—') +
      (agent.expiresAt ? ', expires ' +
        this.esc(new Date(agent.expiresAt * 1000).toISOString()) : '') +
      '</td></tr></table>' +
      '<form method="post" action="/admin/spiffe/agents"><div ' +
      'class="formrow"><input type="hidden" name="action" value="' +
      (agent.banned ? 'unban' : 'ban') + '">' + carried +
      '<button class="' + (agent.banned ? 'secondary' : 'danger') + '">' +
      (agent.banned ? 'Unban' : 'Ban') + ' this agent</button>' +
      this.note('A banned agent is refused at <code>AttestAgent</code> with ' +
      '<code>PermissionDenied</code>. Whatever SVID it already holds keeps ' +
      'working until it expires &mdash; there is no revocation in SPIFFE.') +
      '</div></form>' +
      '<form method="post" action="/admin/spiffe/agents"><div ' +
      'class="formrow"><input type="hidden" name="action" value="delete">' +
      carried +
      '<button class="danger">Delete this agent</button>' +
      this.note('It reappears the next time it attests, because ' +
      'attestation is not checked &mdash; deleting is forgetting, not ' +
      'revoking.') + '</div></form>' +
      '<h3>The directory entry</h3>' +
      '<table><tr><th>Attribute</th><th>Value</th></tr>' + attributeRows +
      '</table>';
    log.debug("Leaving AdminConsole.spiffeAgentDetailPage().");
    return { json: { agent: agent }, inner: inner };
  }

  spiffeAgentsView(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.spiffeAgentsView().");
    const wanted = String(req.query.agent || '').trim();
    if (wanted) {
      const detail = this.spiffeAgentDetailPage(req, wanted);
      log.debug("Leaving AdminConsole.spiffeAgentsView(). The drill-down.");
      return { json: detail.json, inner: detail.inner, title: 'Agent ' + wanted,
               up: this.upTo('/admin/spiffe/agents', wanted,
                             this.listViewOf('/admin/spiffe/agents',
                                             req.query)) };
    }
    const list = this.spiffeAgentsListPage(req);
    log.debug("Leaving AdminConsole.spiffeAgentsView(). The list.");
    return { json: list.json, inner: list.inner, title: 'Attested agents' };
  }

  // THE STATE CELL, and it is the most important thing on the page. Four states
  // and each is a different instruction to the reader, which is why they are
  // four sentences rather than a boolean and a tooltip.
  federationStateCell(row) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.federationStateCell().");
    if (row.usable) {
      log.debug("Leaving AdminConsole.federationStateCell().");
      return '<td class="ok">ready</td>';
    }
    if (!row.enabled) {
      log.debug("Leaving AdminConsole.federationStateCell().");
      return '<td class="off">disabled' +
        (row.ready ? '' : ' <span class="sub">and not configured</span>') +
             '</td>';
    }
    log.debug("Leaving AdminConsole.federationStateCell().");
    return '<td class="bad">ENABLED, not configured<span class="sub">' +
      this.esc(row.missing.join(', ')) + ' still to set. It refuses rather ' +
      'than half-working.</span></td>';
  }

  federationListPage(req) {
    const { log, adminViews, pageParamsOf, queryWith, federation } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.federationListPage().");
    const view = adminViews.federationListJson(req);
    const all = view.all;
    const wantedText = view.wantedText;
    const wantedRole = view.wantedRole;
    const filtered = view.filtered;
    const paging = view.paging;
    const paged = view.paged;
    const nav = this.pageNavPair('/admin/federation', pageParamsOf(req.query),
                                 paging);

    const rows = paged.shown.map(function (row) {
      return '<tr><td><a href="/admin/federation' +
        self.esc(queryWith(self.listViewOf('/admin/federation', req.query),
                           { relationship: row.id })) +
        '">' + self.esc(row.id) + '</a>' +
        (row.name !== row.id ? '<span class="sub">' + self.esc(row.name) +
         '</span>' :
         '') + '</td><td>' + self.esc(row.roleLabel) + '</td><td>' +
        self.esc(row.protocolLabel) + '</td><td ' +
        'class="who">' + self.esc(row.peer || row.application || '') +
        (!row.peer && !row.application ? '<span class="sub">nothing named ' +
                                         'yet</span>' : '') +
        '</td>' +
        self.federationStateCell(row) +
        '<td class="num">' + row.authentications + '</td>' +
        '<td class="num">' + row.users + '</td>' +
        '<td>' + (row.lastError
          ? '<span class="bad">' + self.shortened(row.lastError, 60) + '</span>'
          : '<span class="sub">none recorded</span>') + '</td></tr>';
    }).join('');

    const roleOptions = ['<option value="">any role</option>'].concat(
      federation.ROLES.map(function (one) {
        const n = all.filter(function (r) { return r.role === one.role; })
          .length;
        return '<option value="' + self.esc(one.role) + '"' +
          (one.role === wantedRole ? ' selected' : '') + '>' +
          self.esc(one.short) +
          ' (' + n + ')</option>';
      })).join('');

    const inner = this.messagesOf(req) +
      '<div class="tiles">' +
      this.tile(all.length, 'Relationships') +
      this.tile(all.filter(function (r) { return r.usable; }).length, 'Ready') +
      this.tile(all.filter(function (r) { return r.enabled && !r.ready; })
        .length,
                'Enabled, not configured') +
      this.tile(all.reduce(function (n, r) { return n + r.authentications; },
                           0),
                'Federated sign-ins') +
      '</div>' +
      FEDERATION_CAVEAT +
      // THE PICTURE, pointed at from the page it drills down from. This table
      // has one row per relationship and no row can say anything about another,
      // so three questions an operator arrives with have no cell here: how many
      // applications are behind a partner, how many people have come through it
      // FOR EACH of them, and what an arriving foreign service provider
      // actually meets. All three are facts about two registers at once.
      this.note('<a href="/admin/federation/map">The picture</a> draws this ' +
      'register as a diagram, laid out on the server. It adds the three ' +
      'things a table of relationships has nowhere to put: how many ' +
      'applications are configured to use each partner, how many people have ' +
      'signed in through each <em>application and relationship</em> pair, ' +
      'and what the identity-provider side is configured to do about ' +
      'authenticating somebody — including where one relationship brokers to ' +
      'another, which is what makes this service an identity bridge and is ' +
      'invisible in any single row.') +
      '<form method="get" action="/admin/federation"><div class="formrow">' +
      '<label for="q">Relationship</label>' +
      '<input type="text" id="q" name="q" value="' +
      this.esc(String(req.query.q || '')) +
      '" size="24" placeholder="id, name, partner or application">' +
      '<label for="role">Role</label><select id="role" name="role">' +
      roleOptions + '</select><label for="per">Show</label><select id="per" ' +
      'name="per">' + this.perPageOptions(paging.perPage) +
      '</select><button ' +
      'type="submit">Filter</button>' +
      ((wantedText || wantedRole) ? ' <a href="/admin/federation">clear</a>' :
        '') +
      '</div></form>' + nav.head +
      '<table><tr><th>Relationship</th><th>This service ' +
      'is</th><th>Protocol</th><th>Partner</th><th>State</th><th ' +
      'class="num">Sign-ins</th><th class="num">People</th><th>Last ' +
      'refusal</th></tr>' +
      (rows || '<tr><td colspan="8">No federation relationship ' +
        ((wantedText || wantedRole) ? 'matches. The filter above may be ' +
                                      'hiding some.'
          : 'is configured. Nothing federated happens until one is — and ' +
            'then not until it is enabled, which is a second, deliberate ' +
            'act.') + '</td></tr>') +
      '</table>' + nav.foot +
      this.federationCreateForm() +
      '<h2>The two directions</h2>' +
      '<table><tr><th>This service is</th><th>What it means</th></tr>' +
      federation.ROLES.map(function (one) {
        return '<tr><td>' + self.esc(one.short) + '</td><td>' +
               self.esc(one.what) +
               '</td></tr>';
      }).join('') + '</table>' +
      this.note('<strong>One relationship is one DIRECTION.</strong> A ' +
      'partner this service both consumes from and asserts to is two ' +
      'relationships with two ids, because everything that configures one ' +
      'differs by direction — the endpoints are theirs or ours, the ' +
      'certificate is theirs or ours, the attribute mapping runs inbound or ' +
      'the release list runs outbound.') +
      '<h2>The five protocols</h2><table><tr><th>Protocol</th><th>What ' +
      'happens</th><th>Specification</th></tr>' +
      federation.PROTOCOLS.map(function (one) {
        return '<tr><td>' + self.esc(one.label) + '</td><td>' +
               self.esc(one.what) +
          '</td><td ' +
          'class="sub">' + self.esc(one.spec) + '</td></tr>';
      }).join('') + '</table>' +
      // The federation.* rows (fifteen as of 2026-09-16), on the page that
      // configures the feature. `federation.enabled` and `federation.outbound`
      // are the two that turn halves of it off, and a person reading a
      // relationship that does not work should not have to guess that the
      // answer is a setting somewhere else.
      this.configFormsFor('/admin/federation') +
      FEDERATION_LINKS;

    log.debug("Leaving AdminConsole.federationListPage(). " +
              paged.shown.length + " row(s) of " +
              filtered.length + " matched.");
    return {
      inner: inner,
      json: view.json
    };
  }

  federationCreateForm() {
    const { log, federation } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.federationCreateForm().");
    log.debug("Leaving AdminConsole.federationCreateForm().");
    return '<h2>Add a relationship</h2>' +
      this.note('It is created <strong>disabled</strong>, whatever is filled ' +
      'in here, and nothing about it does anything until it is enabled on ' +
      'its own page. That is not caution for its own sake: a partner that ' +
      'half-exists and silently accepts assertions is the failure this whole ' +
      'register is arranged to prevent, and enabling is the second, ' +
      'deliberate act that says the configuration is finished.') +
      '<form method="post" action="/admin/federation"><div ' +
      'class="formrow"><input type="hidden" name="action" ' +
      'value="create"><label for="fedid">Id</label><input type="text" ' +
      'id="fedid" name="id" size="16" required ' +
      'placeholder="partner-a"><label for="fedrole">This service ' +
      'is</label><select id="fedrole" name="role">' +
      federation.ROLES.map(function (one) {
        return '<option value="' + self.esc(one.role) + '">' +
               self.esc(one.short) +
               '</option>';
      }).join('') + '</select>' +
      '<label for="fedprotocol">Protocol</label>' +
      '<select id="fedprotocol" name="protocol">' +
      federation.PROTOCOLS.map(function (one) {
        return '<option value="' + self.esc(one.protocol) + '">' +
               self.esc(one.label) +
               '</option>';
      }).join('') + '</select><label for="fedname">Name</label><input ' +
      'type="text" id="fedname" name="name" size="16" ' +
      'placeholder="optional"><label for="fedpeer">Partner</label><input ' +
      'type="text" id="fedpeer" name="peer" size="26" placeholder="their ' +
      'entityID, issuer or realm"><button ' +
      'type="submit">Add</button></div></form>' +
      this.note('The <strong>id</strong> is the key, the RDN and a URL ' +
      'segment, so it has to start with a letter or a digit and hold only ' +
      'letters, digits, dot, dash and underscore. <strong>Partner</strong> ' +
      'is their own identifier in whatever their protocol calls it, and on a ' +
      'service-provider-side relationship it is CHECKED: an assertion whose ' +
      'issuer is not that string is refused, even when the signature ' +
      'verifies.');
  }

  // The drill-down. It is the page that actually does the work, because a
  // relationship is configured field by field and the fields differ by role and
  // by protocol — which is why the form is BUILT from the schema rather than
  // typed out: `fieldsForRole()` is the same call the action validates against,
  // so this page cannot offer a field the action would refuse.
  federationDetailPage(req, id) {
    const { log, adminViews, queryWith, federation } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.federationDetailPage(). id=" + id);
    const view = adminViews.federationDetailJson(req, id);
    const record = view.record;
    const carryBack = '<input type="hidden" name="back" value="' +
      this.esc(queryWith(this.listViewOf('/admin/federation', req.query), {})) +
      '">';
    if (!record) {
      log.debug("Leaving AdminConsole.federationDetailPage(). No such " +
                "relationship.");
      return {
        missing: true,
        inner: this.messagesOf(req) +
          '<p class="warn">There is no federation relationship called <code>' +
          this.esc(id) +
          '</code>. Unlike almost everything else in this console, one does ' +
          'not appear because somebody used it: this register is configured ' +
          'and nothing creates an entry in it by turning ' +
          'up.</p>' + FEDERATION_LINKS,
        json: view.json
      };
    }
    const row = view.row;
    const base = view.base;
    const acs = view.acs;
    const login = view.login;
    const metadata = view.metadata;
    const loginPath = view.loginPath;
    const setFields = view.setFields;
    const multiFields = view.multiFields;
    const fieldRows = setFields.map(function (field) {
      const value = record[field.name] || '';
      return '<tr><td><code>' + self.esc(field.name) + '</code></td>' +
        '<td><form method="post" action="/admin/federation"><div ' +
        'class="formrow">' + carryBack +
        '<input type="hidden" name="action" value="set">' +
        '<input type="hidden" name="id" value="' + self.esc(row.id) + '">' +
        '<input type="hidden" name="field" value="' + self.esc(field.name) +
        '">' +
        // A FIELD WITH A FIXED SET OF VALUES GETS A SELECT, and the list comes
        // off the schema row rather than being typed here — the same reason the
        // form itself is built from fieldsForRole(): a page offering a value
        // the action refuses is a page that cannot be trusted about the ones it
        // accepts. It matters most on fedAuthnMechanism, where a typo does not
        // fail loudly: the relationship simply says nothing this service
        // recognises and the sign-in falls through to a password box, which is
        // indistinguishable from the feature not being configured at all.
        //
        // The blank option is FIRST and is not a value — it clears the
        // attribute, which for this field is meaningfully different from
        // `password`: empty means "this relationship says nothing" and falls
        // through to the application entry.
        (Array.isArray(field.enum) && field.enum.length
          ? '<select name="value"' + self.tip(field.what) + '>' +
            '<option value=""' + (value ? '' : ' selected') + '>' +
            '(not set — this relationship says nothing)</option>' +
            field.enum.map(function (one) {
              const row = federation.mechanismRow(one);
              return '<option value="' + self.esc(one) + '"' +
                (String(value) === one ? ' selected' : '') + '>' +
                self.esc(one) +
                (row ? ' — ' + self.esc(row.label) : '') + '</option>';
            }).join('') + '</select>'
          : '<input type="text" name="value" size="42"' + self.tip(field.what) +
            ' value="' + self.esc(field.sensitive && value ? '' : value) + '"' +
            (field.sensitive ? ' placeholder="set — not shown"' : '') + '>') +
        '<button type="submit">Set</button></div></form></td>' +
        '<td class="sub">' + self.note(self.esc(field.what) +
          (field.sensitive
            ? ' <strong>Never printed on this page or in the audit ' +
              'log</strong>, though an ldapsearch of this directory will ' +
              'show it — see the note at the foot.'
            : '')) + '</td></tr>';
    }).join('');

    const switches = ['fedEnabled'].concat(
      row.role === 'service-provider'
        ? ['fedAutocreateUsers', 'fedUpdateUserAttributes',
           'fedAllowUnsolicited', 'fedSignRequest']
        : []).map(function (name) {
      const field = federation.SCHEMA.attributes.filter(
          function (f) { return f.name === name; })[0];
      if (!field) return '';
      // The two provisioning switches default ON; the rest default off.
      const dflt = name === 'fedAutocreateUsers' ||
                   name === 'fedUpdateUserAttributes';
      const on = federation.boolOf(record[name], dflt);
      return '<tr><td><code>' + self.esc(name) + '</code></td>' +
        '<td class="' + (on ? 'ok' : 'off') + '">' + (on ? 'TRUE' : 'FALSE') +
        '</td><td><form ' +
        'method="post" action="/admin/federation"><div ' +
        'class="formrow">' + carryBack +
        '<input type="hidden" name="action" value="set">' +
        '<input type="hidden" name="id" value="' + self.esc(row.id) + '">' +
        '<input type="hidden" name="field" value="' + self.esc(name) + '">' +
        '<input type="hidden" name="value" value="' + (on ? 'FALSE' : 'TRUE') +
        '"><button ' +
        'type="submit"' + (name === 'fedEnabled' && !on && !row.ready ? ' ' +
            'class="danger"' : '') +
        '>' + (on ? 'Turn off' : 'Turn on') + '</button></div></form></td>' +
        '<td class="sub">' + self.esc(field.what) + '</td></tr>';
    }).join('');

    const multiSections = multiFields.map(function (field) {
      const values = record[field.name] || [];
      return '<h3><code>' + self.esc(field.name) + '</code></h3>' +
        self.note(self.esc(field.what)) +
        (values.length
          ? '<table><tr><th>Value</th><th></th></tr>' +
            values.map(function (value) {
              return '<tr><td class="who"><code>' + self.esc(value) +
                '</code></td><td><form ' +
                'method="post" action="/admin/federation"><div ' +
                'class="formrow">' +
                carryBack +
                '<input type="hidden" name="action" value="remove-value">' +
                '<input type="hidden" name="id" value="' + self.esc(row.id) +
                '"><input type="hidden" name="field" value="' + self.esc(
                    field.name) + '"><input ' +
                'type="hidden" name="value" value="' + self.esc(value) + '">' +
                '<button type="submit">Remove</button></div></form></td></tr>';
            }).join('') + '</table>'
          : '<p class="sub">None' +
            (field.name === 'fedRelease'
              ? ' — <strong>and that means no release policy rather than ' +
                'release nothing</strong>. This partner receives exactly ' +
                'what /admin/claims and /admin/saml-attributes would give ' +
                'anybody. Adding the first name here starts filtering.'
              : '') + '.</p>') +
        '<form method="post" action="/admin/federation"><div class="formrow">' +
        carryBack +
        '<input type="hidden" name="action" value="add-value">' +
        '<input type="hidden" name="id" value="' + self.esc(row.id) + '">' +
        '<input type="hidden" name="field" value="' + self.esc(field.name) +
        '"><input type="text" name="value" size="40" placeholder="' +
        (field.name === 'fedAttributeMap' ? 'incoming name=ldapAttribute'
          : (field.name === 'fedRelease' ? 'a claim or attribute name' :
             'a value')) + '"><button ' +
        'type="submit">Add</button></div></form>';
    }).join('');

    const inner = this.messagesOf(req) +
      '<div class="tiles">' +
      this.tile(row.authentications, 'Federated sign-ins') +
      this.tile(row.users, 'Distinct people') +
      this.tile(row.usable ? 'ready' : (row.enabled ? 'NOT READY' : 'disabled'),
                'State') +
      '</div>' +
      (row.lastError
        ? '<p class="warn"><strong>The last attempt was refused.</strong> ' +
          this.esc(row.lastError) +
          (row.lastErrorAt ?
           ' <span class="sub">at ' + this.esc(row.lastErrorAt) +
           '</span>' : '') +
          ' A success clears this, so it standing here means nothing has ' +
          'worked since.</p>'
        : '') +
      (row.enabled && !row.ready
        ? '<p class="warn"><strong>Enabled and not configured.</strong> ' +
          this.esc(row.missing.join(', ')) + ' still to set. Every endpoint ' +
          'for this relationship refuses in this state rather than ' +
          'half-working — a federated sign-in that got half way and produced ' +
          'a session would be the worst possible outcome.</p>'
        : '') +
      '<table><tr><th>What</th><th>Value</th></tr>' +
      '<tr><td>This service is</td><td>' + this.esc(row.roleLabel) +
      '</td></tr>' +
      '<tr><td>Protocol</td><td>' + this.esc(row.protocolLabel) + '</td></tr>' +
      '<tr><td>Partner</td><td class="who"><code>' +
      this.esc(row.peer || '(none ' +
          'named)') +
        '</code></td></tr>' +
      (row.role === 'identity-provider'
        ? '<tr><td>Application</td><td class="who">' +
          (row.application
            ? '<a href="/admin/applications?application=' +
              encodeURIComponent(row.application) +
              '"><code>' + this.esc(row.application) + '</code></a>'
            : '<span class="sub">none named — this relationship configures ' +
              'nothing until one is</span>') + '</td></tr>'
        : '') +
      '<tr><td>Last used</td><td>' + this.esc(row.lastSeen || '(never)') +
        (row.lastUser ? ' <span class="sub">by ' + this.esc(row.lastUser) +
         '</span>' :
         '') +
        '</td></tr>' +
      '<tr><td>Directory entry</td><td class="who"><code>' + this.esc(row.dn) +
      '</code></td></tr></table>' +
      (row.role === 'service-provider'
        ? '<h2>What to configure at the partner</h2>' +
          this.note('These are the URLs to give whoever runs the identity ' +
          'provider. The assertion consumer service is the one that matters: ' +
          'a partner sending its answer anywhere else produces a 404 in a ' +
          'browser AFTER a successful sign-in somewhere else, which is the ' +
          'least diagnosable failure this feature has.') +
          '<table><tr><th>What they need</th><th>Value</th></tr>' +
          '<tr><td>Assertion consumer service / <code>wreply</code> / ' +
            '<code>redirect_uri</code></td><td class="who"><code>' +
            this.esc(acs) +
            '</code></td></tr>' +
          '<tr><td>Our entityID / <code>wtrealm</code></td><td ' +
          'class="who"><code>' + this.esc(acs) +
            '</code><span class="sub">the same string, deliberately: one ' +
            'name for this service per partner, so a partner keying its ' +
            'trust store off an entityID gets one that is only ' +
            'ours-with-them</span></td></tr>' +
          ((row.protocol === 'saml2' || row.protocol === 'saml11')
            ? '<tr><td>Our SAML metadata</td><td class="who"><a href="' +
              this.esc(federation.PATHS.metadata + '/' +
                       encodeURIComponent(row.id)) +
              '"><code>' +
              this.esc(metadata) + '</code></a><span class="sub">unsigned, ' +
              'deliberately — a signature over it made by the very key it ' +
              'publishes proves nothing they did not already have to ' +
              'trust</span></td></tr>'
            : '') +
          '</table>' +
          this.note('<a class="btn" href="' + this.esc(login) +
                    '">Start a federated ' +
          'sign-in through this ' +
          'partner</a> ' + (row.usable ? '' : '<span class="sub">— it will ' +
          'refuse until this relationship is enabled and configured</span>'))
        : '<h2>What this relationship does</h2>' +
          this.note('Every protocol endpoint here already issues to anybody ' +
          'that asks, so this relationship changes nothing about whether the ' +
          'partner is answered. What it adds is two things: the partner is ' +
          'marked as a FEDERATION PARTNER rather than a test client, and ' +
          '<code>fedRelease</code> below decides which attributes are ' +
          'released to it.') +
          this.note('<strong>The release list can only remove, and only from ' +
          'what <a href="/admin/claims">custom claims</a>, <a ' +
          'href="/admin/saml-attributes">custom SAML attributes</a> and the ' +
          'groups claim would add.</strong> It cannot touch ' +
          '<code>sub</code>, <code>iss</code>, <code>exp</code>, a NameID or ' +
          'anything else the protocol puts in an artifact itself — those are ' +
          'what make the artifact verifiable, and a release list that could ' +
          'drop <code>iss</code> would produce tokens that fail to verify ' +
          'with nothing pointing back at this page.')) +
      '<h2>Settings</h2>' +
      '<table><tr><th>Field</th><th>Value</th><th>What it is</th></tr>' +
      fieldRows +
      '</table><h2>Switches</h2><table><tr><th>Field</th><th>Now</th><th>' +
      '</th>' +
      '<th>What it is</th></tr>' + switches +
      '</table>' +
      '<h2>Lists</h2>' + multiSections +
      '<h2>Delete</h2>' +
      '<form method="post" action="/admin/federation"><div class="formrow">' +
      carryBack +
      '<input type="hidden" name="action" value="delete">' +
      '<input type="hidden" name="id" value="' + this.esc(row.id) + '">' +
      '<button type="submit" class="danger">Delete this relationship</button>' +
      '<span class="sub">The entry goes and takes its ' + row.authentications +
      ' recorded sign-in(s) with it. The PEOPLE it authenticated keep their ' +
      'entries under <code>ou=users</code> — nothing is ever deleted from ' +
      'there — and any session they hold is unaffected until it expires or ' +
      'is ended.</span></div></form>' +
      this.note('<strong>Everything on this page is an attribute on one ' +
      'directory entry</strong>, so an <code>ldapmodify</code> of <code>' +
      this.esc(row.dn) +
      '</code> does exactly what these forms do — two doors onto one ' +
      'register, not two registers. That cuts both ways: ' +
      '<code>fedClientSecret</code> is this service\'s own credential AT the ' +
      'partner, held in the clear, in a directory where every bind succeeds. ' +
      'It is never shown here and never written to the audit log, and ' +
      'anybody who can read this directory can authenticate as this service ' +
      'at that partner. A deployment federating with something real should ' +
      'know that.') +
      FEDERATION_CAVEAT +
      '<p class="sub"><a href="' +
      this.esc('/admin/federation' +
               queryWith(this.listViewOf('/admin/federation', req.query), {})) +
      '">back to the list</a></p>' + FEDERATION_LINKS;

    log.debug("Leaving AdminConsole.federationDetailPage(). " + row.id + ".");
    return { inner: inner, json: view.json };
  }

  federationView(req) {
    const { log } = this.deps;
    log.debug("Entering AdminConsole.federationView().");
    const wanted = String(req.query.relationship || '').trim();
    if (wanted) {
      const detail = this.federationDetailPage(req, wanted);
      log.debug("Leaving AdminConsole.federationView(). The drill-down.");
      return { missing: !!detail.missing, json: detail.json,
               inner: detail.inner,
               title: 'Federation relationship ' + wanted,
               up: this.upTo('/admin/federation', wanted,
                             this.listViewOf('/admin/federation', req.query)) };
    }
    const list = this.federationListPage(req);
    log.debug("Leaving AdminConsole.federationView(). The list.");
    return { json: list.json, inner: list.inner, title: 'Federation' };
  }

  // ---------------------------------------------------------------------------
  // GET /admin/federation/map — THE SAME REGISTER, AS A PICTURE.
  //
  // `/admin/federation` above is a table: one row per relationship, each row a
  // complete description of one arrangement and none of them saying anything
  // about the others. That is the right shape for CONFIGURING a partner and the
  // wrong shape for three questions an operator actually arrives with, all of
  // which are facts about two registers at once and none of which a row has
  // anywhere to put:
  //
  //   * HOW MANY APPLICATIONS ARE BEHIND THIS PARTNER, and which. It is
  //     `appFederationRelationship` on entries under `ou=applications` pointing
  //     BACK at a relationship, so the relationship's own entry has never
  //     known.
  //   * HOW MANY PEOPLE HAVE ACTUALLY COME THROUGH IT, PER APPLICATION.
  //     `fedAuthentications` has always answered the first half; the split by
  //     application is `fedApplicationUse`, which exists because a partner
  //     shared by two applications makes one number into two different
  //     questions.
  //   * WHAT HAPPENS TO SOMEBODY WHO ARRIVES AT THE IDENTITY-PROVIDER SIDE —
  //     `fedAuthnMechanism`, and the onward relationship when it says
  //     `federation`. A table can print the attribute; only a picture can show
  //     that the onward relationship is the one two rows down and that the two
  //     together are a BRIDGE.
  //
  // THE PICTURE IS OF ONE TRUST REALM, which is the console's rule everywhere
  // and is a real constraint here rather than a convention followed for
  // tidiness: the register is per realm, an id that names a relationship in
  // another realm names nothing here, and the realm switcher at the top of the
  // page is how you get to the other one. The hexagon carries the realm for the
  // same reason — a saved `?format=svg` document with no realm on it is a
  // picture of somewhere.
  //
  // IT IS DRAWN ON THE SERVER, and that is the root CLAUDE.md's second CSP rule
  // being honoured rather than argued again: `federation_diagram.js`'s header
  // makes the case in full. Nothing here relaxes `script-src 'none'`, which is
  // why the picture does not pan or zoom — the page says so out loud below
  // rather than leaving somebody to wonder why dragging does nothing, and
  // `?format=svg` hands over a document for something that does zoom.
  // ---------------------------------------------------------------------------

  // What every box is called, where it links, and nothing about its shape — the
  // shape is `federation_diagram.js`'s and is decided from the node's kind,
  // which is the one thing a caller must not be able to override. See lookOf()
  // there.
  federationMapLooks(graph) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.federationMapLooks().");
    const looks = {};
    graph.nodes.forEach(function (node) {
      if (node.kind === 'sts') {
        looks[node.id] = {};
        return;
      }
      if (node.kind === 'application') {
        // An application box goes to the APPLICATIONS registry and not to the
        // relationship, because the thing somebody clicks it to change is
        // `appFederationRelationship`, which lives on that entry. The
        // relationship is one click away on the line's own label.
        looks[node.id] = {
          href: '/admin/applications' +
                queryWith({}, { application: node.label })
        };
        return;
      }
      // BOTH PARTNER SHAPES GO TO THE RELATIONSHIP, and where a partner has
      // more than one they go to the FIRST — which is a real limitation rather
      // than a choice, and it is the delegation picture's own: an SVG anchor
      // wraps one shape and can have one href. The table under the picture
      // lists every relationship a partner has, which is where a reader with
      // two goes.
      looks[node.id] = {
        href: '/admin/federation' +
              queryWith({}, { relationship: node.relationships[0] || '' })
      };
    });
    log.debug("Leaving AdminConsole.federationMapLooks(). " +
              graph.nodes.length +
              " box(es).");
    return {
      looks: looks,
      resolve: function (node) {
        log.debug("Entering resolve().");
        log.debug("Leaving resolve().");
        return looks[node.id];
      }
    };
  }

  // The drawing and the two links under it, and `?format=svg`'s answer — the
  // same pair of functions `/admin/delegation/map` has, for the same reason.
  // The standalone document is rendered a SECOND time with `links: false`
  // rather than the page's markup being reused, and that is not waste: it
  // carries no links deliberately, because `app.js` rewrites root-relative
  // hrefs into the current realm on the way out of a `text/html` response ONLY
  // — so a link inside an `image/svg+xml` body would silently leave the realm,
  // and in a saved file it would point at somebody's own machine.
  federationDrawing(graph, look, params, label) {
    const { log, federationDiagram, queryWith } = this.deps;
    log.debug("Entering AdminConsole.federationDrawing().");
    const drawn = federationDiagram.render(graph, {
      resolve: look.resolve, links: true, id: 'fedmap', label: label
    });
    const html = '<div class="diagram">' + drawn.svg + '</div>' +
      this.note(drawn.width + '&times;' + drawn.height + ' &mdash; ' +
      '<a href="' +
      this.esc('/admin/federation/map' + queryWith(params, { format: 'svg' })) +
      '">the document on its own</a> (SVG, no links in it), or ' +
      '<a href="' +
      this.esc('/admin/federation/map' +
               queryWith(params, { format: 'json' })) +
      '">the graph as JSON</a>. It does not pan or zoom: it is generated on ' +
      'the server and arrives as markup, which is what keeps this console ' +
      'free of scripts. The SVG document does zoom.' +
      (drawn.failed ? ' <span class="state-revoked">The layout failed: ' +
        this.esc(drawn.failed) + '</span>' : ''));
    log.debug("Leaving AdminConsole.federationDrawing(). " + drawn.width + "x" +
              drawn.height +
              ".");
    return { drawn: drawn, html: html };
  }

  // THE KEY, drawn by the same render() the picture is, so that a legend cannot
  // come to describe a diagram this service no longer draws. Every swatch is a
  // one-node, one-edge graph put through the real code path — which is the
  // delegation page's rule and is worth the few extra bytes: a legend
  // hand-drawn out of the same colour constants would still go stale the day a
  // shape changed.
  federationMapKey() {
    const { log, federationDiagram } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.federationMapKey().");
    const shapes = [
      { kind: 'sts', label: 'This service',
        realm: 'default', realmName: 'this realm',
        what: 'The trust realm you are looking at. Every line on the picture ' +
              'starts or ends here, because every relationship is between ' +
              'this realm and somebody else.' },
      { kind: 'application', label: 'an application',
        what: 'An application registered HERE whose people are authenticated ' +
              'somewhere else. What points it at a partner is ' +
              '<code>appFederationRelationship</code> on its entry under ' +
              '<code>ou=applications</code>.' },
      { kind: 'partner-sp', label: 'a partner',
        what: 'A FOREIGN SERVICE PROVIDER. It asks this service to ' +
              'authenticate somebody. Dashed, because it is not this service ' +
              'and nothing here can see inside it.' },
      { kind: 'partner-idp', label: 'a partner',
        what: 'A FOREIGN IDENTITY PROVIDER. It authenticates the person and ' +
              'this service consumes what it issues. Dashed for the same ' +
              'reason — and a hexagon rather than a rectangle because it is ' +
              'an identity service, which is what this service is too.' }
    ];
    const shapeRows = shapes.map(function (one) {
      const drawn = federationDiagram.render(
        { nodes: [Object.assign({ id: 'k', relationships: [] }, one)],
          edges: [] },
        { links: false, id: 'key-' + one.kind, label: one.label });
      return '<tr><td>' + drawn.svg + '</td><td>' + one.what + '</td></tr>';
    }).join('');

    // THE LINES, AND THE FOUR STATES ARE THE LIST PAGE'S FOUR. Each is drawn by
    // handing render() a relationship in that state, so the colour in the key
    // is the colour edgeLook() will actually choose rather than a second
    // opinion about it.
    const states = [
      { what: '<strong>Ready</strong> — enabled and fully configured. It ' +
              'will work.',
        row: { id: 'ready', protocolLabel: 'SAML 2.0', enabled: true,
               ready: true,
               usable: true, missing: [], applicationCount: 0,
               authentications: 0,
               users: 0, releases: [], lastError: '', mechanismLabel: '' } },
      { what: '<strong>Disabled</strong> — which is how every relationship ' +
              'starts. This is the ordinary state of something somebody has ' +
              'not finished setting up, not a fault.',
        row: { id: 'disabled', protocolLabel: 'SAML 2.0', enabled: false,
               ready: true,
               usable: false, missing: [], applicationCount: 0, authentications:
                                                                  0,
               users: 0, releases: [], lastError: '', mechanismLabel: '' } },
      { what: '<strong>Enabled and NOT configured</strong> — the loud one, ' +
              'and it earns being the only red on this page: it will REFUSE ' +
              'at the moment somebody tries to use it, and it looks finished ' +
              'from every angle except this one.',
        row: { id: 'half', protocolLabel: 'SAML 2.0', enabled: true,
               ready: false,
               usable: false, missing: ['fedSigningCertificate'],
               applicationCount: 0, authentications: 0, users: 0, releases: [],
               lastError: '', mechanismLabel: '' } },
      { what: '<strong>A broker that cannot broker</strong> — the ' +
              'relationship is fine and the relationship it authenticates ' +
              'THROUGH is not, so the person meets the sign-in screen ' +
              'instead of the partner. That screen checks no password, which ' +
              'is why this is worth a colour of its own: it is the only ' +
              'failure here that produces a working sign-in.',
        row: { id: 'broker', protocolLabel: 'OpenID Connect', enabled: true,
               ready: true, usable: true, missing: [], applicationCount: 0,
               authentications: 0, users: 0, releases: [], lastError: '',
               mechanismLabel: 'Another federation relationship',
               brokersTo: 'somewhere', brokerUsable: false,
               brokerProblem: 'it is disabled' } }
    ];
    const stateRows = states.map(function (one, i) {
      const drawn = federationDiagram.render({
        nodes: [{ id: 'a', kind: 'application', label: 'from',
                  relationships: [] },
                { id: 'b', kind: 'sts', label: 'to', realm: '',
                  realmName: '' }],
        edges: [{ id: 'e', from: 'a', to: 'b',
                  relation: one.row.brokersTo ? 'asks' : 'signs-in',
                  relationship: one.row.id, row: one.row, use: null }]
      }, { links: false, id: 'key-state-' + i, label: 'a line' });
      // Only the line is wanted, not the two boxes it needs in order to exist,
      // so the swatch is the label panel's own words. It is drawn rather than
      // written because these four colours are the whole content of the key.
      return '<tr><td><svg xmlns="http://www.w3.org/2000/svg" width="120" ' +
        'height="26" viewBox="0 0 120 26" role="img"><title>' +
        self.esc(self.plainTextOf(one.what)) + '</title>' +
        drawn.svg.replace(/^[\s\S]*?<defs>/, '<defs>')
                 .replace(/<rect[\s\S]*$/, '') +
        '</svg></td><td>' + one.what + '</td></tr>';
    }).join('');

    log.debug("Leaving AdminConsole.federationMapKey().");
    return '<h3>The boxes</h3>' +
      '<table><tr><th>Drawn as</th><th>What it is</th></tr>' + shapeRows +
      '</table><h3>The ' +
      'lines</h3>' +
      this.note('<strong>An arrow is a REQUEST and not an ' +
      'assertion</strong>, which is the one thing about this picture that ' +
      'looks backwards until it is said. Everything on the left arrives ' +
      'wanting somebody signed in; everything on the right is asked to do ' +
      'the signing in. So an identity-provider-side relationship — where ' +
      'this service ASSERTS to the partner — points INWARD, because what the ' +
      'partner did was ask. Drawn the other way an identity broker is two ' +
      'arrows leaving the same box with nothing joining them; drawn this way ' +
      'it is one straight line through the middle, which is what a bridge ' +
      'is.') +
      '<table><tr><th>Colour</th><th>What it means</th></tr>' + stateRows +
      '</table>';
  }

  // ---------------------------------------------------------------------------
  // THE VIEW. The page and `?format=json` are built from ONE call to
  // `federationGraph.graph()`, so the picture, the tables under it and the API
  // answer cannot come to disagree about what is in it — `delegationView()`'s
  // rule one section up, and the failure it prevents is the quiet one: a filter
  // that narrowed the drawing and left the rows standing looks exactly like a
  // filter working.
  // ---------------------------------------------------------------------------
  federationMapView(req) {
    const { log, federationGraph } = this.deps;
    log.debug("Entering AdminConsole.federationMapView().");
    const wanted = {
      role: String(req.query.role || '').trim(),
      protocol: String(req.query.protocol || '').trim(),
      q: String(req.query.q || '').trim()
    };
    const graph = federationGraph.graph(wanted);
    log.debug("Leaving AdminConsole.federationMapView(). " +
              graph.relationships.length +
              " relationship(s).");
    return { wanted: wanted, graph: graph };
  }

  // One relationship's row in the table under the picture. It carries the two
  // numbers the picture was asked for and the ones a label had no room for, and
  // its first cell links back to the drill-down that configures it.
  federationMapRow(row) {
    const { log, queryWith } = this.deps;
    log.debug("Entering AdminConsole.federationMapRow().");
    const state = row.usable
      ? '<span class="ok">ready</span>'
      : !row.enabled
          ? '<span class="off">disabled</span>'
          : '<span class="bad">ENABLED, not configured</span>';
    log.debug("Leaving AdminConsole.federationMapRow().");
    return '<tr><td><a href="' +
      this.esc('/admin/federation' + queryWith({}, { relationship: row.id })) +
      '">' +
      this.esc(row.id) + '</a>' +
      (row.name !== row.id ? '<span class="sub">' + this.esc(row.name) +
       '</span>' :
       '') +
      '</td>' +
      '<td>' + this.esc(row.roleLabel) + '</td>' +
      '<td>' + this.esc(row.protocolLabel) + '</td>' +
      '<td class="who">' + this.esc(row.peer || row.application || '') +
        (!row.peer && !row.application
          ? '<span class="sub">nothing named yet</span>' : '') + '</td>' +
      '<td>' + state + '</td>' +
      // THE ANSWER TO "HOW MANY APPLICATIONS", and a dash where the question
      // does not apply rather than a zero: an identity-provider-side
      // relationship names exactly one application by construction, so `0`
      // there would be false and `1` would be a number nobody needs.
      '<td class="num">' + (row.role === 'service-provider'
        ? row.applicationCount
        : '<span class="state-none" title="An identity-provider-side ' +
          'relationship names exactly one application, in fedApplication. ' +
          'There is no count to make.">&mdash;</span>') + '</td>' +
      // ---------------------------------------------------------------------
      // THE TWO COUNTS, AND A BARE `0` IS REFUSED ON THE IDENTITY-PROVIDER
      // SIDE.
      //
      // `fedAuthentications` there is not a number that happens to be low: it
      // is a number NOTHING WRITES. What it counts is assertions CONSUMED and
      // that side issues them, which `federation/CLAUDE.md` states as a
      // deliberate non-goal. So printing it would assert that nobody has ever
      // signed in for this partner, in the same column that means exactly that
      // two rows up.
      //
      // Where the relationship BROKERS, there is a real number and it belongs
      // to the pair — the sign-ins happened and were counted against the
      // relationship they went through — so `brokeredUse` is printed instead,
      // marked as belonging to the onward relationship rather than to this one.
      // ---------------------------------------------------------------------
      (row.role === 'identity-provider'
        ? (row.brokeredUse
            ? '<td class="num">' + row.brokeredUse.users +
              '<span class="sub">via ' + this.esc(row.brokersTo) +
              '</span></td><td class="num">' + row.brokeredUse.authentications +
              '<span class="sub">via ' + this.esc(row.brokersTo) +
              '</span></td>'
            : '<td class="num"><span class="state-none" title="Nothing ' +
              'counts sign-ins on this side. What fedAuthentications counts ' +
              'is assertions CONSUMED, and an identity-provider-side ' +
              'relationship issues them — so the figure would be zero ' +
              'however busy the partner was.">&mdash;</span></td>' +
              '<td class="num"><span class="state-none" title="Nothing ' +
              'counts sign-ins on this side. What fedAuthentications counts ' +
              'is assertions CONSUMED, and an identity-provider-side ' +
              'relationship issues them — so the figure would be zero ' +
              'however busy the partner was.">&mdash;</span></td>')
        : '<td class="num">' + row.users + '</td>' +
          '<td class="num">' + row.authentications + '</td>') +
      // THE AUTHENTICATION METHOD, WHICH IS THE IDENTITY-PROVIDER SIDE'S
      // COLUMN. An unset mechanism is printed as what it MEANS rather than left
      // blank: it is the sign-in screen, which is a decision and the commonest
      // one.
      '<td>' + (row.role === 'identity-provider'
        ? (row.brokersTo
            ? 'through <a href="' +
              this.esc('/admin/federation' +
                       queryWith({}, { relationship: row.brokersTo })) +
              '">' + this.esc(row.brokersTo) + '</a>' +
              (row.brokerUsable ? ''
                : '<span class="sub bad">' + this.esc(row.brokerProblem ||
                    'that relationship is not usable, so the sign-in screen ' +
                    'is drawn instead') + '</span>')
            : this.esc(row.mechanismLabel) +
              (row.mechanismKnown ? ''
                : '<span class="sub bad">not a mechanism this service ' +
                  'has</span>'))
        : '<span class="state-none" title="The authentication happens at the ' +
          'PARTNER on a service-provider-side relationship, so there is ' +
          'nothing here to configure.">&mdash;</span>') + '</td>' +
      '</tr>';
  }

  // THE ROUTES, registered in the order they always were: the composition
  // root (`common/protocol_stack.ts`) calls this through the export at the
  // foot of the file, at 18, where requiring this module used to register
  // them (#50, R1) — the file header says why the transitional call sat where
  // the LAST of them was rather than the first — so the route order is
  // unchanged (rule 1).
  registerRoutes(app: RouteApp): void {
    const { log, oidcRp, mode, gateStateFor, rbac, realms, errorCodes,
            LOGIN_PATH, loginRealmChooser, websecurity, parseBody, adminScope,
            accessGate, validation, endSessionById, clearSessionCookie, stats,
            baseUrlOf, sessions, persistence, metricsJson, MAX_ROWS,
            tokenAction, sessionsView, queryWith, config, sessionsAction,
            tokensView, tokenSetView, knownUserKeys, credentialGraph,
            delegation, userGraph, logoutAction, auditView, auditLog,
            usedAssertionsView, errorCodesView, delegationView, permissionsView,
            pageParamsOf, pagedRows, DELEGATION_PER_PAGE, pagingJson,
            permissionsAction, queryOne, clusterSummary, appPermissions,
            permissionGroupsView, applications, createClaims, truthy,
            userFieldsFrom, applicationsAction, resourceMetadata,
            multipartParts, asAction, saml2Action, saml11Action, consentView,
            consentAction, rolesRegister, ROLE_MEMBER_KINDS, issuanceGate,
            rolesPreview, ROLE_ACTIONS, rolesAction, adminViews, pagingOf,
            adminActions, rbacAction, claimsAction, claimsPreviewUser,
            claimAttributes, claimsJson, samlAttributesJson,
            claimsRequestParameter, claimsRequestPreview, userFor,
            userinfoClaimsJson, vcAction, vcPreviewUser, vcJson, vpConfigAction,
            vpConfig, vpConfigJson, loadHelpers, realmsAction, configAction,
            tokenLifetimesJson, tokenLifetimesAction, samlAssertionsJson,
            samlAssertionRowFor, SAML_ASSERTION_SETTINGS, samlAssertionSeconds,
            samlAssertionsAction, scimJson, scimMonitorJson, signals,
            signalsAction, signalsJson, ssfDeadLettersJson, ssfJson, ssfAction,
            caepJson, caepAction, caepSessionsState, caepApplicationsState,
            riscJson, riscAction, riscAccountsState, riscApplicationsState,
            spiffeAction, spiffeEntriesAction, spiffeAgentsAction,
            federationAction, federationDiagram, federation } = this.deps;
    const self = this;
    log.debug("Entering AdminConsole.registerRoutes().");
    // -------------------------------------------------------------------------
    // THE CONSOLE RENEWS ITS OWN TOKENS BEFORE THE GATE READS ITS SESSION
    // (2026-09-12).
    //
    // A console session holds the ID Token, access token and refresh token its
    // sign-in was issued, and when the first two run out this middleware
    // redeems the refresh token and writes the new tokens onto THE SAME SESSION
    // — so the gate below finds the operator still signed in and the page they
    // asked for is drawn, where it used to send them through the sign-in screen
    // an hour after they signed in. `common/oidc_rp.ts`'s section 4 argues all
    // of it.
    //
    // **ABOVE THE GATE, AND IT HAS TO BE.** Rule 1: middleware applies only to
    // routes added after it, and the gate is what reads the session. Registered
    // below it, a renewal would happen after the gate had already sent the
    // browser away. It answers no request itself: a renewal that could not
    // happen ends the session and leaves the gate to do what it does for a
    // request with none.
    // -------------------------------------------------------------------------
    app.use('/admin', oidcRp.renewal('admin'));

    app.use('/admin', function (req, res, next) {
      log.debug("Entering the admin console gate. " + req.method + " " +
                req.originalUrl);
      // -----------------------------------------------------------------------
      // THE ONE PATH UNDER /admin THAT THIS GATE MUST NOT GUARD (2026-09-06).
      //
      // `/admin/callback` is where the authorization endpoint sends the browser
      // back with a code, and a person arriving there has BY DEFINITION no
      // console session yet — that is what they are about to get. Guarded, it
      // would send them to `/oauth2/authorize` again, which would send them
      // back here, which would send them again: a redirect loop that looks
      // exactly like a broken sign-in and is the single most likely way to get
      // this move wrong.
      //
      // **IT IS AN EXEMPTION IN THE GATE AND NOT A ROUTE REGISTERED ABOVE IT**,
      // and that is deliberate. The gate's whole mechanism is that it is
      // registered above every route in this file, so that a console page added
      // tomorrow is guarded by construction (rule 1); a route above the gate
      // would break that invariant for every reader who came after, and the
      // next person to add one would have no way to know which side of the line
      // they were on. One named path, checked here, keeps "everything below is
      // guarded" true.
      //
      // **IT IS NOT A HOLE.** What that route does with an unauthenticated
      // request is redeem a code against a state THIS SERVICE minted, with a
      // client secret, and refuse everything else — see `oidc_rp.js`'s
      // callback, where every refusal is reported rather than redirected. It
      // grants nothing on its own.
      //
      // `req.path` and not `originalUrl`: the realm prefix is already stripped
      // by `app.js`'s first middleware, and the query string must not be part
      // of the comparison — `/admin/callback?code=…` is this path.
      if (req.path === '/callback') {
        log.debug("Leaving the admin console gate. The OIDC callback is " +
                  "exempt: somebody arriving there has no session yet, by " +
                  "definition.");
        next();
        return;
      }
      // -----------------------------------------------------------------------
      // THE SECOND PATH UNDER /admin THAT THIS GATE MUST NOT GUARD
      // (2026-09-10), AND IT IS THE SAME SHAPE OF EXEMPTION AS THE ONE ABOVE
      // RATHER THAN A NEW KIND.
      //
      // This console is a Shared Signals RECEIVER now — `ssf/ssf_receivers.ts`
      // argues the whole of it — and `/admin/signals/receive` is where its own
      // stream's Security Event Tokens are POSTed. A push carries NO CONSOLE
      // SESSION by construction: it is a server-to-server request and must not
      // present a browser's credentials, which is the same thing the OIDC back
      // channel says about itself. Guarded, this endpoint could only ever
      // refuse every delivery, and the symptom would be an inbox page that
      // stays empty while the stream's log fills with 401s.
      //
      // **IT IS NOT A HOLE, AND THE REASON IS NOT "IT IS ONLY A READ".** It is
      // a write — it puts a row in this realm's inbox. What guards it is the
      // stream's own `delivery.authorization_header`: a bearer token minted per
      // stream and per start, compared in constant time, that nothing but this
      // service's own transmitter is ever given. `accept()` refuses without it,
      // refuses a SET addressed to another audience, and records both. So the
      // check moved rather than went away — which is the test to hold a third
      // exemption to, if one is ever proposed.
      //
      // **AND IT IS EXEMPT FROM THE CSRF CHECK FOR THE SAME REASON**, which is
      // the half the sign-out exemption below deliberately does NOT take. A
      // CSRF token is a defence for a form submitted by a browser holding a
      // cookie; there is no browser here and no cookie, and a token would be a
      // secret this service would have to hand its own transmitter in addition
      // to the one it already checks.
      //
      // It is an exemption IN the gate and not a route registered above it, for
      // `/callback`'s reason: the gate is registered above every route in this
      // file so that a console page added tomorrow is guarded by construction
      // (rule 1), and a route above it would break that invariant for every
      // reader who came after.
      // -----------------------------------------------------------------------
      if (req.path === '/signals/receive') {
        log.debug("Leaving the admin console gate. The Shared Signals " +
                  "receive endpoint is exempt: a push carries the stream's " +
                  "own bearer token and no session. See ssf/ssf_receivers.ts.");
        next();
        return;
      }
      if (!mode.gatesConsole()) {
        log.debug("Leaving the admin console gate. The gate is off; " +
                  "everything is allowed. UNREACHABLE since 2026-09-06: " +
                  "mode.gatesConsole() is unconditional.");
        next();
        return;
      }

      let state = gateStateFor(req);
      // THE BOOTSTRAP ADMINISTRATOR'S ARRIVAL (2026-09-13) closes the window in
      // which every signed-in person may use this console — see admin_rbac.js's
      // noteConsoleSignIn(). Asked before the decision below, so the request
      // that closes it is already decided under the enforced roster.
      if (state.session &&
          rbac.noteConsoleSignIn(state.username, state.session,
                                 realms.DEFAULT_ID)) {
        state = gateStateFor(req);
      }

      if (!state.session) {
        if (self.wantsJson(req)) {
          errorCodes.mark(res, 'STS-ADMIN-0003');
          self.refuse(req, res, 401, 'login_required',
                      'Sign in first.',
                      'This console needs a browser sign-on session from the ' +
                      'DEFAULT realm\'s ' + LOGIN_PATH + ', and this request ' +
                      'carries none. It has to be that realm\'s: a trust ' +
                      'realm has its own directory and its own groups, and ' +
                      'the two roles this console decides from are groups in ' +
                      'the default realm\'s ou=groups only. The session is a ' +
                      'cookie, so a program driving this console has to sign ' +
                      'in at that screen and keep it — or use /admin-api, ' +
                      'which takes an access token of its own rather than a ' +
                      'session.',
                      { needed: 'session', signIn: LOGIN_PATH,
                        signInRealm: realms.DEFAULT_ID });
          log.debug("Leaving the admin console gate. Refused 401 to a JSON " +
                    "caller.");
          return;
        }
        // GET and HEAD only. A POST with no session is NOT redirected: the
        // browser would repeat it as a GET after signing in — a 303 turns the
        // method into GET by definition — and the form's fields would be gone,
        // so "revoke everything" would come back as a page view of the tokens
        // list with the click silently discarded. Better to say what happened
        // and let them post it again.
        if (req.method === 'GET' || req.method === 'HEAD') {
          // WHICH REALM FIRST (2026-09-14, #32), for a bare /admin on a service
          // with realms defined: `common/realm_chooser.ts` says when, and a
          // choice is a redirect to that realm's own console.
          const choice = loginRealmChooser.decide(req, 'admin');
          if (choice && choice.kind === 'redirect') {
            res.set('Cache-Control', 'no-store').redirect(303, choice.location);
            log.debug("Leaving the admin console gate. To the chosen realm.");
            return;
          }
          if (choice && choice.kind === 'page') {
            if (choice.error) {
              errorCodes.mark(res, 'STS-ADMIN-0790');
            }
            res.status(choice.error ? 400 : 200)
              .set('Cache-Control', 'no-store')
               .type('text/html').send(self.page('Choose your realm', null,
                 '<div class="card"><h2>Choose your realm</h2>' +
                 loginRealmChooser.form(req, 'admin', choice.error) + '</div>',
                 null, null, req));
            log.debug("Leaving the admin console gate. The realm chooser.");
            return;
          }
          self.sendToConsoleSignIn(req, res);
          log.debug("Leaving the admin console gate. Sent to the sign-in " +
                    "screen.");
          return;
        }
        errorCodes.mark(res, 'STS-ADMIN-0003');
        self.refuse(req, res, 401, 'login_required', 'Sign in first.',
                    'This form needs a browser sign-on session and this ' +
                    'request carries none — the session it was posted with ' +
                    'has probably expired.',
                    { needed: 'session', signIn: LOGIN_PATH,
                      signInRealm: realms.DEFAULT_ID,
                      // AN ABSOLUTE URL, and that is not decoration. `app.js`
                      // rewrites every root-relative href in an HTML response
                      // to carry the realm being read, so `href="/admin"` would
                      // send this reader to THIS realm's console — where,
                      // before 2026-09-06, they would sign in successfully and
                      // still be refused, because the gate accepted the default
                      // realm's session only. (The console's own session now
                      // lives in the default realm whichever realm the flow
                      // runs in, and the roster asked is the realm signed in
                      // through — 8d in admin-ui/CLAUDE.md.) An absolute URL is
                      // not matched by that rewrite, which is what makes it the
                      // right shape here rather than a way around it.
                      //
                      // **AND THE LINK IS OUTSIDE THE `note()`, WHICH IT WAS
                      // NOT UNTIL 2026-09-06.** A note longer than about a line
                      // folds itself and its `<summary>` is the opening
                      // sentence rendered as PLAIN TEXT — so a note that OPENS
                      // with an anchor has that anchor stripped into the
                      // summary, and the only way off this page was a piece of
                      // text that looked like a link and was not. That is
                      // exactly the rule `bullet()` already enforces for a list
                      // item ("a row whose POINT is the link is never folded"),
                      // and `note()` has no such rule because folding is
                      // decided on the rendered text rather than on what is in
                      // it. The rule to take from it: **prose that carries the
                      // only control on the page does not go inside a fold.**
                      // The explanation still does, which is what a fold is
                      // for.
                      html: '<p><a href="' +
                            self.esc(self.defaultRealmSignInUrl(req)) +
                            '">Sign in to the default realm</a>, then post ' +
                            'the form again.</p>' +
                            self.note('It is not resubmitted for you: a ' +
                            'redirect after a POST becomes a GET and the ' +
                            'fields would be lost, so a control you did not ' +
                            'press twice would not fire twice.') });
        log.debug("Leaving the admin console gate. Refused 401 to a form " +
                  "POST.");
        return;
      }

      // -----------------------------------------------------------------------
      // SIGNING YOURSELF OUT NEEDS A SESSION AND NOTHING ELSE (2026-09-06).
      //
      // The Sign out button is on every page of this console, which means it is
      // on the pages of everybody who can reach one — including a person who
      // holds **Admin Read** and not Write, and including the one the roles
      // refuse entirely, who still gets a refusal page drawn in this shell with
      // the button on it. Under the rule below every one of them would be
      // refused: it is a POST, so it would need Write.
      //
      // **A SIGN-OUT IS NOT A WRITE TO THIS SERVICE.** It changes no
      // configuration, no registry and nobody else's state; it ends the session
      // of the person pressing it, which is the one act on this console that
      // needs no permission because the alternative is a console somebody can
      // enter and cannot leave.
      //
      // WHAT IS NOT SKIPPED IS THE CSRF TOKEN. A sign-out fired from another
      // site is a real nuisance — it is the classic example of a "harmless"
      // CSRF that is not — so the check below runs here too, in front of the
      // exemption rather than after it. What is skipped is the ROLE and the
      // POLICY, and only those.
      //
      // It is an exemption IN the gate, like `/callback` above and for the same
      // reason: a route registered above the gate would break "everything below
      // is guarded" for every reader who came after.
      // -----------------------------------------------------------------------
      if (req.path === SIGNOUT_PATH.replace(/^\/admin/, '')) {
        const token = websecurity.checkCsrf(state.session.id, parseBody(req));
        if (!token.ok) {
          log.warn('admin console: a sign-out was refused on CSRF (' +
                   token.reason + '), signed in as ' + state.username + '.');
          errorCodes.mark(res, 'STS-ADMIN-0004');
          self.refuse(req, res, 403, 'csrf',
                      'That form did not come from this console.',
                      token.detail,
                      { html: self.note('The ordinary cause is a page left ' +
                        'open across a sign-out and then submitted — you are ' +
                        'probably signed out already. <a href="/admin">Open ' +
                        'the console</a> to find out.') });
          log.debug("Leaving the admin console gate. The sign-out was " +
                    "refused on CSRF.");
          return;
        }
        log.debug("Leaving the admin console gate. The sign-out is exempt " +
                  "from the role check: ending your own session needs no " +
                  "permission.");
        next();
        return;
      }

      // WRITE for anything that is not a read. Every control on this console is
      // a POST — there is no script here, so nothing uses any other method —
      // and the test is written as "not GET and not HEAD" rather than "is POST"
      // so that a method added later is refused by default rather than allowed
      // by omission.
      const needsWrite = req.method !== 'GET' && req.method !== 'HEAD';

      // ---------------------------------------------------------------------
      // A REALM ADMINISTRATOR IS CONFINED TO THEIR REALM (2026-09-14, #32).
      //
      // Two refusals the role check below cannot make, because it asks WHETHER
      // somebody holds a role and not WHERE the role holds. Signed in through a
      // realm, they hold nothing in any other realm — `gateStateFor()` has
      // already said so as `outsideRealm` — and in their own realm they still
      // may not reach what belongs to the whole service, which `admin_scope.js`
      // decides. Both are answered before the role check so that the page says
      // the true reason rather than "you hold no role".
      // ---------------------------------------------------------------------
      if (state.outsideRealm) {
        errorCodes.mark(res, 'STS-ADMIN-0786');
        const home = realms.get(state.identityRealm);
        self.refuse(req, res, 403, 'outside_realm',
                    'You administer another realm.',
                    'Signed in as ' + state.username + ' through the "' +
                    state.identityRealm + '" realm, whose administrators ' +
                    'administer that realm only. This console is being read ' +
                    'in the "' +
                    realms.currentId() + '" realm.',
                    { identityRealm: state.identityRealm,
                      html: self.note((home
                        ? '<a href="' +
                          self.esc(self.realmRoot(req) + realms.prefixOf(home) +
                          '/admin') + '">Open the "' + self.esc(home.id) +
                          '" realm\'s console</a>, or sign out'
                        : 'Sign out') + ' and sign in through the default ' +
                        'realm as a service administrator to read this ' +
                        'realm.') });
        log.debug("Leaving the admin console gate. A realm administrator " +
                  "outside their realm.");
        return;
      }
      const scoped = adminScope.refusalFor(state, '/admin' + req.path,
                                           needsWrite ? parseBody(req) : null,
                                           req.query);
      if (scoped) {
        errorCodes.mark(res, scoped.code);
        // error-code: none — the code is scoped.code, marked on the line above
        self.refuse(req, res, 403, scoped.reason,
                    'That belongs to the whole service.', scoped.detail,
                    { identityRealm: state.identityRealm,
                      settings: scoped.settings || undefined,
                      html: self.note('Signed in as ' +
                                      self.esc(state.username) +
                        ' through the "' + self.esc(state.identityRealm) +
                        '" realm. ' +
                        'A realm\'s ' +
                        'administrators administer that realm; the store, ' +
                        'the listeners, the service Root, process-wide ' +
                        'settings and the realm registry belong to the ' +
                        'service administrators of the default realm.') });
        log.debug("Leaving the admin console gate. Service scope refused to " +
                  "a realm administrator: " + scoped.reason + ".");
        return;
      }

      if (needsWrite ? state.write : state.read) {
        // ---------------------------------------------------------------------
        // AND THE CSRF TOKEN, ON THE WAY THROUGH (2026-09-06). OWASP A01/A08.
        //
        // **HERE, BECAUSE THIS IS ALREADY THE ONE PLACE EVERY NON-GET ON THIS
        // CONSOLE PASSES THROUGH.** The same argument the role check makes for
        // itself two lines up: sixty pages and a hundred and forty forms, and a
        // check per handler is a hundred and forty chances to add the
        // hundred-and-forty-first without one. `withCsrf()` puts the token INTO
        // every form this console draws, so the two halves cannot drift — a
        // page author does neither.
        //
        // A JSON caller is refused the same way. `/admin` answers JSON to
        // `?format=json` and a POST from a script is exactly what CSRF is not
        // about — but the token is cheap to send and letting `Content-Type:
        // application/json` past would make the header the bypass.
        if (needsWrite) {
          const token = websecurity.checkCsrf(state.session ? state.session.id :
                                              '',
                                              parseBody(req));
          if (!token.ok) {
            log.warn('admin console: a ' + req.method + ' to ' +
                     (req.originalUrl || req.url) + ' was refused on CSRF (' +
                     token.reason + '), signed in as ' + state.username + '.');
            errorCodes.mark(res, 'STS-ADMIN-0005');
            self.refuse(req, res, 403, 'csrf',
                        'That form did not come from this console.',
                        token.detail,
                        { html: self.note('Every form this console draws ' +
                          'carries a one-time-per-session token, and this ' +
                          'request did not have the right one. The ordinary ' +
                          'cause is a page left open across a sign-out and ' +
                          'then submitted — sign in again and retry. The ' +
                          'other cause is the one this check exists for: a ' +
                          'request assembled somewhere else and sent using ' +
                          'your browser\'s session.') });
            log.debug("Leaving the admin console gate. Refused on CSRF.");
            return;
          }
        }
        // ---------------------------------------------------------------------
        // AND THE POLICY (2026-09-06). OWASP A01, decided by the PDP rather
        // than by this file.
        //
        // **THE ROLE CHECK ABOVE STAYS AND IS NOT REPLACED.** `admin_rbac.js`
        // decides WHAT roles this person holds — out of the two groups of the
        // realm they signed in through, the default realm's being the service
        // roster, and `admin_scope.js` above has already refused a realm
        // administrator anything outside their realm. This asks whether the
        // policy permits them, given those roles, to do this to this resource.
        // The two are different questions and the second is the one an operator
        // can change without a deployment.
        //
        // The SUBJECT is the session's person — `state.username` comes from
        // `consoleRpSession(req)` and from nothing in the request — and their
        // directory entry is available to the policy through the PIP, so a rule
        // can be written against anything the entry holds.
        //
        // On an unedited service this permits exactly what the roles already
        // permitted: the console requires no role in the application registry,
        // so the policy's "requires nothing" arm answers. What it buys is that
        // narrowing the console is now editing an entry and writing a rule.
        const policy = accessGate.check({
          resource: accessGate.RESOURCE.CONSOLE,
          action: needsWrite ? accessGate.ACTION.WRITE : accessGate.ACTION.READ,
          subject: { name: state.username,
                     authenticated: state.session
                       ? state.session.authenticated !== false : false,
                     roles: state.roles.slice(0),
                     sessionId: state.session ? state.session.id : '' },
          context: { method: req.method, path: req.originalUrl || req.url }
        });
        if (!policy.allowed) {
          log.info('admin console: the access policy refused ' + req.method +
                   ' ' +
                   (req.originalUrl || req.url) + ' for ' + state.username +
                   '. ' +
                   policy.why);
          errorCodes.mark(res, 'STS-ADMIN-0006');
          self.refuse(req, res, 403, 'policy_denied',
                      'The access policy does not permit this.',
                      policy.why,
                      { decision: policy.decision, policy: policy.policy,
                        html: self.note('This is a XACML decision rather ' +
                          'than a role check — the roles you hold were put ' +
                          'INTO the request and the policy decided. <a ' +
                          'href="/admin/xacml/policies">The policies</a> are ' +
                          'where it is written, and <a ' +
                          'href="/admin/xacml/decide">the decision page</a> ' +
                          'will reproduce this exact question against the ' +
                          'same attribute source.') });
          log.debug("Leaving the admin console gate. Refused by policy.");
          return;
        }
        log.debug("Leaving the admin console gate. Allowed: " + state.username +
            " " +
            "holds " +
                  (state.roles.join(', ') || '(the empty roster)') + ".");
        next();
        return;
      }

      const group = needsWrite ? state.writeGroup : state.readGroup;
      const role = rbac.roleFor(needsWrite ? 'write' : 'read');
      errorCodes.mark(res, needsWrite ? 'STS-ADMIN-0008' : 'STS-ADMIN-0007');
      self.refuse(req, res, 403, 'insufficient_role',
                  'You are signed in and that is not enough.',
                  'Signed in as ' + state.username + ', holding ' +
                  (state.roles.length ? state.roles.join(' and ') :
                   'no console role') +
                  '. ' +
                  (needsWrite ? 'Posting a form on this console' :
                   'Reading this console') +
                  ' needs ' + role.label + ', which is membership of cn=' +
                  group + '.',
                  { needed: needsWrite ? 'write' : 'read', group: group,
                    roles: state.roles,
                    html: self.note('The role is an ordinary group in the ' +
                          'embedded LDAP directory, and there are four doors ' +
                          'onto it: the <a href="/admin/rbac">Admin ' +
                          'roles</a> screen (which you also need a role to ' +
                          'use), <code>POST /admin-api/rbac/grant</code> ' +
                          '(the management API is NOT gated, so this is the ' +
                          'one that works from here), an ' +
                          '<code>ldapmodify</code> on 389 or 636, and a SCIM ' +
                          'PATCH of the group.') +
                          self.note('Nothing else about this service is ' +
                          'affected. Every protocol endpoint is exactly as ' +
                          'open as it was — these two groups grant this ' +
                          'console and nothing else.') });
      log.debug("Leaving the admin console gate. Refused 403 to " +
                state.username +
                ".");
    });

    app.use('/admin', function (req, res, next) {
      log.debug("Entering the console query check.");
      const asked = validation.check(req, 'query', CONSOLE_QUERY);
      if (!asked.ok) {
        log.debug("Leaving the console query check. Refused: " + asked.code +
                  ' on "' + asked.field + '".');
        // The console answers a person in a browser, so the refusal is a page
        // in this console's own shell rather than a JSON body nobody here would
        // see. 400 and never 403: nothing was refused about WHO is asking.
        errorCodes.mark(res, 'STS-ADMIN-0009');
        res.status(400).type('text/html').send(self.page('Bad request', null,
          '<div class="card"><h2>Bad request</h2><p>' + self.esc(asked.detail) +
          '</p></div>', null, null, req));
        return undefined;
      }
      log.debug("Leaving the console query check. Accepted.");
      return next();
    });

    // =========================================================================
    // THE REDIRECT URI. WHERE THE AUTHORIZATION ENDPOINT SENDS THE BROWSER
    // BACK.
    //
    // It is the first route in this file so that it sits directly under the
    // exemption in the gate above that names it — the two are one arrangement
    // and a reader meeting one has to be able to find the other.
    //
    // **THIS ROUTE IS THE CONSOLE ACTING AS AN ORDINARY CLIENT.** Everything it
    // does is in `common/oidc_rp.ts`: spend the state, redeem the code with the
    // client secret, verify the ID Token against the published JWKS, and
    // establish this console's own session. What is HERE is only what belongs
    // to this console — where to go afterwards, and what a refusal looks like
    // in this console's shell.
    //
    // A refusal is DRAWN and never redirected, which is `federation_sp.ts`'s
    // decision 6 and holds for its reason: the person's sign-in has already
    // succeeded at the authorization endpoint, so the only interesting question
    // is what this side disliked about the answer, and a redirect throws that
    // away. It is drawn through `page()` with NO gate state, because there is
    // no session — `gateStateFor()` answers that honestly and the banner says
    // so.
    // =========================================================================
    app.get('/admin/callback', function (req, res) {
      log.debug("Entering the admin console's OIDC callback.");
      oidcRp.handleCallback(req, res, 'admin').then(function (answer) {
        if (!answer.ok) {
          log.info('admin: a console sign-in did not complete. ' + answer.why);
          const inner = '<div class="err"><strong>Signing in did not ' +
                        'complete.</strong> ' +
            self.esc(answer.why) + '</div>' +
            self.note('This console signs people in through this service\'s ' +
                      'own authorization server, as the registered client ' +
                      '<code>sts-admin-console</code> — the same ' +
                      'authorization code flow any other application would ' +
                      'use. What failed above is one step of that flow, ' +
                      'named exactly rather than reported as &ldquo;sign-in ' +
                      'failed&rdquo;. <a href="/admin">Start again</a>, and ' +
                      'if it keeps happening, <code>/admin-api</code> is not ' +
                      'gated and can do everything this console can.') +
            self.note('The client this console signs in as is an ORDINARY ' +
                      'entry in the registry: <a ' +
                      'href="/admin/applications?application=' +
                      'sts-admin-console">' +
                      'sts-admin-console</a> under ' +
                      '<code>ou=applications</code>. If it has been deleted ' +
                      'or edited — its redirect URI, its secret — this is ' +
                      'where that shows up.');
          errorCodes.mark(res, answer.errorCode || errorCodes.codeOf(answer) ||
                              errorCodes.codeOf(res) || 'STS-ADMIN-0010');
          res.status(400).type('text/html').set('Cache-Control', 'no-store')
             .send(self.page('Signing in did not complete', '', inner, null,
                             gateStateFor(req), req));
          log.debug("Leaving the admin console's OIDC callback. Refused.");
          return;
        }
        // 303 so the reload after it is a GET, and to the page they asked for
        // BEFORE any of this started — which `oidc_rp.js` has been holding
        // server-side since the flow began, precisely so that no return address
        // ever rides in a parameter a caller could write.
        res.status(303).set('Cache-Control', 'no-store')
           .set('Location', answer.returnTo || '/admin').end();
        log.debug("Leaving the admin console's OIDC callback. Signed in as " +
                  answer.username + ".");
      }).catch(function (e) {
        // A rejection here is a bug in this repository rather than anything a
        // request can cause — `handleCallback()` resolves its refusals — so it
        // is reported as one rather than swallowed into a generic failure.
        log.error(errorCodes.tag('STS-ADMIN-0011') +
                  "The admin console's OIDC callback threw: " +
                  (e.stack || e.message));
        errorCodes.mark(res, 'STS-ADMIN-0011');
        res.status(500).type('text/html').set('Cache-Control', 'no-store')
           .send(self.page('Signing in did not complete', '',
                           '<div class="err"><strong>Signing in did not ' +
                           'complete.</strong> ' +
                           self.esc(e.message) + '</div>', null,
                           gateStateFor(req), req));
      });
    });

    // =========================================================================
    // SIGNING OUT. THE OTHER END OF THE ROUTE ABOVE (2026-09-06).
    //
    // Registered beside the callback deliberately: those two routes are a
    // session's beginning and its end, and a reader who has just understood how
    // somebody gets in should not have to search for how they get out.
    //
    // **IT ENDS TWO SESSIONS AND THAT IS THE WHOLE DESIGN DECISION.** This
    // console holds a relying-party session of its own (`oidc_rp.js`), derived
    // from the SIGN-ON session the person has with this service's authorization
    // server. Ending only the console's would be a Sign out button that does
    // not sign anybody out: the next request to `/admin` runs the authorization
    // code flow, meets the sign-on session that is still live, gets an ID Token
    // back without prompting for anything, and draws the console again. The
    // person pressing the button would see the console they just left and
    // conclude the button is broken — and they would be right.
    //
    // So the console session goes first (which clears this surface's cookie)
    // and then the sign-on session behind it. `authn.js`'s `dropSession()`
    // cascades to every session derived from that one, so a person who is also
    // signed in to `/portal` is signed out there too. **THE PAGE BELOW SAYS
    // SO**: a sign-out that quietly reaches further than the button's label is
    // the same defect as one that does not reach far enough.
    //
    // **WHAT IT IS NOT is `/logout`.** That endpoint ends everything an
    // identity holds in every protocol — tokens, tickets, artifacts, LDAP binds
    // — and it is linked from this console in half a dozen places for exactly
    // that. This one is the browser's sign-out, which is what a Sign out button
    // in the corner of a page means everywhere else, and the page below points
    // at the other for somebody who wants it. `/logout` also refuses when there
    // is no SIGN-ON session to act on, which would leave somebody holding a
    // live console session looking at a 401 that says there is nobody to sign
    // out.
    //
    // **AND IT MIRRORS NO `/admin-api` OPERATION**, which is the one place this
    // console departs from rule 7 rather than paying it. That rule exists so
    // that every control here can be driven by a program; this control ends the
    // session of the browser that pressed it, and `/admin-api` authenticates
    // with an access token rather than a session (since 2026-09-09) and is
    // sessionless — an operation there would have nothing to end.
    // `tests/vendored/sts_admin_console.js` carries the exemption with that
    // sentence in it, so the check that reads the API's index for "every
    // console POST reaches a mirrored route" does not go quietly amber.
    // =========================================================================
    app.post(SIGNOUT_PATH, function (req, res) {
      log.debug("Entering the admin console sign-out.");
      // The gate above has already established that there IS a console session
      // and that the form carried this session's CSRF token, so what is left
      // here is the ending itself.
      const rp = oidcRp.sessionFor(req, 'admin');
      const parent = rp ? String(rp.derivedFrom || '') : '';
      const username = rp ? rp.user.username : '';
      oidcRp.endSessionFor(req, res, 'admin', 'the Sign out button on the ' +
                                              'admin console');
      const signOnEnded = parent
        ? !!endSessionById(parent, 'the Sign out button on the admin console')
        : false;
      // AND THE SIGN-ON COOKIE, because `endSessionById()` has no response to
      // clear one on — it is the door /logout uses to end sessions that are not
      // the caller's. Leaving it would have the browser presenting a cookie
      // naming a session this service no longer holds: refused everywhere, and
      // confusing wherever anybody looks at what the browser is carrying.
      if (signOnEnded) {
        clearSessionCookie(res);
      }
      log.info('admin console: ' + (username || 'somebody') + ' signed out. ' +
               'The console session is gone' +
               (signOnEnded ? ' and so is the sign-on session behind it (' +
                              parent + '), with every session derived from it.'
                            : '; there was no sign-on session left to end.'));
      const inner =
        '<div class="ok"><strong>You are signed out' +
        (username ? ', ' + self.esc(username) : '') + '.</strong> This ' +
        'console session has ended' + (signOnEnded
          ? ', and so has the sign-on session it was built on — so anything ' +
            'else that was signed in through it, the user portal included, ' +
            'is signed out too.'
          : '. There was no sign-on session left behind it to end.') +
            '</div>' +
        self.note('<strong>Signing out of the console alone would not have ' +
        'signed you out.</strong> This console is an ordinary OpenID Connect ' +
        'client of this service — <code>sts-admin-console</code> in the ' +
        'registry — so the next page would have run the code flow again, met ' +
        'the sign-on session, and let you back in with nothing to type. That ' +
        'is why both ended.') +
        self.note('What is NOT ended here is everything else this identity ' +
        'holds in every other protocol: access and refresh tokens, Kerberos ' +
        'tickets, credential offers, LDAP binds. <a ' +
        'href="/logout">/logout</a> is the protocol-independent sign-out ' +
        'that lists all of it and ends what you choose.') +
        '<p><a class="btn" href="/admin">Sign in again</a></p>';
      // DRAWN THROUGH page() WITH THE GATE STATE READ AGAIN, which now reports
      // nobody signed in — so the shell draws its "not signed in" banner and
      // takes away everything that would need a session: the account menu, the
      // Refresh link (this page is the answer to a POST, so it has no GET to
      // reload) and THE WHOLE NAVIGATION COLUMN. That is the page telling the
      // truth about the state it is in rather than this handler asserting it.
      //
      // **THE COLUMN WENT ON 2026-09-10 AND THE OTHER TWO WERE ALREADY RIGHT**,
      // which is what made it hard to see: the button was conditional from the
      // day this route was written, so the corner of the page was honest while
      // forty links down the left-hand side were not — every one of them a
      // console page behind the gate, and the realm switcher among them a FORM
      // posting to a console with no session for it. `sideColumn()` has the
      // argument; `tests/vendored/sts_admin_console.js` asserts it beside the
      // button it sits next to.
      res.status(200).type('text/html').set('Cache-Control', 'no-store')
         .send(self.page('Signed out', '', inner, null, gateStateFor(req),
                         req));
      log.debug("Leaving the admin console sign-out.");
    });

    app.get('/admin', function (req, res) {
      log.debug("Entering the admin console index.");
      const snap = stats.snapshot();
      const base = baseUrlOf(req);
      const inner = self.messagesOf(req) +
        '<div class="tiles">' +
          self.tile(snap.calls.total, 'endpoint calls') +
          self.tile(snap.tokens.held, 'tokens issued') +
          self.tile(snap.tokens.revoked, 'tokens revoked') +
          self.tile(snap.artifacts.held, 'other artifacts') +
          self.tile(snap.users.known, 'users known') +
          self.tile(sessions.size, 'sign-on sessions') +
          self.tile(self.durationText(snap.uptimeMs), 'uptime') +
        '</div>' +
        '<h2>What this console is</h2>' +
        self.note('This service exists to exercise clients, and the pages ' +
        'here exist to exercise the parts of a client that only show ' +
        'themselves when something changes underneath it: what happens when ' +
        'a token it holds stops being valid, and what happens when a token ' +
        'it reads grows a claim it was not expecting.') +
        self.note('Every page in the sidebar is below, in the sidebar\'s own ' +
        'order and grouping, because this list is DERIVED from the same ' +
        'table the sidebar is drawn from. A page cannot be added to this ' +
        'console and left out of here; one added without a description says ' +
        'so on its own row rather than going quietly.') +
        self.consoleGuide('/admin', gateStateFor(req)) +
        '<h2>What it deliberately does not do</h2>' +
        self.note('Worth knowing before looking for a control that is not ' +
        'here. Most of these are things this console CANNOT do rather than ' +
        'things somebody has not got to yet, and each says which.') +
        '<ul>' +
        self.bullet('<strong>It does not invalidate a SAML assertion, a ' +
        'Kerberos ticket or a credential.</strong> It counts them, it lists ' +
        'the first two on the tokens page beside the JWTs, and it says when ' +
        'each expires — but none of those has a revocation mechanism a ' +
        'relying party consults. A SAML assertion is valid because its ' +
        'signature verifies and its Conditions hold, and a Kerberos ticket ' +
        'because the service it names can decrypt it; nothing about this ' +
        'service is asked in either case. A button claiming to revoke one ' +
        'would change a number here and nothing at all out there, which is ' +
        'why those rows carry a dash and the reason for it. The same is true ' +
        'one family further out: <strong>an X509-SVID already handed to a ' +
        'workload keeps working until it expires</strong>, and banning a ' +
        'SPIFFE identity records who may still be ISSUED one, which is a ' +
        'different claim.') +
        self.bullet('<strong>It DOES end a sign-on session now, and it used ' +
        'to say it did not.</strong> The old reason was a good one: ' +
        '<code>/oauth2/logout</code> and WS-Federation\'s ' +
        '<code>wsignout1.0</code> each had a fan-out written into it, so a ' +
        'third button here would have been a third copy that quietly ' +
        'notified nobody. What changed on 2026-08-24 is that those fan-outs ' +
        'became FUNCTIONS owned by the protocol module they belong to, and ' +
        'one function in <code>authn.js</code> is the only place a session ' +
        'actually stops existing. <a href="/admin/logout">/admin/logout</a> ' +
        'calls them; so does <a href="/logout">/logout</a>, which is the ' +
        'same act without a console role. What this console still cannot do ' +
        'is DELIVER the front-channel notifications — each is an iframe in ' +
        'the signed-out person\'s own browser, and this is not that ' +
        'browser. The back-channel Logout Tokens need no browser, and this ' +
        'service sends them whichever door ended the session.') +
        self.bullet('<strong>It does not keep the tokens ' +
        'themselves</strong>, only their claims. A page listing a thousand ' +
        'live bearer credentials in a form a browser will render is a page ' +
        'that leaks them, and the <code>jti</code> is all any button here ' +
        'needs. One configured field is held back for the same reason and no ' +
        'stronger one: a federation relationship\'s ' +
        '<code>fedClientSecret</code> is this service\'s own credential AT a ' +
        'real foreign service, so it is never printed here and never reaches ' +
        'the audit log — an <code>ldapsearch</code> shows it anyway, and ' +
        'this console not being a second way to read it is not a security ' +
        'boundary.') +
        self.bullet('<strong>NOTHING THIS SERVICE MINTS SURVIVES A RESTART, ' +
        'and a restart is not a reload.</strong> The counters, the token and ' +
        'artifact registries, the audit ring, the sessions, the ' +
        'authorization codes and the Kerberos tickets are all in memory, and ' +
        'the signing key is REGENERATED on every start — so a token issued ' +
        'by the previous process no longer verifies against the published ' +
        'JWKS. That is deliberate rather than a limitation, and it is what ' +
        'makes the rest of it impossible: a statistics file, or a stored ' +
        'token, that outlived the key it was signed under would be worse ' +
        'than none. ' +
        (persistence.status().enabled
          ? '<strong>THREE THINGS DO SURVIVE on this process</strong>, ' +
            'because <code>persistence.mode</code> is <code>' +
            self.esc(persistence.status().mode) + '</code>: ' +
            'the embedded directory — which is also the applications ' +
            'registry, the federation register and the SPIFFE registry — the ' +
            'trust realm registry, and the settings changed on these pages. ' +
            'Those are the things somebody TYPED. See <a ' +
            'href="/admin/persistence">Persistence</a>.'
          : 'THREE THINGS CAN BE MADE TO SURVIVE and are not on this process ' +
            '— the embedded directory, the trust realms and the settings ' +
            'changed here. See <a href="/admin/persistence">Persistence</a>, ' +
            'which is off by default.')) +
        self.bullet('<strong>It does not remember everything, ' +
        'either.</strong> The token, artifact and user registries and the ' +
        'audit ring are CAPPED and drop their oldest rows — and each says ' +
        'how many it dropped rather than pretending they were never there: ' +
        '<code>forgotten</code> on <a href="/admin/metrics">Metrics</a>, ' +
        '<code>dropped</code> on <a href="/admin/audit">the audit log</a>, ' +
        'and one collapsed row for the paths that matched no route. A count ' +
        'that grew without limit would be one a scanner inventing URLs could ' +
        'exhaust.') +
        self.bullet('<strong>It checks no password — not even at its own ' +
        'door.</strong> The gate proves that somebody typed a name that ' +
        'holds one of two roles, and nothing verifies that it is them. The ' +
        'roles are ordinary directory groups, so an <code>ldapmodify</code> ' +
        'or a SCIM PATCH grants them too; while neither group has a member, ' +
        'anybody who signs in holds both; and ' +
        '<strong><code>/admin-api</code> is deliberately NOT gated</strong>, ' +
        'which is what a test drives and the way back in when nobody holds a ' +
        'role — and also means anybody who can reach this port can grant ' +
        'themselves both. The gate exists so a client can be driven through ' +
        '302, 401, 403 and a role model. It does not make this port safe to ' +
        'expose.') +
        self.bullet('<strong>It enforces nothing by default, and widening is ' +
        'what most of these pages do.</strong> RFC 9700 mode is the one MODE ' +
        'that makes this service refuse what it would otherwise accept, and ' +
        'it is off unless <a href="/admin/oauth2">the OAuth 2.0 / OIDC ' +
        'settings</a> turn it on. Three surfaces ask for a credential ' +
        'without it — <a href="/admin/scim">SCIM</a>, the SPIRE Server API ' +
        'and this console — and all three are a turnstile rather than a ' +
        'lock: each can be switched off, and none of them checks a password. ' +
        '<a href="/admin/federation">Federation</a> is the one refusal that ' +
        'is not a mode and cannot be switched off, because at an assertion ' +
        'consumer service there is no permissive answer available.') +
        self.bullet('<strong>It does not decide who may delegate to ' +
        'whom.</strong> <a href="/admin/delegation">Delegation</a> reports ' +
        'eight mechanisms and polices none of them: Kerberos is the only ' +
        'family here that polices delegation at all, and that policy belongs ' +
        'to the principal database rather than to this console. WS-Trust\'s ' +
        '<code>OnBehalfOf</code> and RFC 8693\'s token exchange are ' +
        'unpoliced, and the rows say so rather than being tidied into an em ' +
        'dash.') +
        self.bullet('<strong>It does not attest a workload or a ' +
        'node</strong>, and no setting on <a href="/admin/spiffe">SPIFFE</a> ' +
        'turns that on. The Workload API authenticates nobody because its ' +
        'specification says it MUST NOT — a workload has no root of trust ' +
        'until that call gives it one — so what is missing there is ' +
        'attestation rather than authentication.') +
        self.bullet('<strong>It deletes CONFIGURATION and never a ' +
        'person.</strong> An authorization server, a SPIFFE registration ' +
        'entry, an attested agent\'s record and a federation relationship ' +
        'can all be removed here. A PERSON cannot be: somebody can be ' +
        'created on <a href="/admin/users">Users</a> ahead of their first ' +
        'sign-in and a SPIFFE identity can be banned so that no further SVID ' +
        'is issued to it, but no control in this console deletes an entry ' +
        'under <code>ou=users</code>. One name is ONE entry however that ' +
        'name authenticated, which is the property most of these pages are ' +
        'reading. <a href="/admin/scim">SCIM</a> is the exception and it is ' +
        'a whole protocol rather than a button — a <code>DELETE ' +
        '/scim/v2/Users/{id}</code> really does remove the entry, while its ' +
        '<code>active: false</code> DISABLES the account (2026-09-17), which is ' +
        'the same act as Disable on a person\'s page.') +
        self.bullet('<strong>It shows ONE trust realm at a time, and it ' +
        'writes the one it is read in.</strong> Every page here reports the ' +
        'realm in the path it was reached by, and the switcher above the nav ' +
        'is how you leave it — so a setting saved on any of these pages ' +
        'changes that realm and no other. A realm has administrators of its ' +
        'own since 2026-09-14: the two role groups in its own directory, ' +
        'confined to that realm, while the default realm\'s two groups ' +
        'remain the service administrators over every realm. A realm ' +
        'administrator does not see the pages about the whole process, and ' +
        'is refused them if they ask.') +
        self.bullet('<strong>It runs no script, and that costs one thing ' +
        'worth naming.</strong> <code>script-src \'none\'</code> holds over ' +
        'every page in this console, which is what makes a family of ' +
        'reflected-content problems moot here rather than merely unlikely. ' +
        '<a href="/admin/delegation/map">The delegation picture</a> is ' +
        'therefore laid out on the SERVER and arrives as ordinary markup, so ' +
        'it does not pan or zoom — <code>?format=svg</code> hands over the ' +
        'document for something that does.') +
        '</ul>' +
        '<h2>Reading it from a test</h2>' +
        self.note('Every page above answers <code>?format=json</code>, and ' +
        'so does every drill-down under one — the same object, the same 200, ' +
        'the same <code>Cache-Control: no-store</code>. A caller that asks ' +
        'for JSON is never redirected to a sign-in screen either: it gets ' +
        '401 or 403 with a body, because a 302 to an HTML login page arrives ' +
        'at a program as a 200 full of markup. Every form takes a JSON body, ' +
        'and the same service is at <a href="/admin-api">/admin-api</a>, ' +
        'which is not gated at all.') +
        '<ul><li><code>GET ' + self.esc(base) +
        '/admin/metrics?format=json</code></li>' +
        self.bullet('<code>GET ' + self.esc(base) +
                    '/admin/groups?format=json</code>, and ' +
        '<code>GET ' + self.esc(base) +
        '/admin/groups?group=cn=developers,ou=groups,...&amp;format=json' +
        '</code> ' +
        '— the second carries every attribute of that group and every member ' +
        'resolved.') +
        self.bullet('<code>GET ' + self.esc(base) +
                    '/admin/users?format=json</code>, and ' +
        '<code>GET ' + self.esc(base) +
        '/admin/users?user=alice&amp;format=json</code> — the second carries ' +
        '<code>sessions</code>, each with the tokens issued on it, plus the ' +
        'tokens that belong to no session and the artifacts') +
        self.bullet('<code>GET ' + self.esc(base) +
        '/admin/tokens?format=json&amp;page=1&amp;per=100</code> ' +
        '— the reply carries <code>page</code>, <code>pages</code> and ' +
        '<code>matched</code>, so walking the whole list needs no guess ' +
        'about where it ends. Every paged list here answers the same three ' +
        'fields, so one walker serves all of them.') +
        self.bullet('<code>GET ' + self.esc(base) +
        '/admin/sts-metadata?format=json</code> ' +
        '— every endpoint this process registered, which is the list to ' +
        'check a client\'s assumptions against rather than this page.') +
        self.bullet('<code>GET ' + self.esc(base) +
        '/admin/delegation/map?format=json</code> ' +
        'for the graph, and <code>?format=svg</code> for the drawing alone — ' +
        'the one page here with a fourth shape.') +
        '<li><code>POST ' + self.esc(base) + '/admin/tokens</code> with ' +
        '<code>{"action":"revoke","target":"&lt;jti or ' +
        'token&gt;"}</code></li><li><code>POST ' + self.esc(base) +
        '/admin/claims</code> with <code>' +
        '{"action":"replace","set":"id_token","claims":[{"name":"dept",' +
        '"value":"engineering"}]}</code></li></ul>';
      self.respond(req, res, self.consoleJson(), 'Admin console', '/admin',
                   inner);
      log.debug("Leaving the admin console index.");
    });

    app.get('/admin/metrics', function (req, res) {
      log.debug("Entering the admin metrics page.");
      const json = metricsJson();
      // The snapshot's keys are the top level of that object; see the note on
      // it.
      const snap = json;
      const signOn = json.signOnSessions.rows;
      const liveSignOn = signOn.filter(function (s) { return !s.expired; });

      // The Who column holds SUBJECTS, and one family's are not names in any
      // readable sense: an OID4VCI credential's subject is a `did:jwk:` — a
      // couple of hundred characters of base64url with not one place in it a
      // browser will break a line. Emitted as plain text that made the cell's
      // minimum width wider than the card, so the table overflowed and took the
      // OAuth 2.0 / OIDC row's column with it, even though a subject was rarely
      // the long one (a `urn:uuid:` subject, since 2026-09-14, is 45 characters
      // and is shortened like the rest). So each subject is drawn the way the
      // tokens page draws a jti — shortened, with the whole string in the title
      // so it is still recoverable by hovering — and inside <code>, which the
      // stylesheet already lets break mid-string.
      const sessionFamilyRows = snap.sessions.families.map(function (row) {
        const shown = row.who.slice(0, MAX_WHO).map(function (subject) {
          return self.shortened(subject, 28);
        }).join(' ');
        return '<tr><td>' + self.esc(row.family) + '</td><td class="num">' +
          row.subjects + '</td><td ' +
          'class="who">' + shown +
          (row.who.length > MAX_WHO ?
           ' &hellip; and ' + (row.who.length - MAX_WHO) + ' ' +
              'more' : '') +
          '</td></tr>';
      }).join('');

      const signOnRows = signOn.slice(0, MAX_ROWS).map(function (s) {
        return '<tr><td>' + self.esc(s.username) + '</td>' +
          '<td class="' + (s.expired ? 'state-expired' : 'state-valid') + '">' +
            (s.expired ? 'expired, not yet swept' : 'active') + '</td>' +
          '<td>' + self.esc(s.amr || '—') + '</td><td>' +
          self.esc(s.acr || '—') + '</td>' +
          '<td>' + self.esc(self.whenText(s.startedAt)) + '</td>' +
          '<td>' + self.esc(self.whenText(s.authTime)) +
          (s.authentications > 1 ? ' (' + s.authentications + ')' : '') +
          '</td>' +
          '<td>' + self.esc(self.whenText(s.expires)) + '</td><td>' +
          (s.wsfedRealms.length ? self.esc(s.wsfedRealms.join(', ')) : '—') +
          '</td></tr>';
      }).join('');

      const inner = self.messagesOf(req) +
        '<div class="tiles">' +
          self.tile(snap.calls.total, 'endpoint calls') +
          self.tile(snap.calls.paths, 'routes called') +
          self.tile(snap.tokens.held, 'tokens issued') +
          self.tile(snap.tokens.revoked, 'tokens revoked') +
          self.tile(snap.artifacts.held, 'assertions, tickets, credentials') +
          self.tile(liveSignOn.length, 'sign-on sessions') +
          self.tile(snap.sessions.distinctSubjects,
                    'subjects with a live artifact') +
          self.tile(self.durationText(snap.uptimeMs), 'uptime') +
        '</div>' +
        self.note('Since <code>' + self.esc(self.whenText(snap.startedAt)) +
                  '</code>. Every ' +
        'figure on this page is computed when the page is drawn rather than ' +
        'kept up to date as things happen, because &ldquo;valid&rdquo; and ' +
        '&ldquo;expired&rdquo; are functions of the clock: a counter ' +
        'incremented at issuance would be wrong a second later.') +

        '<h2>Endpoint calls</h2>' +
        self.note('Keyed on the route Express matched, not on the URL ' +
        'requested, so <code>/oauth2/register/:client_id</code> is one row ' +
        'rather than one row per client. These are the same route patterns ' +
        '<a href="/admin/sts-metadata">/admin/sts-metadata</a> lists.') +
        self.callTable(snap) +

        '<h2>Tokens</h2>' +
        self.note('Every JWT this service signs, by <code>typ</code> — which ' +
        'is the only thing that tells them apart, since all of them are ' +
        'RS256 and signed with the same key. <a href="/admin/tokens">The ' +
        'tokens page</a> lists them one by one — beside the SAML assertions ' +
        'and Kerberos tickets, which it also lists — and can invalidate ' +
        'these.') +
        self.tokenKindTable(snap) +
        (snap.tokens.forgotten > 0
          ? self.note(snap.tokens.forgotten + ' older token(s) have been ' +
            'forgotten: the registry holds the most ' +
            'recent ' + snap.tokens.cap + '. Their ' +
            'revocations are NOT forgotten — the set of revoked ' +
            '<code>jti</code>s is kept separately and is not capped, so a ' +
            'token revoked long ago stays revoked.')
          : '') +

        '<h2>Assertions, tickets and credentials</h2>' +
        self.note('The artifacts that are not JWTs. None of them can be ' +
        'revoked and the console does not pretend otherwise — see the index ' +
        'for why — so the only distinction here is whether the validity ' +
        'window has closed. The assertions and the tickets are listed one by ' +
        'one on <a href="/admin/tokens">the tokens page</a>, beside the JWTs ' +
        'and in the order they were all issued; the credentials are counted ' +
        'here and nowhere else.') +
        self.artifactKindTable(snap) +
        (snap.artifacts.forgotten > 0
          ? self.note(snap.artifacts.forgotten + ' older artifact(s) have ' +
            'been forgotten; the registry holds the most ' +
            'recent ' + snap.artifacts.cap + '.')
          : '') +

        '<h2>Sessions, counted both ways</h2>' +
        self.note('The two numbers mean different things and disagree on ' +
        'purpose. A <strong>sign-on session</strong> is a real one: a ' +
        'browser holding the <code>sts_session</code> cookie, shared between ' +
        'the OAuth 2.0 / OIDC login screen and WS-Federation, which is what ' +
        'makes single sign-on across the two work. An ' +
        '<strong>artifact-derived session</strong> is an inference: a ' +
        'subject that holds at least one artifact from that protocol family ' +
        'which is still valid. A <code>client_credentials</code> token is ' +
        'the second and not the first (there is no human and no browser ' +
        'behind it); a browser that has signed in but been issued nothing ' +
        'yet is the first and not the second; a Kerberos client is never the ' +
        'first at all. Both are counted here per family; <a ' +
        'href="/admin/users">the users page</a> is where one person\'s ' +
        'sessions and the tokens issued on each of them are.') +
        '<h3>Sign-on sessions (' + liveSignOn.length + ' active of ' +
        signOn.length + ' ' +
        'held)</h3><table><tr><th>User</th><th>State</th><th>amr</th><th>acr' +
        '</th><th>Signed in</th><th>Last authenticated</th>' +
        '<th>Expires</th><th>WS-Fed relying parties signed into</th></tr>' +
        (signOnRows || '<tr><td colspan="8">Nobody is signed in.</td></tr>') +
        '</table>' +
        self.note('An expired session stays in the map until something reads ' +
        'it — <code>sessionOf()</code> drops one when it finds it stale — so ' +
        'it is listed as held but not active rather than quietly omitted.') +
        '<h3>Artifact-derived sessions (' + snap.sessions.distinctSubjects +
        ' distinct subject(s))</h3><table><tr><th>Protocol family</th><th ' +
        'class="num">Subjects</th><th>Who</th></tr>' +
        (sessionFamilyRows ||
         '<tr><td colspan="3">Nothing valid has been issued ' +
                              'yet.</td></tr>') +
        '</table>' +
        self.note('A Kerberos TGT is counted as a session and a service ' +
        'ticket is not, because that is what they are: the TGT is the ' +
        'credential the session consists of, and a service ticket is one use ' +
        'of it. Counting both would report the same session twice.');

      self.respond(req, res, json, 'Metrics', '/admin/metrics', inner);
      log.debug("Leaving the admin metrics page.");
    });

    app.post('/admin/tokens', function (req, res) {
      log.debug("Entering the admin token action endpoint.");
      const body = parseBody(req);
      const result = tokenAction(body);
      self.respondToAction(req, res, self.backTo(body), result);
      log.debug("Leaving the admin token action endpoint.");
    });

    app.get('/admin/sessions', function (req, res) {
      log.debug("Entering the admin sessions page.");
      const view = sessionsView(req);
      const gate = gateStateFor(req);

      if (!view.installed) {
        const inner = self.messagesOf(req) +
          '<div class="err"><strong>The logout reader is not installed in ' +
          'this process</strong>, so nothing here can say what is live. ' +
          'Every row on this page is read from ' +
          '<code>logout/logout.ts</code>, which is the one model of what a ' +
          'session IS across protocol families.</div>';
        self.respond(req, res, view.json, 'Sessions', '/admin/sessions', inner);
        log.debug("Leaving the admin sessions page. No reader.");
        return;
      }

      const nowMs = Date.now();
      const paging = view.paging;
      // What every paging link carries with it. The page number is not in here
      // — pageNavPair() supplies that per link — and neither is `format`,
      // because JSON has no links in it.
      const filterParams = { q: view.wantedText, protocol: view.wantedProtocol,
                             per: req.query.per ? paging.perPage : '' };
      const nav = self.pageNavPair('/admin/sessions', filterParams, paging);
      // Where a Revoke sends the reader back to: THIS page of THIS filter,
      // because the row above and below the one they ended is what they were
      // reading.
      const back = queryWith(filterParams, { page: paging.page });

      const protocolOptions = ['<option value=""' +
          (view.wantedProtocol ? '' : ' selected') + '>any protocol</option>']
        .concat(view.protocols.map(function (name) {
          return '<option value="' + self.esc(name) + '"' +
                 (name === view.wantedProtocol ? ' selected' : '') + '>' +
                 self.esc(name) + '</option>';
        })).join('');

      const rows = view.shown.length
        ? view.shown.map(function (row) {
            return self.sessionRow(row, nowMs, gate.write, back);
          }).join('')
        : '<tr><td colspan="8">' +
          (view.all.length
            ? 'Nothing matches. ' + view.all.length + ' session(s) are live ' +
              'under other names or other protocols.'
            : 'Nothing is signed in. Sign somebody in &mdash; an OIDC flow, ' +
              'a SAML 2.0 sign-in, an <code>ldapsearch</code> that binds, a ' +
              '<code>kinit</code> &mdash; and a row appears here.') +
          '</td></tr>';

      const inner = self.messagesOf(req) +

        self.note('<strong>Every session this service is holding right ' +
        'now</strong>, across the three protocols that have one. A session ' +
        'is state THIS SERVICE holds that makes somebody currently ' +
        'authenticated; a token, an assertion, a ticket and an SVID are ' +
        'things it has HANDED OUT, they outlive every session here, and they ' +
        'are <a href="/admin/tokens">Tokens</a>. Keeping the two apart is ' +
        'the whole point of a page of each.') +

        '<div class="tiles">' +
        self.tile(view.all.length, 'live sessions') +
        self.tile(view.byKind.session || 0, 'browser sign-on') +
        self.tile(view.byKind.krb5 || 0, 'Kerberos TGTs') +
        self.tile(view.byKind.ldap || 0, 'LDAP connections') +
        // The fifth tile is a SLICE of the first four rather than a fifth kind,
        // so the four above it still add up to `live sessions` and this one
        // does not join that sum. It earns a tile anyway: it is the number
        // somebody scans this page for, and a zero here is as informative as a
        // non-zero.
        self.tile(view.unauthenticated.length, 'unauthenticated') +
        '</div>' +

        self.note('<strong>The three are not variants of one thing and their ' +
        'expiries are worked out differently</strong>, which is why the ' +
        'Expires column carries the rule as well as the time &mdash; hover ' +
        'it on any row:') +
        '<ul><li><strong>The browser sign-on session</strong> &mdash; the ' +
        'cookie from <code>/authn/login</code>, which OAuth 2.0 / OIDC, ' +
        'WS-Federation, SAML 2.0, SAML 1.1 and this console all read. It ' +
        'expires at an ABSOLUTE instant fixed when it was created ' +
        '(<code>authn.sessionLifetimeS</code>) and <strong>using it does not ' +
        'extend it</strong>. An idle timeout ' +
        '(<code>authn.sessionIdleTimeoutS</code>) ends it earlier when it ' +
        'goes unused; it is off by default, and then a session in constant ' +
        'use dies at the same moment as one nobody has touched. The ' +
        '<em>Carries</em> column is what has signed in ON it, which is what ' +
        'makes ending one reach further than it looks.</li><li><strong>The ' +
        'Kerberos ticket-granting ticket</strong> &mdash; a TGT IS the ' +
        'Kerberos session and a service ticket is one use of it. It expires ' +
        'at the <code>endtime</code> the KDC sealed INTO the ticket, and ' +
        'nothing here can move it or take it back: a ticket is valid because ' +
        'it decrypts and its endtime has not passed. Short lifetimes are the ' +
        'whole of Kerberos\'s revocation model.</li><li><strong>The LDAP ' +
        'connection</strong> &mdash; RFC 4511 section 4.2 makes a Bind the ' +
        'authorization state of a CONNECTION, so in LDAP the connection is ' +
        'the session and closing it is the only sign-out the protocol has. ' +
        'It has <strong>no expiry at all</strong>: it lasts until the next ' +
        'Bind, an Unbind, or the socket closing.</li></ul>' +

        self.warn('<strong>Revoke is not one act either.</strong> On a ' +
        'browser session it ends that session and everything hanging off it ' +
        '&mdash; the relying parties are notified, the refresh tokens issued ' +
        'on it are revoked. On an LDAP row it closes the socket, which the ' +
        'client sees as its connection dropping mid-conversation. On a ' +
        'Kerberos row <strong>it does more than the row it is on</strong>: ' +
        'it stamps a sign-out instant on the PRINCIPAL, so every ' +
        'ticket-granting ticket that principal authenticated before now is ' +
        'refused &mdash; and it still reaches no service ticket already in a ' +
        'cache, because accepting one never contacts this KDC. Every button ' +
        'carries its own sentence; hover it before pressing it. All three go ' +
        'through the same termination <a href="/logout">the ' +
        'protocol-independent sign-out</a> performs, so they write the same ' +
        'audit row and honour the same two settings.') +

        '<h2>Live sessions</h2>' +
        // No `page` input in this form, and that is the point: changing the
        // filter or the page size sends the reader back to page 1. Carrying the
        // old page number over would land somebody on page 6 of a two-page
        // result.
        '<form method="get" action="/admin/sessions"><div class="formrow">' +
          '<label for="q">Search</label>' +
          '<input type="text" id="q" name="q" size="28" value="' +
          self.esc(view.wantedText) + '" placeholder="a username, a subject, ' +
          'a DN or a session id">' +
          '<label for="protocol">Protocol</label>' +
          '<select id="protocol" name="protocol">' + protocolOptions +
          '</select><label for="per">Per page</label><select id="per" ' +
          'name="per">' + self.perPageOptions(paging.perPage) +
          '</select>' +
          '<button class="secondary">Filter</button>' +
          (view.wantedText || view.wantedProtocol
            ? ' <a href="/admin/sessions">clear</a>' : '') +
        '</div></form>' +
        self.note('The search matches the username, the subject, the session ' +
        'id, the bind DN and the principal name together, because a reader ' +
        'arrives holding exactly one of those. The protocol is the one the ' +
        'sign-in came THROUGH and not the only one the session serves: every ' +
        'browser family here reads the same session, so a row saying ' +
        '<code>SAML 2.0</code> may well be carrying OIDC relying parties too ' +
        '&mdash; which is what the <em>Carries</em> column says.') +
        nav.head +
        '<table><tr><th>Kind</th><th>Protocol</th><th>Who</th><th>Since</th>' +
        '<th>Expires</th><th>Carries</th><th>Credentials</th><th></th></tr>' +
        rows + '</table>' +
        nav.foot +
        self.note(view.filtered.length + ' row(s) match' +
        (paging.pages > 1
          ? ', of which rows ' + paging.firstRow + '&ndash;' + paging.lastRow +
            ' are on this page (' + paging.page + ' of ' + paging.pages + ')'
          : '') +
        '; ' + view.all.length + ' live in total. Newest first. Everything ' +
        'here is read live from the module that owns it every time this page ' +
        'is drawn &mdash; there is no cache, deliberately, because a cached ' +
        'answer to <em>is this still live</em> would be the half a reader is ' +
        'about to press a button on.') +

        // ---------------------------------------------------------------------
        // THE UNAUTHENTICATED SESSIONS (2026-09-05).
        //
        // A section rather than a column, for the reason `sessionsView()`
        // gives: the answer is almost always "none", and a column that says the
        // same thing on every row for weeks stops being read.
        //
        // It draws whether or not the setting that CREATES these is on, and
        // that is deliberate — a service that had the setting on this morning
        // and off now may still be holding sessions it minted then, and a
        // section that disappeared with the setting would hide exactly those.
        // What changes with the setting is the sentence, not the presence.
        // ---------------------------------------------------------------------
        '<h2>Unauthenticated sessions</h2>' +

        self.note('<strong>A session where nobody authenticated.</strong> ' +
        'Somebody pressed <em>Continue without signing in</em> at ' +
        '<code>/authn/login</code>, so this service holds a real session for ' +
        'them &mdash; it has a cookie, it satisfies a flow already in ' +
        'progress, and tokens can be issued on it &mdash; and it records ' +
        'that no credential was ever checked. They are the ' +
        '<code>anonymous</code> principal, which is one directory entry ' +
        'however many of these there are.') +

        self.note('<strong>This is the only place the difference between two ' +
        'of the built-in roles is visible.</strong> Every session in the ' +
        'table above holds <code>EVERYBODY</code> <em>and</em> ' +
        '<code>ALL_AUTHENTICATED_USERS</code>; every session in this one ' +
        'holds <code>EVERYBODY</code> and ' +
        '<code>ALL_UNAUTHENTICATED_USERS</code> instead. So an application ' +
        'whose <code>appRequiredRole</code> is ' +
        '<code>ALL_AUTHENTICATED_USERS</code> refuses these with ' +
        '<code>access_denied</code>, and one that names no role at all ' +
        '&mdash; which requires <code>EVERYBODY</code> &mdash; does not. <a ' +
        'href="/admin/roles">The role register</a> is where that is ' +
        'configured.') +

        (config.value('authn.unauthenticatedSessions')
          ? self.note('<strong><code>authn.unauthenticatedSessions</code> is ' +
            'ON</strong> in this realm, so the sign-in screen is offering ' +
            'the third button. <a href="/admin/roles">Turn it off</a> and no ' +
            'new ones can be started; any already here stay until they ' +
            'expire or are ended.')
          : self.warn('<strong><code>authn.unauthenticatedSessions</code> is ' +
            'OFF</strong> in this realm, so no new ones can be started and ' +
            'this section will stay empty. It is off by default because it ' +
            'puts a third button on every sign-in screen in the service. <a ' +
            'href="/admin/roles">Turn it on</a> to make ' +
            '<code>ALL_UNAUTHENTICATED_USERS</code> reachable.')) +

        (view.unauthenticated.length
          ? '<table><tr><th>Kind</th><th>Protocol</th><th>Who</th><th>Since' +
            '</th>' +
            '<th>Expires</th><th>Carries</th><th>Credentials</th><th></th>' +
            '</tr>' +
            view.unauthenticated.map(function (row) {
              return self.sessionRow(row, nowMs, gate.write, back);
            }).join('') + '</table>' +
            self.note(view.unauthenticated.length + ' unauthenticated ' +
              'session(s), of ' +
            view.all.length + ' live. <strong>Not filtered and not ' +
            'paged</strong>, unlike the table above &mdash; the question ' +
            'this section answers is about the whole service, and a search ' +
            'box somebody had left set could hide the one row that matters.')
          : self.note('<strong>None.</strong> Every session this service is ' +
            'holding right now had a credential accepted for it.')) +

        self.note('<a href="/admin/logout">What ONE person is still signed ' +
        'into</a>, which is this question asked the other way round and ' +
        'reaches seven more families &middot; <a href="/admin/tokens">what ' +
        'has been issued</a> &middot; <a href="/admin/caep-sessions">what ' +
        'has been SAID about these sessions</a> over Shared Signals &middot; ' +
        '<a href="/admin/metrics">the counts</a> &middot; <a ' +
        'href="/admin/sessions?format=json">this page as JSON</a> &middot; ' +
        '<a href="/admin-api/sessions">the same over the management API</a>');

      self.respond(req, res, view.json, 'Sessions', '/admin/sessions', inner);
      log.debug("Leaving the admin sessions page.");
    });

    app.post('/admin/sessions', function (req, res) {
      log.debug("Entering the admin sessions action endpoint.");
      const body = parseBody(req);
      // NO ROLE CHECK HERE, and that is not an omission: the console's gate is
      // MIDDLEWARE and it refuses every non-GET without Admin Write with a 403
      // and a page saying which group grants it — see the gate above. A second
      // check here would be a second refusal, in a different shape (a 303
      // carrying a message), for the same act; the browser suite asserts the
      // 403, so the two would have disagreed the first time anybody looked.
      const gate = gateStateFor(req);
      const result = sessionsAction(body, { actor: gate.username || '',
                                            by: 'the /admin/sessions page' });
      // Back to the page and filter the button was on, rebuilt from the
      // whitelist rather than echoed — the rule every form on this console
      // follows.
      const back = '/admin/sessions' +
        queryWith(self.listViewFromBack('/admin/sessions', body.back), {});
      self.respondToAction(req, res, back, result);
      log.debug("Leaving the admin sessions action endpoint.");
    });

    app.get('/admin/tokens', function (req, res) {
      log.debug("Entering the admin tokens page.");
      const view = tokensView(req.query);
      const wantedFamily = view.wantedFamily;
      const wantedKind = view.wantedKind;
      const wantedState = view.wantedState;
      const wantedSession = view.wantedSession;
      const all = view.all;
      const filtered = view.filtered;
      const paging = view.paging;
      const shown = view.shown;
      const heldByFamily = view.heldByFamily;
      // What every paging link has to carry with it. The page number is not in
      // here — pageNavPair() supplies that per link — and neither is `format`,
      // because JSON has no links in it and a caller asking for JSON passes its
      // own parameters anyway.
      const filterParams = { family: wantedFamily, kind: wantedKind,
                             state: wantedState,
                             session: wantedSession,
                             per: req.query.per ? paging.perPage : '' };
      const nav = self.pageNavPair('/admin/tokens', filterParams, paging);
      // What the POST handler sends the browser back to. A row button returns
      // to THIS page of THIS filter; the bulk buttons below keep the filter but
      // not the page, because after "revoke everything" the list they were
      // looking at is a different list and page 7 of it means nothing.
      const backRow = queryWith(filterParams, { page: paging.page });
      const backFilter = queryWith(filterParams, {});

      // The list as the reader left it, for the drill-down link on every row:
      // the filter AND the page, so the way back from a credential is the row
      // it was clicked on rather than the top of everything.
      const listView = Object.assign({}, filterParams, { page: paging.page });
      const rows = shown.map(function (set) {
        return self.issuedSetRow(set, backRow, listView);
      }).join('');

      const familyOptions = ['<option value=""' +
          (wantedFamily ? '' : ' selected') + '>any ' +
          'family</option>']
        .concat(stats.ISSUED_FAMILIES.map(function (entry) {
          return '<option value="' + self.esc(entry.family) + '"' +
                 (entry.family === wantedFamily ? ' selected' : '') + '>' +
                 self.esc(entry.label) + '</option>';
        })).join('');

      // Grouped by family rather than flat, because "SAML 2.0" and "id_token"
      // in one list of nine reads as nine unrelated things. Both this and the
      // family select are built from the same structure in admin_stats.js, so
      // the two cannot come to disagree about which kind belongs to which
      // family — which they would, being two hand-written lists of the same
      // nine strings.
      const kindOptions = '<option value=""' + (wantedKind ? '' : ' selected') +
          '>any ' +
          'kind</option>' +
        stats.ISSUED_FAMILIES.map(function (entry) {
          return '<optgroup label="' + self.esc(entry.label) + '">' +
                 entry.kinds.map(function (k) {
            return '<option value="' + self.esc(k) + '"' +
                   (k === wantedKind ? ' ' +
                'selected' : '') + '>' +
                   self.esc(k) + '</option>';
          }).join('') + '</optgroup>';
        }).join('');
      const stateOptions = ['', 'valid', 'expired', 'revoked', 'not yet valid',
                            'no ' +
          'expiry stated']
        .map(function (s) {
          return '<option value="' + self.esc(s) + '"' +
                 (s === wantedState ? ' selected' : '') + '>' +
                 self.esc(s || 'any state') + '</option>';
        }).join('');
      const perOptions = self.perPageOptions(paging.perPage);

      const inner = self.messagesOf(req) +
        self.note('Everything this service has issued and still remembers: ' +
        'every JWT, every SAML assertion — whether WS-Trust issued it or a ' +
        'WS-Federation sign-in did — and every Kerberos ticket the KDC ' +
        'minted, in one table, newest first. One table rather than three ' +
        'because a WS-Federation sign-in that produced an ID Token and a ' +
        'SAML 1.1 assertion is <em>one event</em>, and three tables would ' +
        'leave it to be reassembled by comparing timestamps.') +
        self.note('<strong>One row is one issuance, not one ' +
        'credential.</strong> OAuth 2.0 and OIDC are the only protocols here ' +
        'that hand back several credentials at once — redeeming an ' +
        'authorization code returns an access token, a refresh token and an ' +
        'ID Token in a single reply, and <code>response_type=id_token ' +
        'token</code> returns two in one fragment — so those are drawn as ' +
        'one row saying what it contains, with a link to the set. Every ' +
        'other family issues one credential per act, so a SAML assertion, a ' +
        'Kerberos ticket and a SPIFFE SVID are each a <em>set of one</em> ' +
        'and look exactly as they always did. The grouping comes from an ' +
        'identifier the <em>issuer</em> stated at the moment it built the ' +
        'reply, never from guessing that two rows near each other in time ' +
        'belong together: two people redeeming two codes at the same client ' +
        'in the same millisecond produce six credentials that agree on every ' +
        'column below, and a table that merged them would report a reply ' +
        'nobody ever received.') +
        self.note('<strong>Refreshing makes a new set beside the old one, ' +
        'not a bigger one.</strong> A set is one <em>response</em>: it has ' +
        'one issued instant and one grant, which a row that grew all ' +
        'afternoon could not have. What joins the generations of a grant is ' +
        'the refresh lineage, and that is drawn — as a picture, back to the ' +
        'issuance the whole line rests on — on each credential\'s own page.') +
        self.note('Only the JWTs can be invalidated. Revoking one here is ' +
        'the SAME operation RFC 7009\'s <code>/oauth2/revoke</code> performs ' +
        '— there is one set of revoked <code>jti</code>s in this service, ' +
        'not one per page. So a token revoked here immediately introspects ' +
        'as inactive at <code>/oauth2/introspect</code>, is refused by ' +
        '<code>/oauth2/userinfo</code> with <code>invalid_token</code>, and ' +
        'fails the refresh grant with <code>invalid_grant</code>. Two sets ' +
        'would each look correct on their own and never see each other, ' +
        'which is a debugging session with no error message anywhere in it.') +
        self.note('<strong>An assertion, a ticket and an SVID have a button ' +
        'since 2026-09-05, and pressing it changes nothing out ' +
        'there.</strong> That is not a contradiction and the distinction is ' +
        'the whole of what this row means: <em>what this service knows</em> ' +
        'and <em>what a relying party will honour</em> are two different ' +
        'claims. Nothing consults this service when one of these is ' +
        'presented — an assertion is valid because its signature verifies ' +
        'and its <code>Conditions</code> hold, a ticket because the service ' +
        'it names can decrypt it with a key it already has, an SVID because ' +
        'it chains to a bundle — so a revocation here reaches none of them ' +
        'and never will. What it does is record that <em>this identity ' +
        'provider has disowned the credential</em>, which is what a sign-out ' +
        'has to be able to say, what CAEP can carry to a receiver that ' +
        'subscribed, and what SAML Single Logout can carry for an assertion ' +
        'issued through a browser profile. A WS-Trust assertion has neither ' +
        'channel and the mark is the whole of what exists for it — which is ' +
        'exactly why it is worth having, because otherwise the answer to ' +
        '"did you sign them out" would depend on which endpoint issued the ' +
        'credential. The button says <em>(record only)</em> and its tooltip ' +
        'says this again, so it cannot be pressed by somebody who thinks it ' +
        'did more.') +

        '<h2>Invalidate</h2>' +
        '<form method="post" action="/admin/tokens">' +
          '<input type="hidden" name="action" value="revoke">' +
          '<input type="hidden" name="back" value="' + self.esc(backFilter) +
          '"><div class="formrow"><label for="target">A jti, or paste the ' +
          'whole token</label><input type="text" id="target" name="target" ' +
          'size="60" placeholder="jti, or eyJhbGciOi..."><button ' +
          'class="danger">Revoke</button></div></form>' +
        self.note('Pasting a token is read for its <code>jti</code> and the ' +
        'signature is not checked, which is safe: a forged token yields a ' +
        'jti this service never issued, and revoking one of those ' +
        'invalidates nothing. To undo a revocation, use the Restore button ' +
        'in the table — a NON-SPEC operation no real authorization server ' +
        'can offer, kept because otherwise getting back to a working token ' +
        'means restarting this service.') +
        '<div class="formrow">' +
          ['access_token', 'id_token', 'refresh_token'].map(function (kind) {
            return '<form method="post" action="/admin/tokens" ' +
              'class="inline"><input type="hidden" name="action" ' +
              'value="revoke-kind"><input type="hidden" name="kind" value="' +
              self.esc(kind) + '">' +
              '<input type="hidden" name="back" value="' +
              self.esc(backFilter) + '">' +
              '<button class="danger">Revoke every ' + self.esc(kind) +
              '</button></form>';
          }).join(' ') +
          '<form method="post" action="/admin/tokens" class="inline">' +
          '<input type="hidden" name="action" value="revoke-all">' +
          '<input type="hidden" name="back" value="' + self.esc(backFilter) +
          '"><button class="danger">Revoke everything</button></form>' +
        '</div>' +
        '<form method="post" action="/admin/tokens">' +
          '<input type="hidden" name="action" value="revoke-subject">' +
          '<input type="hidden" name="back" value="' + self.esc(backFilter) +
          '"><div class="formrow"><label for="subject">Everything for one ' +
          'subject or username</label><input type="text" id="subject" ' +
          'name="subject" size="40" placeholder="alice, or ' +
          'urn:uuid:…"><button ' +
          'class="danger">Revoke</button></div></form><h2>What has been ' +
          'issued</h2>' +
        // No `page` input in this form, and that is the point: changing the
        // filter or the page size sends the reader back to page 1. Carrying the
        // old page number over would land somebody on page 6 of a two-page
        // result, and the clamp in pagingOf() would then quietly move them
        // again.
        '<form method="get" action="/admin/tokens"><div class="formrow">' +
          '<label for="family">Family</label><select id="family" ' +
          'name="family">' +
        familyOptions +
          '</select>' +
          '<label for="kind">Kind</label><select id="kind" name="kind">' +
        kindOptions + '</select><label ' +
          'for="state">State</label><select id="state" ' +
          'name="state">' + stateOptions + '</select><label ' +
          'for="per">Per page</label><select id="per" ' +
          'name="per">' + perOptions + '</select>' +
          // The session filter rides as a HIDDEN input rather than as a fourth
          // select: it is not a choice out of a short list, it is one session
          // somebody arrived from — so it survives a Filter and is taken off by
          // the sentence below, which names the session it is holding.
          (wantedSession
            ? '<input type="hidden" name="session" value="' +
              self.esc(wantedSession) +
              '">'
            : '') +
          '<button class="secondary">Filter</button>' +
          (wantedFamily || wantedKind || wantedState || wantedSession
            ? ' <a href="/admin/tokens">clear</a>' : '') +
        '</div></form>' +
        (wantedSession
          ? self.note('Narrowed to what was issued on the browser sign-on ' +
            'session <code>' + self.esc(wantedSession) + '</code>, which is ' +
            'how <a href="' + self.esc('/admin/sessions') +
            '">Sessions</a> links here. ' +
            'Only credentials issued UNDER a session carry one, so an ' +
            'assertion, a Kerberos ticket, a token from either direct grant ' +
            'and anything an RFC 8693 exchange produced are absent by ' +
            'construction rather than missing &mdash; that is a fact about ' +
            'the credential and not a gap in the recording. <a href="' +
            self.esc('/admin/tokens' +
              queryWith({ family: wantedFamily, kind: wantedKind,
                          state: wantedState }, {})) +
            '">Show every session\'s</a>.')
          : '') +
        // Family and Kind are ANDed, like any two filters, so a contradictory
        // pair (Kerberos tickets, id_token) matches nothing. Said here rather
        // than prevented, because the alternative is a page that silently
        // ignores one of the two selects the reader can see it obeying.
        self.note('Family and Kind narrow together: choosing a family and a ' +
        'kind from a different one matches nothing, which is what an empty ' +
        'table below then means.') +
        // WHAT A FILTER MEANS NOW THAT A ROW IS A SET, said on the page because
        // it is the one behaviour a reader would otherwise call a bug: asking
        // for id_token and being shown an access token too looks like the
        // filter being ignored until somebody explains that the row IS the
        // reply.
        self.note('<strong>A filter matches a set when any credential in it ' +
        'matches.</strong> Asking for <code>id_token</code> answers with the ' +
        'replies that <em>contain</em> an ID Token — the access token and ' +
        'the refresh token that came back with it are still on the row, ' +
        'because they are part of the same reply. The same goes for State: a ' +
        'set holding an expired access token and a valid refresh token is ' +
        'found by both, and its State column reads <em>mixed</em> rather ' +
        'than picking one.') +
        nav.head +
        '<table><tr><th>Contents</th><th>State</th><th>User</th><th>Subject' +
        '</th>' +
        '<th>Client, audience or service</th><th>Detail</th><th>Presented ' +
        'as</th><th>Issued</th><th>Expires</th><th>jti, ID or ' +
        'set</th><th></th></tr>' +
        (rows || '<tr><td colspan="11">Nothing matches.</td></tr>') +
        '</table>' +
        nav.foot +
        self.note(filtered.length + ' set(s) match, holding ' +
                  view.matchedCredentials +
        ' credential(s)' +
        (paging.pages > 1 ?
         ', of which sets ' + paging.firstRow + '&ndash;' + paging.lastRow +
                            ' are on this page (' + paging.page + ' of ' +
                            paging.pages + ')' : '') +
        '; ' + all.length + ' set(s) over ' + view.heldCredentials + ' ' +
            'credential(s) held in total — ' +
        stats.ISSUED_FAMILIES.map(function (entry) {
          return (heldByFamily[entry.family] || 0) + ' ' +
                 self.esc(entry.label);
        }).join(', ') +
        '. <strong>The per-family figures are credentials, not ' +
        'sets</strong>, so that they agree with the count on <a ' +
        'href="/admin/metrics">the metrics page</a> — two pages of one ' +
        'console disagreeing about how much has been issued is worse than ' +
        'one line carrying both units and saying which is which. Paging is ' +
        'by SET, so a page is a whole number of replies rather than a ' +
        'boundary drawn through the middle of one. Newest first, so page 1 ' +
        'is what somebody is most likely to be debugging. Only the claims ' +
        'and the facts below are kept, never the signed token, the assertion ' +
        'XML or the ticket: a page rendering a thousand live credentials in ' +
        'a form a browser will display is a page that leaks them, and the ' +
        '<code>jti</code> is all any button here needs.') +

        '<h3>What each column means</h3>' +
        self.note('Three families in one table, so most columns answer a ' +
        'slightly different question depending on the row. Rather than leave ' +
        'that to be inferred:') +
        COLUMN_LEGEND +
        self.note('OID4VCI credentials are <strong>not</strong> in this ' +
        'table. They are recorded and counted on <a ' +
        'href="/admin/metrics">the metrics page</a> and listed nowhere. That ' +
        'is a gap rather than a principle — a credential is as much an ' +
        'issued artifact as an assertion is — and it is named here so that ' +
        '"everything this service has issued" above is read as the three ' +
        'families it says and not as four.') +

        self.note('Paging is <code>?page=</code> and <code>?per=</code> (at ' +
          'most ' +
        MAX_ROWS +
        ' sets a page), and both work with <code>?format=json</code> — where ' +
        'the reply carries <code>page</code>, <code>pages</code> and ' +
        '<code>matched</code>, so a test can walk the whole list without ' +
        'guessing when it has reached the end. <strong>Those three count ' +
        'SETS</strong>, which is what this list is; ' +
        '<code>matchedCredentials</code> and <code>held</code> are the same ' +
        'figures in credentials. The sets are in <code>sets</code>, each ' +
        'carrying its <code>members</code> — and <code>issued</code> is the ' +
        'same members flattened, so a caller written against the older ' +
        'per-credential shape reads exactly what it read and the two cannot ' +
        'disagree, because one is built out of the other. Every button on ' +
        'this page acts on a <code>jti</code> or a <code>setKey</code> and ' +
        'never on a row number, so a revocation between two clicks cannot ' +
        'make the wrong token the target — the most it can do is shift a row ' +
        'onto another page.');

      self.respond(req, res, view.json, 'Tokens', '/admin/tokens', inner);
      log.debug("Leaving the admin tokens page.");
    });

    app.get('/admin/tokens/set', function (req, res) {
      log.debug("Entering the admin token set page.");
      const view = tokenSetView(req.query);
      const set = view.set;
      const listView = self.listViewOf('/admin/tokens', req.query);
      const up = self.upTo('/admin/tokens', 'One issuance', listView);
      const back = self.note('<a class="btn" href="' + self.esc(up.href) +
        '">&larr; Back to the tokens table</a>');

      if (!set) {
        const inner = self.messagesOf(req) + back +
          self.note('<strong>' + self.esc(view.json.why) + '</strong> This ' +
          'page draws the credentials that came back in ONE reply, and the ' +
          'way to it is the link in the last column of <a ' +
          'href="' + self.esc(up.href) + '">the tokens ' +
          'table</a> on any row that shows more than one. A row showing a ' +
          'single credential links to that credential\'s own lineage ' +
          'instead, because for one credential this page would be a click ' +
          'that added nothing.');
        self.respond(req, res, view.json, 'Tokens — one issuance',
                     '/admin/tokens',
                     inner, up);
        log.debug("Leaving the admin token set page. Nothing was asked for.");
        return;
      }

      // Where a member's own button sends the browser: back HERE, with the list
      // view the reader arrived through, so revoking one credential of three
      // does not land them on page 1 of everything. `from=set` is read as an
      // ENUM by backTo() and never as a path — the same rule the users page's
      // buttons follow, and what keeps a `back` field from becoming an open
      // redirect.
      const backRow = queryWith(Object.assign({}, listView, { id: set.setKey }),
                                { from: 'set' });
      const memberRows = set.members.map(function (record) {
        return self.issuedRow(record, backRow, listView);
      }).join('');

      // The grant, spelled as the console spells it everywhere else. Empty for
      // anything minted where nothing states how — which cannot happen for a
      // GROUP, since only the two OAuth issuance sites group, but can for a set
      // of one.
      const grantText = set.grant || 'not stated';

      const inner = self.messagesOf(req) + back +
        self.note('<strong>' + (set.grouped
          ? self.esc(set.size + ' credentials came back in one reply') +
            ', from the <code>' + self.esc(grantText) + '</code> grant.'
          : 'One credential, issued on its own.') +
        '</strong> This is that reply, member by member — each with its own ' +
        'identifier, its own expiry and its own button, which is what the ' +
        'tokens table drew before it started grouping. The columns mean what ' +
        '<a href="' + self.esc(up.href) + '">its legend</a> says they mean.') +

        '<h2>The issuance</h2>' +
        '<table>' +
        '<tr><th>Set</th><td><code>' + self.esc(set.setId || set.setKey) +
        '</code>' +
          (set.setId
            ? ' <span class="state-none">— this service\'s own handle on the ' +
              'reply. It is in no token, no client ever sees it, and it is ' +
              'not a claim: it exists so that a console page can say which ' +
              'credentials arrived together, which nothing in the protocol ' +
              'records.</span>'
            : ' <span class="state-none">— a set of one has no issuance id, ' +
              'so this is the row\'s own handle in the issued ' +
              'register.</span>') + '</td></tr><tr><th>Contents</th><td>' +
        self.esc(set.kinds.join(' ' +
                  '+ ')) + '</td></tr><tr><th>State</th><td>' +
                  self.esc(set.state) +
          (set.state === 'mixed'
            ? ' <span class="state-none">— ' +
              self.esc(Object.keys(set.states).map(function (state) {
                return set.states[state] + ' ' + state;
              }).join(', ')) +
              '. The ordinary case rather than a fault: an access token and ' +
              'the refresh token issued with it have very different ' +
              'lifetimes.</span>'
            : '') + '</td></tr>' +
        '<tr><th>Grant</th><td>' + self.esc(grantText) + '</td></tr>' +
        '<tr><th>User</th><td>' + self.userCell(set.members[0]) + '</td></tr>' +
        '<tr><th>Subject</th><td>' + self.subjectCell(set.members[0]) +
        '</td></tr><tr><th>Client</th><td>' + self.partyCell(set.members[0]) +
        '</td></tr><tr><th>Session</th><td>' + (set.sessionId
          ? '<a href="' + self.esc('/admin/tokens' +
              queryWith({ session: set.sessionId }, {})) + '"><code>' +
            self.esc(set.sessionId) + '</code></a>' +
            (set.sessionAuthenticated ? ''
              : ' <span class="state-revoked">— nobody had authenticated on ' +
                'that session when this was issued.</span>')
          : '<span class="state-none">none — this was issued with no browser ' +
            'sign-on session behind it, which is true of both direct grants, ' +
            'a pre-authorized code, every token exchange, and every ' +
            'assertion and ticket. A fact about the credential rather than a ' +
            'gap in the recording.</span>') +
            '</td></tr><tr><th>Issued</th><td>' +
        self.esc(self.whenText(set.issuedAt)) +
        '</td></tr><tr><th>Expires</th><td>' +
        self.setExpiryCell(set) + '</td></tr></table>' +

        (set.grouped && set.revocableCount
          ? '<h2>Invalidate the whole set</h2>' +
            self.note('One act rather than one click per credential, and it ' +
            'writes nowhere new: each member goes through the same ' +
            'revocation <code>/oauth2/revoke</code> performs, one at a time. ' +
            'What it saves is the mistake of revoking two of three and ' +
            'believing the grant is dead — a refresh token left behind mints ' +
            'a new access token, which is the whole reason a reply is worth ' +
            'being one row. ' +
            (set.size > set.revocableCount
              ? 'Only ' + set.revocableCount + ' of the ' + set.size + ' can ' +
                'be revoked; the rest are left alone.'
              : 'All ' + set.size + ' can be revoked.')) +
            '<div class="formrow">' +
            '<form method="post" action="/admin/tokens" class="inline">' +
              '<input type="hidden" name="action" value="revoke-set">' +
              '<input type="hidden" name="set" value="' + self.esc(set.setKey) +
              '"><input type="hidden" name="back" value="' + self.esc(backRow) +
              '"><button class="danger">Revoke this set</button></form> ' +
            '<form method="post" action="/admin/tokens" class="inline">' +
              '<input type="hidden" name="action" value="restore-set">' +
              '<input type="hidden" name="set" value="' + self.esc(set.setKey) +
              '"><input type="hidden" name="back" value="' + self.esc(backRow) +
              '"><button class="secondary">Restore this set</button></form>' +
            '</div>' +
            self.note('Restore is <strong>NON-SPEC</strong> and no real ' +
            'authorization server can offer it — a resource server may ' +
            'already have cached the refusal. It is here because getting ' +
            'back to a working token otherwise means restarting this service.')
          : '') +

        '<h2>' + (set.grouped ? 'The ' + set.size + ' credentials' : 'The ' +
            'credential') +
        '</h2><table><tr><th>Kind</th><th>State</th><th>User</th><th>Subject' +
        '</th>' +
        '<th>Client, audience or service</th><th>Detail</th><th>Presented ' +
        'as</th><th>Issued</th><th>Expires</th><th>jti or ' +
        'ID</th><th></th></tr>' +
        memberRows + '</table>' +
        self.note('In the order they were minted. Every identifier is a link ' +
        'to where that credential came from — who it was issued to, in whose ' +
        'name, to reach what, and every generation behind it if a token ' +
        'exchange or a refresh produced it. <strong>That lineage is a ' +
        'different relation from this page</strong>: this is what arrived ' +
        '<em>together</em>, and that is what one credential descends ' +
        '<em>from</em>. Refreshing this set produces a new set beside it, ' +
        'not a fourth member of it, and the lineage is what joins the two.') +
        (set.grouped
          ? self.note('<strong>The Detail column will disagree with itself ' +
            'here, and that is by design.</strong> An access token\'s ' +
            '<code>scope</code> is what that token can do; the refresh token ' +
            'beside it carries what was <em>authorized</em>, which is wider ' +
            'whenever a scope became the audience — see the tokens page. A ' +
            'set that reported one scope would be hiding the one place the ' +
            'two halves of a grant deliberately differ.')
          : '');

      self.respond(req, res, view.json, 'Tokens — one issuance',
                   '/admin/tokens', inner,
                   up);
      log.debug("Leaving the admin token set page. " + set.size +
                " member(s).");
    });

    // -------------------------------------------------------------------------
    // GET /admin/tokens/credential?id=… — WHERE ONE CREDENTIAL CAME FROM.
    //
    // A DRILL-DOWN OF THE TOKENS PAGE, and the first that page has had: every
    // identifier in its last column links here. It hangs under it exactly as
    // the three delegation drill-downs hang under /admin/delegation — no `NAV`
    // row, `active` is '/admin/tokens', `up` carries the filter and the page
    // the reader left — so the trail reads `Tokens › One credential` and the
    // way back is the row they clicked on rather than the top of a list of
    // everything.
    //
    // **IT IS THE SAME PICTURE AS THE DELEGATION MAP, ASKED A DIFFERENT
    // QUESTION.** `common/credential_graph.ts` holds the model (rule 3l, the
    // same division `delegation.js` and `user_graph.js` are on the other side
    // of) and returns a graph in `delegation.graph()`'s shape, so
    // `delegation_map.js` draws it, the party table is `delegationNodeRow()`
    // and the line table is `userEdgeRow()` — the one that already knows the
    // two relations an ISSUANCE uses. Nothing here draws anything of its own,
    // which is what keeps a party on this page the same party, drawn the same
    // way, as on the four pages that had it first.
    //
    // **THE QUESTION IS ANCESTRY AND THE ANSWER IS USUALLY "NONE", WHICH IS NOT
    // AN EMPTY PAGE.** Most credentials were issued directly: no exchange, one
    // generation, three boxes and the grant that produced them. That is the
    // common case and the page leads with it. The interesting case is the other
    // one — a token that came out of an RFC 8693 exchange, whose subject token
    // came out of another, back to a browser sign-in three tiers away — and it
    // is drawn as one picture with every generation on it, because the whole
    // point is that the far end of that chain holds a credential naming
    // somebody who never spoke to it.
    //
    // **A CREDENTIAL THIS SERVICE NO LONGER HOLDS IS NOT A 404**, which is the
    // rule /admin/logout and both delegation drill-downs already follow and
    // which matters more here than anywhere: BOTH registers behind this page
    // are capped and drop the oldest, and they are capped separately — so a
    // lineage can know an identifier existed (a delegation act names it) and
    // nothing else about it, and an old link coming back thin is the ordinary
    // outcome rather than a mistake. The page says which of the two states it
    // is in.
    //
    // No form, so no operation on /admin-api — rule 7, satisfied exactly as the
    // delegation drill-downs satisfy it. `?format=json` is the lineage and the
    // graph; `?format=svg` is the document alone.
    // -------------------------------------------------------------------------
    app.get('/admin/tokens/credential', function (req, res) {
      log.debug("Entering the admin credential lineage page.");
      const asked = String(req.query.id || '').trim();
      const known = knownUserKeys();
      const lineage = asked ? credentialGraph.lineageOf(asked) : null;
      // An empty graph rather than none when nothing is selected: `?format=svg`
      // is answered before the prose below is drawn, and a route that answered
      // HTML there would send a browser a diagram it asked for as an image.
      const graph = lineage ? lineage.graph : delegation.graph([]);
      const look = self.delegationLooks(graph, known);
      const labelOf = look.labelOf;

      // WHICH BOX IS THE ONE THIS CREDENTIAL IS AT NOW. `delegationNodeLook()`
      // stays the one answer to "what is this box" — that is what keeps every
      // page here drawing one party one way — so this is APPENDED to what it
      // said, the way the person's page appends its own counts.
      const holder = lineage && lineage.credential
        ? stats.identityKeyOf(userGraph.holderOf(lineage.credential)) : '';
      if (holder) {
        const entry = look.looks[holder];
        if (entry) {
          entry.title = entry.title + '\nTHIS IS WHERE THE CREDENTIAL THIS ' +
            'PAGE IS ABOUT ENDED ' +
            'UP: ' + (lineage.credential.kind || 'a credential') + ' ' +
            asked + '.';
        }
      }

      const drawingLabel = lineage
        ? 'How ' +
          (lineage.credential ? lineage.credential.kind : 'this credential') +
          ' ' + asked + ' came to exist'
        : 'One credential, and every generation behind it';

      if (String(req.query.format || '') === 'svg') {
        self.sendDelegationSvg(res, graph, look, drawingLabel);
        log.debug("Leaving the admin credential lineage page. Answered SVG.");
        return;
      }

      const listView = self.listViewOf('/admin/tokens', req.query);
      const up = self.upTo('/admin/tokens', 'One credential', listView);
      const back = self.note('<a class="btn" href="' + self.esc(up.href) +
        '">&larr; Back to the tokens table</a>');

      if (!lineage) {
        const inner = self.messagesOf(req) + back +
          self.note('<strong>Name a credential.</strong> This page draws ONE ' +
          'of them and the way to it is a link on <a href="' +
          self.esc(up.href) +
          '">the tokens table</a> — every identifier there is one. It is ' +
          'keyed on the identifier the protocol gave the credential (a ' +
          '<code>jti</code>, an <code>AssertionID</code>), which is the only ' +
          'thing the issued register and the delegation register both hold ' +
          'about the same object, and is the reason a Kerberos ticket has no ' +
          'link: that protocol has no identifier to quote.');
        self.respond(req, res, { identifier: null, lineage: null },
                     'Tokens — one credential', '/admin/tokens', inner, up);
        log.debug("Leaving the admin credential lineage page. Nothing was " +
                  "asked for.");
        return;
      }

      const parties = graph.nodes.filter(function (node) {
        return node.kind !== 'sts';
      });
      const picture = self.delegationDrawing(graph, look,
                                             '/admin/tokens/credential',
                                             Object.assign({}, listView,
                                                 { id: asked }),
                                             drawingLabel);
      const credential = lineage.credential;

      // THE ONE SENTENCE THIS PAGE OPENS WITH. A reader arriving from a row of
      // a ten-column table has to be told immediately that they are looking at
      // the same credential — and then the one fact the row could not tell
      // them, which is whether anything is behind it.
      const sentence = self.note('<strong><code>' + self.esc(asked) +
        '</code></strong> — ' +
        (credential
          ? self.esc(credential.kind) + ', issued ' +
            self.esc(self.whenText(credential.issuedAt)) +
            ', <span class="' + self.stateClass(credential.state) + '">' +
            self.esc(credential.state) + '</span>'
          : '<span class="state-expired" title="' +
            self.esc('The issued register is capped and drops the oldest, so ' +
                     'a credential a delegation act still names can be one ' +
                     'this service can no longer describe. That is a bounded ' +
                     'store working as intended rather than a gap in the ' +
                     'recording.') +
            '">no longer held in the issued register</span>') +
        '. ' +
        (lineage.counts.exchanges
          ? '<strong>' + self.esc(lineage.counts.exchanges) + ' ' +
            'exchange(s)</strong> are behind it, so it is generation ' +
            self.esc(lineage.generations.length - 1) + ' of a line that ' +
            'starts at an ordinary issuance.'
          : '<strong>Nothing was exchanged to get it.</strong> It was issued ' +
            'directly, which is the ordinary case: three of the sixteen ' +
            'protocol families here can delegate at all.'));

      const inner = self.messagesOf(req) + back +
        '<div class="tiles">' +
          self.tile(lineage.counts.generations, 'generations') +
          self.tile(lineage.counts.exchanges, 'exchanges behind it') +
          self.tile(lineage.counts.parties, 'parties involved') +
          self.tile(lineage.counts.acts, 'delegation acts') +
        '</div>' +

        sentence +

        (lineage.truncated
          ? '<p class="note state-expired"><strong>The line is longer than ' +
            self.esc(credentialGraph.MAX_GENERATIONS) + ' generations and ' +
            'the rest was not walked.</strong> What is drawn below is the ' +
            'newest ' +
            self.esc(credentialGraph.MAX_GENERATIONS) + ' of it, so the ' +
            'oldest box on this picture is NOT the origin. Said rather than ' +
            'left to be assumed, because a lineage that stops quietly reads ' +
            'as an issuance that never happened.</p>'
          : '') +

        '<h2>How it came to exist</h2>' +
        self.note('One row per generation, newest first: the credential ' +
        'itself, then whatever was handed in to get it, and so on. ' +
        '<strong>The last row is the origin</strong> — the row with no ' +
        'exchange behind it, which is the issuance the whole line rests on.') +
        '<table><tr><th class="num">Gen</th><th>Identifier</th><th>Kind</th>' +
        '<th>Held by</th><th>In whose name</th><th>Issued</th>' +
        '<th>How it was got</th></tr>' +
        lineage.generations.map(function (row) {
          const held = row.credential;
          return '<tr>' +
            '<td class="num">' + self.esc(row.generation) + '</td>' +
            '<td class="who">' + self.shortened(row.identifier, 14) +
              (row.identifier === asked
                ? '<br><span class="state-none">this page</span>' : '') +
                  '</td>' +
            '<td>' + (held ? self.esc(held.kind)
              : '<span class="state-none" title="' +
                self.esc('Named by a delegation act, and no longer in the ' +
                         'issued register — the two stores are capped ' +
                         'separately.') +
                '">not held</span>') + '</td>' +
            '<td class="who">' + (held
              ? self.esc(userGraph.holderOf(held) || '—') : '<span ' +
                'class="state-none">&mdash;</span>') + '</td>' +
            '<td class="who">' + (held
              ? self.esc((held.family === 'token' ? (held.username || held.sub)
                                                  : held.subject) || '—')
              : '<span class="state-none">&mdash;</span>') + '</td>' +
            '<td>' + (held ? self.esc(self.whenText(held.issuedAt))
              : '<span class="state-none">&mdash;</span>') + '</td>' +
            '<td>' + (row.act
              ? '<code>' + self.esc(row.act.type) + '</code><br>' +
                '<span class="state-none">' + self.esc(row.act.typeLabel) +
                '</span><br><a href="' + self.esc('/admin/delegation/chain' +
                  queryWith({}, { chain: row.act.chainKey })) +
                '">the relationship</a>'
              : '<strong>the origin</strong><br><span class="state-none">' +
                self.esc(held && held.family === 'token'
                  ? userGraph.flowRow(held.grant).label
                  : (held ? userGraph.artifactFlowRow(held.kind).label
                          : 'nothing here produced it')) + '</span>') +
                            '</td>' +
            '</tr>';
        }).join('') + '</table>' +

        (lineage.walls.length
          ? self.note('<strong>One line stops at a credential this service ' +
            'cannot name.</strong> ' +
            lineage.walls.map(function (wall) {
              return self.esc(wall.credential.kind) + ' — ' +
                self.esc(wall.credential.note || 'no identifier');
            }).join('; ') +
            '. That is a different answer from "this is the origin": ' +
            'something was presented and exchanged, and the protocol gave it ' +
            'nothing this register could write down. A Kerberos ticket has ' +
            'no identifier at all, and WS-Trust consumes the requester\'s ' +
            'WS-Security credential, which this service never issued.')
          : '') +

        '<h2>The whole line, as one picture</h2>' +
        self.note('Every actor and every relationship behind this one ' +
        'credential. <strong>The hexagon is this service</strong>; a dashed ' +
        'grey line from it is a credential being handed to whoever asked. An ' +
        '<em>issued for</em> line is an ordinary grant — this client holds a ' +
        'credential naming that person — and it is the console\'s neutral ' +
        'indigo, because an authorization code grant claims neither ' +
        'impersonation nor delegation. <em>acts for</em> and ' +
        '<em>reaches</em> are the delegation picture\'s own two claims and ' +
        'are coloured by mode where a delegation is what produced them, ' +
        'exactly as they are on <a href="/admin/delegation/map">the map</a>. ' +
        'A <em>reaches</em> line out of an ordinary grant takes no mode and ' +
        'stays indigo, for the reason the <em>issued for</em> line beside it ' +
        'does: what a token is ADDRESSED to is a relationship this service ' +
        'granted, and nothing was impersonated to get it. The audience the ' +
        'token carries is in that line\'s tooltip, because the box is named ' +
        'after whichever application registered it.') +
        picture.html +

        '<h2>The parties</h2>' +
        self.note('Every box on the picture. A party can appear because it ' +
        'held one of these credentials, because it exchanged one, or both — ' +
        'the middle tier of a chain is the TARGET of one generation and the ' +
        'INTERMEDIARY of the next, which is what makes the two hops one line ' +
        'rather than two pictures.') +
        '<table><tr><th>Label</th><th>Drawn as</th><th>Identity</th>' +
        '<th>Roles it played</th><th>Acts</th><th>Protocols</th></tr>' +
        (parties.map(function (node) {
          return self.delegationNodeRow(node, known, look.looks[node.id]);
        }).join('') || '<tr><td colspan="6">No parties.</td></tr>') +
          '</table>' +

        '<h2>Every line, in words</h2>' +
        self.note('The picture read as a table, because a diagram nobody can ' +
        'quote is a diagram nobody can put in a bug report.') +
        '<table><tr><th>From</th><th>To</th><th>Relationship</th><th>' +
        'Mechanism ' +
        'or grant</th><th>Kind</th><th>Acts</th><th>Credentials</th><th>What ' +
        'came out</th></tr>' +
        (graph.edges.map(function (edge) {
          return self.userEdgeRow(edge, labelOf);
        }).join('') || '<tr><td colspan="8">No lines.</td></tr>') + '</table>' +

        (lineage.acts.length
          ? '<h2>Every delegation act in the line</h2>' +
            self.note('The rows <a href="/admin/delegation">the delegation ' +
            'table</a> holds for this lineage, in order, not paged.') +
            '<table><tr><th class="num">#</th><th>When</th><th>Mechanism</th>' +
            '<th>Kind</th><th>Outcome</th><th>Initial identity</th>' +
            '<th>Intermediary</th><th>Target</th><th>Authorized by / why ' +
            'not</th><th>Credentials</th></tr>' +
            lineage.acts.map(function (row) {
              return self.delegationRow(row, known, { listView: {} });
            }).join('') + '</table>'
          : '') +

        (credential && credential.family === 'token' &&
         (credential.username || credential.sub)
          ? self.note('<a href="' + self.esc('/admin/delegation/user' +
              queryWith({}, { user: stats.identityKeyOf(credential.username ||
                                                        credential.sub) })) +
            '">Everything this service has done in that person\'s name</a> ' +
            'is the other picture: this one is one credential and its ' +
            'ancestors, that one is one person and everything ever issued ' +
            'naming them.')
          : '') +

        self.note('<code>?format=json</code> carries the lineage — every ' +
        'generation with the act that produced it, the acts, the origins and ' +
        'the graph behind the picture; <code>?format=svg</code> is the ' +
        'document alone. There is no form on this page and therefore no ' +
        'operation on <code>/admin-api</code>: the acts are in <code>GET ' +
        '/admin-api/delegation</code> and the credentials are in <code>GET ' +
        '/admin-api/tokens</code>.');

      self.respond(req, res, {
        identifier: asked,
        held: lineage.held,
        credential: lineage.credential,
        counts: lineage.counts,
        generations: lineage.generations,
        origins: lineage.origins,
        issuances: lineage.issuances,
        walls: lineage.walls,
        truncated: lineage.truncated,
        acts: lineage.acts,
        graph: graph,
        drawing: { width: picture.drawn.width, height: picture.drawn.height,
                   failed: picture.drawn.failed || null }
      }, 'Tokens — one credential', '/admin/tokens', inner, up);
      log.debug("Leaving the admin credential lineage page. " +
                lineage.counts.generations + " generation(s).");
    });

    app.get('/admin/logout', function (req, res) {
      log.debug("Entering the admin sign-out page.");
      const view = self.logoutView(req);
      self.respond(req, res, view.json, view.title, '/admin/logout', view.inner,
                   view.up);
      log.debug("Leaving the admin sign-out page.");
    });

    app.post('/admin/logout', function (req, res) {
      log.debug("Entering the admin sign-out action endpoint.");
      const body = parseBody(req);
      const result = logoutAction(body);
      // Back to the same person's page carrying whatever filter and page the
      // form came from — the rule every form on this console follows.
      //
      // SINCE 2026-09-05 THIS HANDLER SERVES TWO PAGES. `/admin/users` has a
      // global sign-out button of its own, which posts here rather than
      // reimplementing the act, so `from` says which page to return to. It is
      // read as an ENUM and never as a path — both targets are written out here
      // — which is the rule `backTo()` follows on the tokens page and what
      // keeps a `back` field carrying `//evil.example` from becoming a redirect
      // off this service.
      const back = String(body.from || '') === 'users'
        ? '/admin/users' +
          queryWith(self.listViewFromBack('/admin/users', body.back),
                    { user: String(body.user || '') })
        : '/admin/logout' +
          queryWith(self.listViewFromBack('/admin/logout', body.back),
                    { user: String(body.user || '') });
      self.respondToAction(req, res, back, result);
      log.debug("Leaving the admin sign-out action endpoint.");
    });

    app.get('/admin/audit', function (req, res) {
      log.debug("Entering the admin audit page.");
      const view = auditView(req.query);
      const paging = view.paging;
      const summary = view.summary;
      const known = knownUserKeys();
      // What every paging link carries with it. The page number is not in here
      // — pageNavPair() supplies that per link — for the reason the tokens page
      // gives: a "next" that dropped the filter would be page 2 of a different
      // list.
      const filterParams = { category: view.wantedCategory,
                             action: view.wantedAction,
                             outcome: view.wantedOutcome,
                             actor: view.wantedActor,
                             q: view.wantedText, code: view.wantedCode,
                             address: view.wantedAddress,
                             per: req.query.per ? paging.perPage : '' };
      const nav = self.pageNavPair('/admin/audit', filterParams, paging);

      const rows = view.shown.map(function (row) {
        return self.auditRow(row, known);
      }).join('');

      const categoryOptions = ['<option value=""' +
                               (view.wantedCategory ? '' : ' ' +
          'selected') +
                               '>any category</option>']
        .concat(auditLog.CATEGORIES.map(function (entry) {
          return '<option value="' + self.esc(entry.category) + '"' +
                 (entry.category === view.wantedCategory ? ' selected' : '') +
                 '>' +
                 self.esc(entry.label) + ' (' +
                 (summary.byCategory[entry.category] || 0) + ')</option>';
        })).join('');

      // Grouped by category, and built from the SAME table the category select
      // is, so the two cannot come to disagree about which action belongs where
      // — which they would, being two hand-written lists of the same
      // twenty-four strings.
      const actionOptions = '<option value=""' + (view.wantedAction ? '' : ' ' +
          'selected') +
        '>any action</option>' +
        auditLog.CATEGORIES.map(function (entry) {
          const inGroup = auditLog.ACTIONS.filter(function (a) {
            return a.category === entry.category;
          });
          return '<optgroup label="' + self.esc(entry.label) + '">' +
                 inGroup.map(function (a) {
            return '<option value="' + self.esc(a.action) + '"' +
                   (a.action === view.wantedAction ? ' selected' : '') + '>' +
                   self.esc(a.action) + ' (' +
                   (summary.byAction[a.action] || 0) +
                   ')</option>';
          }).join('') + '</optgroup>';
        }).join('');

      const outcomeOptions = ['<option value=""' +
                              (view.wantedOutcome ? '' : ' ' +
          'selected') +
                              '>any outcome</option>']
        .concat(auditLog.OUTCOMES.map(function (name) {
          return '<option value="' + self.esc(name) + '"' +
                 (name === view.wantedOutcome ? ' selected' : '') + '>' +
                 self.esc(name) +
                 ' (' + (summary.byOutcome[name] || 0) + ')</option>';
        })).join('');

      const perOptions = self.perPageOptions(paging.perPage);

      const filtering = view.wantedCategory || view.wantedAction ||
                        view.wantedOutcome ||
                        view.wantedActor || view.wantedText ||
                        view.wantedCode || view.wantedAddress;

      const inner = self.messagesOf(req) +
        '<div class="tiles">' +
          self.tile(summary.held, 'events held') +
          self.tile(summary.recorded, 'events recorded') +
          self.tile(summary.dropped, 'dropped (oldest first)') +
          self.tile(summary.byCategory.directory || 0, 'directory') +
          self.tile(summary.byCategory.authentication || 0, 'authentications') +
          self.tile(summary.byCategory.session || 0, 'session events') +
        '</div>' +

        self.note('What this service has been asked to do, in the order it ' +
        'was asked, newest first. The other pages here are <em>state</em> — ' +
        'how many calls, which tokens are still valid, who is in ' +
        '<code>cn=developers</code>. This one is <em>history</em>: the ' +
        'metrics page can say the directory holds eleven entries, and only ' +
        'this page can say that a twelfth was created at 14:02 and deleted ' +
        'at 14:03 by somebody bound as <code>uid=carol</code>, over LDAPS.') +

        self.note('<strong>No credential is ever recorded here.</strong> Not ' +
        'a password, not a bearer token, not an assertion, and no request or ' +
        'response body. An event carries the facts of what happened — who, ' +
        'what, where, the outcome — and the identifiers that are already ' +
        'safe to show. A modify names the attributes it changed and never ' +
        'their values, because a modify is where a <code>userPassword</code> ' +
        'gets set; a compare says whether it matched and not what was tried; ' +
        'an <code>authorization code</code> in a query string is replaced ' +
        'with <code>(redacted)</code>. The debug log is where somebody who ' +
        'wants the bodies looks, and it is a log rather than a web page.') +

        self.note('<strong>One act usually produces several rows, and they ' +
        'are not duplicates.</strong> Signing in at ' +
        '<code>/authn/login</code> writes three: the HTTP call ' +
        '(<code>protocol.call</code>), the credential being accepted ' +
        '(<code>authentication</code>) and the session that came out of it ' +
        '(<code>session.start</code>). Those are three facts at three ' +
        'layers, and which one answers your question depends on the question ' +
        '— a Kerberos AS-REQ authenticates somebody and starts no session at ' +
        'all, and an LDAP bind does both without an HTTP request anywhere in ' +
        'it. Collapsing them would mean choosing, once and for everybody, ' +
        'which of the three this page can answer.') +

        self.note('<strong>This log observes itself.</strong> Drawing this ' +
        'page is console access, so fetching it records an ' +
        '<code>admin.view</code> event and the list is one row longer than ' +
        'it was when you asked. That is not a defect being left unfixed: ' +
        'suppressing it would put a blind spot exactly where the person ' +
        'reading the audit log stands. Filter by category to read past it.') +

        '<h2>What happened</h2>' +
        // No `page` input in this form, deliberately: changing a filter or the
        // page size returns to page 1. Carrying the old page number over would
        // land somebody on page 6 of a two-page result and the clamp in
        // pagingOf() would then move them again, which reads as the form
        // ignoring them.
        '<form method="get" action="/admin/audit"><div ' +
          'class="formrow"><label for="category">Category</label><select ' +
          'id="category" name="category">' +
            categoryOptions + '</select>' +
          '<label for="action">Action</label><select id="action" ' +
          'name="action">' +
            actionOptions + '</select>' +
          '<label for="outcome">Outcome</label><select id="outcome" ' +
          'name="outcome">' +
            outcomeOptions + '</select>' +
          '<label for="per">Per page</label><select id="per" name="per">' +
        perOptions +
            '</select>' +
        '</div><div class="formrow">' +
          '<label for="actor">Actor</label>' +
          '<input type="text" id="actor" name="actor" size="20" value="' +
            self.esc(view.wantedActor) + '" placeholder="alice">' +
          '<label for="q">Text</label>' +
          '<input type="text" id="q" name="q" size="30" value="' +
        self.esc(view.wantedText) +
            '" placeholder="a DN, a path, anything in the summary">' +
          '<label for="code">Error code</label>' +
          '<input type="text" id="code" name="code" size="16" value="' +
            self.esc(view.wantedCode) + '" placeholder="STS-OAUTH">' +
          '<label for="address">From</label>' +
          '<input type="text" id="address" name="address" size="16" ' +
            'value="' + self.esc(view.wantedAddress) +
            '" placeholder="10.0.0.">' +
          '<button class="secondary">Filter</button>' +
          (filtering ? ' <a href="/admin/audit">clear</a>' : '') +
        '</div></form>' +
        self.note('Category and Action narrow together, like any two ' +
        'filters, so an action from another category matches nothing — which ' +
        'is what an empty table below then means. Actor matches a substring ' +
        'of either spelling of the name, because the actor on a directory ' +
        'row is a bind DN and the one on a Kerberos row is ' +
        '<code>alice@REALM</code>; the collapse to a single key can only be ' +
        'done where an identity is normalised. Error code matches the front ' +
        'of a code, so <code>STS-OAUTH</code> is every OAuth failure and a ' +
        'whole code is one condition; every refused or failed request ' +
        'carries one, listed on the <em>Error codes</em> page of the ' +
        'documentation. A code is recorded here and in the service log and ' +
        'is never sent to the client; <a href="/admin/error-codes">Error ' +
        'codes</a> says what each one means.') +
        nav.head +
        '<table><tr><th class="num">#</th><th>When</th><th>Category</th><th>' +
        'Action</th><th>Outcome</th><th>Actor</th><th>From</th>' +
        '<th>Target</th><th>What happened</th><th>Detail</th></tr>' +
        (rows || '<tr><td colspan="10">Nothing matches.</td></tr>') +
        '</table>' +
        nav.foot +

        self.note(view.filtered.length + ' row(s) match' +
        (paging.pages > 1 ?
         ', of which rows ' + paging.firstRow + '&ndash;' + paging.lastRow +
                            ' are on this page (' + paging.page + ' of ' +
                            paging.pages + ')' : '') +
        '; ' + summary.held + ' held of ' + summary.recorded + ' recorded ' +
        'since this process started' +
        (summary.dropped
          ? ', and <strong>' + summary.dropped + ' dropped</strong> — the ' +
                                                 'log holds at most ' +
            summary.maxEvents + ' events and discards the oldest first. ' +
            'Raise <code>audit.maxEvents</code> in the settings at the foot ' +
            'of this page if that is losing something you need.'
          : '. The cap is ' + summary.maxEvents + ' events and nothing has ' +
                                                  'been dropped yet.')) +

        self.note('The <strong>#</strong> column is a sequence number, and ' +
        'it is monotonic and never reused — including across a drop. That is ' +
        'what makes it a stable name for an event: a caller can say &ldquo;I ' +
        'have read up to ' +
        (summary.newestSeq || 0) + '&rdquo; and mean it, where a row number ' +
        'would silently name a different event as soon as anything was ' +
        'discarded. <code>?format=json</code> carries <code>oldestSeq</code> ' +
        'and <code>newestSeq</code> for exactly that: a gap between the last ' +
        'one you saw and <code>oldestSeq</code> is how many events you ' +
        'missed.') +

        '<h3>Where the rows come from</h3>' +
        self.note('Six categories and five recording points, rather than a ' +
        'recording site per feature. Each of these is a funnel this service ' +
        'already had:') +
        '<ul>' + auditLog.CATEGORIES.map(function (entry) {
          // The label, the category and the count stay on the row and the
          // paragraph explaining the category folds under them, which is the
          // shape every legend on this console now has.
          return '<li><strong>' + self.esc(entry.label) + '</strong> (<code>' +
                 self.esc(entry.category) +
                 '</code>, ' + (summary.byCategory[entry.category] || 0) +
                 ') ' +
                 self.note(self.esc(entry.what)) + '</li>';
        }).join('') + '</ul>' +

        self.note('<strong>Every row says where it came from</strong> ' +
        '(since 2026-09-18): <em>From</em> is the client\'s IP address for ' +
        'whatever the request, LDAP operation, Kerberos message or SPIRE ' +
        'Server API call caused — an authentication, a refused sign-in, a ' +
        'consent, a sign-out — and is empty for what this service did on its ' +
        'own. It is the address <code>global.trustProxy</code> and ' +
        '<code>global.trustedProxies</code> resolve: the right-most ' +
        '<code>X-Forwarded-For</code> hop that is not a proxy you named, or ' +
        'the client in a PROXY protocol header. <strong>With neither set ' +
        'behind a proxy, it is the proxy</strong> — on a laptop, the compose ' +
        'bridge — because that is the nearest hop that did not say who it ' +
        'forwarded for. The CHANNEL is still under the target: ' +
        '<code>http</code>, <code>ldap</code>, <code>ldaps</code>, ' +
        '<code>kerberos</code>, <code>grpc</code>, <code>tls</code>, ' +
        '<code>console</code> (an act on this console or ' +
        '<code>/admin-api</code>), <code>internal</code> (something this ' +
        'service did on its own), or <code>none</code> (a session that ' +
        'expired).') +

        self.note('<strong>It is in memory and dies with the ' +
        'process</strong>, like the counters, the sessions and the signing ' +
        'key. There is no compliance story here to serve: this service ' +
        'checks no password anywhere, so an audit log of it is a debugging ' +
        'aid and not a record of anything. It also has no clear button, and ' +
        'that is a decision rather than an omission — an erase control on an ' +
        'unprotected console would make the page unable to answer the one ' +
        'question an audit log exists for. Restarting the service is how you ' +
        'get an empty one.') +

        // THE TWO SETTINGS ARE ON THIS PAGE NOW rather than being described
        // here and typed in somewhere else. `audit.protocolCalls` is the reason
        // this one matters: it is the noisy category, and somebody turning it
        // off is doing it BECAUSE they are looking at this page and cannot read
        // it.
        self.configFormsFor('/admin/audit') +
        self.note('<code>audit.protocolCalls</code> (now ' +
        (summary.protocolCalls ? 'on' : '<strong>off</strong>') + ') is the ' +
        'noisy one — every JWKS poll and metadata fetch is an event — so ' +
        'turning it off is how somebody watching the directory or the ' +
        'console gets a readable page. It never affects the other five ' +
        'categories, and <a href="/admin/metrics">the metrics page</a> ' +
        'counts every call either way. Both take effect immediately, and ' +
        'lowering <code>audit.maxEvents</code> ' +
        '(now ' + summary.maxEvents + ') discards ' +
        'the oldest rows at once rather than on the next event.') +

        self.note('<strong>One request is deliberately never a row here: ' +
        '<code>GET /healthcheck</code> when it answered 200.</strong> It is ' +
        'asked every few seconds for the whole life of this service — by the ' +
        'compose healthcheck and by every launcher that waits for it to come ' +
        'up — and it always answers the same thing, so recorded it would be ' +
        'by a wide margin the most common row on this page and would push ' +
        'everything you came here to read off the end of the cap above. A ' +
        'probe that answered anything ELSE is recorded as usual, which is ' +
        'the half worth knowing: a failing healthcheck is exactly the event ' +
        'somebody hunting a start-up failure is looking for. <a ' +
        'href="/admin/metrics">The metrics page</a> counts every probe ' +
        'either way.') +

        self.note('Paging is <code>?page=</code> and <code>?per=</code> (at ' +
          'most ' +
        MAX_ROWS + ' rows a page) and both work with ' +
        '<code>?format=json</code>, whose reply carries <code>page</code>, ' +
        '<code>pages</code> and <code>matched</code> so a test can walk the ' +
        'whole list without guessing where it ends. The same list is at ' +
        '<code>GET /admin-api/audit</code> with the same parameters.');

      self.respond(req, res, Object.assign({}, view.json,
              { settings: self.configSettingsJson('/admin/audit') }),
                   'Audit log', '/admin/audit', inner);
      log.debug("Leaving the admin audit page.");
    });

    // -------------------------------------------------------------------------
    // GET /admin/used-assertions — WHAT THIS REALM HAS BEEN HANDED AND SPENT
    // (2026-09-13).
    //
    // Drawn from `adminViews.usedAssertionsView()`, which
    // `GET /admin-api/used-assertions` answers from too — rule 7, one function.
    // A PROMISE, because on a postgres store the history is the database's and
    // reading it is a query; a store that cannot be read is a page saying so
    // rather than a request that never answers.
    //
    // **NO CONTROL, AND THAT IS THE PAGE.** A Forget button would make an
    // assertion that is still inside its validity usable a second time, which
    // is the one thing the history exists to prevent. A row goes away when the
    // assertion expires, and no other way.
    // -------------------------------------------------------------------------
    app.get('/admin/used-assertions', function (req, res) {
      log.debug("Entering the admin used assertions page.");
      usedAssertionsView(req.query).then(function (view) {
        const json = view.json;
        const paging = view.paging;
        const filter = view.filter;
        const filterParams = { q: filter.q, format: filter.format,
                               use: filter.use,
                               state: filter.state,
                               per: req.query.per ? paging.perPage : '' };
        const nav = self.pageNavPair('/admin/used-assertions', filterParams,
                                     paging);

        const optionsOf = function (table, chosen, allLabel) {
          log.debug("Entering optionsOf().");
          log.debug("Leaving optionsOf().");
          return '<option value=""' + (chosen ? '' : ' selected') + '>' +
            self.esc(allLabel) + '</option>' +
            Object.keys(table).map(function (id) {
              return '<option value="' + self.esc(id) + '"' +
                     (id === chosen ? ' selected' : '') + '>' + self.esc(id) +
                     ' — ' +
                     self.esc(table[id]) + '</option>';
            }).join('');
        };

        const rows = view.rows.map(function (row) {
          return '<tr><td>' + self.esc(self.whenText(row.usedAt)) + '</td>' +
            '<td>' + self.esc(row.format === 'saml' ? 'SAML 2.0' : 'JWT') +
            '</td><td>' + self.esc(row.use === 'authorization-grant' ? 'grant'
              : row.use === 'request-object' ? 'request object'
                : 'client auth') +
            '</td>' +
            '<td class="who">' + self.shortened(row.issuer, 40) + '</td>' +
            '<td class="who">' + self.shortened(row.identifier, 32) + '</td>' +
            '<td class="who">' +
            (row.clientId ? self.shortened(row.clientId, 32)
              : '<span class="state-none">—</span>') + '</td>' +
            '<td class="who">' + (row.subject ? self.shortened(row.subject, 32)
              : '<span class="state-none">—</span>') + '</td>' +
            '<td>' + (row.state === 'spent' ? 'spent'
              : '<span class="state-none" title="' +
                self.esc(json.states.reserved) + '">in flight</span>') +
                '</td>' +
            '<td>' + self.esc(self.whenText(row.expiresAt)) + '</td></tr>';
        }).join('');

        const filtering = filter.q || filter.format || filter.use ||
                          filter.state;
        const storeSentence = json.persistent
          ? self.note('<strong>Held in the <code>' + self.esc(json.store) +
                      '</code> store</strong>, so it survives a restart' +
                      (json.atomicAcrossProcesses
                        ? ', and recording a use is one atomic claim in that ' +
                          'database, so every process against it agrees at ' +
                          'once.'
                        : '. That store does not coordinate processes, and a ' +
                          'service that dispatches refuses to start without ' +
                          'one that does.'))
          : self.warn('<strong>Held in this process only.</strong> ' +
                      self.esc(json.storeNote),
                      'Not persisted');

        const inner = self.messagesOf(req) +
          '<div class="tiles">' +
            self.tile(json.live, 'unexpired rows in this realm') +
            self.tile(json.cap, 'the most it will hold') +
            self.tile(json.matched, filtering ? 'match' : 'listed') +
            self.tile(json.store, 'store') +
          '</div>' +

          self.note('Every <strong>RFC 7523</strong> JWT and <strong>RFC ' +
          '7522</strong> SAML assertion this realm has accepted — to ' +
          'authenticate a client (<code>client_assertion</code>) or as an ' +
          'authorization grant (<code>assertion</code>) — and that has not ' +
          'yet expired. <strong>An assertion is accepted once, ' +
          'ever</strong>: this is ONE history for both uses and both ' +
          'profiles, keyed by the document\'s format, its issuer and its ' +
          '<code>jti</code> or <code>ID</code>, so a JWT that authenticated ' +
          'a client cannot then be spent as a grant. The assertion itself is ' +
          'never stored.') +

          self.note('<strong>In flight</strong> is an assertion that was ' +
          'accepted on a token request whose response has not finished; a ' +
          'replay racing it is refused exactly as on a spent one. It becomes ' +
          '<strong>spent</strong> only when that response is a 2xx — tokens ' +
          'were issued — and a request that failed for another reason (a bad ' +
          'code, an invalid scope, the issuance gate) RELEASES it, because ' +
          'an assertion that bought nothing has not been used.') +

          storeSentence +

          self.note('A row is kept until the assertion would have expired — ' +
          'its <code>exp</code> or <code>NotOnOrAfter</code> plus the clock ' +
          'skew allowed when it was read — and not a moment longer. When ' +
          '<code>oauth2.assertionReplayCacheSize</code> unexpired rows are ' +
          'held, the next assertion is REFUSED rather than a live row ' +
          'forgotten.') +

          '<form method="get" action="/admin/used-assertions"><div ' +
          'class="formrow">' +
            '<label for="q">Text</label>' +
            '<input type="text" id="q" name="q" size="28" value="' +
            self.esc(filter.q) + '" placeholder="an issuer, a jti, a client">' +
            '<label for="format">Format</label><select id="format" ' +
            'name="format">' +
            optionsOf(json.formats, filter.format, 'both') + '</select>' +
            '<label for="use">Use</label><select id="use" name="use">' +
            optionsOf(json.uses, filter.use, 'both') + '</select>' +
            '<label for="state">State</label><select id="state" name="state">' +
            optionsOf(json.states, filter.state, 'both') + '</select>' +
            '<label for="per">Per page</label><select id="per" name="per">' +
            self.perPageOptions(paging.perPage) + '</select>' +
            '<button class="secondary">Filter</button>' +
            (filtering ? ' <a href="/admin/used-assertions">clear</a>' : '') +
          '</div></form>' +
          nav.head +
          '<table><tr><th>Used</th><th>Format</th><th>As</th><th>Issuer</th>' +
          '<th>jti / ID</th><th>Client</th><th>Subject</th><th>State</th>' +
          '<th>Remembered until</th></tr>' +
          (rows || '<tr><td colspan="9">' + (filtering ? 'Nothing matches.'
            : 'No assertion has been accepted in this realm, or every one ' +
              'has expired.') + '</td></tr>') +
          '</table>' +
          nav.foot +

          self.note('This list is <code>GET ' +
          '/admin-api/used-assertions</code> with the same parameters. ' +
          'Paging is <code>?page=</code> and <code>?per=</code> (at most ' +
          MAX_ROWS + ' rows a page).');

        self.respond(req, res, json, 'Used assertions',
                     '/admin/used-assertions', inner);
        log.debug("Leaving the admin used assertions page.");
      }).catch(function (e) {
        log.error(errorCodes.tag('STS-ADMIN-0643') + 'admin: the ' +
                  'used-assertion history could not be read: ' +
                  ((e && e.message) || e));
        errorCodes.mark(res, 'STS-ADMIN-0643');
        self.respond(req, res,
                     { ok: false, error: String((e && e.message) || e) },
                     'Used assertions', '/admin/used-assertions',
                     self.warn('The used-assertion history could not be read ' +
                               'from the store: ' +
                               self.esc(String((e && e.message) || e)),
                               'It could not be read'));
        log.debug("Leaving the admin used assertions page. It threw.");
      });
    });

    // -------------------------------------------------------------------------
    // GET /admin/error-codes — THE ERROR CODE TABLE, AND WHICH OF IT IS
    // HAPPENING.
    //
    // Drawn from `adminViews.errorCodesView()`, which `GET
    // /admin-api/error-codes` answers from too, so the page and the operation
    // cannot disagree about a count. There is no control on it: the table is
    // source, and changing what a code means at runtime would make every alert
    // rule written against it a statement about a different condition.
    // -------------------------------------------------------------------------
    app.get('/admin/error-codes', function (req, res) {
      log.debug("Entering the admin error codes page.");
      const view = errorCodesView(req.query);
      const json = view.json;
      const paging = view.paging;
      const filterParams = { subsystem: view.wantedSubsystem,
                             q: view.wantedText,
                             seen: view.wantedSeen ? '1' : '',
                             per: req.query.per ? paging.perPage : '' };
      const nav = self.pageNavPair('/admin/error-codes', filterParams, paging);

      const subsystemOptions = '<option value=""' +
        (view.wantedSubsystem ? '' : ' ' +
          'selected') +
        '>every subsystem</option>' +
        view.subsystems.map(function (sub) {
          return '<option value="' + self.esc(sub.id) + '"' +
                 (sub.id === view.wantedSubsystem ? ' selected' : '') + '>' +
                 self.esc(sub.prefix + ' — ' + sub.label) + ' (' + sub.codes +
                 ')</option>';
        }).join('');

      const subsystemRows = view.subsystems.map(function (sub) {
        return '<tr><td><a href="/admin/error-codes?subsystem=' +
               encodeURIComponent(sub.id) + '"><code>' + self.esc(sub.prefix) +
               '</code></a></td><td>' + self.esc(sub.label) + '</td><td ' +
               'class="num">' + self.esc(sub.codes) + '</td>' +
               '<td class="num">' + (sub.seen
                 ? '<a href="/admin/audit?code=' +
                   encodeURIComponent(sub.prefix) +
                   '">' +
                   self.esc(sub.seen) + '</a>'
                 : '<span class="state-none">0</span>') + '</td>' +
               '<td class="who">' + self.esc(sub.where) + '</td></tr>';
      }).join('');

      const codeRows = view.shown.map(function (row) {
        return '<tr id="' + self.esc(row.code) + '">' +
          '<td><code>' + self.esc(row.code) + '</code>' +
            (row.retired ? '<br><span class="state-none">retired</span>' : '') +
          '</td><td>' + self.esc(row.summary) + '</td><td>' +
          (row.spec ? self.esc(row.spec) : '<span ' +
                'class="state-none">—</span>') + '</td><td ' +
          'class="num">' + (row.seen
            ? '<a href="/admin/audit?code=' + encodeURIComponent(row.code) +
              '">' +
              self.esc(row.seen) + '</a><br><span class="state-none">last ' +
              self.esc(self.whenText(row.lastSeenAt)) + '</span>'
            : '<span class="state-none">0</span>') + '</td>' +
          '</tr>';
      }).join('');

      const unregistered = view.unregisteredSeen.length
        ? '<h3>Codes on audit rows that the table does not hold</h3>' +
          self.note('These were recorded as given rather than dropped, so ' +
          'that the row saying the table is incomplete survives. Each one is ' +
          'a failure site whose code was never added to ' +
          '<code>common/error_codes.js</code>.') +
          '<table><tr><th>Code</th><th class="num">Rows</th></tr>' +
          view.unregisteredSeen.map(function (u) {
            return '<tr><td><a href="/admin/audit?code=' +
                   encodeURIComponent(u.code) +
                   '"><code>' + self.esc(u.code) +
                   '</code></a></td><td class="num">' +
                   self.esc(u.seen) + '</td></tr>';
          }).join('') + '</table>'
        : '';

      const filtering = view.wantedSubsystem || view.wantedText ||
                        view.wantedSeen;

      const inner = self.messagesOf(req) +
        '<div class="tiles">' +
          self.tile(json.registered, 'codes') +
          self.tile(json.subsystemCount, 'subsystems') +
          self.tile(json.distinctCodesSeen, 'codes on held audit rows') +
          self.tile(json.auditRowsWithCode, 'audit rows with a code') +
          self.tile(json.unregisteredSeen.length, 'unregistered codes seen') +
        '</div>' +

        self.note('Every way this service can fail or refuse has a code of ' +
        'the form <code>STS-&lt;SUBSYSTEM&gt;-&lt;NNNN&gt;</code>. It is ' +
        'recorded on the audit row for the event and at the front of the ' +
        'service\'s log line, and <strong>it is never sent to a ' +
        'client</strong> — not in a body, a header or a redirect. Each ' +
        'protocol here already defines how it reports an error, and a client ' +
        'under test must see exactly that: the <em>Client sees</em> column ' +
        'says what it is sent, and the code changes nothing about it.') +

        self.note('<strong>Seen</strong> counts the rows <a ' +
        'href="/admin/audit">the audit log</a> holds in this realm right now ' +
        '(' + self.esc(json.auditRowsHeld) + '), ' +
        'so it falls as that log\'s cap discards the oldest, and it is zero ' +
        'for a failure recorded only as a log line — one that stops the ' +
        'service starting, and everything the remote PEP container records, ' +
        'since that container has no audit log of its own. A zero here is ' +
        'not "never happens".') +

        self.note('<code>STS-HTTP-0002</code> and <code>STS-HTTP-0003</code> ' +
        'are what the HTTP call log records for a 4xx or 5xx response no ' +
        'handler gave a more specific code. <strong>A row carrying either ' +
        'names a failure site that is missing its own code.</strong> ' +
        '<code>STS-HTTP-0001</code> is an unrouted path, which is an ' +
        'ordinary outcome.') +

        '<h2>Subsystems</h2>' +
        '<table><tr><th>Prefix</th><th>Subsystem</th><th ' +
        'class="num">Codes</th><th class="num">Seen</th><th>Raised ' +
        'from</th></tr>' + subsystemRows +
        '</table><h2>Codes</h2><form ' +
        'method="get" action="/admin/error-codes"><div ' +
        'class="formrow"><label for="subsystem">Subsystem</label><select ' +
        'id="subsystem" name="subsystem">' +
            subsystemOptions + '</select>' +
          '<label for="q">Text</label>' +
          '<input type="text" id="q" name="q" size="30" value="' +
        self.esc(view.wantedText) +
            '" placeholder="a code, a word, invalid_grant">' +
          '<label for="seen"><input type="checkbox" id="seen" name="seen" ' +
          'value="1"' +
            (view.wantedSeen ? ' checked' : '') + '> only codes on held ' +
          'rows</label><label for="per">Per page</label><select id="per" ' +
          'name="per">' +
            self.perPageOptions(paging.perPage) + '</select>' +
          '<button class="secondary">Filter</button>' +
          (filtering ? ' <a href="/admin/error-codes">clear</a>' : '') +
        '</div></form>' +
        nav.head +
        '<table><tr><th>Code</th><th>What failed</th><th>Client sees</th>' +
        '<th class="num">Seen</th></tr>' +
        (codeRows || '<tr><td colspan="4">Nothing matches.</td></tr>') +
        '</table>' +
        nav.foot +
        unregistered +

        self.note('The table is <code>common/error_codes.js</code> and this ' +
        'page has no control: a code is never renumbered or reused, because ' +
        'it ends up in alert rules and saved searches, and a condition that ' +
        'stops existing keeps its row marked retired. The same table is ' +
        'published as <code>docs/error-codes.md</code>, generated from the ' +
        'source, and this list is at <code>GET /admin-api/error-codes</code> ' +
        'with the same parameters. Paging is <code>?page=</code> and ' +
        '<code>?per=</code> (at most ' +
        MAX_ROWS + ' rows a page).');

      self.respond(req, res, json, 'Error codes', '/admin/error-codes', inner);
      log.debug("Leaving the admin error codes page.");
    });

    app.get('/admin/delegation', function (req, res) {
      log.debug("Entering the admin delegation page.");
      const view = delegationView(req.query);
      // THE CONFIGURED REGISTER, built once and handed to the section and to
      // the JSON alike. It is deliberately NOT merged into `delegationView()`:
      // that function is the acts, it is shared with /admin/delegation/map and
      // with /admin-api/delegation, and folding a second register into it would
      // make every caller of the acts view pay for a walk of ou=applications it
      // did not ask for — and would make the two look like one thing in the one
      // place they must not.
      const permissions = permissionsView();
      // The same searches and slices permissionsSection() draws, computed here
      // for the reply. One pure function, called twice with the same arguments
      // — see its header for why it is not four lines inside the section.
      const allowedState = self.permissionsListState(req.query,
                                                     permissions.register);
      const paging = view.paging;
      const summary = view.summary;
      const policy = view.policy;
      const known = knownUserKeys();
      // What every paging link on this page carries with it, for the reason the
      // tokens page gives: a "next" that dropped the filter would be page 2 of
      // a different list. Each control then overrides its OWN list's page
      // number and leaves every other name alone, which pageNavPair() does off
      // the paging object it is handed rather than off this set.
      //
      // IT IS THE WHOLE QUERY SINCE 2026-09-01 AND IT USED TO BE FIVE NAMED
      // KEYS. Five was right while this page had one paged list; it has SEVEN
      // now, plus two chooser searches and two section searches, and a
      // hand-written set is exactly the thing that comes to be short by one —
      // `next ›` on the acts table would then reset the grants table to page 1
      // and clear the search that produced it, silently, three screens further
      // down where nobody is looking. pageParamsOf() takes everything except
      // `format`, `notice` and `error`, and pageNavPair() overrides only the
      // one name off the paging object it is handed, so every control on this
      // page moves its own list and nothing else.
      const navParams = pageParamsOf(req.query);
      const nav = self.pageNavPair('/admin/delegation', navParams, paging);

      // THE OTHER SIX LISTS. Every one of them is capped at DELEGATION_PER_PAGE
      // and shares this page's single `per`, which is the arrangement the
      // drill-downs already have — see perPageForm()'s header. Each carries a
      // page parameter of its own so that paging one leaves the other six where
      // the reader left them.
      //
      // THE MECHANISM CATALOGUE IS IN HERE AND THAT IS DELIBERATE, though
      // `delegation.TYPES` holds eight entries today and no control will be
      // drawn for it until it holds eleven. It is read off that table rather
      // than written down — a mechanism cannot be recordable and undocumented —
      // so its length is decided somewhere else, by somebody adding a ninth
      // mechanism who has no reason to be reading this page's layout. Paging it
      // now costs one line and makes the cap true by construction instead of
      // true today.
      const chainPage = pagedRows(req.query, view.chains,
        { name: 'chains', noun: 'chains', defaultPer: DELEGATION_PER_PAGE });
      const chainsNav = self.pageNavPair('/admin/delegation', navParams,
                                         chainPage.paging);
      const pairPage = pagedRows(req.query, policy.pairs,
        { name: 'pairs', noun: 'pairs', defaultPer: DELEGATION_PER_PAGE });
      const pairsNav = self.pageNavPair('/admin/delegation', navParams,
                                        pairPage.paging);
      const flagPage = pagedRows(req.query, policy.accounts,
        { name: 'flags', noun: 'accounts', defaultPer: DELEGATION_PER_PAGE });
      const flagsNav = self.pageNavPair('/admin/delegation', navParams,
                                        flagPage.paging);
      const mechanismPage = pagedRows(req.query, delegation.TYPES,
        { name: 'mechanisms', noun: 'mechanisms',
          defaultPer: DELEGATION_PER_PAGE });
      const mechanismsNav = self.pageNavPair('/admin/delegation', navParams,
                                             mechanismPage.paging);

      const listView = self.listViewOf('/admin/delegation', req.query);
      const rows = view.shown.map(function (row) {
        return self.delegationRow(row, known, { listView: listView });
      }).join('');

      // Grouped by protocol and built from the SAME table the filter offers, so
      // the two cannot come to disagree about which mechanism belongs to which
      // family.
      const protocolsInOrder = [];
      delegation.TYPES.forEach(function (entry) {
        if (protocolsInOrder.indexOf(entry.protocol) < 0) {
          protocolsInOrder.push(entry.protocol);
        }
      });
      const typeOptions = '<option value=""' +
        (view.wantedType ? '' : ' selected') +
        '>any mechanism</option>' +
        protocolsInOrder.map(function (protocol) {
          return '<optgroup label="' + self.esc(protocol) + '">' +
            delegation.TYPES.filter(function (entry) {
              return entry.protocol === protocol;
            }).map(function (entry) {
              return '<option value="' + self.esc(entry.type) + '"' +
                     (entry.type === view.wantedType ? ' selected' : '') + '>' +
                     self.esc(entry.label) + ' (' +
                     (summary.byType[entry.type] || 0) +
                     ')</option>';
            }).join('') + '</optgroup>';
        }).join('');

      const modeOptions = ['<option value=""' +
                           (view.wantedMode ? '' : ' selected') +
                           '>either kind</option>']
        .concat(delegation.MODES.map(function (entry) {
          return '<option value="' + self.esc(entry.mode) + '"' +
                 (entry.mode === view.wantedMode ? ' selected' : '') + '>' +
                 self.esc(entry.label) + ' (' +
                 (summary.byMode[entry.mode] || 0) +
                 ')</option>';
        })).join('');

      const outcomeOptions = ['<option value=""' +
                              (view.wantedOutcome ? '' : ' ' +
          'selected') +
                              '>any outcome</option>']
        .concat(delegation.OUTCOMES.map(function (name) {
          return '<option value="' + self.esc(name) + '"' +
                 (name === view.wantedOutcome ? ' selected' : '') + '>' +
                 self.esc(name) +
                 ' (' + (summary.byOutcome[name] || 0) + ')</option>';
        })).join('');

      const perOptions = self.perPageOptions(paging.perPage);

      const filtering = view.wantedType || view.wantedMode ||
                        view.wantedOutcome ||
                        view.wantedProtocol || view.wantedText;

      const inner = self.messagesOf(req) +
        '<div class="tiles">' +
          self.tile(summary.held, 'acts held') +
          self.tile(view.chains.length, 'distinct chains') +
          self.tile(summary.byMode.impersonation || 0, 'impersonations') +
          self.tile(summary.byMode.delegation || 0, 'delegations') +
          self.tile(summary.byOutcome.refused || 0, 'refused') +
          self.tile(policy.pairs.length, 'configured pairs') +
        '</div>' +

        self.note('<strong>Who acted on whose behalf, through what, to reach ' +
        'what.</strong> Three of the protocol families here can delegate and ' +
        'each calls it something different — Kerberos has S4U2Self, two ' +
        'flavours of S4U2Proxy and a forwarded ticket-granting ticket; ' +
        'WS-Trust has <code>OnBehalfOf</code> and <code>ActAs</code>; OAuth ' +
        '2.0 Token Exchange has impersonation and delegation. This page ' +
        'records all eight against one model, because the question people ' +
        'arrive with is protocol-independent: <em>alice never touched the ' +
        'back end, so why is there a ticket to it in her name, and who asked ' +
        'for it?</em>') +

        self.note('<strong>The three columns in the middle are the layers of ' +
        'the architecture</strong>, and the names are this page\'s own ' +
        'rather than any protocol\'s — a Kerberos front end, a WS-Trust ' +
        'requester and an OAuth client doing an exchange are the same ' +
        'position in the same picture:') +
        '<ul>' + delegation.ROLES.map(function (entry) {
          return '<li><strong>' + self.esc(entry.label) + '</strong> — ' +
                 self.esc(entry.what) +
                 '</li>';
        }).join('') + '</ul>' +
        self.note('A party can be a <em>person</em>, an ' +
        '<em>application</em>, or both, and the middle one routinely is ' +
        'both: <code>HTTP/frontend.example.com</code> has an entry under ' +
        '<code>ou=users</code> (it authenticates, so this service files it ' +
        'with the people) and an entry under <code>ou=applications</code> ' +
        '(tickets are issued FOR it). Each cell links to whichever of the ' +
        'two exist. An application marked <em>not in the registry</em> is ' +
        'not an error — the registry holds what this service has been ASKED ' +
        'ABOUT, and an RFC 8693 <code>audience</code> nobody has otherwise ' +
        'mentioned is exactly that.') +

        self.note('<strong>Impersonation and delegation are the axis worth ' +
        'filtering on</strong>, and they are not a matter of degree. Under a ' +
        'delegation the credential CARRIES the chain — an <code>act</code> ' +
        'claim, a composite <code>ActAs</code>, ' +
        '<code>S4U_DELEGATION_INFO</code> in the PAC — so the far end can ' +
        'see who is really asking and can decide differently because of it. ' +
        'Under an impersonation nothing does, which means <strong>this page ' +
        'is the only place it will ever be visible</strong>: no reading of ' +
        'the token afterwards, at the resource server or in a log, can ' +
        'recover the fact that a middle tier was involved.') +

        self.note('<strong>Refusals are recorded and are most of what this ' +
        'page is for.</strong> A delegation that worked tells you the ' +
        'plumbing is connected. A delegation that was refused names the two ' +
        'accounts, the two attributes and which of them was missing, at the ' +
        'moment the KDC decided — and the text in the <em>Authorized by / ' +
        'why not</em> column is the KDC\'s OWN words, the same sentence the ' +
        'client was sent, rather than a second wording that could come to ' +
        'disagree with it.') +

        // THE WAY TO THE PICTURE, ABOVE THE TABLE RATHER THAN UNDER IT. The
        // chains table lower down is what the diagram is drawn from and the
        // obvious place to put this link is beside it — which is most of a page
        // below the fold on a busy day. A reader who wants the shape of things
        // wants it before they have read four hundred rows, so the offer is
        // here and is repeated where the chains are.
        //
        // It carries the CURRENT FILTER AND NOT THE PAGE: the picture has no
        // paging (see that route's header) and a `page` in its query would be a
        // parameter that does nothing.
        self.note('<a class="btn" href="' +
          self.esc('/admin/delegation/map' + queryWith({ type: view.wantedType,
            mode: view.wantedMode, outcome: view.wantedOutcome,
            protocol: view.wantedProtocol, q: view.wantedText }, {})) +
          '">See this as a picture &rarr;</a> <strong>The same acts drawn as ' +
        'a diagram</strong> — a stick figure per person, a rectangle per ' +
        'application, a hexagon for this service in this trust realm, and a ' +
        'line per relationship: who acts for whom, and what each credential ' +
        'was FOR. ' + (filtering
          ? 'It opens with the filter you have set here.'
          : 'Filter first if this list is long — the picture is drawn from ' +
            'everything that matches, not from one page.')) +

        // THE SECOND WAY IN, BESIDE THE PICTURE AND ABOVE THE TABLE. The
        // picture link answers "what does all of this look like"; this answers
        // "what has this one application got itself into", which is the other
        // question a person arrives with and the one the tables below cannot be
        // sorted into. Both are here rather than at the foot, because a reader
        // who wants either wants it before reading four hundred rows.
        self.note('<strong>Or pivot on an application.</strong> A delegation ' +
        'has three parties and an application can be two of them — the ' +
        '<em>intermediary</em> that acts on somebody\'s behalf, and the ' +
        '<em>target</em> the credential is for. Search for one and click it, ' +
        'and see everything that has been delegated through it or to it, in ' +
        'either role, with every credential that came out. <strong>The ' +
        'search follows the filter above and the page it opens does ' +
        'not</strong>: it shows everything that application has ever been ' +
        'part of, because <em>what exists because of this thing</em> is not ' +
        'a question a half-answer is useful for. Any part of a name matches, ' +
        'EVERY spelling an act presented is searched, and twenty are shown ' +
        'at a time with the rest a click away.') +
        self.delegationApplicationChooser(view.applications, '', listView,
            { path: '/admin/delegation', query: req.query }) +

        // AND THE THIRD WAY IN, which is the other half of the same question.
        // The application chooser answers *what has this thing got itself
        // into*; this one answers *what has this service done in somebody's
        // NAME* — and it is the only one of the three that leaves this
        // register: a person's picture draws their ordinary OAuth 2.0, OIDC,
        // SAML, Kerberos and SPIFFE issuance too, because most of what happens
        // in somebody's name is not a delegation and a page drawn from these
        // acts alone would be empty for anybody who merely signed in. See that
        // route's header.
        self.note('<strong>Or pivot on a person.</strong> Everything issued ' +
        'in their name, end to end: every grant and flow with the exact one ' +
        'labelled, every assertion, ticket and SVID, the applications ' +
        'holding all of it, and the sign-ins it rests on — with any ' +
        'delegation naming them drawn in the same picture. <strong>The list ' +
        'includes people nothing was ever issued to</strong>, because an ' +
        'S4U2Self or an <code>OnBehalfOf</code> names somebody who was never ' +
        'present, and that is the row worth opening. Search it the same way ' +
        '— any part of a name, every spelling they arrived under, twenty at ' +
        'a time — and clicking a result IS the choice.') +
        self.delegationUserChooser(userGraph.userList(), '', listView,
            { path: '/admin/delegation', query: req.query }) +

        '<h2>What happened</h2>' +
        // No `page` input in this form, deliberately: changing a filter or the
        // page size returns to page 1. Carrying the old page number over would
        // land somebody on page 6 of a two-page result and the clamp in
        // pagingOf() would then move them again, which reads as the form
        // ignoring them. THE ANCHOR, for the reason chooserPane() gives at
        // length: this is a GET that reloads the page, and without it a reader
        // who had scrolled down to the filter was thrown back to the top of the
        // document by the click that answered them — on the longest page in
        // this console. It lands on the form rather than on the first row of
        // the table because the reader has usually just CHANGED a control and
        // wants to see what they set beside what came back; `scroll-margin-top`
        // then keeps the `What happened` heading above it on screen, so the
        // answer arrives with its question.
        '<form method="get" id="filter-acts" class="finder" ' +
          'action="/admin/delegation#filter-acts"><div class="formrow">' +
          self.chooserCarry(req.query) +
          '<label for="type">Mechanism</label><select id="type" name="type">' +
            typeOptions + '</select>' +
          '<label for="mode">Kind</label><select id="mode" name="mode">' +
            modeOptions + '</select>' +
          '<label for="outcome">Outcome</label><select id="outcome" ' +
          'name="outcome">' +
            outcomeOptions + '</select>' +
          '<label for="per">Per page</label><select id="per" name="per">' +
        perOptions +
            '</select>' +
        '</div><div class="formrow">' +
          '<label for="q">Text</label>' +
          '<input type="text" id="q" name="q" size="40" value="' +
        self.esc(view.wantedText) +
            '" placeholder="a person, an SPN, a client_id, an attribute">' +
          '<button class="secondary">Filter</button>' +
          (filtering
            ? ' <a href="/admin/delegation#filter-acts">clear</a>'
            : '') +
        '</div></form>' +
        self.note('The text box searches every party of the chain and both ' +
        'explanations at once, because the fact somebody arrives with names ' +
        'one of them and they do not know which column it will be in.') +
        self.note('The <strong>chain</strong> link under each row\'s number ' +
        'draws THAT RELATIONSHIP on its own — the whole chain the act ' +
        'belongs to, with everything else in the service left out. It goes ' +
        'to the chain rather than to the act because an act has no picture ' +
        'of its own: a diagram has the times taken out, and four acts a ' +
        'second apart between the same three parties are one line.') +
        nav.head +
        '<table><tr><th class="num">#</th><th>When</th><th>Mechanism</th>' +
        '<th>Kind</th><th>Outcome</th><th>Initial identity</th>' +
        '<th>Intermediary</th><th>Target</th><th>Authorized by / why not</th>' +
        '<th>Credentials</th></tr>' +
        (rows || '<tr><td colspan="10">' +
          (view.all.length
            ? 'Nothing matches this filter.'
            : 'Nothing has delegated anything yet. Three things put a row ' +
              'here: a Kerberos S4U2Self, S4U2Proxy or forwarded-TGT request ' +
              'at the KDC; a WS-Trust <code>RequestSecurityToken</code> ' +
              'carrying <code>&lt;wst:OnBehalfOf&gt;</code> or ' +
              '<code>&lt;wst14:ActAs&gt;</code>; and an RFC 8693 token ' +
              'exchange at <code>/oauth2/token</code>. A REFUSED attempt ' +
              'counts — the delegation page in the debugger will produce one ' +
              'on purpose.') +
          '</td></tr>') + '</table>' +
        nav.foot +

        self.note(view.filtered.length + ' act(s) match' +
        (paging.pages > 1 ?
         ', of which rows ' + paging.firstRow + '&ndash;' + paging.lastRow +
                            ' are on this page (' + paging.page + ' of ' +
                            paging.pages + ')' : '') +
        '; ' + summary.held + ' held' +
        // ---------------------------------------------------------------------
        // ONE PROCESS OR SEVERAL, AND THE TWO NUMBERS ARE NOT COMPARABLE WHEN
        // IT IS SEVERAL (2026-09-11). `held` is every process's acts, fanned in
        // by `delegation.merged()`; `recorded` is a plain counter in THIS
        // process and there is no store to fan in. So "N held of M recorded"
        // reads as nonsense the moment N exceeds M, which in `dispatch` mode it
        // routinely does. The sentence says which is which instead of quietly
        // putting them in one comparison.
        // ---------------------------------------------------------------------
        (summary.processes > 1
          ? ' across ' + summary.processes + ' process(es), ' +
            summary.heldHere +
            ' of them here; ' + summary.recorded + ' recorded by this one ' +
            'since it started'
          : ' of ' + summary.recorded +
            ' recorded since this process started') +
        (summary.dropped
          ? ', and <strong>' + summary.dropped + ' dropped</strong> — this ' +
            'page holds at ' +
            'most ' + summary.maxRecords + ' acts and discards the oldest ' +
            'first. Raise <code>delegation.maxRecords</code> in the settings ' +
            'at the foot of this page if that is losing something you need.'
          : '. The cap is ' + summary.maxRecords + ' acts and nothing has ' +
            'been dropped yet.') +
        ' The <strong>#</strong> column is a sequence number and is ' +
        'monotonic and never reused, including across a drop, so ' +
        '<code>?format=json</code>\'s <code>oldestSeq</code> and ' +
        '<code>newestSeq</code> let a caller poll this without guessing what ' +
        'it missed.') +

        '<h2>The chains</h2>' +
        self.note('The same acts with the time and the credentials taken ' +
        'out: one row per distinct <em>(mechanism, initial, intermediary, ' +
        'target)</em>. <a href="/admin/delegation/map">This is what the ' +
        'picture is drawn from</a> — one edge per row, and up to TWO lines ' +
        'per row, because a chain has three parties. It is already the more ' +
        'useful answer to <em>what talks to what</em>. The outcome is ' +
        'deliberately NOT part of a chain\'s identity, so a chain refused ' +
        'nine times and then fixed is one row that changes rather than two ' +
        'that do not meet. <strong>The last column draws one row ' +
        'alone</strong>, which is the answer to <em>what is this one, ' +
        'exactly</em> on a service that has been driven for an afternoon and ' +
        'whose whole picture is forty boxes.') +
        chainsNav.head +
        '<table><tr><th>Mechanism</th><th>Kind</th><th>Initial identity</th>' +
        '<th>Intermediary</th><th>Target</th><th>Acts</th><th>Last seen</th>' +
        '<th>Just this one</th></tr>' +
        (chainPage.shown.map(function (chain) {
          return '<tr>' +
            '<td><code>' + self.esc(chain.type) + '</code></td>' +
            '<td>' + self.modeCell(chain.mode) + '</td>' +
            '<td class="who">' +
            self.delegationPartyCell(chain.initial, known) +
            '</td><td class="who">' +
            self.delegationPartyCell(chain.intermediary, known) +
            '</td><td ' +
            'class="who">' + self.delegationPartyCell(chain.target, known) +
            '</td><td ' +
            'class="num">' + self.esc(chain.acts) + ' — ' +
              '<span class="state-valid">' + self.esc(chain.issued) +
            ' issued</span>, ' +
              (chain.refused
                ? '<span class="state-revoked">' + self.esc(chain.refused) +
                  ' ' +
                    'refused</span>'
                : '<span class="state-none">0 refused</span>') + '</td>' +
            '<td>' + self.esc(self.whenText(chain.lastAt)) + '</td>' +
            // An eighth column here where the acts table above puts the same
            // link inside its `#` cell, and the difference is width: this
            // table's five party columns are already narrow, but it has seven
            // of them against that one's ten and none of the long explanation
            // cells. A column can be afforded here and cannot be there.
            '<td><a href="' + self.esc('/admin/delegation/chain' +
              queryWith(listView, { chain: chain.chainKey })) +
              '">picture &rarr;</a></td>' +
            '</tr>';
        }).join('') || '<tr><td colspan="8">No chains yet.</td></tr>') +
        '</table>' +
        chainsNav.foot +

        self.permissionsSection(req, permissions, listView) +

        '<h2>Who may delegate to whom &mdash; Kerberos</h2>' +
        self.note('<strong>The SECOND configured register on this page, and ' +
        'the older one.</strong> Until delegated permissions arrived this ' +
        'was the only configuration here and this paragraph said so — ' +
        '<em>this half is configuration rather than history, and it is ' +
        'Kerberos only</em> — which is no longer true and is worth saying ' +
        'rather than quietly editing. What IS still true is the sentence ' +
        'underneath it: <strong>Kerberos is the one family here that polices ' +
        'delegation IN THE ACT</strong>. The permissions above are policy ' +
        'this service was configured with and refuses on in product mode, ' +
        'and in development only when ' +
        '<code>oauth2.delegatedPermissionsEnforced</code> is set; these two ' +
        'attributes are a KDC decision that has always been made, on every ' +
        'S4U request, whatever anything is set to. WS-Trust puts no ' +
        'authorization on <code>OnBehalfOf</code> or <code>ActAs</code> and ' +
        'this service adds none; RFC 8693 leaves the policy to the ' +
        'authorization server, and what this one now has is the register ' +
        'above rather than nothing. All of that is stated on every row it ' +
        'produces, in the same column that names an attribute for a Kerberos ' +
        'row — <strong>the asymmetry is still the most useful thing on this ' +
        'page</strong>: the same picture, policed at one end and not at the ' +
        'other.') +
        self.note('The whole of the KDC\'s decision rests on two attributes ' +
        'on two OPPOSITE accounts, which is why they are in one table with a ' +
        'column saying which account carries the permission. Same messages, ' +
        'same KDC options, opposite direction of trust — and the second one ' +
        'turns <em>I can write to this computer object</em> into <em>I can ' +
        'reach this service as anybody</em>.') +
        self.note('Each mechanism needs one thing BEYOND the attribute, and ' +
        'it is the same thing on every row of that kind, so it is here ' +
        'rather than in a column: <strong>classic</strong> needs a ' +
        'FORWARDABLE evidence ticket, which S4U2Self returns only to an ' +
        'account flagged ' +
        '<code>TRUSTED_TO_AUTHENTICATE_FOR_DELEGATION</code>; ' +
        '<strong>resource-based</strong> needs <code>PA-PAC-OPTIONS</code> ' +
        '(padata type 167) carrying the resource-based bit, and [MS-SFU] ' +
        'requires a KDC to answer <code>KDC_ERR_BADOPTION</code> without it ' +
        '— an error that says nothing about padata. Resource-based needs no ' +
        'forwardable evidence and no flag on the front end at all, which is ' +
        'why it is the easier path.') +
        pairsNav.head +
        '<table><tr><th>Mechanism</th><th>Front end (who acts)</th>' +
        '<th>Target (what is reached)</th><th>Attribute, and where it ' +
        'lives</th><th>Anything missing?</th></tr>' +
        (pairPage.shown.map(self.policyPairRow.bind(self)).join('') ||
          '<tr><td colspan="5">No principal here is configured for ' +
          'constrained delegation of either kind.</td></tr>') + '</table>' +
        pairsNav.foot +

        '<h3>Account flags</h3>' +
        self.note('Two of these three STOP delegation rather than permit it, ' +
        'and the third is not a control at all. An account appears here ' +
        'whether or not any pair above names it, because an account named in ' +
        'no pair is precisely the one somebody is wondering about.') +
        flagsNav.head +
        '<table><tr><th>Principal</th><th>Flags</th><th>What each one ' +
        'does</th></tr>' +
        (flagPage.shown.map(self.policyAccountRow.bind(self)).join('') ||
          '<tr><td colspan="3">No principal here carries one of these ' +
          'flags.</td></tr>') +
        '</table>' +
        flagsNav.foot +

        '<h3>The mechanisms</h3>' +
        self.note('Read off the same table this page records against, so a ' +
        'mechanism cannot be recordable and undocumented, nor described here ' +
        'and never occur.') +
        mechanismsNav.head +
        '<ul>' + mechanismPage.shown.map(function (entry) {
          // The mechanism, its id, its specification and how many have been
          // recorded stay on the row; the paragraph saying what it IS folds
          // under them. Folding the whole item would have put the count — the
          // one figure that changes while somebody watches this page — behind a
          // click.
          return '<li><strong>' + self.esc(entry.label) + '</strong> (<code>' +
            self.esc(entry.type) + '</code>, ' + self.esc(entry.protocol) +
            ', ' +
            self.esc(entry.spec) + ' — ' + (summary.byType[entry.type] || 0) +
            ' ' +
                'recorded) ' +
            self.note(self.esc(entry.what) +
              (entry.policed
                ? ' <strong>This service decides who may do it.</strong>'
                : ' <strong>Nothing here checks who may do it.</strong>')) +
            '</li>';
        }).join('') + '</ul>' +
        mechanismsNav.foot +

        self.note('<strong>It is in memory and dies with the ' +
        'process</strong>, like the counters, the audit log, the sessions ' +
        'and the signing key. It also has no clear button and no way to add ' +
        'a row by hand, which is a decision rather than an omission: every ' +
        'row here is something that actually happened, and a table mixing ' +
        'those with typed-in ones would be worth much less than either. ' +
        'Restarting the service is how you get an empty one.') +

        self.note('<strong>A delegation that SUCCEEDED also appears on <a ' +
        'href="/admin/audit">the audit log</a></strong> as an ordinary ' +
        '<code>authentication</code> row, and on <a href="/admin/users">the ' +
        'users page</a> as a credential accepted for the initial identity — ' +
        'which is right: this service did accept one. A delegation that was ' +
        'REFUSED appears in NEITHER, because nothing was accepted, and that ' +
        'gap is the reason this page keeps its own list rather than a filter ' +
        'over one of theirs.') +

        // THE ONE SETTING THIS PAGE HAS, and it is the one a reader wants
        // exactly when the paragraph above says something was dropped. It is
        // also the only control on a page that had none — which does not change
        // rule 7's answer for /admin-api/delegation: the form posts `set-many`
        // to /admin/config, which POST /admin-api/config/set-many already
        // mirrors.
        self.configFormsFor('/admin/delegation') +

        self.note('<strong>Every table on this page is paged, at ' +
        DELEGATION_PER_PAGE + ' rows, and they share one size.</strong> ' +
        'There are seven of them here with several screens of prose between, ' +
        'so fifty rows apiece — which is what every other page in this ' +
        'console uses — would put the last heading tens of thousands of ' +
        'pixels down. <code>?per=</code> changes all seven together (at ' +
        'most ' + MAX_ROWS + ' rows a page) and the control above the acts ' +
        'table is the one that sets it. Each table then has a page parameter ' +
        'of ITS OWN, so moving one leaves the other six where you left them: ' +
        '<code>?page=</code> for the acts, and <code>?chainsPage=</code>, ' +
        '<code>?permissionsPage=</code>, <code>?grantsPage=</code>, ' +
        '<code>?pairsPage=</code>, <code>?flagsPage=</code> and ' +
        '<code>?mechanismsPage=</code> for the rest. The two searches over ' +
        'the configured register are <code>?permq=</code> and ' +
        '<code>?grantq=</code>, and a new search starts that table at its ' +
        'first page.') +

        self.note('<strong><code>?format=json</code> carries the WHOLE of ' +
        'every list and not the page you are looking at, and that is ' +
        'deliberate rather than an oversight.</strong> The acts are the ' +
        'exception and always were: they are capped at ' +
        '<code>delegation.maxRecords</code> and can be thousands, so the ' +
        'reply pages them and carries <code>page</code>, <code>pages</code> ' +
        'and <code>matched</code> for a caller to walk them with. Everything ' +
        'else — <code>chains</code>, the configured <code>policy</code> and ' +
        'the whole of <code>allowed</code> — comes back entire, because each ' +
        'is derived from something already bounded and because <code>GET ' +
        '/admin-api/permissions</code> answers with that same register under ' +
        'its own name. A caller that had to walk seven pagings to read a ' +
        'register a page draws in one screen would be paying for this ' +
        'page\'s layout. <code>allowed.filter</code> and ' +
        '<code>allowed.paging</code> report what the BROWSER was shown, so ' +
        'nothing here is silent about the difference. The acts are also at ' +
        '<code>GET /admin-api/delegation</code> with the same parameters.');

      self.respond(req, res, Object.assign({}, view.json, {
                // UNDER A MEMBER OF ITS OWN AND NOT MERGED INTO THE ACTS, for
                // the reason the two headings on the page give: they are
                // different registers and a caller that had to tell them apart
                // by looking at the shape of a row would eventually get it
                // wrong. `allowed` is the word the page uses, so a reader
                // moving between the two is reading one vocabulary.
                allowed: {
                  resources: permissions.register.resources,
                  permissions: permissions.register.permissions,
                  grants: permissions.register.grants,
                  counts: permissions.register.counts,
                  graph: permissions.graph,
                  // WHAT THE BROWSER WAS SHOWN, BESIDE THE WHOLE LISTS RATHER
                  // THAN INSTEAD OF THEM (2026-09-01).
                  //
                  // The two arrays above stay ENTIRE. `GET
                  // /admin-api/permissions` answers with the same register
                  // under its own name, and a caller that had to walk ten-row
                  // pages of it would be paying for this page's layout — the
                  // paging here exists because seven tables share one screen,
                  // which is not a fact about the data.
                  //
                  // But a `?permq=` that the markup honours and the reply
                  // ignores is exactly the silent disagreement this console
                  // keeps warning about, so what the page did is REPORTED. A
                  // caller reading `paging.permissions.total` against
                  // `permissions.length` can see the filter that was applied
                  // and the slice that was drawn, and neither of them had to be
                  // guessed from the markup.
                  filter: { permissions: allowedState.permWanted || null,
                            grants: allowedState.grantWanted || null },
                  paging:
                    { permissions: pagingJson(allowedState.permPage.paging),
                            grants: pagingJson(allowedState.grantPage.paging) }
                },
                settings: self.configSettingsJson('/admin/delegation')
              }),
                   'Delegation', '/admin/delegation', inner);
      log.debug("Leaving the admin delegation page.");
    });

    // -------------------------------------------------------------------------
    // POST /admin/delegation — THE FIVE ACTIONS THE CONFIGURED HALF HAS.
    //
    // The acts half has none and never will; see `permissionsSection()`'s
    // header, where the reversal of that route's "NO FORM, AND THAT IS A
    // DECISION" is argued rather than assumed. Every action here writes a
    // directory attribute through `common/app_permissions.ts` and therefore
    // through `applications.updateApplication()`, which is where the rules are.
    //
    // IT SAID "CONTROLS" AND IT SAYS "ACTIONS" NOW, and the difference stopped
    // being pedantic on 2026-09-01: the FORMS are on two pages and the ACTIONS
    // are all here. `grant-permission` is drawn on an application's own page
    // under /admin/applications, where the client half of a grant is the entry
    // being looked at rather than an option in a list of every application
    // here; `revoke-permission` is drawn on both, as a row button either way.
    // Moving a form is not moving an action — PERMISSION_ACTIONS is unchanged,
    // and rule 7's parity check reads this handler's refusal sentence exactly
    // as it always did. See applicationPermissionsSection()'s header for the
    // move itself.
    //
    // WHERE IT LANDS AFTERWARDS IS THEREFORE A DECISION AND NOT A CONSTANT.
    // permissionsReturnTo() makes it, off the `from` field the form carries:
    // `#allowed` on this page, and the application's own page at `#permissions`
    // for a form drawn there. A fragment either way, for `chooserPane()`'s
    // reason, because this is the longest page in the console and a reader who
    // has just granted a permission is several screens down whichever page they
    // are on. The list state rides on `back` exactly as /admin/applications'
    // forms carry it, and `from` is a NAME rather than a URL, which is that
    // function's whole subject.
    // -------------------------------------------------------------------------
    app.post('/admin/delegation', function (req, res) {
      log.debug("Entering the admin delegation action endpoint.");
      const body = parseBody(req);
      // The actor is the person whose session got them through the gate above,
      // read here rather than inside `permissionsAction()` for `rbacAction()`'s
      // reason: the management API calls the same function with an actor of its
      // own, and a function that reached for a cookie would only work from one
      // of them. It ends up on the `application.update` audit row that
      // `applications.updateApplication()` writes.
      const state = gateStateFor(req);
      const result = permissionsAction(Object.assign({}, body,
        { actor: (state && state.username) || '' }));
      self.respondToAction(req, res, self.permissionsReturnTo(body), result);
      log.debug("Leaving the admin delegation action endpoint.");
    });

    // -------------------------------------------------------------------------
    // GET /admin/delegation/allowed — THE CONFIGURED MAPPINGS, AS A PICTURE.
    //
    // A DRILL-DOWN OF /admin/delegation, exactly as /admin/delegation/map is:
    // no NAV row, an `up` that reads `Admin console › Delegation › The allowed
    // mappings`, and the delegation page's own tab active. Rule 7a's test is
    // the one that decided it and it decides it the same way — this is a second
    // VIEW of a list on that page rather than a filter over it.
    //
    // **IT IS A SECOND PICTURE AND NOT A MODE OF THE FIRST, and that is the
    // whole decision.** Drawing configured grants and recorded acts on one
    // canvas was the obvious thing and it is wrong for a reason the two graphs
    // make unavoidable: an act has three layers and the first is a PERSON, and
    // a permission has no person in it at all — it says `webapp1 may reach the
    // API as whoever is signed in`, and there is no whoever yet. One canvas
    // would put a drawing of what may happen and a drawing of what did happen
    // in one frame with no way to tell a box that has been used from a box that
    // has merely been described, which is the single distinction both pictures
    // exist to make. They are cross-linked in both directions instead, and each
    // says in its first paragraph what the other one is.
    //
    // The renderer is the SAME `delegation_map.js` and the same
    // `delegationLooks()` resolver — the graph arrives in
    // `delegation.graph()`'s shape, which is what that file's header says it
    // was split out to make possible. What it draws differently comes off ONE
    // new relation (`may-reach`) and nothing else.
    //
    // `?format=svg` and `?format=json` answer the same two things they answer
    // on the map, for the same reasons, through the same two functions.
    // -------------------------------------------------------------------------
    app.get('/admin/delegation/allowed', function (req, res) {
      log.debug("Entering the admin allowed-delegation picture.");
      const permissions = permissionsView();
      const graph = permissions.graph;
      const known = knownUserKeys();
      const look = self.delegationLooks(graph, known);
      const drawingLabel = 'Delegated permissions between applications, as a ' +
                           'diagram';

      if (String(req.query.format || '') === 'svg') {
        self.sendDelegationSvg(res, graph, look, drawingLabel);
        log.debug("Leaving the admin allowed-delegation picture. Answered " +
                  "SVG.");
        return;
      }

      const picture = self.delegationDrawing(graph, look,
                                             '/admin/delegation/allowed',
                                             {},
                                             drawingLabel);
      const listView = self.listViewOf('/admin/delegation', req.query);
      const up = self.upTo('/admin/delegation', 'The allowed mappings',
                           listView);
      const counts = permissions.register.counts;

      // THE GROUPS, PAGED. The whole-register picture above is one document
      // however large the register is; this list is one row per group and a
      // service with eighty applications configured has as many rows as it has
      // groups, so it pages the way every other list in this console does.
      // `name: 'groups'` gives it a parameter of its own for `pagingOf()`'s
      // reason — this page could acquire a second list tomorrow, and two
      // controls sharing `page` would page each other's table.
      const groups = permissions.clusters;
      const groupPage = pagedRows(req.query, groups.clusters,
                                  { name: 'groups', noun: 'groups' });
      const groupNav = self.pageNavPair('/admin/delegation/allowed',
                                        pageParamsOf(req.query),
                                        groupPage.paging);
      // What a link OUT of this page carries: the acts list's state, so the
      // breadcrumb on the group page still leads back where the reader came
      // from, AND this page's own search, so its copy of the chooser opens
      // holding the term that was typed here. allowedChooserState() says why
      // the second one is spelled out rather than added to LIST_PARAMS.
      const onward = Object.assign({}, listView,
                                   self.allowedChooserState(req.query));

      const inner =
        self.note('<strong>What is ALLOWED, not what happened.</strong> ' +
        'Every line here is a delegated permission somebody configured: a ' +
        'client application has been granted a permission that a resource ' +
        'application exposes, and a request naming it in a ' +
        '<code>scope</code> would be issued an access token audienced to ' +
        'that resource. Not one of these lines has been issued anything. <a ' +
        'href="/admin/delegation/map">The other picture</a> is the one that ' +
        'draws what actually happened.') +

        self.note('<strong>Every box is an application and there is no ' +
        'person on this diagram</strong>, which is the visual difference ' +
        'between the two and the reason they are not one drawing. A ' +
        'delegation ACT has three layers and the first of them is somebody — ' +
        'a stick figure, the person on whose behalf it happened. A ' +
        'permission has nobody in it: it says <em>this client may reach that ' +
        'API as whoever is signed in</em>, and there is no whoever yet. ' +
        '<strong>This service is not on it either</strong>, for the same ' +
        'reason the hexagon is on the other one: every line there exists ' +
        'because this service issued or refused something, and none of these ' +
        'has been asked for.') +

        self.note('<strong>A line leaves the box with the ROUND end and ' +
        'arrives at the box with the ARROWHEAD.</strong> That is worth ' +
        'saying on this page in particular, because it is the one where ' +
        'nearly every box is at both ends of something: an application here ' +
        'is usually a client of one and a resource of another, and the ' +
        'question is always <em>which of the lines touching this box are its ' +
        'own?</em> Both marks are on every line, and each one has a berth of ' +
        'its own on the box\'s edge, so a grant this application holds and a ' +
        'grant somebody holds on it never start from the same point. No line ' +
        'here is two-way: where two applications may reach each other, that ' +
        'is two grants and it is drawn as two lines.') +

        self.note('<strong>A DASHED line is a grant nobody has ever ' +
        'used</strong> and a solid one has been asked for at least once — ' +
        'read off the client\'s own <code>oauthScope</code>, which records ' +
        'the scopes it has requested. That one bit is what a configured ' +
        'picture can say and an acts diagram cannot: a grant nobody needed ' +
        'draws no act at all, so it is invisible on the other one. It is ' +
        'evidence rather than proof — that attribute records what was ASKED ' +
        'FOR, not what was issued.') +

        (counts.dangling
          ? self.note('<strong>' + counts.dangling + ' grant(s) are DANGLING ' +
            'and are not drawn.</strong> They name a permission no ' +
            'application in this registry defines, so there is no box at the ' +
            'far end to reach — and a line to nowhere would be a drawing of ' +
            'a resource that is there. They are in the table on <a ' +
            'href="/admin/delegation#allowed">the register</a>, which is ' +
            'where that state belongs.')
          : '') +

        picture.html +

        '<div class="tiles">' +
          self.tile(counts.grants, 'grants') +
          self.tile(counts.permissions, 'permissions defined') +
          self.tile(counts.unused, 'never asked for') +
          self.tile(counts.dangling, 'dangling, not drawn') +
        '</div>' +

        '<h2 id="groups">The groupings: which applications are joined to ' +
        'each other</h2>' +

        self.note('<strong>A GROUP IS A SET OF APPLICATIONS THAT CAN BE ' +
        'REACHED FROM ONE ANOTHER BY FOLLOWING GRANTS, IGNORING WHICH WAY ' +
        'EACH ONE POINTS.</strong> That is the whole definition, and the ' +
        'direction is dropped ON PURPOSE. A grant is directed — a client is ' +
        'granted a permission a resource exposes, which is why every line ' +
        'above has a round end and a pointed one — but following the arrows ' +
        'would answer <em>what can this client eventually reach</em>, and a ' +
        'permission register has no chains in it: holding a permission on an ' +
        'API does not grant that API\'s permissions to anybody. Following a ' +
        'grant EITHER WAY answers the question somebody actually arrives ' +
        'with — <em>which applications are in the same conversation as this ' +
        'one</em> — and it is the only reading under which an API and the ' +
        'three front ends holding permissions on it come out as ONE group ' +
        'rather than as four. Every picture still draws every line with its ' +
        'direction on it, so what is dropped is direction as a test of ' +
        'MEMBERSHIP, never direction as a fact.') +

        self.note('<strong>This is what the whole-register picture above ' +
        'stops being able to say.</strong> One canvas is the right drawing ' +
        'of five applications and the wrong drawing of eighty, where the ' +
        'interesting reading is almost never the whole of it. Search for an ' +
        'application below and the picture you get is its group and nothing ' +
        'else — the applications it is joined to, however many hops away, ' +
        'and none of the ones it is not.') +

        self.allowedApplicationChooser(permissions, '', onward,
          { path: '/admin/delegation/allowed', query: req.query }) +

        // This page has no filter form to hang `per` on, which is the case
        // perPageForm() exists for. The leaf it carries is the CHOOSER'S SEARCH
        // rather than a selected thing, because that is the only state on this
        // page a reader would lose by changing the size.
        self.perPageForm('/admin/delegation/allowed', 'permappq',
                         queryOne(req.query, 'permappq'),
                         groupPage.paging.perPage,
                         'There is one table below. The drawing above is ' +
                         'never paged: paging a picture draws the pagination ' +
                         'rather than the service.',
                         self.filterOnly(listView)) +

        groupNav.head +
        self.allowedClusterTable(groups, groupPage.shown, onward) +
        groupNav.foot +

        '<div class="tiles">' +
          self.tile(groups.counts.clusters, 'groups') +
          self.tile(groups.counts.joined, 'with more than one in them') +
          self.tile(groups.counts.alone, 'of one application') +
          self.tile(groups.counts.largest, 'in the largest') +
        '</div>' +

        self.note('<strong>A group of ONE is a real answer and not an empty ' +
        'row.</strong> Three different things produce one and they are worth ' +
        'telling apart: an application carrying a base URI and permissions ' +
        'that nobody has been granted — somebody described an API and ' +
        'nothing may reach it; a client holding only DANGLING grants, which ' +
        'name permissions no application defines, so there is no far end to ' +
        'be in a group with; and an application granted its OWN permission, ' +
        'which is one application however it is drawn. The last two columns ' +
        'say which.') +

        self.note('The register itself, with the forms that change it, is on ' +
        '<a href="/admin/delegation#allowed">the delegation page</a>. ' +
        '<strong>Nothing on this page changes anything</strong> — the search ' +
        'above is the only control, and it narrows nothing here: it opens a ' +
        'picture of its own. There is still no FILTER over the drawing at ' +
        'the top, because this register has no dimension to narrow on the ' +
        'way the acts have a mechanism, a mode and an outcome — the one ' +
        'division it does have is which applications can reach each other at ' +
        'all, and that is the list above rather than a filter. ' +
        '<code>?format=json</code> is the graph and the groups, ' +
        '<code>?format=svg</code> is the document alone, and the graph is ' +
        'also in the <code>allowed.graph</code> member of <code>GET ' +
        '/admin-api/delegation</code> with the groups at <code>GET ' +
        '/admin-api/permissions/groups</code>.');

      self.respond(req, res, { graph: graph, counts: counts,
                               grants: permissions.register.grants,
                               // THE GROUPS THIS PAGE IS SHOWING, through the
                               // SAME `clusterSummary()` that `GET
                               // /admin-api/permissions/groups` answers with —
                               // so a caller reading a group off this reply and
                               // a caller reading one off that operation are
                               // reading the same object. It is this page's own
                               // slice rather than that operation's, because
                               // `?format=json` must describe the page it is a
                               // format of; the paging is beside it.
                               //
                               // It carries the counts and NOT the rows: a
                               // caller wanting one group's grants asks for
                               // that group, and repeating every grant once per
                               // group here would grow this reply with the
                               // square of the register.
                               groups: groupPage.shown.map(clusterSummary),
                               groupCounts: groups.counts,
                               groupsPaging: pagingJson(groupPage.paging) },
                   'The allowed mappings', '/admin/delegation', inner, up);
      log.debug("Leaving the admin allowed-delegation picture.");
    });

    // -------------------------------------------------------------------------
    // GET /admin/delegation/cluster?application=… — ONE GROUP OF APPLICATIONS,
    // DRAWN ALONE.
    //
    // THE DRILL-DOWN OF /admin/delegation/allowed, and the sixth page in the
    // delegation family. That page draws the WHOLE configured register on one
    // canvas, which is the right document for five applications and the wrong
    // one for eighty: past a certain size the interesting reading of a
    // permission register is never the whole of it, it is which applications
    // are joined to each other at all. This page is that reading — search for
    // an application on the allowed picture, and get a diagram of ITS GROUP and
    // nothing else.
    //
    // **A GROUP IS A CONNECTED COMPONENT OF THE GRANT GRAPH WITH THE DIRECTION
    // IGNORED, AND THAT IS THE ONE DECISION ON THIS PAGE.**
    // `app_permissions.js`'s `clusters()` carries the argument in full and it
    // is not repeated here; the short of it is that following the arrows would
    // answer *what can this client eventually reach*, which is a question about
    // a chain, and a permission register has no chains in it — holding a
    // permission on an API grants nobody that API's own permissions. Following
    // a grant either way answers the question somebody arrives with, and it is
    // the only reading under which an API and the three front ends holding
    // permissions on it come out as ONE group. **Membership ignores direction;
    // the PICTURE does not.** Every line is still drawn with its round end and
    // its arrowhead, because which way a grant points is a fact about the grant
    // and this page changes nothing about it.
    //
    // **IT IS A DRILL-DOWN OF /admin/delegation, LIKE ITS FIVE SIBLINGS, AND
    // NOT OF /admin/delegation/allowed** — even though that is the page it is
    // reached from. `upTo()` takes a NAV path and the allowed picture is not
    // one; the trail reads `Admin console › Delegation › One group of
    // applications`, the delegation tab stays active, and the way back to the
    // picture is a button at the top of the page carrying this page's own
    // search. That is rule 7a's arrangement and the same one
    // /admin/delegation/chain has, where the row that was clicked was also on a
    // page rather than in the nav.
    //
    // **THE SAME RENDERER AND THE SAME GRAPH BUILDER**, handed the group's
    // grants instead of every grant in the register. So a line here and a line
    // on the whole-register picture are the same line drawn by the same code,
    // and a reader who has learnt what a dash means on one has learnt it on the
    // other.
    //
    // **A BARE PAGE IS THE CHOOSER AND NOT A 404** — /admin/logout's rule,
    // which /admin/delegation/application and /admin/delegation/user both
    // follow. Arriving with nothing selected is how somebody gets here from a
    // bookmark, and an application the configured register has never heard of
    // is a state to explain rather than an error: it is the ordinary condition
    // of every entry in `ou=applications` that has not been given a base URI or
    // a grant.
    //
    // **NO FORM AND THEREFORE NO NEW OPERATION ON /admin-api** — rule 7,
    // satisfied the way the other picture pages satisfy it. What this page
    // shows without a browser is `?format=json` here and `GET
    // /admin-api/permissions/groups`, which answers the list and one group
    // alike; `?format=svg` is the document alone, with no links in it for the
    // reason the map's header gives.
    // -------------------------------------------------------------------------
    app.get('/admin/delegation/cluster', function (req, res) {
      log.debug("Entering the admin delegated-permission group page.");
      const asked = String(req.query.application || '').trim();
      const permissions = permissionsView();
      const groups = permissions.clusters;
      // EXACT EQUALITY ON THE IDENTIFIER, which is `clusterFor()`'s rule and is
      // deliberately NOT the normalisation /admin/delegation/application
      // performs. That page is keyed on what an ACT presented, and one
      // application arrives spelled several ways; this one is keyed on an entry
      // in `ou=applications`, where the identifier is the key and nothing in
      // this service case-folds one.
      const group = asked ? appPermissions.clusterFor(asked, groups) : null;
      const known = knownUserKeys();
      const graph = appPermissions.graph(group ? group.grants : []);
      const look = self.delegationLooks(graph, known);
      const drawingLabel = group
        ? 'Delegated permissions across the ' + group.counts.applications +
          ' application(s) joined to ' + asked
        : 'Applications joined by delegated permissions';

      if (String(req.query.format || '') === 'svg') {
        self.sendDelegationSvg(res, graph, look, drawingLabel);
        log.debug("Leaving the admin delegated-permission group page. " +
                  "Answered SVG.");
        return;
      }

      const listView = self.listViewOf('/admin/delegation', req.query);
      const up = self.upTo('/admin/delegation', 'One group of applications',
                           listView);
      // The way back to the picture this page was opened from, carrying the
      // search that opened it — see allowedChooserState(). The breadcrumb above
      // goes to the delegation TABLE, which is the section; this button goes to
      // the page the reader was actually standing on, and the two are different
      // places.
      const onward = Object.assign({}, listView,
                                   self.allowedChooserState(req.query));
      const back = self.note('<a class="btn" href="' +
        self.esc('/admin/delegation/allowed' + queryWith(onward, {})) +
        '">&larr; Back to the allowed mappings</a>');
      const chooser = self.allowedApplicationChooser(permissions,
                                                     group ? asked : '',
        onward, { path: '/admin/delegation/cluster', query: req.query });

      if (!group) {
        const groupPage = pagedRows(req.query, groups.clusters,
                                    { name: 'groups', noun: 'groups' });
        const groupNav = self.pageNavPair('/admin/delegation/cluster',
                                          pageParamsOf(req.query),
                                          groupPage.paging);
        const inner = self.messagesOf(req) + back +
          (asked
            ? self.note('<strong>The configured register has nothing about ' +
              '<code>' + self.esc(asked) + '</code>.</strong> That is not an ' +
              'error and it is the ordinary state of most entries in this ' +
              'registry: an application arrives in this register only when ' +
              'it is given a base URI or a permission of its own, which ' +
              'makes it a RESOURCE, or is granted a permission somebody else ' +
              'exposes, which makes it a CLIENT. Until one of those has been ' +
              'done there is nothing for it to be joined to. Two other ' +
              'things could be true: the name is spelled differently from ' +
              'the identifier on the entry &mdash; nothing here case-folds ' +
              'one, so <code>WebApp1</code> and <code>webapp1</code> are two ' +
              'applications &mdash; or the entry has been deleted since the ' +
              'link was made. <a href="/admin/applications' +
              self.esc(queryWith({ application: asked }, {})) + '">The ' +
              'registry</a> settles both.')
            : '') +
          self.note('<strong>Choose an application and get a picture of ' +
          'everything it is joined to.</strong> A group is a set of ' +
          'applications that can be reached from one another by following ' +
          'delegated permissions, IGNORING which way each one points &mdash; ' +
          'because a grant is directed but a permission register has no ' +
          'chains in it, and the question worth answering is <em>which ' +
          'applications are in the same conversation as this one</em>. <a ' +
          'href="/admin/delegation/allowed">The whole register</a> is one ' +
          'picture of all of them at once, which is the right drawing of ' +
          'five applications and the wrong drawing of eighty.') +
          chooser +
          self.perPageForm('/admin/delegation/cluster', 'permappq',
                           queryOne(req.query, 'permappq'),
                           groupPage.paging.perPage,
                           '', self.filterOnly(listView)) +
          groupNav.head +
          self.allowedClusterTable(groups, groupPage.shown, onward) +
          groupNav.foot +
          self.note('The list is built from the CONFIGURED register and not ' +
          'from <a href="/admin/applications">the registry</a>, so an ' +
          'application with no base URI and no grant is in neither this ' +
          'table nor the search above. <a ' +
          'href="/admin/delegation/application">The acts drill-down</a> is ' +
          'the other question entirely: what has actually been delegated ' +
          'through an application or to it, which can name something no ' +
          'permission mentions.');
        self.respond(req, res, permissionGroupsView(req.query, permissions),
                     'Delegation — one group', '/admin/delegation', inner, up);
        log.debug("Leaving the admin delegated-permission group page. " +
                  (asked ? "Nothing configured names that." :
                   "Drew the chooser."));
        return;
      }

      // WHAT EACH MEMBER IS IN THIS GROUP, counted once over the group's own
      // rows rather than filtered per member — the same reason the chooser does
      // it: a filter per application over the grant list is quadratic, and a
      // group is the one place in this console where the list being walked is
      // deliberately allowed to be large.
      const holds = {};
      const reached = {};
      const dangles = {};
      group.grants.forEach(function (one) {
        holds[one.client] = (holds[one.client] || 0) + 1;
        if (one.dangling) {
          dangles[one.client] = (dangles[one.client] || 0) + 1;
        } else if (one.resource) {
          reached[one.resource] = (reached[one.resource] || 0) + 1;
        }
      });
      const exposes = {};
      group.permissions.forEach(function (one) {
        exposes[one.resource] = (exposes[one.resource] || 0) + 1;
      });

      const picture = self.delegationDrawing(graph, look,
                                             '/admin/delegation/cluster',
                                             Object.assign(
                                               {}, onward,
                                               { application: asked }),
                                             drawingLabel);

      const memberRows = group.members.map(function (identifier) {
        const registered = applications.get(identifier);
        const name = registered
          ? (registered.name || registered.dnLabel || identifier) : '';
        const href = '/admin/applications' +
          queryWith(listView, { application: identifier });
        // The chosen application is marked with a WORD and not with a class:
        // `.on` in this console's stylesheet is scoped to the chooser's own
        // list, so a class here would be markup that styles nothing — and a
        // colour alone would say it to sighted readers only.
        return '<tr>' +
          '<td class="who">' + (registered
            ? '<a href="' + self.esc(href) + '">' + self.esc(name) +
              '</a><br><code>' +
              self.esc(identifier) + '</code>'
            : '<code>' + self.esc(identifier) + '</code><br><span ' +
              'class="state-none" title="No entry under ou=applications ' +
              'answers to this identifier. Both console doors read the ' +
              'registry, so a grant naming something that is not in it can ' +
              'only have been written by an ldapmodify.">not in the ' +
              'registry</span>') +
            (identifier === asked
              ? '<br><span class="state-valid">the one you chose</span>' : '') +
            '</td>' +
          '<td class="num">' + (exposes[identifier]
            ? '<strong>' + self.esc(exposes[identifier]) + '</strong>'
            : '<span class="state-none">0</span>') + '</td>' +
          '<td class="num">' + (reached[identifier]
            ? '<strong>' + self.esc(reached[identifier]) + '</strong>'
            : '<span class="state-none">0</span>') + '</td>' +
          '<td class="num">' + (holds[identifier]
            ? '<strong>' + self.esc(holds[identifier]) + '</strong>'
            : '<span class="state-none">0</span>') +
            (dangles[identifier]
              ? '<br><span class="state-revoked" title="Naming a permission ' +
                'no application in this registry defines. It joins this ' +
                'application to nothing, because there is no far end.">' +
                self.esc(dangles[identifier]) + ' dangling</span>'
              : '') + '</td>' +
          '<td class="who"><a href="' +
          self.esc('/admin/delegation/application' +
            queryWith(listView, { application: identifier })) + '">what it ' +
            'has actually delegated</a></td>' +
          '</tr>';
      }).join('');

      const grantPage = pagedRows(req.query, group.grants,
                                  { name: 'groupGrants', noun: 'grants' });
      const grantNav = self.pageNavPair('/admin/delegation/cluster',
                                        pageParamsOf(req.query),
                                        grantPage.paging);
      const permissionPage = pagedRows(req.query, group.permissions,
                                       { name: 'groupPermissions',
                                         noun: 'permissions' });
      const permissionNav = self.pageNavPair('/admin/delegation/cluster',
                                             pageParamsOf(req.query),
                                             permissionPage.paging);

      const inner = self.messagesOf(req) + back +

        '<div class="tiles">' +
          self.tile(group.counts.applications, 'applications in the group') +
          self.tile(group.counts.lines, 'lines drawn') +
          self.tile(group.counts.asked, 'asked for at least once') +
          self.tile(group.counts.unused, 'never asked for') +
          self.tile(group.counts.permissions, 'permissions exposed') +
          self.tile(group.counts.dangling, 'dangling, not drawn') +
        '</div>' +

        self.note('<strong><code>' + self.esc(asked) + '</code> is one of ' +
        self.esc(group.counts.applications) + ' application(s) that can be ' +
        'reached from one another by following delegated ' +
        'permissions.</strong> ' +
        (group.counts.applications === 1
          ? 'It is joined to nothing: no grant leads from it to another ' +
            'application, and none leads to it. That is a real answer rather ' +
            'than an empty page &mdash; an API with permissions defined and ' +
            'nothing granted on them is a description nobody may use, and a ' +
            'client holding only dangling grants has been granted something ' +
            'that does not exist.'
          : 'The group is named after <code>' + self.esc(group.key) +
            '</code>, which is simply the identifier that sorts first ' +
            '&mdash; a group has no name of its own, and using the union of ' +
            'its members rather than one of them means adding a grant inside ' +
            'a group does not rename it.') +
        ' <strong>DIRECTION IS IGNORED FOR MEMBERSHIP AND DRAWN ON EVERY ' +
        'LINE.</strong> Following the arrows would answer <em>what can this ' +
        'client eventually reach</em>, and there is no such chain here: ' +
        'holding a permission on an API grants nobody that API\'s own ' +
        'permissions. Following a grant either way is what puts an API and ' +
        'the front ends holding permissions on it in one group instead of ' +
        'four.') +

        // Three paged tables on this page and no filter form to hang the size
        // on, which is exactly the drill-down case perPageForm()'s header
        // describes. The leaf is the application, and the chooser's search
        // rides along so that changing the size does not clear a search the
        // reader is still reading by.
        self.perPageForm('/admin/delegation/cluster', 'application', asked,
                         grantPage.paging.perPage,
                         'The diagram is never paged: it is the whole group ' +
                         'or it is a drawing of the pagination.',
                         Object.assign({}, self.filterOnly(listView),
                                       self.allowedChooserState(req.query))) +

        (group.counts.lines
          ? '<h2>The group, drawn</h2>' +
            self.note('<strong>A line leaves the box with the ROUND end and ' +
            'arrives at the box with the ARROWHEAD</strong>, and a DASHED ' +
            'line is a grant nobody has ever asked for. Both are the same ' +
            'marks the <a href="' +
            self.esc('/admin/delegation/allowed' + queryWith(onward, {})) +
            '">whole register</a> uses, drawn by the same code &mdash; this ' +
            'page hands it a subset of the grants and changes nothing else. ' +
            'ONE LINE PER PERMISSION rather than per pair, so two grants ' +
            'between the same two applications are two lines. There is no ' +
            'person and no hexagon on it: a configured permission says ' +
            '<em>this client may reach that API as whoever is signed ' +
            'in</em>, and there is no whoever yet.') +
            picture.html
          // WHY THERE IS NOTHING TO DRAW, and the three reasons are three
          // different states rather than one empty page. Saying "every grant
          // here is dangling" about an application that holds no grants at all
          // would be the page inventing rows to explain their absence.
          : self.note('<strong>There is nothing to draw.</strong> ' +
            (!group.counts.grants
              ? 'No grant in this group names anything: this application ' +
                'carries a base URI and permissions of its own, and nobody ' +
                'has been granted one of them. That is an API somebody ' +
                'described and nothing may reach &mdash; the most ' +
                'interesting group of one there is, and a real answer rather ' +
                'than an empty page. Grant one of the permissions listed ' +
                'below on a client\'s own page and this application joins ' +
                'that client\'s group.'
              : 'Every grant in this group is one the picture leaves out on ' +
                'purpose &mdash; a DANGLING grant, which names a permission ' +
                'no application defines and so has no far end to reach, or a ' +
                'grant an application made to ITSELF, which would be an ' +
                'arrow from a box back to the same box. Both are in the ' +
                'table below, which is where those states belong.') +
            ' <code>?format=svg</code> answers an empty document rather than ' +
            'refusing, so a link that worked yesterday still resolves.')) +

        '<h2>The applications in it</h2>' +
        self.note('<strong>Every member, with which side of a grant it is ' +
        'on.</strong> An application here is usually both &mdash; a client ' +
        'of one thing and a resource of another &mdash; which is the whole ' +
        'reason this group exists as something bigger than a pair. The last ' +
        'column crosses to the other register: what this application has ' +
        'ACTUALLY delegated, which is a different question and can name ' +
        'applications that are not in this group at all.') +
        '<table><tr><th>Application</th><th>Permissions it exposes</th>' +
        '<th>Grants held on it</th><th>Grants it holds</th>' +
        '<th>On the acts side</th></tr>' +
        (memberRows || '<tr><td colspan="5">No applications.</td></tr>') +
        '</table>' +

        '<h2>What may be asked for in it</h2>' +
        self.note('<strong>Every permission the applications in this group ' +
        'expose</strong>, granted or not. The UNGRANTED ones are why this ' +
        'table is here and not left to the grants below: a permission nobody ' +
        'holds appears on no line of the picture and in no row of that ' +
        'table, so a group whose whole content is a described API with ' +
        'nothing granted on it would otherwise be a page with nothing on it ' +
        'at all.') +
        permissionNav.head +
        '<table><tr><th>Resource</th><th>Permission</th><th>Identifier</th>' +
        '<th class="num">Held by</th><th>Who holds it</th><th></th></tr>' +
        (permissionPage.shown.map(function (one) {
          return self.permissionDefinitionRow(one, listView,
                                              { readOnly: true });
        }).join('') ||
          '<tr><td colspan="6">No application in this group exposes ' +
          'a permission, so every grant in it is dangling &mdash; each names ' +
          'a permission this registry does not define.</td></tr>') +
        '</table>' +
        permissionNav.foot +

        '<h2>Every grant in the group</h2>' +
        self.note('The rows <a href="/admin/delegation#allowed">the ' +
        'register</a> holds, narrowed to this group. <strong>Nothing here ' +
        'changes anything</strong> &mdash; the Revoke button for a grant is ' +
        'on the register itself, so that the picture pages stay documents ' +
        'rather than becoming a fourth door onto the same five actions.') +
        grantNav.head +
        '<table><tr><th>Client &mdash; who may ask</th><th>Resource &mdash; ' +
        'what is reached</th><th>Permission</th><th>Identifier</th><th>What ' +
        'the access token will say</th><th>Ever asked for?</th><th></th></tr>' +
        (grantPage.shown.map(function (one) {
          return self.permissionGrantRow(one, listView, { readOnly: true });
        }).join('') ||
          '<tr><td colspan="7">No grant in this group, which means ' +
          'this application exposes an API and nothing has been granted on ' +
          'it.</td></tr>') +
        '</table>' +
        grantNav.foot +

        '<h2>Another application</h2>' +
        self.note('The same search that is on <a href="' +
        self.esc('/admin/delegation/allowed' + queryWith(onward, {})) +
        '">the allowed picture</a>, drawn again here so that comparing two ' +
        'groups is one click rather than two.') +
        chooser +

        self.note('<code>?format=json</code> is this group, its grants and ' +
        'its graph; <code>?format=svg</code> is the picture alone, with no ' +
        'links in it. Every group at once is <code>GET ' +
        '/admin-api/permissions/groups</code>, and this one is that ' +
        'operation with <code>?application=' + self.esc(asked) + '</code>.');

      self.respond(req, res, permissionGroupsView(req.query, permissions),
                   'Delegation — one group', '/admin/delegation', inner, up);
      log.debug("Leaving the admin delegated-permission group page. " +
                group.counts.applications + " application(s), " +
                group.counts.lines + " line(s).");
    });

    app.get('/admin/delegation/map', function (req, res) {
      log.debug("Entering the admin delegation map page.");
      const view = delegationView(req.query);
      const known = knownUserKeys();
      // THE PICTURE IS OF EVERYTHING THAT MATCHED, not of the page that is
      // showing — see the header. It is `delegationView()`'s graph rather than
      // a second call to `delegation.graph()`, so this page and `GET
      // /admin-api/delegation` cannot come to disagree about what is in it.
      const graph = view.graph;

      // The labels and the shapes, worked out once. `labelOf` is handed to the
      // renderer separately because a `reaches` line says WHOSE name the
      // credential carries, and that party is neither end of it — so the
      // renderer has no `resolve()` answer for it and would otherwise print the
      // raw identity beside two boxes labelled with their cn. One name per
      // party, wherever it appears.
      const look = self.delegationLooks(graph, known);
      const looks = look.looks;
      const labelOf = look.labelOf;
      const drawingLabel =
        'Delegation relationships in this service, as a diagram';

      if (String(req.query.format || '') === 'svg') {
        // The document alone, for saving or embedding. No links (see the
        // header) and `no-store` like every other page here, because it
        // describes live state.
        self.sendDelegationSvg(res, graph, look, drawingLabel);
        log.debug("Leaving the admin delegation map page. Answered SVG.");
        return;
      }

      const filterParams = { type: view.wantedType, mode: view.wantedMode,
                             outcome: view.wantedOutcome,
                             protocol: view.wantedProtocol, q: view.wantedText,
                             per: req.query.per ? view.paging.perPage : '' };
      // The drawing and the two links under it. The `per` is dropped from the
      // links deliberately — this page has no paging, so carrying a page size
      // into the document link would be a parameter that does nothing.
      const picture = self.delegationDrawing(graph, look,
                                             '/admin/delegation/map',
        { type: view.wantedType, mode: view.wantedMode,
          outcome: view.wantedOutcome,
          protocol: view.wantedProtocol, q: view.wantedText }, drawingLabel);
      const drawn = picture.drawn;
      const listView = self.listViewOf('/admin/delegation', req.query);
      const up = self.upTo('/admin/delegation', 'The picture', listView);

      const filtering = view.wantedType || view.wantedMode ||
                        view.wantedOutcome ||
                        view.wantedProtocol || view.wantedText;

      // The filter, exactly the table's, so that narrowing the picture and
      // narrowing the table are one control rather than two that could take
      // different values. It is a GET form and carries no `page`: this page has
      // none, and a page number carried into a view with no paging is a
      // parameter that does nothing and comes back with the reader on the next
      // hop.
      const protocolsInOrder = [];
      delegation.TYPES.forEach(function (entry) {
        if (protocolsInOrder.indexOf(entry.protocol) < 0) {
          protocolsInOrder.push(entry.protocol);
        }
      });
      const typeOptions = '<option value=""' +
        (view.wantedType ? '' : ' selected') +
        '>any mechanism</option>' +
        protocolsInOrder.map(function (protocol) {
          return '<optgroup label="' + self.esc(protocol) + '">' +
            delegation.TYPES.filter(function (entry) {
              return entry.protocol === protocol;
            }).map(function (entry) {
              return '<option value="' + self.esc(entry.type) + '"' +
                     (entry.type === view.wantedType ? ' selected' : '') + '>' +
                     self.esc(entry.label) + ' (' +
                     (view.summary.byType[entry.type] || 0) +
                     ')</option>';
            }).join('') + '</optgroup>';
        }).join('');
      const modeOptions = ['<option value=""' +
                           (view.wantedMode ? '' : ' selected') +
                           '>either kind</option>']
        .concat(delegation.MODES.map(function (entry) {
          return '<option value="' + self.esc(entry.mode) + '"' +
                 (entry.mode === view.wantedMode ? ' selected' : '') + '>' +
                 self.esc(entry.label) + ' (' +
                 (view.summary.byMode[entry.mode] || 0) +
                 ')</option>';
        })).join('');
      const outcomeOptions = ['<option value=""' +
                              (view.wantedOutcome ? '' : ' ' +
          'selected') +
                              '>any outcome</option>']
        .concat(delegation.OUTCOMES.map(function (name) {
          return '<option value="' + self.esc(name) + '"' +
                 (name === view.wantedOutcome ? ' selected' : '') + '>' +
                 self.esc(name) +
                 ' (' + (view.summary.byOutcome[name] || 0) + ')</option>';
        })).join('');

      const parties = graph.nodes.filter(function (node) {
        return node.kind !== 'sts';
      });

      const inner = self.messagesOf(req) +
        // THE WAY BACK, AT THE TOP, ABOVE EVERYTHING. The trail under the nav
        // already offers it and this is deliberately a second one: the trail is
        // a strip of small grey text a reader learns to look at, and somebody
        // who followed a link into a diagram is looking at the diagram. It
        // carries the filter, so "back" means the table they came from and not
        // the top of an unfiltered one.
        self.note('<a class="btn" href="' + self.esc(up.href) +
                  '">&larr; Back to ' +
        'the delegation table</a>') +

        '<div class="tiles">' +
          self.tile(parties.length, 'parties') +
          self.tile(graph.edges.filter(function (e) {
            return e.relation !== 'issued';
          }).length,
                    'relationships') +
          self.tile(graph.chains, 'distinct chains') +
          self.tile(graph.acts, 'acts drawn') +
          self.tile(view.summary.byMode.impersonation || 0, 'impersonations') +
          self.tile(graph.tokens.length, 'credentials issued') +
        '</div>' +

        self.note('<strong>The same acts as ' +
        '<a href="' + self.esc(up.href) +
        '">the table</a>, with the time taken out ' +
        'and the parties shared.</strong> A party that is the intermediary ' +
        'of six chains is ONE box here with six lines leaving it, which is ' +
        'the thing a table of rows cannot show and the reason to draw this ' +
        'at all. Every box and every line carries the whole story in its ' +
        'tooltip; the two tables under the picture say the same things in ' +
        'words, because a diagram nobody can quote is a diagram nobody can ' +
        'put in a bug report.') +

        self.note('<strong>It is drawn from everything that MATCHED the ' +
        'filter and not from one page of it.</strong> ' + view.filtered.length +
        ' act(s) match' + (view.all.length !== view.filtered.length
          ? ' of ' + view.all.length + ' held' : '') +
        ', and all of them are in the picture — paging a diagram would draw ' +
        'the boxes that happen to be on page 2 and the lines that happen to ' +
        'join them, which is a picture of the pagination.') +

        '<form method="get" action="/admin/delegation/map"><div ' +
        'class="formrow">' +
          self.chooserCarry(req.query) +
          '<label for="type">Mechanism</label><select id="type" name="type">' +
            typeOptions + '</select>' +
          '<label for="mode">Kind</label><select id="mode" name="mode">' +
            modeOptions + '</select>' +
          '<label for="outcome">Outcome</label><select id="outcome" ' +
          'name="outcome">' +
            outcomeOptions + '</select>' +
        '</div><div class="formrow">' +
          '<label for="q">Text</label>' +
          '<input type="text" id="q" name="q" size="40" value="' +
        self.esc(view.wantedText) +
            '" placeholder="a person, an SPN, a client_id, an attribute">' +
          '<button class="secondary">Redraw</button>' +
          (filtering ? ' <a href="/admin/delegation/map">clear</a>' : '') +
        '</div></form>' +
        self.note('The same filter the table has, so narrowing one narrows ' +
        'the other. <strong>Filtering to one person is how a busy picture is ' +
        'read</strong> — the text box searches every party of the chain and ' +
        'both explanations at once.') +

        // THE TWO PIVOTS, HERE AS WELL AS ON THE TABLE, and this is where the
        // person one was actually asked for: somebody looking at a picture
        // wants to narrow the PICTURE, and the text box above narrows it to
        // acts that mention a name where these two redraw it around one party.
        // They are the same two controls the delegation table carries, so a
        // reader who learnt them there finds them here.
        //
        // The difference between them is worth the sentence: the application
        // chooser stays inside this register, and the person chooser leaves it
        // — that page also draws the ordinary issuance, which is most of what
        // happens in somebody's name and none of what is on this diagram.
        self.note('<strong>Or draw one party\'s picture instead.</strong> ' +
        'These redraw around a single party rather than narrowing these ' +
        'acts: an application, with everything delegated through it or to it ' +
        'in either role — or a <strong>person</strong>, which is the wider ' +
        'picture of the two, because it adds every ordinary grant, ' +
        'assertion, ticket and SVID issued in their name and the sign-ins ' +
        'the lot rests on. None of that is a delegation, so none of it can ' +
        'be on this diagram.') +
        self.delegationApplicationChooser(view.applications, '', listView,
            { path: '/admin/delegation/map', query: req.query }) +
        self.delegationUserChooser(userGraph.userList(), '', listView,
            { path: '/admin/delegation/map', query: req.query }) +

        (graph.acts
          ? picture.html
          : self.note('<strong>Nothing has delegated anything yet' +
            (filtering ? ' that matches this filter' : '') + ', so there is ' +
            'nothing to draw.</strong> Three things put a box on this page: ' +
            'a Kerberos S4U2Self, S4U2Proxy or forwarded-TGT request at the ' +
            'KDC; a WS-Trust <code>RequestSecurityToken</code> carrying ' +
            '<code>&lt;wst:OnBehalfOf&gt;</code> or ' +
            '<code>&lt;wst14:ActAs&gt;</code>; and an RFC 8693 token ' +
            'exchange at <code>/oauth2/token</code>. A REFUSED attempt ' +
            'counts and is drawn in red.')) +

        '<h2>The key</h2>' +
        self.note('The shapes are drawn by the same functions the picture ' +
        'uses, so a legend cannot come to describe a diagram this service no ' +
        'longer draws.') +
        self.delegationMapKey() +

        '<h2>The parties</h2>' +
        self.note('Every box, with both of its links where it has two. The ' +
        'picture can only put a shape inside ONE anchor, so a party that is ' +
        'a person AND an application links to the users page there and to ' +
        'both here.' +
        (directoryReader ? '' :
          ' <strong>No LDAP directory is loaded in this process</strong>, so ' +
          'nothing here can be resolved to a person and every box is drawn ' +
          'from its role. That is a build without ' +
          '<code>ldap_server.js</code> and not a failure.')) +
        '<table><tr><th>Label</th><th>Drawn as</th><th>Identity</th>' +
        '<th>Roles it played</th><th>Acts</th><th>Protocols</th></tr>' +
        (parties.map(function (node) {
          return self.delegationNodeRow(node, known, looks[node.id]);
        }).join('') || '<tr><td colspan="6">No parties yet.</td></tr>') +
        '</table><h2>The ' +
        'relationships</h2>' +
        self.note('Every line, as a sentence. <strong>The two kinds are ' +
        'different claims and the picture colours them differently</strong>: ' +
        '<em>acts for</em> is the DELEGATION relationship — who is acting on ' +
        'whose behalf — and <em>reaches</em> is the TRUST relationship, what ' +
        'the credential is FOR, which is the question <em>what is this ' +
        'token\'s audience</em> asked as a picture. The grey lines from the ' +
        'hexagon are neither: they are this service handing a credential to ' +
        'whoever asked for one.') +
        self.note('<strong>The last column collapses one sentence and it is ' +
        'the same collapse the delegation table makes.</strong> Kerberos is ' +
        'the only family here that polices delegation at all, so a Kerberos ' +
        'row names an ATTRIBUTE and an account — short, and different on ' +
        'every row — while every WS-Trust and RFC 8693 row carries the ' +
        'identical paragraph saying that nothing checked it. Repeated down a ' +
        'table that paragraph is the column, so it is <em>nothing checks ' +
        'this</em> with the wording in the tooltip. <strong>A REFUSAL is ' +
        'never collapsed</strong> and prints in full: it is the KDC\'s own ' +
        'words, the same sentence the client was sent, and it is specific to ' +
        'the act rather than to the mechanism.') +
        '<table><tr><th>From</th><th>To</th><th>Relationship</th><th>' +
        'Mechanism</th><th>Kind</th><th>Acts</th><th>What came ' +
        'out</th><th>Authorized by / why not</th></tr>' +
        (graph.edges.map(function (edge) {
          return self.delegationEdgeRow(edge, labelOf);
        }).join('') || '<tr><td colspan="8">No relationships yet.</td></tr>') +
        '</table>' +

        '<h2>What was issued</h2>' +
        self.note('<strong>Every credential that came out of an act in this ' +
        'picture</strong>, newest first — a Kerberos service ticket, a SAML ' +
        'assertion, an access token — with the chain it came out of. ' +
        '<strong>NO CREDENTIAL IS EVER HERE, only its kind and its ' +
        'identifier</strong>, which is the rule the audit log follows and ' +
        'which applies here for one more reason: a delegation act is ' +
        'precisely the request that carries two credentials at once. A ' +
        'Kerberos ticket genuinely has no identifier to quote, which this ' +
        'says rather than leaving a blank column to be read as a bug.') +
        self.note('A REFUSED act produced nothing by definition, so it is ' +
        'not in this list — which is why ' + graph.tokens.length + ' ' +
        'credential(s) sit under ' + graph.acts +
        ' act(s) and the two numbers do not have to agree.') +
        '<table><tr><th class="num">#</th><th>When</th><th>Credential</th>' +
        '<th>Subject</th><th>Actor</th><th>Target</th><th>Mechanism</th></tr>' +
        (graph.tokens.map(function (token) {
          return self.delegationTokenRow(token, labelOf);
        }).join('') ||
          '<tr><td colspan="7">Nothing has been issued through a ' +
          'delegation yet. A REFUSED act produces nothing, so a page of red ' +
          'lines and an empty table here is a consistent state rather than a ' +
          'broken one.</td></tr>') + '</table>' +
        (graph.tokensLeftOff
          ? self.note('<strong>' + graph.tokensLeftOff + ' more ' +
            'credential(s) are not listed.</strong> This list holds at most ' +
            graph.maxTokenRows +
            ' and keeps the newest; every one of them is still COUNTED on ' +
            'its line in the picture and in the relationship table, so what ' +
            'is lost is the individual identifiers of the oldest. Filter to ' +
            'narrow it.')
          : '') +

        '<h2>What this picture cannot say</h2>' +
        self.note('<strong>An IMPERSONATION is invisible everywhere else, ' +
        'and that is why the amber lines matter.</strong> Under a delegation ' +
        'the credential carries the chain, so a resource server can read the ' +
        'actor off the token afterwards. Under an impersonation nothing ' +
        'does: no reading of the token, at the far end or in a log, can ' +
        'recover the fact that a middle tier was involved. This diagram and ' +
        'the table behind it are the only places that fact will ever exist.') +
        self.note('<strong>A line is a RELATIONSHIP and not a ' +
        'request.</strong> Four acts a second apart between the same three ' +
        'parties are one line with <em>4 issued</em> on it; the outcome is ' +
        'deliberately not part of a chain\'s identity, so a chain refused ' +
        'nine times and then fixed is one line that changes colour rather ' +
        'than two that never meet. <a href="' + self.esc(up.href) +
        '">The table</a> is where the individual ' +
        'acts are, in order, with their times.') +
        self.note('<strong>Who MAY delegate to whom is not on this ' +
        'page</strong>, because it is CONFIGURATION rather than history and ' +
        'it is Kerberos-only — Kerberos is the one family here that polices ' +
        'delegation at all. It is the second half of <a ' +
        'href="' + self.esc(up.href) + '">the delegation page</a>, ' +
        'and the asymmetry between the two is worth reading there: the same ' +
        'picture, policed at one end and not at the other.') +
        self.note('<strong>This page runs no script and neither does ' +
        'anything else in this console.</strong> The diagram is generated on ' +
        'the server and arrives as markup, which is why it does not pan, ' +
        'zoom or drag — and why nothing here relaxes <code>script-src ' +
        '\'none\'</code>. Use the filter to narrow a busy picture, or take ' +
        'the document and open it in something that does zoom.') +
        self.note('<code>?format=json</code> carries the whole graph — the ' +
        'nodes, the edges, the credentials folded onto each edge, and the ' +
        'token list — and it is also in the <code>graph</code> member of ' +
        '<code>GET /admin-api/delegation</code>, so a test can assert what ' +
        'this page draws without parsing an SVG. <code>?format=svg</code> is ' +
        'the document alone.');

      self.respond(req, res, Object.assign({}, graph, {
        // The filter that produced it, so a caller polling this endpoint can
        // tell a picture of everything from a picture of one person.
        filter: view.json.filter,
        matched: view.filtered.length,
        held: view.summary.held,
        // The rendered size, which is the one fact about the DRAWING rather
        // than the graph that a caller might reasonably want — an empty picture
        // and a picture that failed to lay out are otherwise indistinguishable
        // in JSON.
        drawing: { width: drawn.width, height: drawn.height,
                   failed: drawn.failed || null }
      }), 'Delegation — the picture', '/admin/delegation', inner, up);
      log.debug("Leaving the admin delegation map page.");
    });

    // -------------------------------------------------------------------------
    // GET /admin/delegation/chain?chain=… — ONE RELATIONSHIP, DRAWN ALONE.
    //
    // A second drill-down of /admin/delegation, beside /admin/delegation/map,
    // and it exists because of what the whole picture cannot do: on a service
    // that has been driven for an afternoon the map is forty boxes, and the
    // question somebody brings to a ROW of the table — *what is this one,
    // exactly* — is answered by deleting everything else. Every row of both
    // tables on that page links here.
    //
    // **IT IS THE SAME GRAPH FUNCTION OVER A SUBSET, AND THAT IS THE WHOLE
    // IMPLEMENTATION.** `delegation.graph()` takes the acts it is to draw (its
    // header says so), so this route hands it one chain's and everything below
    // — the shapes, the labels, the two tables, the credential list — is the
    // map's own, through the three helpers above. A second drawing routine for
    // "one chain" would be a second answer to what a box is called, and the
    // reader comparing this page with the map would have no way to tell that
    // from two boxes that really are different parties.
    //
    // **THE KEY IN THE URL IS THE `chainKey` AND NOT AN INDEX**, for the reason
    // `actsOfChain()`'s header gives: an index into a capped list moves when
    // the cap bites, so a link somebody put in a ticket would come back
    // describing a DIFFERENT relationship rather than nothing. The key is long
    // and that is the price; it is exact, and a link that has gone stale says
    // so.
    //
    // **A CHAIN THAT IS NOT HELD IS NOT A 404**, which is the rule
    // /admin/logout's lookup follows and it matters more here: this store is
    // capped and drops the oldest, so "no acts under this key" is the ORDINARY
    // outcome of an old link rather than a mistake. The page says which of the
    // two it is — a key nobody ever recorded, or one whose acts have been
    // dropped — and offers the way back.
    //
    // No form, so no operation on /admin-api — rule 7, satisfied exactly as
    // /admin/delegation and its map satisfy it. `?format=json` is the graph and
    // the acts; `?format=svg` is the document alone.
    // -------------------------------------------------------------------------
    app.get('/admin/delegation/chain', function (req, res) {
      log.debug("Entering the admin delegation chain page.");
      const wanted = String(req.query.chain || '');
      const known = knownUserKeys();
      const all = delegation.list();
      const acts = delegation.actsOfChain(all, wanted);
      // chainList() over the acts of ONE chain returns exactly one row, and it
      // is that function's answer rather than a shape built here — the counts,
      // the first and last times and the rule about which explanation wins are
      // all decisions it makes, and a second copy of them would drift.
      const chain = delegation.chainList(acts)[0] || null;
      const graph = delegation.graph(acts);
      const look = self.delegationLooks(graph, known);
      const labelOf = look.labelOf;
      const drawingLabel = chain
        ? 'One delegation relationship: ' + chain.typeLabel
        : 'A delegation relationship that is no longer held';

      if (String(req.query.format || '') === 'svg') {
        self.sendDelegationSvg(res, graph, look, drawingLabel);
        log.debug("Leaving the admin delegation chain page. Answered SVG.");
        return;
      }

      const listView = self.listViewOf('/admin/delegation', req.query);
      const up = self.upTo('/admin/delegation', 'One relationship', listView);
      const back = self.note('<a class="btn" href="' + self.esc(up.href) +
        '">&larr; Back to the delegation table</a>');

      const json: Record<string, any> = {
        chain: chain, chainKey: wanted, found: !!chain,
        acts: acts, graph: graph,
        held: delegation.summary().held
      };

      if (!chain) {
        const inner = self.messagesOf(req) + back +
          (wanted
            ? self.note('<strong>No act held here belongs to that ' +
              'relationship.</strong> <code>' + self.esc(wanted) + '</code> ' +
              'names a chain this page can describe only while at least one ' +
              'of its acts is still held, and this store is CAPPED — it ' +
              'keeps at most ' +
              self.esc(delegation.summary().maxRecords) + ' acts and drops ' +
              'the oldest first. So an old link coming back empty is the ' +
              'ordinary outcome rather than a mistake, and so is a link from ' +
              'a service that has restarted since: nothing here is ' +
              'persisted. Raise <code>delegation.maxRecords</code> on <a ' +
              'href="/admin/delegation">the delegation page</a> if this ' +
              'keeps happening to something you need.')
            : self.note('<strong>Name a relationship.</strong> This page ' +
              'draws ONE of them, and the way to it is a link on ' +
              '<a href="' + self.esc(up.href) +
              '">the delegation table</a> — every row of both tables there ' +
              'has one, because the key that identifies a chain is ' +
              '<em>(mechanism, initial identity, intermediary, target)</em> ' +
              'and is not something worth typing.')) +
          self.note('<a href="/admin/delegation/map">The whole picture</a> ' +
          'is everything that is still held, drawn together.');
        self.respond(req, res, json, 'Delegation — one relationship',
                     '/admin/delegation',
                     inner, up);
        log.debug("Leaving the admin delegation chain page. Nothing under " +
                  "that key.");
        return;
      }

      const parties = graph.nodes.filter(function (node) {
        return node.kind !== 'sts';
      });
      const picture = self.delegationDrawing(graph, look,
                                             '/admin/delegation/chain',
                                             Object.assign({}, listView,
                                                           { chain: wanted }),
                                             drawingLabel);
      json.drawing = { width: picture.drawn.width, height: picture.drawn.height,
                       failed: picture.drawn.failed || null };

      // The chain as ONE SENTENCE, above everything. It is the row the reader
      // clicked, said in words: the table gave them four columns and this page
      // has to open by confirming it is describing the same four, or a reader
      // who followed the wrong link finds out three tables later.
      const sentence =
        self.note('<strong>' +
        self.esc(chain.initial.presented || chain.initial.application ||
                 'somebody nobody named') +
        '</strong> — ' +
        (chain.intermediary.presented || chain.intermediary.application
          ? 'acted for by <strong>' +
            self.esc(chain.intermediary.presented ||
                     chain.intermediary.application) +
            '</strong>'
          : '<span class="state-none">with no intermediary this service was ' +
            'ever told the name of</span>') +
        ' — reaching <strong>' +
        self.esc(chain.target.application || chain.target.presented ||
                 'nothing in particular') +
        '</strong>, by <code>' + self.esc(chain.type) + '</code> (' +
        self.esc(chain.typeLabel) + ', ' + self.esc(chain.protocol) + '). ' +
        'It is an ' + self.modeCell(chain.mode) + ' and it has happened ' +
        self.esc(chain.acts) + ' time(s) — first ' +
        self.esc(self.whenText(chain.firstAt)) +
        ', last ' + self.esc(self.whenText(chain.lastAt)) + '.');

      const inner = self.messagesOf(req) + back +
        '<div class="tiles">' +
          self.tile(chain.acts, 'acts on it') +
          self.tile(chain.issued, 'issued') +
          self.tile(chain.refused, 'refused') +
          self.tile(graph.tokens.length, 'credentials issued') +
          self.tile(parties.length, 'parties') +
          self.tile(graph.edges.filter(function (e) {
            return e.relation !== 'issued';
          }).length,
                    'lines') +
        '</div>' +

        sentence +

        self.note('<strong>This is one row of ' +
        '<a href="' + self.esc(up.href) +
        '">the chains table</a> drawn on its ' +
        'own</strong> , with everything else in the service left out. A ' +
        'chain is <em>(mechanism, initial identity, intermediary, ' +
        'target)</em> and the OUTCOME is deliberately not part of it, so a ' +
        'relationship refused nine times and then fixed is this one page ' +
        'rather than two that never meet — which is why the acts below can ' +
        'be red and green at once. <a href="/admin/delegation/map">The whole ' +
        'picture</a> is every relationship at once, where this one\'s ' +
        'parties are shared with the others they take part in.') +

        (graph.acts
          ? picture.html
          : self.note('There is nothing to draw.')) +

        '<h2>The key</h2>' +
        self.note('The shapes are drawn by the same functions the picture ' +
        'uses, so a legend cannot come to describe a diagram this service no ' +
        'longer draws.') +
        self.delegationMapKey() +

        '<h2>The parties</h2>' +
        self.note('Up to three boxes — the layers of the architecture — with ' +
        'both of a party\'s links where it has two. <strong>A box here can ' +
        'carry more acts than this relationship has</strong> only if it ' +
        'played two roles in one of them, which is what S4U2Self is: the ' +
        'requester asks for a ticket to itself, so it is the intermediary ' +
        'AND the target.') +
        '<table><tr><th>Label</th><th>Drawn as</th><th>Identity</th>' +
        '<th>Roles it played</th><th>Acts</th><th>Protocols</th></tr>' +
        (parties.map(function (node) {
          return self.delegationNodeRow(node, known, look.looks[node.id]);
        }).join('') || '<tr><td colspan="6">No parties.</td></tr>') +
          '</table>' +

        '<h2>The relationships</h2>' +
        self.note('A chain has three parties and therefore up to TWO lines, ' +
        'and they are different claims: <em>acts for</em> is the DELEGATION ' +
        'relationship — who is acting on whose behalf — and <em>reaches</em> ' +
        'is the TRUST relationship, what the credential is FOR. The grey ' +
        'line from the hexagon is neither: it is this service handing a ' +
        'credential to whoever asked for one.') +
        '<table><tr><th>From</th><th>To</th><th>Relationship</th><th>' +
        'Mechanism</th><th>Kind</th><th>Acts</th><th>What came ' +
        'out</th><th>Authorized by / why not</th></tr>' +
        (graph.edges.map(function (edge) {
          return self.delegationEdgeRow(edge, labelOf);
        }).join('') || '<tr><td colspan="8">No relationships.</td></tr>') +
        '</table>' +

        '<h2>What was issued on it</h2>' +
        self.note('<strong>Every credential that came out of this ' +
        'relationship</strong>, newest first. <strong>NO CREDENTIAL IS EVER ' +
        'HERE, only its kind and its identifier</strong> — the rule the ' +
        'audit log follows, and one that applies here for one more reason: a ' +
        'delegation act is precisely the request that carries two ' +
        'credentials at once. A REFUSED act produced nothing by definition, ' +
        'which is why ' + graph.tokens.length +
        ' credential(s) sit under ' + chain.acts + ' act(s).') +
        '<table><tr><th class="num">#</th><th>When</th><th>Credential</th>' +
        '<th>Subject</th><th>Actor</th><th>Target</th><th>Mechanism</th></tr>' +
        (graph.tokens.map(function (token) {
          return self.delegationTokenRow(token, labelOf);
        }).join('') || '<tr><td colspan="7">Nothing was issued on this ' +
          'relationship. A page of red acts and an empty table here is a ' +
          'consistent state rather than a broken one.</td></tr>') + '</table>' +

        '<h2>Every act on it</h2>' +
        self.note('The same rows <a href="' + self.esc(up.href) +
                  '">the delegation ' +
        'table</a> holds, narrowed to this ' +
        'relationship and not paged — there are ' +
        self.esc(acts.length) + ' of them and the cap on the whole store is ' +
        self.esc(delegation.summary().maxRecords) + '. This is where the ' +
        'TIMES are: the picture has them taken out, because four acts a ' +
        'second apart between the same three parties are one line.') +
        '<table><tr><th class="num">#</th><th>When</th><th>Mechanism</th>' +
        '<th>Kind</th><th>Outcome</th><th>Initial identity</th>' +
        '<th>Intermediary</th><th>Target</th><th>Authorized by / why not</th>' +
        '<th>Credentials</th></tr>' +
        acts.map(function (row) {
          // No `chain` link on this table: every row on it belongs to the chain
          // being drawn, so the link would point at the page it is on.
          return self.delegationRow(row, known, { chainLink: false });
        }).join('') + '</table>' +

        self.note('<code>?format=json</code> carries this chain, its acts ' +
        'and the graph behind the picture; <code>?format=svg</code> is the ' +
        'document alone, with no links in it. There is no form on this page ' +
        'and therefore no operation on <code>/admin-api</code> — everything ' +
        'here is an observation, and the acts are in <code>GET ' +
        '/admin-api/delegation</code> where a caller can filter them.');

      self.respond(req, res, json, 'Delegation — one relationship',
                   '/admin/delegation',
                   inner, up);
      log.debug("Leaving the admin delegation chain page. " + acts.length +
                " " +
          "act(s).");
    });

    // -------------------------------------------------------------------------
    // GET /admin/delegation/application?application=… — ONE APPLICATION, IN
    // EVERY ROLE IT HAS EVER PLAYED, AND EVERYTHING DELEGATED THROUGH OR TO IT.
    //
    // The third drill-down of /admin/delegation and the one that asks a
    // different question from the other two. The map and the chain page are
    // both *what talks to what*; this one is **what has been issued because of
    // this application**, which is the question somebody brings when they are
    // about to turn one off, or when a resource server is seeing tokens it did
    // not expect.
    //
    // **REGARDLESS OF ROLE, AND THAT IS THE WHOLE POINT OF THE PAGE.** A middle
    // tier is the intermediary of the chains it acts on and the TARGET of the
    // ones that reach it, and those two lists are the same application seen
    // from two sides. Splitting them into two pages — or offering only the
    // targets, which is the easy half — would leave somebody able to see what
    // was issued FOR a service and not what was issued THROUGH it, and the
    // second is the interesting half of a delegation. The credentials table
    // therefore carries a ROLE column rather than being filtered by one.
    //
    // **THE CHOICE IS FROM A LIST AND NOT A TEXT BOX**, which is the one place
    // this page departs from /admin/logout's lookup. That page takes an
    // identity, and any name a person can type is a legitimate thing to ask
    // about — nothing has to have happened first. Here the question only means
    // anything for an application some act NAMED, so the console offers what it
    // has rather than inviting somebody to guess a spelling and be told nothing
    // matched. `applicationList()` in delegation.js is where that list comes
    // from, and its header says why an application is keyed on its IDENTIFIER
    // rather than on a box in the picture.
    //
    // **A BARE PAGE IS THE CHOOSER, NOT A 404** — /admin/logout's rule, for the
    // same reason: this is a lookup, and arriving with nothing selected is how
    // somebody gets here from the sidebar or a bookmark.
    //
    // No form that CHANGES anything, so no operation on /admin-api — rule 7
    // exactly as the other two satisfy it. `?format=json` carries the chooser's
    // list or the selected application's acts and graph; `?format=svg` is the
    // picture alone.
    // -------------------------------------------------------------------------
    app.get('/admin/delegation/application', function (req, res) {
      log.debug("Entering the admin delegation application page.");
      const asked = String(req.query.application || '').trim();
      // Normalised the same way the store normalises one, so a link carrying
      // the RAW identifier — which is what a reader will paste out of the acts
      // table — finds the same application the chooser's own link does.
      const key = delegation.applicationKeyOf(asked);
      const known = knownUserKeys();
      const all = delegation.list();
      const catalogue = delegation.applicationList(all);
      const entry =
          catalogue.filter(function (one) { return one.key === key; })[0] ||
            null;
      const acts = entry ? delegation.actsForApplication(all, key) : [];
      const graph = delegation.graph(acts);
      const look = self.delegationLooks(graph, known);
      const labelOf = look.labelOf;
      const drawingLabel = entry
        ? 'Everything delegated through or to ' + entry.identifier
        : 'Applications with delegated access';

      if (String(req.query.format || '') === 'svg') {
        self.sendDelegationSvg(res, graph, look, drawingLabel);
        log.debug("Leaving the admin delegation application page. Answered " +
                  "SVG.");
        return;
      }

      const listView = self.listViewOf('/admin/delegation', req.query);
      const up = self.upTo('/admin/delegation', 'One application', listView);
      const back = self.note('<a class="btn" href="' + self.esc(up.href) +
        '">&larr; Back to the delegation table</a>');

      // The chooser itself, drawn on the bare page AND under a selected
      // application — the second is what makes comparing two of them one click
      // rather than two.
      const chooser = self.delegationApplicationChooser(catalogue, key,
                                                        listView,
          { path: '/admin/delegation/application', query: req.query });

      if (!entry) {
        const inner = self.messagesOf(req) + back +
          (asked
            ? self.note('<strong>No act held here names ' +
              '<code>' + self.esc(asked) + '</code> as an ' +
              'application.</strong> Three things could be true and they are ' +
              'different: nothing has ever delegated through or to it; ' +
              'something did and the acts have been DROPPED, because this ' +
              'store keeps at most ' +
              self.esc(delegation.summary().maxRecords) + ' and discards the ' +
              'oldest; or the name is spelled differently from the way a ' +
              'protocol presented it. The list below is every application ' +
              'some act actually named, which settles the third.')
            : '') +
          self.note('<strong>Choose an application to see everything that ' +
          'has been delegated through it or to it.</strong> A delegation has ' +
          'three parties and an application can be two of them — the ' +
          'INTERMEDIARY that acts on somebody\'s behalf, and the TARGET the ' +
          'credential is for — so this page shows both sides of one ' +
          'application rather than making you pick a side. That is the ' +
          'question worth asking before turning a middle tier off: not ' +
          '<em>what reaches it</em>, but <em>what exists because of it</em>.') +
          chooser +
          self.delegationApplicationTable(catalogue, known, listView) +
          self.note('The list is built from the ACTS rather than from <a ' +
          'href="/admin/applications">the registry</a>, which is why an ' +
          'entry here can be marked <em>not in the registry</em>: the ' +
          'registry holds what this service has been asked about, and an RFC ' +
          '8693 <code>audience</code> nobody has otherwise mentioned is a ' +
          'real delegation target that nothing else in this console knows ' +
          'the name of.');
        self.respond(req, res, { application: null, asked: asked || null,
                                 applications: catalogue,
                                 held: delegation.summary().held },
                     'Delegation — one application', '/admin/delegation', inner,
                     up);
        log.debug("Leaving the admin delegation application page. " +
                  (asked ? "Nothing named that." : "Drew the chooser."));
        return;
      }

      // WHICH ROLES THIS APPLICATION PLAYED IN THE ACT EACH CREDENTIAL CAME OUT
      // OF. Keyed on `seq`, which is the act's own identifier and is monotonic
      // and never reused — so this cannot attach a role to the wrong credential
      // even after the cap has dropped something between the two walks.
      const rolesBySeq = {};
      acts.forEach(function (row) {
        rolesBySeq[row.seq] = delegation.applicationRolesIn(row, key);
      });

      const parties = graph.nodes.filter(function (node) {
        return node.kind !== 'sts';
      });
      const picture = self.delegationDrawing(graph, look,
                                             '/admin/delegation/application',
                                             Object.assign(
                                               {}, listView,
                                               { application:
                                                   entry.identifier }),
                                             drawingLabel);

      // The registry's answer, tried against every spelling: `ou=applications`
      // is keyed by what a caller presented, and this page's key is normalised
      // — so asking with the normalised form alone would report "not in the
      // registry" for an application that is in it under the realm-qualified
      // name.
      let registered = null;
      entry.spellings.forEach(function (spelling) {
        if (!registered) {
          registered = applications.get(spelling) || null;
        }
      });

      const inner = self.messagesOf(req) + back +
        '<div class="tiles">' +
          self.tile(entry.acts, 'acts') +
          self.tile(entry.issued, 'issued') +
          self.tile(entry.refused, 'refused') +
          self.tile(graph.tokens.length, 'credentials issued') +
          self.tile(entry.chains, 'relationships') +
          self.tile(entry.roles.intermediary, 'as the intermediary') +
        '</div>' +

        self.note('<strong><code>' + self.esc(entry.identifier) +
                  '</code></strong> — ' +
        (registered
          ? '<a href="' + self.esc('/admin/applications' +
              queryWith({ application: entry.identifier }, {})) + '">in the ' +
            'registry</a> as <strong>' +
            self.esc(registered.name || registered.dnLabel ||
                     entry.identifier) +
            '</strong>'
          : '<span class="state-none" title="No entry under ou=applications ' +
            'names this. The registry holds what this service has been ASKED ' +
            'ABOUT, and a delegation naming something nobody has otherwise ' +
            'mentioned is ordinary — an RFC 8693 audience is exactly ' +
            'that.">not in the registry</span>') +
        (entry.identityKey
          ? '. It has also PRESENTED a credential of its own, so it is a ' +
            'person here as well as an application: ' +
            self.usersPageCell(entry.identityKey, known) + ' on the users ' +
            'page. That is the middle tier being both, which is ordinary — a ' +
            'service account authenticates, so the identity funnel files it ' +
            'under <code>ou=users</code>, and tickets are issued FOR it, so ' +
            'the registry files it under <code>ou=applications</code>.'
          : '.') +
        (entry.spellings.length > 1
          ? ' <strong>It has been spelled ' + entry.spellings.length + ' ' +
            'ways</strong> and they are one application ' +
            'here: ' + self.codeList(entry.spellings) +
            '. Two spellings of one identity is two people, so they are ' +
            'collapsed on the same normalisation the picture uses — the ' +
            'spellings are kept so that the collapse is something you can ' +
            'see rather than take on trust.'
          : '') +
        ' Protocols: ' +
        (entry.protocols.length ? self.codeList(entry.protocols) : 'none') +
        '. First seen ' + self.esc(self.whenText(entry.firstAt)) + ', last ' +
        self.esc(self.whenText(entry.lastAt)) + '.') +

        '<h2>What it does in a delegation</h2>' +
        self.note('<strong>Both sides of one application.</strong> The ' +
        'counts below are of ACTS, and one act can count twice here — an ' +
        'S4U2Self names the requester as the intermediary and as the target, ' +
        'because the ticket is to itself.') +
        '<table><tr><th>Role</th><th>Acts</th><th>What the role is</th></tr>' +
        delegation.ROLES.map(function (role) {
          const n = entry.roles[role.role] || 0;
          return '<tr>' +
            '<td>' + self.esc(role.label) + '</td>' +
            '<td class="num">' + (n
              ? '<strong>' + self.esc(n) + '</strong>'
              : '<span class="state-none">0</span>') + '</td>' +
            '<td>' + self.esc(role.what) + '</td>' +
            '</tr>';
        }).join('') + '</table>' +

        (graph.acts
          ? '<h2>The relationships it is part of</h2>' +
            self.note('Every chain this application appears in, drawn ' +
            'together — so a middle tier shows the people it acts for on one ' +
            'side and what it reaches on the other, which is the shape a ' +
            'list of rows cannot show. The <strong>chain</strong> link ' +
            'beside each act at the foot of this page draws ONE of them ' +
            'alone.') + picture.html
          : '') +

        '<h2>The key</h2>' +
        self.note('The shapes are drawn by the same functions the picture ' +
        'uses, so a legend cannot come to describe a diagram this service no ' +
        'longer draws.') +
        self.delegationMapKey() +

        '<h2>The parties it deals with</h2>' +
        self.note('Every box in the picture above, including this ' +
        'application itself.') +
        '<table><tr><th>Label</th><th>Drawn as</th><th>Identity</th>' +
        '<th>Roles it played</th><th>Acts</th><th>Protocols</th></tr>' +
        (parties.map(function (node) {
          return self.delegationNodeRow(node, known, look.looks[node.id]);
        }).join('') || '<tr><td colspan="6">No parties.</td></tr>') +
          '</table>' +

        '<h2>Every delegated credential related to it</h2>' +
        self.note('<strong>This is the list this page exists for.</strong> ' +
        'Every credential that came out of an act this application took part ' +
        'in, newest first, WHATEVER ROLE IT PLAYED — so a token issued ' +
        'THROUGH it (it was the intermediary) and one issued FOR it (it was ' +
        'the target) are both here, with the role in its own column. ' +
        '<strong>NO CREDENTIAL IS EVER HERE, only its kind and its ' +
        'identifier</strong>, which is the rule the audit log follows; a ' +
        'Kerberos ticket genuinely has no identifier to quote. A REFUSED act ' +
        'produced nothing by definition, which is why ' + graph.tokens.length +
        ' credential(s) sit under ' + entry.acts + ' act(s).') +
        '<table><tr><th class="num">#</th><th>When</th><th>Its role</th>' +
        '<th>Credential</th><th>Subject</th><th>Actor</th><th>Target</th>' +
        '<th>Mechanism</th></tr>' +
        (graph.tokens.map(function (token) {
          return self.delegationTokenRow(
            token, labelOf,
            self.delegationRoleCell(rolesBySeq[token.seq]));
        }).join('') ||
          '<tr><td colspan="8">Nothing has been issued through this ' +
          'application or to it. A page of red acts and an empty table here ' +
          'is a consistent state rather than a broken one.</td></tr>') +
          '</table>' +
        (graph.tokensLeftOff
          ? self.note('<strong>' + graph.tokensLeftOff + ' more ' +
            'credential(s) are not listed.</strong> This list holds at most ' +
            graph.maxTokenRows +
            ' and keeps the newest; every one of them is still COUNTED on ' +
            'its line in the picture, so what is lost is the individual ' +
            'identifiers of the oldest.')
          : '') +

        '<h2>Every act it took part in</h2>' +
        self.note('The rows <a href="' + self.esc(up.href) +
                  '">the delegation table</a> ' +
        'holds, narrowed to this application and not paged. This is where ' +
        'the TIMES and the REFUSALS are — a refusal produced no credential, ' +
        'so it is in this table and not in the one above.') +
        '<table><tr><th class="num">#</th><th>When</th><th>Mechanism</th>' +
        '<th>Kind</th><th>Outcome</th><th>Initial identity</th>' +
        '<th>Intermediary</th><th>Target</th><th>Authorized by / why not</th>' +
        '<th>Credentials</th></tr>' +
        acts.map(function (row) {
          return self.delegationRow(row, known, { listView: listView });
        }).join('') + '</table>' +

        '<h2>Another application</h2>' + chooser +
        self.delegationApplicationTable(catalogue, known, listView) +

        self.note('<code>?format=json</code> carries this application, its ' +
        'acts and the graph behind the picture; <code>?format=svg</code> is ' +
        'the document alone. There is no form that changes anything on this ' +
        'page and therefore no operation on <code>/admin-api</code> — the ' +
        'acts are in <code>GET /admin-api/delegation</code>, where a caller ' +
        'can filter them by the same free text.');

      self.respond(req, res, {
        application: entry,
        asked: asked,
        registered: !!registered,
        acts: acts,
        graph: graph,
        // The role played per act, keyed by the act's sequence number. It is
        // the one thing here a caller could not work out from `acts` without
        // reimplementing the normalisation — which is exactly the drift the
        // store owns that rule to prevent.
        rolesBySeq: rolesBySeq,
        applications: catalogue,
        held: delegation.summary().held,
        drawing: { width: picture.drawn.width, height: picture.drawn.height,
                   failed: picture.drawn.failed || null }
      }, 'Delegation — one application', '/admin/delegation', inner, up);
      log.debug("Leaving the admin delegation application page. " +
                acts.length +
                " act(s), " + graph.tokens.length + " credential(s).");
    });

    // -------------------------------------------------------------------------
    // GET /admin/delegation/user?user=… — ONE PERSON, END TO END.
    //
    // The FOURTH drill-down of /admin/delegation and the only page in this
    // console that draws MORE than the delegation register. That is the whole
    // of what makes it worth having and it is worth stating plainly rather than
    // leaving to be discovered: /admin/delegation/map answers *what talks to
    // what*, /admin/delegation/application answers *what exists because of this
    // application*, and neither of them can answer *what has this service done
    // in alice's name* — because most of what it has done in alice's name is
    // not a delegation. An authorization code grant is not an act. A Kerberos
    // AS-REQ is not an act. A SAML assertion is not an act. Drawn from the
    // delegation register alone, a person who signed in nine times and holds
    // twenty tokens is an empty picture.
    //
    // **SO IT IS A UNION OF TWO REGISTERS AND THE UNION IS NOT HERE.** It is in
    // `common/user_graph.ts`, which holds the model for the reason every other
    // store rule in this console holds: what counts as one credential seen
    // twice, what a `client_id` on a token and an `application` on a party have
    // in common, and which spelling of a person is THE spelling are statements
    // about the stores, and a renderer holding a second opinion about any of
    // them is drift nothing can see. This file renders what it is handed, and
    // every table below is drawn from the same graph the picture is.
    //
    // **WHAT THE PICTURE GAINS IS TWO KINDS OF LINE**, and each answers a
    // question the delegation picture cannot:
    //
    //   * a DOTTED line from the person into the hexagon — they authenticated,
    //     in that family, by that method. It is the OIDC/Kerberos/SAML
    //     *authentication* half, and without it the diagram shows tokens beside
    //     somebody who, as far as it goes, has never been here.
    //   * a SOLID indigo line from the person to an application — a credential
    //     naming them was issued to it, LABELLED WITH THE GRANT: `Authorization
    //     Code grant`, `Refresh Token grant`, `Client Credentials grant`, with
    //     RFC 6749's section beside it and OpenID Connect's name for the same
    //     exchange where it has one.
    //
    // Neither takes a MODE colour, and that is deliberate rather than an
    // omission: amber and green are this console's judgement about
    // impersonation versus delegation, and an ordinary grant makes no such
    // claim. A reader who has learnt that pairing from the delegation table
    // would be told something false by a green authorization code grant.
    //
    // **A TOKEN EXCHANGE IS IN BOTH REGISTERS AND IS DRAWN ONCE**, on its
    // delegation line, because that line says strictly more (it names the actor
    // and the mode). The count of what was left off is printed rather than left
    // to be noticed. A Kerberos S4U ticket is the one overlap that survives,
    // because a ticket has no identifier in either register to collapse the two
    // on — the page says so where it matters rather than pretending the picture
    // is exact.
    //
    // **THE CHOICE IS FROM A LIST AND A BARE PAGE IS THE CHOOSER**, both of
    // them /admin/delegation/application's rules and both for its reasons. The
    // list unions the identity register with the delegation register, so it
    // offers people nothing was ever issued to — somebody an S4U2Self named who
    // has never been near this service is exactly the row worth opening.
    //
    // No form that CHANGES anything, so no operation on /admin-api — rule 7 as
    // the other three drill-downs satisfy it. `?format=json` carries the
    // person, their credentials, their acts and the graph; `?format=svg` is the
    // picture alone.
    // -------------------------------------------------------------------------
    app.get('/admin/delegation/user', function (req, res) {
      log.debug("Entering the admin delegation user page.");
      const asked = String(req.query.user || '').trim();
      // Normalised the way the identity register normalises one, so a link
      // carrying `alice@STS.MOCK` — which is what a reader will paste out of
      // the acts table — finds the same person the chooser's own link does. It
      // is the same normalisation /admin/users applies to its own `?user=`.
      const key = stats.identityKeyOf(asked);
      const known = knownUserKeys();
      const catalogue = userGraph.userList();
      const activity = key ? userGraph.activityFor(key) : null;
      // An empty graph rather than none when nobody is selected: `?format=svg`
      // is answered before the chooser is drawn, and a route that answered HTML
      // there would send a browser a diagram it asked for as an image.
      const graph = activity ? activity.graph : delegation.graph([]);
      const look = self.delegationLooks(graph, known);

      // WHAT THE ISSUANCE HALF ADDS TO A TOOLTIP. `delegationNodeLook()` is the
      // one answer to "what is this box" and must stay that way — it is what
      // keeps this page, the map and the two other drill-downs drawing one
      // party one way — so the credentials are APPENDED to what it said rather
      // than folded into it.
      graph.nodes.forEach(function (node) {
        const entry = look.looks[node.id];
        if (!entry || node.kind === 'sts') {
          return;
        }
        // A CLIENT IS DRAWN AS AN APPLICATION, and only where neither store has
        // an opinion. `delegationNodeLook()`'s fallback is the shape the ROLE
        // implies and the subject of this page is an initial identity, so a
        // `client_credentials` client — which is a client BY ITS OWN SAYING, at
        // the one funnel that can know — was coming out as a stick figure.
        // Where the directory or the registry DOES know it, that answer stands:
        // the fallback is what is being corrected, not the stores.
        if (node.isClient && entry.dashed) {
          entry.shape = 'application';
          entry.sublabel = 'a client, not a person';
        }
        const extra = [];
        if (node.isSubject) {
          extra.push('THIS IS THE PERSON THIS PAGE IS ABOUT.');
        }
        if (node.isClient) {
          extra.push('It is a CLIENT rather than a person: something ' +
                     'authenticated under this name and said the client is ' +
                     'the identity, which the client_credentials grant is ' +
                     'the usual way of doing.');
        }
        if (node.credentials) {
          extra.push(node.credentials + ' credential(s) issued' +
                     (node.isSubject ? ' naming them' : ' to it') +
                     (node.kinds.length ? ': ' + node.kinds.join(', ') : '') +
                     '.');
        }
        if (node.flows.length) {
          extra.push('By: ' + node.flows.join(', ') + '.');
        }
        if (node.authentications) {
          extra.push(node.authentications + ' authentication(s) here.');
        }
        if (extra.length) {
          entry.title = entry.title + '\n' + extra.join('\n');
        }
      });

      const drawingLabel = activity
        ? 'Everything issued in the name of ' + activity.key
        : 'People and the credentials issued in their name';

      if (String(req.query.format || '') === 'svg') {
        self.sendDelegationSvg(res, graph, look, drawingLabel);
        log.debug("Leaving the admin delegation user page. Answered SVG.");
        return;
      }

      const listView = self.listViewOf('/admin/delegation', req.query);
      const up = self.upTo('/admin/delegation', 'One person', listView);
      const back = self.note('<a class="btn" href="' + self.esc(up.href) +
        '">&larr; Back to the delegation table</a>');
      const chooser = self.delegationUserChooser(catalogue, key, listView,
          { path: '/admin/delegation/user', query: req.query });

      if (!activity) {
        const inner = self.messagesOf(req) + back +
          (asked
            ? self.note('<strong>Neither register names ' +
              '<code>' + self.esc(asked) + '</code>.</strong> Three things ' +
              'could be true and they are different: nothing has ever ' +
              'authenticated, been issued anything or been delegated under ' +
              'that name; something did and the records have been DROPPED, ' +
              'because each of these stores keeps a bounded number and ' +
              'discards the oldest; or the name is spelled differently from ' +
              'the way a protocol presented it. The list below is every ' +
              'identity either register actually holds, which settles the ' +
              'third.')
            : '') +
          self.note('<strong>Choose a person to see everything this service ' +
          'has done in their name.</strong> Not just the delegations — this ' +
          'is the one picture here that also draws the ordinary issuance: ' +
          'every OAuth 2.0 grant and OIDC flow, every SAML assertion, every ' +
          'Kerberos ticket and every SVID, each labelled with exactly what ' +
          'produced it, beside the applications that hold them and the ' +
          'sign-ins the whole lot rests on.') +
          chooser +
          self.delegationUserTable(catalogue, known, listView) +
          self.note('The list is the identity register and the delegation ' +
          'register UNIONED, which is why a row can say <em>never ' +
          'authenticated here</em>: a delegation names somebody who was not ' +
          'present and proved nothing — that is what S4U2Self and ' +
          '<code>OnBehalfOf</code> ARE — so their name exists here and in no ' +
          'other list this console keeps.');
        self.respond(req, res,
                     { user: null, asked: asked || null, users: catalogue },
                     'Delegation — one person', '/admin/delegation', inner, up);
        log.debug("Leaving the admin delegation user page. " +
                  (asked ? "Nothing named that." : "Drew the chooser."));
        return;
      }

      const parties = graph.nodes.filter(function (node) {
        return node.kind !== 'sts';
      });
      const picture = self.delegationDrawing(graph, look,
                                             '/admin/delegation/user',
                                             Object.assign(
                                               {}, listView,
                                               { user: activity.key }),
                                             drawingLabel);
      const entry = activity.entry;

      const inner = self.messagesOf(req) + back +
        '<div class="tiles">' +
          self.tile(activity.counts.credentials, 'credentials issued') +
          self.tile(activity.counts.authentications, 'sign-ins') +
          self.tile(activity.flows.length, 'grants used') +
          self.tile(activity.counts.applications, 'other parties') +
          self.tile(activity.counts.acts, 'delegation acts') +
          self.tile(activity.counts.chains, 'delegation relationships') +
        '</div>' +

        self.note('<strong><code>' + self.esc(activity.key) +
                  '</code></strong> — ' +
        (entry.authenticated
          ? 'they have <a href="' + self.esc('/admin/users' +
              queryWith({ user: activity.key }, {})) + '">authenticated ' +
                'here</a> ' +
            self.esc(entry.authentications) + ' time(s)'
          : '<span class="state-expired" title="Nothing has ever presented a ' +
            'credential under this name in this process. Something was ' +
            'issued in it, or somebody delegated using it — which is exactly ' +
            'the state this page exists to make visible.">they have NEVER ' +
            'authenticated here</span>') +
        (entry.isClient
          ? '. It is a <strong>client rather than a person</strong>: ' +
            'something authenticated under this name and said so, which the ' +
            '<code>client_credentials</code> grant is the usual way of doing'
          : '') +
        (entry.forms.length > 1
          ? '. They have been spelled ' + entry.forms.length + ' ways and ' +
            'they are one person here: ' + self.codeList(entry.forms) +
            '. Two spellings of one identity is two people, so the console ' +
            'collapses them on the same normalisation the picture uses'
          : '') +
        '. Protocols: ' +
        (entry.protocols.length ? self.codeList(entry.protocols) : 'none') +
        '. Last seen ' + self.esc(self.whenText(entry.lastAt)) + '.') +

        '<h2>Everything, as one picture</h2>' +
        self.note('<strong>This is the page.</strong> The dotted line into ' +
        'the hexagon is them SIGNING IN, and it is why anything else here ' +
        'was allowed. Every solid indigo line is a credential issued NAMING ' +
        'them, labelled with the exact grant or flow that produced it: out ' +
        'of THEM it went to that application, out of an APPLICATION it is ' +
        'the resource that application may reach with it, and out of the ' +
        'HEXAGON nobody else holds it — a <code>client_credentials</code> ' +
        'token is about the client itself and an X509-SVID has no audience. ' +
        'The amber and green lines, where there are any, are delegations — ' +
        'somebody acting on their behalf — and they are the only lines here ' +
        'that carry a mode, because impersonation and delegation are ' +
        'properties of a delegation mechanism and an ordinary grant makes ' +
        'neither claim.') +
        picture.html +

        '<h2>The key</h2>' +
        self.note('The shapes are drawn by the same functions the picture ' +
        'uses, so a legend cannot come to describe a diagram this service no ' +
        'longer draws. The last three rows are this page\'s own — no other ' +
        'picture in this console has a line for an ordinary grant, because ' +
        'no other picture is drawn from anything but the delegation ' +
        'register.') +
        self.delegationMapKey({ issuance: true }) +

        '<h2>What was used to get a credential</h2>' +
        self.note('<strong>Exactly which OAuth 2.0 grant or OpenID Connect ' +
        'flow, with the section that defines it.</strong> Only the ones this ' +
        'person\'s credentials actually used are here; the rest of the table ' +
        'is on no page, because a list of eight grants under a person who ' +
        'used one is a list nobody reads. A SAML assertion, a Kerberos ' +
        'ticket and an SVID have no grant at all and are not in this table — ' +
        'the credentials below say what their own specifications call the ' +
        'mechanism instead.') +
        (activity.flows.length
          ? '<table><tr><th>Grant</th><th>OpenID Connect calls ' +
            'it</th><th>Specification</th><th>Through a browser</th><th>What ' +
            'it is</th></tr>' +
            activity.flows.map(function (flow) {
              return '<tr>' +
                '<td><code>' + self.esc(flow.flow) + '</code><br>' +
                  '<strong>' + self.esc(flow.label) + '</strong></td>' +
                '<td>' + (flow.oidc ? self.esc(flow.oidc)
                  : '<span class="state-none" title="OpenID Connect defines ' +
                    'no flow of its own for this grant — it is OAuth 2.0\'s, ' +
                    'used as it is.">&mdash;</span>') + '</td>' +
                '<td>' + self.esc(flow.spec) + '</td>' +
                '<td>' + (flow.browser
                  ? '<span class="state-valid" title="The person was at an ' +
                    'authorization endpoint in a browser, so this issuance ' +
                    'can be put under a sign-on session.">yes</span>'
                  : '<span class="state-none" title="A direct grant: there ' +
                    'is no browser anywhere in it, which is why its ' +
                    'credentials are listed with no session.">no</span>') +
                    '</td>' +
                '<td>' + self.esc(flow.what) +
                  (flow.delegating
                    ? ' <strong>It is also a delegation act</strong>, so its ' +
                      'credentials are drawn on the delegation line rather ' +
                      'than twice.'
                    : '') + '</td>' +
                '</tr>';
            }).join('') + '</table>'
          : self.note('No credential of theirs states a grant. That is the ' +
            'ordinary state for somebody who has only ever been issued ' +
            'assertions, tickets or SVIDs — none of those protocols has a ' +
            'grant — and for a JWT minted outside the token endpoint.')) +

        '<h2>Every credential issued in their name</h2>' +
        self.note('<strong>The issued register: every JWT, assertion, ' +
        'ticket, SVID and verifiable credential this service has minted ' +
        'naming them, newest first.</strong> <strong>NO CREDENTIAL IS EVER ' +
        'HERE, only its kind and its identifier</strong> — the rule the ' +
        'audit log follows — and a Kerberos ticket genuinely has none to ' +
        'quote. <em>Went to</em> is the application that holds it: a ' +
        'token\'s <code>client_id</code>, an assertion\'s audience, the ' +
        'service principal a ticket was cut for. Nothing holds an X509-SVID, ' +
        'which is why some rows have none.' +
        (activity.onDelegationLines
          ? ' <strong>' + self.esc(activity.onDelegationLines) + ' more are ' +
            'not in this table</strong> and are not missing: a token ' +
            'exchange writes a row in BOTH registers for one credential, so ' +
            'those are listed under the delegation acts below, where the row ' +
            'says more — it names the actor and whether the far end can see ' +
            'them.'
          : '')) +
        '<table><tr><th>When</th><th>Credential</th><th>What issued it</th>' +
        '<th>Went to</th><th>State</th><th>Session</th></tr>' +
        (activity.credentials.map(function (credential) {
          return self.userCredentialRow(credential, known);
        }).join('') ||
          '<tr><td colspan="6">Nothing has been issued naming them. ' +
          'For somebody only a delegation names — an S4U2Self subject, an ' +
          '<code>OnBehalfOf</code> — that is the expected state and not a ' +
          'broken one.</td></tr>') + '</table>' +
        self.note('The same rows with their revoke buttons, grouped by the ' +
        'sign-on session each was issued on, are on <a href="' +
        self.esc('/admin/users' + queryWith({ user: activity.key }, {})) +
        '">their page in the identity register</a>. This page draws the ' +
        'RELATIONSHIPS; that one is where a token is acted on.') +

        '<h2>The parties</h2>' +
        self.note('Every box in the picture above, including them. A box ' +
        'with credentials and no delegation roles is an ordinary client — it ' +
        'has never been part of a delegation, which is a fact about the ' +
        'other register rather than a gap here.') +
        '<table><tr><th>Label</th><th>Drawn as</th><th>Identity</th>' +
        '<th>Credentials</th><th>By</th><th>Delegation ' +
        'roles</th><th>Acts</th><th>Protocols</th></tr>' +
        (parties.map(function (node) {
          return self.userNodeRow(node, known, look.looks[node.id]);
        }).join('') || '<tr><td colspan="8">No parties.</td></tr>') +
          '</table>' +

        '<h2>Every line, in words</h2>' +
        self.note('The picture read as a table, because a diagram nobody can ' +
        'quote is a diagram nobody can put in a bug report. <strong>Acts and ' +
        'credentials are different units</strong> and have their own ' +
        'columns: an act is one delegation exchange, a credential is one ' +
        'thing that came out of the issued register, and a line can carry ' +
        'either, both or — a refused delegation — acts and nothing else.') +
        '<table><tr><th>From</th><th>To</th><th>Relationship</th><th>' +
        'Mechanism ' +
        'or grant</th><th>Kind</th><th>Acts</th><th>Credentials</th><th>What ' +
        'came out</th></tr>' +
        (graph.edges.map(function (edge) {
          return self.userEdgeRow(edge, look.labelOf);
        }).join('') || '<tr><td colspan="8">No lines.</td></tr>') + '</table>' +

        (activity.acts.length
          ? '<h2>Every delegation act naming them</h2>' +
            self.note('The rows <a href="' + self.esc(up.href) +
                      '">the delegation ' +
            'table</a> holds, narrowed to this person and not paged — in ANY ' +
            'of the three roles, because the whole reason to look somebody ' +
            'up in a delegation register is that their name appears in ' +
            'exchanges they were never present for. This is where the ' +
            'REFUSALS are: a refusal produced no credential, so it is in ' +
            'this table and in none of the ones above.') +
            '<table><tr><th class="num">#</th><th>When</th><th>Mechanism</th>' +
            '<th>Kind</th><th>Outcome</th><th>Initial identity</th>' +
            '<th>Intermediary</th><th>Target</th><th>Authorized by / why ' +
            'not</th><th>Credentials</th></tr>' +
            activity.acts.map(function (row) {
              return self.delegationRow(row, known, { listView: listView });
            }).join('') + '</table>'
          : '<h2>Delegation</h2>' +
            self.note('No delegation act names them, in any role. Everything ' +
            'above was issued to them directly — which is the ordinary ' +
            'state, since three of the sixteen families here can delegate at ' +
            'all.')) +

        '<h2>Somebody else</h2>' + chooser +
        self.delegationUserTable(catalogue, known, listView) +

        self.note('<code>?format=json</code> carries this person, their ' +
        'credentials with the grant on each, their acts and the graph behind ' +
        'the picture; <code>?format=svg</code> is the document alone. There ' +
        'is no form that changes anything on this page and therefore no ' +
        'operation on <code>/admin-api</code> — the acts are in <code>GET ' +
        '/admin-api/delegation</code> and the tokens in <code>GET ' +
        '/admin-api/users</code>.');

      self.respond(req, res, {
        user: entry,
        asked: asked,
        // The whole model, so a test can assert what the page draws without
        // parsing an SVG — the property /admin/delegation/map's header insists
        // on, kept here for the same reason.
        credentials: activity.credentials,
        flows: activity.flows,
        onDelegationLines: activity.onDelegationLines,
        acts: activity.acts,
        graph: graph,
        counts: activity.counts,
        users: catalogue,
        drawing: { width: picture.drawn.width, height: picture.drawn.height,
                   failed: picture.drawn.failed || null }
      }, 'Delegation — one person', '/admin/delegation', inner, up);
      log.debug("Leaving the admin delegation user page. " +
                activity.credentials.length + " credential(s), " +
                activity.acts.length + " act(s).");
    });

    app.get('/admin/users', function (req, res) {
      log.debug("Entering the admin users page.");
      const view = self.usersView(req);
      self.respond(req, res, view.json, view.title, '/admin/users', view.inner,
                   view.up);
      log.debug("Leaving the admin users page. " + view.title + ".");
    });

    app.post('/admin/users', function (req, res, next) {
      log.debug("Entering the admin users action endpoint.");
      const body = parseBody(req);
      // A CREATE CLAIMS ITS NAME FIRST where several processes write one store
      // — `ldap/directory_create_claims.ts`. Everywhere else `runClaimed()`
      // runs the body below synchronously, exactly as before.
      const creating = String(body.action || '') === 'create'
        ? { username: String(body.username || body.user || '') } : null;
      createClaims.runClaimed(creating, function (held) {
        self.usersPost(req, res, body, held);
      }, function (held) {
        errorCodes.mark(res, held.code);
        self.respondToAction(req, res, '/admin/users' +
          queryWith(self.listViewFromBack('/admin/users', body.back), {}),
          { ok: false, errors: [createClaims.refusalMessage(held)] });
      }, next);
      log.debug("Leaving the admin users action endpoint.");
    });

    app.get('/admin/users/new', function (req, res) {
      log.debug("Entering the admin new-user page.");
      const view = self.newUserView(req);
      self.respond(req, res, view.json, view.title, '/admin/users', view.inner,
                   self.newUserUp(view.title));
      log.debug("Leaving the admin new-user page.");
    });

    app.post('/admin/users/new', function (req, res, next) {
      log.debug("Entering the admin new-user action endpoint.");
      const body = parseBody(req);
      // FILL IS READ FIRST AND NOT AS A VALUE OF `action`. See the two buttons
      // in the form above: `action` is a hidden field that always says
      // `create`, and the Fill button carries a name of its own — so a press of
      // it arrives with both, and reading `fill` first is what decides between
      // them. The other way round would create somebody every time that button
      // was pressed.
      const action = truthy(body.fill) ? 'fill' :
                     String(body.action || 'create');
      const posted = { username: String(body.username || ''),
                       credential: String(body.credential || 'none'),
                       fields: userFieldsFrom(body) };

      // A JSON caller gets JSON, exactly as every other action on this console
      // does. It is /admin-api that a script should be driving, and this branch
      // is here so that a script pointed at the page does not get a wall of
      // HTML back with the secret buried in it.
      const wantsJson = /json/i.test(String(req.headers['content-type'] || ''));

      // ------------------------------------------------------------------
      // FILL. It creates nothing and answers with this same form, filled in.
      //
      // **ON THE SERVER BECAUSE THIS CONSOLE HAS NO JAVASCRIPT.** `app.js` sets
      // `script-src 'none'` for the whole service and seven pages are the only
      // exceptions, each of which CANNOT work without a script; this one
      // plainly can, at the cost of a round trip. It is the same answer the
      // XACML guided editor gives to the same question, and CLAUDE.md's rule is
      // that a new scripted page needs its argument made from scratch — "a
      // field could be filled in without a reload" is not one.
      // ------------------------------------------------------------------
      if (action === 'fill') {
        if (!mode.isDevelopment()) {
          // REFUSED AS WELL AS UNDRAWN. The button is not on the page in
          // product mode, and a POST that arrives anyway is answered rather
          // than obeyed — a control that is only hidden is not a control that
          // is off.
          const refusal = { ok: false,
                            errors: ['Example data is a development-mode ' +
                                     'convenience and this service is ' +
                                     'running as a product ' +
                                     '(global.mode=product), so nothing was ' +
                                     'filled in. Type what you know.'] };
          errorCodes.mark(res, 'STS-ADMIN-0016');
          if (wantsJson) {
            self.respondToAction(req, res, '/admin/users/new', refusal);
            log.debug("Leaving the admin new-user action endpoint. Fill " +
                      "refused.");
            return;
          }
          const view = self.newUserPage(req, posted);
          self.respond(req, res, Object.assign({ filled: false }, view.json),
                       'New user',
                       '/admin/users',
                       self.warn(self.esc(refusal.errors[0])) + view.inner,
                       self.newUserUp());
          log.debug("Leaving the admin new-user action endpoint. Fill " +
                    "refused.");
          return;
        }
        if (!posted.username.trim()) {
          errorCodes.mark(res, 'STS-ADMIN-0017');
          const view = self.newUserPage(req, posted);
          self.respond(req, res, Object.assign({ filled: false }, view.json),
                       'New user',
                       '/admin/users',
                       self.warn('<strong>Type a username first.</strong> ' +
                       'The example person is seeded FROM the username — ' +
                       'that is what makes it the same person this service ' +
                       'would have invented — so there is nothing to invent ' +
                       'until there is a name to invent it from.') + view.inner,
                       self.newUserUp());
          log.debug("Leaving the admin new-user action endpoint. Fill needs " +
                    "a name.");
          return;
        }
        // ABSENT-ONLY, which is `applyVcAttributes()`'s first rule applied to a
        // form: what somebody has already typed is never overwritten by an
        // invention, so pressing Fill twice, or after editing, cannot lose
        // work.
        const invented = self.inventedFieldValues(posted.username);
        const merged = {};
        Object.keys(invented)
              .forEach(function (name) { merged[name] = invented[name]; });
        Object.keys(posted.fields)
              .forEach(function (name) { merged[name] = posted.fields[name]; });
        const filled = Object.assign({}, posted, { fields: merged });
        const view = self.newUserPage(req, filled);
        self.respond(req, res,
                     Object.assign({ filled: true, values: merged }, view.json),
                     'New user', '/admin/users',
                     '<div class="ok"><strong>Filled in with the invented ' +
                     'person for ' +
                     self.esc(posted.username.trim()) + '.</strong> Nobody ' +
                     'has been created. Edit or clear anything you like and ' +
                     'press Create the user.</div>' +
                     view.inner, self.newUserUp());
        log.debug("Leaving the admin new-user action endpoint. Filled.");
        return;
      }

      if (action !== 'create') {
        const refusal = { ok: false,
                          errors: ['Unknown action "' +
                                   String(action).slice(0, 64) +
                                   '". This page does two: create, fill.'] };
        errorCodes.mark(res, 'STS-ADMIN-0018');
        if (wantsJson) {
          self.respondToAction(req, res, '/admin/users/new', refusal);
        } else {
          const view = self.newUserPage(req, posted);
          self.respond(req, res, view.json, 'New user', '/admin/users',
                       self.warn(self.esc(refusal.errors[0])) + view.inner,
                       self.newUserUp());
        }
        log.debug("Leaving the admin new-user action endpoint. Unknown " +
                  "action.");
        return;
      }

      // WHO IS DOING IT, off the gate's own state rather than out of the body:
      // the audit row for a create and for a password set names the
      // administrator, and a value taken from the form would be whatever the
      // poster typed. The same way `/admin/delegation` and `/admin/sessions`
      // name their actor.
      createClaims.runClaimed({ username: posted.username }, function (held) {
        self.newUserCreate(req, res, body, posted, wantsJson, held);
      }, function (held) {
        errorCodes.mark(res, held.code);
        const refusal = { ok: false,
                          errors: [createClaims.refusalMessage(held)] };
        if (wantsJson) {
          self.respondToAction(req, res, '/admin/users/new', refusal);
          return;
        }
        const view = self.newUserPage(req, posted);
        self.respond(req, res, Object.assign({ created: false,
                                               errors: refusal.errors },
                                               view.json),
                     'New user', '/admin/users',
                     self.warn('<strong>Nobody was created.</strong> ' +
                               self.esc(refusal.errors[0])) + view.inner,
                               self.newUserUp());
      }, next);
      log.debug("Leaving the admin new-user action endpoint. Create.");
    });

    app.post('/admin/applications', function (req, res) {
      log.debug("Entering the admin applications action endpoint.");
      const body = parseBody(req);
      // TWO SPELLINGS OF THE DECLARED PROTOCOL LIST, joined, exactly as
      // /admin/claims takes its two and for the same reason: the create form's
      // checkbox column is one `protocol` field repeated, and a JSON body
      // carries one `protocols` array. helpers.parseBody() cannot see a repeat
      // — it builds a plain object, so the last box ticked would be the only
      // one to arrive — which is what listField() exists for.
      const protocols = self.listField(req, body, 'protocol').concat(
          self.listField(req, body, 'protocols'));
      // The realm's authorization servers, for `load-resource-metadata` — the
      // one action that compares something against the address this request
      // arrived on, and which every other action ignores.
      const result = applicationsAction(body, protocols, {
        authorizationServers: resourceMetadata.authorizationServersOf(req),
        // The address this request arrived on, for `issue-software-statement`,
        // whose statement names the issuer published there.
        base: baseUrlOf(req)
      });
      // `refresh-metadata` answers with a promise; everything else answers with
      // the object. Resolved here rather than making the whole function async,
      // for the reason that action's own comment gives.
      if (result && typeof result.then === 'function') {
        result.then(function (answer) {
          // A TLS CLIENT CERTIFICATE THAT WAS ISSUED (RFC 8705, 2026-09-13) is
          // answered with a PAGE and never a redirect: the reply carries the
          // only copy of the private key, and a redirect carries a message on a
          // query string. `/admin/pki/person`'s arrangement. A refusal
          // redirects like every other action, because it carries no key.
          if (answer && answer.ok && answer.files &&
              String(body.action || '') === 'issue-tls-client-certificate') {
            self.answerIssuedTlsClientCertificate(req, res, body, answer);
            return;
          }
          self.respondToApplicationAction(req, res, body, answer);
        }).catch(function (e) {
          log.error(errorCodes.tag('STS-ADMIN-0724') + 'admin: the ' +
                    'applications action "' + String(body.action || '') +
                    '" failed: ' +
                    (e && e.stack ? e.stack : e));
          errorCodes.mark(res, 'STS-ADMIN-0724');
          self.respondToApplicationAction(req, res, body, { ok: false,
            errors: ['That action failed: ' +
                     (e && e.message ? e.message : e)] });
        });
        log.debug("Leaving the admin applications action endpoint. Awaiting " +
                  "a metadata fetch.");
        return;
      }
      self.respondToApplicationAction(req, res, body, result);
    });

    app.get('/admin/applications', function (req, res) {
      log.debug("Entering the admin applications page.");
      const view = self.applicationsView(req);
      if (view.missing) {
        errorCodes.mark(res, 'STS-ADMIN-0021');
      }
      self.respond(req, res, view.json, view.title, '/admin/applications',
                   view.inner,
                   view.up);
      log.debug("Leaving the admin applications page. " + view.title + ".");
    });

    // This page has no row in `SECTIONS` — the argument is beside the
    // Applications row up there — so it is a DRILL-DOWN of
    // `/admin/applications`: `active` is the SECTION's path, which marks that
    // tab, and `up` turns it into a link and gives the trail its third crumb
    // (`Admin console › Applications › New application`).
    //
    // **THE FORM WITH NO DOCUMENT LOADED POSTS TO `/admin/applications`** and
    // that handler answers with the list page's 303 and a message, so a refusal
    // and a success of the ordinary create are drawn over there. The RFC 9728
    // import is the exception and has a POST of its own below, because what it
    // has to keep across a refusal — a loaded document and every edit made to
    // it — is not something a 303 can carry.
    //
    // It carries NO LIST VIEW. The button that opens it is on the list page and
    // does not forward the filter (there is nothing typed here to come back
    // to), so `upTo()` reports `filtered:false` and the crumb's tooltip says
    // `Back to Applications` rather than promising a filter that was never
    // here.
    app.get('/admin/applications/new', function (req, res) {
      log.debug("Entering the admin new-application page.");
      const view = self.newApplicationView(req);
      self.respond(req, res, view.json, view.title, '/admin/applications',
                   view.inner,
                   self.upTo('/admin/applications', view.title));
      log.debug("Leaving the admin new-application page.");
    });

    app.post('/admin/applications/new', function (req, res) {
      log.debug("Entering the admin new-application action endpoint.");
      const body = parseBody(req);
      const action = String(body.action || '');
      const wantsJson = /json/i.test(String(req.headers['content-type'] || ''));
      const context = {
        authorizationServers: resourceMetadata.authorizationServersOf(req)
      };

      if (action === 'load-resource-metadata') {
        // THE FILE, with its name, off the multipart parts. `parseBody()` has
        // put its content under `file` as text already; this adds the filename
        // the page names it by, and ignores a file input left empty — a browser
        // posts one with no filename and no bytes, which is not a document.
        const input = Object.assign({}, body);
        delete input.file;
        const upload = multipartParts(req).filter(function (part) {
          return part.name === 'file' && part.filename && part.data.length;
        })[0];
        if (upload) {
          input.file = { name: upload.filename,
                         text: upload.data.toString('utf8') };
        }
        applicationsAction(input, [], context).then(function (result) {
          if (wantsJson) {
            self.respondToAction(req, res, '/admin/applications/new', result);
            log.debug("Leaving the admin new-application action endpoint. " +
                      "Answered JSON.");
            return;
          }
          if (!result.ok) {
            self.newApplicationRedraw(req, res, {
              loadError: true, error: result.errors,
              errorTitle: 'The protected resource metadata was not loaded.',
              loadInput: { document: body.document, url: body.url }
            }, errorCodes.codeOf(result) || 'STS-ADMIN-0644');
            log.debug("Leaving the admin new-application action endpoint. " +
                      "The document was refused.");
            return;
          }
          self.newApplicationRedraw(req, res, { loaded: result,
                                                loadInput:
                                                  { url: result.url } });
          log.debug("Leaving the admin new-application action endpoint. " +
                    "Loaded.");
        });
        return;
      }

      const protocols = self.listField(req, body, 'protocol').concat(
          self.listField(req, body, 'protocols'));
      // GENERATE SECRET (2026-09-18): the whole create form, posted here by
      // the button's `formaction`, drawn again with a new secret in
      // `oauthClientSecret` and every other box as it was. The secret comes
      // from the same `generate-secret` action `/admin-api` answers, and
      // nothing is written. With an RFC 9728 document loaded, the pane is
      // read again from the ORIGINAL document, as a refused create's is. A
      // JSON caller falls through and gets the action's reply.
      if (action === 'generate-secret' && !wantsJson) {
        const minted = applicationsAction(body, protocols, context);
        const draft = Object.assign({}, body, {
          'field.oauthClientSecret': minted.clientSecret
        });
        let loaded = null;
        if (body.metadata) {
          const again = resourceMetadata.analyse(String(body.metadata), {
            source: String(body.metadataSource || 'pasted'),
            url: String(body['field.oauthResourceMetadataUrl'] || ''),
            filename: String(body.metadataFilename || '')
          }, context);
          loaded = again.ok ? again : null;
        }
        self.newApplicationRedraw(req, res, {
          loaded: loaded, draft: draft, protocols: protocols,
          tab: loaded ? 'fields' : undefined,
          notice: 'A client secret was generated and is in the ' +
                  'oauthClientSecret box below. Nothing has been written: ' +
                  'it becomes this application\'s credential when you ' +
                  'create it. With OAuth 2.0 or OpenID Connect ticked, the ' +
                  'create records client_secret_basic as its token endpoint ' +
                  'authentication method, and the token endpoint accepts ' +
                  'the secret either in an Authorization: Basic header ' +
                  '(client_secret_basic) or as a client_secret form ' +
                  'parameter (client_secret_post).'
        });
        log.debug("Leaving the admin new-application action endpoint. " +
                  "A secret was generated.");
        return;
      }
      if (action === 'create' && body.metadata) {
        // The document the third tab left, into the attribute the create
        // writes.
        const stored = resourceMetadata.documentFromForm(body);
        if (stored) {
          body['field.oauthResourceMetadata'] = stored;
        }
      }
      const result = applicationsAction(body, protocols, context);
      const answer = function (outcome) {
        log.debug("Entering answer().");
        if (action === 'create' && body.metadata && !outcome.ok && !wantsJson) {
          // REDRAWN, with the ORIGINAL document read again for the pane — never
          // fetched again — and the posted values in every box.
          const loaded = resourceMetadata.analyse(String(body.metadata), {
            source: String(body.metadataSource || 'pasted'),
            url: String(body['field.oauthResourceMetadataUrl'] || ''),
            filename: String(body.metadataFilename || '')
          }, context);
          if (loaded.ok) {
            self.newApplicationRedraw(req, res, {
              loaded: loaded, draft: body, protocols: protocols, tab: 'fields',
              error: outcome.errors,
              errorTitle: 'The application was not created.'
            }, errorCodes.codeOf(outcome) || 'STS-ADMIN-0645');
            log.debug("Leaving answer(). Redrawn.");
            return;
          }
        }
        self.respondToApplicationAction(req, res, body, outcome);
        log.debug("Leaving answer().");
      };
      if (result && typeof result.then === 'function') {
        result.then(answer);
        log.debug("Leaving the admin new-application action endpoint. " +
                  "Awaiting.");
        return;
      }
      answer(result);
      log.debug("Leaving the admin new-application action endpoint.");
    });

    app.get('/admin/authorization-servers', function (req, res) {
      log.debug("Entering the admin authorization servers page.");
      const view = self.authorizationServersView(req);
      if (view.missing) {
        errorCodes.mark(res, 'STS-ADMIN-0021');
      }
      self.respond(req, res, view.json, view.title,
                   '/admin/authorization-servers',
                   view.inner,
                   view.up);
      log.debug("Leaving the admin authorization servers page.");
    });

    app.post('/admin/authorization-servers', function (req, res) {
      log.debug("Entering the admin authorization servers action endpoint.");
      const body = parseBody(req);
      const result = asAction(body);
      const id = String(body.profile || body.id || '').trim();
      // The list state the form carried, for the reason the applications action
      // gives.
      const listView = self.listViewFromBack('/admin/authorization-servers',
                                             body.back);
      const back = id && result.ok !== false
        ? '/admin/authorization-servers' + queryWith(listView, { profile: id })
        : '/admin/authorization-servers' + queryWith(listView, {});
      self.respondToAction(req, res, back, result);
      log.debug("Leaving the admin authorization servers action endpoint.");
    });

    app.get('/admin/saml2', function (req, res) {
      log.debug("Entering the admin SAML 2.0 page.");
      const view = self.saml2View(req);
      self.respond(req, res, view.json, view.title, '/admin/saml2', view.inner,
                   view.up);
      log.debug("Leaving the admin SAML 2.0 page.");
    });

    app.post('/admin/saml2', function (req, res) {
      log.debug("Entering the admin SAML 2.0 action endpoint.");
      const body = parseBody(req);
      const identifier = String(body.sp || body.serviceProvider || '').trim();
      // The list state the form carried, REBUILT rather than echoed — see
      // listViewFromBack(), and the note in admin-ui/CLAUDE.md about a new form
      // on a drill-down needing `carryBack` in it.
      const listView = self.listViewFromBack('/admin/saml2', body.back);
      const answer = function (result) {
        log.debug("Entering answer().");
        const back = identifier && result.ok !== false
          ? '/admin/saml2' + queryWith(listView, { sp: identifier })
          : '/admin/saml2' + queryWith(listView, {});
        self.respondToAction(req, res, back, result);
        log.debug("Leaving answer().");
      };
      // `refresh-metadata` and `mdq-import` dial out and answer with a
      // promise (#37 follow-up); everything else answers with the object.
      Promise.resolve(saml2Action(body)).then(answer, function (e) {
        log.error(errorCodes.tag('STS-ADMIN-0724') + 'admin: the SAML 2.0 ' +
                  'action "' + String(body.action || '') + '" failed: ' +
                  ((e && e.message) || e));
        errorCodes.mark(res, 'STS-ADMIN-0724');
        answer({ ok: false, errors: ['The action failed: ' +
                                     ((e && e.message) || e)] });
      });
      log.debug("Leaving the admin SAML 2.0 action endpoint.");
    });

    app.get('/admin/saml11', function (req, res) {
      log.debug("Entering the admin SAML 1.1 page.");
      const view = self.saml11View(req);
      self.respond(req, res, view.json, view.title, '/admin/saml11', view.inner,
                   view.up);
      log.debug("Leaving the admin SAML 1.1 page.");
    });

    app.post('/admin/saml11', function (req, res) {
      log.debug("Entering the admin SAML 1.1 action endpoint.");
      const body = parseBody(req);
      const result = saml11Action(body);
      const identifier = String(body.rp || body.relyingParty || '').trim();
      // The list state the form carried, REBUILT rather than echoed — see
      // listViewFromBack(), and the note in admin-ui/CLAUDE.md about a new form
      // on a drill-down needing `carryBack` in it.
      const listView = self.listViewFromBack('/admin/saml11', body.back);
      const back = identifier && result.ok !== false
        ? '/admin/saml11' + queryWith(listView, { rp: identifier })
        : '/admin/saml11' + queryWith(listView, {});
      self.respondToAction(req, res, back, result);
      log.debug("Leaving the admin SAML 1.1 action endpoint.");
    });

    app.get('/admin/groups', function (req, res) {
      log.debug("Entering the admin groups page.");
      const view = self.groupsView(req);
      self.respond(req, res, view.json, view.title, '/admin/groups', view.inner,
                   view.up);
      log.debug("Leaving the admin groups page. " + view.title + ".");
    });

    // ONE ROUTE FOR BOTH CONTROLS, and the action switch is in groupsAction()
    // rather than here so that POST /admin-api/groups/{action} makes the same
    // choice. The two forms live on different pages — Create on the list, Add
    // member on the drill-down — and both post here, which is why the redirect
    // below has to work out which of the two to go back to.
    app.post('/admin/groups', function (req, res, next) {
      log.debug("Entering the admin groups action endpoint.");
      const body = parseBody(req);
      // A CREATE CLAIMS ITS NAME FIRST where several processes write one store
      // — `ldap/directory_create_claims.ts`; synchronous everywhere else.
      const creating = String(body.action || '') === 'create'
        ? { group: String(body.group || body.displayName || body.cn || '') }
        : null;
      createClaims.runClaimed(creating, function (held) {
        self.groupsPost(req, res, body, held);
      }, function (held) {
        errorCodes.mark(res, held.code);
        self.respondToAction(req, res, '/admin/groups' +
          queryWith(self.listViewFromBack('/admin/groups', body.back), {}),
          { ok: false, errors: [createClaims.refusalMessage(held)] });
      }, next);
      log.debug("Leaving the admin groups action endpoint.");
    });

    app.get('/admin/consent', function (req, res) {
      log.debug("Entering the admin consent page.");
      // ONE PAGE OF EACH HALF, AND THE SAME ONE THE API ANSWERS
      // (2026-09-18). The search and both pagers were computed here, and
      // `GET /admin-api/consent` answered the whole register — so the one
      // door that grew without a bound was the one a script reads.
      // `adminViews.consentPageView()` is both now; what is left here is
      // markup.
      //
      // THE SEARCH IS OVER THE RECORDED HALF ONLY. The overrides table is one
      // row per thing somebody typed; the recorded table grows by one row for
      // every scope every person agrees to. It matches the person, the
      // application OR the scope, because a reader arrives at this page
      // holding exactly one of those three and does not know which column it
      // is in.
      const view = adminViews.consentPageView(req.query);
      const register = view.register;
      const q = view.q;
      const globalPage = view.globalPage;
      const consentPage = view.consentPage;
      const listView = self.listViewOf('/admin/consent', req.query);
      const navParams = pageParamsOf(req.query);
      const globalsNav = self.pageNavPair('/admin/consent', navParams,
                                          globalPage.paging);
      const consentsNav = self.pageNavPair('/admin/consent', navParams,
                                           consentPage.paging);

      const all = applications.list();
      const applicationOptions = all.map(function (row) {
        return '<option value="' + self.esc(row.identifier) + '">' +
               self.esc(row.name && row.name !== row.identifier
                 ? row.name + ' — ' + row.identifier : row.identifier) +
               '</option>';
      }).join('');

      const inner =
        '<h2>Consent</h2>' +
        self.note('<strong>What a person agreed an application may ask for ' +
        'on their behalf.</strong> With <code>oauth2.consentRequired</code> ' +
        'ON &mdash; it is ON by default, and it is the one policy in this ' +
        'service that is &mdash; the authorization endpoint draws a screen ' +
        'the first time a given username signs in to a given ' +
        '<code>client_id</code> for a given scope, and issues nothing until ' +
        'they answer. Allow writes one <code>' + self.esc(register.attribute) +
        '</code> value per scope onto that person\'s own entry under ' +
        '<code>ou=users</code>; Deny returns <code>access_denied</code> to ' +
        'the client and records nothing at all. A delegated permission is ' +
        'recorded by its WHOLE identifier &mdash; ' +
        '<code>https://example.com/write</code> and never the bare ' +
        '<code>write</code> &mdash; because two resources may both expose a ' +
        'permission of the same name and a consent to one must not cover the ' +
        'other.') +
        (register.required ? '' : self.warn('<strong>Nothing is being ' +
          'asked.</strong> <code>oauth2.consentRequired</code> is OFF, so ' +
          'the authorization endpoint issues whatever is requested and this ' +
          'page is a record of what was agreed while it was on. Turning it ' +
          'back on asks again for anything not listed below &mdash; OFF does ' +
          'not mean everybody consented, it means nobody was asked. The ' +
          'switch is on <a href="/admin/oauth2">OAuth 2.0 / OIDC</a>.')) +
        (register.storable ? '' : self.warn('<strong>Nothing can be written ' +
          'down.</strong> This process has no directory installed behind the ' +
          'consent register, so an answer given at the screen is honoured ' +
          'for that one request and forgotten &mdash; every sign-in draws ' +
          'the screen again. That is deliberate rather than a fallback to ' +
          'consenting silently: an agreement that cannot be remembered is ' +
          'one nobody gave.')) +

        '<h3 id="globals">Global consent &mdash; nobody is asked about ' +
        'these</h3>' +
        self.note('<strong>Configuration, not a record.</strong> One value ' +
        'per (application, scope), held as <code>' +
        self.esc(register.globalAttribute) +
        '</code> ' +
        'on the APPLICATION\'s own entry. Everybody who signs in to that ' +
        'application skips the prompt for that scope, and <strong>nothing is ' +
        'written about anybody</strong> &mdash; so removing a row here asks ' +
        'everybody again, including the people who would have said yes. That ' +
        'is the whole difference from the table below it, where removing a ' +
        'row asks one person.<br><br>It is keyed on the PAIR and not on the ' +
        'scope alone: consenting <code>read</code> here consents it for this ' +
        'application, and an application registered five minutes from now ' +
        'that spells the same word is still asked. It is an ordinary ' +
        'attribute on an ordinary entry, so an <code>ldapmodify</code> ' +
        'reaches it exactly as it reaches a redirect URI, and it persists ' +
        'wherever the directory does.') +
        globalsNav.head +
        (globalPage.shown.length
          ? '<table><thead><tr><th>Application</th><th>Scope</th><th>What it ' +
            'is</th><th></th></tr></thead><tbody>' +
            globalPage.shown.map(function (one) {
              return self.globalConsentRow(one, listView);
            }).join('') + '</tbody></table>'
          : self.note('<strong>Nothing is globally consented.</strong> Every ' +
            'person is asked about every scope the first time an application ' +
            'requests it, which is what this service does out of the box.')) +
        globalsNav.foot +

        '<h4>Consent a scope for everybody</h4>' +
        self.note('The scope is written exactly as a client puts it in a ' +
        '<code>scope</code> parameter: <code>openid</code>, ' +
        '<code>profile</code>, or a whole delegated permission identifier ' +
        'such as <code>https://example.com/write</code>. It must be a legal ' +
        'OAuth scope token (RFC 6749 section 3.3: any printable ASCII except ' +
        'space, double quote and backslash), because a value with a space in ' +
        'it is two scopes and could never match one. A scope no application ' +
        'here defines a permission for is accepted rather than refused ' +
        '&mdash; most scopes are not permissions.') +
        '<form method="post" action="/admin/consent">' +
        self.consentBack(listView) +
          '<div class="formrow">' +
          '<input type="hidden" name="action" value="grant-global-consent">' +
          '<label for="gc-client">Application</label>' +
          '<select id="gc-client" name="client">' + applicationOptions +
        '</select><label ' +
          'for="gc-scope">Scope</label><input type="text" id="gc-scope" ' +
          'name="scope" size="30" placeholder="openid"><button ' +
          'type="submit">Consent it for everybody</button></div></form><h3 ' +
          'id="recorded">Recorded consent &mdash; what people actually ' +
          'answered</h3>' +
        self.note('<strong>One row per (person, application, scope), and ' +
        'that IS the answer.</strong> A person who agreed to five scopes for ' +
        'one application is five rows rather than one labelled <em>5</em>, ' +
        'because a client that later asks for a sixth is asked about the ' +
        'sixth alone &mdash; which is what the screen shows and what these ' +
        'rows have to be able to express. The timestamp is when they pressed ' +
        'Allow.<br><br>Revoking a row asks that one person again the next ' +
        'time that one application requests that one scope. It does NOT ' +
        'touch anything already issued: an access token minted before the ' +
        'revoke is still valid, exactly as taking a delegated permission ' +
        'away does not re-judge a grant already made. <a ' +
        'href="/admin/tokens">Tokens</a> is where something already issued ' +
        'is revoked.') +
        self.sectionSearchForm({
          path: '/admin/consent', query: req.query, param: 'q',
          // The list this search narrows, so that a new search starts at page 1
          // rather than at whatever page the reader happened to be on when the
          // list was longer — which reads as "nothing matched".
          pageParam: 'usersPage', label: 'Search',
          placeholder: 'a person, an application or a scope'
        }) +
        consentsNav.head +
        (consentPage.shown.length
          ? '<table><thead><tr><th>Person</th><th>Application</th><th>Scope' +
            '</th>' +
            '<th></th></tr></thead><tbody>' +
            consentPage.shown.map(function (one) {
              return self.recordedConsentRow(one, listView);
            }).join('') + '</tbody></table>'
          : self.note(q
              ? '<strong>Nothing matches &ldquo;' + self.esc(q) +
                '&rdquo;.</strong> ' +
                'The search is over the person, the application and the scope.'
              : '<strong>Nobody has consented anything yet.</strong> That is ' +
                'what a service nobody has signed in to looks like &mdash; ' +
                'and also what one looks like where every scope anybody has ' +
                'asked for is under global consent above, because an ' +
                'override writes nothing down.')) +
        consentsNav.foot +

        '<h4>Forget everything one person agreed to</h4>' +
        self.note('Every recorded consent for one person, in one act, so ' +
        'that they are asked again by every application. It is a separate ' +
        'control rather than a loop over the Revoke buttons above because ' +
        'being asked again is the one thing somebody wants after testing ' +
        'this screen, and doing it a row at a time for a person with thirty ' +
        'consents is a chore rather than a control. It reaches nothing under ' +
        'global consent, because there is nothing on their entry to reach.') +
        '<form method="post" action="/admin/consent">' +
        self.consentBack(listView) +
          '<div class="formrow">' +
          '<input type="hidden" name="action" value="forget-user-consent">' +
          '<label for="fc-username">Person</label>' +
          '<input type="text" id="fc-username" name="username" size="24" ' +
            'placeholder="alice">' +
          '<button type="submit" class="danger">Forget them all</button>' +
        '</div></form>' +

        self.note('<strong>Both halves are ordinary attributes on ordinary ' +
        'directory entries.</strong> <code>' +
        self.esc(register.globalAttribute) + '</code> on ' +
        'an application under <code>ou=applications</code>, <code>' +
        self.esc(register.attribute) + '</code> on a person under ' +
        '<code>ou=users</code> as <code>&lt;when&gt; &lt;scope&gt; ' +
        '&lt;client_id&gt;</code> &mdash; the client_id last, because it is ' +
        'the one field with no rule about what it may contain and therefore ' +
        'has to take the remainder of the value. <code>GET ' +
        '/admin/ldap/directory</code> shows them as they are, and they ' +
        'persist wherever the directory does.');

      self.respond(req, res, view.json, 'Consent', '/admin/consent', inner);
      log.debug("Leaving the admin consent page.");
    });

    app.post('/admin/consent', function (req, res) {
      log.debug("Entering the admin consent action endpoint.");
      const body = parseBody(req);
      // The actor is the person whose session got them through the gate above,
      // read here rather than inside consentAction() for rbacAction()'s reason:
      // the management API calls the same function with an actor of its own,
      // and a function that reached for a cookie would only work from one of
      // them.
      const state = gateStateFor(req);
      const result = consentAction(Object.assign({}, body,
        { actor: (state && state.username) || '' }));
      const back = '/admin/consent' +
        queryWith(self.listViewFromBack('/admin/consent', body.back), {}) +
        // WHICH HALF OF THE PAGE THE READER WAS ON. `from` is a NAME and never
        // a URL, for configReturnTo()'s reason: a redirect target taken out of
        // a request body is an open redirect and one carrying a newline is a
        // header injection. The worst a hand-written value can reach is the
        // other heading on this same page.
        (String(body.from || '') === 'recorded' ? '#recorded' : '#globals');
      self.respondToAction(req, res, back, result);
      log.debug("Leaving the admin consent action endpoint.");
    });

    app.get('/admin/roles', function (req, res) {
      log.debug("Entering the admin roles page.");
      const register = rolesRegister();
      const listView = self.listViewOf('/admin/roles', req.query);
      const navParams = pageParamsOf(req.query);

      const q = String((Array.isArray(req.query.q) ? req.query.q[0] :
                        req.query.q) || '')
        .trim().toLowerCase();
      // OVER THE NAME, THE DESCRIPTION AND EVERY MEMBER, because a reader
      // arrives at this page holding exactly one of those and does not know
      // which column it is in — the same reasoning /admin/consent's filter
      // carries.
      const matched = q
        ? register.roles.filter(function (one) {
            return [one.name, one.description].concat(one.users, one.groups,
                                                      one.applications)
              .some(function (text) {
                return String(text).toLowerCase().indexOf(q) >= 0;
              });
          })
        : register.roles;

      const rolePage = pagedRows(req.query, matched,
        { name: 'roles', noun: 'roles', defaultPer: 25 });
      const rolesNav = self.pageNavPair('/admin/roles', navParams,
                                        rolePage.paging);

      const roleOptions = register.roles.map(function (one) {
        return '<option value="' + self.esc(one.name) + '">' +
               self.esc(one.name) +
               '</option>';
      }).join('');
      const kindOptions = ROLE_MEMBER_KINDS.map(function (one) {
        return '<option value="' + self.esc(one.kind) + '">' +
               self.esc(one.label) +
               '</option>';
      }).join('');
      const applicationOptions = applications.list().map(function (row) {
        return '<option value="' + self.esc(row.identifier) + '">' +
               self.esc(row.name && row.name !== row.identifier
                 ? row.name + ' — ' + row.identifier : row.identifier) +
               '</option>';
      }).join('');
      const issuanceOptions = issuanceGate.KINDS.map(function (one) {
        return '<option value="' + self.esc(one) + '">' + self.esc(one) +
               '</option>';
      }).join('');

      const preview = rolesPreview(req.query);

      const inner =
        '<h2>Roles</h2>' +
        self.note('<strong>A role is a name somebody may hold, and holding ' +
        'one is what this service decides an ISSUANCE on.</strong> Three ' +
        'kinds of thing can be mapped into a role &mdash; a person, a group ' +
        '(so every member of it holds the role) and an APPLICATION (so a ' +
        'client authenticating as itself holds one, which is what a ' +
        '<code>client_credentials</code> grant is decided on, where there is ' +
        'no person at all). An application entry names the roles it REQUIRES ' +
        'in <code>appRequiredRole</code>, and nothing is issued for that ' +
        'application to somebody who holds none of them &mdash; a decision ' +
        'made by the XACML PDP against the policy ' +
        '<code>xacml.issuancePolicy</code> names, never by an ' +
        '<code>if</code> in an issuance site. So the reason for a refusal is ' +
        'a document you can read on <a ' +
        'href="/admin/xacml/policies">Policies</a>, test on <a ' +
        'href="/admin/xacml/decide">Try a decision</a>, and see in the <a ' +
        'href="/admin/audit">audit log</a>.') +

        self.note('<strong>This is not <a href="/admin/rbac">Admin ' +
        'roles</a>, which is the page above it in the sidebar.</strong> That ' +
        'one has exactly two roles, they are ordinary directory GROUPS, and ' +
        'what they grant is this console. This one has as many roles as you ' +
        'make, they live in <code>' +
        self.esc(register.container || 'ou=roles') + '</code>, and what they ' +
        'grant is being issued something. <strong>It is not <a ' +
        'href="/admin/groups">Groups</a> either</strong>: a group still ' +
        'grants nothing on its own, and that sentence is still true ' +
        'everywhere it is written. What changed is that a role may NAME a ' +
        'group &mdash; so adding somebody to <code>cn=developers</code> can ' +
        'now give them a role, through this register and only through it.') +

        (register.storable ? '' : self.warn('<strong>There is nowhere to ' +
          'keep a role.</strong> This process has no embedded directory ' +
          'installed behind the register, so no role can be created. The six ' +
          'BUILT-IN roles below still answer &mdash; they are computed ' +
          'rather than stored &mdash; so an application requiring EVERYBODY ' +
          'still admits everybody, which is the default and means nothing is ' +
          'refused. There is no fallback store, deliberately: a role ' +
          'register that quietly lived in memory would decide things nobody ' +
          'could find.')) +

        (register.enforced ? '' : self.warn('<strong>Nothing is being ' +
          'decided.</strong> <code>roles.enforceIssuance</code> is OFF, so ' +
          'no issuance asks the PDP at all and every application admits ' +
          'everybody whatever its entry says. This page still records what ' +
          'would be asked. The switch is in Settings at the foot of this ' +
          'page, and it is the way back if a policy edit locks something ' +
          'out.')) +

        (register.gated ? '' : self.warn('<strong>The XACML family is not ' +
          'loaded in this process.</strong> ' +
          '<code>common/issuance_gate.js</code> has no decider, so every ' +
          'issuance is allowed and this page is a register that nothing ' +
          'reads. That is the designed behaviour for a process without the ' +
          'engine &mdash; a smaller service rather than a broken one &mdash; ' +
          'and it is what <code>npm test</code> and the parent project\'s ' +
          'in-process Kerberos jobs run as.')) +

        '<h3 id="built-in">The six built-in roles</h3>' +
        self.note('<strong>Computed, in no container, and not ' +
        'editable.</strong> Every one of them is answered from the CONTEXT ' +
        'of the decision being made rather than from a store, so they have ' +
        'no members to list and cannot be created, renamed or deleted. ' +
        '<code>' + self.esc(register.defaultRequired) + '</code> is the one ' +
        'that matters most: an application that names no required role ' +
        'requires it, everybody holds it, and nothing is refused &mdash; ' +
        'which is exactly how this service behaved before roles existed. ' +
        '<strong>That is what makes this feature off by default without ' +
        'being absent</strong>: the machinery is always running and always ' +
        'visible, and narrowing an application is shortening a list rather ' +
        'than switching on a subsystem that has never run. The pairs are ' +
        'complementary ON PURPOSE and both halves exist because ' +
        '&ldquo;everyone who did not sign in&rdquo; is a thing policy ' +
        'authors reach for and cannot express as a negation in a XACML ' +
        'target.') +
        '<table><thead><tr><th>Role</th><th>Who holds ' +
        'it</th></tr></thead><tbody>' +
        register.builtIn.map(function (one) {
          return '<tr><td class="who"><code>' + self.esc(one.name) +
                 '</code></td><td>' + self.esc(one.what) + '</td></tr>';
        }).join('') +
        '</tbody></table>' +

        '<h3 id="roles">Roles you have made</h3>' +
        self.note('<strong>Membership, and nothing else.</strong> Each row ' +
        'is one entry under ' +
        '<code>' + self.esc(register.container || 'ou=roles') + '</code>; ' +
        'every column but the first is somebody who HOLDS the role. What a ' +
        'role is REQUIRED for is not here &mdash; that lives on the ' +
        'application entry that requires it and is drawn read-only further ' +
        'down, because it is a fact about the application rather than about ' +
        'the role.') +
        '<form method="get" action="/admin/roles"><div class="formrow">' +
          '<label for="q">Text</label>' +
          '<input type="text" id="q" name="q" size="40" value="' + self.esc(q) +
            '" placeholder="a role, a description or a member">' +
          '<button class="secondary">Filter</button>' +
          (q ? ' <a href="/admin/roles#roles">clear</a>' : '') +
        '</div></form>' +
        '<table><thead><tr><th>Role</th>' +
        ROLE_MEMBER_KINDS.map(function (one) {
          return '<th>Held by ' + self.esc(one.label) + '</th>';
        }).join('') +
        '<th></th></tr></thead><tbody>' +
        (rolePage.shown.length
          ? rolePage.shown.map(function (one) { return self.roleRow(one,
              listView); })
                          .join('')
          : '<tr><td colspan="5"><span class="state-none">' +
            (q ? 'No role matches &ldquo;' + self.esc(q) + '&rdquo;.'
               : 'No role has been made. Every application therefore ' +
                 'requires ' +
                 self.esc(register.defaultRequired) + ' and refuses nobody.') +
            '</span></td></tr>') +
        '</tbody></table>' + rolesNav +

        '<h3 id="create">Make a role</h3>' +
        self.note('The name becomes an LDAP RDN under <code>' +
        self.esc(register.container || 'ou=roles') + '</code> and a value in ' +
        'a token claim, so it is up to 64 characters of letters, digits, and ' +
        '<code>. _ : @ -</code> or a space. It may not be one of the six ' +
        'above: those are computed and answered first, so a stored role of ' +
        'the same name could never be reached.') +
        '<form method="post" action="/admin/roles">' +
        self.rolesBack(listView) +
        '<input type="hidden" name="action" value="create-role"><div ' +
        'class="formrow"><label>Name <input type="text" name="role" ' +
        'required></label><label>Description <input type="text" ' +
        'name="description" size="50"></label><button ' +
        'type="submit">Create</button></div></form>' +

        // "SOMEBODY" IS THREE KINDS AND THE HEADING USED TO HIDE TWO OF THEM.
        //
        // This read `Give somebody a role`, and the note under it was the three
        // ROLE_MEMBER_KINDS joined — so `note()` derived its summary from the
        // first of them and the fold read `a person — A username.` The one
        // sentence saying an application may hold a role AS ITSELF was inside a
        // collapsed block whose opening words were about people, on a page
        // whose heading was also about people. Somebody looking for how to put
        // an application in a role could read this whole section and not find
        // it, which is exactly what happened.
        //
        // The heading names all three now, and the note LEADS with a sentence
        // naming all three so that the derived summary does too. The per-kind
        // detail is unchanged and still folded — the fix is which sentence is
        // visible with the fold shut, not how much prose there is.
        '<h3 id="member">Give a person, a group or an application a role</h3>' +
        self.note('<strong>All three are first-class members of a role ' +
        'here</strong> &mdash; a person, a group, and an APPLICATION holding ' +
        'it as itself.<br>' +
        ROLE_MEMBER_KINDS.map(function (one) {
          return '<strong>' + self.esc(one.label) + '</strong> &mdash; ' +
                 self.esc(one.what);
        }).join('<br>')) +
        (register.roles.length ? '' : self.warn('<strong>There is no role to ' +
          'add anybody to yet, so this form is disabled.</strong> ' +
          '<a href="#create">Create a role</a> first &mdash; the six ' +
          'built-in roles have no membership, because they are COMPUTED from ' +
          'what the party is rather than from anything anybody wrote down. ' +
          'That is why the Role list below is empty on a service where ' +
          'nobody has made one.')) +
        '<form method="post" action="/admin/roles">' +
        self.rolesBack(listView) +
        '<input type="hidden" name="action" value="add-member">' +
        '<div class="formrow">' +
        '<label>Role <select name="role" required>' + roleOptions +
        '</select></label><label>Kind ' +
        '<select name="kind">' + kindOptions + '</select></label>' +
        // THE SUGGESTIONS ARE THE APPLICATIONS, AND ONLY THE APPLICATIONS.
        //
        // A datalist SUGGESTS and never constrains, which is what makes one
        // field serving three kinds tolerable: a person or a group is still
        // typed by hand and still need not exist yet, which is this service's
        // rule everywhere and is stated in the fold above.
        //
        // Applications are the kind with a knowable set — this realm's registry
        // has them all — and they are the kind somebody cannot guess, because
        // an application's identifier is whatever it registered itself as. The
        // other two are open by design: a person need not exist, and a group
        // lives in the directory behind a hook this page has no reader for.
        //
        // ITS OWN datalist rather than the preview form's `role-preview-apps`
        // below. Both render the same `applicationOptions` — one computation,
        // two renderings, so they cannot disagree — but referencing an id
        // defined by another section would make this field's suggestions vanish
        // silently if that section were ever made conditional or moved.
        '<label title="' +
          self.esc('The suggestions are the applications registered in this ' +
                   'realm. A person or a group is typed by hand and need not ' +
                   'exist yet — this service creates an entry for any name ' +
                   'on first sight.') +
          '">Name <input type="text" name="member" list="role-member-apps" ' +
          'required></label>' +
        '<datalist id="role-member-apps">' + applicationOptions +
        '</datalist><button type="submit"' +
          (register.roles.length ? '' : ' disabled') + '>Add</button>' +
        '</div></form>' +

        '<h3 id="requiring">What requires a role</h3>' +
        self.note('<strong>The other relation, and it is edited somewhere ' +
        'else.</strong> An application demands roles through ' +
        '<code>appRequiredRole</code> on its own entry, which is where it is ' +
        'written &mdash; on the application\'s page, in the attribute ' +
        'editor, or with an <code>ldapmodify</code>. This table RESOLVES it, ' +
        'which that page cannot: it says whether anything actually defines ' +
        'each role named. <strong>Only NARROWED applications are ' +
        'listed.</strong> Every other application in this realm requires ' +
        '<code>' +
        self.esc(register.defaultRequired) + '</code>, which everybody ' +
        'holds, and listing all of them would bury the ones somebody ' +
        'restricted on purpose.') +
        '<table><thead><tr><th>Application</th><th>Requires</th>' +
        '<th>Any of them holdable?</th></tr></thead><tbody>' +
        (register.requiring.length
          ? register.requiring.map(function (one) {
              return '<tr><td class="who"><a href="' +
                self.esc('/admin/applications' +
                         queryWith(listView,
                                   { application: one.application })) +
                '">' + self.esc(one.name) + '</a>' +
                (one.name === one.application ? ''
                  : '<br><code>' + self.esc(one.application) + '</code>') +
                    '</td>' +
                '<td>' + one.required.map(function (role) {
                  return '<code>' + self.esc(role) + '</code>';
                }).join(' or ') + '</td>' +
                '<td>' + (one.unknown.length
                  ? '<span class="state-none">No. ' +
                    one.unknown.map(function (role) {
                      return '<code>' + self.esc(role) + '</code>';
                    }).join(', ') + ' is not a built-in role and no entry ' +
                    'defines it, so NOBODY can hold it and this application ' +
                    'is issued nothing at all &mdash; which looks exactly ' +
                    'like the application being broken. Make the role, or ' +
                    'clear the value.</span>'
                  : 'Yes &mdash; every role named is defined.') + '</td></tr>';
            }).join('')
          : '<tr><td colspan="3"><span class="state-none">Nothing has been ' +
            'narrowed. Every application in this realm requires <code>' +
            self.esc(register.defaultRequired) + '</code> and refuses ' +
            'nobody, which is the state this service starts ' +
            'in.</span></td></tr>') +
        '</tbody></table>' +

        '<h3 id="preview">Would this be issued?</h3>' +
        self.note('<strong>The same call the nine issuance sites ' +
        'make.</strong> It goes through the embedded PEP and the PDP against ' +
        'the issuance policy, so a preview that agreed with the enforcement ' +
        'only by coincidence is impossible &mdash; which is the only reason ' +
        'it is worth having. Nothing is issued and nothing is recorded. <a ' +
        'href="/admin/xacml/decide">Try a decision</a> asks the same engine ' +
        'the other way round: an arbitrary request against the repository ' +
        'root, which is the policy about somebody ELSE\'s boundary.') +
        // NEITHER FIELD IS `required`, and that is not laziness. A browser will
        // not SUBMIT a form with an empty required field at all — constraint
        // validation blocks it silently — so a reader who pressed Ask with one
        // box empty would get no navigation, no message and no way to tell that
        // from a control that does nothing. The handler answers the honest
        // thing instead: with nothing asked it draws no decision, exactly as
        // `GET /admin-api/roles/preview` answers 200 with `answered: false`. It
        // was `required` for one run, and `tests/vendored/sts_admin_console.js`
        // caught it — that job submits every GET form on this console and
        // asserts the browser actually went somewhere.
        '<form method="get" action="/admin/roles"><div ' +
        'class="formrow"><label>Application <input type="text" ' +
        'name="application" list="role-preview-apps" value="' +
          self.esc((preview && preview.asked.application) || '') +
          '"></label>' +
        '<datalist id="role-preview-apps">' + applicationOptions +
        '</datalist><label>Subject <input type="text" name="subject" value="' +
          self.esc((preview && preview.asked.subject) || '') + '"></label>' +
        '<label>as <select name="subjectKind">' +
          '<option value="user">a person</option>' +
          '<option value="application"' +
          (preview && preview.asked.subjectKind === 'application' ?
           ' selected' :
           '') +
          '>an application</option></select></label>' +
        '<label>Issuing <select name="kind">' + issuanceOptions +
        '</select></label><button ' +
        'type="submit">Ask</button></div></form>' +
        (preview
          ? (preview.available === false
              ? self.warn(self.esc(preview.why))
              : '<div class="' + (preview.allowed ? 'ok' : 'err') + '">' +
                '<strong>' + self.esc(preview.decision) + ' &mdash; ' +
                (preview.allowed ? 'it would be issued' :
                 'it would be REFUSED') +
                '.</strong><br>' + self.esc(preview.why) + '<br>' +
                '<span class="sub">Holds: ' +
                (preview.roles.length ? self.codeList(preview.roles) :
                 'no role at all') +
                '. Requires: ' +
                (preview.required.length ? self.codeList(preview.required) :
                 'nothing') +
                '. Policy: <code>' +
                self.esc(String(preview.policy || '(none)')) +
                '</code>.</span></div>'
            )
          : '') +

        '<h3 id="policy">The policy that decides</h3>' +
        (register.policy
          ? (register.policy.ok
              ? self.note('<strong>' + (register.policy.builtIn
                  ? 'The BUILT-IN issuance policy is deciding.'
                  : 'A policy you wrote is deciding: <code>' +
                    self.esc(register.policy.name) + '</code>.') +
                '</strong> The rule it states is the same either way &mdash; ' +
                'the party being authenticated must hold one of the roles ' +
                'the application requires &mdash; and the requirement ' +
                'travels in the REQUEST rather than being written into the ' +
                'document, which is why ONE policy decides for every ' +
                'application and narrowing one is editing its entry rather ' +
                'than editing a policy.<br><br>' +
                (register.policy.builtIn
                  ? '<strong>Nothing is seeded and there is nothing to ' +
                    'delete.</strong> It was seeded once and the realm case ' +
                    'killed that: <code>ou=policies</code> is per realm and ' +
                    'the seed is written in the default realm at startup, so ' +
                    'a realm created afterwards had no issuance policy and ' +
                    'every application narrowed in it was issued NOTHING. To ' +
                    'EDIT the rule, create a policy named <code>' +
                    self.esc(register.policy.name) + '</code> from the ' +
                    '<code>role-issuance</code> template on ' +
                    '<a href="/admin/xacml/policies">Policies</a>: an entry ' +
                    'of that name overrides the built-in one.'
                  : 'It is an ordinary entry under <code>ou=policies</code>, ' +
                    'so the <a href="/admin/xacml/editor">editor</a>, ALFA, ' +
                    '<code>/admin-api/xacml</code> and an ' +
                    '<code>ldapmodify</code> all reach it. DELETING it puts ' +
                    'the built-in policy back &mdash; it is an override ' +
                    'rather than the only copy.') +
                '<br><br><strong>It is not the repository ROOT and it is not ' +
                'sent to remote PEPs.</strong> Two questions, two documents: ' +
                'the root answers what a caller asks at ' +
                '<code>/xacml/pdp</code> and what every remote PEP pulls, ' +
                'and this answers who may be issued something HERE. Making ' +
                'them one document would mean editing the demo policy ' +
                'changed who could sign in.')
              : self.warn('<strong>The issuance policy is not being ' +
                'evaluated.</strong> ' +
                self.esc(register.policy.why) + '<br><br>An application that ' +
                'requires only <code>' + self.esc(register.defaultRequired) +
                '</code> is unaffected &mdash; everybody holds it, so there ' +
                'was never anything to decide. <strong>An application whose ' +
                'entry names a role is REFUSED</strong>, and that asymmetry ' +
                'is deliberate: somebody deliberately asked for a ' +
                'restriction, and permitting because the document ' +
                'implementing it is missing would be a configured refusal ' +
                'silently not happening.'))
          : '') +

        '<h3 id="claim">The claim</h3>' +
        self.note('<strong>' + (register.claim
          ? 'Every access token, ID Token, SAML 2.0 assertion and SAML 1.1 ' +
            'assertion names the roles its subject holds, in <code>' +
            self.esc(register.claimName) + '</code>.'
          : 'The claim is OFF, so nothing carries a role.') +
        '</strong> The claim is OMITTED ENTIRELY for anybody holding no ' +
        'CONFIGURED role, and the six built-in ones are never in it: <code>' +
        self.esc(register.defaultRequired) + '</code> and ' +
        '<code>ALL_AUTHENTICATED_USERS</code> are true of almost every token ' +
        'this service issues, so carrying them would add two meaningless ' +
        'members to every token every existing client parses and tell a ' +
        'relying party nothing it did not know from holding the token. They ' +
        'exist to be REQUIRED, not to be ' +
        'carried.<br><br><code>' + self.esc(register.claimName) + '</code> ' +
        'is ALSO what is looked for on the way IN: the embedded PEP reads ' +
        'that member out of a token a caller presented and unions what it ' +
        'finds with this register\'s own answer. <strong>That half is weaker ' +
        'and the policy says so in its own description</strong> &mdash; this ' +
        'service does not verify access tokens it did not issue, so a claim ' +
        'is evidence about a token rather than about a person. The ' +
        '<code>role-issuance</code> template has a parameter that leaves ' +
        'that arm out.') +

        self.configFormsFor('/admin/roles');

      const json = Object.assign({}, register, {
        matched: matched.length,
        shown: rolePage.shown.length,
        paging: pagingJson(rolePage.paging),
        query: { q: q },
        memberKinds: ROLE_MEMBER_KINDS,
        issuanceKinds: issuanceGate.KINDS,
        actions: ROLE_ACTIONS.slice(),
        preview: preview,
        settings: self.configSettingsJson('/admin/roles')
      });
      self.respond(req, res, json, 'Roles', '/admin/roles', inner);
      log.debug("Leaving the admin roles page.");
    });

    app.post('/admin/roles', function (req, res) {
      log.debug("Entering the admin roles action endpoint.");
      const body = parseBody(req);
      // The actor is the person whose session got them through the gate, read
      // here rather than inside rolesAction() for rbacAction()'s reason: the
      // management API calls the same function with an actor of its own, and a
      // function that reached for a cookie would only work from one of them.
      const state = gateStateFor(req);
      const result = rolesAction(body, { actor: (state && state.username) || '',
                                         via: 'console' });
      const back = '/admin/roles' +
        queryWith(self.listViewFromBack('/admin/roles', body.back), {}) +
        // WHICH HEADING THE READER WAS ON. `from` is a NAME and never a URL,
        // for configReturnTo()'s reason: a redirect target taken out of a
        // request body is an open redirect and one carrying a newline is a
        // header injection. The worst a hand-written value can reach is another
        // heading on this same page.
        (['built-in', 'roles', 'create', 'member', 'requiring', 'preview',
          'policy', 'claim'].indexOf(String(body.from || '')) >= 0
           ? '#' + String(body.from) : '#roles');
      self.respondToAction(req, res, back, result);
      log.debug("Leaving the admin roles action endpoint.");
    });

    app.get('/admin/policies', function (req, res) {
      log.debug("Entering the admin policies page.");
      const view = adminViews.passwordPoliciesView(req.query);
      const pw = view.password;
      const profile = pw.profile;
      const listedNav = self.pageNavPair('/admin/policies',
                                         pageParamsOf(req.query),
                                         pagingOf(req.query,
                                                  view.paging.total));

      const inner = self.messagesOf(req) +
        '<div class="tiles">' +
          self.tile(view.kinds.length, 'kind of policy') +
          self.tile(view.paging.total, 'password profile') +
          self.tile(view.enforced ? 'yes' : 'no', 'enforced in this realm') +
          self.tile(profile.stored ? 'stored' : 'built-in',
                    'the default profile is') +
        '</div>' +

        self.note('<strong>A policy here is a rule this realm holds a ' +
        'credential to.</strong> The first kind is the password policy, and ' +
        'there is one profile of it — <code>default</code> — which applies ' +
        'to every person in this realm. It is stored in the directory as ' +
        '<code>' +
        self.esc(profile.dn || ('cn=default,ou=passwordPolicies,…')) +
        '</code>, in the shape of draft-behera-ldap-password-policy (the ' +
        'schema OpenLDAP\'s ppolicy overlay reads), so an ' +
        '<code>ldapsearch</code> finds it and an <code>ldapmodify</code> ' +
        'changes it. <strong>This is not the XACML policy ' +
        'repository</strong>: <a href="/admin/xacml/policies">that one</a> ' +
        'holds documents a PDP evaluates, in <code>ou=policies</code>; this ' +
        'holds numbers every door that sets a password checks.') +

        (view.enforced
          ? '<div class="ok">' + self.esc(view.enforcement) + '</div>'
          : self.warn(self.esc(view.enforcement) + ' Switch the realm with ' +
            '<code>global.mode</code> on <a ' +
            'href="/admin/config">Configuration</a>.')) +

        (profile.problems.length
          ? self.warn('<strong>The stored profile has ' +
                      profile.problems.length +
            ' problem(s), and the built-in default is in force for each ' +
            'field named:</strong> ' +
            profile.problems.map(self.esc.bind(self)).join(' '))
          : '') +

        '<h2 id="password">Password policy — the default profile</h2>' +
        self.note(profile.stored
          ? 'This profile is <strong>stored</strong> at <code>' +
            self.esc(profile.dn) +
            '</code>. Saving replaces it; putting the defaults back deletes ' +
            'it, after which the built-in defaults below are in force.'
          : '<strong>Nothing is stored yet, so the built-in defaults are in ' +
            'force.</strong> Saving this form writes <code>cn=default</code> ' +
            'under <code>ou=passwordPolicies</code> in this realm\'s ' +
            'directory. A realm created later starts with the same built-in ' +
            'defaults rather than a copy of this one — the profile is not ' +
            'seeded, so it cannot be missing from a realm nobody seeded.') +

        '<form method="post" action="/admin/policies">' +
        '<input type="hidden" name="action" value="save-password-policy">' +
        '<input type="hidden" name="profile" value="' + self.esc(profile.name) +
        '">' +
        self.wideTable('The default password profile',
          '<table><tr><th>Rule</th><th>Value</th><th>Attribute</th>' +
          '<th>Built-in default</th><th>Source</th></tr>' +
          pw.fields.map(self.passwordPolicyFieldRow.bind(self)).join('') +
          '<tr><td><label for="pp-description">Description</label></td>' +
          '<td colspan="4"><input type="text" id="pp-description" ' +
          'name="description" size="60" maxlength="1024" value="' +
          self.esc(profile.description) + '" placeholder="what this profile ' +
          'is for"></td></tr></table>') +
        '<p><button>Save the profile</button></p>' +
        self.note('Every field is checked before anything is written, and ' +
        'two rules relate fields to each other: a generated password must be ' +
        'at least the minimum length, and at least twice the symbol count ' +
        'plus two — a generator asked for more symbols than that would be ' +
        'drawing for a very long time. <strong>A change applies to the NEXT ' +
        'password set in this realm</strong>; nothing already stored is ' +
        're-checked, because a stored password is a hash and there is ' +
        'nothing left to check it against.') +
        '</form>' +

        (profile.stored
          ? '<form method="post" action="/admin/policies" class="inline">' +
            '<input type="hidden" name="action" ' +
            'value="reset-password-policy"><input type="hidden" ' +
            'name="profile" value="' + self.esc(profile.name) +
            '"><button ' +
            'class="secondary">Put the built-in defaults back</button></form>'
          : '') +

        '<h2 id="rules">What a password must be, right now</h2>' +
        self.note('The rules as the <a href="/portal/password">user ' +
        'portal</a> and the activation page print them to the person ' +
        'choosing a password, so the page they read and the rule this ' +
        'service applies are one sentence.') +
        '<ul>' + pw.rules.map(function (rule) {
          return '<li>' + self.esc(rule) + '</li>';
        }).join('') + '</ul>' +

        '<h2 id="doors">Where it is enforced</h2>' +
        self.note('Every door that sets a password ends in one function in ' +
        '<code>common/credentials.ts</code>, which is what makes the list ' +
        'below complete rather than a list somebody remembered. ' +
        self.esc(pw.notDoors)) +
        '<table><tr><th>Door</th><th>Reaches</th></tr>' +
        pw.doors.map(function (row) {
          return '<tr><td>' + self.esc(row.door) + '</td><td><code>' +
                 self.esc(row.via) +
            '</code></td></tr>';
        }).join('') + '</table>' +

        '<h2 id="history">The history, and what it costs</h2>' +
        self.note('<strong>A remembered password is the scrypt hash it was ' +
        'already stored as</strong>, moved into <code>pwdHistory</code> on ' +
        'the person\'s own entry when the next one replaces it — no new hash ' +
        'is made, and the password itself is never kept. Checking a new ' +
        'password costs one scrypt comparison per remembered one, about 70ms ' +
        'each on this thread, so a history of ' + self.esc(profile.history) +
        ' is up to ' +
        self.esc((profile.history + 1) * 70) + 'ms at a password change. A ' +
        'generated password skips the comparison, because nothing drawn at ' +
        'random is a previous password. <code>pwdHistory</code> and ' +
        '<code>pwdChangedTime</code> are maintained by this service and an ' +
        'LDAP modify naming either is refused in product mode.') +

        '<h2 id="generator">The generator</h2>' +
        self.note('<strong>New users created from the console or ' +
        '<code>/admin-api</code> get a generated password by ' +
        'default</strong>, shown or returned ONCE. It is drawn by ' +
        '<code>' + self.esc(pw.generator.module) + '</code>' +
        (pw.generator.version ? ' ' + self.esc(pw.generator.version) : '') +
        ' from ' +
        self.esc(pw.generator.source) + ', using ' +
        self.esc(pw.generator.pools.join(', ')) +
        ' (leaving out ' + pw.generator.excluded.map(function (one) {
          return '<code>' + self.esc(one) + '</code>';
        }).join(' and ') + ', which silently end or change a string pasted ' +
        'into a shell or a JSON body), and it draws until ' +
        self.esc(pw.generator.drawsUntil) +
        '.') +

        '<h2 id="profiles">Profiles</h2>' +
        self.note('One profile today, and the list is paged like every list ' +
        'in this console. A second profile cannot be created yet: nothing ' +
        'assigns a profile to a person, so a second one would decide nothing ' +
        'while looking exactly like one that does.') +
        listedNav.head +
        '<table><tr><th>Kind</th><th>Profile</th><th>Stored at</th>' +
        '<th>Problems</th></tr>' +
        view.profiles.map(function (row) {
          return '<tr><td>' + self.esc(row.kind) + '</td><td><a ' +
            'href="#password">' +
            self.esc(row.name) + '</a></td><td>' +
            (row.stored ? '<code>' + self.esc(row.dn) + '</code>'
                        : '<span class="state-none">not stored — built-in ' +
                          'defaults</span>') +
            '</td><td>' + self.esc(String(row.problems.length)) + '</td></tr>';
        }).join('') + '</table>' +
        listedNav.foot +

        '<h2 id="schema">Schema</h2>' +
        self.note('This directory is schemaless, so a container of entries ' +
        'carrying invented attributes says what they mean here. The ' +
        '<code>pwd*</code> names are draft-behera-ldap-password-policy\'s; ' +
        'the <code>stsPwd*</code> ones are this service\'s own, because the ' +
        'draft delegates composition rules to the server and defines none.') +
        '<table><tr><th>Object class</th><th>What it is</th></tr>' +
        pw.schema.objectClasses.map(function (row) {
          return '<tr><td><code>' + self.esc(row.name) + '</code></td><td>' +
            self.esc(row.what) + '</td></tr>';
        }).join('') + '</table>' +
        '<table><tr><th>Attribute</th><th>On</th><th>What it holds</th></tr>' +
        pw.schema.attributes.map(function (row) {
          return '<tr><td><code>' + self.esc(row.name) +
                 '</code></td><td>the ' +
            'profile</td><td>' + self.esc(row.what) + '</td></tr>';
        }).join('') +
        pw.schema.personAttributes.map(function (row) {
          return '<tr><td><code>' + self.esc(row.name) + '</code></td><td>a ' +
            'person</td><td>' + self.esc(row.what) + '</td></tr>';
        }).join('') + '</table>' +

        self.note('The same over JSON is ' +
        '<code>/admin/policies?format=json</code> and <code>GET ' +
        '/admin-api/policies</code>; the two actions on this page are ' +
        '<code>POST /admin-api/policies/save-password-policy</code> and ' +
        '<code>/reset-password-policy</code>.');

      self.respond(req, res, view, 'Policies', '/admin/policies', inner);
      log.debug("Leaving the admin policies page.");
    });

    app.post('/admin/policies', function (req, res) {
      log.debug("Entering the admin policies action endpoint.");
      const body = parseBody(req);
      // The actor is read here rather than inside the action for
      // rolesAction()'s reason: the management API calls the same function with
      // an actor of its own, and a function that reached for a cookie would
      // work from one of them.
      const state = gateStateFor(req);
      const result = adminActions.passwordPoliciesAction(body, {
        actor: (state && state.username) || '', via: 'console' });
      self.respondToAction(req, res, '/admin/policies' +
        queryWith(self.listViewFromBack('/admin/policies', body.back), {}) +
        (result.ok ? '#password' : ''), result);
      log.debug("Leaving the admin policies action endpoint.");
    });

    app.get('/admin/rbac', function (req, res) {
      log.debug("Entering the admin roles page.");
      const view = self.rbacView(req);
      self.respond(req, res, view.json, view.title, '/admin/rbac', view.inner,
                   view.up);
      log.debug("Leaving the admin roles page.");
    });

    app.post('/admin/rbac', function (req, res) {
      log.debug("Entering the admin roles action endpoint.");
      const body = parseBody(req);
      // The actor is the person whose session got them through the gate above,
      // and it is read here rather than inside admin_rbac.js because that
      // module has no request — the management API calls the same function with
      // an actor of its own, and a module that reached for a cookie would only
      // work from one of them.
      const state = gateStateFor(req);
      const result = rbacAction(body,
                                { via: 'console', actor: state.username });
      const back = '/admin/rbac' +
                   queryWith(self.listViewFromBack('/admin/rbac', body.back),
                             {});
      self.respondToAction(req, res, back, result);
      log.debug("Leaving the admin roles action endpoint.");
    });

    app.post('/admin/claims', function (req, res) {
      log.debug("Entering the admin claims action endpoint.");
      const body = parseBody(req);
      // Two names for the list, exactly as /admin/vc takes them and for the
      // same reason: a checkbox column is one `attribute` repeated, and a JSON
      // body carries one `attributes` array. Neither spelling is wrong and
      // refusing either would make the console's own form and the API's
      // document disagree.
      const names = self.listField(req, body, 'attribute').concat(
          self.listField(req, body, 'attributes'));
      const result = claimsAction(body, names, stats.JWT_CLAIM_SET_IDS);
      // Back to the page the reader was on, preview user and all: the two
      // tables show that person's values, and a redirect that dropped the
      // parameter would answer "what did that do" with somebody else's row.
      self.respondToAction(req, res, self.claimsPageUrl(req.query), result);
      log.debug("Leaving the admin claims action endpoint.");
    });

    // The same handler for the SAML half, and it is a SEPARATE ROUTE rather
    // than a hidden field on the one above for two reasons that are both about
    // the reply: the redirect has to land back on the page the form was on, and
    // the refusal for a set this page does not carry has to name the sets it
    // does. Everything between those two lines is the same function on the same
    // store.
    app.post('/admin/saml-attributes', function (req, res) {
      log.debug("Entering the admin SAML attributes action endpoint.");
      const body = parseBody(req);
      const names = self.listField(req, body, 'attribute').concat(
          self.listField(req, body, 'attributes'));
      const result = claimsAction(body, names, stats.SAML_CLAIM_SET_IDS);
      self.respondToAction(req, res, self.samlAttributesPageUrl(req.query),
                           result);
      log.debug("Leaving the admin SAML attributes action endpoint.");
    });

    app.get('/admin/claims', function (req, res) {
      log.debug("Entering the admin claims page.");
      const previewUser = claimsPreviewUser(req.query);
      const pageUrl = self.claimsPageUrl(req.query);
      // ONE read of the directory and one invented persona for the whole page,
      // not one per set: both tables show the same catalogue of values for the
      // same person, and a read of one entry per section would be one that
      // exists only because the sections were written separately.
      const values = claimAttributes.catalogueValuesFor(previewUser);

      const inner = self.messagesOf(req) +
        self.note('What to add to every <strong>token</strong> this service ' +
        'issues <em>from now on</em>. Nothing already issued changes — a ' +
        'token is a signed document and this page cannot reach inside one. ' +
        'Two sets, because an OAuth 2.0 access token and an OIDC ID Token go ' +
        'to different readers: one to a resource server and one to a client, ' +
        'and the interesting configuration is usually the one where they ' +
        'DIFFER.') +

        self.note('The other three sets are next door. <strong>The UserInfo ' +
        'response is configured on <a href="/admin/userinfo-claims">UserInfo ' +
        'claims</a></strong>, beside this page — separate because that ' +
        'response is rebuilt on every call rather than issued once, so a ' +
        'change there reaches a client that is already holding its tokens. ' +
        '<strong>SAML 2.0 and SAML 1.1 assertions are on <a ' +
        'href="/admin/saml-attributes">Custom SAML attributes</a></strong>, ' +
        'under SAML, because those two spell an attribute differently enough ' +
        'from a JWT claim — and from each other — that one page had to ' +
        'explain three vocabularies before a reader could change one. The ' +
        'store is the same one whichever page is used, and so is the audit ' +
        'row.') +

        self.claimHalvesNote('jwt') +

        self.claimPreviewForm('/admin/claims', previewUser, values) +

        self.warn('<strong>Custom claims are additive.</strong> A configured ' +
        'claim is added to what the protocol already puts in the token and ' +
        'never replaces one. The names this service sets itself are refused ' +
        'rather than silently ignored: ' +
        self.codeList(stats.RESERVED_JWT_CLAIMS) + '. Every one of them is ' +
        'load-bearing somewhere here — an <code>exp</code> settable from a ' +
        'web form would produce tokens that fail to verify with nothing ' +
        'pointing back at this page. The list is a JWT rule and only a JWT ' +
        'rule: an <a href="/admin/saml-attributes">assertion attribute</a> ' +
        'called <code>exp</code> collides with nothing and is allowed.') +

        '<h2>The two token sets</h2>' +
        stats.JWT_CLAIM_SET_IDS.map(function (id) {
          return self.claimSetSection(id, previewUser, values, pageUrl);
        }).join('') +

        self.groupClaimSection(previewUser) +

        self.attributeCatalogueNotes('jwt') +

        self.claimValueNotes('jwt') +

        self.replaceSetForm(stats.JWT_CLAIM_SET_IDS, pageUrl, 'jwt');

      self.respond(req, res, claimsJson(previewUser), 'Custom claims',
                   '/admin/claims',
                   inner);
      log.debug("Leaving the admin claims page.");
    });

    // -------------------------------------------------------------------------
    // GET /admin/saml-attributes
    //
    // THE SAME TWO HALVES FOR THE TWO ASSERTION VOCABULARIES, on their own page
    // and under the console's own SAML group since 2026-08-24. It was four
    // sections of /admin/claims, and the reason for moving is not tidiness: a
    // reader who has come to add an attribute to a WS-Federation assertion had
    // to read past an OAuth access token and an OIDC ID Token, in a page whose
    // every warning was about JWT claim names that do not apply to them. The
    // two SAML sets differ from the JWT pair in three ways that all have to be
    // said on whichever page carries them — a NameFormat, an
    // AttributeNamespace, and a content model that is text and cannot nest — so
    // saying them here costs one page and buys two that are each about one
    // thing.
    //
    // WHAT DID NOT MOVE IS THE STORE. admin_stats.js still holds one CLAIM_SETS
    // object with all four sets in it, setClaimSet() is still the one door onto
    // it, and claimsAction() is still the one function both pages post to. This
    // page is a VIEW of two of those sets and an `allowed` list on the way in;
    // it is not a second place assertions are configured.
    // -------------------------------------------------------------------------
    app.get('/admin/saml-attributes', function (req, res) {
      log.debug("Entering the admin SAML attributes page.");
      const previewUser = claimsPreviewUser(req.query);
      const pageUrl = self.samlAttributesPageUrl(req.query);
      const values = claimAttributes.catalogueValuesFor(previewUser);

      const inner = self.messagesOf(req) +
        self.note('What to add to every <strong>SAML assertion</strong> this ' +
        'service issues <em>from now on</em>. Nothing already issued changes ' +
        '— an assertion is a signed document and this page cannot reach ' +
        'inside one. Two sets, because SAML 2.0 and SAML 1.1 spell an ' +
        'attribute differently enough that one list could not serve both: ' +
        '2.0 has <code>Name</code> and an optional <code>NameFormat</code>, ' +
        'and 1.1 has <code>AttributeName</code> and a required ' +
        '<code>AttributeNamespace</code>.') +

        self.note('<strong>Where these assertions come from.</strong> The ' +
        'SAML 2.0 set reaches every assertion <a ' +
        'href="/admin/sts-metadata">WS-Trust</a> issues with a 2.0 token ' +
        'type; the SAML 1.1 set reaches the 1.1 ones, which is what ' +
        '<strong>WS-Federation\'s passive requestor profile</strong> carries ' +
        '— so the 1.1 half is the one a browser sign-in exercises. There is ' +
        'no SAML 2.0 Web SSO profile here, deliberately, so no assertion of ' +
        'that kind reaches a browser: the 2.0 set is exercised by a WS-Trust ' +
        'client.') +

        self.note('Tokens are next door. <strong>The OAuth 2.0 access token ' +
        'and the OIDC ID Token are configured on <a ' +
        'href="/admin/claims">Custom claims</a></strong>, and the ' +
        '<strong>UserInfo response on <a ' +
        'href="/admin/userinfo-claims">UserInfo claims</a></strong>, both ' +
        'under OAuth2 / OIDC. The store behind all three pages is one store ' +
        '— the same five sets, the same <code>ldapmodify</code>-visible ' +
        'directory attributes, and one row in <a href="/admin/audit">the ' +
        'audit log</a> per change whichever page made it.') +

        self.claimHalvesNote('saml') +

        self.claimPreviewForm('/admin/saml-attributes', previewUser, values) +

        self.warn('<strong>Custom attributes are additive.</strong> A ' +
        'configured attribute is added to what the protocol already puts in ' +
        'the assertion and never replaces one — a SAML 2.0 assertion sets ' +
        '<code>name</code> from the sign-in and a WS-Federation one sets the ' +
        'whole identity claim list, and neither can be displaced from here. ' +
        '<strong>The reserved list on <a href="/admin/claims">the claims ' +
        'page</a> does not apply to these two sets</strong>: those names are ' +
        'load-bearing in a JWT, and an assertion attribute called ' +
        '<code>exp</code> or <code>scope</code> collides with nothing. What ' +
        'is refused here is the same thing that is refused there — an entry ' +
        'with no name, and two entries of one name, because the second would ' +
        'win silently.') +

        '<h2>The two assertion sets</h2>' +
        stats.SAML_CLAIM_SET_IDS.map(function (id) {
          return self.claimSetSection(id, previewUser, values, pageUrl);
        }).join('') +

        self.groupClaimSection(previewUser) +

        self.attributeCatalogueNotes('saml') +

        self.claimValueNotes('saml') +

        '<h2>The SAML 1.1 namespace</h2>' +
        self.note('A SAML 1.1 attribute is a NAME IN A NAMESPACE, and an ' +
        'attribute configured without one gets ' +
        '<code>' + self.esc(stats.DEFAULT_SAML11_NAMESPACE) + '</code> — the ' +
        'claim namespace every WS-Federation relying party already reads. ' +
        'That default is why an attribute added with a name and a value ' +
        'alone arrives somewhere useful instead of in a namespace nothing ' +
        'looks in. SAML 2.0 has no equivalent: <code>NameFormat</code> is ' +
        'optional there and is left off unless it is typed.') +

        self.replaceSetForm(stats.SAML_CLAIM_SET_IDS, pageUrl, 'saml');

      self.respond(req, res, samlAttributesJson(previewUser),
                   'Custom SAML attributes',
                   '/admin/saml-attributes', inner);
      log.debug("Leaving the admin SAML attributes page.");
    });

    app.post('/admin/userinfo-claims', function (req, res) {
      log.debug("Entering the admin UserInfo claims action endpoint.");
      const body = parseBody(req);
      // Both spellings of the list, exactly as the two pages next door take
      // them and for the same reason: a checkbox column is one `attribute`
      // repeated and a JSON body carries one `attributes` array.
      const names = self.listField(req, body, 'attribute').concat(
          self.listField(req, body, 'attributes'));
      const result = claimsAction(body, names, stats.USERINFO_CLAIM_SET_IDS);
      self.respondToAction(req, res, self.userinfoClaimsPageUrl(req.query),
                           result);
      log.debug("Leaving the admin UserInfo claims action endpoint.");
    });

    app.get('/admin/userinfo-claims', function (req, res) {
      log.debug("Entering the admin UserInfo claims page.");
      const previewUser = claimsPreviewUser(req.query);
      const raw = claimsRequestParameter(req.query);
      const pageUrl = self.userinfoClaimsPageUrl(req.query);
      // ONE read of the directory for the whole page, exactly as /admin/claims
      // does: the set's table and the request preview describe the same person,
      // and two reads of one entry could answer one page with two versions of
      // them.
      const values = claimAttributes.catalogueValuesFor(previewUser);
      const preview = claimsRequestPreview(previewUser, raw);

      const inner = self.messagesOf(req) +
        self.note('What to put in every <strong>UserInfo response</strong> ' +
        'this service returns. <strong>This is the one claims page with no ' +
        '&ldquo;nothing already issued changes&rdquo; warning on it, and ' +
        'that is the point of it existing.</strong> An access token, an ID ' +
        'Token and both assertions are signed documents: a claim added to ' +
        'one of those sets reaches a client at its next sign-in and never ' +
        'reaches what it already holds. This response is built on <em>every ' +
        'call</em>, so a claim added here reaches the next <code>GET ' +
        '/oauth2/userinfo</code> from a client that signed in an hour ago ' +
        'and has done nothing since.') +

        self.note('Tokens and assertions are next door. The OAuth 2.0 access ' +
        'token and the OIDC ID Token are on <a href="/admin/claims">Custom ' +
        'claims</a>; SAML 2.0 and SAML 1.1 are on <a ' +
        'href="/admin/saml-attributes">Custom SAML attributes</a>. ' +
        '<strong>The store behind all three pages is one store</strong> ' +
        '&mdash; the same five sets, the same ' +
        '<code>ldapmodify</code>-visible directory attributes, and one row ' +
        'in <a href="/admin/audit">the audit log</a> per change whichever ' +
        'page made it.') +

        self.claimHalvesNote('userinfo') +

        self.claimPreviewForm('/admin/userinfo-claims', previewUser, values) +

        self.warn('<strong>Custom claims are additive here too.</strong> A ' +
        'configured claim is added to what OpenID Connect Core section 5.4 ' +
        'already puts in the response for the scopes the token carries, and ' +
        'never replaces one. The names this service sets itself are refused ' +
        'rather than silently ignored: ' +
        self.codeList(stats.RESERVED_JWT_CLAIMS) + '. ' +
        'That list is the same one <a href="/admin/claims">the claims ' +
        'page</a> enforces and it applies HERE for a reason worth stating, ' +
        'because <a href="/admin/saml-attributes">the SAML page</a> does not ' +
        'enforce it: <code>sub</code> is REQUIRED in this response (Core ' +
        '5.3.2, and a client MUST check it against the ID Token\'s), and ' +
        'when a client has registered a ' +
        '<code>userinfo_signed_response_alg</code> the whole response is a ' +
        'JWT carrying <code>iss</code>, <code>aud</code> and ' +
        '<code>exp</code>. Every name on that list is load-bearing in at ' +
        'least one of those two shapes.') +

        '<h2>The UserInfo claim set</h2>' +
        stats.USERINFO_CLAIM_SET_IDS.map(function (id) {
          return self.claimSetSection(id, previewUser, values, pageUrl);
        }).join('') +

        self.groupClaimSection(previewUser) +

        '<h2>What a client can ask for &mdash; ' +
        'OpenID Connect Core section 5.5</h2>' +
        self.note('<strong>This is the one claim set a CLIENT can add ' +
        'to.</strong> Section 5.5 lets a client send a <code>claims</code> ' +
        'request parameter at the authorization endpoint naming individual ' +
        'claims it wants back from this endpoint, and since 2026-08-26 this ' +
        'service parses it, refuses a malformed one by name, carries it on ' +
        'the authorization code and <em>inside the access token</em>, and ' +
        'answers it by reading the named claims off that person\'s entry ' +
        'under <code>ou=users</code>. ' +
        '<code>claims_parameter_supported</code> in <a ' +
        'href="/.well-known/openid-configuration">the discovery document</a> ' +
        'said <code>false</code> until that day.') +

        self.note('<strong>Four layers, and the last one wins.</strong> (1) ' +
        'the set configured on this page, which is what everybody gets; (2) ' +
        'section 5.4\'s scope-driven claims &mdash; <code>profile</code> and ' +
        '<code>email</code>, the one place in this service where a scope ' +
        'genuinely changes an answer; (3) the claims a client asked for BY ' +
        'NAME, read off the directory; (4) <code>sub</code>, which nothing ' +
        'may displace. <strong>Layer 3 beating layer 2 is the one choice ' +
        'here that is not obvious</strong>, so it is said out loud: a scope ' +
        'asks for a category and a request names a claim, and answering ' +
        '<code>{"email":null}</code> with the invented <code>' +
        self.esc(userFor(previewUser).email) + '</code> while the entry ' +
        'holds a real <code>mail</code> would defeat the only reason the ' +
        'feature is worth having.') +

        self.requestableClaimsSection() +

        '<h3>Try one</h3>' +
        self.claimsRequestSection(previewUser, raw, preview) +

        self.tip('<strong>NON-SPEC: this endpoint also takes a claims ' +
        'request directly.</strong> Section 5.3.1 defines no request ' +
        'parameters at all &mdash; an access token and nothing else &mdash; ' +
        'and <code>/oauth2/userinfo</code> accepts one anyway, because ' +
        'exercising section 5.5 through the specified route means running a ' +
        'whole authorization flow per variation. Two spellings: ' +
        '<code>?claims={"userinfo":{"birthdate":null}}</code>, the section ' +
        '5.5 structure whole, and ' +
        '<code>?claim=birthdate&amp;claim=address</code>, one name each. ' +
        'Both work on GET and on a form-encoded POST. It is a ' +
        '<strong>union</strong> with what the access token carries and can ' +
        'never take a claim away from it &mdash; what the client was ' +
        'authorized for is what the token says. A malformed one is refused ' +
        '<code>invalid_request</code>, because ignoring a debugging ' +
        'parameter that was typed wrong produces the same response as one ' +
        'that was never sent.') +

        self.warn('<strong><code>essential</code>, <code>value</code> and ' +
        '<code>values</code> are carried and not enforced</strong>, and that ' +
        'is the honest reading of section 5.5.1 rather than a shortfall. An ' +
        '<em>essential</em> claim is a statement about what the CLIENT will ' +
        'do without it, and the same section says a server MUST NOT return ' +
        'an error because a requested claim is unavailable &mdash; so an ' +
        'essential claim this service cannot produce is absent and logged. ' +
        '<em>value</em> and <em>values</em> ask for a claim to come back ' +
        'with a particular value, which this service could satisfy by ' +
        'echoing it and deliberately does not: everything it says about a ' +
        'person comes from the directory or the invented persona, and a ' +
        'UserInfo response that agreed with whatever a client asked it to ' +
        'say would be the one surface here that cannot be used to test ' +
        'anything. The mismatch is reported instead.') +

        self.attributeCatalogueNotes('userinfo') +

        self.claimValueNotes('userinfo') +

        self.replaceSetForm(stats.USERINFO_CLAIM_SET_IDS, pageUrl, 'userinfo');

      self.respond(req, res, userinfoClaimsJson(previewUser, raw),
                   'UserInfo claims',
                   '/admin/userinfo-claims', inner);
      log.debug("Leaving the admin UserInfo claims page.");
    });

    app.post('/admin/vc', function (req, res) {
      log.debug("Entering the admin credential-claims action endpoint.");
      const body = parseBody(req);
      // Two names for the list, because the two callers spell it differently
      // and neither spelling is wrong: a checkbox is one `attribute` repeated,
      // and a JSON body carries one `attributes` array.
      const names = self.listField(req, body, 'attribute').concat(
          self.listField(req, body, 'attributes'));
      const result = vcAction(body, names);
      self.respondToAction(req, res, '/admin/vc', result);
      log.debug("Leaving the admin credential-claims action endpoint.");
    });

    app.get('/admin/vc', function (req, res) {
      log.debug("Entering the admin credential-claims page.");
      const previewUser = vcPreviewUser(req.query);

      const inner = self.messagesOf(req) +
        self.note('Which claims a Verifiable Credential issued by this ' +
        'service carries, <em>from now on</em>. Nothing already issued ' +
        'changes — a credential is a signed document and this page cannot ' +
        'reach inside one. It applies to all five OID4VCI configurations: ' +
        'the SD-JWT VC, the <code>jwt_vc_json</code> W3C credential, the ' +
        '<code>ldp_vc</code> one with a BBS proof, and the two whose only ' +
        'difference is that the issuer names itself by DID.') +

        self.note('The list is of <strong>LDAP attribute types</strong> and ' +
        'not of claim names, because this service has a directory and a ' +
        'claim with a value nothing else can see is half a demonstration. A ' +
        'selected attribute becomes the claim named beside it, and the value ' +
        'is the one on that person\'s entry under <code>ou=users</code> — so ' +
        'an LDAP client and an OID4VCI wallet pointed at this service are ' +
        'shown the same person. Three rows are not RFC 4519/4524/2798: there ' +
        'is no standard attribute type for a birthdate or a nationality, so ' +
        'the SCHAC schema\'s names are borrowed rather than invented.') +

        self.warn('<strong>None of this is verified, and the values are ' +
        'garbage on purpose.</strong> This service authenticates nobody — ' +
        'the username typed at the sign-in screen is the identity in every ' +
        'token and credential it issues — so there is no source of a real ' +
        'birthdate here and there had better not be. What a person is ' +
        'missing is invented from their username: the same invented person ' +
        'every time, across restarts, so that two credentials issued a ' +
        'minute apart describe one human being rather than two. A verifier ' +
        'that believed any of it would be believing this page.') +

        '<h2>The attributes</h2>' +
        self.vcAttributeTable(previewUser) +

        '<h2>What a credential would carry</h2>' +
        self.vcPreviewSection(previewUser) +

        '<h2>Where a value comes from</h2>' +
        self.note('Three sources, in this order. <strong>The access ' +
        'token</strong>, where it carries a claim of that name — that is a ' +
        'statement this service already made about the person, from the ' +
        'sign-in or from the <a href="/admin/claims">custom claims</a> page, ' +
        'and a credential contradicting the token that authorised it would ' +
        'be indefensible. Then <strong>the directory entry</strong>, which ' +
        'is where the generated values live once an entry exists and also ' +
        'where an <code>ldapmodify</code> lands: change <code>mail</code> on ' +
        '<code>uid=alice,ou=users</code> and the next credential says so. ' +
        'Then <strong>the generated persona</strong>, for a person with no ' +
        'entry, or an entry without that attribute, or a directory that is ' +
        'not running.') +
        self.note('Populating never overwrites. An attribute an entry ' +
        'already carries is left exactly as it is — which is why the three ' +
        'seeded people keep their names and only gain what they had nothing ' +
        'for, and why a sweep run twice does nothing the second time.') +

        '<h2>What these claims do not do</h2>' +
        self.note('Nothing reads them back. No access token, ID Token, SAML ' +
        'assertion or Kerberos PAC carries a claim from this page, and no ' +
        'endpoint makes a decision on one — it reaches a credential and ' +
        'stops there. The <a href="/admin/users">users</a> page shows the ' +
        'directory entry each of these values was written onto.');

      self.respond(req, res, vcJson(previewUser), 'Credential claims',
                   '/admin/vc',
                   inner);
      log.debug("Leaving the admin credential-claims page.");
    });

    app.post('/admin/vc-verifier-config', function (req, res) {
      log.debug("Entering the admin verifier-request action endpoint.");
      const body = parseBody(req);
      // Two names for the list, for the reason /admin/vc has two: a checkbox is
      // one `claim` repeated, and a JSON body carries one `claims` array.
      const names = self.listField(req, body, 'claim').concat(
          self.listField(req, body, 'claims'));
      const result = vpConfigAction(body, names);
      self.respondToAction(req, res, '/admin/vc-verifier-config', result);
      log.debug("Leaving the admin verifier-request action endpoint.");
    });

    app.get('/admin/vc-verifier-config', function (req, res) {
      log.debug("Entering the admin verifier-request page.");
      const format = vpConfig.defaultFormatId();
      const requested = vpConfig.requestedClaims();
      const query = vpConfig.dcqlQuery(format);

      const inner = self.messagesOf(req) +
        self.note('What the mock Verifier at <a ' +
        'href="/oid4vp/verifier">/oid4vp/verifier</a> — the pages call it ' +
        '<em>The Bar Door</em> — asks a wallet for, and in which credential ' +
        'format. It reaches the wire as the <code>dcql_query</code> of the ' +
        'next OID4VP Authorization Request, and it is what the Verifier then ' +
        'checks the presentation against: a claim asked for and not ' +
        'presented fails the <em>Requested claims</em> check by name.') +

        self.note('The claims are the same catalogue <a ' +
        'href="/admin/vc">/admin/vc</a> fills a credential from, grouped ' +
        'into <strong>claims</strong> rather than listed as attribute types. ' +
        'A credential carries one Disclosure per top-level claim, so ' +
        '<code>address</code> is one unit of disclosure however many LDAP ' +
        'attributes feed it — asking for it gets the street, the locality, ' +
        'the region, the postal code and the country together, and a page ' +
        'offering six address checkboxes would be offering a choice that ' +
        'does not exist on the wire.') +

        self.warn('<strong>This asks; it does not admit anybody.</strong> A ' +
        'presentation made to this door starts no session, issues no token ' +
        'and grants no access — the door says yes and that is the whole of ' +
        'it. Signing in with a wallet is a different door, <code>' +
        '/authn/wallet</code>, which asks for a credential this realm ' +
        'issued with a request of its own and is not configured here ' +
        '(<a href="/admin/oid4vp">OpenID4VP</a> has its switch). The two ' +
        'settings are also deliberately separate: this page decides what is ' +
        'ASKED FOR and <a href="/admin/vc">/admin/vc</a> decides what is ' +
        'ISSUED, so that asking for a claim the issuer does not mint stays ' +
        'reachable. That is the negative worth testing, and one page setting ' +
        'both would make it impossible to reach.') +

        '<h2>The claims</h2>' +
        (requested.length
          ? self.note('Asking for ' + requested.length + ': ' +
                      self.codeList(requested) +
                      '.')
          : self.warn('<strong>No claim is selected, and that is a real ' +
            'request rather than an empty form.</strong> DCQL reads an ' +
            'absent <code>claims</code> member as the WHOLE credential, so ' +
            'the query below carries none and the wallet is being asked for ' +
            'everything — the opposite of what selective disclosure is for, ' +
            'which is exactly why it is worth being able to ask for it.')) +
        self.vpClaimsSection(format) +

        '<h2>The credential types that can be submitted</h2>' +
        self.vpFormatsSection(format) +

        '<h2>The query this builds</h2>' +
        self.note('Built by the function that builds the real one, not by a ' +
        'second walk of the table above — a preview that agreed with this ' +
        'page and disagreed with the request would be worse than no preview. ' +
        'It is the <code>dcql_query</code> parameter of the next ' +
        'Authorization Request, by value or inside the signed Request ' +
        'Object.') +
        '<textarea readonly spellcheck="false">' +
        self.esc(JSON.stringify(query, null, 2)) + '</textarea><h2>What ' +
        'this page does not change</h2>' +
        self.note('Not what the issuer mints — that is <a ' +
        'href="/admin/vc">/admin/vc</a>, and the <em>Issued now</em> column ' +
        'above is this page reporting on that one. Not a request already in ' +
        'flight, which keeps the claims it was built with. Not the ' +
        '<code>vct</code> or the type array a credential is identified by. ' +
        'And not what a verified presentation is worth: nothing here turns ' +
        'one into a credential of any kind, and nothing here decides whether ' +
        'one signs anybody in — that is <code>/authn/wallet</code>\'s ' +
        'question, asked only of a credential this realm issued.');

      self.respond(req, res, vpConfigJson(), 'Verifier request',
                   '/admin/vc-verifier-config', inner);
      log.debug("Leaving the admin verifier-request page.");
    });

    // -------------------------------------------------------------------------
    // WHERE THE REALM CHOOSER SUBMITS.
    //
    // A `<select>` cannot navigate on its own without script, and this console
    // runs none (`script-src 'none'`, see common/app.js) — so the chooser is a
    // GET form and this is what it submits to. It exists only to turn a realm
    // id and a path into one redirect.
    //
    // **THE TARGET IS BUILT HERE AND NOT ECHOED.** `to` arrives in a query
    // string and ends up in a `Location` header, which is the shape of every
    // open-redirect there has ever been. So it is accepted only as a
    // single-slash-rooted path — `//host` and `https://host` and anything else
    // is refused rather than corrected — and the realm is looked up in the
    // registry rather than trusted, with `/admin` as the answer to both
    // refusals. What goes out is root + the chosen realm's prefix + a path this
    // service has vouched for.
    //
    // **THE REDIRECT IS ABSOLUTE, for the reason the form's action is.** app.js
    // wraps `res.location()` so that a root-relative target gains the CURRENT
    // realm's prefix — which is right for the six modules that send a browser
    // to the sign-in screen and exactly wrong here, where the whole point is to
    // land in a DIFFERENT realm. An absolute URL names a host, so that wrapper
    // passes it through.
    //
    // 303 rather than 302 so the reload after it is a GET, like every other
    // action on this console.
    // -------------------------------------------------------------------------
    app.get('/admin/realm-switch', function (req, res) {
      log.debug("Entering the admin realm switch endpoint.");
      const wanted = String(req.query.realm || '').trim();
      const target = realms.get(wanted);
      const asked = String(req.query.to || '');
      // A path this service is willing to put in a Location header: rooted, one
      // slash, no scheme, and short enough not to be a place to hide one.
      const path = (/^\/(?!\/)[^\s]*$/.test(asked) && asked.length <= 2000) ?
        asked : '/admin';
      if (asked && path !== asked) {
        errorCodes.mark(res, 'STS-ADMIN-0022');
      }
      if (!target) {
        log.debug("Leaving the admin realm switch endpoint. No such realm " +
          JSON.stringify(wanted) + ".");
        errorCodes.mark(res, 'STS-ADMIN-0020');
        res.set('Cache-Control', 'no-store')
           .redirect(303, self.realmRoot(req) + realms.currentPrefix() + path);
        return;
      }
      const to = self.realmRoot(req) + realms.prefixOf(target) + path;
      log.debug("Leaving the admin realm switch endpoint. To " + to + ".");
      res.set('Cache-Control', 'no-store').redirect(303, to);
    });

    app.get('/admin/realms', function (req, res, next) {
      log.debug("Entering the admin trust realms page.");
      // The list and the drill-down both show a realm's `kid`, which MAKES the
      // key set of a realm this process holds none for — made off the event
      // loop first, `GET /admin-api/realms`'s reason (#46). Never rejects.
      loadHelpers().prepareKeySets(realms.list().map(function (one) {
        return one.id;
      })).then(function () {
        const view = self.realmsView(req);
        if (view.missing) {
          errorCodes.mark(res, 'STS-ADMIN-0021');
        }
        self.respond(req, res, view.json, view.title, '/admin/realms',
                     view.inner,
                     view.up);
        log.debug("Leaving the admin trust realms page.");
      }).catch(function (e) {
        log.debug("Caught in the admin trust realms page: " +
                  ((e && e.message) || e));
        // Express 4 does not look at a returned promise; handed on, a throw is
        // the 500 it was before the page awaited anything.
        next(e);
      });
    });

    app.post('/admin/realms', function (req, res) {
      log.debug("Entering the admin trust realms action endpoint.");
      const body = parseBody(req);
      const result = realmsAction(body);
      const listView = self.listViewFromBack('/admin/realms', body.back);
      const id = String(body.id || '').trim();
      // Back to the realm that was acted on, EXCEPT after a removal — there is
      // nothing to drill into any more, and a 303 to a drill-down for a realm
      // that has just gone would answer with this page's own "no such realm"
      // message, which reads as a failure after an action that succeeded.
      const back = (id && result.ok !== false &&
                    String(body.action) !== 'remove')
        ? '/admin/realms' + queryWith(listView, { realm: id })
        : '/admin/realms' + queryWith(listView, {});
      // A NEW REALM'S BOOTSTRAP PASSWORD, ONCE (2026-09-14, #32). Product mode
      // hands the realm's `admin` a generated password in the create's result,
      // and a redirect would put it in a query string; so it is drawn on a page
      // of its own, `/admin/users`' reset arrangement. A JSON caller gets JSON.
      if (result.ok && result.password &&
          !/json/i.test(String(req.headers['content-type'] || ''))) {
        const made = realms.get(result.realm);
        const realmUrl = self.realmRoot(req) +
                         (made ? realms.prefixOf(made) : '') +
                         '/admin';
        self.respond(req, res, result, 'Realm created', '/admin/realms',
          '<h2>The "' + self.esc(result.realm) +
          '" realm\'s administrator, shown once</h2><table ' +
          'class="key"><tr><th>Username</th><td><code>' +
          self.esc(result.username) + '</code></td></tr></table>' +
          '<div class="secret">' + self.esc(result.password) + '</div>' +
          self.warn('<strong>Note it now.</strong> It is stored as a scrypt ' +
          'hash and cannot be shown again. It works once, at that realm\'s ' +
          'sign-in screen, where a new password must be chosen. This account ' +
          'administers the "' + self.esc(result.realm) +
          '" realm and nothing else.') +
          self.note('<a class="btn" href="' + self.esc(back) +
                    '">Back to the realm</a> ' +
          '&middot; <a href="' + self.esc(realmUrl) + '">its console</a>'),
          self.upTo('/admin/realms', 'Realm created', listView));
        log.debug("Leaving the admin trust realms action endpoint. A " +
                  "one-time page.");
        return;
      }
      self.respondToAction(req, res, back, result);
      log.debug("Leaving the admin trust realms action endpoint.");
    });

    app.post('/admin/config', function (req, res) {
      log.debug("Entering the admin configuration action endpoint.");
      const body = parseBody(req);
      // A RESET ARRIVES AS `?reset=<key>` ON THE SECTION'S OWN FORM — see the
      // comment above the button in configRow() for why the key is in the URL.
      // It is normalised into the body here rather than in configAction(),
      // because this is the shape a BROWSER posts and that function is also
      // what the management API calls with an `action` taken from its own URL.
      const reset = String(req.query.reset || '').trim();
      const result = configAction(reset
        ? { action: 'reset', key: reset, from: body.from }
        : body);
      // BACK TO THE PAGE THE FORM WAS ON. Every protocol page draws its own
      // settings and every one of those forms posts here, so this endpoint
      // serves twenty-one pages and can no longer assume it serves one. See
      // configReturnTo(), which is why an unrecognised `from` cannot become a
      // redirect to somewhere this file did not write.
      self.respondToAction(req, res, self.configReturnTo(body), result);
      log.debug("Leaving the admin configuration action endpoint.");
    });

    app.get('/admin/token-lifetimes', function (req, res) {
      log.debug("Entering the admin token lifetimes page.");
      const json = tokenLifetimesJson();
      const settings = json.settings;
      const snapshot = { tokens: json.tokens };
      const anyOverridden =
          settings.some(function (setting) { return setting.overridden; });

      const inner = self.messagesOf(req) +
        self.note('How long an access token, an ID Token and a refresh token ' +
        'issued here are good for, and how far out a clock may be before ' +
        'this service stops believing one of its own. All four are <a ' +
        'href="/admin/oauth2">configuration settings</a> and this page is a ' +
        'shorter way to the same four rows — it writes through the same ' +
        'function, so a change made here and one made there are one change.') +

        self.warn('<strong>A change applies to the NEXT token and to nothing ' +
        'already issued.</strong> A lifetime is stamped into a token as its ' +
        '<code>exp</code> claim when it is signed, so a token in a client’s ' +
        'hands cannot be shortened or extended afterwards by anything on ' +
        'this page. That is a property of a signed statement rather than a ' +
        'limitation here — to take an issued token out of circulation, ' +
        'revoke it on <a href="/admin/tokens">the tokens page</a>. Changes ' +
        'are in memory and are gone on restart; to make one stick, put it in ' +
        '<code>' +
        self.esc(process.env.CONFIG_FILE || 'env/local.js') + '</code>.') +

        self.tokenLifetimeWarnings() +

        '<h2>The six settings</h2>' +
        self.note('Every lifetime is a whole number of ' +
        '<strong>thirty-second</strong> units. That is not a formatting ' +
        'rule: these exist to be set short and watched, and below half a ' +
        'minute a token expires between the response being written and the ' +
        'client reading it, which is an hour spent debugging the wrong half. ' +
        'The clock skew is capped at <strong>300 seconds</strong> — five ' +
        'minutes is the allowance Kerberos uses here ' +
        '(<code>krb5.clockSkew</code>), and a window wider than that has ' +
        'stopped being a tolerance and become a lifetime extension nobody ' +
        'asked for.') +
        '<form method="post" action="/admin/token-lifetimes"><input ' +
        'type="hidden" name="action" ' +
        'value="set"><table><tr><th>Setting</th><th>Value</th><th>Which ' +
        'is</th><th>Per client</th><th>Source</th><th>Tokens of that kind ' +
        'held here</th></tr>' +
        settings.map(function (setting) {
          return self.tokenLifetimeRow(setting, snapshot);
        })
                .join('') +
        '</table>' +
        // The caption is a fold BELOW the button rather than a span beside it,
        // and the reason is markup rather than taste: a <details> inside a <p>
        // is invalid and every browser closes the paragraph in front of it,
        // which leaves the fold outside the form it belongs to.
        '<p><button>Save lifetimes</button></p>' +
        self.note('All four are checked before any is applied — a form that ' +
        'took two and refused the third would leave this service issuing a ' +
        'combination nobody chose.') +
        '</form>' +

        (anyOverridden
          ? '<div class="ok">' +
            self.esc(String(settings.filter(function (s) {
              return s.overridden;
            }).length)) +
            ' of the four are set here, in memory only. <form method="post" ' +
            'action="/admin/token-lifetimes" class="inline"><input ' +
            'type="hidden" name="action" value="defaults"><button ' +
            'class="secondary">Put these four back</button></form> It clears ' +
            'the override on these four only, and leaves any other setting ' +
            'alone — <a href="/admin/config">Configuration</a> has the ' +
            'reset-all.</div>'
          : self.note('None of the four is overridden: each is coming from ' +
            'its environment variable, from ' +
            '<code>' + self.esc(process.env.CONFIG_FILE || 'the ' +
                'appconfig file') +
            '</code>, or from <code>' + self.esc(config.DEFAULTS_FILE) +
            '</code> under it. The <em>Source</em> column says which.')) +

        '<h2>The clock skew is not a lifetime, and it is not the assertion ' +
        'skew either</h2>' +
        self.note('<code>oauth2.clockSkewS</code> is the allowance applied ' +
        'to <code>exp</code> and <code>nbf</code> at every place this ' +
        'service reads back a token it signed: ' +
        '<code>/oauth2/introspect</code>, UserInfo, the refresh grant, token ' +
        'exchange, the DPoP-bound access token check the four protected ' +
        'endpoints share, and the state column on every console screen that ' +
        'reports one. It never changes what goes INTO a token. It is ' +
        'deliberately a different setting from ' +
        '<code>oauth2.clientAssertionSkewS</code>, which is how far out a ' +
        '<em>client’s</em> assertion may be under RFC 7523 ' +
        '(<code>private_key_jwt</code> and <code>client_secret_jwt</code>): ' +
        'one is about somebody else’s clock and one is about this service’s, ' +
        'and a deployment wanting a strict check on one and a forgiving ' +
        'reading of the other has to be able to say so.') +

        '<h2>What is already out there</h2>' +
        self.note('Counted against the same clock the endpoints use — the ' +
        'skew above is applied here too, so a token this table calls expired ' +
        'is one <code>/oauth2/introspect</code> will report inactive. ' +
        self.esc(String(json.tokens.held)) + ' token(s) are held, of the ' +
          'most recent ' +
        self.esc(String(json.tokens.cap)) + '; ' +
        self.esc(String(json.tokens.forgotten)) +
        ' older one(s) have been forgotten. Every one of them, with its own ' +
        'expiry, is on <a href="/admin/tokens">the tokens page</a>.') +
        '<table><tr><th>Kind</th><th class="num">Issued</th><th ' +
        'class="num">Valid</th><th class="num">Expired</th><th ' +
        'class="num">Revoked</th><th class="num">Not yet valid</th><th ' +
        'class="num">No expiry stated</th></tr>' +
        (json.tokens.byKind.map(function (row) {
          return '<tr><td><code>' + self.esc(row.kind) + '</code></td>' +
            '<td class="num">' + row.issued + '</td>' +
            '<td class="num state-valid">' + row.valid + '</td>' +
            '<td class="num state-expired">' + row.expired + '</td>' +
            '<td class="num state-revoked">' + row.revoked + '</td>' +
            '<td class="num state-expired">' + row.notYetValid + '</td>' +
            '<td class="num state-none">' + row.noExpiry + '</td></tr>';
        }).join('') ||
         '<tr><td colspan="7">Nothing has been issued yet.</td></tr>') +
        '</table>' +

        '<h2>Two lifetimes this page does not set</h2>' +
        self.note('An <strong>authorization code</strong> is good for five ' +
        'minutes and is not configurable: it is redeemed within seconds of ' +
        'being issued or it is a bug in the client, and a code that could be ' +
        'made long-lived would be an invitation to build one that is. RFC ' +
        '9700 mode’s <strong>refresh idle timeout</strong> ' +
        '(<code>oauth2.refreshIdleSeconds</code>, on <a ' +
        'href="/admin/oauth2">the OAuth 2.0 / OIDC settings</a>) is a ' +
        'different question from the refresh lifetime above: it is measured ' +
        'from the last time any token in a refresh CHAIN was redeemed rather ' +
        'than from issuance, so a busy client keeps its grant indefinitely ' +
        'and a quiet one is cut off. The lifetime here is a wall the chain ' +
        'cannot be refreshed past however busy it is.') +

        self.note('The same four over JSON are at ' +
        '<code>/admin/token-lifetimes?format=json</code> and <code>GET ' +
        '/admin-api/token-lifetimes</code>; the two actions on this page are ' +
        '<code>POST /admin-api/token-lifetimes/set</code> and ' +
        '<code>/defaults</code>. They are also four ordinary rows of ' +
        '<code>GET /admin-api/config</code>.');

      self.respond(req, res, json, 'Token lifetimes', '/admin/token-lifetimes',
                   inner);
      log.debug("Leaving the admin token lifetimes page.");
    });

    app.post('/admin/token-lifetimes', function (req, res) {
      log.debug("Entering the admin token lifetimes action endpoint.");
      const body = parseBody(req);
      const result = tokenLifetimesAction(body);
      self.respondToAction(req, res, '/admin/token-lifetimes', result);
      log.debug("Leaving the admin token lifetimes action endpoint.");
    });

    app.get('/admin/saml-assertions', function (req, res) {
      log.debug("Entering the admin SAML assertions page.");
      const json = samlAssertionsJson();
      const settings = json.settings;
      const snapshot = { assertions: { byKind: json.assertionsIssued.byKind } };
      const anyOverridden =
          settings.some(function (setting) { return setting.overridden; });

      const inner = self.messagesOf(req) +
        self.note('How long an assertion issued here is valid for, and how ' +
        'far its window is widened at each end to allow for a relying party ' +
        'whose clock disagrees with this one. All three are <a ' +
        'href="/admin/config">configuration settings</a>: the two lifetimes ' +
        'are also drawn on <a href="/admin/saml2">SAML 2.0</a> and <a ' +
        'href="/admin/saml11">SAML 1.1</a>, one on each, and the skew on ' +
        'both. This page is a shorter way to the same rows and the only ' +
        'place all three are visible at once — it writes through the same ' +
        'function, so a change made here and one made there are one change.') +

        self.warn('<strong>A change applies to the NEXT assertion and to ' +
        'nothing already issued.</strong> A validity window is stamped into ' +
        'an assertion as <code>Conditions/NotBefore</code> and ' +
        '<code>NotOnOrAfter</code> when it is signed, so an assertion ' +
        'already in a relying party&rsquo;s hands cannot be shortened or ' +
        'extended afterwards by anything on this page. Changes are in memory ' +
        'and are gone on restart; to make one stick, put it in <code>' +
        self.esc(process.env.CONFIG_FILE || 'env/local.js') +
        '</code>.') +

        self.samlAssertionWarnings() +

        '<h2>The sixteen settings</h2>' +
        self.note('<strong>Ten of these eleven are DEFAULTS, not ' +
        'decisions.</strong> Each is what an application gets when its own ' +
        'entry says nothing, and the <em>Per-application</em> column names ' +
        'the attribute that overrides it — set that on an application under ' +
        '<a href="/admin/applications">Applications</a> and this row stops ' +
        'governing it. The eleventh, the clock skew, has no per-application ' +
        'form: it is a fact about the clocks in the estate this service ' +
        'issues into, decided once. <strong>They moved here from the two ' +
        'identity provider pages on 2026-08-27</strong>, because those pages ' +
        'configure this service as an identity provider and these describe ' +
        'what it does for an application nobody has configured.') +
        self.note('The two lifetimes are in <strong>minutes</strong> and the ' +
        'skew and the two artifact lifetimes are in ' +
        '<strong>seconds</strong>, which is not a formatting accident: a ' +
        'lifetime is set to a number of minutes to watch an assertion go ' +
        'stale, and a skew is a handful of seconds covering the difference ' +
        'between two machines. The skew is capped at <strong>300 ' +
        'seconds</strong> for the reason <code>oauth2.clockSkewS</code> is — ' +
        'five minutes is what Kerberos allows here ' +
        '(<code>krb5.clockSkew</code>), and wider than that the window has ' +
        'stopped being a tolerance and become a lifetime nobody chose.') +
        '<form method="post" action="/admin/saml-assertions">' +
        '<input type="hidden" name="action" value="set">' +
        ['saml2', 'saml11', 'wsfed', ''].map(function (profile) {
          const mine = settings.filter(function (setting) {
            const row = samlAssertionRowFor(setting.key);
            return row && row.profile === profile;
          });
          if (!mine.length) return '';
          // The heading is the profile's own name, and the empty profile is the
          // skew — which is headed by what it IS rather than by a profile it
          // does not belong to, because putting it under either would be the
          // claim this page spends a section denying.
          const heading = profile === 'saml2' ? 'SAML 2.0'
                        : (profile === 'saml11' ? 'SAML 1.1'
                        : (profile === 'wsfed' ? 'WS-Federation' :
                           'Every profile'));
          return '<h3>' + self.esc(heading) + '</h3>' +
            '<table><tr><th>Setting</th><th>Value</th><th>Which is</th>' +
            '<th>Per-application</th><th>Source</th>' +
            '<th>Assertions held</th></tr>' +
            mine.map(function (setting) {
              return self.samlAssertionSettingRow(setting, snapshot);
            }).join('') +
            '</table>';
        }).join('') +
        '<p><button>Save assertion settings</button></p>' +
        self.note('All sixteen are checked before any is applied — a form ' +
        'that took eleven and refused the twelfth would leave this service ' +
        'issuing a combination nobody chose.') +
        '</form>' +

        (anyOverridden
          ? '<div class="ok">' +
            self.esc(String(settings.filter(function (s) {
              return s.overridden;
            }).length)) +
            ' of the sixteen are set here, in memory only. <form ' +
            'method="post" action="/admin/saml-assertions" ' +
            'class="inline"><input type="hidden" name="action" ' +
            'value="defaults"><button class="secondary">Put these sixteen ' +
            'back</button></form> It clears the override on these sixteen ' +
            'only, and leaves any other setting alone — <a ' +
            'href="/admin/config">Configuration</a> has the reset-all.</div>'
          : self.note('None of the sixteen is overridden: each is coming ' +
            'from its environment variable, from ' +
            '<code>' + self.esc(process.env.CONFIG_FILE || 'the ' +
                'appconfig file') +
            '</code>, or from <code>' + self.esc(config.DEFAULTS_FILE) +
            '</code> under it. The <em>Source</em> column says which.')) +

        '<h2>What the skew actually does to an assertion</h2>' +
        self.note('It is added at BOTH ends of the window and to neither ' +
        'instant that states when something happened: <code>NotBefore</code> ' +
        'is backdated by it and <code>NotOnOrAfter</code> is extended by it, ' +
        'while <code>IssueInstant</code> and the authentication instant are ' +
        'left at the real time &mdash; backdating those would be a lie about ' +
        'an event rather than an allowance about a clock. So the window an ' +
        'assertion states is its lifetime <em>plus twice</em> the skew, ' +
        'which is why this page reports <code>saml2WindowS</code> and ' +
        '<code>saml11WindowS</code> beside the settings themselves. At the ' +
        'default 0 the documents are byte-for-byte what this service issued ' +
        'before the setting existed.') +
        '<table><tr><th>Profile</th><th>NotBefore</th><th>NotOnOrAfter</th>' +
        '<th class="num">Stated window</th></tr>' +
        SAML_ASSERTION_SETTINGS.filter(function (row) { return row.kind; })
                               .map(function (row) {
          const skew = samlAssertionSeconds('saml.clockSkewS');
          const lifetime = samlAssertionSeconds(row.key);
          return '<tr><td>' + self.esc(row.kind) + '</td>' +
            '<td><code>now' +
            (skew ? ' &minus; ' + self.esc(String(skew)) + 's' : '') +
            '</code></td><td><code>now ' +
            '+ ' + self.esc(self.humanSeconds(lifetime)) +
              (skew ? ' + ' + self.esc(String(skew)) + 's' : '') +
              '</code></td>' +
            '<td class="num">' +
            self.esc(self.humanSeconds(lifetime + 2 * skew)) +
            '</td></tr>';
        }).join('') +
        '</table><h2>It reaches four protocols, and it is not the skew this ' +
        'service reads with</h2>' +
        self.note('<strong>Four.</strong> WS-Trust and WS-Federation build ' +
        'their assertions with the same two functions the browser profiles ' +
        'use, so both settings above reach them without either module ' +
        'knowing these exist. A WS-Federation sign-in carries a SAML 1.1 ' +
        'assertion, so it is <code>saml11.assertionLifetimeMin</code> that ' +
        'governs it. What those two wrap the assertion in — WS-Trust&rsquo;s ' +
        '<code>wsu:Lifetime</code> and the equivalent in a WS-Federation ' +
        'response — states the LIFETIME without the skew, which is the ' +
        'conservative reading and is deliberate: the envelope describes what ' +
        'was asked for and the assertion states what it is actually valid ' +
        'for.') +
        self.note('<strong>And it is not ' +
        '<code>oauth2.clockSkewS</code>.</strong> That one is the allowance ' +
        'applied wherever this service READS something back — including an ' +
        'inbound federation partner&rsquo;s SAML assertion at ' +
        '<code>/federation/acs/{id}</code>, which applies it to exactly the ' +
        'two attributes this page writes. This one is what goes INTO a ' +
        'document this service issues. One is about somebody else&rsquo;s ' +
        'clock and one is about how much of somebody else&rsquo;s clock this ' +
        'service pays for in advance, and a deployment wanting a strict ' +
        'reading and a forgiving issuance has to be able to say so. The ' +
        'reading tolerance is on <a href="/admin/token-lifetimes">Token ' +
        'lifetimes</a>.') +

        '<h2>What is already out there</h2>' +
        self.note('Counted against this service&rsquo;s own clock with no ' +
        'allowance applied — the skew above is written into an assertion ' +
        'rather than applied when one is read here, so an assertion this ' +
        'calls expired is one whose stated <code>NotOnOrAfter</code> has ' +
        'passed. ' + self.esc(String(json.assertionsIssued.held)) + ' ' +
        'artifact(s) are held, of the most ' +
        'recent ' + self.esc(String(json.assertionsIssued.cap)) + '; ' +
        self.esc(String(json.assertionsIssued.forgotten)) + ' older one(s) ' +
        'have been forgotten. Every one of them is on <a ' +
        'href="/admin/tokens">the tokens page</a>, which draws assertions ' +
        'beside the JWTs and the Kerberos tickets.') +
        '<table><tr><th>Profile</th><th class="num">Issued</th><th ' +
        'class="num">Valid</th><th class="num">Expired</th><th ' +
        'class="num">No expiry stated</th></tr>' +
        (json.assertionsIssued.byKind.map(function (row) {
          return '<tr><td><code>' + self.esc(row.kind) + '</code></td>' +
            '<td class="num">' + row.issued + '</td>' +
            '<td class="num state-valid">' + row.valid + '</td>' +
            '<td class="num state-expired">' + row.expired + '</td>' +
            '<td class="num state-none">' + row.noExpiry + '</td></tr>';
        }).join('')) +
        '</table>' +

        '<h2>Two things about a window this page does not set</h2>' +
        self.note('A <strong>SAML artifact</strong> is good for ' +
        '<code>saml2.artifactTtlS</code> and is a different clock entirely — ' +
        'it governs how long an artifact can be RESOLVED for, not how long ' +
        'the assertion it resolves to is valid. And the ' +
        '<strong>session</strong> behind an assertion has its own lifetime: ' +
        'an assertion that has expired does not end the sign-on session that ' +
        'produced it, which is why a relying party refusing a stale ' +
        'assertion can be sent straight back here and get a fresh one with ' +
        'no sign-in screen.') +

        self.note('The same three over JSON are at ' +
        '<code>/admin/saml-assertions?format=json</code> and <code>GET ' +
        '/admin-api/saml-assertions</code>; the two actions on this page are ' +
        '<code>POST /admin-api/saml-assertions/set</code> and ' +
        '<code>/defaults</code>. They are also three ordinary rows of ' +
        '<code>GET /admin-api/config</code>.');

      self.respond(req, res, json, 'SAML assertions', '/admin/saml-assertions',
                   inner);
      log.debug("Leaving the admin SAML assertions page.");
    });

    app.post('/admin/saml-assertions', function (req, res) {
      log.debug("Entering the admin SAML assertions action endpoint.");
      const body = parseBody(req);
      const result = samlAssertionsAction(body);
      self.respondToAction(req, res, '/admin/saml-assertions', result);
      log.debug("Leaving the admin SAML assertions action endpoint.");
    });

    app.get('/admin/scim', function (req, res) {
      log.debug("Entering the admin SCIM page.");
      const json = scimJson(req);
      const counters = json.counters;

      // The `<div class="tiles">` around them was missing since this page was
      // written, so its six tiles were six full-width blocks down the page
      // where every other page here has one row. Nothing failed and nothing
      // could have shown it: `.tile` draws correctly on its own, and it is the
      // CONTAINER that makes a row.
      const tiles = '<div class="tiles">' +
        self.tile(counters.total, 'SCIM requests') +
        self.tile(counters.ok, 'answered') +
        self.tile(counters.failed, 'refused') +
        self.tile(json.store ? json.store.userCount : '—',
                  'people in the directory') +
        self.tile(json.store ? json.store.groupCount : '—', 'groups') +
        self.tile(json.store ? json.store.entryCount + ' / ' +
                  json.store.maxEntries :
                  '—', 'entries ' +
            '/ max') +
        '</div>';

      const operationRows = counters.operations.map(function (row) {
        return '<tr><td><code>' + self.esc(row.method) + '</code> ' +
               self.esc(row.label) +
          '</td><td ' +
          'class="num">' + row.count + '</td>' +
          '<td class="sub">' + self.esc(row.what) + '</td></tr>';
      }).join('');

      const typeRows = counters.resourceTypes.map(function (row) {
        return '<tr><td><code>' + self.esc(row.resourceType) + '</code></td>' +
          '<td class="num">' + row.count + '</td></tr>';
      }).join('');

      const statusRows = Object.keys(counters.byStatus).sort()
        .map(function (code) {
        return '<tr><td><code>' + self.esc(code) + '</code></td><td ' +
          'class="num">' +
          counters.byStatus[code] + '</td></tr>';
      }).join('') ||
        '<tr><td colspan="2">Nothing has been answered yet.</td></tr>';

      const scimTypeRows = Object.keys(counters.byScimType)
                                 .sort()
                                 .map(function (name) {
        return '<tr><td><code>' + self.esc(name) + '</code></td><td ' +
          'class="num">' +
          counters.byScimType[name] + '</td></tr>';
      }).join('') ||
        '<tr><td colspan="2">Nothing has been refused yet, which on ' +
                     'a server this permissive usually means nothing has ' +
                     'tried the error paths.</td></tr>';

      const endpointRows = json.endpoints.map(function (row) {
        return '<tr><td><code>' + self.esc(row.method) +
               '</code></td><td><code>' +
          self.esc(row.path) + '</code></td><td class="sub">' +
          self.esc(row.what) +
          '</td></tr>';
      }).join('');

      const negativeRows = json.reachableNegatives.map(function (row) {
        return '<tr><td>' + self.esc(row.what) + '</td><td>' +
               self.esc(row.answer) +
               '</td></tr>';
      }).join('');

      function mappingTable(rows) {
        log.debug("Entering mappingTable().");
        log.debug("Leaving mappingTable().");
        return '<table><tr><th>SCIM</th><th>LDAP attribute</th><th>How</th>' +
          '<th>Defined by</th></tr>' +
          rows.map(function (row) {
            return '<tr><td><code>' + self.esc(row.scim) + '</code>' +
              (row.required ? ' <span class="state-valid">required</span>' :
               '') +
              (row.extension ?
               ' <span class="sub">(enterprise extension)</span>' :
               '') +
              '</td>' +
              '<td><code>' + self.esc(row.ldap) + '</code></td>' +
              '<td>' + self.esc(row.kind) +
              (row.readOnly ? ', read-only' : '') +
              (row.note ? '<div class="sub">' + self.esc(row.note) + '</div>' :
               '') +
              '</td><td ' +
              'class="sub">' + self.esc(row.schema) + '</td></tr>';
          }).join('') + '</table>';
      }

      const inner = self.messagesOf(req) +
        (!json.installed
          ? '<div class="err"><strong>SCIM is not loaded in this ' +
            'process.</strong> The module registers no routes here, so there ' +
            'is nothing to report. Everything else on this console is ' +
            'unaffected.</div>'
          : '') +

        // WHERE THE TRAFFIC QUESTION IS ANSWERED. This page keeps the headline
        // counts — a page about a surface with no evidence anything ever called
        // it is a page about a hypothesis — and everything past them is over
        // there: who is calling, how long it took, what came back, and the last
        // fifty requests individually. Both are drawn from ONE set of counters
        // in `admin_stats.js` through two functions, so there is no second
        // tally to disagree with this one.
        self.note('The counters below are the headline. <strong><a ' +
        'href="/admin/scim/monitor">Monitoring &rarr; SCIM ' +
        'metrics</a></strong> is the traffic in full: who is calling &mdash; ' +
        'one row per authenticated principal &mdash; how long each kind of ' +
        'call took, how many bytes went back, what the failures were, and ' +
        'the last fifty requests one by one. Both pages read one set of ' +
        'numbers, so they cannot disagree; this page is about what the ' +
        'surface IS, and that one about what it is DOING.') +
        (json.installed && !json.enabled
          ? self.warn('<strong>SCIM is turned off</strong> ' +
            '(<code>scim.enabled</code>). The routes are still registered ' +
            'and answer <code>501</code> rather than <code>404</code>, ' +
            'because the feature being off and the URL being wrong are ' +
            'different sentences to a client. Turn it back on in the ' +
            'settings at the foot of this page.')
          : '') +

        self.note('SCIM 2.0 — RFC 7642, 7643 and 7644 — at <code>' +
        self.esc(json.baseUrl || '/scim/v2') + '</code>. It is the only ' +
        'protocol family here whose purpose is to WRITE, and what it writes ' +
        'is the embedded LDAP directory: there is no second store and no ' +
        'cache. A <code>POST /scim/v2/Users</code> and an ' +
        '<code>ldapadd</code> create the same entry, so somebody provisioned ' +
        'over SCIM appears on <a href="/admin/users">Users</a>, gains ' +
        'whatever attributes <a href="/admin/vc">Credential claims</a> ' +
        'selects, and lands in whatever group a client puts them in on <a ' +
        'href="/admin/groups">Groups</a>.') +

        self.warn('<strong>These endpoints create and delete accounts, and ' +
        'they are the one surface in this service that requires a credential' +
        (json.authentication && !json.authentication.required
          ? ' — except that it is currently turned off here, so ' +
            'right now they do not'
          : '') + '.</strong> Almost nothing is checked about it: every ' +
        'scheme below is permissive, so this is a turnstile rather than a ' +
        'lock. What it buys is that a client\'s 401, 403 and ' +
        'challenge-response paths can be exercised at all. <strong>And ' +
        '<code>active: false</code> disables the account</strong> (since ' +
        '2026-09-17) — it writes the password-policy lock ' +
        '<code>pwdAccountLockedTime</code>, every door then refuses the ' +
        'person, and everything they held is ended, as the Disable button ' +
        'on their <code>/admin/users</code> page does.') +

        tiles +

        (json.authentication ?
         self.authenticationSection(json.authentication, counters) : '') +

        '<h2>Operations</h2>' +
        self.note('Every operation this server implements, including the ' +
        'ones nothing has used yet — a table listing only what has happened ' +
        'would answer &ldquo;does this support PATCH?&rdquo; by omission. ' +
        '<strong>The column does not tally</strong>, on purpose: one ' +
        '<code>Bulk</code> carrying five creates is one bulk AND five ' +
        'creates, because each of the five really is performed.') +
        '<table><tr><th>Operation</th><th class="num">Count</th><th>What it ' +
        'is</th></tr>' +
        operationRows + '</table>' +

        '<h2>By resource type</h2>' +
        '<table><tr><th>Resource type</th><th class="num">Count</th></tr>' +
        typeRows +
        '</table>' +

        '<h2>What went back</h2>' +
        self.note('The HTTP status of every answer, and the ' +
        '<code>scimType</code> of every refusal (RFC 7644 section 3.12). ' +
        '<code>(none)</code> is a refusal that carried no such code — a 404 ' +
        'has none — and is counted rather than dropped, so the two failure ' +
        'tables agree with each other.') +
        '<div class="tiles" style="align-items:flex-start">' +
        '<div><table><tr><th>Status</th><th class="num">Count</th></tr>' +
        statusRows +
        '</table></div>' +
        '<div><table><tr><th>scimType</th><th class="num">Count</th></tr>' +
        scimTypeRows + '</table></div></div>' +

        (json.identifiers
          ? '<h2>The <code>id</code> is the DN</h2>' +
            self.note(self.esc(json.identifiers.why)) +
            self.note('For example: <code>' +
                      self.esc(json.identifiers.example) +
            '</code>')
          : '') +

        (endpointRows
          // NOT "Endpoints": that heading is the realm's addresses, which
          // `respond()` draws at the top of every Protocols page. This table is
          // what each operation DOES, by method and path under `/scim/v2`.
          ? '<h2>What each operation does</h2><table><tr><th>Method</th>' +
            '<th>Path</th><th>' +
            'What</th></tr>' + endpointRows + '</table>'
          : '') +

        (json.doesNotDo.length
          ? '<h2>What it deliberately does not do</h2><ul>' +
            json.doesNotDo.map(function (text) {
              return self.bullet(self.esc(text));
            }).join('') + '</ul>'
          : '') +

        (negativeRows
          ? '<h2>Things you can make fail</h2>' +
            self.note('A permissive server is hard to write error handling ' +
            'against, so these are here on purpose — the same device as the ' +
            'reserved password <code>invalid</code> everywhere else in this ' +
            'service.') +
            '<table><tr><th>Do this</th><th>Get this</th></tr>' + negativeRows +
            '</table>'
          : '') +

        '<h2>The User mapping</h2>' +
        self.note('Which LDAP attribute each SCIM member is. The attribute ' +
        'spellings are the same catalogue <a href="/admin/vc">Credential ' +
        'claims</a> and <a href="/admin/claims">Custom claims</a> read, ' +
        'checked against it at startup rather than copied — four ' +
        'independently maintained lists of spellings is how one of them ' +
        'comes to be quietly wrong.') +
        mappingTable(json.mapping.user) +

        '<h2>The Group mapping</h2>' +
        mappingTable(json.mapping.group) +

        // THIS PAGE USED TO SAY IT HAD NO CONTROLS, and the sentence it said it
        // in was the one every other page here cited: "a form here would be a
        // second door to one setting". What that argument was actually
        // protecting is the ONE-STORE rule, and the form below does not break
        // it — it is `configSection()`, posting to the same action against the
        // same override map as /admin/config, which is the arrangement
        // /admin/token-lifetimes established. What has changed is only which
        // page draws the door.
        self.configFormsFor('/admin/scim') +
        self.note('These are the settings; everything else on this page is a ' +
        'reading. What SCIM has WRITTEN is not configuration at all — it ' +
        'went into the embedded directory, so it is on <a ' +
        'href="/admin/users">Users</a> and <a ' +
        'href="/admin/groups">Groups</a>.') +

        self.note('<a href="/scim">What this is, for a person</a> &middot; ' +
        '<a href="/admin/scim/monitor">what it has actually been asked to ' +
        'do</a> &middot; <a href="/admin/scim?format=json">this page as ' +
        'JSON</a> &middot; <a href="/admin-api/scim">the same over the ' +
        'management API</a> &middot; <a href="/admin/ldap/service">the ' +
        'directory it writes into</a>');

      self.respond(req, res, json, 'SCIM 2.0', '/admin/scim', inner);
      log.debug("Leaving the admin SCIM page.");
    });

    app.get('/admin/scim/monitor', function (req, res) {
      log.debug("Entering the admin SCIM monitor page.");
      const json = scimMonitorJson(req);
      const c: any = json.counters;

      const tiles = '<div class="tiles">' +
        self.tile(c.calls, 'API calls') +
        self.tile(c.ok, 'successful') +
        self.tile(c.failed, 'failed') +
        self.tile(c.successRate === null ? '—' : c.successRate + '%',
                  'success rate') +
        self.tile(c.authentication.distinct, 'clients') +
        self.tile(c.latency.averageMs === null ? '—' : c.latency.averageMs +
                  ' ms',
                  'average') +
        '</div>';

      // THE BREAKDOWN BY CALL TYPE, which is the table this page exists for.
      // Every operation the server implements is drawn INCLUDING the ones at
      // zero — the vocabulary is admin_stats.js's SCIM_OPERATIONS, so an
      // operation cannot be performed and go unreported nor be reported and
      // never occur.
      const operationRows = c.operations.map(function (row) {
        return '<tr><td>' +
          (row.method ? '<code>' + self.esc(row.method) + '</code> ' : '') +
          self.esc(row.label) + '</td>' +
          '<td class="num">' + row.count + '</td>' +
          '<td class="num">' + row.ok + '</td>' +
          '<td class="num">' + row.failed + '</td>' +
          '<td class="num">' + (row.averageMs === null ? '—' :
                                row.averageMs) + '</td><td ' +
          'class="num">' + (row.maxMs === null ? '—' : row.maxMs) + '</td>' +
          '<td class="num">' + row.bytes + '</td>' +
          '<td class="sub">' + self.esc(row.what) + '</td></tr>';
      }).join('');

      const resourceRows = c.resourceTypes.map(function (row) {
        return '<tr><td><code>' + self.esc(row.resourceType) + '</code></td>' +
          '<td class="num">' + row.count + '</td></tr>';
      }).join('');

      // THE CLIENTS. Sorted busiest first by the snapshot, because that and
      // "most recent" are the two orders somebody reading a traffic page wants
      // and alphabetical is neither.
      const clientRows = c.clients.map(function (row) {
        return '<tr><td>' + self.shortened(row.principal, 40) + '</td>' +
          '<td>' + (row.kind === 'application'
            ? 'application <span class="sub">(a client_id)</span>'
            : 'identity') + '</td>' +
          '<td>' + row.schemes.map(function (s) {
            return '<code>' + self.esc(s) + '</code>';
          }).join(' ') + '</td>' +
          '<td class="num">' + row.calls + '</td>' +
          '<td class="num">' + row.ok + '</td>' +
          '<td class="num">' + row.failed + '</td>' +
          '<td>' + self.esc(row.lastOperation || '—') + ' <span class="sub">' +
          self.esc(row.lastStatus || '') + '</span></td>' +
          '<td class="sub">' + self.esc(self.whenText(row.firstAt)) + '</td>' +
          '<td class="sub">' + self.esc(self.whenText(row.lastAt)) +
          '</td></tr>';
      }).join('') || '<tr><td colspan="9">Nothing has authenticated yet. ' +
        (json.authRequired
          ? 'Every call to <code>/scim/v2</code> needs a credential, so this ' +
            'table filling up is the first sign a provisioning client has ' +
            'been configured at all. <strong>The gate being on is not the ' +
            'same as the credential being checked</strong>: what ' +
            '<code>global.mode</code> decides is whether the answer is ' +
            'VERIFIED, and the turnstile is there in both modes.'
          : '<strong>The SCIM gate is off in this build</strong> ' +
            '(<code>mode.gatesScim()</code>), so callers are not asked for a ' +
            'credential and are counted as anonymous rather than as clients. ' +
            'This table stays empty however much traffic there is until it ' +
            'is on.') +
        '</td></tr>';

      // The scheme table, with the declared vocabulary first and anything
      // counted under a name it does not declare after it.
      const declared = {};
      const schemeRows = json.schemes.map(function (row) {
        declared[row.id] = true;
        return self.scimSchemeRow(row.id, row.name, row.enabled,
                                  c.authentication.byScheme[row.id] || 0,
                                  self.esc(row.spec || ''));
      }).join('') +
      self.scimSchemeRow('anonymous', '', null,
                         c.authentication.byScheme.anonymous || 0,
        'Nothing authenticated the caller: a discovery endpoint, which is ' +
        'open unless <code>scim.authDiscovery</code> says otherwise.') +
      self.scimSchemeRow('refused', '', null,
                         c.authentication.byScheme.refused || 0,
        '<strong>The gate turned the caller away.</strong> These are counted ' +
        'here and deliberately NOT attributed to a client, even when the ' +
        'credential carried a name.') +
      Object.keys(c.authentication.byScheme).sort().filter(function (id) {
        return !declared[id] && id !== 'anonymous' && id !== 'refused';
      }).map(function (id) {
        return self.scimSchemeRow(id, '', null, c.authentication.byScheme[id],
          'Counted under a name the surface does not declare. Shown rather ' +
          'than dropped, so the column still adds up.');
      }).join('');

      const statusClassRows = Object.keys(c.byStatusClass).sort()
        .map(function (k) {
        return '<tr><td><code>' + self.esc(k) + '</code></td><td class="num">' +
          c.byStatusClass[k] + '</td></tr>';
      }).join('') ||
        '<tr><td colspan="2">Nothing has been answered yet.</td></tr>';

      const statusRows = Object.keys(c.byStatus).sort().map(function (code) {
        return '<tr><td><code>' + self.esc(code) + '</code></td><td ' +
          'class="num">' +
          c.byStatus[code] + '</td></tr>';
      }).join('') ||
        '<tr><td colspan="2">Nothing has been answered yet.</td></tr>';

      const scimTypeRows = Object.keys(c.byScimType).sort()
        .map(function (name) {
        return '<tr><td><code>' + self.esc(name) + '</code></td><td ' +
          'class="num">' +
          c.byScimType[name] + '</td></tr>';
      }).join('') ||
        '<tr><td colspan="2">Nothing has been refused yet.</td></tr>';

      const recentRows = c.recent.map(function (row) {
        return '<tr><td class="sub">' + self.esc(self.whenText(row.at)) +
               '</td>' +
          '<td>' +
          (row.method ? '<code>' + self.esc(row.method) + '</code> ' : '') +
          self.esc(row.operation) + '</td>' +
          '<td><code>' + self.esc(row.resourceType) + '</code></td>' +
          '<td>' +
          (row.ok ? '<span class="state-valid">' + self.esc(row.status) +
           '</span>'
                           : '<span class="state-revoked">' +
                             self.esc(row.status) +
                             '</span>') +
          (row.scimType ? ' <span class="sub">' + self.esc(row.scimType) +
           '</span>' :
           '') +
          '</td>' +
          '<td>' + (row.principal ? self.shortened(row.principal, 24)
                                  : '<span class="sub">' +
                                    self.esc(row.scheme) +
                                    '</span>') +
          '</td>' +
          '<td class="num">' + (row.ms === null ? '—' : row.ms) + '</td>' +
          '<td class="num">' + row.bytes + '</td></tr>';
      }).join('') ||
        '<tr><td colspan="7">Nothing has been called yet.</td></tr>';

      const inner = self.messagesOf(req) +
        (!json.installed
          ? '<div class="err"><strong>SCIM is not loaded in this ' +
            'process.</strong> The module registers no routes here, so there ' +
            'is no traffic to report and the zeroes below mean "no such ' +
            'endpoint" rather than "no calls". Everything else on this ' +
            'console is unaffected.</div>'
          : '') +
        (json.installed && !json.enabled
          ? self.warn('<strong>SCIM is turned off</strong> ' +
            '(<code>scim.enabled</code>), so every call under ' +
            '<code>/scim/v2</code> is answered <code>501</code> — and IS ' +
            'COUNTED HERE, because it is a request this service answered. A ' +
            'page whose totals went flat while a client kept calling would ' +
            'be hiding the very thing somebody came to this page to find. ' +
            'Turn it back on at <a href="/admin/scim">Protocols &rarr; ' +
            'SCIM</a>.')
          : '') +

        self.note('<strong>What the provisioning surface has actually been ' +
        'asked to do</strong>, since this process started at <code>' +
        self.esc(self.whenText(c.since)) + '</code>. Every request the SCIM ' +
        'implementation had an opinion about is counted here — including the ' +
        'ones its own authentication gate refused, because a ' +
        '<code>401</code> is a call this service answered and a total that ' +
        'quietly omitted them would be smaller than the access log for no ' +
        'stated reason. The counters are IN MEMORY and per trust realm (this ' +
        'is <code>' + self.esc(c.realm.name || c.realm.id ||
        'the default realm') + '</code>): they die with the process, because ' +
        'these are observations and this service persists nothing it ' +
        'observes. The durable record of what SCIM was ASKED to do is the <a ' +
        'href="/admin/audit">audit log</a>, which has the actor and the ' +
        'target as well as the count. What the surface IS — the schemes, the ' +
        'endpoints, the attribute mapping and the eighteen settings — is <a ' +
        'href="/admin/scim">Protocols &rarr; SCIM</a>; both pages read one ' +
        'set of counters, so they cannot disagree.') +

        tiles +

        '<h2>By API call type</h2>' +
        self.note('Every operation this server implements, <strong>including ' +
        'the ones nothing has called</strong> — a table listing only what ' +
        'has happened would answer &ldquo;does this support PATCH?&rdquo; by ' +
        'omission. <strong>The count column does not tally with the call ' +
        'total, on purpose</strong>: one <code>Bulk</code> carrying five ' +
        'creates is one <code>bulk</code> AND five <code>create</code>s, ' +
        'because each of the five really is performed. A duration is shown ' +
        'as <code>&mdash;</code> rather than <code>0</code> where nothing ' +
        'was measured, because an average over no samples is absent and not ' +
        'zero — a column of <code>0.0</code> would read as a server ' +
        'answering instantly.') +
        '<table><tr><th>Operation</th><th class="num">Calls</th>' +
        '<th class="num">OK</th><th class="num">Failed</th>' +
        '<th class="num">Avg ms</th><th class="num">Max ms</th>' +
        '<th class="num">Bytes out</th><th>What it is</th></tr>' +
        operationRows + '</table>' +

        '<h2>By resource type</h2>' +
        '<table><tr><th>Resource type</th><th class="num">Calls</th></tr>' +
        resourceRows + '</table>' +

        '<h2>Clients</h2>' +
        self.note('<strong>A client here is an authenticated principal, not ' +
        'a connection.</strong> SCIM is stateless HTTP — there is no ' +
        'session, no registration and nothing to be connected — so the only ' +
        'honest reading of &ldquo;how many clients&rdquo; is how many ' +
        'distinct names have successfully authenticated since this process ' +
        'started. The figure never goes down: a provisioning client that has ' +
        'stopped calling is indistinguishable from one that is between ' +
        'calls. The name is whatever the credential carried — a username for ' +
        'the five user-bearing schemes, a <code>client_id</code> for a ' +
        'Bearer token minted for an application (shown as ' +
        '<em>application</em>), and an RFC 4514 subject DN for a client ' +
        'certificate. <strong>A caller the gate refused is not a client and ' +
        'is not in this table</strong>, even when the credential carried a ' +
        'name: Basic and Digest both put one on the wire, and attributing ' +
        'traffic to an identity this service declined to believe is the one ' +
        'mistake this page could make that would matter. Those are the ' +
        '<code>refused</code> row below.') +
        '<div class="tiles">' +
        self.tile(c.authentication.distinct, 'distinct clients') +
        self.tile(c.authentication.identities, 'people') +
        self.tile(c.authentication.applications, 'applications') +
        self.tile(c.authentication.anonymous, 'anonymous calls') +
        self.tile(c.authentication.refused, 'refused at the gate') +
        '</div>' +
        (c.authentication.capped
          ? self.warn('<strong>The client table stopped growing at ' +
            c.authentication.cap + ' names.</strong> Every call is still ' +
            'counted in every total above; it is only this breakdown that is ' +
            'capped, because the principal is whatever the caller typed — ' +
            'Basic here accepts any username — and an unbounded table is a ' +
            'store somebody else decides the size of. The cap is said out ' +
            'loud rather than letting the page under-report quietly.')
          : '') +
        '<table><tr><th>Principal</th><th>Kind</th><th>Schemes</th><th ' +
        'class="num">Calls</th><th class="num">OK</th><th ' +
        'class="num">Failed</th><th>Last</th><th>First seen</th><th>Last ' +
        'seen</th></tr>' +
        clientRows + '</table>' +

        '<h2>By authentication scheme</h2>' +
        self.note('All six schemes RFC 7644 section 2 names, whether each is ' +
        'switched on, and how many calls came in over it — the ones at zero ' +
        'included, because a scheme that is OFF and unused is the most ' +
        'useful row here for somebody asking why a client cannot get in. The ' +
        'list is <code>scim_auth.js</code>\'s own, the same table the ' +
        '<code>WWW-Authenticate</code> challenge and the ' +
        '<code>ServiceProviderConfig</code> are built from, so this cannot ' +
        'name a scheme a client would not be offered.') +
        '<table><tr><th>Scheme</th><th>Enabled</th><th class="num">Calls</th>' +
        '<th>Notes</th></tr>' + schemeRows + '</table>' +

        '<h2>What went back</h2>' +
        self.note('The status class, the exact status of every answer, and ' +
        'the <code>scimType</code> of every refusal (RFC 7644 section 3.12). ' +
        '<code>(none)</code> is a refusal that carried no such code — a ' +
        '<code>404</code> has none — and is counted rather than dropped, so ' +
        'the two failure tables agree with each other.') +
        '<div class="tiles" style="align-items:flex-start">' +
        '<div><table><tr><th>Class</th><th class="num">Count</th></tr>' +
        statusClassRows + '</table></div>' +
        '<div><table><tr><th>Status</th><th class="num">Count</th></tr>' +
        statusRows + '</table></div>' +
        '<div><table><tr><th>scimType</th><th class="num">Count</th></tr>' +
        scimTypeRows + '</table></div></div>' +

        '<h2>Volume</h2>' +
        '<div class="tiles">' +
        self.tile(c.bytesOut, 'bytes returned') +
        self.tile(c.latency.maxMs, 'slowest call (ms)') +
        self.tile(c.latency.totalMs, 'total time (ms)') +
        '</div>' +
        // THE TWO INSTANTS ARE A SENTENCE AND NOT TWO MORE TILES. A tile draws
        // its value in the headline face, and a timestamp there is twenty-four
        // characters set like a three-digit count — it reads as the most
        // important figure on the page and is the least. Every other tile in
        // this console holds a number for the same reason.
        self.note('First call <code>' + self.esc(self.whenText(c.firstAt)) +
                  '</code>, last ' +
        'call <code>' + self.esc(self.whenText(c.lastAt)) + '</code>. ' +
        '<code>Bytes returned</code> is the SCIM payload this service ' +
        'wrote, headers excluded. It answers one question and it is a common ' +
        'one: whether a client is listing the whole directory on every poll. ' +
        'The time figures are a SUM and a maximum rather than a running ' +
        'mean, so any other statistic can still be computed from them.') +

        '<h2>The last ' + c.recentCap + ' calls</h2>' +
        self.note('Newest first. Everything else on this page is an ' +
        'aggregate, and an aggregate cannot answer &ldquo;what did the call ' +
        'that just failed actually look like&rdquo; — which is the first ' +
        'thing anybody asks. It is a ring ' +
        'of ' + c.recentCap + ', so anything older has been dropped; the ' +
        'durable record is the <a href="/admin/audit">audit log</a>. Where ' +
        'no principal authenticated, the scheme is shown instead.') +
        '<table><tr><th>When</th><th>Operation</th><th>Resource</th><th>' +
        'Status</th><th>Who</th><th class="num">ms</th><th ' +
        'class="num">Bytes</th></tr>' +
        recentRows + '</table>' +

        (json.store
          ? '<h2>What it wrote</h2>' +
            // The opening sentence carries no HTML entity, deliberately: the
            // console derives an h2 tooltip from it through plainTextOf() and
            // esc(), which leaves an entity showing as its own source text.
            // Every other note here happens to be cut before its first one;
            // this was the first that was not, and rewording it was cheaper
            // than teaching the tooltip pass to decode.
            self.note('The directory as it is NOW, which is the other half ' +
            'of the question and is not a counter: a page reporting four ' +
            'hundred successful creates beside a directory holding three ' +
            'people is reporting something worth knowing. There is no second ' +
            'store — a SCIM <code>POST</code> and an <code>ldapadd</code> ' +
            'write the same entry — so these are the same figures <a ' +
            'href="/admin/users">Users</a> and <a ' +
            'href="/admin/groups">Groups</a> are drawn from.') +
            '<div class="tiles">' +
            self.tile(json.store.userCount, 'people in the directory') +
            self.tile(json.store.groupCount, 'groups') +
            self.tile(json.store.entryCount + ' / ' + json.store.maxEntries,
                      'entries / max') +
            '</div>'
          : '') +

        self.note('<strong>There is no reset button, and it was refused ' +
        'rather than forgotten.</strong> A console that could zero its own ' +
        'monitoring would make every number on this page a number somebody ' +
        'might have zeroed — and the <a href="/admin/audit">audit log</a>, ' +
        'which is the durable record, cannot be reset either. Restarting the ' +
        'process is the only way these go back to zero, which is why the ' +
        'epoch is printed above.') +

        self.note('<a href="/admin/scim">the surface these calls arrive ' +
        'at</a> &middot; <a href="/admin/scim/monitor?format=json">this page ' +
        'as JSON</a> &middot; <a href="/admin-api/scim/monitor">the same ' +
        'over the management API</a> &middot; <a href="/admin/audit">the ' +
        'durable record</a> &middot; <a href="/admin/metrics">every endpoint ' +
        'call, by route</a>');

      self.respond(req, res, json, 'SCIM metrics', '/admin/scim/monitor',
                   inner);
      log.debug("Leaving the admin SCIM monitor page.");
    });

    // =========================================================================
    // GET /admin/signals, POST /admin/signals, POST /admin/signals/receive —
    // THIS CONSOLE AS A SHARED SIGNALS RECEIVER (2026-09-10).
    //
    // `ssf/ssf_receivers.ts` holds the design and is not summarised here. What
    // this block is: the receive endpoint this console hosts, the page that
    // draws what arrived, and the one control on it.
    //
    // **THE MODULE IS A PLAIN REQUIRE AND NOT AN ELEVENTH SLOT**, and rule 3e's
    // test is why — it answers NO in both directions, which is the answer that
    // means "do not add a slot". `ssf/ssf_receivers.ts` registers no route
    // (rule 3) and requires only libraries, none of which requires this file,
    // so a require here can neither move a route nor close a cycle. The slots
    // on this file (the root CLAUDE.md's rule 3e table lists them) exist
    // because `ssf/ssf.ts`, `ldap/ldap_server.js` and the rest register routes
    // and sit BELOW this line in the require order (since #50's R1 a converted
    // one's routes are registered by `common/protocol_stack.ts` instead, but
    // each still requires this file, so the cycle half stands); a library
    // costs a reader nothing and an indirection is not free.
    //
    // It is required at the top of this file with the others.
    // =========================================================================





    // -------------------------------------------------------------------------
    // THE RECEIVE ENDPOINT. Three lines, because everything a receiver checks
    // is in `accept()` — see the note in the gate above about why this path is
    // exempt from it, and `ssf/ssf_receivers.ts` for what is checked instead.
    // -------------------------------------------------------------------------
    app.post('/admin/signals/receive', function (req, res) {
      log.debug("Entering the admin console's Shared Signals receive " +
                "endpoint.");
      const taken = signals.accept(signals.ADMIN, req);
      if (taken.status >= 400) {
        errorCodes.mark(res,
                        taken.errorCode || errorCodes.codeOf(taken) ||
                        'STS-ADMIN-0019');
      }
      res.status(taken.status).set('Cache-Control', 'no-store');
      if (taken.body) {
        res.type('application/json').send(JSON.stringify(taken.body, null, 2));
      } else {
        // 202 with an EMPTY body, which is what RFC 8935 section 2.3 says.
        res.end();
      }
      log.debug("Leaving the admin console's Shared Signals receive " +
        "endpoint. " +
                taken.status + ".");
    });

    app.post('/admin/signals', function (req, res) {
      log.debug("Entering the admin signals action endpoint.");
      const body = parseBody(req);
      const result = signalsAction(body);
      self.respondToAction(req, res, '/admin/signals' +
        queryWith(self.listViewFromBack('/admin/signals', body.back), {}),
        result);
      log.debug("Leaving the admin signals action endpoint.");
    });

    app.get('/admin/signals', function (req, res) {
      log.debug("Entering the admin signals page.");
      const json = signalsJson(req);
      const st: any = json.status || {};
      const stream = st.stream;

      const tiles = '<div class="tiles">' +
        self.tile(json.total, 'delivered here') +
        self.tile(stream ? stream.caepDelivered : 0, 'CAEP types') +
        self.tile(stream ? stream.riscDelivered : 0, 'RISC types') +
        self.tile(stream ? stream.counters.delivered : 0, 'pushes accepted') +
        self.tile(stream ? stream.counters.failed : 0, 'pushes failed') +
        self.tile(stream ? stream.queued : 0, 'still queued') +
        '</div>';

      // WHY NOTHING IS HERE, ABOVE THE TABLE AND NOT BELOW IT. An empty inbox
      // has five causes and only one of them is "nothing has happened";
      // `status()` works out which apply and this draws them in the order a
      // reader should check them. It is drawn even when rows ARE present,
      // because a stream that has been paused since this morning explains a
      // page that stops rather than a page that is empty.
      const why = st.why && st.why.length
        ? '<div class="err"><p><strong>Some or all of this console\'s ' +
          'signals are not arriving.</strong></p><ul>' +
          st.why.map(function (line) {
            return '<li>' + self.esc(line) + '</li>';
          }).join('') + '</ul></div>'
        : '';

      const streamBlock = stream
        ? '<table>' +
          '<tr><th>Stream</th><td><code>' + self.esc(stream.stream_id) +
          '</code></td></tr><tr><th>Audience</th><td><code>' +
          self.esc(String(stream.aud)) + '</code> ' +
          '<span class="sub">' +
          self.esc('This console checks for this name in every SET\'s aud ' +
                   'and refuses one addressed to anybody else with ' +
                   'invalid_audience — recording it either way, so a ' +
                   'misaddressed event is visible rather than merely absent.') +
                   '</span></td></tr>' +
          '<tr><th>Issuer</th><td><code>' + self.esc(String(stream.iss)) +
          '</code></td></tr><tr><th>Delivered ' +
          'to</th><td><code>' +
          self.esc(stream.endpoint_url) + '</code> <span class="sub">' +
          self.esc('RFC 8935 push, over the loopback interface, with this ' +
                   'service\'s own TLS certificate pinned. It is a real HTTP ' +
                   'request on purpose: handing the event to the page in ' +
                   'process would skip the body, the media type, the ' +
                   'authorization header and the signature.') +
          '</span></td></tr>' +
          '<tr><th>Event types</th><td>' + self.esc(String(stream.delivers)) +
          ' delivered of ' + self.esc(String(stream.requested)) + ' ' +
          'requested <span class="sub">' +
          self.esc('The difference is the intersection SSF 1.0 section 7.1.1 ' +
                   'defines: a type this transmitter does not support is ' +
                   'answered by its absence from events_delivered rather ' +
                   'than by a refusal.') +
          '</span></td></tr>' +
          '<tr><th>Status</th><td><span class="' +
          (stream.status === 'enabled' ? '' : 'state-invalid') + '">' +
          self.esc(stream.status) + '</span> <span class="sub">' +
          self.esc(stream.statusReason) + '</span></td></tr>' +
          '<tr><th>Last push</th><td>' +
          (stream.lastPushAt
            ? self.esc(stream.lastPushAt) +
              (stream.lastPushError
                ? ' <span class="state-invalid">' +
                  self.esc(stream.lastPushError) +
                  '</span>'
                : '')
            : '<span class="sub">nothing has been pushed here yet</span>') +
          '</td></tr></table>'
        : self.note('<strong>There is no stream for this console in the ' +
          '&ldquo;' +
          self.esc(st.realm) + '&rdquo; realm.</strong> It is seeded at ' +
          'startup and is an ORDINARY stream — if it was paused, narrowed or ' +
          'deleted at <a href="/admin/ssf">Shared Signals</a> or through ' +
          '<code>/admin-api/ssf</code>, it stays that way until a restart. ' +
          'That is the same rule this service\'s seeded application entries ' +
          'follow.');

      const search = self.sectionSearchForm({
        path: '/admin/signals', param: 'sigq', pageParam: 'receivedPage',
        query: req.query, label: 'Find',
        placeholder: 'alice, session-revoked, a jti, a stream id',
        what: 'Over the event name, the type URI, the subject as this ' +
              'receiver read it, the issuer, the audience, the jti and the ' +
              'stream — because a reader arrives holding one of those and ' +
              'does not know which column it is in.' });

      const nav = self.pageNavPair('/admin/signals', pageParamsOf(req.query),
                                   json.paging.received);

      const rows = json.received.map(function (row) {
        return '<tr>' +
          '<td class="sub">' + self.esc(self.whenText(Date.parse(row.at))) +
          '</td><td><code>' + self.esc(row.vocabulary) + '</code> ' +
          self.esc(row.name) +
          (row.types.length > 1
            ? ' <span class="sub">and ' +
              self.esc(String(row.types.length - 1)) +
              ' more in the same SET</span>'
            : '') +
          '<div class="sub"><code>' + self.esc(row.types[0] || '(none)') +
          '</code></div></td>' +
          '<td>' + (row.subject
            ? self.esc(row.subject)
            : '<span class="sub" title="' +
              self.esc('SSF\'s own two events are about the STREAM rather ' +
                       'than about anybody, so they carry no subject at all. ' +
                       'Every CAEP and RISC event does.') +
                       '">&mdash;</span>') + '</td>' +
          '<td>' + (row.verified
            ? '<span title="' + self.esc(row.verificationNote) +
              '">verified</span>'
            : '<span class="state-invalid" title="' +
              self.esc(row.verificationNote) + '">not verified</span>') +
          (row.audienceOk
            ? ''
            : '<div class="state-invalid" title="' +
              self.esc('This receiver is "' + String(st.audience) +
                       '" and that name is not in this token\'s aud. It was ' +
                       'refused with invalid_audience and recorded anyway, ' +
                       'because what arrived is the question being asked.') +
                       '">wrong audience</div>') +
          (row.correctMediaType
            ? ''
            : '<div class="sub" title="' +
              self.esc('RFC 8935 section 2.1 says application/secevent+jwt. ' +
                       'This one said "' +
                       String(row.contentType || '(nothing)') + '". It was ' +
                       'accepted — a receiver that refused would be testing ' +
                       'the transmitter\'s pedantry — and it is said out ' +
                       'loud rather than passed over.') +
                       '">media type</div>') +
          '</td>' +
          '<td class="sub"><code>' + self.esc(row.jti) + '</code>' +
          '<div><code>' + self.esc(row.stream || '') + '</code></div></td>' +
          '<td>' + (Object.keys(row.payload).length
            ? '<details><summary>' +
              self.esc(String(Object.keys(row.payload).length) + ' member(s)') +
              '</summary><pre>' +
              self.esc(JSON.stringify(row.payload, null, 2)) +
              '</pre></details>'
            : '<span class="sub" title="' +
              self.esc('Eleven of RISC\'s fourteen event types have no ' +
                       'payload members at all — the SUBJECT carries the ' +
                       'entire message, which is why a subject naming the ' +
                       'wrong person is a wholly wrong event rather than a ' +
                       'partly wrong one.') + '">no members</span>') +
          '</td></tr>';
      }).join('') || '<tr><td colspan="6">' +
        self.esc(json.filter.received
          ? 'Nothing delivered here matches that search.'
          : 'Nothing has been delivered to this console yet.') +
        '</td></tr>';

      const inner = '<h1>Signals received</h1><p>Every Security Event Token ' +
        '<strong>delivered to this console</strong> in the ' +
        '&ldquo;' + self.esc(st.realm) + '&rdquo; realm. This console is a ' +
        'registered Shared Signals receiver: it has a stream of its own, it ' +
        'is POSTed each event over RFC 8935 push at <code>' +
        self.esc(st.receivePath) + '</code>, and it verifies the signature ' +
        'and the audience before recording anything.</p>' +
        why +
        tiles +
        self.note('<strong>This is not the transmitter\'s copy.</strong> ' +
        '<a href="/admin/ssf">Shared Signals</a> shows every stream this ' +
        'service holds and what it has SENT on each; <a ' +
        'href="/admin/caep-sessions">CAEP sessions</a> and <a ' +
        'href="/admin/risc-accounts">RISC accounts</a> show what it BELIEVES ' +
        'about a session and an account. This page shows what came back ' +
        'through the door — which is the only one of the four that goes ' +
        'empty when delivery is broken, and is therefore the only one that ' +
        'can tell you it is.') +

        '<h2>This console\'s stream</h2>' +
        streamBlock +

        '<h2 id="find-sigq">Delivered events</h2>' +
        search +
        nav.head +
        '<table><tr><th>When</th><th>Event</th><th>Subject</th>' +
        '<th>How it arrived</th><th>Identifiers</th><th>Payload</th></tr>' +
        rows + '</table>' +
        nav.foot +

        // THE ONE CONTROL, and it is drawn only when there is something to
        // clear: a button that would drop nothing is a button somebody presses
        // to find out what it does. The CSRF token is put into this form by
        // `withCsrf()` on the way out, like every other form on this console —
        // rule 8's arrangement, so a page author does neither half.
        (json.total
          ? '<h2>Clear</h2>' +
            self.note('This drops what is HELD HERE and nothing else. The ' +
            'stream is untouched and goes on delivering, and the <a ' +
            'href="/admin/audit">audit log</a>\'s record of each delivery ' +
            'cannot be cleared — which is the point of it being the durable ' +
            'half.') +
            '<form method="post" action="/admin/signals">' +
            '<input type="hidden" name="back" value="' +
            self.esc(queryWith(self.listViewFromBack('/admin/signals',
                                                     queryOne(req.query,
                                                         'back')), {})) +
            '">' +
            '<div class="formrow">' +
            '<input type="hidden" name="action" value="clear">' +
            '<button type="submit" class="secondary" title="' +
            self.esc('Drops the ' + json.total + ' delivered event(s) held ' +
                     'in this console\'s inbox in this realm.') +
            '">Clear this inbox</button></div></form>'
          : '') +

        self.note('<a href="/admin/signals?format=json">this page as ' +
        'JSON</a> &middot; <a href="/admin-api/signals">the same over the ' +
        'management API</a> &middot; <a href="/admin/ssf">the streams and ' +
        'the settings</a> &middot; <a href="/portal/signals">what a person ' +
        'sees about themselves</a> &middot; <a href="/admin/audit">the ' +
        'durable record</a>');

      self.respond(req, res, json, 'Signals received', '/admin/signals', inner);
      log.debug("Leaving the admin signals page.");
    });

    app.get('/admin/ssf/dead-letters', function (req, res) {
      log.debug("Entering the admin dead letters page.");
      const json = ssfDeadLettersJson(req);
      if (!json.installed) {
        const missing = '<h1>Dead letters</h1><div ' +
          'class="err"><strong>Shared Signals is not loaded in this ' +
          'process</strong>, so there are no dead-letter queues to report ' +
          'on.</div>';
        self.respond(req, res, json, 'Dead letters', '/admin/ssf/dead-letters',
                     missing);
        log.debug("Leaving the admin dead letters page. Not installed.");
        return;
      }
      const totals = json.totals;
      const causeById = {};
      json.causes.forEach(function (cause) {
        causeById[cause.id] = cause;
      });
      const listView = self.listViewOf('/admin/ssf/dead-letters', req.query);
      // A link to the letters narrowed, keeping the rest of the view and going
      // back to the first page of what it narrows to.
      const lettersOf = function (overrides) {
        log.debug("Entering lettersOf().");
        log.debug("Leaving lettersOf().");
        return self.esc('/admin/ssf/dead-letters' + queryWith(listView,
          Object.assign({ lettersPage: '' }, overrides))) + '#find-dlq';
      };
      const share = function (count) {
        log.debug("Entering share().");
        log.debug("Leaving share().");
        return totals.held
          ? Math.round(count * 100 / totals.held) + '%' : '&mdash;';
      };

      const tiles = '<div class="tiles">' +
        self.tile(totals.held, 'dead letters held') +
        self.tile(totals.streamsHolding, 'streams holding them') +
        self.tile(totals.deadStreams, 'dead streams') +
        self.tile(totals.halfOpenStreams, 'half-open') +
        self.tile(totals.failingStreams, 'failing') +
        self.tile(totals.unsigned, 'never signed') +
        self.tile(totals.newestAgeS === null ? '—'
          : self.durationText(totals.newestAgeS * 1000), 'since the newest') +
        self.tile(totals.oldestAgeS === null ? '—'
          : self.durationText(totals.oldestAgeS * 1000), 'since the oldest') +
        '</div>';

      const off = (!json.enabled
        ? self.warn('<strong>Shared Signals is turned off</strong> ' +
          '(<code>ssf.enabled</code>), so nothing new is sent or ' +
          'dead-lettered. What is below is held until ' +
          '<code>ssf.deadLetterRetentionS</code> passes.')
        : '') +
        (json.enabled && !json.pushDelivery
          ? self.warn('<strong>Push delivery is off</strong> ' +
            '(<code>ssf.pushDelivery</code>), so no push is made and none ' +
            'can fail. Poll streams have no dead letters.')
          : '');

      // WHEN. The chart, its legend, and the same numbers as a table.
      const timeline = json.timeline;
      const legend = '<div class="legend">' + json.causes.map(function (cause) {
        return '<span title="' + self.esc(cause.what) + '">' +
          self.deadLetterSwatch(DEAD_LETTER_COLOURS[cause.id] || '#8a8a99') +
          self.esc(cause.label) + ' ' + self.esc(String(cause.count)) +
          '</span>';
      }).join('') + '</div>';
      const busy = timeline.buckets.filter(function (bucket) {
        return bucket.total > 0;
      });
      const timelineTable = busy.length
        ? '<details><summary>The same as a table (' + busy.length + ' of ' +
          timeline.buckets.length + ' columns hold anything)</summary>' +
          self.wideTable('Dead letters by time', '<table><tr><th>From</th>' +
          json.causes.map(function (cause) {
            return '<th class="num">' + self.esc(cause.label) + '</th>';
          }).join('') + '<th class="num">All</th></tr>' +
          busy.map(function (bucket) {
            return '<tr><td class="sub">' +
              self.esc(self.whenText(Date.parse(bucket.start))) + '</td>' +
              json.causes.map(function (cause) {
                return '<td class="num">' + bucket.counts[cause.id] + '</td>';
              }).join('') + '<td class="num">' + bucket.total + '</td></tr>';
          }).join('') + '</table>') + '</details>'
        : '';
      const older = timeline.olderThanWindow
        ? self.note(self.esc(String(timeline.olderThanWindow)) + ' letter(s) ' +
          'are older than the window and are not drawn: the next sweep ' +
          'deletes them. That happens when ' +
          '<code>ssf.deadLetterRetentionS</code> is shortened, and before a ' +
          'process has swept since it started.')
        : '';

      // WHY.
      const causeRows = json.causes.map(function (cause) {
        return '<tr><td>' +
               self.deadLetterSwatch(DEAD_LETTER_COLOURS[cause.id]) +
          ' ' + self.esc(cause.label) +
          (cause.code
            ? '<div><a href="/admin/error-codes#' + self.esc(cause.code) +
              '"><code class="ec">' + self.esc(cause.code) + '</code></a></div>'
            : '') + '</td>' +
          '<td>' + self.esc(cause.what) + '</td>' +
          '<td class="num">' + (cause.count
            ? '<a href="' + lettersOf({ dlcause: cause.id }) + '">' +
              cause.count + '</a>'
            : '0') + '</td>' +
          '<td class="num">' + share(cause.count) + '</td></tr>';
      }).join('');
      const codeRows = json.byCode.map(function (row) {
        const cause = causeById[row.cause] || { label: row.cause };
        return '<tr><td>' + (row.errorCode
            ? '<a href="/admin/error-codes#' + self.esc(row.errorCode) + '">' +
              '<code class="ec">' + self.esc(row.errorCode) + '</code></a>'
            : '<span class="sub">none recorded</span>') + '</td>' +
          '<td>' + self.esc(cause.label) + '</td>' +
          '<td>' + self.esc(row.summary) + '</td>' +
          '<td class="num">' + (row.errorCode
            ? '<a href="' + lettersOf({ dlq: row.errorCode }) + '">' +
              row.count + '</a>'
            : String(row.count)) + '</td></tr>';
      }).join('') || '<tr><td colspan="4">Nothing held.</td></tr>';
      const statusRows = json.byStatus.map(function (row) {
        return '<tr><td>' + (row.status
            ? 'HTTP ' + self.esc(String(row.status))
            : '<span class="sub">no HTTP answer &mdash; not pushed, or ' +
              'nothing answered</span>') + '</td>' +
          '<td class="num">' + row.count + '</td>' +
          '<td class="num">' + share(row.count) + '</td></tr>';
      }).join('') || '<tr><td colspan="3">Nothing held.</td></tr>';
      const typeRows = json.byEventType.map(function (row) {
        return '<tr><td>' + self.esc(row.name || '(unreadable)') + '</td>' +
          '<td class="sub"><code>' + self.esc(row.type || '') + '</code></td>' +
          '<td class="num">' + (row.name
            ? '<a href="' + lettersOf({ dlq: row.type || row.name }) + '">' +
              row.count + '</a>'
            : String(row.count)) + '</td>' +
          '<td class="num">' + share(row.count) + '</td></tr>';
      }).join('') || '<tr><td colspan="4">Nothing held.</td></tr>';

      // WHICH STREAMS.
      const stateText = {
        dead: '<span class="state-revoked">dead</span>',
        'half-open': '<span class="state-expired">half-open</span>',
        failing: '<span class="state-expired">failing</span>',
        healthy: '<span class="state-valid">delivering</span>',
        poll: '<span class="sub">poll</span>',
        unknown: '<span class="sub">not held here</span>'
      };
      const streamRows = json.streams.map(function (row) {
        const when = row.state === 'dead'
          ? 'dead since ' + self.whenText(Date.parse(row.deadSince)) +
            (row.nextProbeAt
              ? '; next probe ' + self.whenText(Date.parse(row.nextProbeAt)) :
                '')
          : (row.failingSince
            ? 'failing since ' + self.whenText(Date.parse(row.failingSince)) :
              '');
        return '<tr><td class="who">' + (row.state === 'unknown'
            ? '<code>' + self.esc(row.stream_id) + '</code>'
            : '<a href="/admin/ssf#stream-' + self.esc(row.stream_id) +
              '" title="' +
              self.esc('This stream\'s card on Protocols → Shared Signals, ' +
                       'where Revive and Drop its dead letters are.') +
                       '"><code>' +
              self.esc(row.stream_id) + '</code></a>') +
          (row.aud
            ? '<div class="sub">' + self.esc(Array.isArray(row.aud)
              ? row.aud.join(', ') : String(row.aud)) + '</div>'
            : '') + '</td>' +
          '<td>' + (stateText[row.state] || self.esc(row.state)) +
          (when ? '<div class="sub">' + self.esc(when) + '</div>' : '') +
          '</td><td>' + self.esc(row.deadReason || row.lastPushError || '') +
          '</td><td class="num">' + (row.held
            ? '<a href="' + lettersOf({ dlstream: row.stream_id }) + '">' +
              row.held + '</a>'
            : '0') + '</td>' +
          '<td class="sub">' + json.causes.filter(function (cause) {
            return row.causes[cause.id] > 0;
          }).map(function (cause) {
            return self.esc(cause.label) + ' ' + row.causes[cause.id];
          }).join('<br>') + '</td>' +
          '<td class="num">' + row.deadLetteredEver + '</td></tr>';
      }).join('') ||
        '<tr><td colspan="6">Every push stream is delivering and ' +
        'none holds a dead letter.</td></tr>';

      // THE LETTERS.
      const search = self.sectionSearchForm({
        path: '/admin/ssf/dead-letters', param: 'dlq', pageParam: 'lettersPage',
        query: req.query, label: 'Find',
        placeholder: 'a jti, STS-SSF-0092, session-revoked, 503',
        what: 'Over the jti, the stream, the reason, the error code, the ' +
              'receiver\'s status, the event name and type URI and the ' +
              'subject. The counts above are the whole realm\'s whatever is ' +
              'searched.' });
      const narrowed = [];
      if (json.filter.stream) {
        narrowed.push('stream <code>' + self.esc(json.filter.stream) +
                      '</code> ' +
          '(<a href="' + lettersOf({ dlstream: '' }) + '">any stream</a>)');
      }
      if (json.filter.cause) {
        narrowed.push('cause ' + self.esc((causeById[json.filter.cause] ||
          { label: json.filter.cause }).label) + ' (<a href="' +
          lettersOf({ dlcause: '' }) + '">any cause</a>)');
      }
      const nav = self.pageNavPair('/admin/ssf/dead-letters',
                                   pageParamsOf(req.query),
        Object.assign({ param: 'lettersPage', noun: 'dead letters' },
                      json.paging.letters));
      const letterRows = self.deadLetterRows(json, causeById, listView) ||
        '<tr><td colspan="7">' + (json.matched === 0 && totals.held
          ? 'No dead letter matches.'
          : 'No dead letters are held in this realm.') + '</td></tr>';

      // THIS PROCESS.
      const proc = json.process;
      const pushes = proc.pushes || {};
      const sweepRows = proc.sweeps.map(function (row) {
        return '<tr><td class="sub">' +
               self.esc(self.whenText(Date.parse(row.at))) +
          '</td><td class="num">' + row.letters + '</td>' +
          '<td class="num">' + row.held + '</td>' +
          '<td class="num">' + row.expired + '</td>' +
          '<td class="num">' + row.trimmed + '</td>' +
          '<td class="num">' + row.orphaned + '</td>' +
          '<td class="num">' + row.deadStreams + '</td>' +
          '<td class="num">' + row.probes + '</td></tr>';
      }).join('') || '<tr><td colspan="8">This process has not swept this ' +
        'realm yet. It sweeps every ' +
        '<code>ssf.deadLetterSweepS</code>.</td></tr>';
      const since = proc.sinceStart;

      const s = json.settings;
      const settingRows = [
        ['ssf.deadLetterRetentionS', s.retentionS, 'seconds a letter is kept'],
        ['ssf.deadLetterMaxPerStream', s.maxPerStream,
         'letters one stream keeps; past it the oldest go'],
        ['ssf.deadStreamTimeoutS', s.deadStreamTimeoutS,
         'seconds of failed pushes before a stream is dead (0: never)'],
        ['ssf.deadLetterSweepS', s.sweepS, 'seconds between sweeps'],
        ['ssf.pushConcurrency', s.pushConcurrency,
         'pushes in flight per process (0: no cap)'],
        ['ssf.pushBacklog', s.pushBacklog,
         'pushes waiting per process before STS-SSF-0092'],
        ['ssf.pushRetries', s.pushRetries, 'retries before a push has failed']
      ].map(function (row) {
        return '<tr><td><code>' + self.esc(row[0]) + '</code></td>' +
          '<td class="num">' + self.esc(String(row[1])) + '</td>' +
          '<td>' + self.esc(row[2]) + '</td></tr>';
      }).join('');

      const inner = '<h1>Dead letters</h1><p>Every Security Event Token the ' +
        'transmitter <strong>could not deliver</strong> and is still ' +
        'holding, in the &ldquo;' + self.esc(json.realm) + '&rdquo; realm ' +
        '&mdash; each realm has dead-letter queues of its own. A letter is ' +
        'kept for <code>ssf.deadLetterRetentionS</code> with the reason, and ' +
        'nothing resends it except the probe that tries to revive a dead ' +
        'stream.</p>' +
        off +
        tiles +
        self.note('<strong>This page reports; it changes nothing.</strong> ' +
        'Revive a dead stream or drop its letters on that stream\'s card at ' +
        '<a href="/admin/ssf">Protocols &rarr; Shared Signals</a> &mdash; ' +
        'every stream below links to it. Counted ' +
        self.esc(self.whenText(Date.parse(json.generatedAt))) + '.') +

        '<h2>When</h2>' +
        self.note('Held letters by when they were dead-lettered, in ' +
        self.esc(self.deadLetterSpan(timeline.bucketS)) + ' columns over the ' +
          'last ' +
        self.esc(self.deadLetterSpan(timeline.windowS)) + ' &mdash; the ' +
        'whole retention window, so a letter that has aged out is gone from ' +
        'the chart as it is from the queue. Point at a column for its ' +
        'counts.') +
        legend +
        self.deadLetterTimeline(timeline, json.causes) +
        timelineTable +
        older +

        '<h2>Why</h2>' +
        self.note('Four causes, each a different thing to do about it. Every ' +
        'other code a failed push can carry is a push that failed, and is ' +
        'broken out below.') +
        '<table><tr><th>Cause</th><th>What it means</th>' +
        '<th class="num">Held</th><th class="num">Share</th></tr>' +
        causeRows + '</table>' +
        '<h3>By error code</h3>' +
        '<table><tr><th>Code</th><th>Cause</th><th>What the code means</th>' +
        '<th class="num">Held</th></tr>' + codeRows + '</table>' +
        '<h3>By the receiver\'s answer</h3>' +
        '<table><tr><th>Status</th><th class="num">Held</th>' +
        '<th class="num">Share</th></tr>' + statusRows + '</table>' +
        '<h3>By event type</h3>' +
        '<table><tr><th>Event</th><th>Type</th><th class="num">Held</th>' +
        '<th class="num">Share</th></tr>' + typeRows + '</table>' +

        '<h2>Streams</h2>' +
        self.note('Every stream that holds a dead letter or is not ' +
        'delivering. <strong>Dead</strong>: nothing is pushed to it and one ' +
        'letter is pushed as a probe each ' +
        '<code>ssf.deadStreamTimeoutS</code>. <strong>Half-open</strong>: ' +
        'failing for that long without being dead, so the next failure kills ' +
        'it. <strong>Failing</strong>: younger than that. <em>Ever</em> is ' +
        'the stream\'s own count of every letter it was given, including the ' +
        'ones since deleted.') +
        self.wideTable('Streams with dead letters',
                       '<table><tr><th>Stream</th>' +
        '<th>State</th><th>Last failure</th><th class="num">Held</th>' +
        '<th>Held, by cause</th><th class="num">Ever</th></tr>' +
        streamRows + '</table>') +

        '<h2 id="letters">The letters</h2>' +
        search +
        (narrowed.length
          ? '<p class="sub">Showing only ' + narrowed.join(' and ') + '.</p>'
          : '') +
        nav.head +
        self.wideTable('Dead letters', '<table><tr><th>Dead-lettered</th>' +
        '<th>Stream</th><th>Event</th><th>Cause</th>' +
        '<th class="num">Status</th><th>Reason</th><th>SET</th></tr>' +
        letterRows + '</table>') +
        nav.foot +
        self.note('No token is shown or returned: a SET is a signed ' +
        'statement about somebody. <em>Not signed</em> is a SET for a dead ' +
        'stream, kept as its claims because signing what nothing would ' +
        'receive is the cost dead streams exist to stop.') +

        '<h2>This process</h2>' +
        self.warn('<strong>Everything in this section is process ' +
        self.esc(String(proc.pid)) + '\'s alone</strong> (' +
        self.esc(proc.role) + '). ' +
        'In a service with request workers the next refresh may be answered ' +
        'by another process with different numbers, and the push cap is not ' +
        'per realm: every realm\'s pushes from one process share it, so a ' +
        'burst in one realm can dead-letter another\'s with ' +
        '<code>STS-SSF-0092</code>.') +
        '<div class="tiles">' +
        self.tile(String(pushes.active || 0) + ' / ' +
                  (pushes.concurrency ? String(pushes.concurrency) : '∞'),
                  'pushes in flight') +
        self.tile(String(pushes.waiting || 0) + ' / ' +
                  String(pushes.backlog || 0), 'pushes waiting') +
        self.tile(since.sweeps, 'sweeps of this realm') +
        self.tile(since.letters, 'dead-lettered here') +
        self.tile(since.expired, 'expired') +
        self.tile(since.trimmed, 'over the per-stream cap') +
        self.tile(since.probes, 'probes') +
        '</div>' +
        '<h3>Recent sweeps</h3>' +
        self.note('The last twenty sweeps of this realm by this process, ' +
        'newest first. <em>New</em> counts the letters this process added ' +
        'since its previous sweep; <em>held</em>, <em>expired</em>, <em>over ' +
        'cap</em> and <em>orphaned</em> (a letter whose stream is gone) are ' +
        'the shared store as this process found it.') +
        self.wideTable('Recent sweeps', '<table><tr><th>When</th>' +
        '<th class="num">New</th><th class="num">Held</th>' +
        '<th class="num">Expired</th><th class="num">Over cap</th>' +
        '<th class="num">Orphaned</th><th class="num">Dead streams</th>' +
        '<th class="num">Probes</th></tr>' + sweepRows + '</table>') +

        '<h2>Settings</h2>' +
        self.note('What decides what is dead-lettered and for how long. ' +
        'Changed on <a href="/admin/ssf">Protocols &rarr; Shared ' +
        'Signals</a>, with every other <code>ssf.*</code> setting.') +
        '<table><tr><th>Setting</th><th ' +
        'class="num">Value</th><th>Meaning</th></tr>' + settingRows +
        '</table>' +

        self.note('<a href="/admin/ssf/dead-letters?format=json">this page ' +
        'as JSON</a> &middot; <a href="/admin-api/ssf/dead-letters">the same ' +
        'over the management API</a> &middot; <a href="/admin/ssf">the ' +
        'streams and their controls</a> &middot; <a ' +
        'href="/admin/error-codes">every error code</a> &middot; <a ' +
        'href="/admin/audit">the audit log</a>');

      self.respond(req, res, json, 'Dead letters', '/admin/ssf/dead-letters',
                   inner);
      log.debug("Leaving the admin dead letters page.");
    });

    // -------------------------------------------------------------------------
    // GET /admin/tls/trust — THE CLIENT-CERTIFICATE TRUSTSTORE (2026-09-12).
    //
    // Every anchor the main port verifies a client certificate against (LDAPS
    // 636 asks for none — tls/CLAUDE.md), where each came from, and two
    // controls: add one or more PEM certificates, and remove one row. It is the
    // runtime door product mode did not have — `POST /tls/trust` answers
    // anybody who can reach the port and is refused there, so until this page
    // anchors could only come from `tls.trustAnchorsFile` at startup.
    //
    // **FILED UNDER PROTOCOLS, DIRECTLY BENEATH `/admin/tls`, AND UNGROUPED.**
    // It is configuration of the TLS / mutual TLS family — the 8443 and 9443
    // listeners it was first written for were deleted on 2026-09-16, and the
    // main port is what reads it now — which is the question Protocols answers.
    // There is no TLS group to put it in and it does not make one: a heading
    // `TLS` over `TLS / mutual TLS` would say the label twice, which is the
    // test `SECTIONS` applies to a group.
    //
    // **NO `clear` BUTTON**, and the page says why where one would be. The two
    // controls are drawn only for a reader who holds Admin Write; the GATE is
    // what refuses a POST without it, whatever the page drew.
    //
    // It holds no state and decides nothing: every row is `tls/tls_server.js`'s
    // own description through `admin-core/admin_views.ts`, and every change is
    // `truststoreAction()` in `admin-core/admin_actions.ts`, the function
    // `POST /admin-api/tls/trust/{action}` calls.
    // -------------------------------------------------------------------------
    app.get('/admin/tls/trust', function (req, res) {
      log.debug("Entering the admin truststore page.");
      const json = adminViews.truststoreJson(req);
      const state = gateStateFor(req);
      const mayChange = !!(state && state.write);
      if (!json.installed) {
        const missing = self.messagesOf(req) + '<div class="err"><strong>The ' +
          'client-certificate truststore is not installed in this process.' +
          '</strong> ' + self.esc(json.note) + '</div>';
        self.respond(req, res, json, 'Client-certificate truststore',
                     '/admin/tls/trust', missing);
        log.debug("Leaving the admin truststore page. Not installed.");
        return;
      }
      const back = queryWith(self.listViewOf('/admin/tls/trust', req.query),
                             {});
      const nav = self.pageNavPair('/admin/tls/trust', pageParamsOf(req.query),
                                   json.paging && Object.assign({ param: 'page',
                                     noun: 'anchors', offset: 0 },
                                     json.paging));
      const rows = json.anchors.map(function (anchor) {
        return '<tr>' +
          '<td><code>' + self.esc(anchor.subject) + '</code>' +
          (anchor.readable ? '' : '<div class="state-invalid">' +
            self.esc('OpenSSL cannot read this certificate, so no handshake ' +
                     'uses it.') +
            '</div>') +
          '<div class="sub">' + (anchor.ca ? 'CA' : 'not a CA') +
          '</div></td>' +
          '<td><code>' + self.esc(anchor.issuer || '—') + '</code></td>' +
          '<td class="sub"><code>' + self.esc(anchor.serial || '—') +
          '</code></td>' +
          '<td class="sub">' + self.esc(anchor.notBefore || '—') + '<br>' +
          self.esc(anchor.notAfter || '—') + '</td>' +
          '<td class="sub"><code>' + self.esc(anchor.fingerprint256) +
          '</code></td><td>' + (anchor.source === 'file'
            ? '<span title="' +
              self.esc('Loaded from tls.trustAnchorsFile at ' +
                'startup. Removing it here lasts until the next start, when ' +
                'the file is read again.') + '">file</span>'
            : '<span title="' + self.esc(anchor.persisted
                ? 'Added at runtime and written to ou=trustAnchors, so it ' +
                  'survives a restart wherever the directory is persisted.'
                : 'Added at runtime and NOT written down — the directory was ' +
                  'full or no store is installed — so it is gone at the next ' +
                  'start.') +
              '">runtime' + (anchor.persisted ? '' : ' (not stored)') +
              '</span>') +
          '</td>' +
          '<td>' + (mayChange
            ? '<form method="post" action="/admin/tls/trust">' +
              '<input type="hidden" name="action" value="remove">' +
              '<input type="hidden" name="fingerprint" value="' +
              self.esc(anchor.fingerprint256) + '">' +
              '<input type="hidden" name="back" value="' + self.esc(back) +
              '"><button type="submit" class="secondary" title="' +
              self.esc(anchor.source === 'file'
                ? 'Stops client certificates chaining only to this anchor ' +
                       'from verifying, until the next start re-reads ' +
                       'tls.trustAnchorsFile.'
                : 'Stops client certificates chaining only to this anchor ' +
                       'from verifying. Nothing brings it back.') +
                       '">Remove</button></form>'
            : '<span class="sub">Admin Write</span>') + '</td></tr>';
      }).join('') || '<tr><td colspan="7">' + self.esc('Empty. No client ' +
        'certificate verifies on any listener, so every one presented ' +
        'arrives UNVERIFIED and everything that reads one refuses it.') +
        '</td></tr>';

      const inner = self.messagesOf(req) +
        self.note('Every anchor <strong>LDAPS 636 and the main ' +
        'port</strong> verify a client certificate against. A certificate ' +
        'that chains to one of these is VERIFIED — and since 2026-09-06 a ' +
        'verified certificate is an identity here: it starts a sign-on ' +
        'session, and it is what admits a remote XACML PEP to ' +
        '<code>/xacml/pep/*</code>. So this list decides whose certificates ' +
        'this service believes.') +
        self.warn('<strong>Nothing on this page is persisted.</strong> ' +
        self.esc(json.notes.persisted)) +
        self.note(self.esc(json.notes.scope) + ' ' +
                  self.esc(json.notes.effect) + ' ' +
        self.esc(json.notes.revocation)) +
        '<div class="tiles">' +
        self.tile(json.total, 'anchors') +
        self.tile(json.fromFile, 'from the file') +
        self.tile(json.atRuntime, 'added at runtime') +
        self.tile(json.max, 'maximum') +
        '</div>' +
        self.note('<code>tls.trustAnchorsFile</code> is ' + (json.anchorsFile
          ? '<code>' + self.esc(json.anchorsFile) + '</code>, and ' +
            self.esc(String(json.loadedFromFile)) + ' anchor(s) were loaded ' +
            'from it at startup.'
          : 'not set, so every anchor here was added while this process was ' +
            'running.') +
        ' The two test controls on <a href="/tls">/tls</a> — <code>POST ' +
        '/tls/trust</code> and <code>POST /tls/trust/clear</code> — ' +
        (json.doors.testControls.open
          ? 'answer anybody in this development-mode process.'
          : 'are refused in product mode; this page and ' +
            '<code>/admin-api/tls/trust</code> are the runtime doors.')) +
        '<h2>Anchors</h2>' + nav.head +
        '<table><tr><th>Subject</th><th>Issuer</th><th>Serial</th>' +
        '<th>Not before / not after</th><th>SHA-256 fingerprint</th>' +
        '<th>Source</th><th></th></tr>' + rows + '</table>' + nav.foot +
        self.perPageForm('/admin/tls/trust', 'page', '1', json.perPage) +
        (mayChange
          ? '<h2>Add anchors</h2>' +
            self.note('One or more <code>-----BEGIN CERTIFICATE-----</code> ' +
            'blocks — the root, or the whole chain above the leaf. Every ' +
            'block must be one OpenSSL can read, or NONE is added: a block ' +
            'the listener cannot parse makes the next truststore change ' +
            'throw on every listener. A certificate already held is counted ' +
            'and not added twice.') +
            '<form method="post" action="/admin/tls/trust">' +
            '<input type="hidden" name="action" value="add">' +
            '<input type="hidden" name="back" value="' + self.esc(back) + '">' +
            '<textarea name="certificates" rows="8" ' +
            'placeholder="-----BEGIN CERTIFICATE-----"></textarea>' +
            '<div class="formrow"><button type="submit">Trust these</button>' +
            '</div></form>'
          : self.note('Changing this list needs <strong>Admin Write</strong>' +
            '.')) +
        '<h2>What there is deliberately no button for</h2>' +
        self.note('<strong>Emptying the truststore.</strong> A bulk clear on ' +
        'this door would be the one control whose reach is every client ' +
        'certificate every other caller relies on — a remote PEP ' +
        'authenticating as nobody for the rest of a run is what one ' +
        'unguarded clear has already cost. Remove the rows you mean, one at ' +
        'a time; the ones you did not put there are the ones you have to ' +
        'name. Removing a <code>file</code> anchor is allowed and lasts ' +
        'until the next start.') +
        self.note('<a href="/admin/tls/trust?format=json">this page as ' +
        'JSON</a> &middot; <a href="/admin-api/tls/trust">the same over the ' +
        'management API</a> &middot; <a href="/admin/tls">the TLS ' +
        'listeners\' settings</a> &middot; <a href="/tls">the TLS ' +
        'endpoint</a> &middot; <a href="/admin/pki">this service\'s own ' +
        'certificate authority</a>');

      self.respond(req, res, json, 'Client-certificate truststore',
                   '/admin/tls/trust',
                   inner);
      log.debug("Leaving the admin truststore page. " + json.total +
                " anchor(s).");
    });

    app.post('/admin/tls/trust', function (req, res) {
      log.debug("Entering the admin truststore action endpoint.");
      const body = parseBody(req);
      // The actor is read here, not inside the action, for rolesAction()'s
      // reason: the management API calls the same function with an actor of its
      // own.
      const state = gateStateFor(req);
      const result = adminActions.truststoreAction(body,
        { actor: (state && state.username) || '', via: 'console' });
      self.respondToAction(req, res, '/admin/tls/trust' +
        queryWith(self.listViewFromBack('/admin/tls/trust', body.back), {}),
        result);
      log.debug("Leaving the admin truststore action endpoint.");
    });

    // -------------------------------------------------------------------------
    // GET /admin/kerberos/principals — THE STORED KERBEROS KEYS (2026-09-12).
    //
    // Two tables: the directory people who hold Kerberos keys derived from
    // their own password, and the service principals an operator created here
    // with a random key. `admin-core/admin_views.ts`'s
    // `kerberosPrincipalsJson()` computes both, from the PUBLIC half of each
    // pair of attributes, so nothing is opened to draw this page and no key is
    // on it. Every change is `kerberosPrincipalsAction()` in
    // `admin-core/admin_actions.ts`, the function `POST
    // /admin-api/kerberos/principals/{action}` calls.
    //
    // **A CREATE AND A ROTATE ANSWER WITH A PAGE, NOT A 303 — AND NOT A FILE.**
    // The keytab exists outside the seal exactly once, in the answer to the
    // request that made it. `respondToAction()` puts its message on a query
    // string, which is the browser history and every access log, so it cannot
    // carry a key. A bare file download was the other candidate and it was not
    // taken: the answer has to say WHICH kvno and enctypes were minted, and a
    // download is a response the browser does not draw. So the page names the
    // principal and the kvno and carries the keytab twice — a `data:` link with
    // a `download` attribute, which needs no script, and the base64 in a
    // textarea for a terminal — marked shown-once, in a `no-store` response.
    // `/admin/pki/person` hands a private key over the same way and for the
    // same reason.
    //
    // **NO SCRIPT**, and the argument is made rather than cited: nothing here
    // needs one. A download link is markup, and every control is a form.
    // -------------------------------------------------------------------------
    app.get('/admin/kerberos/principals', function (req, res) {
      log.debug("Entering the admin Kerberos principals page.");
      const json = adminViews.kerberosPrincipalsJson(req);
      const state = gateStateFor(req);
      const mayChange = !!(state && state.write);
      const back = queryWith(self.listViewOf('/admin/kerberos/principals',
                                             req.query),
                             {});
      const peopleNav = self.pageNavPair('/admin/kerberos/principals',
        pageParamsOf(req.query),
        Object.assign({ param: 'peoplePage', noun: 'people', offset: 0 },
                      json.peoplePaging));
      const servicesNav = self.pageNavPair('/admin/kerberos/principals',
        pageParamsOf(req.query),
        Object.assign({ param: 'servicesPage', noun: 'service principals',
                        offset: 0 },
                      json.servicesPaging));
      const etypeList = function (etypes) {
        log.debug("Entering etypeList().");
        log.debug("Leaving etypeList().");
        return (etypes || []).map(function (one) {
          return '<code>' + self.esc(one.name) + '</code>';
        }).join(' ');
      };

      // THE PREVIOUS VERSIONS A KEY STILL KEEPS (2026-09-12): kvno, enctypes
      // and when each stops being accepted. Never a key — the row carries none.
      const retainedCell = function (retained) {
        log.debug("Entering retainedCell().");
        log.debug("Leaving retainedCell().");
        return (retained || []).length
          ? retained.map(function (one) {
              return 'kvno ' + self.esc(String(one.kvno)) + ' ' +
                     etypeList(one.etypes) +
                '<br><span class="sub">until ' + self.esc(one.expiresAt) +
                '</span>';
            }).join('<br>')
          : '<span class="sub">none</span>';
      };

      const rowButton = function (action, field, value, label, title) {
        log.debug("Entering rowButton().");
        log.debug("Leaving rowButton().");
        return '<form method="post" action="/admin/kerberos/principals">' +
          '<input type="hidden" name="action" value="' + self.esc(action) +
          '"><input type="hidden" name="' + self.esc(field) + '" value="' +
          self.esc(value) +
          '"><input ' +
          'type="hidden" name="back" value="' + self.esc(back) + '">' +
          '<button type="submit" class="secondary" title="' + self.esc(title) +
          '">' +
          self.esc(label) + '</button></form>';
      };
      const serviceRows = json.services.map(function (row) {
        return '<tr><td><code>' + self.esc(row.principal) + '</code>' +
          (row.held ? '' : '<div class="state-invalid">' +
            self.esc('The key record is missing; the info attribute is all ' +
                     'that is left.') +
            '</div>') + '</td>' +
          '<td>' + self.esc(row.kvno == null ? '—' : String(row.kvno)) +
          '</td><td>' + etypeList(row.etypes) + '</td>' +
          '<td class="sub">' + self.esc(row.createdAt || '—') +
          (row.rotatedAt ? '<br>rotated ' + self.esc(row.rotatedAt) : '') +
          '</td><td>' + retainedCell(row.retained) + '</td>' +
          '<td>' + (row.sealed ? 'sealed' : 'clear (development)') + '</td>' +
          '<td>' + (mayChange
            ? rowButton('rotate-service', 'spn', row.spn, 'Rotate and show ' +
                                                          'the keytab',
                'Makes new random keys at kvno ' + (Number(row.kvno) + 1) +
                ' and shows their keytab once, with the kvno it replaces ' +
                'still in it. Tickets already issued under the current key ' +
                'go on being accepted for krb5.retainedKeyTtlS.') +
              ((row.retained || []).length
                ? rowButton('drop-previous-service-keys', 'spn', row.spn,
                    'Drop previous versions',
                    'Stops accepting tickets under kvno ' +
                    row.retained.map(function (one) {
                      return one.kvno;
                    }).join(', ') + ' now, rather than when their window ' +
                    'ends. The current key is untouched.')
                : '') +
              rowButton('delete-service', 'spn', row.spn, 'Delete the key',
                'Removes the stored key and every previous version. Tickets ' +
                'for this SPN are then keyed as they were before a key was ' +
                'stored.')
            : '<span class="sub">Admin Write</span>') + '</td></tr>';
      }).join('') || '<tr><td colspan="7">' + self.esc('No service principal ' +
        'holds a stored key.') + '</td></tr>';
      const peopleRows = json.people.map(function (row) {
        return '<tr><td><code>' + self.esc(row.principal) + '</code></td>' +
          '<td>' + self.esc(row.kvno == null ? '—' : String(row.kvno)) +
          '</td><td>' + etypeList(row.etypes) + '</td>' +
          '<td class="sub">' + self.esc(row.derivedAt || '—') +
          (row.derivedOn ? '<br>on a password ' + self.esc(row.derivedOn) :
           '') +
          '</td><td>' + (row.current ? 'yes'
            : '<span class="state-invalid" title="' +
              self.esc('The password on the entry is not the one these keys ' +
                'were derived from, so the KDC refuses them. The next ' +
                'verified sign-in derives new ones.') + '">no</span>') +
          '</td>' +
          '<td>' + retainedCell(row.retained) + '</td>' +
          '<td>' + (row.sealed ? 'sealed' : 'clear (development)') + '</td>' +
          '<td>' + (mayChange
            ? rowButton('clear-person-keys', 'username', row.username,
                        'Clear ' +
                'the keys',
                'Removes this person\'s Kerberos keys, previous versions ' +
                'included. Their next AS-REQ is refused with "sign in once"; ' +
                'their next verified sign-in derives new keys at the next ' +
                'kvno.') +
              ((row.retained || []).length
                ? rowButton('drop-previous-person-keys', 'username',
                            row.username,
                    'Drop previous versions',
                    'Stops accepting tickets sealed under kvno ' +
                    row.retained.map(function (one) { return one.kvno; })
                                .join(', ') +
                    ' — this person\'s keys before their last password ' +
                    'change — now. Their current keys, and so their sign-in, ' +
                    'are untouched.')
                : '')
            : '<span class="sub">Admin Write</span>') + '</td></tr>';
      }).join('') || '<tr><td colspan="8">' + self.esc(json.productKdc
        ? 'Nobody holds Kerberos keys yet. A person gets them the first time ' +
          'their password is set or verified.'
        : 'Development mode keys every user from krb5.userPassword, so ' +
          'nobody here holds stored keys.') + '</td></tr>';

      const inner = self.messagesOf(req) +
        self.note('<strong>Who this KDC holds a stored key for.</strong> ' +
                  self.esc(json.notes.mode)) +
        self.warn('<strong>No key is on this page, and none is in its ' +
          'JSON.</strong> ' +
                  self.esc(json.notes.keys)) +
        self.note(self.esc(json.notes.realm) + ' ' +
                  self.esc(json.notes.window)) +
        self.note('<strong>Previous key versions.</strong> ' +
                  self.esc(json.notes.previous) +
                  ' Now: ' + self.esc(String(json.retention.versions)) + ' ' +
                    'kept, each for ' +
                  self.esc(String(json.retention.ttlSeconds)) + ' seconds' +
                  (json.retention.ttlSetting > 0 ? '' : ' (the ticket ' +
                    'lifetime plus the clock skew, because ' +
                    '<code>krb5.retainedKeyTtlS</code> is 0)') + '.') +
        '<div class="tiles">' +
        self.tile(json.peopleTotal, 'people with keys') +
        self.tile(json.servicesTotal, 'service principals') +
        self.tile(json.enctypes.length, 'enctypes (krb5.enctypes)') +
        self.tile(json.startingKvno, 'starting kvno') +
        '</div>' +
        self.note('The acceptor on <code>krb5.servicePort</code> and ' +
                  '<code>/authn/spnego</code> answers as ' +
                  '<code>' + self.esc(json.acceptor.spn || '(no SPN)') +
                  '</code>' +
                  (json.acceptor.storedKey
                    ? ', keyed from a service principal stored here.'
                    : json.acceptor.available
                      ? ', keyed from krb5.servicePassword. Creating that ' +
                        'SPN here gives it a random key and a keytab instead.'
                      : ', and holds no key: create that SPN here to give it ' +
                        'one.') +
                  ' <code>krb5.personKeys</code> is ' +
                  (json.personKeys ? 'on' : 'OFF') + '.') +
        '<h2>Service principals</h2>' + servicesNav.head +
        '<table><tr><th>Principal</th><th>kvno</th><th>Enctypes</th>' +
        '<th>Created</th><th>Previous versions</th><th>At ' +
        'rest</th><th></th></tr>' +
        serviceRows + '</table>' +
        servicesNav.foot +
        (mayChange
          ? '<h3>Create a service principal</h3>' +
            self.note('A service principal name — ' +
                      '<code>HTTP/web.example.com</code>, with or without ' +
                      '<code>@' + self.esc(json.realm) + '</code>. It gets a ' +
                      'RANDOM key for every enctype in ' +
                      '<code>krb5.enctypes</code> at kvno ' +
                      self.esc(String(json.startingKvno)) + ', stored sealed ' +
                      'on its application entry, and the next page shows its ' +
                      'keytab ONCE. <code>krbtgt</code> is refused: that key ' +
                      'is <code>krb5.krbtgtPassword</code>.') +
            '<form method="post" action="/admin/kerberos/principals">' +
            '<input type="hidden" name="action" value="create-service">' +
            '<input type="hidden" name="back" value="' + self.esc(back) + '">' +
            '<div class="formrow"><label for="krb5-spn">SPN</label>' +
            '<input type="text" id="krb5-spn" name="spn" size="40" ' +
            'placeholder="HTTP/web.example.com">' +
            '<button type="submit">Create and show the ' +
            'keytab</button></div></form>'
          : self.note('Creating, rotating and deleting a service principal ' +
                      'needs <strong>Admin Write</strong>.')) +
        '<h2>People</h2>' + peopleNav.head +
        '<table><tr><th>Principal</th><th>kvno</th><th>Enctypes</th>' +
        '<th>Derived</th><th>Matches the password</th><th>Previous ' +
        'versions</th><th>At rest</th><th></th></tr>' +
        peopleRows + '</table>' + peopleNav.foot +
        self.perPageForm('/admin/kerberos/principals', 'peoplePage', '1',
                         json.peoplePaging.perPage) +
        '<h2>What there is deliberately no button for</h2>' +
        self.note('<strong>Downloading a keytab again.</strong> A stored key ' +
                  'is never read back out: the keytab is handed over by the ' +
                  'create or rotate that made it, and a lost one is replaced ' +
                  'by rotating. <strong>Setting a person\'s keys.</strong> ' +
                  'They come from the person\'s password and from nothing ' +
                  'else; clearing them is the one control, and the next ' +
                  'verified sign-in derives them again.') +
        self.note('<a href="/admin/kerberos/principals?format=json">this ' +
                  'page as JSON</a> &middot; <a ' +
                  'href="/admin-api/kerberos/principals">the same over the ' +
                  'management API</a> &middot; <a href="/admin/kerberos">the ' +
                  'Kerberos settings</a> &middot; <a ' +
                  'href="/krb5/principals">the principal database</a>');

      self.respond(req, res, json, 'Kerberos principals',
                   '/admin/kerberos/principals',
                   inner);
      log.debug("Leaving the admin Kerberos principals page.");
    });

    app.post('/admin/kerberos/principals', function (req, res) {
      log.debug("Entering the admin Kerberos principals action endpoint.");
      const body = parseBody(req);
      const state = gateStateFor(req);
      const result = adminActions.kerberosPrincipalsAction(body,
        { actor: (state && state.username) || '', via: 'console' });
      const isJson = /json/i.test(String(req.headers['content-type'] || ''));
      if (result.ok && result.keytab && !isJson) {
        // THE ONE TIME THE KEYTAB IS DRAWN. See the header above the GET.
        const shown = Object.assign({}, result);
        delete shown.keytab;
        const inner = self.warn('<strong>This keytab is shown once.</strong> ' +
            'It is not kept in the clear anywhere and this service cannot ' +
            'show it again; a lost keytab is replaced by rotating, which ' +
            'makes a new key.') +
          self.note(self.esc(result.message)) +
          '<table class="key">' +
          '<tr><th>Principal</th><td><code>' + self.esc(result.principal) +
          '</code></td></tr><tr><th>kvno</th><td>' +
          self.esc(String(result.kvno)) +
          '</td></tr>' +
          ((result.keytabKvnos || []).length > 1
            ? '<tr><th>Versions in the keytab</th><td>' +
              self.esc(result.keytabKvnos.join(', ')) + ' — the previous ' +
              'version stays in it, as after <code>ktadd</code>, for tickets ' +
              'already issued under it</td></tr>'
            : '') +
          '<tr><th>Enctypes</th><td>' + result.etypes.map(function (etype) {
            return '<code>' + self.esc(String(etype)) + '</code>';
          }).join(' ') + '</td></tr></table>' +
          '<p><a class="btn" download="' + self.esc(result.keytabFilename) +
          '" href="data:application/octet-stream;base64,' +
          self.esc(result.keytab) +
          '">Save ' + self.esc(result.keytabFilename) + '</a></p>' +
          self.note('Or, in a terminal: <code>base64 -d &gt; ' +
                    self.esc(result.keytabFilename) +
                    '</code> and paste the text below, then <code>klist -k ' +
                    '-e ' +
                    self.esc(result.keytabFilename) +
                    '</code> to see the entries.') +
          '<textarea readonly rows="6" name="keytab-base64">' +
          self.esc(result.keytab) +
          '</textarea>' +
          self.note('<a href="/admin/kerberos/principals">Back to the ' +
                    'Kerberos principals</a>');
        self.respond(req, res, shown, 'Kerberos keytab',
                     '/admin/kerberos/principals',
                     inner);
        log.debug("Leaving the admin Kerberos principals action endpoint. " +
                  "Keytab shown.");
        return;
      }
      self.respondToAction(req, res, '/admin/kerberos/principals' +
        queryWith(self.listViewFromBack('/admin/kerberos/principals',
                                        body.back), {}),
                           result);
      log.debug("Leaving the admin Kerberos principals action endpoint.");
    });

    app.get('/admin/ssf', function (req, res) {
      log.debug("Entering the admin Shared Signals page.");
      const json = ssfJson(req);

      const tiles = '<div class="tiles">' +
        self.tile(json.streamDetail ? json.streamDetail.length : 0, 'streams') +
        self.tile((json.streamDetail || []).filter(function (row) {
          return row.status === 'enabled';
        }).length, 'enabled') +
        self.tile((json.streamDetail || []).reduce(function (n, row) {
          return n + row.counters.delivered;
        }, 0), 'events delivered') +
        self.tile((json.streamDetail || []).reduce(function (n, row) {
          return n + row.counters.failed;
        }, 0), 'deliveries refused') +
        self.tile((json.streamDetail || []).reduce(function (n, row) {
          return n + row.queue.length;
        }, 0), 'waiting') +
        self.tile((json.streamDetail || []).filter(function (row) {
          return row.dead;
        }).length, 'dead streams') +
        self.tile((json.streamDetail || []).reduce(function (n, row) {
          return n + (row.deadLetters || []).length;
        }, 0), 'dead letters') +
        self.tile((json.receivedDetail || []).length, 'received here') +
        '</div>';

      const receivedRows = (json.receivedDetail || []).length
        ? json.receivedDetail.slice(0, 25).map(function (row) {
            return '<tr><td class="sub">' + self.esc(row.at) + '</td><td>' +
              self.esc(row.summary ? row.summary.name : '(unreadable)') +
              '</td>' +
              '<td><code>' + self.esc(row.summary ? row.summary.jti : '') +
              '</code></td>' +
              '<td class="' + (row.verified ? 'state-valid' : 'sub') + '">' +
              self.esc(row.verified ? 'verified' : 'not verified here') +
              '</td>' +
              '<td class="' + (row.correctMediaType ? 'sub' : 'state-invalid') +
              '"><code>' + self.esc(row.contentType || '(none)') +
              '</code></td></tr>';
          }).join('')
        : '<tr><td colspan="5">Nothing has been pushed at this ' +
          'service.</td></tr>';

      const inner = self.messagesOf(req) +
        (!json.installed
          ? '<div class="err"><strong>Shared Signals is not loaded in this ' +
            'process.</strong> The module registers no routes here, so there ' +
            'is nothing to report. Everything else on this console is ' +
            'unaffected.</div>'
          : '') +
        (json.installed && !json.enabled
          ? self.warn('<strong>SSF is turned off</strong> ' +
            '(<code>ssf.enabled</code>). The routes are still registered and ' +
            'answer <code>501</code> rather than <code>404</code>, because ' +
            'the feature being off and the URL being wrong are different ' +
            'sentences to a client. The transmitter metadata still answers, ' +
            'so a receiver can discover that this service speaks SSF and is ' +
            'not currently doing it. Turn it back on in the settings at the ' +
            'foot of this page.')
          : '') +

        self.note('The <strong>Shared Signals Framework</strong> (OpenID SSF ' +
        '1.0, final 2 September 2025) is the one protocol family here that ' +
        'TALKS BACK. Every other family answers a request; this one delivers ' +
        'an event nobody asked for, at the moment it happens, to somebody ' +
        'who agreed in advance to be told. What it solves is that SAML and ' +
        'OpenID Connect authenticate at ONE INSTANT and the relying party ' +
        'then holds a session for hours whatever happens next.') +

        self.warn('<strong>SSF is the pipe and not the vocabulary.</strong> ' +
        'It defines how two parties agree a stream, who the events are about ' +
        '(RFC 9493), what they travel in (RFC 8417) and how they get there ' +
        '(RFC 8935 push, RFC 8936 poll) &mdash; and exactly TWO events of ' +
        'its own, both about the pipe. The vocabularies are ' +
        '<strong>CAEP</strong> (what happened to a session) and ' +
        '<strong>RISC</strong> (what happened to an account). <strong>CAEP ' +
        'is implemented</strong> &mdash; its eight session event types ' +
        'travel on the streams below, and <a href="/admin/caep">its own ' +
        'page</a> carries the settings, the catalogue and the by-hand emit ' +
        'form while <a href="/admin/caep-sessions">CAEP sessions</a> counts ' +
        'what has been said about whom. RISC is the third part and is not ' +
        'here yet. What changed with CAEP is the sentence this page used to ' +
        'carry next: this service DOES now generate an event on its own, ' +
        'when a session starts, is presented or ends. ' +
        '<code>caep.autoEmit</code> puts the old behaviour back.') +

        tiles +

        (json.installed
          ? '<h2>Discovery</h2>' +
            self.note('Everything a receiver needs is at <code>' +
            self.esc(json.metadataUrl || '') + '</code>, which is ' +
            '<strong>never gated</strong>: a receiver has to be able to read ' +
            'what the endpoints are before it can authenticate to one. The ' +
            'issuer is <code>' + self.esc(json.issuer || '') + '</code> and ' +
            'every SET is signed with <code>' +
            self.esc(json.signingAlgorithm || '') + '</code>.') +
            self.note('That algorithm is <code>ssf.signingAlgorithm</code> ' +
            'and it reaches the whole table, <strong>post-quantum ' +
            'included</strong> &mdash; ML-DSA at three sizes, SLH-DSA at ' +
            'two, and the six composite ML-DSA + traditional ones &mdash; ' +
            'because a SET is signed through the same signer every other JWT ' +
            'here goes through. This is the document most worth signing that ' +
            'way: it records that something HAPPENED, RFC 8417 section 4.1.4 ' +
            'forbids it to expire, and it is therefore read long after it ' +
            'was written.')
          : '') +

        (json.installed
          ? '<h2>Streams</h2>' +
            ((json.streamDetail || []).length
              ? json.streamDetail.map(self.ssfStreamCard.bind(self)).join('')
              : self.note('No streams. A receiver creates one by POSTing a ' +
                'Stream Configuration to <code>' +
                self.esc((json.metadata &&
                          json.metadata.configuration_endpoint) ||
                         '/ssf/stream') + '</code>. There is deliberately no ' +
                '&ldquo;create a stream&rdquo; form here: a stream carries a ' +
                'delivery endpoint THIS SERVICE WILL DIAL, and the one place ' +
                'that URL may come from is a receiver that authenticated and ' +
                'asked &mdash; see <code>ssf/ssf_http.ts</code>.'))
          : '') +

        (json.installed
          ? '<h2>Pushed at this service</h2>' +
            self.note('The roles reversed. <code>POST /ssf/receive</code> ' +
            'accepts a Security Event Token pushed AT this service, which is ' +
            'what a client acting as the TRANSMITTER sends to. It accepts ' +
            'one whose signature does not verify and reports why &mdash; a ' +
            'receiver that refused could not show anybody what arrived, ' +
            'which is the question being asked. ' +
            '<code>ssf.receiveRequireSignature</code> turns the 400 on.') +
            '<table><tr><th>When</th><th>Event</th><th>jti</th>' +
            '<th>Signature</th><th>Content-Type</th></tr>' + receivedRows +
            '</table>' +
            '<form method="post" action="/admin/ssf"><div class="formrow">' +
            '<input type="hidden" name="action" value="clear-received">' +
            '<button class="secondary">Clear what has been received</button>' +
            '</div></form>'
          : '') +

        self.configFormsFor('/admin/ssf') +

        self.note('<a href="/ssf">What this is, for a person</a> &middot; ' +
        '<a href="/admin/ssf?format=json">this page as JSON</a> &middot; ' +
        '<a href="/admin-api/ssf">the same over the management API</a> ' +
        '&middot; <a href="/admin/applications">the receivers, as ' +
        'applications</a>');

      self.respond(req, res, json, 'Shared Signals', '/admin/ssf', inner);
      log.debug("Leaving the admin Shared Signals page.");
    });

    app.post('/admin/ssf', function (req, res) {
      log.debug("Entering the admin Shared Signals action endpoint.");
      const body = parseBody(req);
      // The one action handler in this console that awaits. Signing a Security
      // Event Token may be an ML-DSA or SLH-DSA signature on the worker pool,
      // and delivering it is a POST to somebody else's endpoint; answering
      // before either had happened would be this page reporting "sent" about
      // nothing.
      ssfAction(body).then(function (result) {
        self.respondToAction(req, res, '/admin/ssf', result);
        log.debug("Leaving the admin Shared Signals action endpoint.");
      }).catch(function (e) {
        // A rejected promise here is a bug in ssf/ssf.ts rather than anything a
        // request can cause — consoleAction() resolves a refusal rather than
        // throwing one — so it is reported as a refusal naming the message
        // instead of becoming an unhandled rejection that ends the process.
        log.error(errorCodes.tag('STS-ADMIN-0013') + 'admin: the Shared ' +
                                                     'Signals action threw: ' +
                                                     e.message);
        self.respondToAction(req, res, '/admin/ssf',
          { ok: false, errorCode: 'STS-ADMIN-0013',
            errors: ['The action failed: ' + e.message] });
        log.debug("Leaving the admin Shared Signals action endpoint. Threw.");
      });
    });

    // -------------------------------------------------------------------------
    // PROTOCOLS -> CAEP. The settings, the catalogue, and the one control this
    // profile needs that nothing else can produce.
    // -------------------------------------------------------------------------
    app.get('/admin/caep', function (req, res) {
      log.debug("Entering the admin CAEP page.");
      const json = caepJson(req);

      const catalogue = (json.catalogue || []).map(function (row) {
        const members = row.members.map(function (member) {
          return '<tr><td><code>' + self.esc(member.name) + '</code></td>' +
            '<td class="' + (member.required ? '' : 'sub') + '">' +
            (member.required ? 'required' : 'optional') + '</td>' +
            '<td class="sub"><code>' + self.esc(member.type) + '</code>' +
            (member.values.length
              ? ' ' + self.esc(member.values.join(' | ')) : '') + '</td>' +
            '<td class="sub">' + self.esc(member.what) + '</td></tr>';
        }).join('');
        return '<div class="card"><h3>' + self.esc(row.name) + ' &mdash; ' +
          '<code>' +
          self.esc(row.short) + '</code>' +
          (row.offered ? '' :
           ' <span class="state-invalid">not offered</span>') +
          '</h3><div class="sub">' + self.esc(row.what) + '</div>' +
          '<table><tr><th>Member</th><th></th><th>Type</th><th>What it is' +
          '</th></tr>' + members + '</table></div>';
      }).join('');

      // WHICH SESSION THE FORM WILL EMIT ABOUT, and it comes off the query
      // string now rather than out of a `<select>`. See caepSessionChooser(). A
      // `?session=` naming a row that is gone — a stale link, a session revoked
      // since the reader searched — resolves to nothing and the form says so
      // rather than posting an identifier the register will not recognise.
      const here = { path: '/admin/caep', query: req.query };
      const wantedSession = queryOne(here.query, 'session').trim();
      const liveSessions = (json.sessions || []).filter(function (row) {
        return String(row.state || '') !== 'revoked';
      });
      let picked = null;
      liveSessions.forEach(function (row) {
        if (String(row.sessionId || '') === wantedSession) {
          picked = row;
        }
      });
      const sessionChooser = self.caepSessionChooser(here, json.sessions || [],
          picked ? picked.sessionId : '');

      const typeOptions = (json.catalogue || []).map(function (row) {
        return '<option value="' + self.esc(row.short) + '">' +
               self.esc(row.name) +
          '</option>';
      }).join('');

      const inner = self.messagesOf(req) +
        (!json.installed
          ? '<div class="err"><strong>Shared Signals is not loaded in this ' +
            'process</strong>, so CAEP has nothing to run on. CAEP is a ' +
            'VOCABULARY over SSF rather than a family of its own: its events ' +
            'travel on SSF streams, are signed by the SSF signer and are ' +
            'delivered by the two SSF deliveries.</div>'
          : '') +
        (json.installed && !json.enabled
          ? self.warn('<strong>CAEP is turned off</strong> ' +
            '(<code>caep.enabled</code>). The eight event types are dropped ' +
            'from <code>events_supported</code>, so a stream asking for one ' +
            'gets it back missing from <code>events_delivered</code> &mdash; ' +
            'which is the only notice SSF gives, and is exactly the case a ' +
            'receiver ought to be tested against. SSF itself is unaffected.')
          : '') +

        self.note('<strong>CAEP</strong> (OpenID Continuous Access ' +
        'Evaluation Profile 1.0, final 2 September 2025) is the enterprise ' +
        '<strong>session</strong> vocabulary spoken over ' +
        '<a href="/admin/ssf">Shared Signals</a>. SSF is the PIPE and ' +
        'defines two events of its own, both about the pipe; these eight are ' +
        'about a session, and the sentence they carry is <em>this session is ' +
        'no longer trustworthy</em>. RISC says <em>this account is no longer ' +
        'trustworthy</em>, which is a different sentence and the whole ' +
        'reason there are two profiles.') +

        self.warn('<strong>This is the one place in this service where an ' +
        'endpoint is not what starts the work.</strong> With ' +
        '<code>caep.autoEmit</code> on, a sign-in emits ' +
        '<code>session-established</code>, an authorization request answered ' +
        'from a session that already existed emits ' +
        '<code>session-presented</code>, and a sign-out emits ' +
        '<code>session-revoked</code> &mdash; on every stream that asked for ' +
        'the type and whose subjects cover that session, with nobody having ' +
        'typed anything. Every other family here answers a request. Turning ' +
        'it off restores the older and equally honest behaviour: every ' +
        'Security Event Token this service sends was asked for.') +

        '<div class="tiles">' +
        self.tile(json.tracked || 0, 'sessions tracked') +
        self.tile((json.eventTypes || []).length, 'event types') +
        self.tile(Object.keys(json.totals || {}).reduce(function (n, uri) {
          return n + json.totals[uri];
        }, 0), 'events sent') +
        self.tile((json.autoEmitActs || []).length, 'acts emit on their own') +
        '</div>' +

        (json.installed
          ? '<h2>Emit one by hand</h2>' +
            self.note('Five of the eight describe things nothing here does ' +
            '&mdash; no device reports compliance to this service and no ' +
            'risk engine talks to it &mdash; so this form is the only way ' +
            'they are ever produced. The subject is composed from the ' +
            'session you pick: SSF\'s <strong>complex</strong> subject, ' +
            'naming the person AND the session, because the person is not ' +
            'revoked and one session of theirs is. Leave the payload empty ' +
            'for a conforming specimen of the type.') +
            sessionChooser +
            (picked
              ? '<p class="note">Emitting about <strong>' +
                self.esc(String(picked.username || picked.sub || '(unnamed)')) +
                '</strong>&rsquo;s session <code>' +
                self.esc(picked.sessionId) +
                '</code>' +
                (picked.protocol
                  ? ', established over ' + self.esc(picked.protocol) : '') +
                '. The subject below is composed from it.</p>'
              : (wantedSession
                  ? self.warn('No live session has the id <code>' +
                    self.esc(wantedSession) + '</code>. It may have been ' +
                    'signed out since this link was made &mdash; a revoked ' +
                    'row stays on <a href="/admin/caep-sessions">the ' +
                    'sessions page</a> and is not offered here. Search again ' +
                    'above.')
                  : self.note('Pick a session above and the form below will ' +
                    'emit about it. Until one is picked there is nothing to ' +
                    'be about: a CAEP event names a SESSION, and this form ' +
                    'will not compose a subject out of an identifier nobody ' +
                    'chose.'))) +
            (picked
              ? '<form method="post" action="/admin/caep"><div ' +
            'class="formrow"><input type="hidden" name="action" value="emit">' +
            '<input type="hidden" name="session_id" value="' +
            self.esc(picked.sessionId) + '">' +
            '<label>Event <select name="type">' + typeOptions +
            '</select></label> ' +
            '<label>Initiated by <select name="initiating_entity">' +
            '<option value="admin">admin</option>' +
            '<option value="user">user</option>' +
            '<option value="policy">policy</option>' +
            '<option value="system">system</option></select></label>' +
            '</div><div class="formrow">' +
            '<label>Payload (JSON, optional) ' +
            '<input type="text" name="payload" size="60" ' +
            'placeholder="{&quot;current_status&quot;:' +
            '&quot;not-compliant&quot;}">' +
            '</label>' +
            '</div><div class="formrow">' +
            '<label>reason_admin <input type="text" name="reason_admin" ' +
            'size="40"></label> ' +
            '<label>reason_user <input type="text" name="reason_user" ' +
            'size="40"></label>' +
            '</div><div class="formrow"><button>Emit</button></div></form>'
              : '') +
            (liveSessions.length
              ? ''
              : self.warn('There are no LIVE sessions to emit about. A CAEP ' +
                'event is ABOUT a session &mdash; the subject names one ' +
                '&mdash; so sign somebody in first. Anything that starts a ' +
                'session will do, and that is the point rather than a ' +
                'convenience: CAEP is a vocabulary about SESSIONS and not ' +
                'about OAuth, so an OIDC flow, a SAML 2.0 or SAML 1.1 ' +
                'sign-in, WS-Federation and SPNEGO all reach the same funnel ' +
                'here and all produce a row.'))
          : '') +

        // THE CATALOGUE IS FOLDED AND STARTS CLOSED, which is the one place on
        // this console where a section rather than a paragraph is behind a
        // disclosure. Eight cards, each with a table of the event's own
        // members, is five screens of REFERENCE sitting between the settings
        // above it and the links below — so the controls somebody came for were
        // off the bottom of the page on every visit, and the reference they
        // were scrolling past is the half nobody needs twice. Closed by default
        // rather than open: a reader who wants the members knows they want
        // them, and the summary says how many are there. Native <details>, so
        // `script-src 'none'` is untouched — the same answer note() gives, one
        // level up.
        (json.installed
          ? '<details class="fold section"><summary>The eight event types' +
            '</summary><div class="foldbody">' +
            self.note('Each row\'s members are the specification\'s own, ' +
            'with the four CAEP section 2 gives EVERY event beneath them: ' +
            '<code>event_timestamp</code>, <code>initiating_entity</code>, ' +
            '<code>reason_admin</code> and <code>reason_user</code>. All ' +
            'four are OPTIONAL, which surprises people about the first ' +
            '&mdash; a receiver deciding whether to end a session wants it ' +
            'more than anything else in the payload, and a conforming ' +
            'transmitter need not send one. ' +
            '<code>caep.omitEventTimestamp</code> produces that event on ' +
            'purpose.') +
            catalogue +
            '</div></details>'
          : '') +

        self.configFormsFor('/admin/caep') +

        self.note('<a href="/admin/caep-sessions">The sessions and what has ' +
        'been said about them</a> &middot; ' +
        '<a href="/admin/ssf">the streams it all goes out on</a> &middot; ' +
        '<a href="/admin/caep?format=json">this page as JSON</a> &middot; ' +
        '<a href="/admin-api/caep">the same over the management API</a>');

      self.respond(req, res, json, 'CAEP', '/admin/caep', inner);
      log.debug("Leaving the admin CAEP page.");
    });

    app.post('/admin/caep', function (req, res) {
      log.debug("Entering the admin CAEP action endpoint.");
      const body = parseBody(req);
      // THE ONE CAEP ACTION HANDLER, AND THREE PAGES POST TO IT: this one, the
      // sessions table and the one-session drill-down. `from` says which, read
      // as an ENUM and never as a path — the rule backTo() follows on the
      // tokens page, and the reason a hand-written field cannot make this an
      // open redirect.
      const back = self.caepSessionsBackTo(body);
      caepAction(body).then(function (result) {
        self.respondToAction(req, res, back, result);
        log.debug("Leaving the admin CAEP action endpoint.");
      }).catch(function (e) {
        log.error(errorCodes.tag('STS-ADMIN-0013') + 'admin: the CAEP action ' +
                                                     'threw: ' + e.message);
        self.respondToAction(req, res, back,
          { ok: false, errorCode: 'STS-ADMIN-0013',
            errors: ['The action failed: ' + e.message] });
        log.debug("Leaving the admin CAEP action endpoint. Threw.");
      });
    });

    app.get('/admin/caep-sessions', function (req, res) {
      log.debug("Entering the admin CAEP sessions page.");
      const json = caepJson(req);
      const shorts = self.caepShortNames(json);
      const prefix = 'https://schemas.openid.net/secevent/caep/event-type/';

      const headers = shorts.map(function (short) {
        return '<th title="' + self.esc(prefix + short) + '">' +
          self.esc(short.replace('session-', '').replace('-change', '')) +
          '</th>';
      }).join('');

      // ---------------------------------------------------------------------
      // THE SEARCH AND THE PAGING OVER THE SESSIONS TABLE (2026-09-04).
      //
      // This list is unbounded up to `caep.maxSessionsTracked`, which is 200 by
      // default and settable to anything — one row per session this service has
      // held, including every session it has since forgotten — so the page grew
      // by one row per sign-in for the life of the process and the streams
      // table under it went out of reach. It is `sectionSearchForm` +
      // `pagedRows` + `pageNavPair`, the arrangement every other list in this
      // console has, and the parameters are NAMED after the list for
      // pagingOf()'s reason: this page carries a second table and a bare `page`
      // could not serve both.
      //
      // THE SEARCH IS OVER WHO AND WHICH, because those are the two things a
      // reader arrives holding — a username somebody complained about, or a
      // session identifier out of a log — and they do not know which column it
      // will be in. The subject spelling is matched too, since that is what a
      // receiver saw and is therefore what a reader comparing a Security Event
      // Token against this page has in front of them.
      //
      // Filter first, then page — pagingOf()'s rule, for its reason: paging a
      // list and then filtering it gives a page 2 whose length depends on what
      // page 1 happened to hold.
      const state = caepSessionsState(req, json);
      const sessWanted = state.wanted;
      const sessPage = state.page;
      const appState = caepApplicationsState(req, json);
      const appNav = self.pageNavPair('/admin/caep-sessions',
                                      pageParamsOf(req.query),
                                      appState.page.paging);
      // EVERY parameter the reader is already carrying, so that paging this
      // table moves nothing else on the page. pageNavPair() overrides only the
      // one name off the paging object it is handed.
      const navParams = pageParamsOf(req.query);
      const sessNav = self.pageNavPair('/admin/caep-sessions', navParams,
                                       sessPage.paging);
      // The list AS THE READER LEFT IT, for the drill-down links and for the
      // Reset buttons' `back` field. listViewOf() is the whitelist; `back` is
      // the same set as a query string, rebuilt from it rather than echoed.
      const listView = self.listViewOf('/admin/caep-sessions', req.query);
      const back = queryWith(listView, {});

      const rows = sessPage.shown.length
        ? sessPage.shown.map(function (row) {
            return self.caepSessionRow(row, shorts, prefix, listView, back);
          }).join('')
        : '<tr><td colspan="' + (shorts.length + 8) + '">' +
          (sessWanted
            ? 'No tracked session matches <code>' + self.esc(sessWanted) +
              '</code>. ' +
              ((json.sessions || []).length
                ? (json.sessions || []).length + ' session(s) are tracked ' +
                  'under other names.'
                : 'None is tracked at all yet.')
            : 'No sessions are tracked. Sign somebody in &mdash; an OIDC ' +
              'flow, a SAML 2.0 sign-in, WS-Federation &mdash; and a row ' +
              'appears here whether or not any stream is agreed, which is ' +
              'what makes &ldquo;nothing arrived&rdquo; traceable to ' +
              '&ldquo;nobody asked&rdquo;.') +
          '</td></tr>';

      const streamRows = (json.streams || []).length
        ? json.streams.map(function (row) {
            return '<tr><td><code>' + self.esc(row.stream_id) + '</code></td>' +
              '<td class="sub">' + self.esc(row.aud) + '</td><td class="' +
              (row.status === 'enabled' ? 'state-valid' : 'sub') +
              '">' + self.esc(row.status) + '</td>' +
              '<td class="sub">' + self.esc(row.delivery) + '</td>' +
              '<td class="sub">' + self.esc(String(row.subjects)) + '</td>' +
              '<td class="' + (row.takes.length ? '' : 'state-invalid') + '">' +
              self.esc(row.takes.length ? row.takes.join(', ')
                : 'none of CAEP\'s eight') + '</td></tr>';
          }).join('')
        : '<tr><td colspan="6">No streams. Nothing this service says about a ' +
          'session has anywhere to go, and every row above will show a count ' +
          'of zero however many people sign in.</td></tr>';

      const inner = self.messagesOf(req) +
        (!json.installed
          ? '<div class="err"><strong>Shared Signals is not loaded in this ' +
            'process</strong>, so no session state is tracked.</div>'
          : '') +
        (json.installed && !json.enabled
          ? self.warn('<strong>CAEP is turned off</strong> ' +
            '(<code>caep.enabled</code>), so nothing new is being recorded ' +
            'here. What is below is what was recorded before it was turned ' +
            'off.')
          : '') +
        (json.installed && json.enabled && !json.autoEmit
          ? self.warn('<strong>Automatic emission is off</strong> ' +
            '(<code>caep.autoEmit</code>). Sessions still appear here and ' +
            'their state still follows what really happens, and nothing goes ' +
            'out unless somebody emits it from <a href="/admin/caep">the ' +
            'CAEP page</a>. That is this service\'s older behaviour: every ' +
            'Security Event Token it sends was asked for.')
          : '') +

        self.note('One row per session this service has held, ' +
        '<strong>including sessions it no longer holds</strong>. That is ' +
        'deliberate and it is the whole reason this page exists: the session ' +
        'store forgets a session the moment it is signed out, so a row whose ' +
        'state is <code>revoked</code> is the only remaining evidence that ' +
        'it existed and was revoked. The counts are per event type and they ' +
        'never forget; WHICH events those were is <a ' +
        'href="/admin/caep-sessions/session">each session\'s own page</a>, ' +
        'which is a different question and is a click away from every ' +
        'identifier below.') +

        (json.installed && json.omitEventTimestamp
          ? self.warn('<code>caep.omitEventTimestamp</code> is on, so every ' +
            'event leaving here carries NO <code>event_timestamp</code>. ' +
            'That is perfectly conforming &mdash; CAEP section 2 makes it ' +
            'optional &mdash; and it is what a receiver that assumes one ' +
            'falls over on.')
          : '') +

        '<div class="tiles">' +
        self.tile(json.tracked || 0, 'sessions') +
        self.tile((json.sessions || []).filter(function (row) {
          return row.state === 'revoked';
        }).length, 'revoked') +
        self.tile((json.sessions || []).filter(function (row) {
          return row.state === 'presented';
        }).length, 'presented (SSO)') +
        self.tile(Object.keys(json.totals || {}).reduce(function (n, uri) {
          return n + json.totals[uri];
        }, 0), 'events sent') +
        self.tile((json.streams || []).filter(function (row) {
          return row.takes.length;
        }).length, 'streams taking CAEP') +
        '</div>' +

        '<h2>Sessions</h2>' +
        self.note('<strong>Every identifier in the first column opens that ' +
        'session on its own page</strong> &mdash; what has actually been ' +
        'sent about it, in order, with what the register noticed as each one ' +
        'was applied. That was a card per session under this table until ' +
        '2026-09-04, which meant this table was off the top of the screen ' +
        'the moment more than a handful of people had signed in.') +
        self.sectionSearchForm({
          path: '/admin/caep-sessions', query: req.query,
          param: 'sessq', pageParam: 'sessionsPage',
          label: 'Narrow to a person or a session',
          placeholder: 'a username, a subject or a session id',
          what: 'It matches the username, the <code>sub</code>, the session ' +
            'identifier, the SSF subject as a receiver was sent it, and the ' +
            'protocol the session was started through &mdash; because a ' +
            'reader arrives holding exactly one of those and does not know ' +
            'which column it will be in. It narrows this table only; the ' +
            'streams below it are about the transmitter rather than about ' +
            'anybody.'
        }) +
        self.perPageForm('/admin/caep-sessions', 'sessq', sessWanted,
                         sessPage.paging.perPage,
                         'Only the sessions table is paged. The streams ' +
                         'under it are one row per stream agreed with this ' +
                         'transmitter, which is a number somebody configured ' +
                         'rather than one that grows by itself.',
                         {}) +
        sessNav.head +
        '<table><tr><th>Session</th><th>Who</th><th>State</th>' +
        '<th>Assurance</th><th>Device</th><th>Risk</th>' + headers +
        '<th>Total</th><th></th></tr>' + rows + '</table>' +
        sessNav.foot +

        '<h2>Where a CAEP event could go</h2>' +
        self.note('A session with a count of zero almost always means ' +
        '<strong>no stream asked for that type</strong> rather than anything ' +
        'being wrong. SSF has no refusal for an event type a transmitter ' +
        'will not deliver &mdash; its absence from ' +
        '<code>events_delivered</code> is the only notice a receiver gets ' +
        '&mdash; so this table is where that shows up.') +
        '<table><tr><th>Stream</th><th>aud</th><th>Status</th><th>Delivery' +
        '</th>' +
        '<th>Subjects</th><th>CAEP types it takes</th></tr>' + streamRows +
        '</table>' +

        '<h2>Per application</h2>' +
        self.note('<strong>What this transmitter has said to each receiver, ' +
        'across every session.</strong> The table above this one is per ' +
        'STREAM and the one above that is per SESSION; this is the third ' +
        'question, and it is the one somebody actually arrives with once ' +
        'more than one receiver exists: <em>is the application I am testing ' +
        'getting anything, and what.</em> The counts are per event type and ' +
        'they never forget &mdash; they are counted when the Security Event ' +
        'Token is built and queued, which is when this service has SAID ' +
        'something, so a poll stream nobody has polled yet still shows what ' +
        'is waiting for it.') +
        self.note('<strong>An application with no stream is a row rather ' +
        'than an omission</strong>, and it is the commonest state a receiver ' +
        'under test is in: declared here with the Shared Signals box ticked, ' +
        'nothing agreed yet. Every count is zero and the <em>Streams</em> ' +
        'column says so, which is the first thing to check when nothing ' +
        'arrives. A row whose <em>Takes</em> column says <code>none of ' +
        'CAEP\'s eight</code> is the second: SSF has no refusal for a type a ' +
        'transmitter will not deliver, so the type\'s absence from ' +
        '<code>events_delivered</code> is the only notice a receiver ever ' +
        'gets.') +
        self.note('<strong>The identifier is what the receiver authenticated ' +
        'as when it created the stream, and <code>aud</code> is what it ' +
        'asked its Security Event Tokens to be addressed to.</strong> They ' +
        'are different fields and this is the only place both are shown: ' +
        '<code>aud</code> is REQUIRED on a stream and is deliberately never ' +
        'defaulted to the caller, because an audience this service invented ' +
        'would be one the receiver never learns it has to check for. Where ' +
        'they agree the column says so rather than repeating the name. A row ' +
        'named <em>(no application)</em> is the collected total for streams ' +
        'agreed while <code>ssf.authRequired</code> was off &mdash; there ' +
        'was no principal to record, and those events are real.') +
        self.sectionSearchForm({
          path: '/admin/caep-sessions', query: req.query,
          param: 'appq', pageParam: 'applicationsPage',
          label: 'Narrow to a receiver',
          placeholder: 'an application name, an identifier or an aud',
          what: 'It matches the application name, the identifier it ' +
            'authenticated as, and the <code>aud</code> on its streams ' +
            '&mdash; the three strings a reader arrives holding, which are ' +
            'routinely different. It does NOT match the event types: a ' +
            'receiver that takes none of the eight is exactly the row ' +
            'somebody is looking for when they ask why nothing arrived.'
        }) +
        appNav.head +
        '<table><tr><th>Receiver</th><th>Streams</th><th>aud</th><th>Takes' +
        '</th>' +
        headers + '<th>Total</th><th>Sessions</th><th>Delivered / failed</th>' +
        '</tr>' +
        (appState.page.shown.length
          ? appState.page.shown.map(function (row) {
              return self.caepApplicationRow(row, shorts, prefix);
            }).join('')
          : '<tr><td colspan="' + (shorts.length + 7) + '">' +
            (appState.wanted
              ? 'No receiver matches <code>' + self.esc(appState.wanted) +
                '</code>. ' +
                ((json.applications || []).length
                  ? (json.applications || []).length + ' receiver(s) are ' +
                    'known under other names.'
                  : 'None is known at all yet.')
              : 'No application here supports Shared Signals yet. An entry ' +
                'appears the moment a receiver creates a stream at ' +
                '<code>/ssf/stream</code>, and one can be declared ahead of ' +
                'that by ticking Shared Signals on ' +
                '<a href="/admin/applications/new">a new application</a>.') +
            '</td></tr>') +
        '</table>' +
        appNav.foot +
        self.note((appState.matched.length) + ' receiver(s) match' +
        (appState.page.paging.pages > 1
          ? ', of which rows ' + appState.page.paging.firstRow + '&ndash;' +
            appState.page.paging.lastRow + ' are on this page (' +
            appState.page.paging.page + ' of ' + appState.page.paging.pages +
            ')'
          : '') +
        '. Busiest first. <em>Delivered / failed</em> is the PIPE &mdash; ' +
        'how many Security Event Tokens left this service and how many could ' +
        'not be handed over &mdash; and it is a different number from the ' +
        'total beside it, which is what was SAID: on a poll stream nothing ' +
        'is delivered until the receiver asks, and the two agreeing is a ' +
        'property of push delivery rather than of a healthy transmitter. ' +
        '<em>Sessions</em> counts the DISTINCT sessions this receiver has ' +
        'been told about, across all of its streams together.') +


        (json.installed
          ? '<form method="post" action="/admin/caep">' +
            '<div class="formrow">' +
            '<input type="hidden" name="action" value="clear">' +
            '<input type="hidden" name="from" value="sessions">' +
            '<input type="hidden" name="back" value="' + self.esc(back) + '">' +
            '<button class="secondary">Clear the register</button>' +
            '</div></form>' +
            self.note('Clearing forgets the RECORD. Nobody is signed out and ' +
            'no stream is touched &mdash; this page is what has been SAID ' +
            'about these sessions, and a control here that ended one would ' +
            'be a monitoring page with a weapon on it.')
          : '') +

        self.note('<a href="/admin/caep">The settings, the catalogue and the ' +
        'by-hand emit form</a> &middot; ' +
        '<a href="/admin/ssf">the streams</a> &middot; ' +
        '<a href="/admin/metrics">the sessions this service still holds</a> ' +
        '&middot; <a href="/admin/caep-sessions?format=json">this page as ' +
        'JSON</a>');

      // WHAT THE BROWSER WAS SHOWN, BESIDE THE WHOLE LIST RATHER THAN INSTEAD
      // OF IT — the arrangement /admin/delegation's configured half already
      // has. `sessions` stays ENTIRE, because GET /admin-api/caep answers with
      // this same report and a caller that had to walk fifty-row pages of it
      // would be paying for this page's layout, which is not a fact about the
      // register. But a `?sessq=` the markup honours and the reply ignores is
      // exactly the silent disagreement this console keeps warning about, so
      // what the page did is REPORTED: `paging.sessions.total` against
      // `sessions.length` shows the filter that was applied and the slice that
      // was drawn, neither of them guessed from the markup.
      self.respond(req, res, Object.assign({}, json, {
                filter: { sessions: sessWanted || null,
                          applications: appState.wanted || null },
                paging: { sessions: pagingJson(sessPage.paging),
                          applications: pagingJson(appState.page.paging) }
              }), 'CAEP sessions', '/admin/caep-sessions', inner);
      // NOTE: the same document caepSessionsJson() builds for the API. It is
      // assembled here rather than called because the page has already done the
      // filtering and the slicing to draw the table, and a second walk would be
      // the disagreement that function's header warns about.
      log.debug("Leaving the admin CAEP sessions page.");
    });

    // -------------------------------------------------------------------------
    // GET /admin/caep-sessions/session?id=… — WHAT HAS BEEN SAID ABOUT ONE
    // SESSION.
    //
    // A DRILL-DOWN OF /admin/caep-sessions, and the first that page has had:
    // every identifier in its first column links here. It hangs under it
    // exactly as /admin/tokens/credential hangs under the tokens table — no
    // `NAV` row, `active` is '/admin/caep-sessions', `up` carries the search
    // and the page the reader left — so the trail reads `CAEP sessions ›
    // somebody` and the way back is the row they clicked rather than the top of
    // an unfiltered list.
    //
    // **IT EXISTS BECAUSE THE CONTENT USED TO BE ON THE LIST PAGE, ALL OF IT AT
    // ONCE.** That page drew a card per tracked session under its table, each
    // with an event table inside it, so the answer to "what went out about
    // alice" was several hundred nested tables below the table that names her —
    // and the sessions table, which is the thing anybody arrives for, was off
    // the top of the screen for the whole of it. `caep.maxSessionsTracked` is
    // 200 by default and settable higher, so this was not a corner case; it was
    // every service that had been running for an afternoon.
    //
    // **A SESSION THIS REGISTER NO LONGER HOLDS IS NOT A 404**, which is the
    // rule /admin/logout and the delegation drill-downs already follow. The
    // register is capped and drops the oldest, and `Clear the register` empties
    // it outright, so a link that comes back empty is the ordinary outcome of a
    // busy service rather than a mistake — and this page says which of the two
    // states it is in instead of answering with a status code that says
    // neither.
    //
    // One form on it, and it is the SAME reset the table has: rule 7 is
    // satisfied by `POST /admin-api/caep/reset-session`, which already existed,
    // because moving a form is not moving an action.
    // -------------------------------------------------------------------------
    app.get('/admin/caep-sessions/session', function (req, res) {
      log.debug("Entering the admin CAEP one-session page.");
      const json = caepJson(req);
      const asked = String(req.query.id || '').trim();
      const row = (json.sessions || []).filter(function (one) {
        return String(one.sessionId) === asked;
      })[0] || null;

      const listView = self.listViewOf('/admin/caep-sessions', req.query);
      const up = self.upTo('/admin/caep-sessions',
                           row ? (row.username || row.sub || asked) :
                           'One session',
                           listView);
      const backLink = self.note('<a class="btn" href="' + self.esc(up.href) +
        '">&larr; Back to the sessions table</a>');
      // The list view as a POSTable field, for the reset form below: the reader
      // came from page three of a search and should go back to it.
      const back = queryWith(listView, {});

      if (!row) {
        const inner = self.messagesOf(req) + backLink +
          self.note(asked
            ? '<strong>No tracked session is called <code>' + self.esc(asked) +
              '</code>.</strong> That is not necessarily a wrong link. This ' +
              'register is capped at <code>caep.maxSessionsTracked</code> ' +
              'sessions and drops the oldest, and the <em>Clear the ' +
              'register</em> button on the sessions table empties it ' +
              'outright &mdash; so an old link coming back empty is the ' +
              'ordinary outcome on a service that has been running a while. ' +
              'What it does NOT mean is that the session was never real: ' +
              'nothing here is ever deleted because a session ended, and a ' +
              'session that ended is exactly what this register is for keeping.'
            : '<strong>Name a session.</strong> This page draws ONE of them ' +
              'and the way to it is a link on <a href="' + self.esc(up.href) +
              '">the sessions table</a> &mdash; every identifier there is ' +
              'one. It is keyed on the session identifier this service gave ' +
              'the session, which is what the SSF subject sent to a receiver ' +
              'names.');
        self.respond(req, res, { installed: json.installed, id: asked || null,
                                 session: null },
                     'CAEP sessions — one session', '/admin/caep-sessions',
                     inner, up);
        log.debug("Leaving the admin CAEP one-session page. Nothing was " +
                  "found.");
        return;
      }

      // The events, paged. `eventsPage` rather than `page` because this list is
      // named for pagingOf()'s reason and because the parameter travels back to
      // the list page in the trail, where a bare `page` would be the sessions
      // table's.
      const eventPage = pagedRows(req.query, row.events || [],
        { name: 'events', noun: 'events' });
      const navParams = pageParamsOf(req.query);
      const eventNav = self.pageNavPair('/admin/caep-sessions/session',
                                        navParams,
                                        eventPage.paging);

      const eventRows = eventPage.shown.length
        ? eventPage.shown.map(self.caepEventRow.bind(self)).join('')
        : '<tr><td colspan="5">Nothing has been said about this session. ' +
          'That is the ordinary case when no stream asked for the type, and ' +
          'it is the answer to &ldquo;why did nothing arrive&rdquo; nine ' +
          'times out of ten &mdash; <a href="/admin/caep-sessions">the ' +
          'streams table</a> is where that shows up.</td></tr>';

      const inner = self.messagesOf(req) + backLink +

        self.note('<strong>Everything this transmitter has said about one ' +
        'session.</strong> The sessions table counts HOW MANY events of each ' +
        'type went out; this says WHICH, in order, with what the register ' +
        'noticed as each one was applied. They are separate on purpose ' +
        '&mdash; the state machine and the counters answer different ' +
        'questions, and <code>ssf/caep.ts</code> argues why.') +

        '<h2>The session</h2>' +
        self.caepSessionFacts(row) +

        '<h2>What has been said about it</h2>' +
        self.note('A <strong>warning</strong> in the last column is this ' +
        'register\'s own reading of an event it had just applied &mdash; a ' +
        'state change that was not a transition, a payload member the type ' +
        'does not define &mdash; and not something a receiver was told. SSF ' +
        'has no channel for that: an event either goes out or does not.') +
        self.perPageForm('/admin/caep-sessions/session', 'id', asked,
                         eventPage.paging.perPage,
                         'It applies to the event list below.', listView) +
        eventNav.head +
        '<table><tr><th>When</th><th>Event</th><th>jti</th><th>Stream</th>' +
        '<th>What the register noticed</th></tr>' + eventRows + '</table>' +
        eventNav.foot +

        '<h2>Per event type</h2>' +
        self.note('The same eight columns the sessions table carries, ' +
        'written out with room for each type\'s whole name. A count of zero ' +
        'almost always means <strong>no stream asked for that type</strong> ' +
        'rather than anything being wrong.') +
        self.caepSessionCounts(row, json.eventTypes || []) +

        '<h2>Reset this session\'s CAEP state</h2>' +
        '<form method="post" action="/admin/caep">' +
        '<input type="hidden" name="action" value="reset-session">' +
        '<input type="hidden" name="session_id" value="' +
        self.esc(row.sessionId) +
        '">' +
        // WHICH PAGE PRESSED IT. Read as an enum by caepSessionsBackTo(), never
        // as a path — see its header.
        '<input type="hidden" name="from" value="session">' +
        '<input type="hidden" name="back" value="' + self.esc(back) + '">' +
        '<div class="formrow"><button class="secondary">Reset</button></div>' +
        '</form>' +
        self.note('It forgets what has been SAID about this session and puts ' +
        'its state back to where it started. <strong>Nobody is signed out ' +
        'and no stream is touched</strong> &mdash; a control on a monitoring ' +
        'page that ended a session would be a monitoring page with a weapon ' +
        'on it.') +

        self.note('<a href="/admin/caep-sessions">Every session</a> &middot; ' +
        '<a href="/admin/caep">the settings, the catalogue and the by-hand ' +
        'emit form</a> &middot; <a href="' +
        self.esc('/admin/caep' + queryWith({ session: row.sessionId },
                                                         {})) +
        '">emit an event about this session</a> &middot; ' +
        '<a href="/admin/ssf">the streams</a> &middot; ' +
        '<a href="' + self.esc('/admin/caep-sessions/session' +
          queryWith(pageParamsOf(req.query), { format: 'json' })) +
        '">this page as JSON</a>');

      self.respond(req, res,
                   { installed: json.installed, id: asked, session: row,
                               eventTypes: json.eventTypes || [],
                               // WHAT THE BROWSER WAS SHOWN, beside the whole
                               // list rather than instead of it:
                               // `session.events` is entire, and this is the
                               // slice the markup drew. The rule the list page
                               // follows one level up.
                               events: eventPage.shown,
                               paging:
                                 { events: pagingJson(eventPage.paging) } },
                   'CAEP sessions — ' + (row.username || row.sub || asked),
                   '/admin/caep-sessions', inner, up);
      log.debug("Leaving the admin CAEP one-session page. " +
                (row.events || []).length + " event(s).");
    });

    // -------------------------------------------------------------------------
    // PROTOCOLS -> RISC. The settings, the catalogue, and the form that emits
    // the ten event types nothing here can cause.
    //
    // It is the CONFIGURATION page of this pair and /admin/risc-accounts is the
    // MONITORING one, which is the same division /admin/caep and
    // /admin/caep-sessions have and for the same reason: a settings page and an
    // observation are two different things, and the sidebar says so by putting
    // them in two different sections.
    // -------------------------------------------------------------------------
    app.get('/admin/risc', function (req, res) {
      log.debug("Entering the admin RISC page.");
      const json = riscJson(req);

      const catalogue = (json.catalogue || []).map(function (row) {
        const members = row.members.map(function (member) {
          return '<tr><td><code>' + self.esc(member.name) + '</code></td>' +
            '<td class="' + (member.required ? '' : 'sub') + '">' +
            (member.required ? 'required' : 'optional') + '</td>' +
            '<td class="sub"><code>' + self.esc(member.type) + '</code>' +
            (member.values.length
              ? ' ' + self.esc(member.values.join(' | ')) : '') + '</td>' +
            '<td class="sub">' + self.esc(member.what) + '</td></tr>';
        }).join('');
        return '<div class="card"><h3>' + self.esc(row.name) + ' &mdash; ' +
          '<code>' +
          self.esc(row.short) + '</code>' +
          (row.offered ? '' :
           ' <span class="state-invalid">not offered</span>') +
          (row.deprecated
            ? ' <span class="state-invalid">deprecated</span>' : '') +
          '</h3><div class="sub">' + self.esc(row.what) + '</div>' +
          ((row.subjectFormats || []).length
            ? '<div class="sub"><strong>Its subject must be</strong> <code>' +
              self.esc(row.subjectFormats.join('</code> or <code>')) +
              '</code>, and it carries the OLD value.</div>'
            : '') +
          (members
            ? '<table><tr><th>Member</th><th></th><th>Type</th>' +
              '<th>What it is</th></tr>' + members + '</table>'
            : '<div class="sub">No payload members at all. The subject ' +
              'carries the entire message.</div>') +
          '</div>';
      }).join('');

      // WHICH ACCOUNT THE FORM WILL EMIT ABOUT. Off the query string, like the
      // CAEP page's session — see riscAccountChooser(), and note the one place
      // this page is deliberately more permissive than that one.
      const here = { path: '/admin/risc', query: req.query };
      const wantedAccount = queryOne(here.query, 'acctq2').trim();
      let picked = null;
      (json.accounts || []).forEach(function (row) {
        if (String(row.accountId || '') === wantedAccount) {
          picked = row;
        }
      });
      const accountChooser = self.riscAccountChooser(here, json.accounts || [],
          picked ? picked.accountId : '');

      const typeOptions = (json.catalogue || []).map(function (row) {
        return '<option value="' + self.esc(row.short) + '">' +
               self.esc(row.name) +
          (row.deprecated ? ' (deprecated)' : '') + '</option>';
      }).join('');

      const inner = self.messagesOf(req) +
        (!json.installed
          ? '<div class="err"><strong>Shared Signals is not loaded in this ' +
            'process</strong>, so RISC has nothing to run on. RISC is a ' +
            'VOCABULARY over SSF rather than a family of its own: its events ' +
            'travel on SSF streams, are signed by the SSF signer and are ' +
            'delivered by the two SSF deliveries.</div>'
          : '') +
        (json.installed && !json.enabled
          ? self.warn('<strong>RISC is turned off</strong> ' +
            '(<code>risc.enabled</code>). The fourteen event types are ' +
            'dropped from <code>events_supported</code>, so a stream asking ' +
            'for one gets it back missing from <code>events_delivered</code> ' +
            '&mdash; which is the only notice SSF gives. SSF and CAEP are ' +
            'unaffected.')
          : '') +

        self.note('<strong>RISC</strong> (OpenID RISC Profile Specification ' +
        '1.0, published 29 August 2025 and final on 2 September 2025) is the ' +
        '<strong>account</strong> vocabulary spoken over ' +
        '<a href="/admin/ssf">Shared Signals</a>, and the second of the two. ' +
        '<a href="/admin/caep">CAEP</a> says <em>this session is no longer ' +
        'trustworthy</em>; RISC says <em>this account is no longer ' +
        'trustworthy</em>. Those are two different sentences and the second ' +
        'is the larger by orders of magnitude &mdash; a revoked session is ' +
        'one sign-in at one relying party, and a purged account is every ' +
        'session that person has anywhere, for ever. CAEP is aimed WITHIN an ' +
        'enterprise and RISC ACROSS providers: its origin is a consumer ' +
        'provider noticing an account has been taken over and telling every ' +
        'site that account signs in to.') +

        self.warn('<strong>Eleven of the fourteen carry no payload members ' +
        'at all, so the subject IS the message.</strong> ' +
        '<code>account-purged</code> says nothing but its own type and who ' +
        'it is about &mdash; which means a subject naming the wrong person ' +
        'is not a partly wrong event, it is a completely wrong one with ' +
        'nothing else in it to notice by. <code>risc.subjectFormat</code> ' +
        'below is therefore the most consequential setting on this page.') +

        self.warn('<strong>This service emits RISC events when its own ' +
        'DIRECTORY changes</strong>, which is a different observer from ' +
        'CAEP\'s. With <code>risc.autoEmit</code> on, a person deleted emits ' +
        '<code>account-purged</code>, <code>active</code> going false or ' +
        'true emits <code>account-disabled</code> or ' +
        '<code>account-enabled</code>, and a changed mail address or ' +
        'telephone number emits <code>identifier-changed</code>. Those acts ' +
        'reach this directory over <a href="/admin/scim">SCIM</a>, over <a ' +
        'href="/admin/ldap">LDAP</a> and from the console alike, because the ' +
        'observer sits on the WRITE rather than on any one door.') +

        self.note('<strong>Setting <code>active</code> to false DISABLES ' +
        'the account</strong> since 2026-09-17 — it read "deactivates ' +
        'nobody" until then. It writes the password-policy lock ' +
        '<code>pwdAccountLockedTime</code>, every door refuses that person ' +
        'while it is set, and everything they hold is ended at once. The ' +
        'RISC event is unchanged and is what it always was: a transmitter ' +
        'reports and a receiver decides — what changed is that this service ' +
        'now acts on it too.') +

        '<div class="tiles">' +
        self.tile(json.tracked || 0, 'accounts tracked') +
        self.tile((json.eventTypes || []).length, 'event types') +
        self.tile(Object.keys(json.totals || {}).reduce(function (n, uri) {
          return n + json.totals[uri];
        }, 0), 'events sent') +
        self.tile((json.autoEmitActs || []).length, 'acts emit on their own') +
        '</div>' +

        (json.installed && json.googleSubjectType
          ? self.warn('<code>risc.googleSubjectType</code> is on, so every ' +
            'RISC subject leaving here spells its discriminator ' +
            '<code>subject_type</code> rather than <code>format</code>. That ' +
            'is RISC section 3.1\'s own compatibility note about a ' +
            'production transmitter in the field: the specification says new ' +
            'services MUST NOT use the name and then tells relying parties ' +
            'they need code to work around it anyway. This is how a receiver ' +
            'finds out whether it has that code. CAEP and SSF events are ' +
            'untouched.')
          : '') +

        (json.installed
          ? '<h2>Emit one by hand</h2>' +
            self.note('Ten of the fourteen describe things nothing here does ' +
            '&mdash; no breach corpus is searched by this service and no ' +
            'recovery flow runs in it &mdash; so this form is the only way ' +
            'they are ever produced. <strong>Four of those ten CHANGE REAL ' +
            'STATE when they go</strong>: RISC section 2.8 defines each ' +
            'opt-out event as <em>the account is in this state</em> rather ' +
            'than as a report that it moved, so emitting one here IS the ' +
            'transition. The subject is composed from the account you pick, ' +
            'in the format <code>risc.subjectFormat</code> names &mdash; ' +
            'except for the two identifier events, which use an email ' +
            'address regardless, because for those two the identifier is the ' +
            'message.') +
            accountChooser +
            (picked
              ? '<p class="note">Emitting about <strong>' +
                self.esc(String(picked.username || picked.accountId)) +
                '</strong> &mdash; <code>' + self.esc(picked.subject || '') +
                '</code>. Its lifecycle is <code>' +
                self.esc(picked.lifecycle) +
                '</code> and its opt-out state is <code>' +
                self.esc(picked.optOut) +
                '</code>.</p>'
              : self.note('Pick an account above, or type one below. ' +
                '<strong>An account this service has never held is ' +
                'accepted</strong>, which is the opposite of what the CAEP ' +
                'form does with an unknown session and is the ' +
                'specification\'s difference rather than an inconsistency: a ' +
                'session identifier this service never minted is one it can ' +
                'compose no subject from, and an account is a person. RISC ' +
                'is aimed ACROSS providers, so the account a receiver is ' +
                'warned about is usually one it has never seen.')) +
            '<form method="post" action="/admin/risc"><div class="formrow">' +
            '<input type="hidden" name="action" value="emit">' +
            '<label>Account <input type="text" name="account_id" size="24" ' +
            'value="' + self.esc(picked ? picked.accountId : '') +
            '"></label> <label>Event <select name="type">' + typeOptions +
            '</select></label>' +
            '</div><div class="formrow">' +
            '<label>Payload (JSON, optional) ' +
            '<input type="text" name="payload" size="60" ' +
            'placeholder="{&quot;reason&quot;:&quot;hijacking&quot;}">' +
            '</label>' +
            '</div><div class="formrow">' +
            '<label>reason_admin <input type="text" name="reason_admin" ' +
            'size="40"></label> ' +
            '<label>reason_user <input type="text" name="reason_user" ' +
            'size="40"></label>' +
            '</div>' +
            self.note('The two reason fields reach the wire for ONE of the ' +
            'fourteen types. RISC gives <code>reason_admin</code>, ' +
            '<code>reason_user</code> and <code>event_timestamp</code> to ' +
            '<code>credential-compromise</code> and to nothing else &mdash; ' +
            'where CAEP gives four claims to all eight of its own &mdash; so ' +
            'on any other type they are composed and dropped by the ' +
            'catalogue rather than sent as members the specification does ' +
            'not define.') +
            '<div class="formrow"><button>Emit</button></div></form>'
          : '') +

        // Folded and closed by default, for the reason the CAEP catalogue is:
        // fourteen cards is seven screens of REFERENCE between the controls
        // above and the settings below.
        (json.installed
          ? '<details class="fold section"><summary>The fourteen event types' +
            '</summary><div class="foldbody">' +
            self.note('<strong>Only one of the fourteen has a required ' +
            'member</strong> &mdash; <code>credential-compromise</code>\'s ' +
            '<code>credential_type</code>, which RISC defines BY REFERENCE ' +
            'to CAEP\'s <code>credential-change</code>, so the two lists are ' +
            'the same list rather than two alike ones. Eleven have no ' +
            'members at all. And one member name in the whole of Shared ' +
            'Signals uses a HYPHEN: <code>identifier-changed</code>\'s ' +
            '<code>new-value</code>, where everything else in all three ' +
            'vocabularies is snake_case, so <code>new_value</code> typed ' +
            'from habit produces an event that delivers and says nothing.') +
            catalogue +
            '</div></details>'
          : '') +

        self.configFormsFor('/admin/risc') +

        self.note('<a href="/admin/risc-accounts">The accounts and what has ' +
        'been said about them</a> &middot; ' +
        '<a href="/admin/caep">the other vocabulary</a> &middot; ' +
        '<a href="/admin/ssf">the streams it all goes out on</a> &middot; ' +
        '<a href="/admin/risc?format=json">this page as JSON</a> &middot; ' +
        '<a href="/admin-api/risc">the same over the management API</a>');

      self.respond(req, res, json, 'RISC', '/admin/risc', inner);
      log.debug("Leaving the admin RISC page.");
    });

    app.post('/admin/risc', function (req, res) {
      log.debug("Entering the admin RISC action endpoint.");
      const body = parseBody(req);
      // THE ONE RISC ACTION HANDLER, AND THREE PAGES POST TO IT: this one, the
      // accounts table and the one-account drill-down. `from` says which, read
      // as an ENUM and never as a path.
      const back = self.riscAccountsBackTo(body);
      riscAction(body).then(function (result) {
        self.respondToAction(req, res, back, result);
        log.debug("Leaving the admin RISC action endpoint.");
      }).catch(function (e) {
        log.error(errorCodes.tag('STS-ADMIN-0013') + 'admin: the RISC action ' +
                                                     'threw: ' + e.message);
        self.respondToAction(req, res, back,
          { ok: false, errorCode: 'STS-ADMIN-0013',
            errors: ['The action failed: ' + e.message] });
        log.debug("Leaving the admin RISC action endpoint. Threw.");
      });
    });

    app.get('/admin/risc-accounts', function (req, res) {
      log.debug("Entering the admin RISC accounts page.");
      const json = riscJson(req);
      const shorts = self.riscShortNames(json);
      const prefix = 'https://schemas.openid.net/secevent/risc/event-type/';

      const headers = shorts.map(function (short) {
        return '<th title="' + self.esc(prefix + short) + '">' +
          self.esc(self.riscColumnLabel(short)) + '</th>';
      }).join('');

      const state = riscAccountsState(req, json);
      const appState = riscApplicationsState(req, json);
      const navParams = pageParamsOf(req.query);
      const acctNav = self.pageNavPair('/admin/risc-accounts', navParams,
                                       state.page.paging);
      const appNav = self.pageNavPair('/admin/risc-accounts', navParams,
                                      appState.page.paging);
      const listView = self.listViewOf('/admin/risc-accounts', req.query);
      const back = queryWith(listView, {});

      const rows = state.page.shown.length
        ? state.page.shown.map(function (row) {
            return self.riscAccountRow(row, shorts, prefix, listView, back);
          }).join('')
        : '<tr><td colspan="' + (shorts.length + 8) + '">' +
          (state.wanted
            ? 'No tracked account matches <code>' + self.esc(state.wanted) +
              '</code>. ' +
              ((json.accounts || []).length
                ? (json.accounts || []).length + ' account(s) are tracked ' +
                  'under other names.'
                : 'None is tracked at all yet.')
            : 'No accounts are tracked. Change one in the directory &mdash; ' +
              'delete a person, set <code>active</code> to false over ' +
              '<a href="/admin/scim">SCIM</a>, change a mail address with an ' +
              '<code>ldapmodify</code> &mdash; and a row appears here ' +
              'whether or not any stream is agreed, which is what makes ' +
              '&ldquo;nothing arrived&rdquo; traceable to &ldquo;nobody ' +
              'asked&rdquo;.') +
          '</td></tr>';

      const streamRows = (json.streams || []).length
        ? json.streams.map(function (row) {
            return '<tr><td><code>' + self.esc(row.stream_id) + '</code></td>' +
              '<td class="sub">' + self.esc(row.aud) + '</td><td class="' +
              (row.status === 'enabled' ? 'state-valid' : 'sub') +
              '">' + self.esc(row.status) + '</td>' +
              '<td class="sub">' + self.esc(row.delivery) + '</td>' +
              '<td class="sub">' + self.esc(String(row.subjects)) + '</td>' +
              '<td class="' + (row.takes.length ? '' : 'state-invalid') + '">' +
              self.esc(row.takes.length ? row.takes.join(', ')
                : 'none of RISC\'s fourteen') + '</td></tr>';
          }).join('')
        : '<tr><td colspan="6">No streams. Nothing this service says about ' +
          'an account has anywhere to go, and every row above will show a ' +
          'count of zero however much the directory changes.</td></tr>';

      const inner = self.messagesOf(req) +
        (!json.installed
          ? '<div class="err"><strong>Shared Signals is not loaded in this ' +
            'process</strong>, so no account state is tracked.</div>'
          : '') +
        (json.installed && !json.enabled
          ? self.warn('<strong>RISC is turned off</strong> ' +
            '(<code>risc.enabled</code>), so nothing new is being recorded ' +
            'here. What is below is what was recorded before it was turned ' +
            'off.')
          : '') +
        (json.installed && json.enabled && !json.autoEmit
          ? self.warn('<strong>Automatic emission is off</strong> ' +
            '(<code>risc.autoEmit</code>). Accounts still appear here and ' +
            'their state still follows what really happens in the directory, ' +
            'and nothing goes out unless somebody emits it from ' +
            '<a href="/admin/risc">the RISC page</a>.')
          : '') +

        self.note('One row per account this service has been told anything ' +
        'about, <strong>including accounts that no longer exist</strong>. ' +
        'That is deliberate and it is the whole reason this page exists: a ' +
        'purged account is gone from the directory entirely, so a row whose ' +
        'lifecycle is <code>purged</code> is the only remaining evidence ' +
        'that this service ever told anybody it was. The counts are per ' +
        'event type and they never forget; WHICH events those were is <a ' +
        'href="/admin/risc-accounts/account">each account\'s own page</a>.') +

        self.note('<strong>The lifecycle and the opt-out state are two ' +
        'columns and not one.</strong> They move independently: an account ' +
        'can be opted out and perfectly healthy, or compromised and still ' +
        'exchanging. A CAEP session has one state because a session is alive ' +
        'or it is not; an account has three that a receiver acts on ' +
        'differently.') +

        (json.installed && json.honourOptOut
          ? self.note('<code>risc.honourOptOut</code> is on, so an account ' +
            'in the <code>opt-out</code> state has its events SUPPRESSED and ' +
            'the <em>Suppressed</em> column counts them &mdash; the one ' +
            'number on this page that says a receiver heard nothing on ' +
            'purpose. The four opt-out events themselves are never ' +
            'suppressed: <code>opt-out-effective</code> is an event ' +
            'announcing that there will be no more events, and ' +
            '<code>opt-in</code> is the only way a receiver ever learns the ' +
            'account came back.')
          : self.warn('<code>risc.honourOptOut</code> is OFF, so events go ' +
            'out about accounts that have opted out of RISC exchange. RISC ' +
            'section 2.8 says an opted-out account is not participating; ' +
            'this is how a receiver that ignores an opt-out gets to be shown ' +
            'doing it.')) +

        '<div class="tiles">' +
        self.tile(json.tracked || 0, 'accounts') +
        self.tile((json.accounts || []).filter(function (row) {
          return row.lifecycle === 'purged';
        }).length, 'purged') +
        self.tile((json.accounts || []).filter(function (row) {
          return row.lifecycle === 'disabled';
        }).length, 'disabled') +
        self.tile((json.accounts || []).filter(function (row) {
          return row.optOut !== 'opt-in';
        }).length, 'opted out') +
        self.tile(Object.keys(json.totals || {}).reduce(function (n, uri) {
          return n + json.totals[uri];
        }, 0), 'events sent') +
        self.tile((json.streams || []).filter(function (row) {
          return row.takes.length;
        }).length, 'streams taking RISC') +
        '</div>' +

        '<h2>Accounts</h2>' +
        self.note('<strong>Every identifier in the first column opens that ' +
        'account on its own page</strong> &mdash; what has actually been ' +
        'sent about it, in order, with what the register noticed as each one ' +
        'was applied.') +
        self.sectionSearchForm({
          path: '/admin/risc-accounts', query: req.query,
          param: 'acctq', pageParam: 'accountsPage',
          label: 'Narrow to an account',
          placeholder: 'a username, a subject, an email address or a DN',
          what: 'It matches the account identifier, the <code>sub</code>, ' +
            'the email address, the telephone number, the SSF subject as a ' +
            'receiver was sent it, the directory DN &mdash; and every ' +
            'identifier this account has been known by FORMERLY. That last ' +
            'one is not thoroughness: <code>identifier-changed</code> is an ' +
            'event about the key itself, so the address a reader arrives ' +
            'holding is routinely the one the account no longer has.'
        }) +
        self.perPageForm('/admin/risc-accounts', 'acctq', state.wanted,
                         state.page.paging.perPage,
                         'Only the accounts table is paged.', {}) +
        acctNav.head +
        self.wideTable('Accounts',
          '<table><tr><th>Account</th><th>Subject</th><th>Lifecycle</th>' +
          '<th>Opt-out</th><th>Credential</th>' + headers +
          '<th>Total</th><th>Suppressed</th><th></th></tr>' + rows +
          '</table>') +
        acctNav.foot +

        '<h2>Where a RISC event could go</h2>' +
        self.note('An account with a count of zero almost always means ' +
        '<strong>no stream asked for that type</strong> rather than anything ' +
        'being wrong. SSF has no refusal for an event type a transmitter ' +
        'will not deliver &mdash; its absence from ' +
        '<code>events_delivered</code> is the only notice a receiver gets ' +
        '&mdash; so this table is where that shows up.') +
        '<table><tr><th>Stream</th><th>aud</th><th>Status</th><th>Delivery' +
        '</th>' +
        '<th>Subjects</th><th>RISC types it takes</th></tr>' + streamRows +
        '</table>' +

        '<h2>Per application</h2>' +
        self.note('<strong>What this transmitter has said to each receiver, ' +
        'across every account.</strong> The table above this one is per ' +
        'STREAM and the one above that is per ACCOUNT; this is the third ' +
        'question, and it is the one somebody arrives with once more than ' +
        'one receiver exists.') +
        self.sectionSearchForm({
          path: '/admin/risc-accounts', query: req.query,
          param: 'rappq', pageParam: 'rapplicationsPage',
          label: 'Narrow to a receiver',
          placeholder: 'an application name, an identifier or an aud',
          what: 'It matches the application name, the identifier it ' +
            'authenticated as, and the <code>aud</code> on its streams. It ' +
            'does NOT match the event types: a receiver that takes none of ' +
            'the fourteen is exactly the row somebody is looking for when ' +
            'they ask why nothing arrived.'
        }) +
        appNav.head +
        self.wideTable('Per application',
          '<table><tr><th>Receiver</th><th>Streams</th><th>aud</th>' +
          '<th>Takes</th>' + headers +
          '<th>Total</th><th>Accounts</th><th>Delivered / failed</th></tr>' +
          (appState.page.shown.length
            ? appState.page.shown.map(function (row) {
                return self.riscApplicationRow(row, shorts, prefix);
              }).join('')
            : '<tr><td colspan="' + (shorts.length + 7) + '">' +
              (appState.wanted
                ? 'No receiver matches <code>' + self.esc(appState.wanted) +
                  '</code>.'
                : 'No application here supports Shared Signals yet. An entry ' +
                  'appears the moment a receiver creates a stream at ' +
                  '<code>/ssf/stream</code>.') +
              '</td></tr>') +
          '</table>') +
        appNav.foot +

        (json.installed
          ? '<form method="post" action="/admin/risc">' +
            '<div class="formrow">' +
            '<input type="hidden" name="action" value="clear">' +
            '<input type="hidden" name="from" value="accounts">' +
            '<input type="hidden" name="back" value="' + self.esc(back) + '">' +
            '<button class="secondary">Clear the register</button>' +
            '</div></form>' +
            self.note('Clearing forgets the RECORD. <strong>Nobody is ' +
            'disabled, nobody is deleted and no directory entry is ' +
            'touched</strong> &mdash; this page is what has been SAID about ' +
            'these accounts, and a control here that purged one would be a ' +
            'monitoring page with a weapon on it.')
          : '') +

        self.note('<a href="/admin/risc">The settings, the catalogue and the ' +
        'by-hand emit form</a> &middot; ' +
        '<a href="/admin/caep-sessions">what has been said about ' +
        'SESSIONS</a> &middot; ' +
        '<a href="/admin/ssf">the streams</a> &middot; ' +
        '<a href="/admin/users">the people themselves</a> &middot; ' +
        '<a href="/admin/risc-accounts?format=json">this page as JSON</a>');

      self.respond(req, res, Object.assign({}, json, {
                filter: { accounts: state.wanted || null,
                          applications: appState.wanted || null },
                paging: { accounts: pagingJson(state.page.paging),
                          applications: pagingJson(appState.page.paging) }
              }), 'RISC accounts', '/admin/risc-accounts', inner);
      log.debug("Leaving the admin RISC accounts page.");
    });

    // -------------------------------------------------------------------------
    // GET /admin/risc-accounts/account?id=… — WHAT HAS BEEN SAID ABOUT ONE
    // ACCOUNT. A drill-down of /admin/risc-accounts, hung under it exactly as
    // /admin/caep-sessions/session hangs under its list and for that page's
    // reasons.
    //
    // **AN ACCOUNT THIS REGISTER NO LONGER HOLDS IS NOT A 404.** The register
    // is capped and drops the oldest, and `Clear the register` empties it
    // outright. It says which of the two states it is in instead of answering
    // with a status code that says neither.
    // -------------------------------------------------------------------------
    app.get('/admin/risc-accounts/account', function (req, res) {
      log.debug("Entering the admin RISC one-account page.");
      const json = riscJson(req);
      const asked = String(req.query.id || '').trim();
      const row = (json.accounts || []).filter(function (one) {
        return String(one.accountId) === asked;
      })[0] || null;

      const listView = self.listViewOf('/admin/risc-accounts', req.query);
      const up = self.upTo('/admin/risc-accounts',
                           row ? (row.username || row.accountId) :
                           'One account',
                           listView);
      const backLink = self.note('<a class="btn" href="' + self.esc(up.href) +
        '">&larr; Back to the accounts table</a>');
      const back = queryWith(listView, {});

      if (!row) {
        const inner = self.messagesOf(req) + backLink +
          self.note(asked
            ? '<strong>No tracked account is called <code>' + self.esc(asked) +
              '</code>.</strong> That is not necessarily a wrong link. This ' +
              'register is capped at <code>risc.maxAccountsTracked</code> ' +
              'accounts and drops the oldest, and the <em>Clear the ' +
              'register</em> button on the accounts table empties it ' +
              'outright. What it does NOT mean is that the account was never ' +
              'real: nothing here is deleted because a person was, and a ' +
              'person who was deleted is exactly what this register is for ' +
              'keeping.'
            : '<strong>Name an account.</strong> This page draws ONE of them ' +
              'and the way to it is a link on <a href="' + self.esc(up.href) +
              '">the accounts table</a>.');
        self.respond(req, res, { installed: json.installed, id: asked || null,
                                 account: null },
                     'RISC accounts — one account', '/admin/risc-accounts',
                     inner, up);
        log.debug("Leaving the admin RISC one-account page. Nothing found.");
        return;
      }

      const eventPage = pagedRows(req.query, row.events || [],
        { name: 'events', noun: 'events' });
      const eventNav = self.pageNavPair('/admin/risc-accounts/account',
                                        pageParamsOf(req.query),
                                        eventPage.paging);

      const eventRows = eventPage.shown.length
        ? eventPage.shown.map(self.riscEventRow.bind(self)).join('')
        : '<tr><td colspan="5">Nothing has been said about this account. ' +
          'That is the ordinary case when no stream asked for the type, and ' +
          'it is the answer to &ldquo;why did nothing arrive&rdquo; nine ' +
          'times out of ten &mdash; <a href="/admin/risc-accounts">the ' +
          'streams table</a> is where that shows up.</td></tr>';

      const inner = self.messagesOf(req) + backLink +

        self.note('<strong>Everything this transmitter has said about one ' +
        'account.</strong> The accounts table counts HOW MANY events of each ' +
        'type went out; this says WHICH, in order, with what the register ' +
        'noticed as each one was applied. They are separate on purpose ' +
        '&mdash; the state machine and the counters answer different ' +
        'questions, and <code>ssf/risc.ts</code> argues why.') +

        '<h2>The account</h2>' +
        self.riscAccountFacts(row) +

        '<h2>What has been said about it</h2>' +
        self.note('A <strong>warning</strong> in the last column is this ' +
        'register\'s own reading of an event it had just applied &mdash; a ' +
        'state change that RISC section 2.8\'s diagram has no arrow for, a ' +
        'payload member the type does not define &mdash; and not something a ' +
        'receiver was told. SSF has no channel for that: an event either ' +
        'goes out or does not.') +
        self.perPageForm('/admin/risc-accounts/account', 'id', asked,
                         eventPage.paging.perPage,
                         'It applies to the event list below.', listView) +
        eventNav.head +
        '<table><tr><th>When</th><th>Event</th><th>jti</th><th>Stream</th>' +
        '<th>What the register noticed</th></tr>' + eventRows + '</table>' +
        eventNav.foot +

        '<h2>Per event type</h2>' +
        self.note('The same fourteen columns the accounts table carries, ' +
        'written out with room for each type\'s whole name. A count of zero ' +
        'almost always means <strong>no stream asked for that type</strong>.') +
        self.riscAccountCounts(row, json.eventTypes || []) +

        '<h2>Reset this account\'s RISC state</h2>' +
        '<form method="post" action="/admin/risc">' +
        '<input type="hidden" name="action" value="reset-account">' +
        '<input type="hidden" name="account_id" value="' +
        self.esc(row.accountId) +
        '">' +
        '<input type="hidden" name="from" value="account">' +
        '<input type="hidden" name="back" value="' + self.esc(back) + '">' +
        '<div class="formrow"><button class="secondary">Reset</button></div>' +
        '</form>' +
        self.note('It forgets what has been SAID about this account and puts ' +
        'its state back to where it started. <strong>The directory entry is ' +
        'untouched</strong> &mdash; nobody is enabled, disabled or restored, ' +
        'and a control on a monitoring page that did any of those would be a ' +
        'monitoring page with a weapon on it.') +

        self.note('<a href="/admin/risc-accounts">Every account</a> &middot; ' +
        '<a href="' +
        self.esc('/admin/risc' + queryWith({ acctq2: row.accountId },
                                                         {})) +
        '">emit an event about this account</a> &middot; ' +
        '<a href="/admin/risc">the settings and the catalogue</a> &middot; ' +
        '<a href="/admin/ssf">the streams</a> &middot; ' +
        '<a href="' + self.esc('/admin/risc-accounts/account' +
          queryWith(pageParamsOf(req.query), { format: 'json' })) +
        '">this page as JSON</a>');

      self.respond(req, res,
                   { installed: json.installed, id: asked, account: row,
                               eventTypes: json.eventTypes || [],
                               events: eventPage.shown,
                               paging:
                                 { events: pagingJson(eventPage.paging) } },
                   'RISC accounts — ' + (row.username || row.accountId),
                   '/admin/risc-accounts', inner, up);
      log.debug("Leaving the admin RISC one-account page. " +
                (row.events || []).length + " event(s).");
    });

    app.get('/admin/config', function (req, res) {
      log.debug("Entering the admin configuration page.");
      const snapshot = config.snapshot();
      const overridden = snapshot.overridden.length;
      // What this page still edits: the groups with no protocol to belong to
      // (`Global`, `Key material`, `Web security`). Asked for by path rather
      // than by name, like every other page, so that moving one somewhere else
      // one day is a row in SETTING_HOMES and not an edit here.
      const mine = self.settingsGroupsFor('/admin/config');
      const mineCount = mine.reduce(function (n, group) {
        return n + group.settings.length;
      }, 0);

      const inner = self.messagesOf(req) +

        (SETTING_HOME_PROBLEMS.length
          ? '<div class="err"><strong>Some settings are not on any ' +
            'page.</strong><ul>' +
            SETTING_HOME_PROBLEMS.map(function (problem) {
              return '<li>' + self.esc(problem) + '</li>';
            }).join('') + '</ul>This is reported rather than hidden, in the ' +
            'same spirit as <a href="/admin/sts-metadata">Service ' +
            'metadata</a> naming a route nobody described: a setting that ' +
            'exists and appears nowhere is worse than one that is missing, ' +
            'because the service still reads it.</div>'
          : '') +

        self.note('<strong>Every setting this service has lives on the page ' +
        'for the protocol it configures.</strong> This page is the index of ' +
        'that — and the form for the ' + self.esc(String(mineCount)) + ' ' +
        'rows that belong to no protocol, which are facts about the process ' +
        'rather than about anything it speaks. It was ' +
        'all ' + self.esc(String(snapshot.settingCount)) + ' of them ' +
        'until 2026-08-27; what moved is where they are DRAWN, and nothing ' +
        'about what they do or how they are read.') +

        self.note('A value can arrive from four places and the ' +
        '<em>Source</em> column on every one of these pages says which: a ' +
        'runtime override set on the page, an environment variable, the ' +
        'appconfig file this process was started with ' +
        '(<code>' + self.esc(snapshot.configFile || '(none)') + '</code>), ' +
        'or ' +
        '<code>' + self.esc(snapshot.defaultsFile) + '</code> — the default ' +
        'appconfig file that one is unioned on top of. Higher beats lower, ' +
        'so an environment variable set on the container still wins over the ' +
        'file — which is what keeps every existing deployment working ' +
        'unchanged.') +

        self.note('<strong>There is no fifth place.</strong> A setting with ' +
        'no value in either appconfig file and no environment variable stops ' +
        'this service from starting, by name, rather than falling back to a ' +
        'constant buried in a module. So every value on these pages is one ' +
        'somebody can find in a file — which is what makes the ' +
        '<em>Source</em> column worth reading.') +

        self.warn('<strong>Changes are in memory and are gone on ' +
        'restart.</strong> Nothing writes to the appconfig file. That is the ' +
        'same arrangement as the custom claims and the credential claims ' +
        'next door, and it is deliberate: a service that edited a file ' +
        'checked into a repository would leave a test\'s forgotten change ' +
        'behind permanently. To make something stick, put it in ' +
        '<code>' + self.esc(snapshot.configFile || 'env/local.js') +
        '</code>.') +

        '<h2>' + self.esc(String(snapshot.settingCount)) + ' settings, ' +
        self.esc(String(snapshot.editableCount)) + ' of them changeable ' +
        'while this service runs</h2>' +

        (overridden
          ? '<div class="ok">' + self.esc(String(overridden)) + ' runtime ' +
            'override(s) in force, anywhere in the ' +
            'service: ' + self.codeList(snapshot.overridden) + '. ' +
            '<form method="post" action="/admin/config" ' +
            'class="inline"><input type="hidden" name="action" ' +
            'value="reset-all"><button class="secondary">Reset ' +
            'all</button></form></div>'
          : self.note('No runtime overrides are in force anywhere in this ' +
            'service: every value is coming from the environment or from one ' +
            'of the two appconfig files.')) +

        self.note('<strong>Reset all is here and on no protocol ' +
        'page</strong>, because it clears every override in the service and ' +
        'not only the ones below it. A button that reached that far from the ' +
        'Kerberos page would be the one control in this console whose blast ' +
        'radius was invisible from where it was pressed.') +

        '<h2>Where every setting is edited</h2>' +
        self.note('The whole table, group by group, in the order ' +
        '<code>config.js</code> declares them. The <em>Page</em> column is ' +
        'where that group\'s form is drawn; the counts are of the group, and ' +
        '<em>Overridden</em> is how many of them have a runtime override in ' +
        'force right now.') +
        '<table><thead><tr><th>Group</th><th class="num">Settings</th>' +
        '<th class="num">Restart-only</th><th class="num">Overridden</th>' +
        '<th>Page</th></tr></thead><tbody>' +
        snapshot.groups.map(self.configHomeRow.bind(self)).join('') +
        '</tbody></table>' +

        self.configFormsFor('/admin/config') +

        self.note('The whole table over JSON — every setting, whichever page ' +
        'edits it — is at <code>/admin/config?format=json</code> and ' +
        '<code>GET /admin-api/config</code>; the four actions on these pages ' +
        'are <code>POST /admin-api/config/set</code>, ' +
        '<code>/set-many</code>, <code>/reset</code> and ' +
        '<code>/reset-all</code>.');

      self.respond(req, res, self.configJson(), 'Configuration',
                   '/admin/config', inner);
      log.debug("Leaving the admin configuration page.");
    });

    // REGISTERED IN A LOOP, in the one registration pass, which is rule 1 held
    // rather than bent: `registerRoutes()` registers these routes, in this
    // order, below the gate — when `common/protocol_stack.ts` calls it since
    // #50's R1, as requiring this module did before. `row` is captured per
    // iteration because `forEach`'s callback
    // has its own scope — the same reason the equivalent `for (var i…)` would
    // not have worked and is not what this is.
    PROTOCOL_SETTINGS_PAGES.forEach(function (row) {
      app.get(row.path, function (req, res) {
        log.debug("Entering the admin " + row.title + " page.");
        self.respond(req, res, self.protocolSettingsJson(row), row.title,
                     row.path,
                     self.protocolSettingsPage(req, row));
        log.debug("Leaving the admin " + row.title + " page.");
      });
    });

    // The known actions, as a list, so that an unknown one can be answered by
    // NAMING the ones that exist. This repository's tests/vendored/admin_api.js
    // reads exactly that reply to assert console/API parity — it asks each
    // handler for an action that does not exist and compares what comes back
    // with the API's operations — so this list is not decoration.







    // -------------------------------------------------------------------------
    // THE ROUTES.
    // -------------------------------------------------------------------------
    app.get('/admin/spiffe', function (req, res) {
      log.debug("Entering the admin SPIFFE page.");
      const view = self.spiffePage(req);
      self.respond(req, res, view.json, view.title, '/admin/spiffe',
                   view.inner);
      log.debug("Leaving the admin SPIFFE page.");
    });

    // The one action handler here that is ASYNCHRONOUS, because rotating an
    // authority generates a key and key generation is async. `respondToAction`
    // takes the resolved result, so the await is here rather than inside it —
    // every other action in this console is synchronous and making that
    // function async would change the shape of all of them for one caller.
    app.post('/admin/spiffe', function (req, res) {
      log.debug("Entering the admin SPIFFE action endpoint.");
      const body = parseBody(req);
      spiffeAction(body).then(function (result) {
        self.respondToAction(req, res, '/admin/spiffe', result);
        log.debug("Leaving the admin SPIFFE action endpoint.");
      }).catch(function (err) {
        // A throw out of the action itself. Answered rather than left to
        // express's handler, which would replace the Content-Security-Policy
        // header — see the frame-ancestors note in CLAUDE.md.
        log.error(errorCodes.tag('STS-ADMIN-0013') + 'The SPIFFE console ' +
                                                     'action threw: ' +
                                                     err.message);
        self.respondToAction(req, res, '/admin/spiffe',
                             { ok: false, errorCode: 'STS-ADMIN-0013',
                               errors: [err.message] });
        log.debug("Leaving the admin SPIFFE action endpoint. It threw.");
      });
    });

    app.get('/admin/spiffe/entries', function (req, res) {
      log.debug("Entering the admin SPIFFE entries page.");
      const view = self.spiffeEntriesView(req);
      self.respond(req, res, view.json, view.title, '/admin/spiffe/entries',
                   view.inner,
                   view.up);
      log.debug("Leaving the admin SPIFFE entries page. " + view.title + ".");
    });

    app.post('/admin/spiffe/entries', function (req, res) {
      log.debug("Entering the admin SPIFFE entries action endpoint.");
      const body = parseBody(req);
      const result = spiffeEntriesAction(body);
      // Back to the drill-down the form was posted from where there was one,
      // and carrying the list state off the form's `back` field either way — so
      // an edit does not silently cost the reader their place in the list. Rule
      // 7a: a new form on this page needs `carryBack` in it, and nothing can
      // check that it has one.
      const listView = self.listViewFromBack('/admin/spiffe/entries',
                                             body.back);
      const target = String(body.entry || '').trim() && result.ok !== false
        ? '/admin/spiffe/entries' + queryWith(listView,
            { entry: String(body.entry).trim() })
        : '/admin/spiffe/entries' + queryWith(listView, {});
      self.respondToAction(req, res, target, result);
      log.debug("Leaving the admin SPIFFE entries action endpoint.");
    });

    app.get('/admin/spiffe/agents', function (req, res) {
      log.debug("Entering the admin SPIFFE agents page.");
      const view = self.spiffeAgentsView(req);
      self.respond(req, res, view.json, view.title, '/admin/spiffe/agents',
                   view.inner,
                   view.up);
      log.debug("Leaving the admin SPIFFE agents page. " + view.title + ".");
    });

    app.post('/admin/spiffe/agents', function (req, res) {
      log.debug("Entering the admin SPIFFE agents action endpoint.");
      const body = parseBody(req);
      const result = spiffeAgentsAction(body);
      const listView = self.listViewFromBack('/admin/spiffe/agents', body.back);
      const target = String(body.agent || '').trim() && result.ok !== false &&
                     String(body.action || '') !== 'delete'
        ? '/admin/spiffe/agents' + queryWith(listView,
            { agent: String(body.agent).trim() })
        : '/admin/spiffe/agents' + queryWith(listView, {});
      self.respondToAction(req, res, target, result);
      log.debug("Leaving the admin SPIFFE agents action endpoint.");
    });

    app.get('/admin/federation', function (req, res) {
      log.debug("Entering the admin federation page.");
      const view = self.federationView(req);
      if (view.missing) {
        errorCodes.mark(res, 'STS-ADMIN-0021');
      }
      self.respond(req, res, view.json, view.title, '/admin/federation',
                   view.inner,
                   view.up);
      log.debug("Leaving the admin federation page. " + view.title + ".");
    });

    app.post('/admin/federation', function (req, res) {
      log.debug("Entering the admin federation action endpoint.");
      const body = parseBody(req);
      const result = federationAction(body);
      const id = String(body.id || body.relationship || '').trim();
      // The list state the form carried, REBUILT rather than echoed — see
      // listViewFromBack(), and the note in admin-ui/CLAUDE.md about a new form
      // on a drill-down needing `carryBack` in it. Every form on the drill-down
      // above has it.
      const listView = self.listViewFromBack('/admin/federation', body.back);
      // A DELETE goes back to the LIST and everything else stays on the
      // drill-down, which is the one place this differs from the SAML 2.0
      // page's handler: landing on the detail page of something that no longer
      // exists would answer with "there is no relationship called…", which
      // reads as the delete having failed.
      const back = (id && result.ok !== false &&
                    String(body.action || '') !== 'delete')
        ? '/admin/federation' + queryWith(listView, { relationship: id })
        : '/admin/federation' + queryWith(listView, {});
      self.respondToAction(req, res, back, result);
      log.debug("Leaving the admin federation action endpoint.");
    });

    app.get('/admin/federation/map', function (req, res) {
      log.debug("Entering the admin federation map page.");
      const view = self.federationMapView(req);
      const graph = view.graph;
      const look = self.federationMapLooks(graph);
      const drawingLabel = 'Federation relationships in the trust realm "' +
        graph.realm.id + '", as a diagram';

      if (String(req.query.format || '') === 'svg') {
        // The document alone, for saving or embedding. No links (see
        // federationDrawing()) and `no-store` like every other page here,
        // because it describes live state.
        const bare = federationDiagram.render(graph, {
          resolve: look.resolve, links: false, id: 'fedmap', label: drawingLabel
        });
        res.set('Cache-Control', 'no-store').type('image/svg+xml')
          .send(bare.svg);
        log.debug("Leaving the admin federation map page. Answered SVG.");
        return;
      }

      const filterParams = { role: view.wanted.role,
                             protocol: view.wanted.protocol,
                             q: view.wanted.q };
      const picture = self.federationDrawing(graph, look, filterParams,
                                             drawingLabel);
      const filtering = !!(view.wanted.role || view.wanted.protocol ||
                           view.wanted.q);

      // The filter, and it is the list page's own plus a protocol select —
      // because narrowing the picture and narrowing the table under it has to
      // be ONE control. It carries no `page`: this view has no paging, and a
      // page number carried into a view with none is a parameter that does
      // nothing and comes back with the reader on the next hop.
      const roleOptions = ['<option value="">any role</option>'].concat(
        federation.ROLES.map(function (one) {
          return '<option value="' + self.esc(one.role) + '"' +
            (one.role === view.wanted.role ? ' selected' : '') + '>' +
            self.esc(one.short) + '</option>';
        })).join('');
      const protocolOptions = ['<option value="">any protocol</option>'].concat(
        federation.PROTOCOLS.map(function (one) {
          return '<option value="' + self.esc(one.protocol) + '"' +
            (one.protocol === view.wanted.protocol ? ' selected' : '') + '>' +
            self.esc(one.label) + '</option>';
        })).join('');

      // Every application on the picture, once, with which relationships it
      // uses and what has crossed each. It is the per-application half laid out
      // flat, because the picture can carry three lines on a label and this is
      // the place the fourth fact goes.
      const useRows = [];
      graph.relationships.forEach(function (row) {
        row.applications.forEach(function (use) {
          useRows.push({ row: row, use: use });
        });
      });
      useRows.sort(function (a, b) {
        if (b.use.authentications !== a.use.authentications) {
          return b.use.authentications - a.use.authentications;
        }
        return a.use.application < b.use.application ? -1
             : a.use.application > b.use.application ? 1 : 0;
      });

      const inner = self.messagesOf(req) +
        '<div class="tiles">' +
        self.tile(graph.counts.relationships, 'Relationships drawn') +
        self.tile(graph.counts.applications, 'Applications behind them') +
        self.tile(graph.counts.partners, 'Foreign partners') +
        self.tile(graph.counts.authentications, 'Federated sign-ins') +
        '</div>' +

        self.note('<strong>This is one trust realm.</strong> The federation ' +
        'register is per realm — an id that names a relationship in another ' +
        'realm names nothing here — so this picture is of <code>' +
        self.esc(graph.realm.id) +
        '</code> and of nothing else. The realm switcher at the top of the ' +
        'page is how you get to another one, and the realm is drawn on the ' +
        'hexagon so that a saved copy of this document still says which ' +
        'realm it is of.') +

        self.note('<strong>Left asks, right authenticates.</strong> ' +
        'Everything on the left of the hexagon arrives wanting somebody ' +
        'signed in — an application here, or a foreign service provider. ' +
        'Everything on the right is a party this service asks to do the ' +
        'signing in. An identity-provider-side relationship therefore points ' +
        'INWARD even though this service asserts outward, because the arrow ' +
        'is the request; and an identity BROKER, which is both at once, is ' +
        'then a single straight line through the middle instead of two ' +
        'arrows leaving the same box.') +

        '<form method="get" action="/admin/federation/map"><div ' +
        'class="formrow"><label for="q">Relationship</label>' +
        '<input type="text" id="q" name="q" value="' + self.esc(view.wanted.q) +
        '" size="24" placeholder="id, name, partner or application">' +
        '<label for="role">Role</label><select id="role" name="role">' +
        roleOptions + '</select><label ' +
        'for="protocol">Protocol</label><select id="protocol" ' +
        'name="protocol">' +
        protocolOptions + '</select>' +
        '<button type="submit">Filter</button>' +
        (filtering ? ' <a href="/admin/federation/map">clear</a>' : '') +
        '</div></form>' +

        (graph.relationships.length
          ? picture.html
          : self.note(graph.empty
              ? '<strong>This realm federates with nobody, so there is ' +
                'nothing to draw.</strong> A relationship is created on <a ' +
                'href="/admin/federation">the federation page</a> and does ' +
                'nothing until it is enabled, which is a second deliberate ' +
                'act. Federation is the one feature in this service that ' +
                'must be configured before it will do anything at all.'
              : '<strong>No relationship matches this filter.</strong> The ' +
                'register is not empty — <a ' +
                'href="/admin/federation/map">clear the filter</a> to see ' +
                'the rest.')) +

        '<h2>The relationships</h2>' +
        self.note('The same rows <a href="/admin/federation">the list ' +
        'page</a> shows, plus the two columns that are facts about TWO ' +
        'registers rather than about one entry: how many applications are ' +
        'configured to use a partner, and what the identity-provider side ' +
        'does about authenticating somebody. Neither has anywhere to live on ' +
        'a relationship\'s own entry.') +
        '<table><tr><th>Relationship</th><th>This service ' +
        'is</th><th>Protocol</th><th>Partner</th><th>State</th><th ' +
        'class="num">Applications</th><th class="num">People</th><th ' +
        'class="num">Sign-ins</th><th>Authentication method</th></tr>' +
        (graph.relationships.map(self.federationMapRow.bind(self)).join('') ||
          '<tr><td colspan="9">Nothing to show.</td></tr>') +
        '</table>' +

        '<h2>Applications, per relationship</h2>' +
        self.note('<strong>One row per application and relationship, which ' +
        'is the pair the counts on this page are of.</strong> ' +
        '<code>fedAuthentications</code> on a relationship answers "how much ' +
        'has crossed this partner"; this answers "how much has crossed it ' +
        'for each of the applications behind it", which is a different ' +
        'question the moment a second application names the same partner. A ' +
        'pair is counted only where this service is CONFIGURED for it — the ' +
        'application entry names the relationship, or an ' +
        'identity-provider-side relationship brokers to it — and the check ' +
        'is made against the live register when the sign-in completes rather ' +
        'than trusted from the request that started it.') +
        '<table><tr><th>Application</th><th>Relationship</th><th>Partner</th>' +
        '<th class="num">People</th><th class="num">Sign-ins</th>' +
        '<th>Configured by</th><th>Last</th></tr>' +
        (useRows.map(function (one) {
          const use = one.use;
          const row = one.row;
          return '<tr><td><a href="' +
            self.esc('/admin/applications' +
                     queryWith({}, { application: use.application })) +
            '">' + self.esc(use.application) + '</a></td>' +
            '<td><a href="' +
            self.esc('/admin/federation' +
                     queryWith({}, { relationship: row.id })) +
            '">' +
            self.esc(row.id) + '</a></td>' +
            '<td class="who">' + self.esc(row.peer || '') + '</td>' +
            '<td class="num">' + use.users + '</td>' +
            '<td class="num">' + use.authentications + '</td>' +
            '<td>' + (use.configured
              ? (use.source === 'broker'
                  ? 'the relationship <a href="' +
                    self.esc('/admin/federation' +
                             queryWith({}, { relationship: use.via })) +
                    '">' + self.esc(use.via) + '</a>, which brokers to it'
                  : 'its own <code>appFederationRelationship</code>')
              : '<span class="bad">nothing, any more</span>' +
                '<span class="sub">These sign-ins happened and are kept ' +
                'rather than dropped, so the relationship\'s own totals ' +
                'still add up. Something named this pair when they happened ' +
                'and no longer does.</span>') + '</td>' +
            '<td>' + (use.lastSeen
              ? self.esc(use.lastSeen) +
                (use.lastUser ?
                 '<span class="sub">' + self.esc(use.lastUser) + '</span>' : '')
              : '<span class="state-none">never used</span>') + '</td></tr>';
        }).join('') ||
          '<tr><td colspan="7">No application is configured to authenticate ' +
          'through any relationship in this realm, and none has ever done ' +
          'so. That is <code>appFederationRelationship</code> on an entry ' +
          'under <code>ou=applications</code>, which is written by nobody — ' +
          'no protocol presents it and no sighting derives it.</td></tr>') +
        (graph.relationships.filter(function (r) { return r.unattributed; })
          .length
          ? self.note('<strong>Some sign-ins belong to no row above, and ' +
            'that is not a fault.</strong> A relationship\'s own total ' +
            'counts every credential that crossed it; the rows above count ' +
            'only the ones that named an application this service is ' +
            'configured for. Three ordinary things make the difference: the ' +
            'partner buttons at the foot of the sign-in screen belong to no ' +
            'application, <code>/federation/login/{id}</code> needs no ' +
            'configuration at all to reach, and a sign-in naming an ' +
            'application that does not point here is refused a row and ' +
            'logged. The difference is named rather than left to be noticed ' +
            '— this is a page about counting, and a column that does not add ' +
            'up is worse than one that explains itself.') +
            '<table><tr><th>Relationship</th><th class="num">Sign-ins</th>' +
            '<th class="num">Attributed</th>' +
            '<th class="num">Belonging to no application</th></tr>' +
            graph.relationships.filter(function (r) { return r.unattributed; })
              .map(function (r) {
                return '<tr><td><a href="' +
                  self.esc('/admin/federation' +
                           queryWith({}, { relationship: r.id })) +
                  '">' + self.esc(r.id) + '</a></td>' +
                  '<td class="num">' + r.authentications + '</td>' +
                  '<td class="num">' + r.attributed + '</td>' +
                  '<td class="num">' + r.unattributed + '</td></tr>';
              }).join('') + '</table>'
          : '') +

        '<h2>The key</h2>' +
        self.note('The shapes and the colours are drawn by the same ' +
        '<code>render()</code> the picture is, so a legend cannot come to ' +
        'describe a diagram this service no longer draws.') +
        self.federationMapKey();

      self.respond(req, res,
                   { realm: graph.realm, counts: graph.counts,
                     filter: view.wanted,
                     empty: graph.empty, filtered: graph.filtered,
                     relationships: graph.relationships,
                     nodes: graph.nodes.map(function (node) {
                  return { id: node.id, kind: node.kind, label: node.label,
                           relationships: node.relationships || [] };
                }),
                     // The EDGES without their `row`, which is the whole
                     // relationship and is already in `relationships` above. A
                     // caller joins on `relationship`; repeating it here would
                     // make the document several times larger and give two
                     // copies of every state to disagree.
                     edges: graph.edges.map(function (edge) {
                  return { id: edge.id, from: edge.from, to: edge.to,
                           relation: edge.relation,
                           relationship: edge.relationship,
                           brokeredTo: edge.brokeredTo || '',
                           use: edge.use || null };
                }) },
                   'Federation — the picture', '/admin/federation', inner,
                   self.upTo('/admin/federation', 'The picture',
                             self.listViewOf('/admin/federation', req.query)));
      log.debug("Leaving the admin federation map page. " +
                graph.relationships.length + " relationship(s) drawn.");
    });
    log.debug("Leaving AdminConsole.registerRoutes().");
  }
}

// ---------------------------------------------------------------------------
// THE INSTANCE, BUILT BY THE COMPOSITION ROOT (#50, R2). This module builds no
// instance of its own: `common/protocol_stack.ts` builds one and calls
// `installInstance()`, which runs `AdminConsole.wire()`. Every name this
// module exports that the instance answers is a FACADE that forwards to it,
// for the JavaScript that still calls this module through `require()`; a
// process that never runs the root gets a default instance, built from
// `defaultDeps()` (see `common/instance_slot.ts`).
//
// `WIRE_STEPS` is the work loading this module used to do with its own
// instance — the sidebar's flattening, the setting-homes check, the module
// constants drawn with `note()` and `warn()`, and the read layer's settings
// block. Each step stays beside the declaration it fills, and
// `AdminConsole.wire()` runs them in the order they are written, once, for
// whichever instance is installed.
// ---------------------------------------------------------------------------
const WIRE_STEPS: Array<(instance: AdminConsole) => void> = [];

const slot = new InstanceSlot<AdminConsole>(
  'admin-ui/admin',
  () => new AdminConsole(AdminConsole.defaultDeps()),
  AdminConsole.wire,
  log);

// Every page in every section, flattened, with the section it belongs to on
// each row. Derived rather than typed for the reason above; the `section`
// member is what lets the sidebar bold the right heading without a second
// lookup. A page inside a group is flattened to the same shape as one outside
// it, so nothing downstream of here can tell them apart.
const NAV = [];
WIRE_STEPS.push(function (instance: AdminConsole): void {
  SECTIONS.forEach(function (section) {
    instance.sectionPages(section).forEach(function (item) {
      NAV.push({ path: item.path, label: item.label, section: section.title });
    });
  });
});

// ---------------------------------------------------------------------------
// WHICH PAGE OWNS WHICH SETTINGS, AND THE ONE TABLE THAT DECIDES IT.
//
// Until 2026-08-27 every one of `config.js`'s settings was drawn on
// `/admin/config` and nowhere else, and each protocol page that cared about
// its own — `/admin/saml2`, `/admin/saml11`, `/admin/scim` — showed them as
// READINGS with a link to that page, saying in three different sets of words
// that the configuration page owned them. That was one page of a hundred and
// fifty-four rows: somebody who came to the console to change the Kerberos
// realm found the Kerberos page, read what a ticket carries, and was then sent
// somewhere else to type it.
//
// So the settings moved to the page for the protocol they configure, and this
// table is the whole of the mapping. Four rules about it, and each is the
// reason the table exists rather than the placements being spread over twenty
// route handlers:
//
//   * **A GROUP HAS EXACTLY ONE ROW HERE AND A ROW MAY NAME MORE THAN ONE
//     PAGE.** `config.js`'s `group` is the unit — it is already the thing the
//     table declares, the thing `config.groups()` buckets by and the thing a
//     reader looking for "the Kerberos settings" is thinking of — so a page
//     never names individual keys. A page that wanted half a group would be
//     asking for the group to be split in `config.js`, where the reasoning for
//     what belongs together lives.
//   * **`SAML` IS THE ONE ROW WITH TWO PAGES, and it is not an untidiness.**
//     `saml.issuer` is the Issuer of every assertion this service builds — SAML
//     2.0's, SAML 1.1's, and WS-Federation's, which are built by the same two
//     functions — so there is no one page it belongs to. It is drawn on both
//     SAML pages, each form writing through `config.setOverride()` like every
//     other, and `configFormsFor()` says so on both rather than letting a
//     reader discover that the value they set on one page had appeared on the
//     other. The WS-Federation page names it too, as a link: three forms onto
//     one setting is where "shown in more than one place" stops being useful.
//   * **THE PAGE IS NOT A SECOND STORE, which is what makes any of this
//     allowed.** Every form these pages draw is `configSection()`, posting
//     `set-many` to `POST /admin/config` — the same action function, the same
//     override map, the same validation — with a hidden `from` naming the page
//     to come back to. That is the rule `/admin/token-lifetimes` argued when it
//     took four of these rows onto a page of its own: a second DOOR onto one
//     value is fine and a second PLACE THE VALUE LIVES is not.
//   * **DRIFT IS CHECKED AT STARTUP, not trusted.** A group added to
//     `config.js` with no row here would silently stop being editable anywhere
//     — the settings would exist, answer on `GET /admin-api/config`, and appear
//     on no page at all. `checkSettingHomes()` below refuses to let that be
//     quiet: it logs, and `/admin/config` shows it in the same spirit as
//     `/admin/sts-metadata` reporting a route nobody described.
//
// The pages named here are ordinary console pages and every one of them is in
// SECTIONS above. Eight of them were created for this: the OAuth2 / OIDC,
// OpenID4VCI, OpenID4VP, WS-Trust, WS-Federation, Kerberos, LDAP / LDAPS and
// TLS families had settings and no page, which is exactly the gap that made
// them invisible on a console organised by protocol.
const SETTING_HOMES = [
  // The one group with no protocol to belong to, and the reason /admin/config
  // still has a form on it. A bind address and a log level are facts about the
  // PROCESS; there is no family whose page they would be less surprising on.
  { group: 'Global', pages: ['/admin/config'] },

  // KEY MATERIAL lives on /admin/config for the same reason Global does: where
  // this service's signing keys come from and what encrypts them at rest are
  // facts about the PROCESS, not about any one protocol — every family here
  // signs with the same key.
  { group: 'Key material', pages: ['/admin/config'] },
  // WEB SECURITY, on /admin/config for the same reason: a rate limit and a
  // CSRF scheme are facts about the PROCESS and every browser-facing surface
  // shares them.
  { group: 'Web security', pages: ['/admin/config'] },

  { group: 'Trust realms', pages: ['/admin/realms'] },
  { group: 'OAuth 2.0 / OIDC', pages: ['/admin/oauth2'] },
  { group: 'Admin console', pages: ['/admin/rbac'] },
  // The embedded protocol debugger's rows are drawn on its own page, which is
  // also where its listener and api process are reported: a setting like
  // `debugger.port` beside the port it actually bound is the one reading that
  // answers "why is it not where I set it".
  { group: 'Protocol debugger', pages: ['/admin/debugger'] },
  // THE MANAGEMENT API'S THREE SETTINGS SIT BESIDE THE CONSOLE'S, and that is
  // an argument rather than a convenience. /admin/rbac is the page that
  // answers "who may reach the administrative surfaces of this service"; the
  // console's half of that answer has always been drawn there, and since
  // 2026-09-08 /admin-api has a half of its own — whether it demands a token
  // at all, which audience that token must carry, and the secret its client
  // authenticates with. A page of its own would have split one question across
  // two screens, and /admin/config was wrong for the reason its own comment
  // gives: these are not facts about the PROCESS, they are the access policy
  // of one named surface.
  //
  // `adminApi.clientSecret` is drawn here as a SECRET, so the page shows
  // whether one is set and never the value. That matters more here than
  // anywhere else in this table: it is the credential that mints the token
  // this very console's gate would otherwise be the only way to obtain.
  { group: 'Management API', pages: ['/admin/rbac'] },
  { group: 'Applications', pages: ['/admin/applications'] },
  { group: 'Federation', pages: ['/admin/federation'] },
  { group: 'XACML', pages: ['/admin/xacml'] },
  // THE CERTIFICATE AUTHORITY'S FOUR SETTINGS, on the page that builds one.
  // They are DEFAULTS FOR A FORM rather than a policy — what a hierarchy was
  // built with is stored on the hierarchy — so the page that takes each of
  // them as a field is the only place they read as anything but trivia.
  { group: 'PKI', pages: ['/admin/pki'] },
  // Two pages, deliberately. See the header above.
  { group: 'SAML', pages: ['/admin/saml2', '/admin/saml11'] },
  { group: 'SAML 2.0', pages: ['/admin/saml2'] },
  { group: 'SAML 1.1', pages: ['/admin/saml11'] },
  // THE TEN THAT BECAME PER-APPLICATION DEFAULTS ON 2026-08-27, and the two
  // groups exist to say exactly that. Every one of them was a `SAML 2.0` or
  // `SAML 1.1` row drawn on that profile's identity provider page, and every
  // one is now the DEFAULT an application inherits when its own entry says
  // nothing — `saml2AssertionLifetimeMin` and its nine siblings on the entry
  // win where they are set. They are off the two identity provider pages
  // because those pages configure THIS SERVICE as an identity provider, and
  // these ten no longer describe what it does: they describe what it does for
  // an application that has not been told otherwise. `/admin/applications` is
  // where the exception is typed.
  //
  // The pages here are what makes them leave those two screens: a group is
  // drawn where its row says and nowhere else, so moving the rows moved the
  // settings. Nothing about the KEYS or their environment variables changed,
  // which is what keeps every appconfig file and every `STS_SAML*` variable
  // working exactly as it did.
  { group: 'SAML 2.0 assertions', pages: ['/admin/saml-assertions'] },
  { group: 'SAML 1.1 assertions', pages: ['/admin/saml-assertions'] },
  // WS-FEDERATION'S ONE ASSERTION SETTING IS DRAWN WITH THE SAML ONES, and
  // that is not a filing error. A WS-Federation sign-in response CARRIES a
  // SAML 1.1 assertion, built by the same function the SAML 1.1 profiles use,
  // so this row governs the same kind of document as the ten above it. Drawing
  // it on /admin/wsfed would have put the only setting that decides how long a
  // WS-Federation token lives on a different page from every other assertion
  // lifetime in this service. `wsfed.entityId` stays over there because it is
  // this service's own name and no application can have an opinion about it.
  { group: 'WS-Federation assertions', pages: ['/admin/saml-assertions'] },
  // THE FIVE OAUTH SETTINGS A CLIENT MAY ANSWER FOR ITSELF, on the page that
  // already drew three of them. /admin/token-lifetimes was a bespoke page
  // naming its four keys and had no row here at all; it has one now because
  // these five had to LEAVE /admin/oauth2, and a group leaves a page by being
  // homed on another. The four global oauth2 rows — the issuer, RFC 9700 mode
  // and the two clock skews — stay in the `OAuth 2.0 / OIDC` group and stay
  // over there.
  { group: 'OAuth 2.0 / OIDC per-client', pages: ['/admin/token-lifetimes'] },
  { group: 'WS-Trust', pages: ['/admin/wstrust'] },
  { group: 'WS-Federation', pages: ['/admin/wsfed'] },
  { group: 'TLS', pages: ['/admin/tls'] },
  { group: 'OID4VCI', pages: ['/admin/oid4vci'] },
  { group: 'OID4VP', pages: ['/admin/oid4vp'] },
  { group: 'Kerberos', pages: ['/admin/kerberos'] },
  { group: 'LDAP', pages: ['/admin/ldap'] },
  // A page of its own rather than a section of /admin/ldap, and the reason is
  // that these six settings are not about the directory. They decide whether
  // the TRUST REALM REGISTRY and the RUNTIME SETTING CHANGES are written down
  // as well — two things that have nothing to do with LDAP — and one of the
  // three modes involves no directory format at all. Putting them under
  // LDAP / LDAPS would have told a reader looking for "does my realm survive a
  // restart" to look at the wrong page.
  { group: 'Persistence', pages: ['/admin/persistence'] },
  // THE CLUSTER'S SETTINGS (2026-09-14, #46), on the page that shows the
  // members, the leases and the capabilities those settings decide about —
  // `cluster.acceptMissingCapabilities` read anywhere but beside the list of
  // what is missing would be a list of ids with no meaning.
  { group: 'Cluster', pages: ['/admin/cluster'] },
  // SIGNER ROTATION (2026-09-22, #42/#48), on /admin/keys — the page that
  // shows every unit's current, next and retired keys and carries the
  // Rotate controls (rcbj's D5).
  { group: 'Signing keys', pages: ['/admin/keys'] },
  // THE SCHEDULER'S SETTINGS (2026-09-22, #49), on the page that shows the
  // jobs they switch and the ticks they time.
  { group: 'Scheduler', pages: ['/admin/scheduler'] },
  { group: 'SCIM', pages: ['/admin/scim'] },
  // Shared Signals. A page of its own rather than a section of anything, for
  // the reason /admin/federation is ungrouped: SSF is not a variant of another
  // family here, it is the one that runs the other way round — this service
  // delivering an event to somebody who agreed to be told. A group of one
  // would be a heading that said the label twice.
  { group: 'SSF', pages: ['/admin/ssf'] },
  // CAEP. Its own group and its own page, for the reason the sidebar entry
  // gives: a vocabulary over SSF is a second specification and not a corner
  // of the first. The MONITORING page beneath it edits nothing, so it is not
  // named here — this table is about where a setting is EDITED.
  { group: 'CAEP', pages: ['/admin/caep'] },
  // RISK SCORING (#62): its datasets and failure history, on the Monitoring
  // page that shows them, because there is no protocol for them to belong to.
  { group: 'Risk', pages: ['/admin/risk'] },
  // RISC, on the CAEP group's terms: the second vocabulary over SSF is a
  // second specification and not a corner of the first, and the MONITORING
  // page beneath it edits nothing so it is not named here.
  { group: 'RISC', pages: ['/admin/risc'] },
  // The claim itself is an OAuth2/OIDC and SAML concern, but what it NAMES is a
  // directory group and what decides whether somebody is in one is the Groups
  // page. Somebody asking "why is this group not in my token" is looking at
  // that page's membership table when the question occurs to them.
  // Roles. Its own group and its own page: what these four settings configure
  // is the claim and whether the decision is asked for at all, and both halves
  // of a role — who holds one, and what requires one — are edited on that page
  // and on an application's page respectively rather than in a setting.
  { group: 'Roles', pages: ['/admin/roles'] },
  // THE TWO SECOND FACTORS, ONE GROUP AND ONE PAGE EACH, BOTH UNDER PROTOCOLS
  // (2026-09-10).
  //
  // **THIS WAS ONE ROW READING `Multi-factor authentication` -> `/admin/mfa`,
  // AND ITS COMMENT ARGUED FOR FILING IT UNDER IDENTITIES** — because the page
  // answered *who holds a second factor*, which is a question about people.
  // That was right about the ROSTER and wrong about the SETTINGS, and one page
  // could not be filed by both halves at once. The roster is columns on
  // /admin/users now; these two rows are the mechanisms.
  //
  // The old comment also said WebAuthn "has no settings at all and so has no
  // row here". It has thirteen since 2026-09-10 — every parameter of the
  // ceremony, which had been literals in a string in `authn/authn.ts`.
  { group: 'TOTP MFA', pages: ['/admin/totp'] },
  { group: 'GNAP', pages: ['/admin/gnap'] },
  // ===== certificate enrollment setting homes (2026-09-13) =====
  { group: 'ACME', pages: ['/admin/acme'] },
  { group: 'EST', pages: ['/admin/est'] },
  { group: 'SCEP', pages: ['/admin/scep'] },
  { group: 'WebAuthn', pages: ['/admin/webauthn'] },
  // THE THIRD (2026-09-10), and it is a mechanism like the two above it rather
  // than a policy about them — which is why it gets a group and a page of its
  // own instead of four more rows on the TOTP page. A recovery code stands in
  // for EITHER of those two, so filing its settings under one of them would
  // put them where half the readers would not look.
  { group: 'Backup codes', pages: ['/admin/backup-codes'] },
  // A POLICY ABOUT THE TWO MECHANISMS ABOVE (2026-09-13), and drawn on BOTH of
  // their pages — `saml.issuer`'s arrangement — because either one satisfies
  // it and a reader of either page must see that it is in force.
  { group: 'Second-factor requirement',
    pages: ['/admin/totp', '/admin/webauthn'] },
  { group: 'Group claim', pages: ['/admin/groups'] },
  { group: 'Audit log', pages: ['/admin/audit'] },
  { group: 'Delegation', pages: ['/admin/delegation'] },
  { group: 'Logout', pages: ['/admin/logout'] },
  { group: 'SPIFFE', pages: ['/admin/spiffe'] }
];

// Kept, not just logged: /admin/config renders it. A line in a log that scrolls
// past at startup is not a thing anybody reads twice.
let SETTING_HOME_PROBLEMS: ReturnType<AdminConsole['checkSettingHomes']>;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  SETTING_HOME_PROBLEMS = instance.checkSettingHomes();
});

// The two measurements the folds below are decided by. They are declared
// HERE, above everything, rather than beside note() where they are read:
// several of this file's module-level constants are built by calling note()
// and warn() (at require time until #50's R2; in `WIRE_STEPS` now, which a
// default instance runs at the end of this module's load), and a `const` is
// in its temporal dead zone until the line that declares it runs — so a
// definition beside the function throws `Cannot access before
// initialization` while the module is still loading, which takes the whole
// service down (rule 1).
// About one rendered line of `.note` text in this console's content column.
// The column is 62rem at `.note`'s .78em, so a line is nearer 130 characters
// than this; the number is deliberately under that, because the test worth
// applying is "does this read as a paragraph" rather than "does it wrap".
const ONE_LINE_CHARS = 110;

// A summary has to fit on one line beside its marker, whatever the note it
// opens. Past this the opening sentence is truncated rather than allowed to
// become the wall of text this exists to fold away.
const SUMMARY_CHARS = 96;

// A tooltip may run to a couple of lines where a summary may not: it is drawn
// over the page rather than in it, so length costs a reader nothing until they
// ask for it. Past this it is truncated, because a browser renders a title of
// any length and one of them will happily draw a paragraph the width of the
// screen.
const TIP_CHARS = 190;

// WHICH QUERY PARAMETERS BELONG TO A SECTION'S LIST rather than to the page
// under it — the filter the reader typed and the page they had reached.
//
// It is a WHITELIST per section rather than "everything that is not ours", for
// the same reason the tokens page rebuilds its `back` field from a list of
// names instead of echoing it: what comes out of here is put into a URL this
// service hands to a browser, so the set of names has to be one this file
// wrote. It is also why a section that cannot be drilled into has no row —
// carrying a filter through a page nothing hangs under would be state nobody
// can get back to.
const LIST_PARAMS = {
  // `factor` arrived on 2026-09-10 with the second-factor roster /admin/mfa
  // used to draw. It is a VIEW of this list like `q` and `protocol` — a
  // narrowing somebody chose and expects to still be there after they clear
  // an enrolment — so it is carried through the Clear buttons on the
  // drill-down like the other two.
  '/admin/users': ['q', 'protocol', 'factor', 'per', 'page'],
  '/admin/groups': ['q', 'per', 'page'],
  // `application`, `subject`, `subjectKind` and `kind` are the PREVIEW's own
  // parameters and are deliberately NOT here: they are a question somebody
  // asked once, not a view, so carrying them through a Remove button would
  // re-ask it on every write and put a stale answer above the table. The same
  // reasoning NOT_A_VIEW applies to `notice` and `error`.
  '/admin/roles': ['q', 'per', 'page'],
  '/admin/policies': ['per', 'page'],
  '/admin/applications': ['q', 'kind', 'per', 'page'],
  '/admin/saml2': ['q', 'per', 'page'],
  '/admin/saml11': ['q', 'per', 'page'],
  '/admin/authorization-servers': ['per', 'page'],
  // GNAP's two lists and the grant state filter (gnap/gnap_admin.ts).
  '/admin/gnap': ['state', 'per', 'grantsPage', 'resourcesPage'],
  '/admin/gnap/monitor': ['per', 'page'],
  // ===== certificate enrollment list params (2026-09-13) =====
  '/admin/acme': ['per', 'certificatesPage', 'credentialsPage',
                  'accountsPage', 'hostNamesPage'],
  '/admin/acme/monitor': ['per', 'page'],
  '/admin/est': ['per', 'certificatesPage'],
  '/admin/est/monitor': ['per', 'page'],
  '/admin/scep': ['per', 'certificatesPage', 'credentialsPage'],
  '/admin/scep/monitor': ['per', 'page'],
  // The pushed-request filter and both lists' paging (2026-09-13). The page
  // rebuilds a Withdraw's `back` from its own copy of this list, because
  // `listViewFromBack()` is not exported; the two must name the same keys.
  '/admin/oauth2/monitor': ['state', 'client_id', 'per', 'page',
                            'clientsPage'],
  '/admin/spiffe/entries': ['q', 'origin', 'per', 'page'],
  '/admin/spiffe/agents': ['q', 'per', 'page'],
  // `personq` and `personfrom` are the grant pane's search (2026-09-13), so a
  // grant or a Revoke lands back on the results the reader was working
  // through. `person` is deliberately NOT here: it is the one they picked, and
  // carrying it through the grant it was picked FOR would redraw a grant form
  // for somebody who already holds the role.
  '/admin/rbac': ['q', 'role', 'personq', 'personfrom', 'per', 'page'],
  // `family` rather than `q`: this page's filter is a family and there are ten
  // of them, so it is chosen by clicking a row of the summary table rather than
  // typed. `user` is deliberately NOT here — it is the drill-down's own leaf,
  // not the list's filter, and carrying it in the section crumb would make the
  // way back point at the page the reader is already on.
  // The delegation table's five filters and its paging. `/admin/delegation/map`
  // is the drill-down that spends them, and it carries the filters onward
  // through a form of its own — the picture and the table are filtered by one
  // control, so a reader who narrowed the table and then drew it gets the
  // picture of what they were looking at.
  // The four names after `page` are the two CHOOSER SEARCHES and their offsets
  // (chooserPane(), 2026-08-26). They are in here for the same reason `q` is:
  // a reader who searched for `esb` and clicked the one result should come back
  // from the drill-down to the page they left, and the drill-down's own copy of
  // the chooser should open holding the search they were in the middle of. It
  // is also what keeps the two searches independent of each other — each names
  // its own pair, so paging one cannot move the other.
  //
  // The eight names after those are the SEVEN LISTS' page numbers and the two
  // section searches (2026-09-01). Every list on that page is paged separately
  // and they share one `per`, so each needs a parameter of its own — and every
  // one of them has to survive a drill-down for the reason `page` does: a
  // reader who paged the grants table to 4, clicked a client and came back
  // should come back to page 4 of the grants table and not to the top of a
  // page they have already read three screens of.
  '/admin/delegation': ['type', 'mode', 'outcome', 'protocol', 'q', 'per',
                        'page',
                        'appq', 'appfrom', 'userq', 'userfrom',
                        'permq', 'grantq',
                        'chainsPage', 'permissionsPage', 'grantsPage',
                        'pairsPage', 'flagsPage', 'mechanismsPage'],
  // The tokens page's own three filters and its paging, here since 2026-08-26
  // because that page now HAS a drill-down: every identifier links to
  // /admin/tokens/credential, and without this entry the way back from it
  // landed on page 1 of an unfiltered list of everything this service has ever
  // issued. `session` joined the tokens page's three filters on 2026-09-04,
  // when /admin/sessions started linking to it: every row there links to the
  // credentials issued on that session, and there was no way to ask for them.
  '/admin/tokens': ['family', 'kind', 'state', 'session', 'per', 'page'],
  '/admin/sessions': ['q', 'protocol', 'per', 'page'],
  // The Shared Signals inbox's own search and paging (2026-09-10). It has no
  // drill-down, so the only thing that spends these is the Clear button's
  // `back` — which is enough on its own: a reader who searched for a username,
  // read what came back and pressed Clear should not be returned to an
  // unfiltered page 1 of a list that is now empty for two different reasons.
  '/admin/signals': ['sigq', 'per', 'receivedPage'],
  // The dead letters' two exact narrowings, their search and their paging
  // (2026-09-14). No control on that page posts, so what spends these is the
  // links between its own tables — a stream row's letters, a cause's letters
  // — each of which keeps the rest of the view as the reader left it.
  '/admin/ssf/dead-letters': ['dlq', 'dlstream', 'dlcause', 'per',
                              'lettersPage'],
  // The truststore's one list (2026-09-12). Spent by the Remove button's
  // `back`, so removing the last row on page 3 lands on page 3 — clamped to the
  // last page there is — rather than on page 1.
  '/admin/tls/trust': ['per', 'page'],
  // The Kerberos principals' two lists (2026-09-12), paged separately and
  // sharing one `per`. Spent by every row button's `back`.
  '/admin/kerberos/principals': ['per', 'peoplePage', 'servicesPage'],
  // The back-channel deliveries' filter, search and paging (2026-09-17),
  // spent by the Retry button's `back`.
  '/admin/logout': ['family', 'per', 'page', 'deliveryState', 'deliveryq',
                    'backchannelDeliveriesPage'],
  '/admin/realms': ['per', 'page'],
  '/admin/federation': ['q', 'role', 'per', 'page'],
  // TWO lists on one page — the global overrides and the recorded answers —
  // so each gets a page parameter of its own and they share one `per`, which
  // is the arrangement /admin/delegation already has and pagingOf()'s
  // `options.name` exists for. `q` searches the recorded half only; the
  // overrides table is one row per (application, scope) and is short by
  // construction, because somebody typed every one of them.
  '/admin/consent': ['q', 'per', 'page', 'globalsPage', 'usersPage'],
  // The CAEP page's SESSION CHOOSER (2026-09-03), which replaced a
  // `<select name="session_id">` for the reason chooserPane() gives about the
  // application one: a control here must be the same size whatever the
  // register holds, and this register grows by one row per sign-in for the
  // life of the process. `sessq` is the search, `sessfrom` its offset, and
  // `session` the one the reader picked — all three carried, because every
  // control on this page is a GET that reloads it and a reader who searched
  // for a username, paged to the second twenty and picked a session must not
  // lose any of that to pressing Emit.
  '/admin/caep': ['sessq', 'sessfrom', 'session'],
  // The CAEP SESSIONS list (2026-09-04), which has a drill-down of its own
  // since the per-session detail moved off it: every session identifier on
  // that table links to /admin/caep-sessions/session. `sessq` is the search
  // over that one table and `sessionsPage` its page, named after the list for
  // pagingOf()'s reason — that page carries a second table (the streams) and
  // a bare `page` could not serve both. `per` is shared, as it is everywhere
  // a page holds more than one list.
  // The RISC pages, on exactly the CAEP pair's terms. `acctq2` is the
  // chooser's search on the settings page and `acctq` the search on the
  // monitoring one — two names because the two are different controls on two
  // pages, and a shared name would carry a chooser's offset into a table's
  // filter. `rappq` and `rapplicationsPage` are the per-receiver section's,
  // named apart from the CAEP page's `appq` for the same reason.
  '/admin/risc': ['acctq2', 'acctfrom'],
  '/admin/risc-accounts': ['acctq', 'per', 'accountsPage',
                           'rappq', 'rapplicationsPage'],
  '/admin/caep-sessions': ['sessq', 'per', 'sessionsPage',
                          // The per-receiver section's own search and page
                          // (2026-09-04). Carried for the reason every other
                          // list's are: a reader who narrowed that table and
                          // then opened a session should come back to what
                          // they were reading.
                          'appq', 'applicationsPage']
};

// One page's link. Split out of navBar() when groups arrived so that a page
// draws the same way at either depth — the active-tab-is-a-link rule above is
// the kind that gets applied to one of two copies.
// WHAT SCROLLS THE SIDEBAR TO THE PAGE YOU ARE ON (2026-09-05).
//
// `nav` is its own scroll container — `.side` is sticky and the card inside it
// has `overflow-y:auto`, which is what stops a long list pushing the page down
// — and a scroll container starts at the TOP on every load. So navigating to a
// page low in the list left that page's own entry below the fold: the reader
// arrived somewhere and the list did not show where.
//
// **`autofocus` is the whole mechanism and it needs no script**, which is why
// this console can have it at all: a browser scrolls a focused element into
// view, including scrolling the ancestor container it lives in. It adds no
// scripted page to the root CLAUDE.md's list and `script-src 'none'` is
// untouched.
//
// **`tabindex="-1"` is what makes it safe.** The active item is a `<span>`
// when it is the page being drawn, and a span is not focusable without it — so
// autofocus alone would do nothing. `-1` rather than `0` because the element
// must be focusABLE without joining the TAB ORDER: it is the page you are
// already on, so a keyboard user tabbing through the nav should reach the
// links they can go to and not stop on the one they are standing on.
//
// **It is the only `autofocus` in this console**, which was checked rather
// than assumed: two of them and the first in document order wins, so one added
// to a form field on some page would silently stop working the day somebody
// added another. If a page ever needs to focus a field on load, that page has
// to opt this one OUT rather than compete with it.
//
// `scroll-margin` in the stylesheet keeps it off the container's own edge, so
// what is revealed is the item with its neighbours around it rather than the
// item flush against the top where it reads as the start of the list.
const ACTIVE_NAV_FOCUS = ' tabindex="-1" autofocus';

// How long a leaf crumb may be before it is cut. A group's leaf is a DN and a
// user's can be a did:jwk — a few hundred characters of base64url with nowhere
// a browser will break a line — and a trail that wraps to four lines is not a
// trail. The full name is in the <h1> immediately below it and in the `title`
// here, so nothing is lost by cutting: what the crumb is for is the LINKS to
// its left.
const MAX_CRUMB = 44;

// ---------------------------------------------------------------------------
// THE BANNER EVERY PAGE CARRIES, AND IT NOW HAS THREE THINGS TO SAY RATHER THAN
// ONE.
//
// It is repeated on all of them rather than shown once on the index, because
// the pages are linkable and the one somebody arrives at directly is exactly
// the one that needs to say this. What it says depends on the state of the
// gate, and the three states are genuinely different warnings rather than one
// warning with a detail changed:
//
//   * THE GATE IS OFF. The old banner, unchanged. **THAT STATE IS NO LONGER
//     REACHABLE**: `admin.authRequired` was removed on 2026-09-06 and
//     `mode.gatesConsole()` answers `true` in both modes, so OPEN_BANNER and
//     every `!enforced` arm below are dead. They are kept rather than deleted
//     because what they say is "nothing here checks anything", and a banner of
//     that kind is the one sort of dead code whose quiet deletion would matter
//     if a third mode ever brought the state back.
//   * THE GATE IS ON AND NOBODY HOLDS A ROLE. The most dangerous state and the
//     one nothing else would report: a person has signed in, sees a working
//     console, and it is working because the roster is EMPTY rather than
//     because they were allowed. Said loudly, with what to do about it, because
//     the moment somebody grants the first role the door shuts behind whoever
//     is not in it — including, quite possibly, them.
//   * THE GATE IS ON AND THE ROSTER IS ENFORCED. Not a warning at all: who is
//     signed in and what they hold. It is still on every page, because "am I
//     read-only here" is the question behind every button a reader does not
//     find.
//
// It takes what `respond()` worked out rather than asking again, so the banner
// and the guard that let the request through cannot come to disagree about who
// somebody is.
// ---------------------------------------------------------------------------
let OPEN_BANNER: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  OPEN_BANNER =
    instance.warn('<strong>This console is not protected.</strong> The ' +
    'gate is OFF, so nothing here checks a credential — and nothing else in ' +
    'this service does either: the username typed at the sign-in screen is ' +
    'the identity in every token it issues. Anyone who can reach this port ' +
    'can revoke every token and change what the next one contains. That is ' +
    'fine on a laptop or a compose network and is not fine on a public ' +
    'address. Say who may get in on <a href="/admin/rbac">Admin roles</a>, ' +
    'which draws ' +
    'every setting behind this page.');
});

// ---------------------------------------------------------------------------
// THE REALM CHOOSER, at the top of the sidebar's own card.
//
// A trust realm is a whole logical copy of this service (common/realms.js), and
// every page here shows exactly one of them — the one whose prefix the request
// arrived under. That is not something a reader can see from the content: an
// empty tokens table looks the same in a realm that has issued nothing as it
// does in a service that has issued nothing, and /admin/config in a realm both
// reads and WRITES that realm. So the realm is named on every page.
//
// It is INSIDE the nav card and above the sections rather than in a card of its
// own. It was its own card, and that was one surface too many in a column whose
// whole job is to be one list: a reader looking for where they are should find
// it at the top of the thing they are already reading.
//
// **IT IS A FORM AND NOT AN onchange, BECAUSE THIS CONSOLE RUNS NO SCRIPT.**
// `script-src 'none'` (common/app.js) is what makes the whole js/reflected-xss
// family moot here rather than merely unlikely, and a select that navigated on
// change would need an inline handler the browser would refuse to run — so the
// control would silently do nothing. A select and a button is the same shape
// every filter on this console already uses.
//
// **THE ACTION IS AN ABSOLUTE URL, and that is required rather than tidy.**
// app.js rewrites every root-relative `href`, `action` and `src` in an HTML
// response to carry the current realm's prefix, which is what makes this file's
// several hundred hand-written links work inside a realm without one of them
// being edited — and it is exactly wrong for the one control whose job is to
// LEAVE the current realm. An absolute URL names a host, so it passes through
// untouched, and the route it reaches is the one at the root.
//
// It draws NOTHING AT ALL when no realm is defined. This console had no such
// control before realms existed and a service that is not using them should not
// grow one — the row would be a permanent "default", which is a control that
// only ever says the same thing.
// ---------------------------------------------------------------------------
const REALM_SWITCH_PATH = '/admin/realm-switch';

// WHERE THE SIGN OUT BUTTON POSTS. Written once and read in three places — the
// control in the shell, the exemption in the gate, and the route itself — for
// the reason `/admin/callback` is not: that path is a REGISTERED redirect URI
// and is written down in `oidc_rp.js` as well, so it is one string in two
// modules whether anybody likes it or not. This one belongs to this file alone,
// and a gate exemption naming a path the route no longer serves would be a
// sign-out that required the write role again, silently.
const SIGNOUT_PATH = '/admin/signout';

// WHERE THE PERSON READING THIS CONSOLE FINDS THEIR OWN ACCOUNT (2026-09-10).
//
// The user portal is the other surface this service hosts, and it is the one
// place an administrator changes their OWN password, enrols their OWN
// authenticator app or removes their OWN security key. Until now the console
// named it in prose on a page or two and linked it nowhere in its shell, so
// the way there was to know the path.
//
// **IT IS THE DEFAULT REALM'S PORTAL AND NOT THE ONE BEING READ, WHICH IS THE
// WHOLE OF THE DECISION HERE.** The console's session is the DEFAULT realm's
// whichever realm the page was reached in — see `consoleRpSession()` — while
// `/portal` runs in the AMBIENT one. So a link to `/portal` from a page read at
// `/realm/acme/admin` would be rewritten to `/realm/acme/portal` by app.js's
// realm rewrite and would land this person in a portal where they are nobody:
// the flow would run in `acme`, meet no sign-on session, and ask them to sign
// in again — as an account in another realm that has nothing to do with the
// one they hold.
//
// `realmRoot()` is what takes the prefix back off, and the result is an
// ABSOLUTE URL rather than a root-relative one. That is not cosmetic either:
// the rewrite matches `href="/` and leaves `https://…` alone, so an absolute
// URL is the one form that cannot be prefixed again on the way out.
//
// **SINCE #32 THAT REALM IS THE ONE THE PERSON SIGNED IN THROUGH** (fixed
// 2026-09-16). A realm's own administrator runs the console's flow in their
// realm, so their sign-on session — and their account — is there, and the
// default realm's portal was one where they were nobody: the very outcome
// the paragraph above describes. The link now goes to the portal of the
// gate's `identityRealm`, which is the default realm for a service
// administrator and so the same link as before for them.
const PORTAL_PATH = '/portal';

// ---------------------------------------------------------------------------
// THE SAME IDEA FOR A COLUMN OF THEM, AND WHY IT IS NOT shortened().
//
// shortened() above is for ONE identifier in a narrow column — a jti, eighteen
// characters and a native tooltip — and it has been right for the tokens page
// for as long as that page has existed. This is for the directory dumps, where
// the shape of the problem is different in three ways that together make a
// second function cheaper than a mode flag on the first:
//
//   * THERE ARE HUNDREDS OF THEM IN ONE CELL. `/admin/ldap/directory` prints
//     EVERY attribute of every entry, and an entry that has authenticated
//     over TLS carries a certificate subject, a serial, two thumbprints and a
//     DN — none under forty characters, several over two hundred. Wrapping
//     them (which is what the cell did before) made rows four and five lines
//     deep, so a page of fifty entries was a mile long and unreadable; NOT
//     wrapping them pushed the table out past the white card, which is the
//     complaint this was written for. Cutting them is the only answer that
//     leaves a table shaped like a table.
//   * THE VALUE IS THE POINT, so it must be recoverable. A shortened DN that
//     cannot be read in full is a dump that has quietly stopped being a dump.
//   * IT MUST BE COPYABLE, which a `title` attribute is not. Somebody hovering
//     `oauthClientSecret` here is going to paste it into a client's
//     configuration, and a native tooltip cannot be selected. So the full
//     value is a real element — see `.trunc` in page() — that the pointer can
//     move into, with `user-select:all` on it so one click takes the whole
//     thing.
//
// The `title` is set as well and is not redundant: it is what a keyboard user
// and most screen readers get, and it is what a browser with the popup
// scrolled off the edge of the window still shows. Nothing here is said only
// in a tooltip — the rule tip() states — because the full value is in the
// document twice over.
//
// `keep` is a CHARACTER count and not a width, deliberately. These cells are
// monospace and the values are opaque, so characters are the honest measure;
// a CSS width would cut mid-glyph at whatever the browser's font happened to
// be and would give the reader no idea how much was missing. The count in the
// hint is the other half of that: "218 characters" tells somebody at a glance
// whether they are looking at a thumbprint or a whole certificate.
// ---------------------------------------------------------------------------
const CLIP_CHARS = 46;

// The legend for the above, on the page, because a reader cannot see the
// comment this file opens the section with and a table whose columns shift
// meaning between rows has to say so where the rows are.
const COLUMN_LEGEND =
  '<table><tr><th>Column</th><th>A JWT</th><th>A SAML assertion</th><th>A ' +
  'Kerberos ticket</th></tr><tr><td>Contents</td><td>every credential the ' +
  'reply carried, in the order they were minted — one row, however many that ' +
  'is</td><td colspan="2">one credential: these protocols issue one thing ' +
  'per act, so the cell is the kind and nothing ' +
  'else</td></tr><tr><td>State</td><td>the state every member shares, or ' +
  '<em>mixed</em> when they differ — which they usually do, since an access ' +
  'token and the refresh token beside it have very different lifetimes. ' +
  'Hover for the breakdown</td><td colspan="2">the one credential\'s own ' +
  'state</td></tr><tr><td>Expires</td><td>the first member to go, then the ' +
  'last — a set comes apart before it is finished, and the earlier instant ' +
  'is the one a refused call is about</td><td colspan="2">its ' +
  '<code>NotOnOrAfter</code> / its ' +
  '<code>endtime</code></td></tr><tr><td>User</td><td><code>username</code>, ' +
  'as typed at the sign-in screen</td><td colspan="2">nothing: each of these ' +
  'has one name, and it is in ' +
  'Subject</td></tr><tr><td>Subject</td><td><code>sub</code></td><td>the ' +
  '<code>NameID</code></td><td>the client principal, ' +
  '<code>name@REALM</code></td></tr><tr><td>Client, audience or ' +
  'service</td><td><code>client_id</code> (or <code>azp</code>, or the ' +
  '<code>aud</code>)</td><td>the <code>AudienceRestriction</code>, or ' +
  '<em>unrestricted</em> when WS-Trust was given no ' +
  '<code>AppliesTo</code></td><td>the service the ticket is for; hover for ' +
  'the realm that issued it</td></tr><tr><td>Detail</td><td>the ACCESS ' +
  'TOKEN\'s <code>scope</code>. The refresh token beside it deliberately ' +
  'carries a different one — what was <em>authorized</em>, rather than what ' +
  'this token can do — so the two disagree by design, and the set page shows ' +
  'each. A scope that became the audience is not on either</td><td>whether ' +
  'the signature was written — an assertion that failed to sign still went ' +
  'out</td><td>the enc-type it was sealed with</td></tr><tr><td>Presented ' +
  'as</td><td>Bearer, or DPoP when <code>cnf.jkt</code> binds it to a ' +
  'key</td><td>bearer <code>SubjectConfirmation</code>; there is no ' +
  'holder-of-key form here</td><td>in a TGS-REQ (a TGT) or an AP-REQ (a ' +
  'service ticket)</td></tr><tr><td>jti, ID or set</td><td>a set of one ' +
  'shows the <code>jti</code> and opens that credential\'s lineage; a group ' +
  'shows the <em>set id</em> and opens the set. The set id is this ' +
  'service\'s own handle on a reply — it is in no token, no client ever sees ' +
  'it, and it is not a claim</td><td>the <code>ID</code> / ' +
  '<code>AssertionID</code></td><td>none exists — a ticket has no identifier ' +
  'to quote, and the KDC keeps no handle on one</td></tr><tr><td>the ' +
  'button</td><td>Revoke, or <strong>Revoke set</strong> on a group, which ' +
  'sends every revocable member through the same act one at a time. Nothing ' +
  'new is written: it is the same set of revoked <code>jti</code>s ' +
  '<code>/oauth2/revoke</code> writes to</td><td colspan="2">there is none, ' +
  'and there is nothing it could do — see above</td></tr></table>';
// ---------------------------------------------------------------------------
// EVERY QUERY STRING THIS CONSOLE READS, CHECKED IN ONE PLACE.
//
// **A PATH-SCOPED MIDDLEWARE RATHER THAN EIGHTY-SIX EDITS**, and it is the same
// argument the management API's registration wrapper makes one file along:
// there is no generic dispatcher here — every page reads its own `req.query` —
// so a check written inside them would be eighty-six checks and the
// eighty-seventh would be forgotten. Registered ABOVE the first route in this
// file, so it applies to all of them by construction.
//
// **WHAT IT ADDS IS BOUNDS AND SCALAR-NESS.** Control characters are already
// refused service-wide by `common/validation.js`'s guard, and `pagingOf()` has
// always defended `page` and `per` itself — `parseInt`, `isFinite`, clamped to
// MAX_ROWS with a floor of one — so those are not the gap. The gap is that
// `q` was `String(req.query.q || '').trim()` with no cap at all, and it is fed
// to a substring walk over every entry in the directory on four pages. A
// megabyte of `q` is not a search, and nothing before this said so.
//
// **IT VALIDATES AND DOES NOT SUBSTITUTE.** `req.query` is left exactly as
// express built it: this answers 400 or calls `next()`, and never rewrites what
// the handlers read. Rewriting would be a second source of truth for the same
// request, and the failure would be a page reading a value the URL does not
// carry.
//
// **`looseObject`, SO AN UNDECLARED PARAMETER PASSES.** This console builds its
// own links and the realm layer rewrites them; a parameter this table has not
// heard of is far more likely to be a link somebody added than an attack, and
// stripping or refusing it would break a page for no gain. What it is NOT is
// unchecked — the service-wide guard has already been over every value.
// ---------------------------------------------------------------------------
const CONSOLE_QUERY = vz.looseObject((function () {
  // **EVERY FIELD IS DECLARED REPEATABLE, AND THAT IS A FACT ABOUT BROWSERS
  // RATHER THAN A WEAKENING.**
  //
  // `common/validation.js` refuses a repeated parameter everywhere else,
  // because a caller that sent `redirect_uri` twice has a bug or is probing.
  // **This console generates repeats ITSELF and cannot stop.** Several pages
  // carry a GET form — the row-count selector, the search box — and a browser
  // submitting one from a URL that already carries that parameter appends
  // rather than replaces. `sts_admin_console.js` walks every GET form on every
  // page and found it immediately: `/admin/realms?per=300&per=300`, produced
  // by pressing the console's own control twice.
  //
  // Refusing that would mean this console's own forms answering 400 on the
  // second press. So repeats are tolerated HERE and nowhere else, and what
  // this middleware is for is the other two things: **a BOUND on every value**,
  // and a **refusal of a nested object**, neither of which a browser produces
  // by accident.
  //
  // Nothing downstream changes: this middleware VALIDATES and never
  // substitutes, so `pagingOf()` and every page still read exactly what express
  // built — including the array, which `String(query.per)` has always turned
  // into "300,300" and `parseInt` into 300.
  const bounded = function (max) {
    log.debug("Entering bounded().");
    log.debug("Leaving bounded().");
    return vt.repeatable(vz.string().max(max)).optional();
  };
  return {
    // The search box, on four pages, and the reason this middleware exists: it
    // was `String(req.query.q || '').trim()` with no cap, fed to a substring
    // walk over every entry in the directory.
    q: bounded(256),

    // Paging. `pagingOf()` already parses and clamps these — parseInt,
    // isFinite, MAX_ROWS, a floor of one — so the cap here only stops a value
    // long enough to be worth parsing at all.
    page: bounded(16),
    per: bounded(16),

    // Which rendering is being asked for; compared against 'json' and 'svg' at
    // the call sites, so anything else already falls through to HTML.
    format: bounded(16),

    // The identifiers a drill-down page is reached by: a name in the directory,
    // a realm id, a credential handle. None of them is free text.
    user: bounded(validation.CAP.NAME),
    account: bounded(validation.CAP.NAME),
    group: bounded(validation.CAP.NAME),
    role: bounded(validation.CAP.NAME),
    member: bounded(validation.CAP.NAME),
    id: bounded(validation.CAP.IDENTIFIER),
    entry: bounded(validation.CAP.IDENTIFIER),
    agent: bounded(validation.CAP.IDENTIFIER),
    application: bounded(validation.CAP.IDENTIFIER),
    realm: bounded(validation.CAP.IDENTIFIER),
    relationship: bounded(validation.CAP.IDENTIFIER),
    profile: bounded(validation.CAP.IDENTIFIER),
    session: bounded(validation.CAP.IDENTIFIER),
    chain: bounded(validation.CAP.TOKEN),
    sp: bounded(validation.CAP.URI),
    rp: bounded(validation.CAP.URI),
    to: bounded(validation.CAP.URI),
    origin: bounded(validation.CAP.URI),
    kind: bounded(64),
    family: bounded(64),
    protocol: bounded(64),
    reset: bounded(64),

    // Put in the URL by this console's OWN redirects and read straight back
    // onto the page. Every call site already slices them to 500; this says so
    // where a reader will see it and refuses the megabyte version before any
    // page is built. They are escaped through `esc()` and this console is
    // `script-src 'none'`, so the bound is depth rather than the fix.
    notice: bounded(500),
    error: bounded(500)
  };
})());

// ---------------------------------------------------------------------------
// BOTH CHOOSERS ARE A SEARCH NOW, AND WHAT DECIDES THE SHAPE IS THE ONE THING
// THIS CONSOLE CANNOT DO.
//
// Each of them was a `<select>` holding EVERY entry. That is fine at thirty and
// it is what a register looks like after an afternoon; it is not what one looks
// like after a week, and the two things a reader actually does with a long list
// — type the first few letters of a name, or read the handful that match — are
// the two a native select does worst. It also spends the same screen whether it
// holds three names or three hundred, which is the complaint that started this.
//
// **THERE IS NO SCRIPT AND THERE MUST NOT BE ONE**, which is why this is not a
// type-ahead. `script-src 'none'` holds over the whole service (common/app.js)
// and the parent suite asserts it against this console's live headers
// (tests/vendored/admin_api.js) — so there is no keystroke handler, no fetch
// and no debounce to build one out of, and anything written as though there
// were would be a control that silently does nothing rather than one that half
// works. What a browser gives for free is a GET form submitted by the Enter
// key: type, press Enter, and the page comes back around the matches. The cost
// is a round trip per attempt instead of per keystroke, and the reader keeps
// typing until the list is what they wanted, which is the same loop.
//
// **A RESULT IS A LINK AND THE LINK IS THE SELECTION.** A select needed a
// button beside it because a `<select>` chooses nothing until something submits
// the form it is in. A list of matches needs none: clicking a row IS choosing
// that application or that person, which is one click where there were two.
//
// **TWENTY AT A TIME, IN A PANE THAT SCROLLS.** A wall of matching links would
// be the select's own fault with the browser's scrolling taken away, so the
// pane has a fixed maximum height and a scrollbar of its own: the control is
// the same size showing one match or twenty. The line under it says how many
// matched and offers the next twenty, because a list that silently stops at
// twenty is one that has told the reader the twenty-first does not exist.
//
// **AN EMPTY BOX MATCHES EVERYTHING**, so this is useful before anybody types —
// the first twenty with the total beside them, which is what the select showed
// in one line rather than in a column.
// ---------------------------------------------------------------------------
// The number itself lives in admin-core/admin_views.ts since 2026-09-13, so
// that /admin-api/rbac's `candidates` pages by the same twenty the pane does.
const CHOOSER_HITS = adminViews.CHOOSER_HITS;

// ---------------------------------------------------------------------------
// GET /admin/users — who this service has authenticated, and one of them in
// full.
//
// One route and two pages: without `?user=` it is the list, with it the
// drill-down. That is deliberate rather than lazy. A path parameter would have
// been the obvious shape and cannot be used here, because the identities this
// service holds contain the characters a path is made of — a Kerberos service
// principal is `HTTP/host` and a subject is a `urn:` — so
// `/admin/users/HTTP/web.example.com` is a two-segment path naming nobody. A
// query parameter carries any of them unaltered and keeps the console to one
// row in the metrics table for the whole feature.
//
// **What "a user" means here, and the two ways it can surprise you.** The list
// is built in admin_stats.js from the authentications this service recorded,
// PLUS every subject that appears on something it issued — so a subject that
// never authenticated here (an exchanged foreign token, a WS-Trust OnBehalfOf,
// a Kerberos S4U2Self) is listed and marked as such, rather than being absent
// from a page whose whole job is to answer "who has this service seen". And the
// identity is keyed on the local name, so one row covers `alice`,
// `urn:uuid:<entryUUID>` and `alice@STS.MOCK`, which is right on a mock where
// the name you type is who you are everywhere — and would be wrong on a real
// system with two realms. The Realms column exists so that collapse is visible.
// ---------------------------------------------------------------------------

// ---------------------------------------------------------------------------
// THE EMBEDDED DIRECTORY'S VIEW OF ONE USER, AND WHY IT ARRIVES THROUGH A SLOT.
//
// ldap_server.js grows an entry under `ou=users` for everybody who
// authenticates anywhere in this service, so by the time a person has a page
// here they usually have a directory object too — and showing it beside their
// tokens is the point: the two are the same authentication seen from two sides.
//
// This module does NOT require ldap_server.js to get at it, and the reason is
// the route order as much as a cycle (`ldap_server.js` requires this file to
// fill its slots). `common/protocol_stack.ts` requires this file at 18 and
// `ldap/ldap_server.js` at 21 (rule 6), so a require from here would pull the
// directory's routes into the router AHEAD of the console's — and
// /admin/sts-metadata is built by walking that router. So the direction is
// inverted the same way admin_stats.js's user observer is: this file offers a
// slot, and ldap_server.js fills it at its own require time.
//
// It stays null when that module was never required. That is a real state — the
// directory is the newest thing here and a build without it must still render
// this page — so the section says "no directory is loaded" rather than being
// absent, which would read as "this user has no entry".
// ---------------------------------------------------------------------------
// ---------------------------------------------------------------------------
// The two things every LDAP section of this console draws, in one place each
// because there are now three such sections — a user's entry, the groups list
// and one group in full.
// ---------------------------------------------------------------------------

// What a section is showing, in the four grammatical forms the listener warning
// needs. A warning about sockets has to name the thing the reader came for —
// the whole point of it is the gap between "this service holds it" and "an LDAP
// client can fetch it" — and that sentence needs a noun that agrees with
// itself.
const ENTRY_SUBJECT = { upper: 'The entry below', lower: 'the entry below',
                        verb: 'is', pronoun: 'it' };
const GROUPS_SUBJECT = { upper: 'The groups below', lower: 'the groups below',
                         verb: 'are', pronoun: 'they' };
const GROUP_SUBJECT = { upper: 'The group below', lower: 'the group below',
                        verb: 'is', pronoun: 'it' };

let directoryReader = null;

// The groups pages read through their own slot, installed the same way and by
// the same module, for the reason above: this file must not require
// ldap_server.js.
//
// Two slots rather than one object with two functions, because they were added
// at different times and a single slot would mean an ldap_server.js that filled
// it with only one of them silently disabling the other. A missing slot is
// already handled — each page says "no directory is loaded" — and two of those
// are cheaper than one half-filled reader nothing reports.
let groupReader = null;

// The SPIFFE listeners, through a slot for the reason given beside the
// requires above: this file must not require spiffe_server.js. What it holds
// is that module's `bindings()` and its bundle path — two facts about
// SOCKETS, which neither this page nor /admin/sts-metadata can see any other
// way, so a page without this reports "nothing bound" and cannot tell that
// from a listener whose port was taken.
let spiffeReader = null;


// ---------------------------------------------------------------------------
// AND THE SIXTH, WHICH IS THE PROTOCOL-INDEPENDENT LOGOUT.
//
// Same direction and both halves of rule 3e's test at once — see the note
// beside the requires at the top of this file. `logout.ts` requires
// `ldap_server.js`, which requires THIS module, so a require in the obvious
// direction closes a cycle; and it would drag every `/ldap` route into the
// router ahead of the console's own.
//
// It holds THREE things and they are one object, validated when it is
// installed: `FAMILIES` (the prose about what a logout reaches, so this page
// does not carry a second copy that goes stale the day a family is added),
// `inventoryFor` (what is live for one identity) and `terminate` (ending it).
// One object rather than three slots for the reason the directory writer's note
// gives one screen up: a module that filled a combined slot with only the
// readers would silently disable the action with nothing reporting it, so the
// object is checked whole and refused whole.
// ---------------------------------------------------------------------------
// THE SEVENTH SLOT, filled by `./crypto_metadata.js` at its own require time,
// and rule 3e's test answers yes in both directions at once.
//
//   * a require from `mgmt-api/admin_api.ts` (19) to that module (20a) would
//     MOVE ROUTES — `tls/tls_server.js`'s three, which it requires for the
//     server certificate, ahead of the management API's own routes and of
//     ldap, scim and spiffe. (Its own page moved too until #50's R1; since
//     then `common/protocol_stack.ts` registers that at 20a wherever the
//     module is loaded, but `tls_server.js` is still JavaScript.)
//   * a require from THIS file to it would CLOSE A CYCLE: it requires this one
//     for the shell, exactly as `../sts_metadata.js` does.
//
// So `/admin-api/crypto` reaches it the way `/admin-api/logout` reaches the
// sign-out inventory: through a function this console holds. What is carried is
// ONE function — the whole report — because the page and the API must not be
// able to disagree about what this service's cryptography is, which is the
// parity rule's entire subject.
//
// A build where nothing fills it draws no page here and answers the API with a
// 503 that says which module is missing. That is deliberate rather than a 404:
// a route that is registered and cannot answer is a different fact from a route
// that does not exist, and only one of them is a wiring mistake somebody can
// fix.
// ---------------------------------------------------------------------------
let cryptoReporter = null;

let logoutReader = null;

// ---------------------------------------------------------------------------
// THE NINTH SLOT: THE DIRECTORY PAGES' OWN VIEWS, FOR THE MANAGEMENT API —
// FIVE WHEN IT WAS WRITTEN, EIGHT SINCE 2026-09-05 (`DIRECTORY_PAGE_NAMES`).
//
// It is filled by `ldap/ldap_server.js` at its require time, and rule 3e's test
// answers yes both ways round.
//
//   * A require from THIS file to `ldap_server.js` would CLOSE A CYCLE — that
//     module requires this one for the shell and the gate — and it would drag
//     every `/ldap`, `/admin/ldap/*` and (through it) every `/scim` and
//     `/spiffe` route into the router ahead of this console's own.
//   * A require from `mgmt-api/admin_api.ts` (19) to it would MOVE ROUTES for
//     the same reason: that module sits two positions above `ldap_server.js`
//     (21) precisely so that the management API's routes are registered first.
//
// WHY THE API NEEDS IT AT ALL is rule 7, the parity: every page of this console
// has an operation on `/admin-api` that mirrors it, and the first five became
// pages of this console on 2026-09-01. The API answers them by calling exactly
// the function that draws them, so the page and the operation cannot come to
// disagree about what is in the directory.
//
// IT IS ONE SLOT CARRYING EVERY VIEW AND IT IS VALIDATED WHOLE, for the reason
// `setLogoutReader()` gives: a filler that installed all but one of them would
// leave one operation answering "no directory is loaded" on a service whose
// directory is plainly loaded, which is worse than all of them saying so. Each
// view takes the request and returns `{ title, inner, json }` — the same shape
// every view function in this file returns, so `respond()` and the API read
// them the same way.
// ---------------------------------------------------------------------------

// ---------------------------------------------------------------------------
// THE TENTH SLOT, `setXacmlPages()`, FILLED BY `xacml/xacml_admin.ts`, AND IT
// IS THE SIXTH TO PASS RULE 3e's TEST BOTH WAYS ROUND.
//
// A require from THIS file to `xacml/xacml_admin.ts` would CLOSE A CYCLE — it
// requires this one for the shell, the settings block, the gate and the action
// responder. And a require from `mgmt-api/admin_api.ts` (19) to it would MOVE
// ROUTES: every `/xacml` endpoint and all six `/admin/xacml*` pages would be
// registered ahead of the management API's own, and ahead of ldap, scim and
// spiffe. So the slot is the only arrangement left, and it is the same one SSF
// and the directory pages already take.
//
// **THE ROUTE HALF OF THAT WAS WRITTEN WHILE A REQUIRE WAS A REGISTRATION.**
// Since #50's R1 (2026-09-16) `common/protocol_stack.ts` registers the XACML
// family's routes at 23c with its own `register()` calls, wherever the
// modules were first loaded, so a require from `admin_api.ts` would no longer
// move them. It would still load the family — and arm `issuance_gate.js`
// through `xacml_role_pep.ts` — at 19 instead of 23c, which is a load-time
// effect out of order, and the cycle half stands; so the slot stays.
//
// IT CARRIES SEVEN FUNCTIONS — `XACML_PAGE_PARTS` below, `monitor` being the
// seventh (2026-09-06) — AND IS VALIDATED WHOLE, for `setLogoutReader()`'s
// reason: a filler that installed the views without the action would leave
// `/admin-api` able to LIST policies and unable to change any of them, which
// reads as a management API that is working and is not. It was four until
// phase five, which added TWO — `peps` for the remote PEP register, and
// `decide`, which `/admin/xacml/decide` had needed since phase three and had
// silently gone without (rule 7; `tests/vendored/admin_api.js` reads the
// console's OWN page list rather than a list in the test, which is why the
// omission was catchable at all). Both went into THIS set rather than into
// slots of their own because it is the same module filling it and a second
// slot would have been a second indirection for no second reason (rule 3e).
//
// `actionNames` is one more, outside the validated list (`xacmlActionNames()`
// below answers `[]` without it), and is not decoration. The refusal sentence
// for an unknown action names every action and counts them, and that sentence
// is READ by `tests/vendored/admin_api.js` — so the API has to be able to ask
// the module what its actions are rather than keeping a second list that
// could disagree.
// ---------------------------------------------------------------------------
const XACML_PAGE_PARTS = ['overview', 'policies', 'editor', 'peps', 'decide',
                          'monitor', 'action'];

let xacmlPages = null;

// EIGHT SINCE 2026-09-05. The three added that day — `roles`, `policies` and
// `peps` — are the containers whose owning modules published a SCHEMA that
// nothing drew: `common/roles.js`, `xacml/xacml_store.ts` and
// `xacml/xacml_pep_registry.ts` each carried the same comment claiming a page
// under `/admin/ldap/*`, and none of the three had one. The list is checked
// WHOLE below, so adding a name here without adding the view is a refused
// install rather than one operation answering as though no directory were
// loaded.
const DIRECTORY_PAGE_NAMES = ['service', 'directory', 'applications',
                              'federations', 'spiffe', 'roles', 'policies',
                              'peps'];

let directoryPages = null;

// The SCIM slot (the FOURTH, and the third of the ones that READ). Same
// direction and same reason as the directory readers above, and it passes
// rule 3e's test for the same two grounds: requiring scim.js from here would
// pull every /scim route — and, since that module requires ldap_server.js,
// every /ldap route too — into the express router ahead of the console's own,
// and /admin/sts-metadata is built by walking that router.
//
// What it holds is scim.js's description(), which is the same object GET
// /scim?format=json answers with. So /admin/scim shows what that page shows
// rather than a second account of the same feature: the endpoint list, what
// SCIM deliberately does not do, and the reachable negatives are written ONCE,
// in the module that implements them, and this page renders them. A console
// page carrying its own copy of what "active: false" does would be the copy
// that stops being true — as "deactivates nobody" did on 2026-09-17.
let scimReader = null;

// And the one that WRITES, which is the third of these and the only one of the
// three that changes anything. Same direction and same reason as the two above
// — this file must not require ldap_server.js — and a third slot rather than a
// member on one of theirs, for the reason stated there: a module that filled a
// combined slot with only the readers would silently disable creation with
// nothing reporting it.
//
// It holds createUser() and NOT a way to write an arbitrary entry. The console
// is not a second definition of what a user is: what a name may be, and the
// refusal of one that is already here, live in that function so that this form,
// the management API and an `ldapadd` cannot come to disagree about the same
// name.
let directoryWriter = null;

// ---------------------------------------------------------------------------
// AND THE GROUP HALF (2026-09-06) — THE TWELFTH SLOT ON THIS FILE.
//
// It exists because /admin/groups was a READ and nothing else. This console
// could report a dangling member, a claimed membership and the two groups that
// decide who may use it, and could not create the group any of that is about:
// the only two doors onto a group in this directory were an `ldapadd` on the
// raw socket and `POST /scim/v2/Groups`. Rule 7 then owed the management API
// nothing, because there was no control to mirror — which is how a hole stays
// open in a repository that checks console/API parity on every run.
//
// SAME DIRECTION AND SAME REASON AS THE THREE ABOVE: this file must not require
// ldap_server.js. It passes rule 3e's test both ways round — a require from
// here would close a cycle (that module requires this one for the page shell
// and the gate), and a require from mgmt-api/admin_api.ts (19) to it would MOVE
// ROUTES, since ldap_server.js sits at 21 precisely so the management API's own
// are registered first.
//
// IT CARRIES BOTH FUNCTIONS AND IS VALIDATED WHOLE. A filler that installed
// `createGroup` alone would leave the Add member button answering "no directory
// is loaded" on a service whose directory plainly is — which is
// `setLogoutReader()`'s argument, and the reason that slot refuses a partial
// object rather than half-installing one.
//
// AND IT HOLDS NEITHER A DELETE NOR A REMOVE, deliberately. Taking a member out
// of a group is `admin_rbac.js`'s revoke() for the two console roles and an
// `ldapmodify` or a SCIM PATCH for every other group; deleting a group is a
// SCIM DELETE or an `ldapdelete`. Those doors exist and work. What did not
// exist anywhere but SCIM and the socket was CREATION, and a slot that grew the
// operations nobody asked for would be four more things for the two doors onto
// it to disagree about.
// ---------------------------------------------------------------------------
const GROUP_WRITER_MEMBERS = ['createGroup', 'addGroupMember'];
let groupWriter = null;

// The application section's sentences, said about a person: who holds the
// private key is the one thing that changes.
const PERSON_KEY_SOURCE_SENTENCES = {
  'issued': 'Issued here &mdash; generated by this service and signed by ' +
            'this realm&rsquo;s Issuing CA. The private key is sealed on ' +
            'this entry.',
  'uploaded-realm-ca': 'Uploaded &mdash; a certificate this realm&rsquo;s ' +
            'own certificate authority issued to this person. They hold the ' +
            'private key; this service does not.',
  'uploaded-external-ca': 'Uploaded &mdash; a certificate from an external ' +
            'certificate authority, accepted with its whole chain up to a ' +
            'self-signed root, every link verified. They hold the private ' +
            'key; this service does not.',
  'unrecorded': 'A certificate with no recorded source and no private key ' +
            'here &mdash; written by hand, or by a build older than the ' +
            'provenance attribute.'
};

// Why this entry counted as a group, in words. The rule is ldap_server.js's;
// this is only its three values spelled out, and it is on the page because
// "developers is a group" is uninteresting next to "this entry is a group
// because somebody put it under ou=groups and it carries no group objectClass
// at all".
const GROUP_RULES = {
  both: { label: 'placement + objectClass',
          title: 'It is under ou=groups AND carries a group objectClass. ' +
                 'This is what a group written the conventional way looks ' +
                 'like.' },
  placement: { label: 'placement only',
               title: 'It is under ou=groups but carries no group ' +
                      'objectClass. This directory is schemaless, so nothing ' +
                      'refused the add — it is listed here because of where ' +
                      'it sits.' },
  objectClass: { label: 'objectClass only',
                 title: 'It carries a group objectClass but sits outside ' +
                        'ou=groups. Nothing here requires a group to live ' +
                        'under the groups container.' }
};

// What this directory's groups are and are not, said on both pages. Repeated
// rather than shown once on the list, for the reason the open-console banner is
// repeated: the page somebody arrives at directly is exactly the one that needs
// it. It used to say the two halves as one sentence — nothing reads a group AND
// no token carries one — and the second half stopped being true when the groups
// claim was added. They are split now, in that order, because the one a reader
// most needs is still the first: CARRYING a fact and ACTING on it are different
// claims, and this is the same line this service already draws between an
// identity being recorded and an identity being authenticated.
let GROUPS_CAVEAT: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  GROUPS_CAVEAT =
    instance.note('<strong>A group here grants nothing, with exactly ' +
    'two exceptions and they are named below.</strong> No <em>endpoint</em> ' +
    'in this service checks a group, and nothing in any protocol decides ' +
    'anything on one. Adding somebody to <code>cn=directory-admins</code> ' +
    'changes what a directory client sees, and what a token <em>says</em>, ' +
    'and changes nothing at all about what that token can DO &mdash; on a ' +
    'service that ' +
    'authenticates nobody, it could hardly be otherwise.') +
    // THE EXCEPTION, said HERE and not only on the page that owns it. A reader
    // meeting this caveat on the groups page and then finding cn=admin-write in
    // the table above it would be entitled to conclude that one of the two was
    // lying. The general claim is still the one that matters — it is true of
    // every group but these two, and true of these two everywhere except one
    // console — so it is qualified rather than dropped.
    instance.note('<strong>The two exceptions are <code>' +
    instance.esc(config.value('admin.readGroup')) + '</code> and <code>' +
    instance.esc(config.value('admin.writeGroup')) + '</code>, which ' +
    'decide who may use THIS CONSOLE</strong> &mdash; see <a ' +
    'href="/admin/rbac">Admin roles</a>, where they are granted and taken ' +
    'away. They are ordinary groups and appear in the table above like any ' +
    'other, deliberately: the alternative was a membership store of the ' +
    'console\'s own that an <code>ldapmodify</code> could not see. Even ' +
    'those two grant nothing outside <code>/admin</code> &mdash; no token, ' +
    'assertion, ticket, PAC or credential is changed by being in one, and ' +
    'every protocol endpoint answers a member exactly as it answers anybody ' +
    'else.') +
    instance.note('<strong>A token can now carry one.</strong> With ' +
    '<code>groups.claim</code> on &mdash; it is on by default &mdash; every ' +
    'OAuth 2.0 access token, OIDC ID Token, SAML 2.0 assertion and SAML 1.1 ' +
    'assertion this service issues carries a claim naming the groups its ' +
    'subject is in, read from these entries at the moment it is minted. ' +
    'Somebody in no group gets no claim at all rather than an empty list. ' +
    'What it is called, whether each value is a <code>cn</code> or a whole ' +
    'DN, and whether a person\'s own <code>memberOf</code> counts are the ' +
    'four settings at the foot of this page; <a href="/admin/claims">the ' +
    'claims page</a> shows what it would say about one person. No Kerberos ' +
    'PAC and no ' +
    'WS-Federation-specific token carries a group either way.');
});

let GROUPS_LINKS: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  GROUPS_LINKS =
    instance.note('<a href="/admin/ldap/service">What this directory ' +
    'is</a> &middot; <a href="/admin/ldap/directory">every entry in it</a> ' +
    '&middot; <a href="/admin/ldap/directory?format=json">the same as ' +
    'JSON</a> &middot; <a href="/admin/users">the people who have ' +
    'authenticated ' +
    'here</a>.');
});

// ---------------------------------------------------------------------------
// GET /admin/applications
//
// The other side of /admin/users. That page lists every identity that has
// authenticated here; this one lists what they authenticated TO — every OAuth
// client, OpenID Connect relying party, SAML 2.0 or 1.1 service provider,
// WS-Federation application, WS-Trust relying party, OpenID4VP verifier and
// Kerberos service this instance has been asked about.
//
// **It reads the directory and holds nothing.** The registry IS the
// ou=applications container (see applications.js), so this page cannot come to
// disagree with what an LDAP client sees — there is no second copy for it to
// disagree with. An `ldapmodify` made a second ago shows up on the next
// refresh.
//
// **It had no form when it was written, and that was argued as a decision.**
// The write paths into this registry were the protocol endpoints and LDAP
// itself: a client is recorded because it turned up, and an operator changed
// one with `ldapmodify` — which is what makes the directory the source of
// truth rather than a display of one. That argument was about a second STORE,
// and it survived the reversal: the page now carries an *Add an application*
// row, per-entry actions (`APPLICATION_ACTIONS` in
// admin-core/admin_actions.ts, POSTed to the handler below)
// and `/admin/applications/new`, every one of them a DOOR onto the same
// entries through `applications.js` and mirrored on
// `POST /admin-api/applications/{action}` (rule 7).
// ---------------------------------------------------------------------------
let APPLICATIONS_CAVEAT: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  APPLICATIONS_CAVEAT =
    instance.note('<strong>An entry here grants nothing.</strong> Being ' +
    'in this registry does not let an application do anything it could not ' +
    'do before &mdash; this service issues a token to any client_id that ' +
    'asks. The one place it is READ is RFC 9700 mode ' +
    '(<code>oauth2.rfc9700</code>), which matches a redirect_uri against ' +
    '<code>oauthRedirectUri</code> by exact string comparison, decides ' +
    'public-versus-confidential from ' +
    '<code>oauthTokenEndpointAuthMethod</code>, and checks ' +
    '<code>oauthClientSecret</code> at the token endpoint. With that mode ' +
    'off, ' +
    'these entries are a record and nothing more.') +
    instance.note('<strong>Two attributes hold credentials in the ' +
    'clear</strong> &mdash; <code>oauthClientSecret</code> and ' +
    '<code>appRegistrationAccessToken</code> &mdash; in a directory where ' +
    'every bind succeeds. That is the same decision ' +
    '<code>/krb5/principals</code> makes about the Kerberos passwords and it ' +
    'costs more here than it does there: in RFC 9700 mode that secret is ' +
    'checked, so anyone who can read this directory can authenticate as that ' +
    'client. They are never written to the audit log.');
});

const APPLICATIONS_LINKS =
  '<p class="sub"><a href="/admin/ldap/applications">the same registry as ' +
  'the directory sees it, with the schema</a> &middot; <a ' +
  'href="/admin/users">the identities on the other side of these</a> ' +
  '&middot; <a href="/admin/ldap/directory">every entry in the ' +
  'directory</a></p>';

// What each recorded provenance of a managed key pair means, in the words the
// section draws. KEY_SOURCES in `common/applications.js` is the vocabulary;
// `unrecorded` is this page's own word for an entry that holds a certificate
// and no provenance, which only a hand edit or an older build produces.
const KEY_SOURCE_SENTENCES = {
  'issued': 'Issued here &mdash; generated by this service and signed by ' +
            'this realm&rsquo;s Issuing CA. The private key is on this entry.',
  'uploaded-realm-ca': 'Uploaded &mdash; a certificate this realm&rsquo;s ' +
            'own certificate authority issued. The application holds the ' +
            'private key; this service does not.',
  'uploaded-external-ca': 'Uploaded &mdash; a certificate from an external ' +
            'certificate authority, accepted with its whole chain up to a ' +
            'self-signed root, every link verified. The application holds ' +
            'the private key; this service does not.',
  'unrecorded': 'A certificate with no recorded source and no private key ' +
            'here &mdash; written by hand, or by a build older than the ' +
            'provenance attribute.'
};

// WHICH FAMILIES EACH OVERRIDABLE SETTING BELONGS TO, so the section can be
// grouped and made conditional. Keyed by the setting's PREFIX, which is the one
// thing a setting key reliably carries — and the group claim is the case that
// makes this a table rather than a split on the dot: `groups.*` reaches an
// access token, an ID Token and both SAML assertions, so it belongs to five
// families at once and appears whenever any of them is ticked.
const OVERRIDE_SECTIONS = [
  { prefix: 'oauth2.', heading: 'What OAuth 2.0 / OIDC issues for this client',
    families: ['oauth2', 'oidc'],
    intro: 'The three token lifetimes, the refresh idle timeout, whether ' +
           'signing out revokes this client\'s refresh tokens, and whether ' +
           'an RFC 8693 token exchange it performs comes back with a refresh ' +
           'token. The idle timeout and the sign-out revocation are read ' +
           'only in RFC 9700 mode. The last is the ONE field on this whole ' +
           'form that is refused rather than merely inert when the family ' +
           'above is not ticked &mdash; it decides what the token endpoint ' +
           'does for a client_id, so an entry no token request can name ' +
           'would carry it looking like a policy in force.' },
  { prefix: 'saml2.', heading: 'What SAML 2.0 issues for this service provider',
    families: ['saml2'],
    intro: 'The assertion lifetime, the two signature switches, the default ' +
           'NameID format and the artifact lifetime.' },
  { prefix: 'saml11.', heading: 'What SAML 1.1 issues for this relying party',
    families: ['saml11'],
    intro: 'The same five for the older profiles. They are separate settings ' +
           'because 1.1 and 2.0 are separate implementations here, so an ' +
           'application declared for both carries two answers.' },
  { prefix: 'wsfed.', heading: 'What WS-Federation issues for this relying ' +
                               'party',
    families: ['wsfed'],
    intro: 'One setting: how long the SAML 1.1 assertion inside a sign-in ' +
           'response lasts. It also sets the wsu:Lifetime of the ' +
           'RequestSecurityTokenResponse around it, so the envelope and the ' +
           'assertion cannot disagree.' },
  { prefix: 'groups.', heading: 'The groups claim for this application',
    families: ['oauth2', 'oidc', 'saml2', 'saml11', 'wsfed'],
    intro: 'THE ONE GROUP HERE THAT IS NOT A PROTOCOL\'S. These four reach ' +
           'an access token, an ID Token, a SAML 2.0 assertion and a SAML ' +
           '1.1 one at once, so an application declared for two protocols ' +
           'gets the same claim name in both — which is what a claim mapping ' +
           'should do. The name is the one that differs in practice: ' +
           '<code>groups</code>, <code>roles</code> and ' +
           '<code>memberOf</code> are all ordinary.' }
];

// THE THREE ATTRIBUTES THAT SAY WHERE A SERVICE PROVIDER'S KEY COMES FROM.
//
// They are not setting overrides — nothing in config.js corresponds to them —
// so they are not in `overridableSettings()` and would otherwise appear on no
// form at all. They are conditional on SAML 2.0 like everything else in that
// family.
//
// `samlSpMetadata` is a TEXTAREA and the other two are inputs, which is the
// same shape rule declarationFieldRow() follows: a document is not something
// anybody types on one line, and offering a single-line box for one invites a
// paste that loses its newlines.
const SAML_KEY_SOURCE_FIELDS = [
  { attribute: 'samlSpMetadataUrl', label: 'Metadata URL',
    what: 'Where this service provider publishes its metadata. Nothing is ' +
          'fetched until you press Refresh on the entry — an assertion never ' +
          'waits on somebody else\'s web server.' },
  { attribute: 'samlEncryptionCertificate', label: 'Encryption certificate',
    what: 'The certificate an assertion is encrypted to, base64 or PEM. ' +
          'Consuming the metadata writes this; set it by hand for a service ' +
          'provider whose metadata cannot be reached. With none here a ' +
          'registered signing certificate is used, then (development only) ' +
          'the one a signed AuthnRequest carried.' },
  { attribute: 'samlSpMetadata', label: 'Metadata document', multi: true,
    what: 'The metadata itself. Pasted here, it is CONSUMED when the ' +
          'application is created — endpoints, signing and encryption ' +
          'certificates, NameIDFormats — exactly as a refresh would, which ' +
          'is the way to configure an air-gapped service provider, or one ' +
          'behind a proxy this service cannot dial.' }
];

// ---------------------------------------------------------------------------
// WHERE A SIGN-OUT GOES. Four families have one and the rest do not, and the
// absences are the interesting half — each is a fact about the protocol rather
// than a gap in this form.
// ---------------------------------------------------------------------------
let NEW_APPLICATION_LOGOUT_INTRO: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  NEW_APPLICATION_LOGOUT_INTRO =
    instance.note('Where this application is sent, or pinged, when a ' +
    'session it was part of ends. <strong>Every one of these takes MORE THAN ' +
    'ONE address, one per line</strong> — a service provider commonly has a ' +
    'different endpoint per binding, and an OAuth client registering several ' +
    'environments has a post-logout URI for each.') +
    instance.note('<strong>ONLY FOUR FAMILIES HAVE ONE, AND THE ' +
    'ABSENCES ARE NOT AN OVERSIGHT.</strong> SAML 1.1 is the one people look ' +
    'for: it has NO Single Logout at all &mdash; that arrived with SAML 2.0 ' +
    '&mdash; so a field here would be a box whose value nothing could ever ' +
    'read. WS-Trust issues a token and holds no session to end; Kerberos ' +
    'hands out a ticket this service cannot recall; and federation ' +
    'deliberately does not consume a partner\'s sign-out. The rule this form ' +
    'follows is the one the identifiers follow: a field is offered where an ' +
    'attribute exists to ' +
    'hold it, and nowhere else.') +
    instance.warn('<strong>These are DECLARED and, today, mostly not ' +
    'yet READ.</strong> <code>samlSingleLogoutService</code> is the ' +
    'exception and always has been &mdash; SAML 2.0 Single Logout really ' +
    'does send a LogoutRequest there. The OAuth post-logout URIs are matched ' +
    'by RP-Initiated Logout when a client supplies one, and ' +
    '<code>wsfedSignOutUri</code> is NEW and is not read by the sign-out ' +
    'yet: a <code>wsignoutcleanup1.0</code> ping goes to the ' +
    '<code>wreply</code> this service OBSERVED during sign-in, which is a ' +
    'different fact from the one an operator declares here. Storing it is ' +
    'this change; changing where a cleanup goes is a change to what the ' +
    'protocol does, and is deliberately ' +
    'separate.');
});

// ---------------------------------------------------------------------------
// WHERE A RECEIVER EXPECTS ITS EVENTS PUSHED. The `delivery` role of
// `applications.declarationAttributes()`, which `createApplication()` and
// `GET /admin-api/applications/new` both accepted while this form drew no field
// for it until 2026-09-12 — the one role of six the form had no section for.
// `tests/application_form_roles.js` is what stops a seventh going the same way.
// ---------------------------------------------------------------------------
let NEW_APPLICATION_DELIVERY_INTRO: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  NEW_APPLICATION_DELIVERY_INTRO =
    instance.note('Where this Shared Signals receiver expects its ' +
    'Security Event Tokens POSTed &mdash; an RFC 8935 push endpoint, one per ' +
    'line if it runs one per environment.') +
    instance.warn('<strong>This is a DECLARATION and nothing dials ' +
    'it.</strong> A push goes to the <code>endpoint_url</code> on the ' +
    'STREAM, which the receiver names when it creates one at ' +
    '<code>/ssf/stream</code>; this service will not take a URL to open a ' +
    'connection to from an application entry. What this field is for is ' +
    'writing down what the ' +
    'receiver is EXPECTED to use, beside everything else the application is.');
});

// ---------------------------------------------------------------------------
// WHICH SHARED SIGNALS EVENTS A RECEIVER MAY BE SENT (2026-09-12). One field,
// one family, and unlike the fields above it is ENFORCED.
// ---------------------------------------------------------------------------
let NEW_APPLICATION_EVENTS_INTRO: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  NEW_APPLICATION_EVENTS_INTRO =
    instance.note('Shared Signals has no separate CAEP or RISC checkbox ' +
    'because both are sent over it: <strong>CAEP</strong> is what happens to ' +
    'a SESSION, <strong>RISC</strong> what happens to an ACCOUNT. A receiver ' +
    'chooses event types itself, in the <code>events_requested</code> of the ' +
    'stream it creates at <code>/ssf/stream</code>. This field is where an ' +
    'operator LIMITS that choice for one application.') +
    instance.warn('<strong>Leave it empty and nothing is ' +
    'limited.</strong> Otherwise write one value per line: ' +
    '<code>caep</code>, <code>risc</code>, or an event type URI. A stream ' +
    'this application owns is agreed only those types, and every delivery ' +
    'checks again, so removing a value later stops existing streams ' +
    'receiving it. SSF\'s own ' +
    'verification and stream-updated events are always allowed.');
});

// ---------------------------------------------------------------------------
// THE CLIENT SECRET. The `secret` role — `oauthClientSecret` for the two OAuth
// families and, since GNAP arrived, `gnapSymmetricKey` too, though the intro
// below still speaks of OAuth only — and a warning that is the whole point of
// it.
// ---------------------------------------------------------------------------
let NEW_APPLICATION_SECRET_INTRO: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  NEW_APPLICATION_SECRET_INTRO =
    instance.note('The <code>client_secret</code> for the two OAuth ' +
    'families. Leave it empty and the entry simply has none, which is what a ' +
    'public client is; a client registering through <code>POST ' +
    '/oauth2/register</code> is minted one instead. <em>Generate ' +
    'Secret</em> mints one the same way and puts it in the box, keeping ' +
    'everything else on this page as you left it; with a secret, the create ' +
    'records <code>client_secret_basic</code>, and the token endpoint takes ' +
    'the secret by an <code>Authorization: Basic</code> header or a ' +
    '<code>client_secret</code> form parameter alike.') +
    instance.warn('<strong>IT IS STORED IN THE CLEAR, IN A DIRECTORY ' +
    'WHERE EVERY BIND SUCCEEDS.</strong> That is deliberate and it is the ' +
    'same decision <code>GET /krb5/principals</code> makes about the ' +
    'Kerberos passwords: a debugger whose accounts are unusable without ' +
    'reading the source is worse than one that says what they are. Anybody ' +
    'who can reach port 389 can read this value. It is never written to the ' +
    'audit log, which is the one place it is kept back.\n\nNothing checks it ' +
    'today outside RFC 9700 mode. A mode that enforces client authentication ' +
    'everywhere is the intended next step, and this field is what it will ' +
    'check against &mdash; so a secret typed here is worth setting now even ' +
    'though it is not yet a ' +
    'credential this service refuses anybody for.');
});

let NEW_APPLICATION_IDENTIFIERS_INTRO: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  NEW_APPLICATION_IDENTIFIERS_INTRO =
    instance.note('The name this application answers to in each family ' +
    'it speaks. They are all optional and all independent of the ' +
    '<em>Identifier</em> above &mdash; that one is the KEY this registry ' +
    'files the entry under, and these are what the protocols will present. ' +
    'For an ordinary OAuth client the two are the same string, which is what ' +
    'a protocol sighting writes anyway; they differ when one application ' +
    'answers to a client_id in one environment and another in the next, or ' +
    'when it is a SAML service provider whose entityID is a URN and whose ' +
    'entry you would ' +
    'rather file under a readable name.') +
    instance.note('<strong>Several families share a field where the ' +
    'specifications share the identifier.</strong> An OpenID Connect relying ' +
    'party IS an OAuth client and an OpenID4VCI wallet authenticates as one, ' +
    'so all three declare their name in <code>oauthClientId</code>; both ' +
    'SAML profiles name the same party, so both use ' +
    '<code>samlEntityId</code>. Two boxes writing one attribute would be a ' +
    'form that silently kept whichever ' +
    'was filled in second.') +
    instance.note('<strong>Four of these are declaration and only ever ' +
    'declaration.</strong> Nothing in this service writes ' +
    '<code>federationPartnerId</code>, <code>ldapBindDn</code>, ' +
    '<code>scimClientId</code> or <code>spiffeWorkloadId</code>, because ' +
    'those surfaces either authenticate the CALLER rather than an ' +
    'application, or file the identity in a container of their own. The ' +
    'value is a note about what this application is, in the one place the ' +
    'rest of what it is already lives &mdash; and, like every other field ' +
    'here, it grants nothing: a federation partner declared here federates ' +
    'with nobody until a ' +
    'relationship under <code>ou=federations</code> says so.');
});

// THE CORS SECTION'S INTRO (2026-09-18). Its first sentence is the one the
// section exists to say, and it is unfolded on purpose: `note()` folds prose
// longer than a line under its opening sentence, so whatever comes first is
// what a reader sees without opening anything.
let NEW_APPLICATION_CORS_INTRO: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  NEW_APPLICATION_CORS_INTRO =
    '<p><strong>These origins configure CORS on every published protocol ' +
    'endpoint of this service</strong> &mdash; OAuth 2.0 and OpenID Connect, ' +
    'SAML, WS-Federation, OpenID4VCI and OpenID4VP, DID, SCIM, GNAP, the ' +
    'certificate enrollment protocols and the management API alike. A ' +
    'browser page on one of them may read this service\'s answers; a page ' +
    'on any other origin may not.</p>' +
    instance.note('<strong>Which list is asked depends on the request, not ' +
    'on the family.</strong> A request that names this application as its ' +
    'client &mdash; a <code>client_id</code>, a Basic credential, a client ' +
    'assertion, or an access token issued to it &mdash; is answered with ' +
    '<code>Access-Control-Allow-Origin</code> only for an origin listed ' +
    'HERE. A request that names no client at all &mdash; discovery, a ' +
    'JWKS, a DID document, every CORS preflight &mdash; is answered for an ' +
    'origin listed on ANY application in this realm. That is why this field ' +
    'is shown whatever families are ticked below: it is not a property of ' +
    'one protocol.') +
    instance.note('<strong>One exact origin per line</strong> &mdash; ' +
    '<code>https://app.example.com</code>, or with a port, ' +
    '<code>https://app.example.com:8443</code> &mdash; with no path and no ' +
    'wildcard. Each is stored normalised: scheme and host lower-cased and a ' +
    'default port dropped, so two spellings of one origin are one value. A ' +
    'value that is not an origin refuses the whole create ' +
    '(<code>STS-REG-0150</code>) rather than being written beside the ' +
    'others.') +
    instance.note('<strong>Empty allows no third-party origin, in both ' +
    'modes.</strong> This service\'s own origins &mdash; its listeners, ' +
    '<code>global.publicBaseUrl</code>, the embedded debugger and ' +
    '<code>global.corsOrigins</code> &mdash; never need listing. After the ' +
    'application exists, its page lists these origins one per row, with a ' +
    'Remove button on each and a box to add another.');
});

let NEW_APPLICATION_REDIRECTS_INTRO: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  NEW_APPLICATION_REDIRECTS_INTRO =
    instance.note('Where a response goes back to. Only three families ' +
    'send one through a browser, which is why there are three of these and ' +
    'fourteen identifiers above.') +
    instance.note('<strong>Only the OAuth list is ever CHECKED, and ' +
    'only in RFC 9700 mode.</strong> <code>oauthRedirectUri</code> is what ' +
    'section 2.1\'s exact string comparison reads &mdash; give an ' +
    'application its redirect URIs here and the next authorization request ' +
    'is judged against them rather than against the ' +
    '<code>oauth2.redirectUris</code> setting, which is most of the reason ' +
    'to create an entry before the application connects. ' +
    '<code>samlAssertionConsumerService</code> and ' +
    '<code>wsfedReplyUrl</code> are checked in PRODUCT mode only: in ' +
    'development a SAML response goes wherever the AuthnRequest asked and a ' +
    '<code>wsignin1.0</code> response goes to whatever <code>wreply</code> ' +
    'named, because a mock that refused would remove a test case rather than ' +
    'add one &mdash; unless the SAML service provider\'s metadata has been ' +
    'consumed, when a request is answered only at an endpoint it ' +
    'registered, in every mode. The SAML one is READ for something else ' +
    '&mdash; it is the ' +
    'fallback used when a Single Logout has nowhere else to go, which is why ' +
    'WS-Federation\'s <code>wreply</code> stopped being written into it on ' +
    '2026-08-25 and has a ' +
    'field of its own.') +
    instance.note('<strong>What a client has actually USED is a ' +
    'different attribute and is not here.</strong> ' +
    '<code>appRedirectUriObserved</code> records a redirect_uri seen on a ' +
    'request this service answered, it is not editable anywhere in this ' +
    'console, and RFC 9700 section 2.1 is entirely about not confusing the ' +
    'two.');
});

let NEW_APPLICATION_NOTES: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  NEW_APPLICATION_NOTES =
    instance.note('<strong>Declaring a protocol family grants nothing ' +
    'and refuses nothing.</strong> No endpoint in this service reads ' +
    '<code>appAllowedProtocol</code>: an application declared for SAML 2.0 ' +
    'alone is still issued an access token at <code>/oauth2/token</code>, ' +
    'and one declared for nothing at all is treated exactly as it would have ' +
    'been. It is a RECORD OF INTENT on the entry &mdash; what this ' +
    'application is FOR, said before it has connected &mdash; and it is ' +
    'deliberately not a permission, because a mock that refused a protocol ' +
    'would remove a test case rather than add one. The configuration that ' +
    'DOES take effect is the attributes underneath: give the entry its ' +
    'redirect URIs, its grant types and its secret from <a ' +
    'href="/admin/applications">Applications</a>, and ' +
    'RFC 9700 mode judges the next request against them.') +
    instance.note('<strong>The families are DECLARED; ' +
    '<code>appProtocol</code> is what HAPPENED.</strong> Those two ' +
    'attributes sit next to each other on the entry and must not be read as ' +
    'one thing. This form writes the first; the second is accumulated by the ' +
    'protocol endpoints as they accept this identifier and is not editable ' +
    'here, for the reason every derived attribute on that page is not ' +
    '&mdash; a form that could rewrite it would make this console lie about ' +
    'the service\'s own behaviour, in a way indistinguishable from the ' +
    'recording being broken. The Applications drill-down shows both side by ' +
    'side, and says ' +
    'which of the declared families the entry has actually been recorded in.') +
    instance.note('<strong>One entry per identifier, whatever protocol ' +
    'brought it.</strong> The key is the identifier exactly as it arrives ' +
    '&mdash; not lower-cased and not namespaced by protocol &mdash; so this ' +
    'is refused if the registry already holds one under that name, and an ' +
    'application that appears under one name in two protocols is one entry ' +
    'with two kinds. Change what an existing one holds rather than creating ' +
    'it ' +
    'again.') +
    // WHETHER an application entry survives a restart is a property of the
    // whole service rather than of this page, which is why this note is
    // computed here rather than asserted: an application IS a directory
    // entry, so it persists exactly when the directory does.
    (persistence.status().persistsDirectory
      ? '<div class="ok"><strong>This entry will survive a restart.</strong> ' +
        'An application here is a directory entry under ' +
        '<code>ou=applications</code>, and this process is running with ' +
        '<code>persistence.mode=' +
        instance.esc(persistence.status().mode) + '</code> ' +
        '— so it is written down along with every other application, person, ' +
        'group, federation relationship and SPIFFE registration. That is a ' +
        'property of the whole service and not of this page; see <a ' +
        'href="/admin/persistence">Persistence</a>.</div>'
      : instance.note('<strong>Nothing here is persisted on this ' +
        'process.</strong> The entry is gone on restart, along with every ' +
        'other application, person and group in the directory. That is a ' +
        'property of the whole service and not of this page, and it is ' +
        'changeable: <a href="/admin/persistence">Persistence</a> writes the ' +
        'directory down, and is off by default.'));
});

// ---------------------------------------------------------------------------
// CONFIGURE AN APPLICATION FROM ITS RFC 9728 METADATA (2026-09-13).
//
// A protected resource publishes a document saying what it is, and this page
// can be handed one — pasted, uploaded, or fetched from a URL — and turn it
// into the create form, filled in: the `resource` as the default name, the
// permission base URI and the audience; `scopes_supported` as the permissions;
// a client_id minted at random. `oauth-oidc/protected_resource_metadata.ts` is
// the reading and argues every rule; what is decided HERE is the page.
//
// **TWO ROUND TRIPS AND NO SCRIPT.** A file can only reach a server without a
// script as multipart/form-data, and a document read on the server can only
// reach the page as a page — so Load is a POST that answers with this page and
// the pane on it, and Create is a second POST. Both go to
// `/admin/applications/new`, which is why that path takes a POST now: a create
// that was refused must come back HERE with the document still loaded and every
// edit still in its box, and the list page's 303 would lose both. It is the
// arrangement `/admin/users/new` argues for a refused create, and for its
// reason.
//
// **THE TABS ARE CSS.** Three radio buttons and the sibling combinator, in the
// console's one stylesheet — see `page()`. The third tab's boxes are fields of
// the create form whichever tab is showing, so editing them and pressing Create
// from the first tab submits what was edited.
//
// **IT IS NOT A SECOND DOOR EITHER.** Load is `load-resource-metadata` and
// Create is `create`, both through `applicationsAction()` — the switch
// `POST /admin/applications` and `POST /admin-api/applications/{action}`
// dispatch on — so the page adds TRANSPORT and nothing it decides.
// ---------------------------------------------------------------------------
const RESOURCE_METADATA_OWNED = ['oauthClientId', 'oauthPermissionBaseUri',
                                 'oauthAudience', 'oauthPermission',
                                 'oauthAuthorizationDetailsType',
                                 'oauthResourceMetadata',
                                 'oauthResourceMetadataUrl'];

// ---------------------------------------------------------------------------
// GET /admin/authorization-servers, POST /admin/authorization-servers
//
// RFC 9700 section 2.6 asks an authorization server to PUBLISH its metadata so
// that clients stop hard-coding security capabilities. This page is the other
// side of that: it decides what the published document SAYS, per authorization
// server, so that a client which reads the metadata can be shown reading it —
// and a client which does not can be shown not to.
//
// **A profile changes the document and not the endpoints**, and the page says
// so three times because it is the one thing here that could mislead badly.
// Everywhere else in this service a document disagreeing with the code is a
// defect — /admin/sts-metadata exists to report exactly that — and here it is
// the feature. So every view computes the DRIFT: which overridden members
// disagree with what this service would actually publish, and which removals
// hide something real.
// ---------------------------------------------------------------------------
let AS_CAVEAT: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  AS_CAVEAT =
    instance.note('<strong>What a document says is what that ' +
    'authorization server DOES.</strong> Advertise ' +
    '<code>code_challenge_methods_supported: ["S256"]</code> here and this ' +
    'server\'s own authorization endpoint refuses <code>plain</code> — at ' +
    '<code>/{id}/oauth2/authorize</code>, and nowhere else. The members ' +
    'marked <em>enforced</em> below drive behaviour; the rest are published ' +
    'and cannot be made true by this service, which is still useful (a ' +
    'document a client did not expect is a client error path worth running) ' +
    'and is listed ' +
    'as <em>drift</em> so that nobody discovers it the hard way.') +
    instance.note('<strong>Every authorization server starts ' +
    'equal.</strong> A new one — or one created by somebody simply asking ' +
    'for it — has exactly the capabilities the default server has, and ' +
    'differs only where it has been made to. <strong>Every client may use ' +
    'every one of them</strong>: nothing here restricts a client to a ' +
    'server, and <a href="/admin/applications">the applications page</a> ' +
    'records which ones each client has actually used. What does NOT cross ' +
    'between them is a credential — an authorization code issued by one is ' +
    'refused at another\'s ' +
    'token endpoint.');
});

const AS_LINKS =
  '<p class="sub"><a href="/.well-known/oauth-authorization-server">the ' +
  'default RFC 8414 document</a> &middot; <a ' +
  'href="/.well-known/openid-configuration">the default OpenID Provider ' +
  'Configuration</a> &middot; <a href="/admin/applications">the clients that ' +
  'read them</a></p>';

// ---------------------------------------------------------------------------
// GET /admin/rbac, POST /admin/rbac — WHO MAY USE THIS CONSOLE.
//
// The page behind the gate above, and the second page here (after
// /admin/groups) whose content comes from the directory rather than from this
// service's own memory. It renders and decides nothing: `admin_rbac.js` holds
// the two roles, the empty-roster rule and both writes, and this draws them —
// the same division /admin/groups keeps with `ldap_server.js`, and for the same
// reason. A second opinion here about who is an administrator is the one bug in
// this feature that would be genuinely dangerous.
//
// **ONE TABLE OF GRANTS RATHER THAN ONE TABLE PER ROLE**, which is a rendering
// decision with a reason. Two tables would each need their own filter, their
// own page parameter and their own per-page control — three sets of controls
// for a list that is usually four rows long — and a reader's question is "who
// has access", not "who is in cn=admin-read". So a row is one GRANT: a person
// and a role. Somebody holding both roles is two rows, which is honest — they
// were granted twice and can be revoked once.
//
// **THE GRANT FORM IS TWO FORMS AND THAT IS DELIBERATE.** One picks from a list
// of people this service knows about, which is what somebody wants nine times
// in ten and is the only one that can be used without knowing how names are
// spelt here. The other takes a typed name, because the interesting case for a
// mock is granting a role to somebody who has NEVER been here — then watching
// them arrive already holding it. A single control doing both would have to be
// a text box with a datalist, and the list would then look like a set of
// options while silently accepting anything, which is a control that lies about
// what it takes.
//
// **THE PICKING HALF STOPPED BEING A `<select>` ON 2026-09-13.** It held every
// candidate, and once the default realm held thousands of people it could not
// be used. It is a search with a paged results pane — chooserPane(), the
// control /admin/delegation already uses for people and applications — whose
// results are links that PICK a person, and picking opens the grant form for
// them. The argument above survives intact, because the two halves are still
// two forms: the picked form's username is a hidden input that can only hold
// somebody the list offered (the view resolves `person` against the
// candidates rather than echoing it), and the typed form is still the one that
// takes anything.
// ---------------------------------------------------------------------------

// The caveat, on the page rather than only in a comment. It is the exact
// counterpart of GROUPS_CAVEAT and it says the opposite thing about two named
// groups, which is why it is worded to leave the general claim standing.
let RBAC_CAVEAT: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  RBAC_CAVEAT =
    instance.note('<strong>These two groups are the only groups in this ' +
    'service that grant anything, and what they grant is this ' +
    'console.</strong> Every other group here still grants nothing at all — ' +
    'see <a href="/admin/groups">Groups</a>, which says so — and even these ' +
    'two grant nothing outside <code>/admin</code>: no token\'s scopes ' +
    'change, no assertion gains an attribute, no Kerberos PAC is affected, ' +
    'and a member of <code>admin-write</code> gets exactly the same answer ' +
    'from <code>/oauth2/token</code> as anybody else. They are also ordinary ' +
    'directory entries, so <code>ldapmodify</code>, a SCIM PATCH, this page ' +
    'and <code>POST /admin-api/rbac/grant</code> are four doors onto one ' +
    'membership — which is the point rather than a leak: a role no test can ' +
    'grant is a role no test can exercise.');
});

// ---------------------------------------------------------------------------
// GET /admin/roles, POST /admin/roles — WHO HOLDS A ROLE, AND WHO REQUIRES ONE.
//
// **IT IS NOT `/admin/rbac`, WHICH IS THE PAGE NEXT DOOR, AND THE TWO ARE ONE
// KEYSTROKE APART IN THE SIDEBAR.** That one has two roles, they are ordinary
// directory GROUPS, and they grant exactly one thing: this console. This one
// has as many roles as somebody makes, they live in `ou=roles`, and what they
// grant is being ISSUED something — a token, an assertion, a ticket, a
// session. Both headings say so, because a reader who confused them would
// grant somebody the console while meaning to let them sign in to an
// application.
//
// **AND IT IS NOT `/admin/groups`.** A group here still grants nothing; that
// sentence is written in six places and is still true. What changed is that a
// role may NAME a group, so adding somebody to `cn=developers` can now give
// them a role — through this register and only through it. The group is still
// inert; the row on a role entry is what does the work.
//
// TWO RELATIONS, TWO TABLES, AND THEY ARE OPPOSITE:
//
//   * MEMBERSHIP — role -> the users, groups and applications that HOLD it.
//     Stored on the ROLE entry, edited here.
//   * REQUIREMENT — application -> the roles it DEMANDS before this service
//     issues anything for it. Stored on the APPLICATION entry, in
//     `appRequiredRole`, and edited THERE. It is drawn here read-only, which
//     is the same arrangement `applicationPermissionsSection()` has in
//     reverse: one store, one door that writes it, and a second surface that
//     RESOLVES it — this page can say which role an application requires and
//     whether anybody holds it, which the application page cannot.
//
// An application appears in both tables and means opposite things in each: in
// the first it HOLDS the role (which is what a `client_credentials` grant is
// decided on, where there is no person), and in the second it DEMANDS one.
// `common/roles.js` argues that split at length and it is not repeated here.
// ---------------------------------------------------------------------------




// ---------------------------------------------------------------------------
// THE ELEVENTH SLOT ON THIS MODULE, AND IT PASSES RULE 3e'S TEST BOTH WAYS
// ROUND.
//
// `/admin/roles` can answer "would alice be issued a token for this
// application" without anybody having to try it, and the only thing that can
// answer that is `xacml/xacml_role_pep.ts` — the embedded PEP, which asks the
// PDP against the issuance policy. A require from here to that module would
// LOAD THE XACML ENGINE AT 18 and, worse, fill `issuance_gate.js`'s decider
// from the console rather than from the family that owns it, so a process that
// loaded this file and not `xacml/xacml.ts` would gate issuance with half the
// family present. A require the other way — the PEP reaching this module — is
// not a candidate either: it would close a cycle, since `xacml_admin.js`
// requires this file for the page shell.
//
// It carries TWO functions, `preview()` and `policy()`, and is validated
// whole, for `setLogoutReader()`'s reason: a preview installed without the
// thing that says WHICH POLICY answered would be a page able to ask a question
// and unable to explain the answer.
//
// AN EMPTY SLOT IS A SENTENCE AND NOT A BLANK. The section draws anyway and
// says the XACML family is not loaded in this process, which is the honest
// answer and the same one `issuance_gate.check()` gives: nothing is gated.
// ---------------------------------------------------------------------------
let rolePreviewer = null;

// WHICH PLACEHOLDERS ACTUALLY EXPAND IN AN ASSERTION, which is not the list the
// claims page advertises and is worth being exact about rather than tidy.
//
// A placeholder is expanded against the CONTEXT the issuance path hands
// admin_stats.expandValue(), and the two assertion builders hand it
// `{ subject, audience }` — saml2.ts and saml11.ts, the same one line each —
// where oauth2.js hands a token's whole claim context. So `${username}`,
// `${email}` and the rest of PLACEHOLDERS reach an assertion as the characters
// they were written as. That is the documented behaviour of an unknown
// placeholder (it names itself rather than silently becoming empty), and it is
// not a defect of this page — but a page that recited the JWT list under a SAML
// heading would be telling somebody their attribute will carry a name when it
// will carry `${username}`. `${now}` and `${iso}` are computed inside
// expandValue() and so work everywhere.
const SAML_PLACEHOLDERS = ['subject', 'audience', 'now', 'iso'];



// ---------------------------------------------------------------------------
// /admin/realms — SEVERAL LOGICAL COPIES OF THIS SERVICE, IN ONE PROCESS.
//
// A TRUST REALM is a whole mock identity service: its own configuration, its
// own signing key, its own sessions, authorization codes, tokens, offers,
// service providers, statistics and audit log — answering on the SAME sockets
// as every other and told apart by a segment at the front of the path.
//
//   https://host:8081/oauth2/token               the default realm
//   https://host:8081/realm/acme/oauth2/token    the realm `acme`
//
// WHAT THIS PAGE IS FOR, and it is not "somewhere to keep a list". Two things
// nothing else here can tell you:
//
//   * WHAT A REALM'S ENDPOINTS ACTUALLY ARE. The prefix segment is a setting,
//     the ids are whatever somebody typed, and a client being pointed at a
//     realm has no way to derive either. Every row here carries the realm's
//     base URL, and the drill-down carries the four URLs a client asks for
//     first.
//   * WHICH FAMILIES ARE REALM-AWARE AND WHICH ARE NOT, WHICH IS NOT A TIDY
//     ANSWER. What a realm separates completely is what it ISSUES and what it
//     is holding while it issues it. This paragraph used to say the embedded
//     DIRECTORY was not separated at all; it has been a subtree per realm
//     since 2026-08-25, and the two admin roles (2026-09-14, #32), SPIFFE's
//     trust domains and sockets (2026-09-12) and Kerberos (a KDC per realm on
//     the shared port 88, 2026-09-15, #33) have followed it. What is still the
//     process's is what has no name inside its protocol to carry a realm —
//     the certificate the main port and LDAPS 636 present, a client
//     certificate at the handshake, and the Kerberos and directory SOCKETS
//     themselves. `common/realms.js`'s per-family rows are the current answer
//     and are what the table at the foot of this page draws: a person who
//     assumed a realm was a boundary everywhere would otherwise find out the
//     hard way.
//
// **IT KEEPS NOTHING OF ITS OWN.** The registry is common/realms.js's, the
// per-realm settings are config.js's — set through the same
// `config.setOverride()` that /admin/config, /admin/token-lifetimes and
// POST /admin-api/config/set call, against the realm's own override map — and
// the signing key is helpers.js's. That is the one-store rule this console
// follows everywhere: a page holding its own copy of what a realm sets would be
// a second answer to "what is this realm configured with" that the protocol
// endpoints could not see.
//
// **THE DEFAULT REALM IS NOT A ROW SOMEBODY CREATED** and cannot be removed,
// renamed or re-prefixed. Every URL this service published before realms
// existed is a URL in it, so an operator who could delete it could delete the
// service.
// ---------------------------------------------------------------------------
let REALMS_CAVEAT: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  REALMS_CAVEAT =
    instance.note('<strong>A realm separates what this service ISSUES, ' +
    'not who it knows.</strong> Each realm has its own signing key, so a ' +
    'token minted in one does not verify against another\'s JWKS — that is ' +
    'the point of a realm rather than a side effect. Each realm also has a ' +
    'directory of its own — its own <code>ou=users</code>, ' +
    '<code>ou=groups</code> and <code>ou=applications</code> under ' +
    '<code>dc=&lt;id&gt;</code> — and so <strong>administrators of its ' +
    'own</strong>: the two role groups in that directory administer that ' +
    'realm and nothing outside it, while the default realm\'s two groups ' +
    'administer every realm. A new realm is seeded with an ' +
    '<code>admin</code> account that holds both. The table at ' +
    'the foot of this page is the whole list of what is separated how.') +
    (persistence.status().persistsRealms
      ? '<div class="ok"><strong>A realm defined here WILL come ' +
        'back.</strong> ' +
        'This process is running with ' +
        '<code>persistence.mode=' +
        instance.esc(persistence.status().mode) + '</code>, ' +
        'so the realm rows — their names, descriptions and per-realm ' +
        'settings — and each realm\'s own directory are written down and ' +
        'restored at the next start. WHAT DOES NOT COME BACK IS THE KEYS: ' +
        'every realm\'s signing key is regenerated on every start, exactly ' +
        'like the default realm\'s, so a token minted in this realm today ' +
        'verifies against nothing tomorrow. See <a ' +
        'href="/admin/persistence">Persistence</a>.</div>'
      : instance.note('<strong>Nothing here is persisted on this ' +
        'process.</strong> Realms are held in memory and die with it, along ' +
        'with the keys they signed with. Define them from <code>POST ' +
        '/admin-api/realms</code> in whatever starts your stack if you want ' +
        'them back — or turn on <a ' +
        'href="/admin/persistence">Persistence</a>, which writes the realm ' +
        'registry and each realm\'s directory down. The ' +
        'KEYS are regenerated on every start either way.'));
});

// THE SIXTH THING THE READ LAYER IS HANDED, and the only one that is not an
// inverted hook: `scimJson()` embeds this console's settings block in its
// answer, and the alternative was for the page and /admin-api/scim to
// assemble that block separately — which is the drift rule 7 exists to
// prevent. It is handed over once, when the instance is installed (at require
// time until #50's R2), because it is defined here and never replaced.
WIRE_STEPS.push(function (instance: AdminConsole): void {
  adminViews.setConfigSettingsJson(
    instance.configSettingsJson.bind(instance));
});

// ---------------------------------------------------------------------------
// /admin/token-lifetimes — HOW LONG WHAT THIS SERVICE ISSUES IS GOOD FOR.
//
// Four settings when it was written — six now (`TOKEN_LIFETIME_KEYS`, with the
// refresh idle timeout and the sign-out revocation) — all of them `config.js`
// rows, on a page of their own under Protocols › OAuth2 / OIDC. The first
// three were module-level `const`s in
// `../oauth-oidc/oauth2.ts` until 2026-08-24 and could not be changed at all
// without a restart.
//
// WHY THIS IS A PAGE AND NOT JUST FOUR MORE ROWS ON /admin/config, which is the
// question `/admin/scim`'s header answers the other way — it has no form
// precisely because everything about SCIM that can be changed is a config row
// and "a second form here would be a second door to one setting". The rule that
// header is applying is the ONE-STORE rule, and it is untouched here: there is
// no store. This page holds nothing, decides nothing, and writes through
// `config.setOverride()` — the same function `/admin/config`'s Save and
// `POST /admin-api/config/set` call, against the same override map. What would
// break the rule is a second PLACE THE VALUE LIVES, and there is none. What is
// different from SCIM's case is what the reader is doing:
//
//   * These four are a QUANTITY somebody sets to a specific number to watch
//     something happen, over and over, in a session — "make it a minute so I
//     can see my client refresh". `/admin/config`'s form was then a table of
//     forty-nine settings with a text box each; finding four of them in it,
//     every time, is the cost this page removes.
//   * They INTERACT, and a page can say so where a flat table cannot. An access
//     token that outlives the refresh token is a grant that can never be
//     renewed; a clock skew larger than the lifetime is a token that is never
//     expired. Both are legal here — this service refuses nothing it can
//     merely explain — and both are worth being told about before the client
//     author debugs their own code for an hour.
//   * The answer to "why is my client being refused" is usually "the token
//     expired", and the numbers that decide it are worth having beside the
//     count of what already has.
//
// The test for a THIRD page like this one is the same as rule 3e's for a hook:
// it earns its place when the reader's task is not the one /admin/config
// serves, and it costs a reader nothing only while it writes through the same
// function. A page that started keeping its own copy of a value would be the
// thing both rules exist to prevent.
//
// NOTHING ALREADY ISSUED CHANGES, and the page says so twice. A lifetime is
// stamped into a token as `exp` when it is signed, so a change here reaches the
// NEXT token. That is a fact about signed statements, not a limitation, and
// somebody who did not expect it will read the tokens table below and conclude
// the setting did not take.
// ---------------------------------------------------------------------------


// Which token kind each lifetime governs, so the page can put the count of what
// is already out there beside the number that decided it. `oauth2.clockSkewS`
// governs no kind — it is applied when a token of any kind is read back — and
// is deliberately absent rather than mapped to something plausible.
const TOKEN_LIFETIME_KINDS = {
  'oauth2.accessTokenTtlS': 'access_token',
  'oauth2.idTokenTtlS': 'id_token',
  'oauth2.refreshTokenTtlS': 'refresh_token'
};

// ---------------------------------------------------------------------------
// MONITORING -> SHARED SIGNALS -> DEAD LETTERS (2026-09-14).
//
// What every dead-letter queue in the realm being read holds, counted, and the
// letters themselves. `ssf/ssf_dead_letter_report.ts` computes every number
// and `admin-core/admin_views.ts`'s `ssfDeadLettersJson()` searches and pages
// the list, for this page and `GET /admin-api/ssf/dead-letters` alike; this
// draws them.
//
// **READ-ONLY, AND THE CONTROLS ARE ONE LINK AWAY.** Revive and Drop its dead
// letters act on ONE stream and live on that stream's card at /admin/ssf,
// which every stream row here links to. A second copy of either here would be
// a second door onto the same two actions for no reader who needs it — the
// reason to come to this page is to find WHICH stream, and the next click is
// that stream.
//
// **THE CHART IS THE FIRST IN THIS CONSOLE**, so its choices are written down
// here rather than inherited. A stacked column per time bucket over the whole
// retention window, one colour per CAUSE (not per error code — there are more
// than a dozen of those, and four is what a reader acts on). The four colours
// were checked with a colour-vision validator rather than chosen by eye: the
// worst adjacent pair is ΔE 9.1 under protanopia and 22.9 in normal vision.
// Two of them are under 3:1 against the white card, which is why no value is
// ever carried by colour alone: the legend names each cause and its count in
// text, every column's `<title>` gives its counts on hover, and the same
// numbers are a table under it. There is no script, so the hover is the
// browser's own tooltip on an SVG `<title>`, over a hit area the full height
// of the plot so a one-letter column can still be pointed at.
// ---------------------------------------------------------------------------
const DEAD_LETTER_COLOURS = {
  'push-failed': '#2a78d6',
  'backlog-full': '#eb6834',
  'declared-dead': '#1baf7a',
  'dead-stream': '#eda100'
};

// ---------------------------------------------------------------------------
// THE EIGHTH SLOT, filled by `../ssf/ssf.ts` at its own require time, and rule
// 3e's test answers yes in both directions at once.
//
//   * a require from THIS file to `../ssf/ssf.ts` would CLOSE A CYCLE: that
//     module requires this one for the page shell and the gate, exactly as
//     `../sts_metadata.js` and `./crypto_metadata.js` do.
//   * a require the other way round — from `mgmt-api/admin_api.ts` (19) to
//     `../ssf/ssf.ts` — would MOVE ROUTES: every `/ssf` endpoint, and the
//     `/.well-known/ssf-configuration` document, ahead of the management API's
//     own and of ldap, scim and spiffe. **Since #50's R1 (2026-09-16) those
//     are not the routes it would move** — `common/protocol_stack.ts`
//     registers SSF's own at 23b wherever the module is loaded — but
//     `ssf.ts` requires `ldap/ldap_server.js`, which is still JavaScript and
//     registers when required, so the require would still pull the
//     directory's routes (21) ahead of the management API's (19).
//
// So `/admin/ssf` and `/admin-api/ssf` reach it through a function this console
// holds, the way `/admin-api/crypto` reaches the crypto report.
//
// **IT CARRIES ONE OBJECT AND IS VALIDATED WHOLE**, which is the rule
// `setLogoutReader()` and `setCryptoReporter()` follow: a filler that installed
// the reader without the action would leave this page able to LIST streams and
// unable to change any of them, and rule 7's parity would be failing silently
// rather than loudly.
//
// **`action` RETURNS A PROMISE AND IT IS THE ONLY SLOT HERE THAT DOES.** Every
// other action function in this console answers from memory; transmitting a
// Security Event Token signs a JWS — which may be ML-DSA or SLH-DSA on the
// worker pool — and then POSTs it to somebody else's endpoint. Neither can be
// done synchronously, and pretending otherwise would mean this page reporting
// "sent" before anything had been.
// ---------------------------------------------------------------------------
let signalsReporter = null;

// ---------------------------------------------------------------------------
// THE THIRTEENTH SLOT: THE CLIENT-CERTIFICATE TRUSTSTORE (2026-09-12), and it
// passes rule 3e's test both ways round like the eighth above it.
//
//   * a require from THIS file to `../tls/tls_server.js` would MOVE ROUTES on
//     the order this repository documents — `/tls`, `/tls/server-certificate`
//     and `/tls/trust` belong at 20, after the management API — and would make
//     the console the reason they are where they are.
//   * a require the other way round, from `tls_server.js` to this file at its
//     own top level, would CLOSE A CYCLE — and not a theoretical one: that
//     module is really first loaded from inside THIS file's require, through
//     `admin-core/admin_views.ts` → `spiffe/spiffe_auth.ts`, so it would be
//     handed this module's half-built exports and find no `setTruststore` on
//     them. That is why `common/protocol_stack.ts` fills this, on the line
//     after it requires `tls_server.js`, rather than the filler being the
//     module that owns the array. It is the one slot here filled that way.
//
// **IT CARRIES ONE OBJECT AND IS VALIDATED WHOLE**, for `setLogoutReader()`'s
// reason: a truststore that could be LISTED and not changed would be a page
// drawing the anchors under two controls that answer "not installed", and one
// that could be changed and not listed would be a write nothing reads back.
//
// **AND IT IS A PAGE OF A PROCESS'S SOCKETS**, which decides where it is
// answered: `common/request_pool.js` pins `/admin/tls/trust` and
// `/admin-api/tls/trust` to the front process, because the array is the
// configuration of listeners only that process holds. A worker installs this
// slot too — it runs the same stack — and is never asked.
// ---------------------------------------------------------------------------
let truststore = null;


// ---------------------------------------------------------------------------
// THE NINTH SLOT, filled by `../ssf/caep.ts`'s host `../ssf/ssf.ts` at its own
// require time, for exactly the reasons the eighth exists and with the same
// test answering yes in both directions:
//
//   * a require from THIS file to `../ssf/ssf.ts` would CLOSE A CYCLE — that
//     module requires this one for the page shell and the gate;
//   * a require from `mgmt-api/admin_api.ts` would MOVE ROUTES, putting every
//     `/ssf` endpoint and the well-known document ahead of the management
//     API's own and of ldap, scim and spiffe — the directory's routes, since
//     #50's R1; see the eighth.
//
// **A SECOND SLOT RATHER THAN MORE MEMBERS ON THE EIGHTH**, and that is a
// decision rather than a habit. The signals reporter answers "what streams
// exist and what is on them"; this one answers "what has been said about which
// SESSION". They are two questions with two pages, and one object carrying
// both would mean `/admin/ssf` failing whole when the CAEP half was not
// installed — which is precisely the half-working state `setSignalsReporter()`
// refuses a partial filler in order to avoid.
//
// `action` RETURNS A PROMISE, like the eighth's: emitting a CAEP event signs a
// JWS — possibly on the worker pool — and then POSTs it to somebody else's
// endpoint.
// ---------------------------------------------------------------------------
let caepReporter = null;


// ---------------------------------------------------------------------------
// THE TENTH SLOT, filled by `../ssf/risc.ts`'s host `../ssf/ssf.ts` at its own
// require time, for exactly the reasons the eighth and ninth exist and with
// the same test answering yes in both directions:
//
//   * a require from THIS file to `../ssf/ssf.ts` would CLOSE A CYCLE;
//   * a require from `mgmt-api/admin_api.ts` would MOVE ROUTES (the
//     directory's, since #50's R1; see the eighth).
//
// **A THIRD SLOT RATHER THAN MORE MEMBERS ON THE NINTH.** The signals reporter
// answers *what streams exist and what is on them*; the CAEP one answers *what
// has been said about which SESSION*; this one answers *what has been said
// about which ACCOUNT*. Those are three questions with five pages between
// them, and the reason they are three objects rather than one is the reason
// `setSignalsReporter()` refuses a partial filler: an object carrying all
// three would mean /admin/ssf failing whole when the RISC half was not
// installed.
//
// It is also the honest shape of the two registers behind them. A session and
// an account are not the same thing — a session begins, is used and ends,
// there are many per person, and an account IS the person and outlives every
// session on it — so a reporter that served both would be one object whose row
// is sometimes one and sometimes the other.
// ---------------------------------------------------------------------------
let riscReporter = null;

const PROTOCOL_SETTINGS_PAGES = [
  // -------------------------------------------------------------------------
  // THE TWO SECOND FACTORS, ONE PAGE EACH, BOTH UNDER PROTOCOLS (2026-09-10).
  //
  // **THERE WAS ONE PAGE AND IT WAS FILED UNDER IDENTITIES.** `/admin/mfa`
  // drew the eight `totp.*` settings AND a roster of who held a second factor,
  // and the argument for putting it beside Users was that it answered a
  // question about PEOPLE. That argument was right about the roster and wrong
  // about the settings, and the page could not be filed by both halves at
  // once: a reader looking for the skew window and a reader looking for *who
  // has no second factor* landed on the same screen and read past each other.
  //
  // So it is split by the question each half answers, which is the rule
  // `/admin/xacml/monitor` established. **The MECHANISMS are protocols** — RFC
  // 6238 and W3C WebAuthn are specifications this service implements, and they
  // belong beside SCIM and Kerberos. **The PEOPLE are the directory** — the
  // roster is columns on <a href="/admin/users">Users</a> now and the
  // per-person detail, including the Clear buttons, is on that person's own
  // row.
  //
  // **WEBAUTHN HAD NO SETTINGS AT ALL UNTIL THIS DAY**, which is why nothing
  // like this page ever existed for it. `common/config.js`'s own comment said
  // *what it does is decided by the specification and by the browser, and
  // there is nothing an operator could usefully turn* — true of the
  // cryptography and false of the ceremony, every parameter of which was a
  // literal inside a string in `authn/authn.ts`.
  // -------------------------------------------------------------------------
  { path: '/admin/totp', title: 'TOTP MFA',
    lead: '<strong>An authenticator app as a second factor — RFC 6238 over ' +
          'RFC 4226 — and the eight parameters the specification leaves ' +
          'open.</strong> A person enrols one at <code>/portal/mfa</code> or ' +
          'while spending an activation link: a QR code this server drew, ' +
          'and the same secret in base32 beside it. <strong>Codes are ' +
          'checked FOR REAL in both modes</strong>, which almost nothing ' +
          'else on this service is. Who actually holds one is <a ' +
          'href="/admin/users">Users</a>, and clearing an enrolment is on ' +
          'that person\'s own row.',
    also: ['<strong>Changing the digest, the digits or the period affects ' +
           'NEW enrolments only.</strong> An existing secret is verified ' +
           'with the parameters it was enrolled under — the ones the QR code ' +
           'told the app — because this service cannot change them ' +
           'retrospectively and a setting that silently locked out everybody ' +
           'who had already enrolled would be the worst kind of knob. ' +
           '<strong>The skew window is the exception and applies to ' +
           'everybody</strong>: how much a deployment forgives a drifting ' +
           'clock is a policy rather than something the app was told.',
           '<strong>LEAVE THE DIGEST AT SHA1 UNLESS YOU ARE TESTING EXACTLY ' +
           'THAT.</strong> Several widely used authenticator apps — Google ' +
           'Authenticator among them — IGNORE the <code>algorithm</code> ' +
           'parameter in the QR code and always compute SHA-1, so any other ' +
           'value produces a code that scans perfectly and then generates ' +
           'codes this service refuses, with nothing anywhere saying why. ' +
           'SHA-1 is not a weakness here: this is a keyed MAC over a ' +
           'counter, not a collision-resistant digest.',
           '<strong>A shared secret can be read back and a password ' +
           'cannot.</strong> Verifying a code means COMPUTING it, so the ' +
           'secret is stored in a form this service can recover — unlike ' +
           '<code>userPassword</code>, which is a scrypt hash. In PRODUCT ' +
           'mode it is sealed under the same key-encryption key that ' +
           'protects the signing keys, so <a ' +
           'href="/admin/ldap/directory">the directory page</a> shows ' +
           'ciphertext; in DEVELOPMENT mode it is stored as base32, because ' +
           'the key-encryption key there is generated per run and sealing ' +
           'would mean an authenticator that silently stopped working at the ' +
           'next restart. That is also the whole reason this mechanism is a ' +
           'SECOND factor and can never be made a first one.',
           '<strong>A code can only be used once</strong> (RFC 6238 section ' +
           '5.2). The step this service last accepted is stored on the ' +
           'enrolment, so somebody signing in twice inside one window is ' +
           'asked to wait for the next code. That is correct and it ' +
           'surprises people, which is why the sign-in screen says which of ' +
           'the two happened rather than answering &ldquo;wrong code&rdquo; ' +
           'to both.'],
    status: slot.forward('totpMechanismBlock'),
    links: [['/admin/webauthn', 'the other second factor'],
            ['/admin/users', 'who holds one, and how to clear it'],
            ['/admin/crypto-metadata', 'every algorithm this service performs'],
            ['/portal/mfa', 'where a person enrols one']] },

  // -------------------------------------------------------------------------
  // THE THIRD MECHANISM PAGE (2026-09-10), AND THE ONLY ONE ON THIS CONSOLE
  // THAT IMPLEMENTS NO SPECIFICATION.
  //
  // It is a page of its own rather than a section of `/admin/totp` because a
  // recovery code stands in for EITHER of the two mechanisms above it. Filing
  // it under one of them would put it where half the people looking for it
  // would not look, and the console's own rule — where a page goes is decided
  // by the question it answers — says the question here is *how does somebody
  // get back in*, which is neither of those two pages' question.
  // -------------------------------------------------------------------------
  { path: '/admin/backup-codes', title: 'Recovery codes',
    lead: '<strong>The way back in when the second factor is not to ' +
          'hand.</strong> A set of single-use codes that a person generates ' +
          'for themselves on <code>/portal/mfa</code>, is shown once, and ' +
          'which is stored — one scrypt hash per code — only when they ' +
          'confirm they have kept it. Generating again REPLACES the set. ' +
          'There is no control here or on <code>/admin-api</code> that ' +
          'issues a set or shows a code. Who holds one is <a ' +
          'href="/admin/users">Users</a>, and clearing a set is on that ' +
          'person\'s own row.',
    also: ['<strong>THIS IS THE ONLY MECHANISM ON THIS CONSOLE THAT NO ' +
           'SPECIFICATION DEFINES.</strong> Everything else here implements ' +
           'somebody\'s document and can be checked against it; there is no ' +
           'RFC for a recovery code. What every identity provider does ' +
           'converges anyway — a handful of random strings, shown once, each ' +
           'accepted once — and the decisions that are left are this ' +
           'service\'s own. <code>common/backup_codes.ts</code> argues each ' +
           'of them, and the four settings below are what it leaves open.',
           '<strong>THEY ARE HASHED, ONE SCRYPT HASH PER CODE, AS ' +
           '<code>userPassword</code> IS (SINCE 2026-09-11).</strong> This ' +
           'repository\'s own rule is that a secret this service only ' +
           'VERIFIES is hashed. Until 2026-09-11 the codes were encrypted ' +
           'instead, so that a person could read their remaining codes back; ' +
           'the set is now shown exactly once, when it is generated, and ' +
           'nothing — the person included — can see a stored code again.',
           '<strong>A PERSON GENERATES THEIR OWN SET, AND GENERATING AGAIN ' +
           'REPLACES IT.</strong> Until 2026-09-11 a set was issued ' +
           'automatically, once, the first time a second factor was ' +
           'enrolled. Now it is generated on <code>/portal/mfa</code>, held ' +
           'apart from the entry until the person confirms they have kept ' +
           'it, and REPLACES any earlier set whole — a set is never topped ' +
           'up. A person who holds a second factor and no set is prompted ' +
           'to generate one. An operator\'s Clear deletes a set.',
           '<strong>CHANGING THESE SETTINGS AFFECTS NEW SETS ONLY, AND ' +
           'NOTHING HERE SAYS "NEW ENROLMENTS ONLY" THE WAY <a ' +
           'href="/admin/totp">TOTP</a> DOES.</strong> That page has to, ' +
           'because its parameters were TOLD TO AN APP this service cannot ' +
           'reach. Nothing here is told to anybody: a recovery code is ' +
           'compared against its stored hash, so shortening the ' +
           'length changes what the next set looks like and leaves an ' +
           'existing set matching exactly as it did.'],
    status: slot.forward('backupCodesMechanismBlock'),
    links: [['/admin/totp', 'one of the two factors these stand in for'],
            ['/admin/webauthn', 'the other'],
            ['/admin/users', 'who holds a set, and how to clear one'],
            ['/portal/mfa', 'where a person generates their own set']] },

  { path: '/admin/webauthn', title: 'WebAuthn',
    lead: '<strong>Security keys — W3C WebAuthn Level 3 over FIDO CTAP2 — as ' +
          'a second factor OR as the only credential on an account.</strong> ' +
          'This service is the relying party: it builds the ceremony\'s ' +
          'options, and it verifies the challenge, the origin, the RP ID ' +
          'hash, the flags and the signature when the result comes back. Who ' +
          'holds a key is <a href="/admin/users">Users</a>, and removing one ' +
          'is on that person\'s own row.',
    also: ['<strong>NOT ONE of these settings existed until 2026-09-10, and ' +
           'the sentence they replace is worth knowing.</strong> This ' +
           'service said WebAuthn had nothing an operator could usefully ' +
           'turn — true of the cryptography and false of the ceremony. The ' +
           'RP name, the algorithms offered, the user verification ' +
           'requirement, the attestation conveyance and the timeout were ' +
           'literals in a string, so a client author trying to find out what ' +
           'their client does with <code>attestation: "none"</code> or with ' +
           'a resident key had no way to ask this service for one.',
           '<strong>ONE of them is enforced and the rest are ' +
           'requests.</strong> <code>webauthn.userVerification</code> is ' +
           'sent to the browser AND checked against the UV flag when the ' +
           'ceremony returns, because that flag is inside the bytes the ' +
           'authenticator signed. Nothing signed says what the browser was ' +
           'asked about attestation, the resident key or the attachment — so ' +
           'a check on those would be a comparison against a value this ' +
           'service itself supplied. What it does instead is RECORD what ' +
           'came back.',
           '<strong>Raising user verification does not change what a session ' +
           'CLAIMS.</strong> A passwordless sign-in still records <code>amr ' +
           '["hwk"]</code> and <code>acr "1"</code> — one factor — even ' +
           'under <code>required</code>. RFC 8176 has no value for <em>the ' +
           'authenticator verified the user</em> that this service could ' +
           'honestly assert, and claiming <code>mfa</code> because the ' +
           'ceremony was phishing-resistant would be the exact fake this ' +
           'profile refuses everywhere else.',
           '<strong>The RP ID is the host this service was reached on, and ' +
           '<code>webauthn.rpId</code> can only widen it to a registrable ' +
           'domain suffix.</strong> That is WebAuthn\'s own rule and ' +
           'browsers enforce it; this service enforces it too, refusing ' +
           'anything else by name in the log — because a browser refuses it ' +
           'with a <code>SecurityError</code> the ceremony reports as one of ' +
           'its several indistinguishable failures, so a wrong value looks ' +
           'like a broken authenticator. Widening it means every host under ' +
           'that suffix can assert these credentials.'],
    status: slot.forward('webauthnMechanismBlock'),
    links: [['/admin/totp', 'the other second factor'],
            ['/admin/users', 'who holds a key, and how to remove one'],
            ['/admin/crypto-metadata', 'every algorithm this service performs'],
            ['/portal/keys', 'where a person enrols one']] },

  { path: '/admin/oauth2', title: 'OAuth 2.0 / OIDC settings',
    lead: '<strong>The authorization server\'s own settings.</strong> ' +
          'Everything about what this service will ACCEPT at ' +
          '<code>/oauth2/authorize</code> and <code>/oauth2/token</code> and ' +
          'what it puts into what comes back — separately from which ' +
          'authorization server a flow runs against, which is ' +
          '<a href="/admin/authorization-servers">Authorization servers</a>, ' +
          'and from what a token CARRIES, which is ' +
          '<a href="/admin/claims">Custom claims</a> and ' +
          '<a href="/admin/userinfo-claims">UserInfo claims</a>.',
    also: ['<strong><code>oauth2.rfc9700</code> is a MODE, and so is ' +
           '<code>oauth2.oauth21</code></strong>; both are off unless turned ' +
           'on. With them off ' +
           'nothing below is enforced: no PKCE is required, no redirect URI ' +
           'is matched exactly, no client is authenticated. That is what ' +
           'this service is for — a client\'s error paths cannot be ' +
           'exercised against a server that refuses nothing — and it is why ' +
           'the mode exists rather than the checks being on. It is ' +
           'restart-only, because <code>global.https</code> derives from it ' +
           'and a listener\'s scheme is settled when the socket is bound; a ' +
           'TRUST REALM can be in the mode while the process is not, which ' +
           'is the way to have both at once.',
           '<strong><code>oauth2.oauth21</code> is OAuth 2.1 ' +
           '(draft-ietf-oauth-v2-1-16), and it turns RFC 9700 mode ' +
           'on</strong> — everything that mode enforces is part of 2.1 — ' +
           'then adds the rest: PKCE for confidential clients too, a client ' +
           'that has registered its own redirect URI ' +
           '(<code>oauth2.redirectUris</code> below is not read), a ' +
           'presented credential that must verify, a JWT client assertion ' +
           'addressed to the issuer alone, and no SAML client ' +
           'authentication. It also lets a token request leave out ' +
           '<code>redirect_uri</code>, which RFC 9700 mode refuses — so a ' +
           'client written for 2.1 is exercised in this mode and not in that ' +
           'one. Restart-only and realm-settable for the same reason.',
           '<strong>The five sender-constraint settings ask for MORE than ' +
           'either specification does (#34, 2026-09-15).</strong> Neither ' +
           'OAuth 2.1 (section 4.3.1) nor RFC 9700 requires DPoP: section ' +
           '4.3.1 asks a public client\'s refresh token to be ' +
           'sender-constrained <em>or</em> rotated with replay detection, ' +
           'and a sender-constrained access token is a SHOULD. So all five ' +
           'are off unless set, and no mode turns one on. ' +
           '<code>oauth2.refreshTokenRotation</code> takes the rotation ' +
           'answer with both modes off — the modes already rotate for every ' +
           'client. The four <code>Require</code> rows REFUSE rather than ' +
           'downgrade: a token request that would hand out an unconstrained ' +
           'refresh token is refused WHOLE, access token included, because ' +
           'half a token set is discovered an hour later at a refresh that ' +
           'cannot be made; and an unbound refresh token is refused at the ' +
           'refresh grant rather than bound to whoever presents it first.',
           '<strong>The two access-token rows refuse at the RESOURCE, not at ' +
           'the token endpoint.</strong> This service goes on issuing bearer ' +
           'tokens, which every surface that accepts a presented access ' +
           'token then refuses — UserInfo, the step-up resource, the three ' +
           'OpenID4VCI endpoints, <code>/scim/v2</code>, the Shared Signals ' +
           'endpoints, <a href="/admin-api">the management API</a> and the ' +
           'embedded debugger\'s listener. That is deliberate: a client ' +
           'under test needs to MEET the refusal. What they do not cover is ' +
           'what is not a presented OAuth access token — GNAP\'s own tokens, ' +
           'an RFC 7592 registration access token, and the endpoints that ' +
           'take a token as a parameter (introspection, revocation, token ' +
           'exchange). <strong>Two consequences worth knowing before you ' +
           'turn one on</strong>: <a href="/admin/api-explorer">the API ' +
           'explorer</a> stops working while DPoP is required, because its ' +
           'script sends a plain <code>Bearer</code> header; and the mutual ' +
           'TLS rows need <code>global.https</code>, without which every ' +
           'affected request is refused rather than waved through.',
           '<strong>This console and the user portal keep working, and the ' +
           'debugger is an ordinary client.</strong> <code>/admin</code> and ' +
           '<code>/portal</code> are OpenID Connect clients of this service; ' +
           'since 2026-09-15 they carry a DPoP key of their own and prove it ' +
           'on every back-channel token call, so the DPoP rows do not lock ' +
           'you out. They are EXEMPT from ' +
           '<code>oauth2.refreshTokenRequireMtls</code> alone, because their ' +
           'token requests are loopback calls from this process to itself ' +
           'and there is no certificate story to tell about one. The ' +
           'debugger\'s client is not exempt from anything: point it at a ' +
           'realm that requires a constraint and configure it to meet one, ' +
           'the same as any other client here.',
           '<strong><code>oauth2.breakIdTokenNonce</code> makes this service ' +
           'wrong on purpose.</strong> Turn it on and every ID Token carries ' +
           'a <code>nonce</code> that is not the one the client sent, so a ' +
           'client that does not check gets a token it should have refused. ' +
           'It is the same device as the reserved password ' +
           '<code>invalid</code> and the Kerberos names that stay unknown: a ' +
           'permissive server is hard to write error handling against, so ' +
           'the errors have to be reachable deliberately.'],
    links: [['/.well-known/openid-configuration', 'the discovery document'],
            ['/oauth2/rfc9700', 'what RFC 9700 mode enforces'],
            ['/oauth2/oauth21', 'what OAuth 2.1 mode enforces'],
            ['/admin/token-lifetimes', 'how long what it issues lasts'],
            ['/admin/tokens', 'what has been issued']] },

  { path: '/admin/oid4vci', title: 'OpenID4VCI',
    lead: '<strong>The credential issuer at <code>/issuer</code>, and the ' +
          'settings around it.</strong> What a credential CONTAINS is <a ' +
          'href="/admin/vc">Credential claims</a> next door; these are the ' +
          'ones about the exchange — where a holder is sent, which ' +
          'authorization server the credential endpoint will take a token ' +
          'from, how big a batch may be, and how long a deferred issuance ' +
          'pretends to take.',
    also: ['<strong>Every credential names its status.</strong> A ' +
           'dc+sd-jwt and a jwt_vc_json credential carry a Token Status ' +
           'List reference and a jwt_vc_json and an ldp_vc a Bitstring ' +
           'Status List entry, served at <code>/oid4vci/status-lists</code> ' +
           'and signed with the credential key — which may be post-quantum. ' +
           '<code>oid4vci.statusListTtlS</code> is how long a verifier may ' +
           'keep a list; <a href="/admin/vc-status">Credential status</a> ' +
           'suspends and revokes.',
           '<strong>Key attestations.</strong> A wallet may say how its key ' +
           'is kept (OpenID4VCI Appendix D); one signed by a certificate in ' +
           '<code>oid4vci.keyAttestationTrustedCertificates</code> is ' +
           'recorded, and decides whether a wallet sign-in claims ' +
           '<code>hwk</code> and <code>acr "mfa"</code>. ' +
           '<code>oid4vci.keyAttestationRequired</code> requires one.',
           '<strong>The two DID settings change what a VERIFIER has to ' +
           'resolve, and they are restart-only.</strong> With ' +
           '<code>oid4vci.sdJwtIssuerDid</code> or ' +
           '<code>oid4vci.ldpVcIssuerDid</code> on, the issuer names itself ' +
           'with a <code>did:web</code> identifier instead of an https URL, ' +
           'so the key a verifier fetches comes from a DID document rather ' +
           'than from JWKS. They are read while the issuer metadata is being ' +
           'built, which is why they cannot be changed while the service ' +
           'runs.',
           '<strong>Nothing in an issued credential is verified.</strong> ' +
           'The values come off the holder\'s directory entry and the entry ' +
           'is created for anybody who asks; a diploma issued here says ' +
           'whatever <a href="/admin/vc">Credential claims</a> selected. ' +
           'What is real is the signature, the proofs and the formats — ' +
           'which is the half a wallet has to get right.'],
    links: [['/issuer', 'the issuer, for a person'],
            ['/issuer/offer', 'a Credential Offer'],
            ['/.well-known/openid-credential-issuer', 'the issuer metadata'],
            ['/admin/vc', 'what a credential carries']] },

  { path: '/admin/oid4vp', title: 'OpenID4VP',
    lead: '<strong>The mock Verifier at <code>/oid4vp/verifier</code>, and ' +
          'the settings around its request.</strong> The DCQL query — which ' +
          'credential, which claims — is ' +
          '<a href="/admin/vc-verifier-config">Verifier request</a> next ' +
          'door; these settings are the client identifier it presents as, ' +
          'where it sends a holder to present, how fresh a Key Binding JWT ' +
          'has to be, the claims it asks for when nothing else has been ' +
          'chosen — the settings that govern signing in with a wallet, and ' +
          'how long a status list another issuer published is kept.',
    also: ['<strong>A presentation can sign somebody in, at <code>' +
           '/authn/wallet</code>.</strong> <code>oid4vp.signIn</code> offers ' +
           '"Sign in with a wallet" on the sign-in screen: a holder-bound ' +
           'credential <em>this realm</em> issued — in any format ' +
           '<code>oid4vp.signInFormats</code> names: SD-JWT VC, JWT VC or ' +
           'LDP VC — on an access token it verified and nobody has ' +
           'disowned since, presented with a fresh holder proof, signs in ' +
           'the directory entry it was issued for, with <code>amr ' +
           '["pop"]</code> (and <code>hwk</code>, and <code>acr ' +
           '"mfa"</code>, where a verified key attestation says so). A ' +
           'credential from a trusted foreign issuer, another realm or a ' +
           'foreign access token still verifies, is recorded on ' +
           '<a href="/admin/users">/admin/users</a> as a presentation, and ' +
           'signs nobody in. The bar door at <code>/oid4vp/verifier</code> ' +
           'signs nobody in whatever it is shown: nobody there asked to be ' +
           'signed in.',
           '<strong>The Digital Credentials API is the default way in, and ' +
           'the plain QR code is off.</strong> A browser that has the API ' +
           'asks a wallet on this device or a nearby one, which the browser ' +
           'checks is near it; <code>oid4vp.signInDcApiResponseMode</code> ' +
           'decides whether the answer is encrypted. <code>' +
           'oid4vp.signInCrossDevice</code> adds a plain QR code for a ' +
           'wallet the browser cannot reach — the one path somebody can ' +
           'relay to a victim, which is why it is off.',
           '<strong>Every credential\'s status is consulted.</strong> One ' +
           'this realm issued is read from <a href="/admin/vc-status">' +
           'Credential status</a>; one a trusted foreign issuer signed has ' +
           'its status list fetched, kept for its ttl and at most <code>' +
           'oid4vp.statusListMaxCacheS</code>, and a credential whose status ' +
           'cannot be read is refused.'],
    links: [['/oid4vp/verifier', 'the verifier, for a person'],
            ['/admin/vc-verifier-config', 'what it asks for']] },

  { path: '/admin/kerberos', title: 'Kerberos settings',
    lead: '<strong>The KDC on raw TCP and UDP 88, the same exchange over ' +
          'MS-KKDCP at <code>/KdcProxy</code>, the Kerberos-protected ' +
          'service, and SPNEGO over HTTP.</strong> Most of these settings ' +
          'are restart-only and the reason is one fact: the principal ' +
          'database — every long-term key in it — is built from them when ' +
          'the process starts, so a realm or a password changed at runtime ' +
          'would leave every existing ticket undecryptable by the service ' +
          'that issued it.',
    also: ['<strong>This is the one family here that cannot be permissive ' +
           'the way the rest of this service is.</strong> A Kerberos ' +
           'password IS the key: pre-authentication and the AS-REP\'s ' +
           'enc-part are both encrypted under it, so a KDC that accepted ' +
           'anything would still have to pick a key the client could not ' +
           'guess. The permissiveness moved into the ACCOUNT POLICY instead ' +
           '— one password shared by every user account ' +
           '(<code>krb5.userPassword</code>), an account created for any ' +
           'name on first sight — and the acceptor still decrypts a real ' +
           'ticket under a real key and refuses a replay. So the ' +
           'verification is real and the account policy is not, and those ' +
           'are two different sentences.',
           '<strong><code>krb5.unknownUsers</code> is how a client\'s error ' +
           'path is reached.</strong> Every other name gets an account, so ' +
           'these are the only ones that can produce ' +
           '<code>KDC_ERR_C_PRINCIPAL_UNKNOWN</code>. ' +
           '<code>krb5.clockOffset</code> is the same device for skew: it ' +
           'moves this KDC\'s idea of now so that a client can be shown ' +
           '<code>KRB_AP_ERR_SKEW</code> without anybody touching a system ' +
           'clock.',
           '<strong><code>krb5.spnegoAuthentication</code> is a door and not ' +
           'a mode.</strong> With it on, a ticket presented at ' +
           '<code>/authn/spnego</code> starts a browser sign-on session — ' +
           'the same session <code>/oauth2/authorize</code>, ' +
           '<code>/wsfed</code>, <code>/saml2/sso</code> and this console ' +
           'read — which makes Kerberos an authentication mechanism for ' +
           'every application here. With it off that endpoint answers 403 ' +
           'naming this setting, and <code>/spnego/protected</code> still ' +
           'performs the whole handshake and gives no session.'],
    links: [['/krb5/principals', 'the principal database'],
            ['/krb5/service', 'the protected service'],
            ['/spnego', 'what SPNEGO is, for a person'],
            ['/authn/spnego', 'sign in with a ticket'],
            ['/admin/delegation', 'what S4U and a forwarded TGT did here']] },

  // -------------------------------------------------------------------------
  // /admin/persistence — THE PAGE THAT REVERSES THE OLDEST CLAIM IN THIS
  // REPOSITORY.
  //
  // Every document here said this service persists nothing and that everything
  // is gone on restart. Since 2026-08-27 three things are not: the embedded
  // directory, the trust realm registry and the runtime appconfig overrides —
  // and since 2026-09-06, in product mode on postgres, what this service MINTS
  // (persistence/CLAUDE.md has the current list). The page says which is which
  // rather than leaving a reader to infer a boundary — because "it persists
  // now" is exactly the kind of half-sentence that gets somebody expecting
  // their access token back.
  //
  // IT IS IN THIS TABLE RATHER THAN BEING A PAGE OF ITS OWN because it is
  // overwhelmingly a settings page: appconfig rows (six when it was written,
  // nineteen `persistence.*` now) and the sentences that
  // explain them, which is what every other row here is. What it has that they
  // do not is a STATUS block, and that is one optional member on the row rather
  // than a second kind of page — see protocolSettingsJson().
  // -------------------------------------------------------------------------
  { path: '/admin/persistence', title: 'Persistence',
    lead: '<strong>Whether anything here survives a restart, and where it is ' +
          'written.</strong> Three things can be: the embedded LDAP ' +
          'directory — which is also where applications, federation ' +
          'relationships and the SPIFFE registry live — the trust realm ' +
          'registry, and the runtime setting changes made on pages like this ' +
          'one — and, in <strong>product mode</strong> on a Postgres store, ' +
          'a fourth: everything this process MINTS. ' +
          '<code>persistence.mode</code> decides where: <code>memory</code> ' +
          'writes nothing and is what this service did until 2026-08-27, ' +
          '<code>ldif</code> writes an RFC 2849 file per realm and needs no ' +
          'database, <code>postgres</code> writes six tables.',
    also: ['<strong>WHAT THIS SERVICE MINTS IS PERSISTED IN PRODUCT MODE, ' +
           'AND IN NO OTHER CONFIGURATION.</strong> Sessions, access tokens, ' +
           'ID Tokens, refresh tokens, authorization codes, pre-authorized ' +
           'codes, SAML artifacts, Kerberos principals and tickets, the ' +
           'replay caches, <a href="/admin/metrics">the statistics</a> and ' +
           '<a href="/admin/audit">the audit log</a> all survive a restart ' +
           'when <code>global.mode</code> is <code>product</code> and the ' +
           'store is <code>postgres</code>. Every row is encrypted with the ' +
           'same key-encryption key that protects the signing keys, because ' +
           'a session id is a cookie value and an authorization code is ' +
           'redeemable — so a database dump is not a set of usable ' +
           'credentials.',
           '<strong>DEVELOPMENT MODE PERSISTS NONE OF IT, and that is not ' +
           'unfinished either.</strong> <strong>The signing key is ' +
           'regenerated on every start there</strong>, so a restored token ' +
           'would verify against nothing and an assertion restored from a ' +
           'disk would be a lie. Product mode keeps its keys — which is why ' +
           'it requires a store — and that single fact is what makes ' +
           'restoring the rest of it honest. The <code>ldif</code> store ' +
           'does not hold minted state either, whatever the mode: it writes ' +
           'WHOLE FILES per flush, which is right for a directory somebody ' +
           'types into and wrong for a session table that changes on every ' +
           'request. The service says so once at startup rather than letting ' +
           'it be discovered one restart later.',
           '<strong>SEVERAL PROCESSES AGAINST ONE STORE NOW AGREE, and this ' +
           'paragraph said the opposite until 2026-09-06.</strong> It read ' +
           '"persistence is not coordination… one process per database", and ' +
           'that was the honest description of what existed. Every change is ' +
           'now written to a monotonic log INSIDE the transaction that made ' +
           'it, and each process applies what the others committed — the ' +
           'directory, the realms, the settings and the minted rows alike. A ' +
           '<code>LISTEN</code>/<code>NOTIFY</code> nudge only makes that ' +
           'prompt: <strong>the log is the contract</strong>, so a missed ' +
           'notification costs latency and never a change. ' +
           '<code>persistence.coordinate</code> turns it off, which is what ' +
           'this service did before.',
           '<strong>It shares STATE and not SOCKETS, and the difference is ' +
           'where the surprises are.</strong> The KDC, the two LDAP ' +
           'listeners, the main port and SPIFFE\'s four are bound per ' +
           'process and always will be. And the <strong>replay caches and ' +
           'DPoP <code>jti</code> sets converge rather than ' +
           'synchronise</strong>: between a write in one process and its ' +
           'arrival in another there is a window the size of ' +
           '<code>persistence.pollInterval</code> in which a proof one ' +
           'process refused is accepted by another. Sticky sessions at the ' +
           'load balancer close it; nothing here does.',
           '<strong>A restored person shows on ' +
           '<a href="/admin/users">Users</a> as restored rather than as ' +
           'having authenticated.</strong> They exist — an entry, searchable ' +
           'over 389, readable over SCIM, and a token issued to them carries ' +
           'their attributes — and they have not signed in during THIS ' +
           'process, so they are not counted among the sign-ins. The counts, ' +
           'the protocols and the per-person event list are statistics and ' +
           'start at zero with everything else.',
           '<strong>A write that fails is logged and never thrown.</strong> ' +
           'If the database goes away, the LDAP operation that triggered the ' +
           'write still succeeds, this service keeps answering out of ' +
           'memory, and the status below turns red with the reason. Nothing ' +
           'is lost by the failure: the next change recomputes the same ' +
           'difference and writes it. A database outage taking down ' +
           'seventeen protocol families that do not need a database is the ' +
           'one failure mode a mock must not have. <strong>The same is true ' +
           'of coordination</strong>: a process that cannot read the change ' +
           'log keeps answering out of its own copy, says so, and catches up ' +
           'when the database comes back — it does not refuse anything in ' +
           'the meantime.'],
    status: slot.forward('persistenceStatusBlock'),
    links: [['/admin/ldap/service', 'the directory, and this same status'],
            ['/admin/ldap/directory', 'every entry in it'],
            ['/admin/realms', 'the realms that are written down with it'],
            ['/admin/config', 'the whole settings table'],
            ['/admin/users', 'the people, restored and otherwise']] },

  // -------------------------------------------------------------------------
  // /admin/cluster (2026-09-14, #46) — several containers against one store.
  // A settings page with a status block, for /admin/persistence's reason: the
  // five cluster.* settings, and what the membership, the leases and the
  // capability table actually say right now.
  // -------------------------------------------------------------------------
  { path: '/admin/cluster', title: 'Cluster',
    lead: '<strong>Whether several copies of this service against one ' +
          'postgres store behave as one service.</strong> Inside one ' +
          'container the processes agree because the front process ' +
          'coordinates them; between containers the only link used to be the ' +
          'change log, so a second container started, looked healthy and ' +
          'gave wrong answers — a code redeemed twice, a signing key one ' +
          'node did not publish, a revocation another node\'s save threw ' +
          'away. <code>cluster.mode</code> decides what happens instead: ' +
          '<code>active-passive</code> (the default in product mode on ' +
          'postgres) lets ONE node serve and makes the others wait before ' +
          'they restore or bind anything; <code>active-active</code> lets ' +
          'every node serve and refuses to start while anything in the ' +
          'capability table below is missing.',
    also: ['<strong>EVERY WRITE IS FENCED.</strong> A clustered node renews ' +
           'a membership row by the database\'s clock, and every transaction ' +
           'it opens checks that row — and, in active-passive mode, the ' +
           'service lease at the token it was acquired with — before it ' +
           'writes anything. A node that paused past its lifetime, lost its ' +
           'lease or lost its database EXITS rather than carrying on, ' +
           'because a node that has lost its right to write and keeps ' +
           'running would try again on every change.',
           '<strong>A TAKEOVER IS ONE HEARTBEAT AFTER A CLEAN STOP</strong> ' +
           '— a stopping node releases its leases — and one lifetime ' +
           '(<code>cluster.nodeTtlMs</code>) after a crash. A standby has ' +
           'restored nothing while it waited, so it restores the store when ' +
           'it takes over; that is seconds, and it is the price of a standby ' +
           'that can never write to a store another node owns.',
           '<strong>THE SETTINGS EVERY NODE MUST SHARE ARE CHECKED.</strong> ' +
           'The Kerberos keys, the mode, the public address and the rest of ' +
           'the list below are compared, as a digest keyed by the ' +
           'key-encryption key, with every live node\'s at startup; a node ' +
           'that differs does not start, because two nodes with different ' +
           'krbtgt keys seal tickets neither can open for the other.'],
    status: slot.forward('clusterStatusBlock'),
    links: [['/admin/persistence', 'the store the cluster is built on'],
            ['/admin/database', 'the database itself'],
            ['/admin/secrets', 'where the key-encryption key comes from'],
            ['/admin/config', 'the whole settings table']] },

  { path: '/admin/ldap', title: 'LDAP / LDAPS',
    lead: '<strong>The embedded directory: RFC 4511 on raw TCP 389, and the ' +
          'same handlers behind TLS on 636.</strong> One store behind both, ' +
          'and it is not a copy of anything — it IS where people, groups, ' +
          'applications, federation relationships and the SPIFFE registry ' +
          'live, so what a SCIM POST writes and what an <code>ldapadd</code> ' +
          'writes are one entry.',
    also: ['<strong>No bind is ever refused.</strong> Any DN, any password, ' +
           'anonymous, on 389 and 636 alike — there is no setting below that ' +
           'changes it, which is why none of them is called ' +
           '<code>authRequired</code>. What the directory is for here is ' +
           'being READ by a client that expects a directory, and a bind that ' +
           'failed would only ever stop that.',
           '<strong>The base DN is the DEFAULT realm\'s directory, and every ' +
           'other realm is a subtree beneath it.</strong> A search scoped to ' +
           '<code>dc=example,dc=com</code> answers from the default realm ' +
           'only; <code>-b "dc=acme,dc=example,dc=com"</code> answers from ' +
           'acme\'s. The realm is in the DN because a socket has no path to ' +
           'put a segment in — see <a href="/admin/realms">Trust realms</a>.'],
    links: [['/admin/ldap/service', 'the directory, and whether both sockets ' +
                                    'came up'],
            ['/admin/ldap/directory', 'every entry, as a tree'],
            ['/admin/users', 'the people in it'],
            ['/admin/groups', 'the groups in it']] },

  { path: '/admin/wstrust', title: 'WS-Trust',
    lead: '<strong>The security token service at <code>/sts</code>, ' +
          'WS-Trust 1.0 through 1.4.</strong> It answers ' +
          '<code>RequestSecurityToken</code> over SOAP for Issue, Validate, ' +
          'Renew and Cancel, in whichever of the four namespace versions the ' +
          'request used, and it will issue a SAML 1.1 or a SAML 2.0 ' +
          'assertion. One setting is its own: who its tokens say issued them.',
    also: ['<strong>What an assertion CONTAINS is configured on the SAML ' +
           'pages</strong>, because it is built by the same two functions ' +
           'the two identity providers use — <a ' +
           'href="/admin/saml-attributes">Custom SAML attributes</a> adds ' +
           'attributes to it, and <code>saml.issuer</code> on <a ' +
           'href="/admin/saml2">SAML 2.0</a> is the Issuer inside the ' +
           'assertion. <code>wstrust.issuer</code> here is the token ' +
           'service\'s own name and they share a default rather than being ' +
           'one setting: they were one until they had to differ, which is ' +
           'the kind of thing that is discovered the hard way.',
           '<strong>Nothing about a request is checked.</strong> An ' +
           '<code>OnBehalfOf</code> or <code>ActAs</code> element names ' +
           'anybody and gets an assertion for them — this service polices no ' +
           'delegation in WS-Trust, which <a href="/admin/delegation">' +
           'Delegation</a> says beside every act it recorded.'],
    links: [['/sts', 'the token service, for a person'],
            ['/sts/cert', 'the certificate its assertions verify against'],
            ['/admin/saml-attributes', 'what goes into the assertion'],
            ['/admin/delegation', 'what was asked for on whose behalf']] },

  { path: '/admin/wsfed', title: 'WS-Federation',
    lead: '<strong>The passive requestor profile at <code>/wsfed</code>, ' +
          'WS-Federation 1.2.</strong> A browser arrives with ' +
          '<code>wa=wsignin1.0</code> and leaves with a SAML 1.1 assertion ' +
          'in a self-submitting form; <code>wa=wsignout1.0</code> ends the ' +
          'session. One setting is its own — the entity ID its metadata and ' +
          'its responses name this service by.',
    also: ['<strong>The assertion is a SAML 1.1 one, so its issuer and its ' +
           'attributes are configured next door.</strong> ' +
           '<code>saml.issuer</code> is on <a href="/admin/saml11">SAML ' +
           '1.1</a> and <a href="/admin/saml2">SAML 2.0</a>, and it is the ' +
           'same setting in both places; <a ' +
           'href="/admin/saml-attributes">Custom SAML attributes</a> decides ' +
           'what the assertion carries. This page links to them rather than ' +
           'drawing a third form onto one value.',
           '<strong>Single sign-on with OAuth 2.0 / OIDC is the point of the ' +
           'require order.</strong> This module is loaded after the ' +
           'authorization server so that both read one session: sign in at ' +
           '<code>/oauth2/authorize</code> and <code>/wsfed</code> knows it, ' +
           'and the other way round.',
           '<strong><code>wauth</code> is honoured, and ' +
           '<code>wreqptr</code> is never dereferenced.</strong> A relying ' +
           'party that demands a security key or two factors the session ' +
           'does not have sends the person to sign in again with the second ' +
           'factor required (a step-up, since 2026-09-17), and is refused ' +
           'only if that one attempt still does not produce it. And this ' +
           'service fetches nothing from a URL a request names.'],
    links: [['/wsfed', 'the profile, for a person'],
            ['/wsfed/rp', 'the mock relying party'],
            ['/FederationMetadata/2007-06/FederationMetadata.xml', 'its ' +
                'metadata'],
            ['/admin/saml-attributes', 'what goes into the assertion']] },

  { path: '/admin/tls', title: 'TLS / mutual TLS',
    lead: '<strong>This service has no TLS listener of its own since ' +
          '2026-09-16: 8443, which asked every connection for a client ' +
          'certificate, and 9443, which required one at the handshake, were ' +
          'both deleted.</strong> The main port already asks and requires ' +
          'none, so the first was a second socket with the same posture as ' +
          'the one every other protocol answers on; what this page ' +
          'configures is the certificate that port and LDAPS 636 present, ' +
          'and what this service makes of a certificate a CLIENT presents.',
    also: ['<strong>A verified client certificate IS a login since ' +
           '2026-09-05, and it is now <a href="/tls/sign-in">GET ' +
           '/tls/sign-in</a> on the main port.</strong> Presenting a ' +
           'certificate is the CLIENT\'s decision here — this port asks for ' +
           'one and requires none — and the route signs in whoever presented ' +
           'a verified one, for its common name, with the response carrying ' +
           'the cookie. What verification proved is unchanged: a chain to an ' +
           'anchor somebody POSTed to <code>/tls/trust</code>. No token is ' +
           'issued. The session is on <a ' +
           'href="/admin/sessions">Sessions</a> and ends like any other.',
           '<strong>The handshake refusal 9443 performed has no successor, ' +
           'and that is deliberate.</strong> Refusing during the handshake ' +
           'is a property of a SOCKET, and this socket carries OAuth, SAML, ' +
           'SCIM, the console and everything else, so it cannot refuse every ' +
           'caller who has no certificate. A certificate that does not ' +
           'verify is refused where it is USED instead: RFC 8705 client ' +
           'authentication at the token endpoint, <code>/xacml</code>, ' +
           '<code>/scim/v2</code> and the sign-in above.',
           '<strong>One self-signed certificate, regenerated on every start, ' +
           'is shared by two sockets</strong> — LDAPS 636, and the main port ' +
           'when <code>global.https</code> is on — so a caller trusts this ' +
           'service once rather than twice. The names and ' +
           'addresses below are what goes into it, which is why they are ' +
           'restart-only: it is minted before anything is listening.',
           '<strong>Whether the MAIN port is HTTPS is not here.</strong> ' +
           '<code>global.https</code> is on <a ' +
           'href="/admin/config">Configuration</a> with the rest of the ' +
           'process\'s own settings, and it defaults to whatever ' +
           '<code>oauth2.rfc9700</code> is — RFC 9700 section 2.1 says an ' +
           'authorization response must not travel over an unencrypted ' +
           'connection, and the authorization endpoint is on that port.'],
    links: [['/tls', 'the certificate, the truststore and the main port\'s ' +
                'posture'],
            ['/tls/trust', 'the certificate, to trust it'],
            ['/tls/forwarded', 'what a proxy said about the connection'],
            ['/admin/spiffe', 'the one place a certificate authenticates']] }
];


// ---------------------------------------------------------------------------
// GET /admin/federation, POST /admin/federation
//
// THE ONE PAGE IN THIS CONSOLE THAT CONFIGURES A REFUSAL.
//
// Every other page here either reports what happened or widens what this
// service will accept. This one is the opposite in both directions, and the
// page says so at the top rather than leaving it to be discovered: a
// relationship is created DISABLED, an assertion is refused unless it verifies
// against the certificate configured on it, and a relationship that is enabled
// but half-configured refuses rather than half-working.
//
// It reads and writes ONE store — `ou=federations`, through
// `federation/federation.js` — and holds nothing of its own, like every other
// page in this file. What it deliberately does NOT hold is anything the
// applications registry already holds: an identity-provider-side relationship
// NAMES an application and stops, so its entityID, its redirect URIs and its
// certificate stay on `/admin/applications` where every protocol module reads
// them.
// ---------------------------------------------------------------------------
let FEDERATION_CAVEAT: string;
WIRE_STEPS.push(function (instance: AdminConsole): void {
  FEDERATION_CAVEAT =
    instance.note('<strong>This is the one feature here that has to be ' +
    'configured before it will do anything, and the one page in this console ' +
    'that configures a REFUSAL.</strong> Everywhere else this service ' +
    'accepts what it is given — any username, any client_id, any entityID, ' +
    'any LDAP bind. It cannot do that at an assertion consumer service: what ' +
    'arrives there is an unauthenticated HTTP request claiming to be a ' +
    'person, and the session it would produce is the same one ' +
    '<code>/oauth2/authorize</code>, <code>/wsfed</code>, ' +
    '<code>/saml2</code> and this console all read. A permissive version of ' +
    'it would not be a permissive mock; it would be an ' +
    'authentication bypass for every protocol in this process.') +
    instance.note('<strong>The gate is on the SIGNER, not on the ' +
    'subject.</strong> Once a relationship is configured and enabled, ' +
    'everything downstream is as permissive as the rest of this service: any ' +
    'username in the assertion is accepted, any attribute is mapped, nothing ' +
    'about the person is checked, and a directory entry is created for them.');
});

const FEDERATION_LINKS =
  '<p class="sub"><a href="/admin/federation/map">the picture</a> &middot; ' +
  '<a href="' + federation.PATHS.base + '">the federation index</a> &middot; ' +
  '<a href="/admin/applications">the applications registry</a> &middot; <a ' +
  'href="/admin/users">who has signed in</a> &middot; <a ' +
  'href="/admin/ldap/federations">the register as the directory sees ' +
  'it</a></p>';

// ROUTES ARE REGISTERED BY THE COMPOSITION ROOT (#50, R1): requiring this
// module no longer registers anything. `common/protocol_stack.ts` calls the
// exported `registerRoutes(app)` at the point in the route order where
// requiring this module used to register them.

// Standalone, build the default now, as loading this module always did.
slot.buildNowUnlessDeferred();

const consoleExports = {
  registerRoutes: slot.forward('registerRoutes'),
  AdminConsole: AdminConsole,
  installInstance: (instance: AdminConsole): void =>
    slot.install(instance),
  instanceOrigin: (): string => slot.origin(),
  // THE SHELL, written for the first module outside this file that drew a
  // console page (many do now — `crypto_metadata.js`, `pki_admin.js`, the
  // `*_admin.js` of several families, `ldap_server.js`):
  // `../sts_metadata.js`, which builds `/admin/sts-metadata` from the live
  // express router and cannot live in here (it must be the LAST module
  // server.js loads, or it would be the reason a route is missing from its own
  // list). `respond()` is what it calls; `page()` is exported beside it because
  // a caller that needed the shell without the ?format=json half would
  // otherwise reimplement respond() badly. Neither decides anything: what that
  // page SAYS is entirely that module's.
  respond: slot.forward('respond'),
  // The notice or error a redirect brought back, for the family pages drawn
  // outside this file (EST, SCEP, GNAP). Unexported until #70, so those pages
  // tested for it, found nothing and never showed one.
  messagesOf: slot.forward('messagesOf'),
  // For `tests/protocol_endpoints.js`: which Protocols pages the endpoint
  // table and `SECTIONS` disagree about. See above respond().
  protocolEndpointDrift: slot.forward('protocolEndpointDrift'),
  page: slot.forward('page'),
  // AND FOR A SECOND MODULE SINCE THE XACML WORK: `xacml/xacml_admin.ts`
  // draws the /admin/xacml pages (four then, six now) the way
  // `ldap/ldap_server.js` draws its (then five, now eight). Those two helpers
  // were private only because nothing outside this file had needed them — a
  // page drawn elsewhere still wants the settings
  // block for its own group, and still has to answer a form POST the way every
  // other action here does. Keeping them private would have meant a second
  // settings renderer and a second redirect-or-JSON rule, and two of either is
  // how a console starts behaving differently on different pages.
  configFormsFor: slot.forward('configFormsFor'),
  setXacmlPages: slot.forward('setXacmlPages'),
  xacmlActionNames: slot.forward('xacmlActionNames'),
  // The JSON counterpart of the block above, so that a page drawn
  // elsewhere can answer ?format=json with the SAME settings it just
  // rendered. `protocolSettingsJsonFor()` is not that function — it is
  // keyed by PROTOCOL_SETTINGS_PAGES and throws for a path that table
  // does not carry, which is correct for the pages this file generates
  // and wrong for one somebody else draws.
  configSettingsJson: slot.forward('configSettingsJson'),
  // ---------------------------------------------------------------------
  // THE ACTIONS ARE NOT HERE ANY MORE (2026-09-12). All thirty-one live in
  // `admin-core/admin_actions.ts`, which `mgmt-api/admin_api.ts` requires
  // directly — so the management API no longer reaches its decisions through
  // the console module.
  //
  // They are deliberately NOT re-exported from here. Aliasing them back into
  // this file's scope (see the block below the requires) is what keeps the
  // several hundred call sites in this file unchanged; re-exporting them as
  // well would publish a SECOND way to reach the same function, and the
  // second way is the one that goes stale.
  // ---------------------------------------------------------------------
  respondToAction: slot.forward('respondToAction'),
  // For `admin-ui/pki_admin.ts`, whose key-pair controls are drawn on an
  // application's page too — see applicationReturnTo().
  applicationReturnTo: slot.forward('applicationReturnTo'),
  userReturnTo: slot.forward('userReturnTo'),
  // And the trail a page drawn there hangs under, so the one-time key page an
  // issue from a person's own page answers with is `Users › Signing key pair`
  // rather than a page with no way up.
  upTo: slot.forward('upTo'),
  // THE FOLDS AND THE TOOLTIPS, for the same one module. They are exported for
  // the reason page() is: `sts_metadata.js` draws a console page, and a page
  // drawn in this console's shell whose prose did not fold would be the one
  // page here that is still a wall of text — which is exactly what it was,
  // being the longest page in the service. See the block above note().
  note: slot.forward('note'),
  warn: slot.forward('warn'),
  bullet: slot.forward('bullet'),
  tip: slot.forward('tip'),
  // Filled by ldap_server.js at its require time; see the note above it.
  setDirectoryReader: slot.forward('setDirectoryReader'),
  // THE NINTH SLOT, filled by the same module, and the views behind it. See
  // the block above setDirectoryPages().
  setDirectoryPages: slot.forward('setDirectoryPages'),
  // ---------------------------------------------------------------------
  // THE LIST-PAGE FURNITURE, WRITTEN FOR THE FIRST MODULE OUTSIDE THIS
  // DIRECTORY THAT DREW LIST PAGES IN THIS SHELL (the certificate-enrollment,
  // GNAP and OAuth monitor pages use it too now).
  //
  // `ldap/ldap_server.js` has drawn its /admin/ldap/* pages since 2026-09-01
  // (five then, eight now), and they are real list pages: the directory dump
  // is one row per entry over a store with a cap in the hundreds, so it pages
  // exactly the way
  // /admin/applications and /admin/tokens page. Exported for the same reason
  // page(), note() and tip() are exported to sts_metadata.js — a page drawn
  // in this console's shell with a paging control of its own invention would
  // be the one control here that behaves differently, and the reader would
  // have no way to know which one it was.
  //
  // What crosses is the FURNITURE and never the data: nothing in this list
  // decides what a page contains. `esc` goes with them because every one of
  // them returns markup and a caller building a cell beside them needs the
  // same escaping — ldap_server.js's own xmlEscape() is the same function,
  // and passing this one keeps a single answer to "what is escaped how" on
  // pages that mix the two.
  // ---------------------------------------------------------------------
  esc: slot.forward('esc'),
  tile: slot.forward('tile'),
  clipped: slot.forward('clipped'),
  clippedValues: slot.forward('clippedValues'),
  pageNavPair: slot.forward('pageNavPair'),
  // For `admin-ui/pki_admin.ts`, whose two key-pair tables share one `per`
  // (2026-09-13) — the same control every multi-list page here draws.
  perPageForm: slot.forward('perPageForm'),
  perPageOptions: slot.forward('perPageOptions'),
  queryWith: queryWith,
  // Filled by spiffe_server.js at its require time, for the reason beside the
  // requires at the top: this file must not require that module.
  setSpiffeReader: slot.forward('setSpiffeReader'),
  setScimReader: slot.forward('setScimReader'),
  setGroupReader: slot.forward('setGroupReader'),
  setDirectoryWriter: slot.forward('setDirectoryWriter'),
  setGroupWriter: slot.forward('setGroupWriter'),
  // Filled by logout/logout.ts at ITS require time — the sixth slot, and rule
  // 3e's test answers yes for the same two reasons at once. See the block above
  // setLogoutReader().
  setLogoutReader: slot.forward('setLogoutReader'),
  // Filled by ./crypto_metadata.js at ITS require time — the seventh, and the
  // third to pass rule 3e's test both ways round. See the block above
  // setCryptoReporter().
  setCryptoReporter: slot.forward('setCryptoReporter'),
  // Filled by ../ssf/ssf.ts at ITS require time — the eighth, and the fourth
  // to pass rule 3e's test both ways round. See the block above
  // setSignalsReporter().
  setSignalsReporter: slot.forward('setSignalsReporter'),
  // Filled by ../common/protocol_stack.ts on the line after it requires
  // ../tls/tls_server.js — the thirteenth, and the one slot here NOT filled by
  // the module that owns what it carries. See the block above setTruststore().
  setTruststore: slot.forward('setTruststore'),
  // The Shared Signals page's view and its four actions, for admin_api.js.
  // Rule 7: the API calls exactly these, so an action added to that switch is
  // most of adding it there. `ssfAction` RESOLVES rather than returning — it
  // is the only action function in this console that does, because
  // transmitting a Security Event Token signs a JWS and POSTs it to somebody
  // else's endpoint.
  ssfActionNames: function () {
    log.debug("Entering ssfActionNames().");
    log.debug("Leaving ssfActionNames().");
    return signalsReporter ? signalsReporter.actions.slice() : [];
  },
  // Filled by ../ssf/ssf.ts at ITS require time — the ninth, and the fifth to
  // pass rule 3e's test both ways round. A SECOND slot rather than more
  // members on the eighth, because "what streams exist" and "what has been
  // said about which session" are two questions with two pages, and one
  // object carrying both would make /admin/ssf fail whole when the CAEP half
  // was not installed. See the block above setCaepReporter().
  setCaepReporter: slot.forward('setCaepReporter'),
  // THE TENTH SLOT, filled by the same module for RISC. See
  // setRiscReporter()'s header on why it is a third object rather than
  // more members on the ninth.
  setRiscReporter: slot.forward('setRiscReporter'),
  // The CAEP view and its three actions, for admin_api.js. Rule 7: the API
  // calls exactly these. `caepAction` RESOLVES, like `ssfAction` and for the
  // same reason — emitting a CAEP event signs a JWS and POSTs it.
  // EVERY LIVE SESSION, for admin_api.js — the same function the page draws
  // from, so the console and the management API cannot come to disagree about
  // who is signed in. It takes the whole request because the filter and the
  // paging are query parameters, which is the shape every list view here has.
  // The sessions register as /admin/caep-sessions draws it — the list, or one
  // session with `?session=`. Rule 7: that page is a page of this console and
  // owed an operation of its own.
  caepActionNames: function () {
    log.debug("Entering caepActionNames().");
    log.debug("Leaving caepActionNames().");
    return caepReporter ? caepReporter.actions.slice() : [];
  },
  // The RISC view and its three actions, on exactly the CAEP pair's terms.
  // `riscAction` RESOLVES for the same reason: emitting a RISC event signs a
  // JWS and POSTs it.
  // The account register as /admin/risc-accounts draws it — the list, or one
  // account with `?account=`.
  riscActionNames: function () {
    log.debug("Entering riscActionNames().");
    log.debug("Leaving riscActionNames().");
    return riscReporter ? riscReporter.actions.slice() : [];
  },
  // The crypto report, for admin_api.js. One function, so the page and the API
  // cannot disagree about what this service's cryptography is.
  // The key inventory and the export, for admin_api.js. Two functions rather
  // than one because LISTING what this process holds and HANDING A KEY OVER
  // are different acts, and only the second needs Admin Write.
  // For crypto_metadata.js's export route, which is the only caller.
  mayWrite: slot.forward('mayWrite'),
  // The sign-out page's view and its four actions, for admin_api.js. Rule 7:
  // the API calls exactly these, so an action added to that switch is most of
  // adding it there — and the refusal sentence that names the four is what the
  // repository's own tests/vendored/admin_api.js reads to check the parity.
  logoutView: slot.forward('logoutView'),
  jtiFrom: jtiFrom,
  // The four action functions. admin_api.js calls exactly these — it decides
  // nothing about a revocation or a claim that this console does not — which is
  // what makes "every /admin control has an /admin-api operation" a property of
  // the code rather than a promise in a comment.
  // The trust realm registry's five writes and its whole view, for
  // mgmt-api/admin_api.ts. Rule 7 — every page of this console and every action
  // of its handlers has an operation on /admin-api, driven through the SAME
  // function, so that a realm created over the API and a realm created on the
  // form cannot come to mean two different things.
  // The token-lifetimes page's own action. It writes through config.js like
  // configAction does — see the header above it for why it is a page of its own
  // and not four more rows on /admin/config — and it is exported separately
  // because rule 7 is about the CONTROL: the form on that page has two actions,
  // so the API has two operations, and pointing them at configAction instead
  // would give a caller a door that took any setting config.js holds under a
  // name that promised six (`TOKEN_LIFETIME_KEYS`).
  // THE TWO NARROW DOORS' KEY LISTS, for mgmt-api/admin_api.ts to BUILD their
  // request schemas from rather than to keep a second copy of.
  //
  // Both had gone stale in exactly the way a second copy does. The document
  // named four token-lifetime settings where the handler accepted six, and
  // THREE assertion settings where it accepted sixteen — and these are the two
  // operations whose whole claim is that they refuse anything outside their
  // own list BY NAME, so a caller reading the document is refused for
  // following it, and a caller reading the refusal finds settings the document
  // never mentioned. Derived, that cannot happen: adding a row to either table
  // adds the property.
  // The JSON views, one per page, for the same reason. See the block comment
  // The protocol settings pages (PROTOCOL_SETTINGS_PAGES — eight when this was
  // written, thirteen as of 2026-09-16), through ONE export keyed by path.
  // Rule 7 wants an operation per page, all answering the same shape — one
  // exported function per page would have been that many names for one call.
  // There is no action beside it, and that is the rule read exactly: every
  // form on those pages posts `set-many` to /admin/config, which
  // `POST /admin-api/config/set-many` already mirrors. A second POST per page
  // would be that many more doors onto one function.
  protocolSettingsJsonFor: slot.forward('protocolSettingsJsonFor'),
  // Where each group of settings is drawn, for the API's own /config resource
  // and for anything that wants to send a person to the right page.
  settingHomes: function () {
    log.debug("Entering settingHomes().");
    const instance = slot.get();
    log.debug("Leaving settingHomes().");
    return SETTING_HOMES.map(function (row) {
      return { group: row.group, pages: row.pages,
               labels: row.pages.map(instance.labelOfPath.bind(instance)) };
    });
  },
  // above consoleJson().
  consoleJson: slot.forward('consoleJson'),
  // The audit log's view is the whole function rather than a JSON builder, for
  // the reason the block above consoleJson() gives: the filtering and the
  // paging are work both the page and the API need, and two copies of it would
  // be two answers that each looked right alone. The delegation page's view,
  // and the whole function for the same reason the audit log's is: the
  // filtering, the paging and the collapse to chains are work both the page and
  // the API need. It is the second read-only resource here — rule 7 asks for an
  // operation per CONTROL, and this page has none. THE CONFIGURED HALF OF THAT
  // PAGE, and the reason the sentence above it no longer describes the whole
  // route. `/admin/delegation` has five controls now, all of them on this
  // register, so rule 7 asks for five operations — `admin_api.js` calls exactly
  // this function for all of them, which is what keeps the API from deciding
  // anything the console does not. The view is exported beside it for the same
  // reason `delegationView` is: the walk of the registry and the resolution of
  // both directions are work both need, and two copies of it would be two
  // answers that each looked right alone. The action names, read by
  // admin_api.js so that its operations and this console's switch cannot come
  // to name different things. Built from the same constant the refusal sentence
  // is built from.
  permissionActionNames: function () {
    log.debug("Entering permissionActionNames().");
    log.debug("Leaving permissionActionNames().");
    return PERMISSION_ACTIONS.slice();
  },
  // The consent register, its four actions and their names — the same three
  // exports the delegated permission register has, for rule 7's reason: the
  // management API mirrors the page and reads the action list to check it.
  consentActionNames: function () {
    log.debug("Entering consentActionNames().");
    log.debug("Leaving consentActionNames().");
    return CONSENT_ACTIONS.slice();
  },
  // The role register, its five actions, their names and the two closed
  // vocabularies they take — the same shape the consent register above has,
  // for rule 7's reason: the management API mirrors the page and reads the
  // action list to check it. `rolesPreview` is exported too, because the dry
  // run is a control on that page and rule 7 gives every control an operation.
  rolesActionNames: function () {
    log.debug("Entering rolesActionNames().");
    log.debug("Leaving rolesActionNames().");
    return ROLE_ACTIONS.slice();
  },
  roleMemberKinds: function () {
    log.debug("Entering roleMemberKinds().");
    log.debug("Leaving roleMemberKinds().");
    return ROLE_MEMBER_KINDS.slice();
  },
  // The ELEVENTH slot, filled by `xacml/xacml_role_pep.ts` at 23c. See its
  // header: a require in either direction fails rule 3e's test.
  setRolePreviewer: slot.forward('setRolePreviewer'),
  usersView: slot.forward('usersView'),
  // The create page's own view, for GET /admin-api/users/new. Rule 7, and the
  // same argument `newApplicationView` makes below: what it answers is the
  // CATALOGUE the create takes — every attribute a person here may be given,
  // with the claim each one reaches — so a caller of POST
  // /admin-api/users/create learns what may go in `attributes` from the
  // service rather than from a copy of the list in a document. There is no
  // `newUserAction` beside it, on purpose: that page's form posts
  // `action=create`, which `usersAction()` already answers, and a second
  // operation would be a second door onto one function pretending to be two.
  newUserView: slot.forward('newUserView'),
  groupsView: slot.forward('groupsView'),
  applicationsView: slot.forward('applicationsView'),
  // The create page's own view, for GET /admin-api/applications/new. Rule 7:
  // every page of this console has an operation, and this one is worth more
  // than most as an API — what it answers is the two CLOSED VOCABULARIES the
  // create takes (the kinds and the protocol families), so a caller learns what
  // it may send from the service rather than from a copy in a document. There
  // is no `newApplicationAction` beside it on purpose: this page's form posts
  // `action=create` to /admin/applications, so the operation that mirrors its
  // control already exists and a second one would be a second door onto one
  // function pretending to be two.
  newApplicationView: slot.forward('newApplicationView'),
  // The three SPIFFE views and their three action handlers. admin_api.js calls
  // exactly these — rule 7 again: the API decides nothing the console does not,
  // and an action added to one of these switches is most of adding it there.
  spiffeView: slot.forward('spiffePage'),
  spiffeEntriesView: slot.forward('spiffeEntriesView'),
  spiffeAgentsView: slot.forward('spiffeAgentsView'),
  authorizationServersView: slot.forward('authorizationServersView'),
  // The SAML 2.0 identity provider page and its four writes. Rule 7 again: the
  // API calls exactly these, so an action added to that switch is most of
  // adding it to /admin-api.
  saml2View: slot.forward('saml2View'),
  saml11View: slot.forward('saml11View'),
  // The roles page and its two writes. Rule 7 again — and this one is the page
  // most in need of the API half rather than least: `POST
  // /admin-api/rbac/grant` is gated by an access token rather than by the
  // console's roles (since 2026-09-09; `adminApi.authRequired`), so it is a
  // door onto the roster that does not depend on anybody already holding a
  // console role — which is what it was written for when the API was ungated
  // and `admin.openWhenEmpty` was off.
  rbacView: slot.forward('rbacView'),
  // `mfaView` is gone (2026-09-12): the page it belonged to split into
  // /admin/totp and /admin/webauthn and its columns moved onto /admin/users,
  // so nothing here drew from it. It is `adminViews.mfaRosterJson()` now —
  // the resource, and only the resource.
  // The federation register's page and its seven writes. Rule 7 again: the API
  // calls exactly these, so an action added to that switch is most of adding it
  // to /admin-api. This one matters more than most —
  // `POST /admin-api/federation/create`, with an access token (the API is
  // gated since 2026-09-09), is how a test configures a partner with no
  // browser at all, which is the only way the feature can be exercised
  // automatically.
  federationView: slot.forward('federationView'),
  // The gate's answer for one request, so admin_api.js's own /rbac view can say
  // WHO IS ASKING without a second reading of the cookie that could disagree
  // with this one.
  // The SAML half of the same store, as its own view, because it is its own
  // page: GET /admin-api/saml-attributes answers this and GET /admin-api/claims
  // answers the one above. There is no third function underneath them — both
  // are claimSetsJson() with a different list of set ids and the one rule that
  // is theirs alone.
  // The third view onto the same store, for GET /admin-api/userinfo-claims.
  // Same reasoning as the line above it: it is its own page, so it is its own
  // operation, and underneath the three of them is one claimSetsJson() with a
  // different list of set ids and the one rule that is each family's alone.
  // The `request` query parameter /admin/userinfo-claims previews a claims
  // request from. Exported for the reason claimsPreviewUser is: the API takes
  // the same parameter, and a second reader of that query string would be a
  // second cap.
  // Which person the claims page shows attribute values for. Exported for the
  // same reason vcPreviewUser is: GET /admin-api/claims takes the same `user`
  // parameter, and a second reader of that query string would be a second cap
  // and a second default.
  // The TRAFFIC view, for GET /admin-api/scim/monitor. Rule 7: every page
  // of this console has an operation that mirrors it, and this one has no
  // POST beside it because the page has no control — the reset was
  // refused rather than forgotten. See the header on scimMonitorJson().
  // THIS CONSOLE'S OWN SHARED SIGNALS INBOX. Rule 7: `/admin/signals` is a
  // page, so `/admin-api/signals` mirrors it, and both answer with this one
  // function — the reason every parity pair here does.
  SIGNALS_CONSOLE_ACTIONS: SIGNALS_CONSOLE_ACTIONS,
  configJson: slot.forward('configJson'),
  // It takes the REQUEST, unlike most of the views here, and for a reason worth
  // the line: every URL it prints is built from the one the call arrived on —
  // that is what makes a realm's base URL correct through a published port, on
  // a compose network and behind a proxy alike, and a snapshot built without a
  // request could only ever name localhost.
  // A body field that may appear more than once. Exported because the
  // management API takes the same two spellings of a list (`attribute` and
  // `attributes`), and reading a repeated form field is not something
  // helpers.parseBody() can answer.
  listField: slot.forward('listField'),
  // The console's own paging rules, so that /admin-api reports and clamps `per`
  // and `page` the way every page here does rather than inventing a second
  // ceiling. The drill-downs' session blocks start smaller, and the API
  // documents that number rather than repeating it: a document saying 50 beside
  // a service doing 5 is worse than a document that says nothing.
  DEFAULT_BLOCKS_PER_PAGE: DEFAULT_BLOCKS_PER_PAGE
};

export = consoleExports;
